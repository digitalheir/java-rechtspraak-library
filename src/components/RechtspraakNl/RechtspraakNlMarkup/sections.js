const rsSections = {
    xmlSchema: {
        page: '11',
        id: 'xml-schema',
        title: "XML Schema"
    },
};

rsSections.inOrder = [
    rsSections.xmlSchema,
];

export default rsSections;