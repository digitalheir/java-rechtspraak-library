const rsSections = {
    xmlSchema: {
        page: '10',
        id: 'xml-schema',
        title: "XML Schema"
    },
};

rsSections.inOrder = [
    rsSections.xmlSchema,
];

export default rsSections;