(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory();
	else if(typeof define === 'function' && define.amd)
		define([], factory);
	else {
		var a = factory();
		for(var i in a) (typeof exports === 'object' ? exports : root)[i] = a[i];
	}
})(this, function() {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};

/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {

/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;

/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};

/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);

/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;

/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}


/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;

/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;

/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";

/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _server = __webpack_require__(161);

	var _server2 = _interopRequireDefault(_server);

	var _reactRouter = __webpack_require__(162);

	var _reactRouter2 = _interopRequireDefault(_reactRouter);

	var _Routes = __webpack_require__(204);

	var _Routes2 = _interopRequireDefault(_Routes);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	if (typeof document !== 'undefined') {
	    var initialProps = JSON.parse(document.getElementById('initial-props').innerHTML);
	    _reactRouter2.default.run(_Routes2.default, _reactRouter2.default.HistoryLocation, function (Handler) {
	        _react2.default.render(_react2.default.createElement(Handler, initialProps), document);
	    });
	}

	var Entry = function render(locals, callback) {
	    _reactRouter2.default.run(_Routes2.default, locals.path, function (Handler) {
	        var html = _server2.default.renderToStaticMarkup(_react2.default.createElement(Handler, locals));
	        callback(null, '<!DOCTYPE html>' + html);
	    });
	};

	exports.default = Entry;

/***/ },
/* 1 */,
/* 2 */,
/* 3 */,
/* 4 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	module.exports = __webpack_require__(5);


/***/ },
/* 5 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule React
	 */

	'use strict';

	var ReactDOM = __webpack_require__(6);
	var ReactDOMServer = __webpack_require__(151);
	var ReactIsomorphic = __webpack_require__(155);

	var assign = __webpack_require__(42);
	var deprecated = __webpack_require__(160);

	// `version` will be added here by ReactIsomorphic.
	var React = {};

	assign(React, ReactIsomorphic);

	assign(React, {
	  // ReactDOM
	  findDOMNode: deprecated('findDOMNode', 'ReactDOM', 'react-dom', ReactDOM, ReactDOM.findDOMNode),
	  render: deprecated('render', 'ReactDOM', 'react-dom', ReactDOM, ReactDOM.render),
	  unmountComponentAtNode: deprecated('unmountComponentAtNode', 'ReactDOM', 'react-dom', ReactDOM, ReactDOM.unmountComponentAtNode),

	  // ReactDOMServer
	  renderToString: deprecated('renderToString', 'ReactDOMServer', 'react-dom/server', ReactDOMServer, ReactDOMServer.renderToString),
	  renderToStaticMarkup: deprecated('renderToStaticMarkup', 'ReactDOMServer', 'react-dom/server', ReactDOMServer, ReactDOMServer.renderToStaticMarkup)
	});

	React.__SECRET_DOM_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = ReactDOM;
	React.__SECRET_DOM_SERVER_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = ReactDOMServer;

	module.exports = React;

/***/ },
/* 6 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOM
	 */

	/* globals __REACT_DEVTOOLS_GLOBAL_HOOK__*/

	'use strict';

	var ReactCurrentOwner = __webpack_require__(8);
	var ReactDOMTextComponent = __webpack_require__(9);
	var ReactDefaultInjection = __webpack_require__(74);
	var ReactInstanceHandles = __webpack_require__(48);
	var ReactMount = __webpack_require__(31);
	var ReactPerf = __webpack_require__(21);
	var ReactReconciler = __webpack_require__(53);
	var ReactUpdates = __webpack_require__(57);
	var ReactVersion = __webpack_require__(149);

	var findDOMNode = __webpack_require__(94);
	var renderSubtreeIntoContainer = __webpack_require__(150);
	var warning = __webpack_require__(28);

	ReactDefaultInjection.inject();

	var render = ReactPerf.measure('React', 'render', ReactMount.render);

	var React = {
	  findDOMNode: findDOMNode,
	  render: render,
	  unmountComponentAtNode: ReactMount.unmountComponentAtNode,
	  version: ReactVersion,

	  /* eslint-disable camelcase */
	  unstable_batchedUpdates: ReactUpdates.batchedUpdates,
	  unstable_renderSubtreeIntoContainer: renderSubtreeIntoContainer
	};

	// Inject the runtime into a devtools global hook regardless of browser.
	// Allows for debugging when the hook is injected on the page.
	/* eslint-enable camelcase */
	if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== 'undefined' && typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.inject === 'function') {
	  __REACT_DEVTOOLS_GLOBAL_HOOK__.inject({
	    CurrentOwner: ReactCurrentOwner,
	    InstanceHandles: ReactInstanceHandles,
	    Mount: ReactMount,
	    Reconciler: ReactReconciler,
	    TextComponent: ReactDOMTextComponent
	  });
	}

	if (process.env.NODE_ENV !== 'production') {
	  var ExecutionEnvironment = __webpack_require__(12);
	  if (ExecutionEnvironment.canUseDOM && window.top === window.self) {

	    // First check if devtools is not installed
	    if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined') {
	      // If we're in Chrome or Firefox, provide a download link if not installed.
	      if (navigator.userAgent.indexOf('Chrome') > -1 && navigator.userAgent.indexOf('Edge') === -1 || navigator.userAgent.indexOf('Firefox') > -1) {
	        console.debug('Download the React DevTools for a better development experience: ' + 'https://fb.me/react-devtools');
	      }
	    }

	    // If we're in IE8, check to see if we are in compatibility mode and provide
	    // information on preventing compatibility mode
	    var ieCompatibilityMode = document.documentMode && document.documentMode < 8;

	    process.env.NODE_ENV !== 'production' ? warning(!ieCompatibilityMode, 'Internet Explorer is running in compatibility mode; please add the ' + 'following tag to your HTML to prevent this from happening: ' + '<meta http-equiv="X-UA-Compatible" content="IE=edge" />') : undefined;

	    var expectedFeatures = [
	    // shims
	    Array.isArray, Array.prototype.every, Array.prototype.forEach, Array.prototype.indexOf, Array.prototype.map, Date.now, Function.prototype.bind, Object.keys, String.prototype.split, String.prototype.trim,

	    // shams
	    Object.create, Object.freeze];

	    for (var i = 0; i < expectedFeatures.length; i++) {
	      if (!expectedFeatures[i]) {
	        console.error('One or more ES5 shim/shams expected by React are not available: ' + 'https://fb.me/react-warning-polyfills');
	        break;
	      }
	    }
	  }
	}

	module.exports = React;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 7 */
/***/ function(module, exports) {

	// shim for using process in browser

	var process = module.exports = {};
	var queue = [];
	var draining = false;
	var currentQueue;
	var queueIndex = -1;

	function cleanUpNextTick() {
	    draining = false;
	    if (currentQueue.length) {
	        queue = currentQueue.concat(queue);
	    } else {
	        queueIndex = -1;
	    }
	    if (queue.length) {
	        drainQueue();
	    }
	}

	function drainQueue() {
	    if (draining) {
	        return;
	    }
	    var timeout = setTimeout(cleanUpNextTick);
	    draining = true;

	    var len = queue.length;
	    while(len) {
	        currentQueue = queue;
	        queue = [];
	        while (++queueIndex < len) {
	            if (currentQueue) {
	                currentQueue[queueIndex].run();
	            }
	        }
	        queueIndex = -1;
	        len = queue.length;
	    }
	    currentQueue = null;
	    draining = false;
	    clearTimeout(timeout);
	}

	process.nextTick = function (fun) {
	    var args = new Array(arguments.length - 1);
	    if (arguments.length > 1) {
	        for (var i = 1; i < arguments.length; i++) {
	            args[i - 1] = arguments[i];
	        }
	    }
	    queue.push(new Item(fun, args));
	    if (queue.length === 1 && !draining) {
	        setTimeout(drainQueue, 0);
	    }
	};

	// v8 likes predictible objects
	function Item(fun, array) {
	    this.fun = fun;
	    this.array = array;
	}
	Item.prototype.run = function () {
	    this.fun.apply(null, this.array);
	};
	process.title = 'browser';
	process.browser = true;
	process.env = {};
	process.argv = [];
	process.version = ''; // empty string to avoid regexp issues
	process.versions = {};

	function noop() {}

	process.on = noop;
	process.addListener = noop;
	process.once = noop;
	process.off = noop;
	process.removeListener = noop;
	process.removeAllListeners = noop;
	process.emit = noop;

	process.binding = function (name) {
	    throw new Error('process.binding is not supported');
	};

	process.cwd = function () { return '/' };
	process.chdir = function (dir) {
	    throw new Error('process.chdir is not supported');
	};
	process.umask = function() { return 0; };


/***/ },
/* 8 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactCurrentOwner
	 */

	'use strict';

	/**
	 * Keeps track of the current owner.
	 *
	 * The current owner is the component who should own any components that are
	 * currently being constructed.
	 */
	var ReactCurrentOwner = {

	  /**
	   * @internal
	   * @type {ReactComponent}
	   */
	  current: null

	};

	module.exports = ReactCurrentOwner;

/***/ },
/* 9 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMTextComponent
	 * @typechecks static-only
	 */

	'use strict';

	var DOMChildrenOperations = __webpack_require__(10);
	var DOMPropertyOperations = __webpack_require__(25);
	var ReactComponentBrowserEnvironment = __webpack_require__(29);
	var ReactMount = __webpack_require__(31);

	var assign = __webpack_require__(42);
	var escapeTextContentForBrowser = __webpack_require__(24);
	var setTextContent = __webpack_require__(23);
	var validateDOMNesting = __webpack_require__(73);

	/**
	 * Text nodes violate a couple assumptions that React makes about components:
	 *
	 *  - When mounting text into the DOM, adjacent text nodes are merged.
	 *  - Text nodes cannot be assigned a React root ID.
	 *
	 * This component is used to wrap strings in elements so that they can undergo
	 * the same reconciliation that is applied to elements.
	 *
	 * TODO: Investigate representing React components in the DOM with text nodes.
	 *
	 * @class ReactDOMTextComponent
	 * @extends ReactComponent
	 * @internal
	 */
	var ReactDOMTextComponent = function (props) {
	  // This constructor and its argument is currently used by mocks.
	};

	assign(ReactDOMTextComponent.prototype, {

	  /**
	   * @param {ReactText} text
	   * @internal
	   */
	  construct: function (text) {
	    // TODO: This is really a ReactText (ReactNode), not a ReactElement
	    this._currentElement = text;
	    this._stringText = '' + text;

	    // Properties
	    this._rootNodeID = null;
	    this._mountIndex = 0;
	  },

	  /**
	   * Creates the markup for this text node. This node is not intended to have
	   * any features besides containing text content.
	   *
	   * @param {string} rootID DOM ID of the root node.
	   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
	   * @return {string} Markup for this text node.
	   * @internal
	   */
	  mountComponent: function (rootID, transaction, context) {
	    if (process.env.NODE_ENV !== 'production') {
	      if (context[validateDOMNesting.ancestorInfoContextKey]) {
	        validateDOMNesting('span', null, context[validateDOMNesting.ancestorInfoContextKey]);
	      }
	    }

	    this._rootNodeID = rootID;
	    if (transaction.useCreateElement) {
	      var ownerDocument = context[ReactMount.ownerDocumentContextKey];
	      var el = ownerDocument.createElement('span');
	      DOMPropertyOperations.setAttributeForID(el, rootID);
	      // Populate node cache
	      ReactMount.getID(el);
	      setTextContent(el, this._stringText);
	      return el;
	    } else {
	      var escapedText = escapeTextContentForBrowser(this._stringText);

	      if (transaction.renderToStaticMarkup) {
	        // Normally we'd wrap this in a `span` for the reasons stated above, but
	        // since this is a situation where React won't take over (static pages),
	        // we can simply return the text as it is.
	        return escapedText;
	      }

	      return '<span ' + DOMPropertyOperations.createMarkupForID(rootID) + '>' + escapedText + '</span>';
	    }
	  },

	  /**
	   * Updates this component by updating the text content.
	   *
	   * @param {ReactText} nextText The next text content
	   * @param {ReactReconcileTransaction} transaction
	   * @internal
	   */
	  receiveComponent: function (nextText, transaction) {
	    if (nextText !== this._currentElement) {
	      this._currentElement = nextText;
	      var nextStringText = '' + nextText;
	      if (nextStringText !== this._stringText) {
	        // TODO: Save this as pending props and use performUpdateIfNecessary
	        // and/or updateComponent to do the actual update for consistency with
	        // other component types?
	        this._stringText = nextStringText;
	        var node = ReactMount.getNode(this._rootNodeID);
	        DOMChildrenOperations.updateTextContent(node, nextStringText);
	      }
	    }
	  },

	  unmountComponent: function () {
	    ReactComponentBrowserEnvironment.unmountIDFromEnvironment(this._rootNodeID);
	  }

	});

	module.exports = ReactDOMTextComponent;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 10 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule DOMChildrenOperations
	 * @typechecks static-only
	 */

	'use strict';

	var Danger = __webpack_require__(11);
	var ReactMultiChildUpdateTypes = __webpack_require__(19);
	var ReactPerf = __webpack_require__(21);

	var setInnerHTML = __webpack_require__(22);
	var setTextContent = __webpack_require__(23);
	var invariant = __webpack_require__(16);

	/**
	 * Inserts `childNode` as a child of `parentNode` at the `index`.
	 *
	 * @param {DOMElement} parentNode Parent node in which to insert.
	 * @param {DOMElement} childNode Child node to insert.
	 * @param {number} index Index at which to insert the child.
	 * @internal
	 */
	function insertChildAt(parentNode, childNode, index) {
	  // By exploiting arrays returning `undefined` for an undefined index, we can
	  // rely exclusively on `insertBefore(node, null)` instead of also using
	  // `appendChild(node)`. However, using `undefined` is not allowed by all
	  // browsers so we must replace it with `null`.

	  // fix render order error in safari
	  // IE8 will throw error when index out of list size.
	  var beforeChild = index >= parentNode.childNodes.length ? null : parentNode.childNodes.item(index);

	  parentNode.insertBefore(childNode, beforeChild);
	}

	/**
	 * Operations for updating with DOM children.
	 */
	var DOMChildrenOperations = {

	  dangerouslyReplaceNodeWithMarkup: Danger.dangerouslyReplaceNodeWithMarkup,

	  updateTextContent: setTextContent,

	  /**
	   * Updates a component's children by processing a series of updates. The
	   * update configurations are each expected to have a `parentNode` property.
	   *
	   * @param {array<object>} updates List of update configurations.
	   * @param {array<string>} markupList List of markup strings.
	   * @internal
	   */
	  processUpdates: function (updates, markupList) {
	    var update;
	    // Mapping from parent IDs to initial child orderings.
	    var initialChildren = null;
	    // List of children that will be moved or removed.
	    var updatedChildren = null;

	    for (var i = 0; i < updates.length; i++) {
	      update = updates[i];
	      if (update.type === ReactMultiChildUpdateTypes.MOVE_EXISTING || update.type === ReactMultiChildUpdateTypes.REMOVE_NODE) {
	        var updatedIndex = update.fromIndex;
	        var updatedChild = update.parentNode.childNodes[updatedIndex];
	        var parentID = update.parentID;

	        !updatedChild ? process.env.NODE_ENV !== 'production' ? invariant(false, 'processUpdates(): Unable to find child %s of element. This ' + 'probably means the DOM was unexpectedly mutated (e.g., by the ' + 'browser), usually due to forgetting a <tbody> when using tables, ' + 'nesting tags like <form>, <p>, or <a>, or using non-SVG elements ' + 'in an <svg> parent. Try inspecting the child nodes of the element ' + 'with React ID `%s`.', updatedIndex, parentID) : invariant(false) : undefined;

	        initialChildren = initialChildren || {};
	        initialChildren[parentID] = initialChildren[parentID] || [];
	        initialChildren[parentID][updatedIndex] = updatedChild;

	        updatedChildren = updatedChildren || [];
	        updatedChildren.push(updatedChild);
	      }
	    }

	    var renderedMarkup;
	    // markupList is either a list of markup or just a list of elements
	    if (markupList.length && typeof markupList[0] === 'string') {
	      renderedMarkup = Danger.dangerouslyRenderMarkup(markupList);
	    } else {
	      renderedMarkup = markupList;
	    }

	    // Remove updated children first so that `toIndex` is consistent.
	    if (updatedChildren) {
	      for (var j = 0; j < updatedChildren.length; j++) {
	        updatedChildren[j].parentNode.removeChild(updatedChildren[j]);
	      }
	    }

	    for (var k = 0; k < updates.length; k++) {
	      update = updates[k];
	      switch (update.type) {
	        case ReactMultiChildUpdateTypes.INSERT_MARKUP:
	          insertChildAt(update.parentNode, renderedMarkup[update.markupIndex], update.toIndex);
	          break;
	        case ReactMultiChildUpdateTypes.MOVE_EXISTING:
	          insertChildAt(update.parentNode, initialChildren[update.parentID][update.fromIndex], update.toIndex);
	          break;
	        case ReactMultiChildUpdateTypes.SET_MARKUP:
	          setInnerHTML(update.parentNode, update.content);
	          break;
	        case ReactMultiChildUpdateTypes.TEXT_CONTENT:
	          setTextContent(update.parentNode, update.content);
	          break;
	        case ReactMultiChildUpdateTypes.REMOVE_NODE:
	          // Already removed by the for-loop above.
	          break;
	      }
	    }
	  }

	};

	ReactPerf.measureMethods(DOMChildrenOperations, 'DOMChildrenOperations', {
	  updateTextContent: 'updateTextContent'
	});

	module.exports = DOMChildrenOperations;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 11 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule Danger
	 * @typechecks static-only
	 */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(12);

	var createNodesFromMarkup = __webpack_require__(13);
	var emptyFunction = __webpack_require__(18);
	var getMarkupWrap = __webpack_require__(17);
	var invariant = __webpack_require__(16);

	var OPEN_TAG_NAME_EXP = /^(<[^ \/>]+)/;
	var RESULT_INDEX_ATTR = 'data-danger-index';

	/**
	 * Extracts the `nodeName` from a string of markup.
	 *
	 * NOTE: Extracting the `nodeName` does not require a regular expression match
	 * because we make assumptions about React-generated markup (i.e. there are no
	 * spaces surrounding the opening tag and there is at least one attribute).
	 *
	 * @param {string} markup String of markup.
	 * @return {string} Node name of the supplied markup.
	 * @see http://jsperf.com/extract-nodename
	 */
	function getNodeName(markup) {
	  return markup.substring(1, markup.indexOf(' '));
	}

	var Danger = {

	  /**
	   * Renders markup into an array of nodes. The markup is expected to render
	   * into a list of root nodes. Also, the length of `resultList` and
	   * `markupList` should be the same.
	   *
	   * @param {array<string>} markupList List of markup strings to render.
	   * @return {array<DOMElement>} List of rendered nodes.
	   * @internal
	   */
	  dangerouslyRenderMarkup: function (markupList) {
	    !ExecutionEnvironment.canUseDOM ? process.env.NODE_ENV !== 'production' ? invariant(false, 'dangerouslyRenderMarkup(...): Cannot render markup in a worker ' + 'thread. Make sure `window` and `document` are available globally ' + 'before requiring React when unit testing or use ' + 'ReactDOMServer.renderToString for server rendering.') : invariant(false) : undefined;
	    var nodeName;
	    var markupByNodeName = {};
	    // Group markup by `nodeName` if a wrap is necessary, else by '*'.
	    for (var i = 0; i < markupList.length; i++) {
	      !markupList[i] ? process.env.NODE_ENV !== 'production' ? invariant(false, 'dangerouslyRenderMarkup(...): Missing markup.') : invariant(false) : undefined;
	      nodeName = getNodeName(markupList[i]);
	      nodeName = getMarkupWrap(nodeName) ? nodeName : '*';
	      markupByNodeName[nodeName] = markupByNodeName[nodeName] || [];
	      markupByNodeName[nodeName][i] = markupList[i];
	    }
	    var resultList = [];
	    var resultListAssignmentCount = 0;
	    for (nodeName in markupByNodeName) {
	      if (!markupByNodeName.hasOwnProperty(nodeName)) {
	        continue;
	      }
	      var markupListByNodeName = markupByNodeName[nodeName];

	      // This for-in loop skips the holes of the sparse array. The order of
	      // iteration should follow the order of assignment, which happens to match
	      // numerical index order, but we don't rely on that.
	      var resultIndex;
	      for (resultIndex in markupListByNodeName) {
	        if (markupListByNodeName.hasOwnProperty(resultIndex)) {
	          var markup = markupListByNodeName[resultIndex];

	          // Push the requested markup with an additional RESULT_INDEX_ATTR
	          // attribute.  If the markup does not start with a < character, it
	          // will be discarded below (with an appropriate console.error).
	          markupListByNodeName[resultIndex] = markup.replace(OPEN_TAG_NAME_EXP,
	          // This index will be parsed back out below.
	          '$1 ' + RESULT_INDEX_ATTR + '="' + resultIndex + '" ');
	        }
	      }

	      // Render each group of markup with similar wrapping `nodeName`.
	      var renderNodes = createNodesFromMarkup(markupListByNodeName.join(''), emptyFunction // Do nothing special with <script> tags.
	      );

	      for (var j = 0; j < renderNodes.length; ++j) {
	        var renderNode = renderNodes[j];
	        if (renderNode.hasAttribute && renderNode.hasAttribute(RESULT_INDEX_ATTR)) {

	          resultIndex = +renderNode.getAttribute(RESULT_INDEX_ATTR);
	          renderNode.removeAttribute(RESULT_INDEX_ATTR);

	          !!resultList.hasOwnProperty(resultIndex) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Danger: Assigning to an already-occupied result index.') : invariant(false) : undefined;

	          resultList[resultIndex] = renderNode;

	          // This should match resultList.length and markupList.length when
	          // we're done.
	          resultListAssignmentCount += 1;
	        } else if (process.env.NODE_ENV !== 'production') {
	          console.error('Danger: Discarding unexpected node:', renderNode);
	        }
	      }
	    }

	    // Although resultList was populated out of order, it should now be a dense
	    // array.
	    !(resultListAssignmentCount === resultList.length) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Danger: Did not assign to every index of resultList.') : invariant(false) : undefined;

	    !(resultList.length === markupList.length) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Danger: Expected markup to render %s nodes, but rendered %s.', markupList.length, resultList.length) : invariant(false) : undefined;

	    return resultList;
	  },

	  /**
	   * Replaces a node with a string of markup at its current position within its
	   * parent. The markup must render into a single root node.
	   *
	   * @param {DOMElement} oldChild Child node to replace.
	   * @param {string} markup Markup to render in place of the child node.
	   * @internal
	   */
	  dangerouslyReplaceNodeWithMarkup: function (oldChild, markup) {
	    !ExecutionEnvironment.canUseDOM ? process.env.NODE_ENV !== 'production' ? invariant(false, 'dangerouslyReplaceNodeWithMarkup(...): Cannot render markup in a ' + 'worker thread. Make sure `window` and `document` are available ' + 'globally before requiring React when unit testing or use ' + 'ReactDOMServer.renderToString() for server rendering.') : invariant(false) : undefined;
	    !markup ? process.env.NODE_ENV !== 'production' ? invariant(false, 'dangerouslyReplaceNodeWithMarkup(...): Missing markup.') : invariant(false) : undefined;
	    !(oldChild.tagName.toLowerCase() !== 'html') ? process.env.NODE_ENV !== 'production' ? invariant(false, 'dangerouslyReplaceNodeWithMarkup(...): Cannot replace markup of the ' + '<html> node. This is because browser quirks make this unreliable ' + 'and/or slow. If you want to render to the root you must use ' + 'server rendering. See ReactDOMServer.renderToString().') : invariant(false) : undefined;

	    var newChild;
	    if (typeof markup === 'string') {
	      newChild = createNodesFromMarkup(markup, emptyFunction)[0];
	    } else {
	      newChild = markup;
	    }
	    oldChild.parentNode.replaceChild(newChild, oldChild);
	  }

	};

	module.exports = Danger;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 12 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ExecutionEnvironment
	 */

	'use strict';

	var canUseDOM = !!(typeof window !== 'undefined' && window.document && window.document.createElement);

	/**
	 * Simple, lightweight module assisting with the detection and context of
	 * Worker. Helps avoid circular dependencies and allows code to reason about
	 * whether or not they are in a Worker, even if they never include the main
	 * `ReactWorker` dependency.
	 */
	var ExecutionEnvironment = {

	  canUseDOM: canUseDOM,

	  canUseWorkers: typeof Worker !== 'undefined',

	  canUseEventListeners: canUseDOM && !!(window.addEventListener || window.attachEvent),

	  canUseViewport: canUseDOM && !!window.screen,

	  isInWorker: !canUseDOM // For now, this is true - might change in the future.

	};

	module.exports = ExecutionEnvironment;

/***/ },
/* 13 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule createNodesFromMarkup
	 * @typechecks
	 */

	/*eslint-disable fb-www/unsafe-html*/

	'use strict';

	var ExecutionEnvironment = __webpack_require__(12);

	var createArrayFromMixed = __webpack_require__(14);
	var getMarkupWrap = __webpack_require__(17);
	var invariant = __webpack_require__(16);

	/**
	 * Dummy container used to render all markup.
	 */
	var dummyNode = ExecutionEnvironment.canUseDOM ? document.createElement('div') : null;

	/**
	 * Pattern used by `getNodeName`.
	 */
	var nodeNamePattern = /^\s*<(\w+)/;

	/**
	 * Extracts the `nodeName` of the first element in a string of markup.
	 *
	 * @param {string} markup String of markup.
	 * @return {?string} Node name of the supplied markup.
	 */
	function getNodeName(markup) {
	  var nodeNameMatch = markup.match(nodeNamePattern);
	  return nodeNameMatch && nodeNameMatch[1].toLowerCase();
	}

	/**
	 * Creates an array containing the nodes rendered from the supplied markup. The
	 * optionally supplied `handleScript` function will be invoked once for each
	 * <script> element that is rendered. If no `handleScript` function is supplied,
	 * an exception is thrown if any <script> elements are rendered.
	 *
	 * @param {string} markup A string of valid HTML markup.
	 * @param {?function} handleScript Invoked once for each rendered <script>.
	 * @return {array<DOMElement|DOMTextNode>} An array of rendered nodes.
	 */
	function createNodesFromMarkup(markup, handleScript) {
	  var node = dummyNode;
	  !!!dummyNode ? process.env.NODE_ENV !== 'production' ? invariant(false, 'createNodesFromMarkup dummy not initialized') : invariant(false) : undefined;
	  var nodeName = getNodeName(markup);

	  var wrap = nodeName && getMarkupWrap(nodeName);
	  if (wrap) {
	    node.innerHTML = wrap[1] + markup + wrap[2];

	    var wrapDepth = wrap[0];
	    while (wrapDepth--) {
	      node = node.lastChild;
	    }
	  } else {
	    node.innerHTML = markup;
	  }

	  var scripts = node.getElementsByTagName('script');
	  if (scripts.length) {
	    !handleScript ? process.env.NODE_ENV !== 'production' ? invariant(false, 'createNodesFromMarkup(...): Unexpected <script> element rendered.') : invariant(false) : undefined;
	    createArrayFromMixed(scripts).forEach(handleScript);
	  }

	  var nodes = createArrayFromMixed(node.childNodes);
	  while (node.lastChild) {
	    node.removeChild(node.lastChild);
	  }
	  return nodes;
	}

	module.exports = createNodesFromMarkup;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 14 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule createArrayFromMixed
	 * @typechecks
	 */

	'use strict';

	var toArray = __webpack_require__(15);

	/**
	 * Perform a heuristic test to determine if an object is "array-like".
	 *
	 *   A monk asked Joshu, a Zen master, "Has a dog Buddha nature?"
	 *   Joshu replied: "Mu."
	 *
	 * This function determines if its argument has "array nature": it returns
	 * true if the argument is an actual array, an `arguments' object, or an
	 * HTMLCollection (e.g. node.childNodes or node.getElementsByTagName()).
	 *
	 * It will return false for other array-like objects like Filelist.
	 *
	 * @param {*} obj
	 * @return {boolean}
	 */
	function hasArrayNature(obj) {
	  return(
	    // not null/false
	    !!obj && (
	    // arrays are objects, NodeLists are functions in Safari
	    typeof obj == 'object' || typeof obj == 'function') &&
	    // quacks like an array
	    'length' in obj &&
	    // not window
	    !('setInterval' in obj) &&
	    // no DOM node should be considered an array-like
	    // a 'select' element has 'length' and 'item' properties on IE8
	    typeof obj.nodeType != 'number' && (
	    // a real array
	    Array.isArray(obj) ||
	    // arguments
	    'callee' in obj ||
	    // HTMLCollection/NodeList
	    'item' in obj)
	  );
	}

	/**
	 * Ensure that the argument is an array by wrapping it in an array if it is not.
	 * Creates a copy of the argument if it is already an array.
	 *
	 * This is mostly useful idiomatically:
	 *
	 *   var createArrayFromMixed = require('createArrayFromMixed');
	 *
	 *   function takesOneOrMoreThings(things) {
	 *     things = createArrayFromMixed(things);
	 *     ...
	 *   }
	 *
	 * This allows you to treat `things' as an array, but accept scalars in the API.
	 *
	 * If you need to convert an array-like object, like `arguments`, into an array
	 * use toArray instead.
	 *
	 * @param {*} obj
	 * @return {array}
	 */
	function createArrayFromMixed(obj) {
	  if (!hasArrayNature(obj)) {
	    return [obj];
	  } else if (Array.isArray(obj)) {
	    return obj.slice();
	  } else {
	    return toArray(obj);
	  }
	}

	module.exports = createArrayFromMixed;

/***/ },
/* 15 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule toArray
	 * @typechecks
	 */

	'use strict';

	var invariant = __webpack_require__(16);

	/**
	 * Convert array-like objects to arrays.
	 *
	 * This API assumes the caller knows the contents of the data type. For less
	 * well defined inputs use createArrayFromMixed.
	 *
	 * @param {object|function|filelist} obj
	 * @return {array}
	 */
	function toArray(obj) {
	  var length = obj.length;

	  // Some browse builtin objects can report typeof 'function' (e.g. NodeList in
	  // old versions of Safari).
	  !(!Array.isArray(obj) && (typeof obj === 'object' || typeof obj === 'function')) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'toArray: Array-like object expected') : invariant(false) : undefined;

	  !(typeof length === 'number') ? process.env.NODE_ENV !== 'production' ? invariant(false, 'toArray: Object needs a length property') : invariant(false) : undefined;

	  !(length === 0 || length - 1 in obj) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'toArray: Object should have keys for indices') : invariant(false) : undefined;

	  // Old IE doesn't give collections access to hasOwnProperty. Assume inputs
	  // without method will throw during the slice call and skip straight to the
	  // fallback.
	  if (obj.hasOwnProperty) {
	    try {
	      return Array.prototype.slice.call(obj);
	    } catch (e) {
	      // IE < 9 does not support Array#slice on collections objects
	    }
	  }

	  // Fall back to copying key by key. This assumes all keys have a value,
	  // so will not preserve sparsely populated inputs.
	  var ret = Array(length);
	  for (var ii = 0; ii < length; ii++) {
	    ret[ii] = obj[ii];
	  }
	  return ret;
	}

	module.exports = toArray;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 16 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule invariant
	 */

	'use strict';

	/**
	 * Use invariant() to assert state which your program assumes to be true.
	 *
	 * Provide sprintf-style format (only %s is supported) and arguments
	 * to provide information about what broke and what you were
	 * expecting.
	 *
	 * The invariant message will be stripped in production, but the invariant
	 * will remain to ensure logic does not differ in production.
	 */

	function invariant(condition, format, a, b, c, d, e, f) {
	  if (process.env.NODE_ENV !== 'production') {
	    if (format === undefined) {
	      throw new Error('invariant requires an error message argument');
	    }
	  }

	  if (!condition) {
	    var error;
	    if (format === undefined) {
	      error = new Error('Minified exception occurred; use the non-minified dev environment ' + 'for the full error message and additional helpful warnings.');
	    } else {
	      var args = [a, b, c, d, e, f];
	      var argIndex = 0;
	      error = new Error(format.replace(/%s/g, function () {
	        return args[argIndex++];
	      }));
	      error.name = 'Invariant Violation';
	    }

	    error.framesToPop = 1; // we don't care about invariant's own frame
	    throw error;
	  }
	}

	module.exports = invariant;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 17 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getMarkupWrap
	 */

	/*eslint-disable fb-www/unsafe-html */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(12);

	var invariant = __webpack_require__(16);

	/**
	 * Dummy container used to detect which wraps are necessary.
	 */
	var dummyNode = ExecutionEnvironment.canUseDOM ? document.createElement('div') : null;

	/**
	 * Some browsers cannot use `innerHTML` to render certain elements standalone,
	 * so we wrap them, render the wrapped nodes, then extract the desired node.
	 *
	 * In IE8, certain elements cannot render alone, so wrap all elements ('*').
	 */

	var shouldWrap = {};

	var selectWrap = [1, '<select multiple="true">', '</select>'];
	var tableWrap = [1, '<table>', '</table>'];
	var trWrap = [3, '<table><tbody><tr>', '</tr></tbody></table>'];

	var svgWrap = [1, '<svg xmlns="http://www.w3.org/2000/svg">', '</svg>'];

	var markupWrap = {
	  '*': [1, '?<div>', '</div>'],

	  'area': [1, '<map>', '</map>'],
	  'col': [2, '<table><tbody></tbody><colgroup>', '</colgroup></table>'],
	  'legend': [1, '<fieldset>', '</fieldset>'],
	  'param': [1, '<object>', '</object>'],
	  'tr': [2, '<table><tbody>', '</tbody></table>'],

	  'optgroup': selectWrap,
	  'option': selectWrap,

	  'caption': tableWrap,
	  'colgroup': tableWrap,
	  'tbody': tableWrap,
	  'tfoot': tableWrap,
	  'thead': tableWrap,

	  'td': trWrap,
	  'th': trWrap
	};

	// Initialize the SVG elements since we know they'll always need to be wrapped
	// consistently. If they are created inside a <div> they will be initialized in
	// the wrong namespace (and will not display).
	var svgElements = ['circle', 'clipPath', 'defs', 'ellipse', 'g', 'image', 'line', 'linearGradient', 'mask', 'path', 'pattern', 'polygon', 'polyline', 'radialGradient', 'rect', 'stop', 'text', 'tspan'];
	svgElements.forEach(function (nodeName) {
	  markupWrap[nodeName] = svgWrap;
	  shouldWrap[nodeName] = true;
	});

	/**
	 * Gets the markup wrap configuration for the supplied `nodeName`.
	 *
	 * NOTE: This lazily detects which wraps are necessary for the current browser.
	 *
	 * @param {string} nodeName Lowercase `nodeName`.
	 * @return {?array} Markup wrap configuration, if applicable.
	 */
	function getMarkupWrap(nodeName) {
	  !!!dummyNode ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Markup wrapping node not initialized') : invariant(false) : undefined;
	  if (!markupWrap.hasOwnProperty(nodeName)) {
	    nodeName = '*';
	  }
	  if (!shouldWrap.hasOwnProperty(nodeName)) {
	    if (nodeName === '*') {
	      dummyNode.innerHTML = '<link />';
	    } else {
	      dummyNode.innerHTML = '<' + nodeName + '></' + nodeName + '>';
	    }
	    shouldWrap[nodeName] = !dummyNode.firstChild;
	  }
	  return shouldWrap[nodeName] ? markupWrap[nodeName] : null;
	}

	module.exports = getMarkupWrap;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 18 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule emptyFunction
	 */

	"use strict";

	function makeEmptyFunction(arg) {
	  return function () {
	    return arg;
	  };
	}

	/**
	 * This function accepts and discards inputs; it has no side effects. This is
	 * primarily useful idiomatically for overridable function endpoints which
	 * always need to be callable, since JS lacks a null-call idiom ala Cocoa.
	 */
	function emptyFunction() {}

	emptyFunction.thatReturns = makeEmptyFunction;
	emptyFunction.thatReturnsFalse = makeEmptyFunction(false);
	emptyFunction.thatReturnsTrue = makeEmptyFunction(true);
	emptyFunction.thatReturnsNull = makeEmptyFunction(null);
	emptyFunction.thatReturnsThis = function () {
	  return this;
	};
	emptyFunction.thatReturnsArgument = function (arg) {
	  return arg;
	};

	module.exports = emptyFunction;

/***/ },
/* 19 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactMultiChildUpdateTypes
	 */

	'use strict';

	var keyMirror = __webpack_require__(20);

	/**
	 * When a component's children are updated, a series of update configuration
	 * objects are created in order to batch and serialize the required changes.
	 *
	 * Enumerates all the possible types of update configurations.
	 *
	 * @internal
	 */
	var ReactMultiChildUpdateTypes = keyMirror({
	  INSERT_MARKUP: null,
	  MOVE_EXISTING: null,
	  REMOVE_NODE: null,
	  SET_MARKUP: null,
	  TEXT_CONTENT: null
	});

	module.exports = ReactMultiChildUpdateTypes;

/***/ },
/* 20 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule keyMirror
	 * @typechecks static-only
	 */

	'use strict';

	var invariant = __webpack_require__(16);

	/**
	 * Constructs an enumeration with keys equal to their value.
	 *
	 * For example:
	 *
	 *   var COLORS = keyMirror({blue: null, red: null});
	 *   var myColor = COLORS.blue;
	 *   var isColorValid = !!COLORS[myColor];
	 *
	 * The last line could not be performed if the values of the generated enum were
	 * not equal to their keys.
	 *
	 *   Input:  {key1: val1, key2: val2}
	 *   Output: {key1: key1, key2: key2}
	 *
	 * @param {object} obj
	 * @return {object}
	 */
	var keyMirror = function (obj) {
	  var ret = {};
	  var key;
	  !(obj instanceof Object && !Array.isArray(obj)) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'keyMirror(...): Argument must be an object.') : invariant(false) : undefined;
	  for (key in obj) {
	    if (!obj.hasOwnProperty(key)) {
	      continue;
	    }
	    ret[key] = key;
	  }
	  return ret;
	};

	module.exports = keyMirror;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 21 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactPerf
	 * @typechecks static-only
	 */

	'use strict';

	/**
	 * ReactPerf is a general AOP system designed to measure performance. This
	 * module only has the hooks: see ReactDefaultPerf for the analysis tool.
	 */
	var ReactPerf = {
	  /**
	   * Boolean to enable/disable measurement. Set to false by default to prevent
	   * accidental logging and perf loss.
	   */
	  enableMeasure: false,

	  /**
	   * Holds onto the measure function in use. By default, don't measure
	   * anything, but we'll override this if we inject a measure function.
	   */
	  storedMeasure: _noMeasure,

	  /**
	   * @param {object} object
	   * @param {string} objectName
	   * @param {object<string>} methodNames
	   */
	  measureMethods: function (object, objectName, methodNames) {
	    if (process.env.NODE_ENV !== 'production') {
	      for (var key in methodNames) {
	        if (!methodNames.hasOwnProperty(key)) {
	          continue;
	        }
	        object[key] = ReactPerf.measure(objectName, methodNames[key], object[key]);
	      }
	    }
	  },

	  /**
	   * Use this to wrap methods you want to measure. Zero overhead in production.
	   *
	   * @param {string} objName
	   * @param {string} fnName
	   * @param {function} func
	   * @return {function}
	   */
	  measure: function (objName, fnName, func) {
	    if (process.env.NODE_ENV !== 'production') {
	      var measuredFunc = null;
	      var wrapper = function () {
	        if (ReactPerf.enableMeasure) {
	          if (!measuredFunc) {
	            measuredFunc = ReactPerf.storedMeasure(objName, fnName, func);
	          }
	          return measuredFunc.apply(this, arguments);
	        }
	        return func.apply(this, arguments);
	      };
	      wrapper.displayName = objName + '_' + fnName;
	      return wrapper;
	    }
	    return func;
	  },

	  injection: {
	    /**
	     * @param {function} measure
	     */
	    injectMeasure: function (measure) {
	      ReactPerf.storedMeasure = measure;
	    }
	  }
	};

	/**
	 * Simply passes through the measured function, without measuring it.
	 *
	 * @param {string} objName
	 * @param {string} fnName
	 * @param {function} func
	 * @return {function}
	 */
	function _noMeasure(objName, fnName, func) {
	  return func;
	}

	module.exports = ReactPerf;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 22 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule setInnerHTML
	 */

	/* globals MSApp */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(12);

	var WHITESPACE_TEST = /^[ \r\n\t\f]/;
	var NONVISIBLE_TEST = /<(!--|link|noscript|meta|script|style)[ \r\n\t\f\/>]/;

	/**
	 * Set the innerHTML property of a node, ensuring that whitespace is preserved
	 * even in IE8.
	 *
	 * @param {DOMElement} node
	 * @param {string} html
	 * @internal
	 */
	var setInnerHTML = function (node, html) {
	  node.innerHTML = html;
	};

	// Win8 apps: Allow all html to be inserted
	if (typeof MSApp !== 'undefined' && MSApp.execUnsafeLocalFunction) {
	  setInnerHTML = function (node, html) {
	    MSApp.execUnsafeLocalFunction(function () {
	      node.innerHTML = html;
	    });
	  };
	}

	if (ExecutionEnvironment.canUseDOM) {
	  // IE8: When updating a just created node with innerHTML only leading
	  // whitespace is removed. When updating an existing node with innerHTML
	  // whitespace in root TextNodes is also collapsed.
	  // @see quirksmode.org/bugreports/archives/2004/11/innerhtml_and_t.html

	  // Feature detection; only IE8 is known to behave improperly like this.
	  var testElement = document.createElement('div');
	  testElement.innerHTML = ' ';
	  if (testElement.innerHTML === '') {
	    setInnerHTML = function (node, html) {
	      // Magic theory: IE8 supposedly differentiates between added and updated
	      // nodes when processing innerHTML, innerHTML on updated nodes suffers
	      // from worse whitespace behavior. Re-adding a node like this triggers
	      // the initial and more favorable whitespace behavior.
	      // TODO: What to do on a detached node?
	      if (node.parentNode) {
	        node.parentNode.replaceChild(node, node);
	      }

	      // We also implement a workaround for non-visible tags disappearing into
	      // thin air on IE8, this only happens if there is no visible text
	      // in-front of the non-visible tags. Piggyback on the whitespace fix
	      // and simply check if any non-visible tags appear in the source.
	      if (WHITESPACE_TEST.test(html) || html[0] === '<' && NONVISIBLE_TEST.test(html)) {
	        // Recover leading whitespace by temporarily prepending any character.
	        // \uFEFF has the potential advantage of being zero-width/invisible.
	        // UglifyJS drops U+FEFF chars when parsing, so use String.fromCharCode
	        // in hopes that this is preserved even if "\uFEFF" is transformed to
	        // the actual Unicode character (by Babel, for example).
	        // https://github.com/mishoo/UglifyJS2/blob/v2.4.20/lib/parse.js#L216
	        node.innerHTML = String.fromCharCode(0xFEFF) + html;

	        // deleteData leaves an empty `TextNode` which offsets the index of all
	        // children. Definitely want to avoid this.
	        var textNode = node.firstChild;
	        if (textNode.data.length === 1) {
	          node.removeChild(textNode);
	        } else {
	          textNode.deleteData(0, 1);
	        }
	      } else {
	        node.innerHTML = html;
	      }
	    };
	  }
	}

	module.exports = setInnerHTML;

/***/ },
/* 23 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule setTextContent
	 */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(12);
	var escapeTextContentForBrowser = __webpack_require__(24);
	var setInnerHTML = __webpack_require__(22);

	/**
	 * Set the textContent property of a node, ensuring that whitespace is preserved
	 * even in IE8. innerText is a poor substitute for textContent and, among many
	 * issues, inserts <br> instead of the literal newline chars. innerHTML behaves
	 * as it should.
	 *
	 * @param {DOMElement} node
	 * @param {string} text
	 * @internal
	 */
	var setTextContent = function (node, text) {
	  node.textContent = text;
	};

	if (ExecutionEnvironment.canUseDOM) {
	  if (!('textContent' in document.documentElement)) {
	    setTextContent = function (node, text) {
	      setInnerHTML(node, escapeTextContentForBrowser(text));
	    };
	  }
	}

	module.exports = setTextContent;

/***/ },
/* 24 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule escapeTextContentForBrowser
	 */

	'use strict';

	var ESCAPE_LOOKUP = {
	  '&': '&amp;',
	  '>': '&gt;',
	  '<': '&lt;',
	  '"': '&quot;',
	  '\'': '&#x27;'
	};

	var ESCAPE_REGEX = /[&><"']/g;

	function escaper(match) {
	  return ESCAPE_LOOKUP[match];
	}

	/**
	 * Escapes text to prevent scripting attacks.
	 *
	 * @param {*} text Text value to escape.
	 * @return {string} An escaped string.
	 */
	function escapeTextContentForBrowser(text) {
	  return ('' + text).replace(ESCAPE_REGEX, escaper);
	}

	module.exports = escapeTextContentForBrowser;

/***/ },
/* 25 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule DOMPropertyOperations
	 * @typechecks static-only
	 */

	'use strict';

	var DOMProperty = __webpack_require__(26);
	var ReactPerf = __webpack_require__(21);

	var quoteAttributeValueForBrowser = __webpack_require__(27);
	var warning = __webpack_require__(28);

	// Simplified subset
	var VALID_ATTRIBUTE_NAME_REGEX = /^[a-zA-Z_][\w\.\-]*$/;
	var illegalAttributeNameCache = {};
	var validatedAttributeNameCache = {};

	function isAttributeNameSafe(attributeName) {
	  if (validatedAttributeNameCache.hasOwnProperty(attributeName)) {
	    return true;
	  }
	  if (illegalAttributeNameCache.hasOwnProperty(attributeName)) {
	    return false;
	  }
	  if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName)) {
	    validatedAttributeNameCache[attributeName] = true;
	    return true;
	  }
	  illegalAttributeNameCache[attributeName] = true;
	  process.env.NODE_ENV !== 'production' ? warning(false, 'Invalid attribute name: `%s`', attributeName) : undefined;
	  return false;
	}

	function shouldIgnoreValue(propertyInfo, value) {
	  return value == null || propertyInfo.hasBooleanValue && !value || propertyInfo.hasNumericValue && isNaN(value) || propertyInfo.hasPositiveNumericValue && value < 1 || propertyInfo.hasOverloadedBooleanValue && value === false;
	}

	if (process.env.NODE_ENV !== 'production') {
	  var reactProps = {
	    children: true,
	    dangerouslySetInnerHTML: true,
	    key: true,
	    ref: true
	  };
	  var warnedProperties = {};

	  var warnUnknownProperty = function (name) {
	    if (reactProps.hasOwnProperty(name) && reactProps[name] || warnedProperties.hasOwnProperty(name) && warnedProperties[name]) {
	      return;
	    }

	    warnedProperties[name] = true;
	    var lowerCasedName = name.toLowerCase();

	    // data-* attributes should be lowercase; suggest the lowercase version
	    var standardName = DOMProperty.isCustomAttribute(lowerCasedName) ? lowerCasedName : DOMProperty.getPossibleStandardName.hasOwnProperty(lowerCasedName) ? DOMProperty.getPossibleStandardName[lowerCasedName] : null;

	    // For now, only warn when we have a suggested correction. This prevents
	    // logging too much when using transferPropsTo.
	    process.env.NODE_ENV !== 'production' ? warning(standardName == null, 'Unknown DOM property %s. Did you mean %s?', name, standardName) : undefined;
	  };
	}

	/**
	 * Operations for dealing with DOM properties.
	 */
	var DOMPropertyOperations = {

	  /**
	   * Creates markup for the ID property.
	   *
	   * @param {string} id Unescaped ID.
	   * @return {string} Markup string.
	   */
	  createMarkupForID: function (id) {
	    return DOMProperty.ID_ATTRIBUTE_NAME + '=' + quoteAttributeValueForBrowser(id);
	  },

	  setAttributeForID: function (node, id) {
	    node.setAttribute(DOMProperty.ID_ATTRIBUTE_NAME, id);
	  },

	  /**
	   * Creates markup for a property.
	   *
	   * @param {string} name
	   * @param {*} value
	   * @return {?string} Markup string, or null if the property was invalid.
	   */
	  createMarkupForProperty: function (name, value) {
	    var propertyInfo = DOMProperty.properties.hasOwnProperty(name) ? DOMProperty.properties[name] : null;
	    if (propertyInfo) {
	      if (shouldIgnoreValue(propertyInfo, value)) {
	        return '';
	      }
	      var attributeName = propertyInfo.attributeName;
	      if (propertyInfo.hasBooleanValue || propertyInfo.hasOverloadedBooleanValue && value === true) {
	        return attributeName + '=""';
	      }
	      return attributeName + '=' + quoteAttributeValueForBrowser(value);
	    } else if (DOMProperty.isCustomAttribute(name)) {
	      if (value == null) {
	        return '';
	      }
	      return name + '=' + quoteAttributeValueForBrowser(value);
	    } else if (process.env.NODE_ENV !== 'production') {
	      warnUnknownProperty(name);
	    }
	    return null;
	  },

	  /**
	   * Creates markup for a custom property.
	   *
	   * @param {string} name
	   * @param {*} value
	   * @return {string} Markup string, or empty string if the property was invalid.
	   */
	  createMarkupForCustomAttribute: function (name, value) {
	    if (!isAttributeNameSafe(name) || value == null) {
	      return '';
	    }
	    return name + '=' + quoteAttributeValueForBrowser(value);
	  },

	  /**
	   * Sets the value for a property on a node.
	   *
	   * @param {DOMElement} node
	   * @param {string} name
	   * @param {*} value
	   */
	  setValueForProperty: function (node, name, value) {
	    var propertyInfo = DOMProperty.properties.hasOwnProperty(name) ? DOMProperty.properties[name] : null;
	    if (propertyInfo) {
	      var mutationMethod = propertyInfo.mutationMethod;
	      if (mutationMethod) {
	        mutationMethod(node, value);
	      } else if (shouldIgnoreValue(propertyInfo, value)) {
	        this.deleteValueForProperty(node, name);
	      } else if (propertyInfo.mustUseAttribute) {
	        var attributeName = propertyInfo.attributeName;
	        var namespace = propertyInfo.attributeNamespace;
	        // `setAttribute` with objects becomes only `[object]` in IE8/9,
	        // ('' + value) makes it output the correct toString()-value.
	        if (namespace) {
	          node.setAttributeNS(namespace, attributeName, '' + value);
	        } else if (propertyInfo.hasBooleanValue || propertyInfo.hasOverloadedBooleanValue && value === true) {
	          node.setAttribute(attributeName, '');
	        } else {
	          node.setAttribute(attributeName, '' + value);
	        }
	      } else {
	        var propName = propertyInfo.propertyName;
	        // Must explicitly cast values for HAS_SIDE_EFFECTS-properties to the
	        // property type before comparing; only `value` does and is string.
	        if (!propertyInfo.hasSideEffects || '' + node[propName] !== '' + value) {
	          // Contrary to `setAttribute`, object properties are properly
	          // `toString`ed by IE8/9.
	          node[propName] = value;
	        }
	      }
	    } else if (DOMProperty.isCustomAttribute(name)) {
	      DOMPropertyOperations.setValueForAttribute(node, name, value);
	    } else if (process.env.NODE_ENV !== 'production') {
	      warnUnknownProperty(name);
	    }
	  },

	  setValueForAttribute: function (node, name, value) {
	    if (!isAttributeNameSafe(name)) {
	      return;
	    }
	    if (value == null) {
	      node.removeAttribute(name);
	    } else {
	      node.setAttribute(name, '' + value);
	    }
	  },

	  /**
	   * Deletes the value for a property on a node.
	   *
	   * @param {DOMElement} node
	   * @param {string} name
	   */
	  deleteValueForProperty: function (node, name) {
	    var propertyInfo = DOMProperty.properties.hasOwnProperty(name) ? DOMProperty.properties[name] : null;
	    if (propertyInfo) {
	      var mutationMethod = propertyInfo.mutationMethod;
	      if (mutationMethod) {
	        mutationMethod(node, undefined);
	      } else if (propertyInfo.mustUseAttribute) {
	        node.removeAttribute(propertyInfo.attributeName);
	      } else {
	        var propName = propertyInfo.propertyName;
	        var defaultValue = DOMProperty.getDefaultValueForProperty(node.nodeName, propName);
	        if (!propertyInfo.hasSideEffects || '' + node[propName] !== defaultValue) {
	          node[propName] = defaultValue;
	        }
	      }
	    } else if (DOMProperty.isCustomAttribute(name)) {
	      node.removeAttribute(name);
	    } else if (process.env.NODE_ENV !== 'production') {
	      warnUnknownProperty(name);
	    }
	  }

	};

	ReactPerf.measureMethods(DOMPropertyOperations, 'DOMPropertyOperations', {
	  setValueForProperty: 'setValueForProperty',
	  setValueForAttribute: 'setValueForAttribute',
	  deleteValueForProperty: 'deleteValueForProperty'
	});

	module.exports = DOMPropertyOperations;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 26 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule DOMProperty
	 * @typechecks static-only
	 */

	'use strict';

	var invariant = __webpack_require__(16);

	function checkMask(value, bitmask) {
	  return (value & bitmask) === bitmask;
	}

	var DOMPropertyInjection = {
	  /**
	   * Mapping from normalized, camelcased property names to a configuration that
	   * specifies how the associated DOM property should be accessed or rendered.
	   */
	  MUST_USE_ATTRIBUTE: 0x1,
	  MUST_USE_PROPERTY: 0x2,
	  HAS_SIDE_EFFECTS: 0x4,
	  HAS_BOOLEAN_VALUE: 0x8,
	  HAS_NUMERIC_VALUE: 0x10,
	  HAS_POSITIVE_NUMERIC_VALUE: 0x20 | 0x10,
	  HAS_OVERLOADED_BOOLEAN_VALUE: 0x40,

	  /**
	   * Inject some specialized knowledge about the DOM. This takes a config object
	   * with the following properties:
	   *
	   * isCustomAttribute: function that given an attribute name will return true
	   * if it can be inserted into the DOM verbatim. Useful for data-* or aria-*
	   * attributes where it's impossible to enumerate all of the possible
	   * attribute names,
	   *
	   * Properties: object mapping DOM property name to one of the
	   * DOMPropertyInjection constants or null. If your attribute isn't in here,
	   * it won't get written to the DOM.
	   *
	   * DOMAttributeNames: object mapping React attribute name to the DOM
	   * attribute name. Attribute names not specified use the **lowercase**
	   * normalized name.
	   *
	   * DOMAttributeNamespaces: object mapping React attribute name to the DOM
	   * attribute namespace URL. (Attribute names not specified use no namespace.)
	   *
	   * DOMPropertyNames: similar to DOMAttributeNames but for DOM properties.
	   * Property names not specified use the normalized name.
	   *
	   * DOMMutationMethods: Properties that require special mutation methods. If
	   * `value` is undefined, the mutation method should unset the property.
	   *
	   * @param {object} domPropertyConfig the config as described above.
	   */
	  injectDOMPropertyConfig: function (domPropertyConfig) {
	    var Injection = DOMPropertyInjection;
	    var Properties = domPropertyConfig.Properties || {};
	    var DOMAttributeNamespaces = domPropertyConfig.DOMAttributeNamespaces || {};
	    var DOMAttributeNames = domPropertyConfig.DOMAttributeNames || {};
	    var DOMPropertyNames = domPropertyConfig.DOMPropertyNames || {};
	    var DOMMutationMethods = domPropertyConfig.DOMMutationMethods || {};

	    if (domPropertyConfig.isCustomAttribute) {
	      DOMProperty._isCustomAttributeFunctions.push(domPropertyConfig.isCustomAttribute);
	    }

	    for (var propName in Properties) {
	      !!DOMProperty.properties.hasOwnProperty(propName) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'injectDOMPropertyConfig(...): You\'re trying to inject DOM property ' + '\'%s\' which has already been injected. You may be accidentally ' + 'injecting the same DOM property config twice, or you may be ' + 'injecting two configs that have conflicting property names.', propName) : invariant(false) : undefined;

	      var lowerCased = propName.toLowerCase();
	      var propConfig = Properties[propName];

	      var propertyInfo = {
	        attributeName: lowerCased,
	        attributeNamespace: null,
	        propertyName: propName,
	        mutationMethod: null,

	        mustUseAttribute: checkMask(propConfig, Injection.MUST_USE_ATTRIBUTE),
	        mustUseProperty: checkMask(propConfig, Injection.MUST_USE_PROPERTY),
	        hasSideEffects: checkMask(propConfig, Injection.HAS_SIDE_EFFECTS),
	        hasBooleanValue: checkMask(propConfig, Injection.HAS_BOOLEAN_VALUE),
	        hasNumericValue: checkMask(propConfig, Injection.HAS_NUMERIC_VALUE),
	        hasPositiveNumericValue: checkMask(propConfig, Injection.HAS_POSITIVE_NUMERIC_VALUE),
	        hasOverloadedBooleanValue: checkMask(propConfig, Injection.HAS_OVERLOADED_BOOLEAN_VALUE)
	      };

	      !(!propertyInfo.mustUseAttribute || !propertyInfo.mustUseProperty) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'DOMProperty: Cannot require using both attribute and property: %s', propName) : invariant(false) : undefined;
	      !(propertyInfo.mustUseProperty || !propertyInfo.hasSideEffects) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'DOMProperty: Properties that have side effects must use property: %s', propName) : invariant(false) : undefined;
	      !(propertyInfo.hasBooleanValue + propertyInfo.hasNumericValue + propertyInfo.hasOverloadedBooleanValue <= 1) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'DOMProperty: Value can be one of boolean, overloaded boolean, or ' + 'numeric value, but not a combination: %s', propName) : invariant(false) : undefined;

	      if (process.env.NODE_ENV !== 'production') {
	        DOMProperty.getPossibleStandardName[lowerCased] = propName;
	      }

	      if (DOMAttributeNames.hasOwnProperty(propName)) {
	        var attributeName = DOMAttributeNames[propName];
	        propertyInfo.attributeName = attributeName;
	        if (process.env.NODE_ENV !== 'production') {
	          DOMProperty.getPossibleStandardName[attributeName] = propName;
	        }
	      }

	      if (DOMAttributeNamespaces.hasOwnProperty(propName)) {
	        propertyInfo.attributeNamespace = DOMAttributeNamespaces[propName];
	      }

	      if (DOMPropertyNames.hasOwnProperty(propName)) {
	        propertyInfo.propertyName = DOMPropertyNames[propName];
	      }

	      if (DOMMutationMethods.hasOwnProperty(propName)) {
	        propertyInfo.mutationMethod = DOMMutationMethods[propName];
	      }

	      DOMProperty.properties[propName] = propertyInfo;
	    }
	  }
	};
	var defaultValueCache = {};

	/**
	 * DOMProperty exports lookup objects that can be used like functions:
	 *
	 *   > DOMProperty.isValid['id']
	 *   true
	 *   > DOMProperty.isValid['foobar']
	 *   undefined
	 *
	 * Although this may be confusing, it performs better in general.
	 *
	 * @see http://jsperf.com/key-exists
	 * @see http://jsperf.com/key-missing
	 */
	var DOMProperty = {

	  ID_ATTRIBUTE_NAME: 'data-reactid',

	  /**
	   * Map from property "standard name" to an object with info about how to set
	   * the property in the DOM. Each object contains:
	   *
	   * attributeName:
	   *   Used when rendering markup or with `*Attribute()`.
	   * attributeNamespace
	   * propertyName:
	   *   Used on DOM node instances. (This includes properties that mutate due to
	   *   external factors.)
	   * mutationMethod:
	   *   If non-null, used instead of the property or `setAttribute()` after
	   *   initial render.
	   * mustUseAttribute:
	   *   Whether the property must be accessed and mutated using `*Attribute()`.
	   *   (This includes anything that fails `<propName> in <element>`.)
	   * mustUseProperty:
	   *   Whether the property must be accessed and mutated as an object property.
	   * hasSideEffects:
	   *   Whether or not setting a value causes side effects such as triggering
	   *   resources to be loaded or text selection changes. If true, we read from
	   *   the DOM before updating to ensure that the value is only set if it has
	   *   changed.
	   * hasBooleanValue:
	   *   Whether the property should be removed when set to a falsey value.
	   * hasNumericValue:
	   *   Whether the property must be numeric or parse as a numeric and should be
	   *   removed when set to a falsey value.
	   * hasPositiveNumericValue:
	   *   Whether the property must be positive numeric or parse as a positive
	   *   numeric and should be removed when set to a falsey value.
	   * hasOverloadedBooleanValue:
	   *   Whether the property can be used as a flag as well as with a value.
	   *   Removed when strictly equal to false; present without a value when
	   *   strictly equal to true; present with a value otherwise.
	   */
	  properties: {},

	  /**
	   * Mapping from lowercase property names to the properly cased version, used
	   * to warn in the case of missing properties. Available only in __DEV__.
	   * @type {Object}
	   */
	  getPossibleStandardName: process.env.NODE_ENV !== 'production' ? {} : null,

	  /**
	   * All of the isCustomAttribute() functions that have been injected.
	   */
	  _isCustomAttributeFunctions: [],

	  /**
	   * Checks whether a property name is a custom attribute.
	   * @method
	   */
	  isCustomAttribute: function (attributeName) {
	    for (var i = 0; i < DOMProperty._isCustomAttributeFunctions.length; i++) {
	      var isCustomAttributeFn = DOMProperty._isCustomAttributeFunctions[i];
	      if (isCustomAttributeFn(attributeName)) {
	        return true;
	      }
	    }
	    return false;
	  },

	  /**
	   * Returns the default property value for a DOM property (i.e., not an
	   * attribute). Most default values are '' or false, but not all. Worse yet,
	   * some (in particular, `type`) vary depending on the type of element.
	   *
	   * TODO: Is it better to grab all the possible properties when creating an
	   * element to avoid having to create the same element twice?
	   */
	  getDefaultValueForProperty: function (nodeName, prop) {
	    var nodeDefaults = defaultValueCache[nodeName];
	    var testElement;
	    if (!nodeDefaults) {
	      defaultValueCache[nodeName] = nodeDefaults = {};
	    }
	    if (!(prop in nodeDefaults)) {
	      testElement = document.createElement(nodeName);
	      nodeDefaults[prop] = testElement[prop];
	    }
	    return nodeDefaults[prop];
	  },

	  injection: DOMPropertyInjection
	};

	module.exports = DOMProperty;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 27 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule quoteAttributeValueForBrowser
	 */

	'use strict';

	var escapeTextContentForBrowser = __webpack_require__(24);

	/**
	 * Escapes attribute value to prevent scripting attacks.
	 *
	 * @param {*} value Value to escape.
	 * @return {string} An escaped string.
	 */
	function quoteAttributeValueForBrowser(value) {
	  return '"' + escapeTextContentForBrowser(value) + '"';
	}

	module.exports = quoteAttributeValueForBrowser;

/***/ },
/* 28 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule warning
	 */

	'use strict';

	var emptyFunction = __webpack_require__(18);

	/**
	 * Similar to invariant but only logs a warning if the condition is not met.
	 * This can be used to log issues in development environments in critical
	 * paths. Removing the logging code for production environments will keep the
	 * same logic and follow the same code paths.
	 */

	var warning = emptyFunction;

	if (process.env.NODE_ENV !== 'production') {
	  warning = function (condition, format) {
	    for (var _len = arguments.length, args = Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
	      args[_key - 2] = arguments[_key];
	    }

	    if (format === undefined) {
	      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
	    }

	    if (format.indexOf('Failed Composite propType: ') === 0) {
	      return; // Ignore CompositeComponent proptype check.
	    }

	    if (!condition) {
	      var argIndex = 0;
	      var message = 'Warning: ' + format.replace(/%s/g, function () {
	        return args[argIndex++];
	      });
	      if (typeof console !== 'undefined') {
	        console.error(message);
	      }
	      try {
	        // --- Welcome to debugging React ---
	        // This error was thrown as a convenience so that you can use this stack
	        // to find the callsite that caused this warning to fire.
	        throw new Error(message);
	      } catch (x) {}
	    }
	  };
	}

	module.exports = warning;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 29 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactComponentBrowserEnvironment
	 */

	'use strict';

	var ReactDOMIDOperations = __webpack_require__(30);
	var ReactMount = __webpack_require__(31);

	/**
	 * Abstracts away all functionality of the reconciler that requires knowledge of
	 * the browser context. TODO: These callers should be refactored to avoid the
	 * need for this injection.
	 */
	var ReactComponentBrowserEnvironment = {

	  processChildrenUpdates: ReactDOMIDOperations.dangerouslyProcessChildrenUpdates,

	  replaceNodeWithMarkupByID: ReactDOMIDOperations.dangerouslyReplaceNodeWithMarkupByID,

	  /**
	   * If a particular environment requires that some resources be cleaned up,
	   * specify this in the injected Mixin. In the DOM, we would likely want to
	   * purge any cached node ID lookups.
	   *
	   * @private
	   */
	  unmountIDFromEnvironment: function (rootNodeID) {
	    ReactMount.purgeID(rootNodeID);
	  }

	};

	module.exports = ReactComponentBrowserEnvironment;

/***/ },
/* 30 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMIDOperations
	 * @typechecks static-only
	 */

	'use strict';

	var DOMChildrenOperations = __webpack_require__(10);
	var DOMPropertyOperations = __webpack_require__(25);
	var ReactMount = __webpack_require__(31);
	var ReactPerf = __webpack_require__(21);

	var invariant = __webpack_require__(16);

	/**
	 * Errors for properties that should not be updated with `updatePropertyByID()`.
	 *
	 * @type {object}
	 * @private
	 */
	var INVALID_PROPERTY_ERRORS = {
	  dangerouslySetInnerHTML: '`dangerouslySetInnerHTML` must be set using `updateInnerHTMLByID()`.',
	  style: '`style` must be set using `updateStylesByID()`.'
	};

	/**
	 * Operations used to process updates to DOM nodes.
	 */
	var ReactDOMIDOperations = {

	  /**
	   * Updates a DOM node with new property values. This should only be used to
	   * update DOM properties in `DOMProperty`.
	   *
	   * @param {string} id ID of the node to update.
	   * @param {string} name A valid property name, see `DOMProperty`.
	   * @param {*} value New value of the property.
	   * @internal
	   */
	  updatePropertyByID: function (id, name, value) {
	    var node = ReactMount.getNode(id);
	    !!INVALID_PROPERTY_ERRORS.hasOwnProperty(name) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'updatePropertyByID(...): %s', INVALID_PROPERTY_ERRORS[name]) : invariant(false) : undefined;

	    // If we're updating to null or undefined, we should remove the property
	    // from the DOM node instead of inadvertantly setting to a string. This
	    // brings us in line with the same behavior we have on initial render.
	    if (value != null) {
	      DOMPropertyOperations.setValueForProperty(node, name, value);
	    } else {
	      DOMPropertyOperations.deleteValueForProperty(node, name);
	    }
	  },

	  /**
	   * Replaces a DOM node that exists in the document with markup.
	   *
	   * @param {string} id ID of child to be replaced.
	   * @param {string} markup Dangerous markup to inject in place of child.
	   * @internal
	   * @see {Danger.dangerouslyReplaceNodeWithMarkup}
	   */
	  dangerouslyReplaceNodeWithMarkupByID: function (id, markup) {
	    var node = ReactMount.getNode(id);
	    DOMChildrenOperations.dangerouslyReplaceNodeWithMarkup(node, markup);
	  },

	  /**
	   * Updates a component's children by processing a series of updates.
	   *
	   * @param {array<object>} updates List of update configurations.
	   * @param {array<string>} markup List of markup strings.
	   * @internal
	   */
	  dangerouslyProcessChildrenUpdates: function (updates, markup) {
	    for (var i = 0; i < updates.length; i++) {
	      updates[i].parentNode = ReactMount.getNode(updates[i].parentID);
	    }
	    DOMChildrenOperations.processUpdates(updates, markup);
	  }
	};

	ReactPerf.measureMethods(ReactDOMIDOperations, 'ReactDOMIDOperations', {
	  dangerouslyReplaceNodeWithMarkupByID: 'dangerouslyReplaceNodeWithMarkupByID',
	  dangerouslyProcessChildrenUpdates: 'dangerouslyProcessChildrenUpdates'
	});

	module.exports = ReactDOMIDOperations;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 31 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactMount
	 */

	'use strict';

	var DOMProperty = __webpack_require__(26);
	var ReactBrowserEventEmitter = __webpack_require__(32);
	var ReactCurrentOwner = __webpack_require__(8);
	var ReactDOMFeatureFlags = __webpack_require__(44);
	var ReactElement = __webpack_require__(45);
	var ReactEmptyComponentRegistry = __webpack_require__(47);
	var ReactInstanceHandles = __webpack_require__(48);
	var ReactInstanceMap = __webpack_require__(50);
	var ReactMarkupChecksum = __webpack_require__(51);
	var ReactPerf = __webpack_require__(21);
	var ReactReconciler = __webpack_require__(53);
	var ReactUpdateQueue = __webpack_require__(56);
	var ReactUpdates = __webpack_require__(57);

	var assign = __webpack_require__(42);
	var emptyObject = __webpack_require__(61);
	var containsNode = __webpack_require__(62);
	var instantiateReactComponent = __webpack_require__(65);
	var invariant = __webpack_require__(16);
	var setInnerHTML = __webpack_require__(22);
	var shouldUpdateReactComponent = __webpack_require__(70);
	var validateDOMNesting = __webpack_require__(73);
	var warning = __webpack_require__(28);

	var ATTR_NAME = DOMProperty.ID_ATTRIBUTE_NAME;
	var nodeCache = {};

	var ELEMENT_NODE_TYPE = 1;
	var DOC_NODE_TYPE = 9;
	var DOCUMENT_FRAGMENT_NODE_TYPE = 11;

	var ownerDocumentContextKey = '__ReactMount_ownerDocument$' + Math.random().toString(36).slice(2);

	/** Mapping from reactRootID to React component instance. */
	var instancesByReactRootID = {};

	/** Mapping from reactRootID to `container` nodes. */
	var containersByReactRootID = {};

	if (process.env.NODE_ENV !== 'production') {
	  /** __DEV__-only mapping from reactRootID to root elements. */
	  var rootElementsByReactRootID = {};
	}

	// Used to store breadth-first search state in findComponentRoot.
	var findComponentRootReusableArray = [];

	/**
	 * Finds the index of the first character
	 * that's not common between the two given strings.
	 *
	 * @return {number} the index of the character where the strings diverge
	 */
	function firstDifferenceIndex(string1, string2) {
	  var minLen = Math.min(string1.length, string2.length);
	  for (var i = 0; i < minLen; i++) {
	    if (string1.charAt(i) !== string2.charAt(i)) {
	      return i;
	    }
	  }
	  return string1.length === string2.length ? -1 : minLen;
	}

	/**
	 * @param {DOMElement|DOMDocument} container DOM element that may contain
	 * a React component
	 * @return {?*} DOM element that may have the reactRoot ID, or null.
	 */
	function getReactRootElementInContainer(container) {
	  if (!container) {
	    return null;
	  }

	  if (container.nodeType === DOC_NODE_TYPE) {
	    return container.documentElement;
	  } else {
	    return container.firstChild;
	  }
	}

	/**
	 * @param {DOMElement} container DOM element that may contain a React component.
	 * @return {?string} A "reactRoot" ID, if a React component is rendered.
	 */
	function getReactRootID(container) {
	  var rootElement = getReactRootElementInContainer(container);
	  return rootElement && ReactMount.getID(rootElement);
	}

	/**
	 * Accessing node[ATTR_NAME] or calling getAttribute(ATTR_NAME) on a form
	 * element can return its control whose name or ID equals ATTR_NAME. All
	 * DOM nodes support `getAttributeNode` but this can also get called on
	 * other objects so just return '' if we're given something other than a
	 * DOM node (such as window).
	 *
	 * @param {?DOMElement|DOMWindow|DOMDocument|DOMTextNode} node DOM node.
	 * @return {string} ID of the supplied `domNode`.
	 */
	function getID(node) {
	  var id = internalGetID(node);
	  if (id) {
	    if (nodeCache.hasOwnProperty(id)) {
	      var cached = nodeCache[id];
	      if (cached !== node) {
	        !!isValid(cached, id) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactMount: Two valid but unequal nodes with the same `%s`: %s', ATTR_NAME, id) : invariant(false) : undefined;

	        nodeCache[id] = node;
	      }
	    } else {
	      nodeCache[id] = node;
	    }
	  }

	  return id;
	}

	function internalGetID(node) {
	  // If node is something like a window, document, or text node, none of
	  // which support attributes or a .getAttribute method, gracefully return
	  // the empty string, as if the attribute were missing.
	  return node && node.getAttribute && node.getAttribute(ATTR_NAME) || '';
	}

	/**
	 * Sets the React-specific ID of the given node.
	 *
	 * @param {DOMElement} node The DOM node whose ID will be set.
	 * @param {string} id The value of the ID attribute.
	 */
	function setID(node, id) {
	  var oldID = internalGetID(node);
	  if (oldID !== id) {
	    delete nodeCache[oldID];
	  }
	  node.setAttribute(ATTR_NAME, id);
	  nodeCache[id] = node;
	}

	/**
	 * Finds the node with the supplied React-generated DOM ID.
	 *
	 * @param {string} id A React-generated DOM ID.
	 * @return {DOMElement} DOM node with the suppled `id`.
	 * @internal
	 */
	function getNode(id) {
	  if (!nodeCache.hasOwnProperty(id) || !isValid(nodeCache[id], id)) {
	    nodeCache[id] = ReactMount.findReactNodeByID(id);
	  }
	  return nodeCache[id];
	}

	/**
	 * Finds the node with the supplied public React instance.
	 *
	 * @param {*} instance A public React instance.
	 * @return {?DOMElement} DOM node with the suppled `id`.
	 * @internal
	 */
	function getNodeFromInstance(instance) {
	  var id = ReactInstanceMap.get(instance)._rootNodeID;
	  if (ReactEmptyComponentRegistry.isNullComponentID(id)) {
	    return null;
	  }
	  if (!nodeCache.hasOwnProperty(id) || !isValid(nodeCache[id], id)) {
	    nodeCache[id] = ReactMount.findReactNodeByID(id);
	  }
	  return nodeCache[id];
	}

	/**
	 * A node is "valid" if it is contained by a currently mounted container.
	 *
	 * This means that the node does not have to be contained by a document in
	 * order to be considered valid.
	 *
	 * @param {?DOMElement} node The candidate DOM node.
	 * @param {string} id The expected ID of the node.
	 * @return {boolean} Whether the node is contained by a mounted container.
	 */
	function isValid(node, id) {
	  if (node) {
	    !(internalGetID(node) === id) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactMount: Unexpected modification of `%s`', ATTR_NAME) : invariant(false) : undefined;

	    var container = ReactMount.findReactContainerForID(id);
	    if (container && containsNode(container, node)) {
	      return true;
	    }
	  }

	  return false;
	}

	/**
	 * Causes the cache to forget about one React-specific ID.
	 *
	 * @param {string} id The ID to forget.
	 */
	function purgeID(id) {
	  delete nodeCache[id];
	}

	var deepestNodeSoFar = null;
	function findDeepestCachedAncestorImpl(ancestorID) {
	  var ancestor = nodeCache[ancestorID];
	  if (ancestor && isValid(ancestor, ancestorID)) {
	    deepestNodeSoFar = ancestor;
	  } else {
	    // This node isn't populated in the cache, so presumably none of its
	    // descendants are. Break out of the loop.
	    return false;
	  }
	}

	/**
	 * Return the deepest cached node whose ID is a prefix of `targetID`.
	 */
	function findDeepestCachedAncestor(targetID) {
	  deepestNodeSoFar = null;
	  ReactInstanceHandles.traverseAncestors(targetID, findDeepestCachedAncestorImpl);

	  var foundNode = deepestNodeSoFar;
	  deepestNodeSoFar = null;
	  return foundNode;
	}

	/**
	 * Mounts this component and inserts it into the DOM.
	 *
	 * @param {ReactComponent} componentInstance The instance to mount.
	 * @param {string} rootID DOM ID of the root node.
	 * @param {DOMElement} container DOM element to mount into.
	 * @param {ReactReconcileTransaction} transaction
	 * @param {boolean} shouldReuseMarkup If true, do not insert markup
	 */
	function mountComponentIntoNode(componentInstance, rootID, container, transaction, shouldReuseMarkup, context) {
	  if (ReactDOMFeatureFlags.useCreateElement) {
	    context = assign({}, context);
	    if (container.nodeType === DOC_NODE_TYPE) {
	      context[ownerDocumentContextKey] = container;
	    } else {
	      context[ownerDocumentContextKey] = container.ownerDocument;
	    }
	  }
	  if (process.env.NODE_ENV !== 'production') {
	    if (context === emptyObject) {
	      context = {};
	    }
	    var tag = container.nodeName.toLowerCase();
	    context[validateDOMNesting.ancestorInfoContextKey] = validateDOMNesting.updatedAncestorInfo(null, tag, null);
	  }
	  var markup = ReactReconciler.mountComponent(componentInstance, rootID, transaction, context);
	  componentInstance._renderedComponent._topLevelWrapper = componentInstance;
	  ReactMount._mountImageIntoNode(markup, container, shouldReuseMarkup, transaction);
	}

	/**
	 * Batched mount.
	 *
	 * @param {ReactComponent} componentInstance The instance to mount.
	 * @param {string} rootID DOM ID of the root node.
	 * @param {DOMElement} container DOM element to mount into.
	 * @param {boolean} shouldReuseMarkup If true, do not insert markup
	 */
	function batchedMountComponentIntoNode(componentInstance, rootID, container, shouldReuseMarkup, context) {
	  var transaction = ReactUpdates.ReactReconcileTransaction.getPooled(
	  /* forceHTML */shouldReuseMarkup);
	  transaction.perform(mountComponentIntoNode, null, componentInstance, rootID, container, transaction, shouldReuseMarkup, context);
	  ReactUpdates.ReactReconcileTransaction.release(transaction);
	}

	/**
	 * Unmounts a component and removes it from the DOM.
	 *
	 * @param {ReactComponent} instance React component instance.
	 * @param {DOMElement} container DOM element to unmount from.
	 * @final
	 * @internal
	 * @see {ReactMount.unmountComponentAtNode}
	 */
	function unmountComponentFromNode(instance, container) {
	  ReactReconciler.unmountComponent(instance);

	  if (container.nodeType === DOC_NODE_TYPE) {
	    container = container.documentElement;
	  }

	  // http://jsperf.com/emptying-a-node
	  while (container.lastChild) {
	    container.removeChild(container.lastChild);
	  }
	}

	/**
	 * True if the supplied DOM node has a direct React-rendered child that is
	 * not a React root element. Useful for warning in `render`,
	 * `unmountComponentAtNode`, etc.
	 *
	 * @param {?DOMElement} node The candidate DOM node.
	 * @return {boolean} True if the DOM element contains a direct child that was
	 * rendered by React but is not a root element.
	 * @internal
	 */
	function hasNonRootReactChild(node) {
	  var reactRootID = getReactRootID(node);
	  return reactRootID ? reactRootID !== ReactInstanceHandles.getReactRootIDFromNodeID(reactRootID) : false;
	}

	/**
	 * Returns the first (deepest) ancestor of a node which is rendered by this copy
	 * of React.
	 */
	function findFirstReactDOMImpl(node) {
	  // This node might be from another React instance, so we make sure not to
	  // examine the node cache here
	  for (; node && node.parentNode !== node; node = node.parentNode) {
	    if (node.nodeType !== 1) {
	      // Not a DOMElement, therefore not a React component
	      continue;
	    }
	    var nodeID = internalGetID(node);
	    if (!nodeID) {
	      continue;
	    }
	    var reactRootID = ReactInstanceHandles.getReactRootIDFromNodeID(nodeID);

	    // If containersByReactRootID contains the container we find by crawling up
	    // the tree, we know that this instance of React rendered the node.
	    // nb. isValid's strategy (with containsNode) does not work because render
	    // trees may be nested and we don't want a false positive in that case.
	    var current = node;
	    var lastID;
	    do {
	      lastID = internalGetID(current);
	      current = current.parentNode;
	      if (current == null) {
	        // The passed-in node has been detached from the container it was
	        // originally rendered into.
	        return null;
	      }
	    } while (lastID !== reactRootID);

	    if (current === containersByReactRootID[reactRootID]) {
	      return node;
	    }
	  }
	  return null;
	}

	/**
	 * Temporary (?) hack so that we can store all top-level pending updates on
	 * composites instead of having to worry about different types of components
	 * here.
	 */
	var TopLevelWrapper = function () {};
	TopLevelWrapper.prototype.isReactComponent = {};
	if (process.env.NODE_ENV !== 'production') {
	  TopLevelWrapper.displayName = 'TopLevelWrapper';
	}
	TopLevelWrapper.prototype.render = function () {
	  // this.props is actually a ReactElement
	  return this.props;
	};

	/**
	 * Mounting is the process of initializing a React component by creating its
	 * representative DOM elements and inserting them into a supplied `container`.
	 * Any prior content inside `container` is destroyed in the process.
	 *
	 *   ReactMount.render(
	 *     component,
	 *     document.getElementById('container')
	 *   );
	 *
	 *   <div id="container">                   <-- Supplied `container`.
	 *     <div data-reactid=".3">              <-- Rendered reactRoot of React
	 *       // ...                                 component.
	 *     </div>
	 *   </div>
	 *
	 * Inside of `container`, the first element rendered is the "reactRoot".
	 */
	var ReactMount = {

	  TopLevelWrapper: TopLevelWrapper,

	  /** Exposed for debugging purposes **/
	  _instancesByReactRootID: instancesByReactRootID,

	  /**
	   * This is a hook provided to support rendering React components while
	   * ensuring that the apparent scroll position of its `container` does not
	   * change.
	   *
	   * @param {DOMElement} container The `container` being rendered into.
	   * @param {function} renderCallback This must be called once to do the render.
	   */
	  scrollMonitor: function (container, renderCallback) {
	    renderCallback();
	  },

	  /**
	   * Take a component that's already mounted into the DOM and replace its props
	   * @param {ReactComponent} prevComponent component instance already in the DOM
	   * @param {ReactElement} nextElement component instance to render
	   * @param {DOMElement} container container to render into
	   * @param {?function} callback function triggered on completion
	   */
	  _updateRootComponent: function (prevComponent, nextElement, container, callback) {
	    ReactMount.scrollMonitor(container, function () {
	      ReactUpdateQueue.enqueueElementInternal(prevComponent, nextElement);
	      if (callback) {
	        ReactUpdateQueue.enqueueCallbackInternal(prevComponent, callback);
	      }
	    });

	    if (process.env.NODE_ENV !== 'production') {
	      // Record the root element in case it later gets transplanted.
	      rootElementsByReactRootID[getReactRootID(container)] = getReactRootElementInContainer(container);
	    }

	    return prevComponent;
	  },

	  /**
	   * Register a component into the instance map and starts scroll value
	   * monitoring
	   * @param {ReactComponent} nextComponent component instance to render
	   * @param {DOMElement} container container to render into
	   * @return {string} reactRoot ID prefix
	   */
	  _registerComponent: function (nextComponent, container) {
	    !(container && (container.nodeType === ELEMENT_NODE_TYPE || container.nodeType === DOC_NODE_TYPE || container.nodeType === DOCUMENT_FRAGMENT_NODE_TYPE)) ? process.env.NODE_ENV !== 'production' ? invariant(false, '_registerComponent(...): Target container is not a DOM element.') : invariant(false) : undefined;

	    ReactBrowserEventEmitter.ensureScrollValueMonitoring();

	    var reactRootID = ReactMount.registerContainer(container);
	    instancesByReactRootID[reactRootID] = nextComponent;
	    return reactRootID;
	  },

	  /**
	   * Render a new component into the DOM.
	   * @param {ReactElement} nextElement element to render
	   * @param {DOMElement} container container to render into
	   * @param {boolean} shouldReuseMarkup if we should skip the markup insertion
	   * @return {ReactComponent} nextComponent
	   */
	  _renderNewRootComponent: function (nextElement, container, shouldReuseMarkup, context) {
	    // Various parts of our code (such as ReactCompositeComponent's
	    // _renderValidatedComponent) assume that calls to render aren't nested;
	    // verify that that's the case.
	    process.env.NODE_ENV !== 'production' ? warning(ReactCurrentOwner.current == null, '_renderNewRootComponent(): Render methods should be a pure function ' + 'of props and state; triggering nested component updates from ' + 'render is not allowed. If necessary, trigger nested updates in ' + 'componentDidUpdate. Check the render method of %s.', ReactCurrentOwner.current && ReactCurrentOwner.current.getName() || 'ReactCompositeComponent') : undefined;

	    var componentInstance = instantiateReactComponent(nextElement, null);
	    var reactRootID = ReactMount._registerComponent(componentInstance, container);

	    // The initial render is synchronous but any updates that happen during
	    // rendering, in componentWillMount or componentDidMount, will be batched
	    // according to the current batching strategy.

	    ReactUpdates.batchedUpdates(batchedMountComponentIntoNode, componentInstance, reactRootID, container, shouldReuseMarkup, context);

	    if (process.env.NODE_ENV !== 'production') {
	      // Record the root element in case it later gets transplanted.
	      rootElementsByReactRootID[reactRootID] = getReactRootElementInContainer(container);
	    }

	    return componentInstance;
	  },

	  /**
	   * Renders a React component into the DOM in the supplied `container`.
	   *
	   * If the React component was previously rendered into `container`, this will
	   * perform an update on it and only mutate the DOM as necessary to reflect the
	   * latest React component.
	   *
	   * @param {ReactComponent} parentComponent The conceptual parent of this render tree.
	   * @param {ReactElement} nextElement Component element to render.
	   * @param {DOMElement} container DOM element to render into.
	   * @param {?function} callback function triggered on completion
	   * @return {ReactComponent} Component instance rendered in `container`.
	   */
	  renderSubtreeIntoContainer: function (parentComponent, nextElement, container, callback) {
	    !(parentComponent != null && parentComponent._reactInternalInstance != null) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'parentComponent must be a valid React Component') : invariant(false) : undefined;
	    return ReactMount._renderSubtreeIntoContainer(parentComponent, nextElement, container, callback);
	  },

	  _renderSubtreeIntoContainer: function (parentComponent, nextElement, container, callback) {
	    !ReactElement.isValidElement(nextElement) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactDOM.render(): Invalid component element.%s', typeof nextElement === 'string' ? ' Instead of passing an element string, make sure to instantiate ' + 'it by passing it to React.createElement.' : typeof nextElement === 'function' ? ' Instead of passing a component class, make sure to instantiate ' + 'it by passing it to React.createElement.' :
	    // Check if it quacks like an element
	    nextElement != null && nextElement.props !== undefined ? ' This may be caused by unintentionally loading two independent ' + 'copies of React.' : '') : invariant(false) : undefined;

	    process.env.NODE_ENV !== 'production' ? warning(!container || !container.tagName || container.tagName.toUpperCase() !== 'BODY', 'render(): Rendering components directly into document.body is ' + 'discouraged, since its children are often manipulated by third-party ' + 'scripts and browser extensions. This may lead to subtle ' + 'reconciliation issues. Try rendering into a container element created ' + 'for your app.') : undefined;

	    var nextWrappedElement = new ReactElement(TopLevelWrapper, null, null, null, null, null, nextElement);

	    var prevComponent = instancesByReactRootID[getReactRootID(container)];

	    if (prevComponent) {
	      var prevWrappedElement = prevComponent._currentElement;
	      var prevElement = prevWrappedElement.props;
	      if (shouldUpdateReactComponent(prevElement, nextElement)) {
	        var publicInst = prevComponent._renderedComponent.getPublicInstance();
	        var updatedCallback = callback && function () {
	          callback.call(publicInst);
	        };
	        ReactMount._updateRootComponent(prevComponent, nextWrappedElement, container, updatedCallback);
	        return publicInst;
	      } else {
	        ReactMount.unmountComponentAtNode(container);
	      }
	    }

	    var reactRootElement = getReactRootElementInContainer(container);
	    var containerHasReactMarkup = reactRootElement && !!internalGetID(reactRootElement);
	    var containerHasNonRootReactChild = hasNonRootReactChild(container);

	    if (process.env.NODE_ENV !== 'production') {
	      process.env.NODE_ENV !== 'production' ? warning(!containerHasNonRootReactChild, 'render(...): Replacing React-rendered children with a new root ' + 'component. If you intended to update the children of this node, ' + 'you should instead have the existing children update their state ' + 'and render the new components instead of calling ReactDOM.render.') : undefined;

	      if (!containerHasReactMarkup || reactRootElement.nextSibling) {
	        var rootElementSibling = reactRootElement;
	        while (rootElementSibling) {
	          if (internalGetID(rootElementSibling)) {
	            process.env.NODE_ENV !== 'production' ? warning(false, 'render(): Target node has markup rendered by React, but there ' + 'are unrelated nodes as well. This is most commonly caused by ' + 'white-space inserted around server-rendered markup.') : undefined;
	            break;
	          }
	          rootElementSibling = rootElementSibling.nextSibling;
	        }
	      }
	    }

	    var shouldReuseMarkup = containerHasReactMarkup && !prevComponent && !containerHasNonRootReactChild;
	    var component = ReactMount._renderNewRootComponent(nextWrappedElement, container, shouldReuseMarkup, parentComponent != null ? parentComponent._reactInternalInstance._processChildContext(parentComponent._reactInternalInstance._context) : emptyObject)._renderedComponent.getPublicInstance();
	    if (callback) {
	      callback.call(component);
	    }
	    return component;
	  },

	  /**
	   * Renders a React component into the DOM in the supplied `container`.
	   *
	   * If the React component was previously rendered into `container`, this will
	   * perform an update on it and only mutate the DOM as necessary to reflect the
	   * latest React component.
	   *
	   * @param {ReactElement} nextElement Component element to render.
	   * @param {DOMElement} container DOM element to render into.
	   * @param {?function} callback function triggered on completion
	   * @return {ReactComponent} Component instance rendered in `container`.
	   */
	  render: function (nextElement, container, callback) {
	    return ReactMount._renderSubtreeIntoContainer(null, nextElement, container, callback);
	  },

	  /**
	   * Registers a container node into which React components will be rendered.
	   * This also creates the "reactRoot" ID that will be assigned to the element
	   * rendered within.
	   *
	   * @param {DOMElement} container DOM element to register as a container.
	   * @return {string} The "reactRoot" ID of elements rendered within.
	   */
	  registerContainer: function (container) {
	    var reactRootID = getReactRootID(container);
	    if (reactRootID) {
	      // If one exists, make sure it is a valid "reactRoot" ID.
	      reactRootID = ReactInstanceHandles.getReactRootIDFromNodeID(reactRootID);
	    }
	    if (!reactRootID) {
	      // No valid "reactRoot" ID found, create one.
	      reactRootID = ReactInstanceHandles.createReactRootID();
	    }
	    containersByReactRootID[reactRootID] = container;
	    return reactRootID;
	  },

	  /**
	   * Unmounts and destroys the React component rendered in the `container`.
	   *
	   * @param {DOMElement} container DOM element containing a React component.
	   * @return {boolean} True if a component was found in and unmounted from
	   *                   `container`
	   */
	  unmountComponentAtNode: function (container) {
	    // Various parts of our code (such as ReactCompositeComponent's
	    // _renderValidatedComponent) assume that calls to render aren't nested;
	    // verify that that's the case. (Strictly speaking, unmounting won't cause a
	    // render but we still don't expect to be in a render call here.)
	    process.env.NODE_ENV !== 'production' ? warning(ReactCurrentOwner.current == null, 'unmountComponentAtNode(): Render methods should be a pure function ' + 'of props and state; triggering nested component updates from render ' + 'is not allowed. If necessary, trigger nested updates in ' + 'componentDidUpdate. Check the render method of %s.', ReactCurrentOwner.current && ReactCurrentOwner.current.getName() || 'ReactCompositeComponent') : undefined;

	    !(container && (container.nodeType === ELEMENT_NODE_TYPE || container.nodeType === DOC_NODE_TYPE || container.nodeType === DOCUMENT_FRAGMENT_NODE_TYPE)) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'unmountComponentAtNode(...): Target container is not a DOM element.') : invariant(false) : undefined;

	    var reactRootID = getReactRootID(container);
	    var component = instancesByReactRootID[reactRootID];
	    if (!component) {
	      // Check if the node being unmounted was rendered by React, but isn't a
	      // root node.
	      var containerHasNonRootReactChild = hasNonRootReactChild(container);

	      // Check if the container itself is a React root node.
	      var containerID = internalGetID(container);
	      var isContainerReactRoot = containerID && containerID === ReactInstanceHandles.getReactRootIDFromNodeID(containerID);

	      if (process.env.NODE_ENV !== 'production') {
	        process.env.NODE_ENV !== 'production' ? warning(!containerHasNonRootReactChild, 'unmountComponentAtNode(): The node you\'re attempting to unmount ' + 'was rendered by React and is not a top-level container. %s', isContainerReactRoot ? 'You may have accidentally passed in a React root node instead ' + 'of its container.' : 'Instead, have the parent component update its state and ' + 'rerender in order to remove this component.') : undefined;
	      }

	      return false;
	    }
	    ReactUpdates.batchedUpdates(unmountComponentFromNode, component, container);
	    delete instancesByReactRootID[reactRootID];
	    delete containersByReactRootID[reactRootID];
	    if (process.env.NODE_ENV !== 'production') {
	      delete rootElementsByReactRootID[reactRootID];
	    }
	    return true;
	  },

	  /**
	   * Finds the container DOM element that contains React component to which the
	   * supplied DOM `id` belongs.
	   *
	   * @param {string} id The ID of an element rendered by a React component.
	   * @return {?DOMElement} DOM element that contains the `id`.
	   */
	  findReactContainerForID: function (id) {
	    var reactRootID = ReactInstanceHandles.getReactRootIDFromNodeID(id);
	    var container = containersByReactRootID[reactRootID];

	    if (process.env.NODE_ENV !== 'production') {
	      var rootElement = rootElementsByReactRootID[reactRootID];
	      if (rootElement && rootElement.parentNode !== container) {
	        process.env.NODE_ENV !== 'production' ? warning(
	        // Call internalGetID here because getID calls isValid which calls
	        // findReactContainerForID (this function).
	        internalGetID(rootElement) === reactRootID, 'ReactMount: Root element ID differed from reactRootID.') : undefined;
	        var containerChild = container.firstChild;
	        if (containerChild && reactRootID === internalGetID(containerChild)) {
	          // If the container has a new child with the same ID as the old
	          // root element, then rootElementsByReactRootID[reactRootID] is
	          // just stale and needs to be updated. The case that deserves a
	          // warning is when the container is empty.
	          rootElementsByReactRootID[reactRootID] = containerChild;
	        } else {
	          process.env.NODE_ENV !== 'production' ? warning(false, 'ReactMount: Root element has been removed from its original ' + 'container. New container: %s', rootElement.parentNode) : undefined;
	        }
	      }
	    }

	    return container;
	  },

	  /**
	   * Finds an element rendered by React with the supplied ID.
	   *
	   * @param {string} id ID of a DOM node in the React component.
	   * @return {DOMElement} Root DOM node of the React component.
	   */
	  findReactNodeByID: function (id) {
	    var reactRoot = ReactMount.findReactContainerForID(id);
	    return ReactMount.findComponentRoot(reactRoot, id);
	  },

	  /**
	   * Traverses up the ancestors of the supplied node to find a node that is a
	   * DOM representation of a React component rendered by this copy of React.
	   *
	   * @param {*} node
	   * @return {?DOMEventTarget}
	   * @internal
	   */
	  getFirstReactDOM: function (node) {
	    return findFirstReactDOMImpl(node);
	  },

	  /**
	   * Finds a node with the supplied `targetID` inside of the supplied
	   * `ancestorNode`.  Exploits the ID naming scheme to perform the search
	   * quickly.
	   *
	   * @param {DOMEventTarget} ancestorNode Search from this root.
	   * @pararm {string} targetID ID of the DOM representation of the component.
	   * @return {DOMEventTarget} DOM node with the supplied `targetID`.
	   * @internal
	   */
	  findComponentRoot: function (ancestorNode, targetID) {
	    var firstChildren = findComponentRootReusableArray;
	    var childIndex = 0;

	    var deepestAncestor = findDeepestCachedAncestor(targetID) || ancestorNode;

	    if (process.env.NODE_ENV !== 'production') {
	      // This will throw on the next line; give an early warning
	      process.env.NODE_ENV !== 'production' ? warning(deepestAncestor != null, 'React can\'t find the root component node for data-reactid value ' + '`%s`. If you\'re seeing this message, it probably means that ' + 'you\'ve loaded two copies of React on the page. At this time, only ' + 'a single copy of React can be loaded at a time.', targetID) : undefined;
	    }

	    firstChildren[0] = deepestAncestor.firstChild;
	    firstChildren.length = 1;

	    while (childIndex < firstChildren.length) {
	      var child = firstChildren[childIndex++];
	      var targetChild;

	      while (child) {
	        var childID = ReactMount.getID(child);
	        if (childID) {
	          // Even if we find the node we're looking for, we finish looping
	          // through its siblings to ensure they're cached so that we don't have
	          // to revisit this node again. Otherwise, we make n^2 calls to getID
	          // when visiting the many children of a single node in order.

	          if (targetID === childID) {
	            targetChild = child;
	          } else if (ReactInstanceHandles.isAncestorIDOf(childID, targetID)) {
	            // If we find a child whose ID is an ancestor of the given ID,
	            // then we can be sure that we only want to search the subtree
	            // rooted at this child, so we can throw out the rest of the
	            // search state.
	            firstChildren.length = childIndex = 0;
	            firstChildren.push(child.firstChild);
	          }
	        } else {
	          // If this child had no ID, then there's a chance that it was
	          // injected automatically by the browser, as when a `<table>`
	          // element sprouts an extra `<tbody>` child as a side effect of
	          // `.innerHTML` parsing. Optimistically continue down this
	          // branch, but not before examining the other siblings.
	          firstChildren.push(child.firstChild);
	        }

	        child = child.nextSibling;
	      }

	      if (targetChild) {
	        // Emptying firstChildren/findComponentRootReusableArray is
	        // not necessary for correctness, but it helps the GC reclaim
	        // any nodes that were left at the end of the search.
	        firstChildren.length = 0;

	        return targetChild;
	      }
	    }

	    firstChildren.length = 0;

	     true ? process.env.NODE_ENV !== 'production' ? invariant(false, 'findComponentRoot(..., %s): Unable to find element. This probably ' + 'means the DOM was unexpectedly mutated (e.g., by the browser), ' + 'usually due to forgetting a <tbody> when using tables, nesting tags ' + 'like <form>, <p>, or <a>, or using non-SVG elements in an <svg> ' + 'parent. ' + 'Try inspecting the child nodes of the element with React ID `%s`.', targetID, ReactMount.getID(ancestorNode)) : invariant(false) : undefined;
	  },

	  _mountImageIntoNode: function (markup, container, shouldReuseMarkup, transaction) {
	    !(container && (container.nodeType === ELEMENT_NODE_TYPE || container.nodeType === DOC_NODE_TYPE || container.nodeType === DOCUMENT_FRAGMENT_NODE_TYPE)) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'mountComponentIntoNode(...): Target container is not valid.') : invariant(false) : undefined;

	    if (shouldReuseMarkup) {
	      var rootElement = getReactRootElementInContainer(container);
	      if (ReactMarkupChecksum.canReuseMarkup(markup, rootElement)) {
	        return;
	      } else {
	        var checksum = rootElement.getAttribute(ReactMarkupChecksum.CHECKSUM_ATTR_NAME);
	        rootElement.removeAttribute(ReactMarkupChecksum.CHECKSUM_ATTR_NAME);

	        var rootMarkup = rootElement.outerHTML;
	        rootElement.setAttribute(ReactMarkupChecksum.CHECKSUM_ATTR_NAME, checksum);

	        var normalizedMarkup = markup;
	        if (process.env.NODE_ENV !== 'production') {
	          // because rootMarkup is retrieved from the DOM, various normalizations
	          // will have occurred which will not be present in `markup`. Here,
	          // insert markup into a <div> or <iframe> depending on the container
	          // type to perform the same normalizations before comparing.
	          var normalizer;
	          if (container.nodeType === ELEMENT_NODE_TYPE) {
	            normalizer = document.createElement('div');
	            normalizer.innerHTML = markup;
	            normalizedMarkup = normalizer.innerHTML;
	          } else {
	            normalizer = document.createElement('iframe');
	            document.body.appendChild(normalizer);
	            normalizer.contentDocument.write(markup);
	            normalizedMarkup = normalizer.contentDocument.documentElement.outerHTML;
	            document.body.removeChild(normalizer);
	          }
	        }

	        var diffIndex = firstDifferenceIndex(normalizedMarkup, rootMarkup);
	        var difference = ' (client) ' + normalizedMarkup.substring(diffIndex - 20, diffIndex + 20) + '\n (server) ' + rootMarkup.substring(diffIndex - 20, diffIndex + 20);

	        !(container.nodeType !== DOC_NODE_TYPE) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'You\'re trying to render a component to the document using ' + 'server rendering but the checksum was invalid. This usually ' + 'means you rendered a different component type or props on ' + 'the client from the one on the server, or your render() ' + 'methods are impure. React cannot handle this case due to ' + 'cross-browser quirks by rendering at the document root. You ' + 'should look for environment dependent code in your components ' + 'and ensure the props are the same client and server side:\n%s', difference) : invariant(false) : undefined;

	        if (process.env.NODE_ENV !== 'production') {
	          process.env.NODE_ENV !== 'production' ? warning(false, 'React attempted to reuse markup in a container but the ' + 'checksum was invalid. This generally means that you are ' + 'using server rendering and the markup generated on the ' + 'server was not what the client was expecting. React injected ' + 'new markup to compensate which works but you have lost many ' + 'of the benefits of server rendering. Instead, figure out ' + 'why the markup being generated is different on the client ' + 'or server:\n%s', difference) : undefined;
	        }
	      }
	    }

	    !(container.nodeType !== DOC_NODE_TYPE) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'You\'re trying to render a component to the document but ' + 'you didn\'t use server rendering. We can\'t do this ' + 'without using server rendering due to cross-browser quirks. ' + 'See ReactDOMServer.renderToString() for server rendering.') : invariant(false) : undefined;

	    if (transaction.useCreateElement) {
	      while (container.lastChild) {
	        container.removeChild(container.lastChild);
	      }
	      container.appendChild(markup);
	    } else {
	      setInnerHTML(container, markup);
	    }
	  },

	  ownerDocumentContextKey: ownerDocumentContextKey,

	  /**
	   * React ID utilities.
	   */

	  getReactRootID: getReactRootID,

	  getID: getID,

	  setID: setID,

	  getNode: getNode,

	  getNodeFromInstance: getNodeFromInstance,

	  isValid: isValid,

	  purgeID: purgeID
	};

	ReactPerf.measureMethods(ReactMount, 'ReactMount', {
	  _renderNewRootComponent: '_renderNewRootComponent',
	  _mountImageIntoNode: '_mountImageIntoNode'
	});

	module.exports = ReactMount;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 32 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactBrowserEventEmitter
	 * @typechecks static-only
	 */

	'use strict';

	var EventConstants = __webpack_require__(33);
	var EventPluginHub = __webpack_require__(34);
	var EventPluginRegistry = __webpack_require__(35);
	var ReactEventEmitterMixin = __webpack_require__(40);
	var ReactPerf = __webpack_require__(21);
	var ViewportMetrics = __webpack_require__(41);

	var assign = __webpack_require__(42);
	var isEventSupported = __webpack_require__(43);

	/**
	 * Summary of `ReactBrowserEventEmitter` event handling:
	 *
	 *  - Top-level delegation is used to trap most native browser events. This
	 *    may only occur in the main thread and is the responsibility of
	 *    ReactEventListener, which is injected and can therefore support pluggable
	 *    event sources. This is the only work that occurs in the main thread.
	 *
	 *  - We normalize and de-duplicate events to account for browser quirks. This
	 *    may be done in the worker thread.
	 *
	 *  - Forward these native events (with the associated top-level type used to
	 *    trap it) to `EventPluginHub`, which in turn will ask plugins if they want
	 *    to extract any synthetic events.
	 *
	 *  - The `EventPluginHub` will then process each event by annotating them with
	 *    "dispatches", a sequence of listeners and IDs that care about that event.
	 *
	 *  - The `EventPluginHub` then dispatches the events.
	 *
	 * Overview of React and the event system:
	 *
	 * +------------+    .
	 * |    DOM     |    .
	 * +------------+    .
	 *       |           .
	 *       v           .
	 * +------------+    .
	 * | ReactEvent |    .
	 * |  Listener  |    .
	 * +------------+    .                         +-----------+
	 *       |           .               +--------+|SimpleEvent|
	 *       |           .               |         |Plugin     |
	 * +-----|------+    .               v         +-----------+
	 * |     |      |    .    +--------------+                    +------------+
	 * |     +-----------.--->|EventPluginHub|                    |    Event   |
	 * |            |    .    |              |     +-----------+  | Propagators|
	 * | ReactEvent |    .    |              |     |TapEvent   |  |------------|
	 * |  Emitter   |    .    |              |<---+|Plugin     |  |other plugin|
	 * |            |    .    |              |     +-----------+  |  utilities |
	 * |     +-----------.--->|              |                    +------------+
	 * |     |      |    .    +--------------+
	 * +-----|------+    .                ^        +-----------+
	 *       |           .                |        |Enter/Leave|
	 *       +           .                +-------+|Plugin     |
	 * +-------------+   .                         +-----------+
	 * | application |   .
	 * |-------------|   .
	 * |             |   .
	 * |             |   .
	 * +-------------+   .
	 *                   .
	 *    React Core     .  General Purpose Event Plugin System
	 */

	var alreadyListeningTo = {};
	var isMonitoringScrollValue = false;
	var reactTopListenersCounter = 0;

	// For events like 'submit' which don't consistently bubble (which we trap at a
	// lower node than `document`), binding at `document` would cause duplicate
	// events so we don't include them here
	var topEventMapping = {
	  topAbort: 'abort',
	  topBlur: 'blur',
	  topCanPlay: 'canplay',
	  topCanPlayThrough: 'canplaythrough',
	  topChange: 'change',
	  topClick: 'click',
	  topCompositionEnd: 'compositionend',
	  topCompositionStart: 'compositionstart',
	  topCompositionUpdate: 'compositionupdate',
	  topContextMenu: 'contextmenu',
	  topCopy: 'copy',
	  topCut: 'cut',
	  topDoubleClick: 'dblclick',
	  topDrag: 'drag',
	  topDragEnd: 'dragend',
	  topDragEnter: 'dragenter',
	  topDragExit: 'dragexit',
	  topDragLeave: 'dragleave',
	  topDragOver: 'dragover',
	  topDragStart: 'dragstart',
	  topDrop: 'drop',
	  topDurationChange: 'durationchange',
	  topEmptied: 'emptied',
	  topEncrypted: 'encrypted',
	  topEnded: 'ended',
	  topError: 'error',
	  topFocus: 'focus',
	  topInput: 'input',
	  topKeyDown: 'keydown',
	  topKeyPress: 'keypress',
	  topKeyUp: 'keyup',
	  topLoadedData: 'loadeddata',
	  topLoadedMetadata: 'loadedmetadata',
	  topLoadStart: 'loadstart',
	  topMouseDown: 'mousedown',
	  topMouseMove: 'mousemove',
	  topMouseOut: 'mouseout',
	  topMouseOver: 'mouseover',
	  topMouseUp: 'mouseup',
	  topPaste: 'paste',
	  topPause: 'pause',
	  topPlay: 'play',
	  topPlaying: 'playing',
	  topProgress: 'progress',
	  topRateChange: 'ratechange',
	  topScroll: 'scroll',
	  topSeeked: 'seeked',
	  topSeeking: 'seeking',
	  topSelectionChange: 'selectionchange',
	  topStalled: 'stalled',
	  topSuspend: 'suspend',
	  topTextInput: 'textInput',
	  topTimeUpdate: 'timeupdate',
	  topTouchCancel: 'touchcancel',
	  topTouchEnd: 'touchend',
	  topTouchMove: 'touchmove',
	  topTouchStart: 'touchstart',
	  topVolumeChange: 'volumechange',
	  topWaiting: 'waiting',
	  topWheel: 'wheel'
	};

	/**
	 * To ensure no conflicts with other potential React instances on the page
	 */
	var topListenersIDKey = '_reactListenersID' + String(Math.random()).slice(2);

	function getListeningForDocument(mountAt) {
	  // In IE8, `mountAt` is a host object and doesn't have `hasOwnProperty`
	  // directly.
	  if (!Object.prototype.hasOwnProperty.call(mountAt, topListenersIDKey)) {
	    mountAt[topListenersIDKey] = reactTopListenersCounter++;
	    alreadyListeningTo[mountAt[topListenersIDKey]] = {};
	  }
	  return alreadyListeningTo[mountAt[topListenersIDKey]];
	}

	/**
	 * `ReactBrowserEventEmitter` is used to attach top-level event listeners. For
	 * example:
	 *
	 *   ReactBrowserEventEmitter.putListener('myID', 'onClick', myFunction);
	 *
	 * This would allocate a "registration" of `('onClick', myFunction)` on 'myID'.
	 *
	 * @internal
	 */
	var ReactBrowserEventEmitter = assign({}, ReactEventEmitterMixin, {

	  /**
	   * Injectable event backend
	   */
	  ReactEventListener: null,

	  injection: {
	    /**
	     * @param {object} ReactEventListener
	     */
	    injectReactEventListener: function (ReactEventListener) {
	      ReactEventListener.setHandleTopLevel(ReactBrowserEventEmitter.handleTopLevel);
	      ReactBrowserEventEmitter.ReactEventListener = ReactEventListener;
	    }
	  },

	  /**
	   * Sets whether or not any created callbacks should be enabled.
	   *
	   * @param {boolean} enabled True if callbacks should be enabled.
	   */
	  setEnabled: function (enabled) {
	    if (ReactBrowserEventEmitter.ReactEventListener) {
	      ReactBrowserEventEmitter.ReactEventListener.setEnabled(enabled);
	    }
	  },

	  /**
	   * @return {boolean} True if callbacks are enabled.
	   */
	  isEnabled: function () {
	    return !!(ReactBrowserEventEmitter.ReactEventListener && ReactBrowserEventEmitter.ReactEventListener.isEnabled());
	  },

	  /**
	   * We listen for bubbled touch events on the document object.
	   *
	   * Firefox v8.01 (and possibly others) exhibited strange behavior when
	   * mounting `onmousemove` events at some node that was not the document
	   * element. The symptoms were that if your mouse is not moving over something
	   * contained within that mount point (for example on the background) the
	   * top-level listeners for `onmousemove` won't be called. However, if you
	   * register the `mousemove` on the document object, then it will of course
	   * catch all `mousemove`s. This along with iOS quirks, justifies restricting
	   * top-level listeners to the document object only, at least for these
	   * movement types of events and possibly all events.
	   *
	   * @see http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
	   *
	   * Also, `keyup`/`keypress`/`keydown` do not bubble to the window on IE, but
	   * they bubble to document.
	   *
	   * @param {string} registrationName Name of listener (e.g. `onClick`).
	   * @param {object} contentDocumentHandle Document which owns the container
	   */
	  listenTo: function (registrationName, contentDocumentHandle) {
	    var mountAt = contentDocumentHandle;
	    var isListening = getListeningForDocument(mountAt);
	    var dependencies = EventPluginRegistry.registrationNameDependencies[registrationName];

	    var topLevelTypes = EventConstants.topLevelTypes;
	    for (var i = 0; i < dependencies.length; i++) {
	      var dependency = dependencies[i];
	      if (!(isListening.hasOwnProperty(dependency) && isListening[dependency])) {
	        if (dependency === topLevelTypes.topWheel) {
	          if (isEventSupported('wheel')) {
	            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelTypes.topWheel, 'wheel', mountAt);
	          } else if (isEventSupported('mousewheel')) {
	            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelTypes.topWheel, 'mousewheel', mountAt);
	          } else {
	            // Firefox needs to capture a different mouse scroll event.
	            // @see http://www.quirksmode.org/dom/events/tests/scroll.html
	            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelTypes.topWheel, 'DOMMouseScroll', mountAt);
	          }
	        } else if (dependency === topLevelTypes.topScroll) {

	          if (isEventSupported('scroll', true)) {
	            ReactBrowserEventEmitter.ReactEventListener.trapCapturedEvent(topLevelTypes.topScroll, 'scroll', mountAt);
	          } else {
	            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelTypes.topScroll, 'scroll', ReactBrowserEventEmitter.ReactEventListener.WINDOW_HANDLE);
	          }
	        } else if (dependency === topLevelTypes.topFocus || dependency === topLevelTypes.topBlur) {

	          if (isEventSupported('focus', true)) {
	            ReactBrowserEventEmitter.ReactEventListener.trapCapturedEvent(topLevelTypes.topFocus, 'focus', mountAt);
	            ReactBrowserEventEmitter.ReactEventListener.trapCapturedEvent(topLevelTypes.topBlur, 'blur', mountAt);
	          } else if (isEventSupported('focusin')) {
	            // IE has `focusin` and `focusout` events which bubble.
	            // @see http://www.quirksmode.org/blog/archives/2008/04/delegating_the.html
	            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelTypes.topFocus, 'focusin', mountAt);
	            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelTypes.topBlur, 'focusout', mountAt);
	          }

	          // to make sure blur and focus event listeners are only attached once
	          isListening[topLevelTypes.topBlur] = true;
	          isListening[topLevelTypes.topFocus] = true;
	        } else if (topEventMapping.hasOwnProperty(dependency)) {
	          ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(dependency, topEventMapping[dependency], mountAt);
	        }

	        isListening[dependency] = true;
	      }
	    }
	  },

	  trapBubbledEvent: function (topLevelType, handlerBaseName, handle) {
	    return ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(topLevelType, handlerBaseName, handle);
	  },

	  trapCapturedEvent: function (topLevelType, handlerBaseName, handle) {
	    return ReactBrowserEventEmitter.ReactEventListener.trapCapturedEvent(topLevelType, handlerBaseName, handle);
	  },

	  /**
	   * Listens to window scroll and resize events. We cache scroll values so that
	   * application code can access them without triggering reflows.
	   *
	   * NOTE: Scroll events do not bubble.
	   *
	   * @see http://www.quirksmode.org/dom/events/scroll.html
	   */
	  ensureScrollValueMonitoring: function () {
	    if (!isMonitoringScrollValue) {
	      var refresh = ViewportMetrics.refreshScrollValues;
	      ReactBrowserEventEmitter.ReactEventListener.monitorScrollValue(refresh);
	      isMonitoringScrollValue = true;
	    }
	  },

	  eventNameDispatchConfigs: EventPluginHub.eventNameDispatchConfigs,

	  registrationNameModules: EventPluginHub.registrationNameModules,

	  putListener: EventPluginHub.putListener,

	  getListener: EventPluginHub.getListener,

	  deleteListener: EventPluginHub.deleteListener,

	  deleteAllListeners: EventPluginHub.deleteAllListeners

	});

	ReactPerf.measureMethods(ReactBrowserEventEmitter, 'ReactBrowserEventEmitter', {
	  putListener: 'putListener',
	  deleteListener: 'deleteListener'
	});

	module.exports = ReactBrowserEventEmitter;

/***/ },
/* 33 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule EventConstants
	 */

	'use strict';

	var keyMirror = __webpack_require__(20);

	var PropagationPhases = keyMirror({ bubbled: null, captured: null });

	/**
	 * Types of raw signals from the browser caught at the top level.
	 */
	var topLevelTypes = keyMirror({
	  topAbort: null,
	  topBlur: null,
	  topCanPlay: null,
	  topCanPlayThrough: null,
	  topChange: null,
	  topClick: null,
	  topCompositionEnd: null,
	  topCompositionStart: null,
	  topCompositionUpdate: null,
	  topContextMenu: null,
	  topCopy: null,
	  topCut: null,
	  topDoubleClick: null,
	  topDrag: null,
	  topDragEnd: null,
	  topDragEnter: null,
	  topDragExit: null,
	  topDragLeave: null,
	  topDragOver: null,
	  topDragStart: null,
	  topDrop: null,
	  topDurationChange: null,
	  topEmptied: null,
	  topEncrypted: null,
	  topEnded: null,
	  topError: null,
	  topFocus: null,
	  topInput: null,
	  topKeyDown: null,
	  topKeyPress: null,
	  topKeyUp: null,
	  topLoad: null,
	  topLoadedData: null,
	  topLoadedMetadata: null,
	  topLoadStart: null,
	  topMouseDown: null,
	  topMouseMove: null,
	  topMouseOut: null,
	  topMouseOver: null,
	  topMouseUp: null,
	  topPaste: null,
	  topPause: null,
	  topPlay: null,
	  topPlaying: null,
	  topProgress: null,
	  topRateChange: null,
	  topReset: null,
	  topScroll: null,
	  topSeeked: null,
	  topSeeking: null,
	  topSelectionChange: null,
	  topStalled: null,
	  topSubmit: null,
	  topSuspend: null,
	  topTextInput: null,
	  topTimeUpdate: null,
	  topTouchCancel: null,
	  topTouchEnd: null,
	  topTouchMove: null,
	  topTouchStart: null,
	  topVolumeChange: null,
	  topWaiting: null,
	  topWheel: null
	});

	var EventConstants = {
	  topLevelTypes: topLevelTypes,
	  PropagationPhases: PropagationPhases
	};

	module.exports = EventConstants;

/***/ },
/* 34 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule EventPluginHub
	 */

	'use strict';

	var EventPluginRegistry = __webpack_require__(35);
	var EventPluginUtils = __webpack_require__(36);
	var ReactErrorUtils = __webpack_require__(37);

	var accumulateInto = __webpack_require__(38);
	var forEachAccumulated = __webpack_require__(39);
	var invariant = __webpack_require__(16);
	var warning = __webpack_require__(28);

	/**
	 * Internal store for event listeners
	 */
	var listenerBank = {};

	/**
	 * Internal queue of events that have accumulated their dispatches and are
	 * waiting to have their dispatches executed.
	 */
	var eventQueue = null;

	/**
	 * Dispatches an event and releases it back into the pool, unless persistent.
	 *
	 * @param {?object} event Synthetic event to be dispatched.
	 * @param {boolean} simulated If the event is simulated (changes exn behavior)
	 * @private
	 */
	var executeDispatchesAndRelease = function (event, simulated) {
	  if (event) {
	    EventPluginUtils.executeDispatchesInOrder(event, simulated);

	    if (!event.isPersistent()) {
	      event.constructor.release(event);
	    }
	  }
	};
	var executeDispatchesAndReleaseSimulated = function (e) {
	  return executeDispatchesAndRelease(e, true);
	};
	var executeDispatchesAndReleaseTopLevel = function (e) {
	  return executeDispatchesAndRelease(e, false);
	};

	/**
	 * - `InstanceHandle`: [required] Module that performs logical traversals of DOM
	 *   hierarchy given ids of the logical DOM elements involved.
	 */
	var InstanceHandle = null;

	function validateInstanceHandle() {
	  var valid = InstanceHandle && InstanceHandle.traverseTwoPhase && InstanceHandle.traverseEnterLeave;
	  process.env.NODE_ENV !== 'production' ? warning(valid, 'InstanceHandle not injected before use!') : undefined;
	}

	/**
	 * This is a unified interface for event plugins to be installed and configured.
	 *
	 * Event plugins can implement the following properties:
	 *
	 *   `extractEvents` {function(string, DOMEventTarget, string, object): *}
	 *     Required. When a top-level event is fired, this method is expected to
	 *     extract synthetic events that will in turn be queued and dispatched.
	 *
	 *   `eventTypes` {object}
	 *     Optional, plugins that fire events must publish a mapping of registration
	 *     names that are used to register listeners. Values of this mapping must
	 *     be objects that contain `registrationName` or `phasedRegistrationNames`.
	 *
	 *   `executeDispatch` {function(object, function, string)}
	 *     Optional, allows plugins to override how an event gets dispatched. By
	 *     default, the listener is simply invoked.
	 *
	 * Each plugin that is injected into `EventsPluginHub` is immediately operable.
	 *
	 * @public
	 */
	var EventPluginHub = {

	  /**
	   * Methods for injecting dependencies.
	   */
	  injection: {

	    /**
	     * @param {object} InjectedMount
	     * @public
	     */
	    injectMount: EventPluginUtils.injection.injectMount,

	    /**
	     * @param {object} InjectedInstanceHandle
	     * @public
	     */
	    injectInstanceHandle: function (InjectedInstanceHandle) {
	      InstanceHandle = InjectedInstanceHandle;
	      if (process.env.NODE_ENV !== 'production') {
	        validateInstanceHandle();
	      }
	    },

	    getInstanceHandle: function () {
	      if (process.env.NODE_ENV !== 'production') {
	        validateInstanceHandle();
	      }
	      return InstanceHandle;
	    },

	    /**
	     * @param {array} InjectedEventPluginOrder
	     * @public
	     */
	    injectEventPluginOrder: EventPluginRegistry.injectEventPluginOrder,

	    /**
	     * @param {object} injectedNamesToPlugins Map from names to plugin modules.
	     */
	    injectEventPluginsByName: EventPluginRegistry.injectEventPluginsByName

	  },

	  eventNameDispatchConfigs: EventPluginRegistry.eventNameDispatchConfigs,

	  registrationNameModules: EventPluginRegistry.registrationNameModules,

	  /**
	   * Stores `listener` at `listenerBank[registrationName][id]`. Is idempotent.
	   *
	   * @param {string} id ID of the DOM element.
	   * @param {string} registrationName Name of listener (e.g. `onClick`).
	   * @param {?function} listener The callback to store.
	   */
	  putListener: function (id, registrationName, listener) {
	    !(typeof listener === 'function') ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Expected %s listener to be a function, instead got type %s', registrationName, typeof listener) : invariant(false) : undefined;

	    var bankForRegistrationName = listenerBank[registrationName] || (listenerBank[registrationName] = {});
	    bankForRegistrationName[id] = listener;

	    var PluginModule = EventPluginRegistry.registrationNameModules[registrationName];
	    if (PluginModule && PluginModule.didPutListener) {
	      PluginModule.didPutListener(id, registrationName, listener);
	    }
	  },

	  /**
	   * @param {string} id ID of the DOM element.
	   * @param {string} registrationName Name of listener (e.g. `onClick`).
	   * @return {?function} The stored callback.
	   */
	  getListener: function (id, registrationName) {
	    var bankForRegistrationName = listenerBank[registrationName];
	    return bankForRegistrationName && bankForRegistrationName[id];
	  },

	  /**
	   * Deletes a listener from the registration bank.
	   *
	   * @param {string} id ID of the DOM element.
	   * @param {string} registrationName Name of listener (e.g. `onClick`).
	   */
	  deleteListener: function (id, registrationName) {
	    var PluginModule = EventPluginRegistry.registrationNameModules[registrationName];
	    if (PluginModule && PluginModule.willDeleteListener) {
	      PluginModule.willDeleteListener(id, registrationName);
	    }

	    var bankForRegistrationName = listenerBank[registrationName];
	    // TODO: This should never be null -- when is it?
	    if (bankForRegistrationName) {
	      delete bankForRegistrationName[id];
	    }
	  },

	  /**
	   * Deletes all listeners for the DOM element with the supplied ID.
	   *
	   * @param {string} id ID of the DOM element.
	   */
	  deleteAllListeners: function (id) {
	    for (var registrationName in listenerBank) {
	      if (!listenerBank[registrationName][id]) {
	        continue;
	      }

	      var PluginModule = EventPluginRegistry.registrationNameModules[registrationName];
	      if (PluginModule && PluginModule.willDeleteListener) {
	        PluginModule.willDeleteListener(id, registrationName);
	      }

	      delete listenerBank[registrationName][id];
	    }
	  },

	  /**
	   * Allows registered plugins an opportunity to extract events from top-level
	   * native browser events.
	   *
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {DOMEventTarget} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native browser event.
	   * @return {*} An accumulation of synthetic events.
	   * @internal
	   */
	  extractEvents: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
	    var events;
	    var plugins = EventPluginRegistry.plugins;
	    for (var i = 0; i < plugins.length; i++) {
	      // Not every plugin in the ordering may be loaded at runtime.
	      var possiblePlugin = plugins[i];
	      if (possiblePlugin) {
	        var extractedEvents = possiblePlugin.extractEvents(topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget);
	        if (extractedEvents) {
	          events = accumulateInto(events, extractedEvents);
	        }
	      }
	    }
	    return events;
	  },

	  /**
	   * Enqueues a synthetic event that should be dispatched when
	   * `processEventQueue` is invoked.
	   *
	   * @param {*} events An accumulation of synthetic events.
	   * @internal
	   */
	  enqueueEvents: function (events) {
	    if (events) {
	      eventQueue = accumulateInto(eventQueue, events);
	    }
	  },

	  /**
	   * Dispatches all synthetic events on the event queue.
	   *
	   * @internal
	   */
	  processEventQueue: function (simulated) {
	    // Set `eventQueue` to null before processing it so that we can tell if more
	    // events get enqueued while processing.
	    var processingEventQueue = eventQueue;
	    eventQueue = null;
	    if (simulated) {
	      forEachAccumulated(processingEventQueue, executeDispatchesAndReleaseSimulated);
	    } else {
	      forEachAccumulated(processingEventQueue, executeDispatchesAndReleaseTopLevel);
	    }
	    !!eventQueue ? process.env.NODE_ENV !== 'production' ? invariant(false, 'processEventQueue(): Additional events were enqueued while processing ' + 'an event queue. Support for this has not yet been implemented.') : invariant(false) : undefined;
	    // This would be a good time to rethrow if any of the event handlers threw.
	    ReactErrorUtils.rethrowCaughtError();
	  },

	  /**
	   * These are needed for tests only. Do not use!
	   */
	  __purge: function () {
	    listenerBank = {};
	  },

	  __getListenerBank: function () {
	    return listenerBank;
	  }

	};

	module.exports = EventPluginHub;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 35 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule EventPluginRegistry
	 * @typechecks static-only
	 */

	'use strict';

	var invariant = __webpack_require__(16);

	/**
	 * Injectable ordering of event plugins.
	 */
	var EventPluginOrder = null;

	/**
	 * Injectable mapping from names to event plugin modules.
	 */
	var namesToPlugins = {};

	/**
	 * Recomputes the plugin list using the injected plugins and plugin ordering.
	 *
	 * @private
	 */
	function recomputePluginOrdering() {
	  if (!EventPluginOrder) {
	    // Wait until an `EventPluginOrder` is injected.
	    return;
	  }
	  for (var pluginName in namesToPlugins) {
	    var PluginModule = namesToPlugins[pluginName];
	    var pluginIndex = EventPluginOrder.indexOf(pluginName);
	    !(pluginIndex > -1) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'EventPluginRegistry: Cannot inject event plugins that do not exist in ' + 'the plugin ordering, `%s`.', pluginName) : invariant(false) : undefined;
	    if (EventPluginRegistry.plugins[pluginIndex]) {
	      continue;
	    }
	    !PluginModule.extractEvents ? process.env.NODE_ENV !== 'production' ? invariant(false, 'EventPluginRegistry: Event plugins must implement an `extractEvents` ' + 'method, but `%s` does not.', pluginName) : invariant(false) : undefined;
	    EventPluginRegistry.plugins[pluginIndex] = PluginModule;
	    var publishedEvents = PluginModule.eventTypes;
	    for (var eventName in publishedEvents) {
	      !publishEventForPlugin(publishedEvents[eventName], PluginModule, eventName) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'EventPluginRegistry: Failed to publish event `%s` for plugin `%s`.', eventName, pluginName) : invariant(false) : undefined;
	    }
	  }
	}

	/**
	 * Publishes an event so that it can be dispatched by the supplied plugin.
	 *
	 * @param {object} dispatchConfig Dispatch configuration for the event.
	 * @param {object} PluginModule Plugin publishing the event.
	 * @return {boolean} True if the event was successfully published.
	 * @private
	 */
	function publishEventForPlugin(dispatchConfig, PluginModule, eventName) {
	  !!EventPluginRegistry.eventNameDispatchConfigs.hasOwnProperty(eventName) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'EventPluginHub: More than one plugin attempted to publish the same ' + 'event name, `%s`.', eventName) : invariant(false) : undefined;
	  EventPluginRegistry.eventNameDispatchConfigs[eventName] = dispatchConfig;

	  var phasedRegistrationNames = dispatchConfig.phasedRegistrationNames;
	  if (phasedRegistrationNames) {
	    for (var phaseName in phasedRegistrationNames) {
	      if (phasedRegistrationNames.hasOwnProperty(phaseName)) {
	        var phasedRegistrationName = phasedRegistrationNames[phaseName];
	        publishRegistrationName(phasedRegistrationName, PluginModule, eventName);
	      }
	    }
	    return true;
	  } else if (dispatchConfig.registrationName) {
	    publishRegistrationName(dispatchConfig.registrationName, PluginModule, eventName);
	    return true;
	  }
	  return false;
	}

	/**
	 * Publishes a registration name that is used to identify dispatched events and
	 * can be used with `EventPluginHub.putListener` to register listeners.
	 *
	 * @param {string} registrationName Registration name to add.
	 * @param {object} PluginModule Plugin publishing the event.
	 * @private
	 */
	function publishRegistrationName(registrationName, PluginModule, eventName) {
	  !!EventPluginRegistry.registrationNameModules[registrationName] ? process.env.NODE_ENV !== 'production' ? invariant(false, 'EventPluginHub: More than one plugin attempted to publish the same ' + 'registration name, `%s`.', registrationName) : invariant(false) : undefined;
	  EventPluginRegistry.registrationNameModules[registrationName] = PluginModule;
	  EventPluginRegistry.registrationNameDependencies[registrationName] = PluginModule.eventTypes[eventName].dependencies;
	}

	/**
	 * Registers plugins so that they can extract and dispatch events.
	 *
	 * @see {EventPluginHub}
	 */
	var EventPluginRegistry = {

	  /**
	   * Ordered list of injected plugins.
	   */
	  plugins: [],

	  /**
	   * Mapping from event name to dispatch config
	   */
	  eventNameDispatchConfigs: {},

	  /**
	   * Mapping from registration name to plugin module
	   */
	  registrationNameModules: {},

	  /**
	   * Mapping from registration name to event name
	   */
	  registrationNameDependencies: {},

	  /**
	   * Injects an ordering of plugins (by plugin name). This allows the ordering
	   * to be decoupled from injection of the actual plugins so that ordering is
	   * always deterministic regardless of packaging, on-the-fly injection, etc.
	   *
	   * @param {array} InjectedEventPluginOrder
	   * @internal
	   * @see {EventPluginHub.injection.injectEventPluginOrder}
	   */
	  injectEventPluginOrder: function (InjectedEventPluginOrder) {
	    !!EventPluginOrder ? process.env.NODE_ENV !== 'production' ? invariant(false, 'EventPluginRegistry: Cannot inject event plugin ordering more than ' + 'once. You are likely trying to load more than one copy of React.') : invariant(false) : undefined;
	    // Clone the ordering so it cannot be dynamically mutated.
	    EventPluginOrder = Array.prototype.slice.call(InjectedEventPluginOrder);
	    recomputePluginOrdering();
	  },

	  /**
	   * Injects plugins to be used by `EventPluginHub`. The plugin names must be
	   * in the ordering injected by `injectEventPluginOrder`.
	   *
	   * Plugins can be injected as part of page initialization or on-the-fly.
	   *
	   * @param {object} injectedNamesToPlugins Map from names to plugin modules.
	   * @internal
	   * @see {EventPluginHub.injection.injectEventPluginsByName}
	   */
	  injectEventPluginsByName: function (injectedNamesToPlugins) {
	    var isOrderingDirty = false;
	    for (var pluginName in injectedNamesToPlugins) {
	      if (!injectedNamesToPlugins.hasOwnProperty(pluginName)) {
	        continue;
	      }
	      var PluginModule = injectedNamesToPlugins[pluginName];
	      if (!namesToPlugins.hasOwnProperty(pluginName) || namesToPlugins[pluginName] !== PluginModule) {
	        !!namesToPlugins[pluginName] ? process.env.NODE_ENV !== 'production' ? invariant(false, 'EventPluginRegistry: Cannot inject two different event plugins ' + 'using the same name, `%s`.', pluginName) : invariant(false) : undefined;
	        namesToPlugins[pluginName] = PluginModule;
	        isOrderingDirty = true;
	      }
	    }
	    if (isOrderingDirty) {
	      recomputePluginOrdering();
	    }
	  },

	  /**
	   * Looks up the plugin for the supplied event.
	   *
	   * @param {object} event A synthetic event.
	   * @return {?object} The plugin that created the supplied event.
	   * @internal
	   */
	  getPluginModuleForEvent: function (event) {
	    var dispatchConfig = event.dispatchConfig;
	    if (dispatchConfig.registrationName) {
	      return EventPluginRegistry.registrationNameModules[dispatchConfig.registrationName] || null;
	    }
	    for (var phase in dispatchConfig.phasedRegistrationNames) {
	      if (!dispatchConfig.phasedRegistrationNames.hasOwnProperty(phase)) {
	        continue;
	      }
	      var PluginModule = EventPluginRegistry.registrationNameModules[dispatchConfig.phasedRegistrationNames[phase]];
	      if (PluginModule) {
	        return PluginModule;
	      }
	    }
	    return null;
	  },

	  /**
	   * Exposed for unit testing.
	   * @private
	   */
	  _resetEventPlugins: function () {
	    EventPluginOrder = null;
	    for (var pluginName in namesToPlugins) {
	      if (namesToPlugins.hasOwnProperty(pluginName)) {
	        delete namesToPlugins[pluginName];
	      }
	    }
	    EventPluginRegistry.plugins.length = 0;

	    var eventNameDispatchConfigs = EventPluginRegistry.eventNameDispatchConfigs;
	    for (var eventName in eventNameDispatchConfigs) {
	      if (eventNameDispatchConfigs.hasOwnProperty(eventName)) {
	        delete eventNameDispatchConfigs[eventName];
	      }
	    }

	    var registrationNameModules = EventPluginRegistry.registrationNameModules;
	    for (var registrationName in registrationNameModules) {
	      if (registrationNameModules.hasOwnProperty(registrationName)) {
	        delete registrationNameModules[registrationName];
	      }
	    }
	  }

	};

	module.exports = EventPluginRegistry;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 36 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule EventPluginUtils
	 */

	'use strict';

	var EventConstants = __webpack_require__(33);
	var ReactErrorUtils = __webpack_require__(37);

	var invariant = __webpack_require__(16);
	var warning = __webpack_require__(28);

	/**
	 * Injected dependencies:
	 */

	/**
	 * - `Mount`: [required] Module that can convert between React dom IDs and
	 *   actual node references.
	 */
	var injection = {
	  Mount: null,
	  injectMount: function (InjectedMount) {
	    injection.Mount = InjectedMount;
	    if (process.env.NODE_ENV !== 'production') {
	      process.env.NODE_ENV !== 'production' ? warning(InjectedMount && InjectedMount.getNode && InjectedMount.getID, 'EventPluginUtils.injection.injectMount(...): Injected Mount ' + 'module is missing getNode or getID.') : undefined;
	    }
	  }
	};

	var topLevelTypes = EventConstants.topLevelTypes;

	function isEndish(topLevelType) {
	  return topLevelType === topLevelTypes.topMouseUp || topLevelType === topLevelTypes.topTouchEnd || topLevelType === topLevelTypes.topTouchCancel;
	}

	function isMoveish(topLevelType) {
	  return topLevelType === topLevelTypes.topMouseMove || topLevelType === topLevelTypes.topTouchMove;
	}
	function isStartish(topLevelType) {
	  return topLevelType === topLevelTypes.topMouseDown || topLevelType === topLevelTypes.topTouchStart;
	}

	var validateEventDispatches;
	if (process.env.NODE_ENV !== 'production') {
	  validateEventDispatches = function (event) {
	    var dispatchListeners = event._dispatchListeners;
	    var dispatchIDs = event._dispatchIDs;

	    var listenersIsArr = Array.isArray(dispatchListeners);
	    var idsIsArr = Array.isArray(dispatchIDs);
	    var IDsLen = idsIsArr ? dispatchIDs.length : dispatchIDs ? 1 : 0;
	    var listenersLen = listenersIsArr ? dispatchListeners.length : dispatchListeners ? 1 : 0;

	    process.env.NODE_ENV !== 'production' ? warning(idsIsArr === listenersIsArr && IDsLen === listenersLen, 'EventPluginUtils: Invalid `event`.') : undefined;
	  };
	}

	/**
	 * Dispatch the event to the listener.
	 * @param {SyntheticEvent} event SyntheticEvent to handle
	 * @param {boolean} simulated If the event is simulated (changes exn behavior)
	 * @param {function} listener Application-level callback
	 * @param {string} domID DOM id to pass to the callback.
	 */
	function executeDispatch(event, simulated, listener, domID) {
	  var type = event.type || 'unknown-event';
	  event.currentTarget = injection.Mount.getNode(domID);
	  if (simulated) {
	    ReactErrorUtils.invokeGuardedCallbackWithCatch(type, listener, event, domID);
	  } else {
	    ReactErrorUtils.invokeGuardedCallback(type, listener, event, domID);
	  }
	  event.currentTarget = null;
	}

	/**
	 * Standard/simple iteration through an event's collected dispatches.
	 */
	function executeDispatchesInOrder(event, simulated) {
	  var dispatchListeners = event._dispatchListeners;
	  var dispatchIDs = event._dispatchIDs;
	  if (process.env.NODE_ENV !== 'production') {
	    validateEventDispatches(event);
	  }
	  if (Array.isArray(dispatchListeners)) {
	    for (var i = 0; i < dispatchListeners.length; i++) {
	      if (event.isPropagationStopped()) {
	        break;
	      }
	      // Listeners and IDs are two parallel arrays that are always in sync.
	      executeDispatch(event, simulated, dispatchListeners[i], dispatchIDs[i]);
	    }
	  } else if (dispatchListeners) {
	    executeDispatch(event, simulated, dispatchListeners, dispatchIDs);
	  }
	  event._dispatchListeners = null;
	  event._dispatchIDs = null;
	}

	/**
	 * Standard/simple iteration through an event's collected dispatches, but stops
	 * at the first dispatch execution returning true, and returns that id.
	 *
	 * @return {?string} id of the first dispatch execution who's listener returns
	 * true, or null if no listener returned true.
	 */
	function executeDispatchesInOrderStopAtTrueImpl(event) {
	  var dispatchListeners = event._dispatchListeners;
	  var dispatchIDs = event._dispatchIDs;
	  if (process.env.NODE_ENV !== 'production') {
	    validateEventDispatches(event);
	  }
	  if (Array.isArray(dispatchListeners)) {
	    for (var i = 0; i < dispatchListeners.length; i++) {
	      if (event.isPropagationStopped()) {
	        break;
	      }
	      // Listeners and IDs are two parallel arrays that are always in sync.
	      if (dispatchListeners[i](event, dispatchIDs[i])) {
	        return dispatchIDs[i];
	      }
	    }
	  } else if (dispatchListeners) {
	    if (dispatchListeners(event, dispatchIDs)) {
	      return dispatchIDs;
	    }
	  }
	  return null;
	}

	/**
	 * @see executeDispatchesInOrderStopAtTrueImpl
	 */
	function executeDispatchesInOrderStopAtTrue(event) {
	  var ret = executeDispatchesInOrderStopAtTrueImpl(event);
	  event._dispatchIDs = null;
	  event._dispatchListeners = null;
	  return ret;
	}

	/**
	 * Execution of a "direct" dispatch - there must be at most one dispatch
	 * accumulated on the event or it is considered an error. It doesn't really make
	 * sense for an event with multiple dispatches (bubbled) to keep track of the
	 * return values at each dispatch execution, but it does tend to make sense when
	 * dealing with "direct" dispatches.
	 *
	 * @return {*} The return value of executing the single dispatch.
	 */
	function executeDirectDispatch(event) {
	  if (process.env.NODE_ENV !== 'production') {
	    validateEventDispatches(event);
	  }
	  var dispatchListener = event._dispatchListeners;
	  var dispatchID = event._dispatchIDs;
	  !!Array.isArray(dispatchListener) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'executeDirectDispatch(...): Invalid `event`.') : invariant(false) : undefined;
	  var res = dispatchListener ? dispatchListener(event, dispatchID) : null;
	  event._dispatchListeners = null;
	  event._dispatchIDs = null;
	  return res;
	}

	/**
	 * @param {SyntheticEvent} event
	 * @return {boolean} True iff number of dispatches accumulated is greater than 0.
	 */
	function hasDispatches(event) {
	  return !!event._dispatchListeners;
	}

	/**
	 * General utilities that are useful in creating custom Event Plugins.
	 */
	var EventPluginUtils = {
	  isEndish: isEndish,
	  isMoveish: isMoveish,
	  isStartish: isStartish,

	  executeDirectDispatch: executeDirectDispatch,
	  executeDispatchesInOrder: executeDispatchesInOrder,
	  executeDispatchesInOrderStopAtTrue: executeDispatchesInOrderStopAtTrue,
	  hasDispatches: hasDispatches,

	  getNode: function (id) {
	    return injection.Mount.getNode(id);
	  },
	  getID: function (node) {
	    return injection.Mount.getID(node);
	  },

	  injection: injection
	};

	module.exports = EventPluginUtils;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 37 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactErrorUtils
	 * @typechecks
	 */

	'use strict';

	var caughtError = null;

	/**
	 * Call a function while guarding against errors that happens within it.
	 *
	 * @param {?String} name of the guard to use for logging or debugging
	 * @param {Function} func The function to invoke
	 * @param {*} a First argument
	 * @param {*} b Second argument
	 */
	function invokeGuardedCallback(name, func, a, b) {
	  try {
	    return func(a, b);
	  } catch (x) {
	    if (caughtError === null) {
	      caughtError = x;
	    }
	    return undefined;
	  }
	}

	var ReactErrorUtils = {
	  invokeGuardedCallback: invokeGuardedCallback,

	  /**
	   * Invoked by ReactTestUtils.Simulate so that any errors thrown by the event
	   * handler are sure to be rethrown by rethrowCaughtError.
	   */
	  invokeGuardedCallbackWithCatch: invokeGuardedCallback,

	  /**
	   * During execution of guarded functions we will capture the first error which
	   * we will rethrow to be handled by the top level error handler.
	   */
	  rethrowCaughtError: function () {
	    if (caughtError) {
	      var error = caughtError;
	      caughtError = null;
	      throw error;
	    }
	  }
	};

	if (process.env.NODE_ENV !== 'production') {
	  /**
	   * To help development we can get better devtools integration by simulating a
	   * real browser event.
	   */
	  if (typeof window !== 'undefined' && typeof window.dispatchEvent === 'function' && typeof document !== 'undefined' && typeof document.createEvent === 'function') {
	    var fakeNode = document.createElement('react');
	    ReactErrorUtils.invokeGuardedCallback = function (name, func, a, b) {
	      var boundFunc = func.bind(null, a, b);
	      var evtType = 'react-' + name;
	      fakeNode.addEventListener(evtType, boundFunc, false);
	      var evt = document.createEvent('Event');
	      evt.initEvent(evtType, false, false);
	      fakeNode.dispatchEvent(evt);
	      fakeNode.removeEventListener(evtType, boundFunc, false);
	    };
	  }
	}

	module.exports = ReactErrorUtils;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 38 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule accumulateInto
	 */

	'use strict';

	var invariant = __webpack_require__(16);

	/**
	 *
	 * Accumulates items that must not be null or undefined into the first one. This
	 * is used to conserve memory by avoiding array allocations, and thus sacrifices
	 * API cleanness. Since `current` can be null before being passed in and not
	 * null after this function, make sure to assign it back to `current`:
	 *
	 * `a = accumulateInto(a, b);`
	 *
	 * This API should be sparingly used. Try `accumulate` for something cleaner.
	 *
	 * @return {*|array<*>} An accumulation of items.
	 */

	function accumulateInto(current, next) {
	  !(next != null) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'accumulateInto(...): Accumulated items must not be null or undefined.') : invariant(false) : undefined;
	  if (current == null) {
	    return next;
	  }

	  // Both are not empty. Warning: Never call x.concat(y) when you are not
	  // certain that x is an Array (x could be a string with concat method).
	  var currentIsArray = Array.isArray(current);
	  var nextIsArray = Array.isArray(next);

	  if (currentIsArray && nextIsArray) {
	    current.push.apply(current, next);
	    return current;
	  }

	  if (currentIsArray) {
	    current.push(next);
	    return current;
	  }

	  if (nextIsArray) {
	    // A bit too dangerous to mutate `next`.
	    return [current].concat(next);
	  }

	  return [current, next];
	}

	module.exports = accumulateInto;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 39 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule forEachAccumulated
	 */

	'use strict';

	/**
	 * @param {array} arr an "accumulation" of items which is either an Array or
	 * a single item. Useful when paired with the `accumulate` module. This is a
	 * simple utility that allows us to reason about a collection of items, but
	 * handling the case when there is exactly one item (and we do not need to
	 * allocate an array).
	 */
	var forEachAccumulated = function (arr, cb, scope) {
	  if (Array.isArray(arr)) {
	    arr.forEach(cb, scope);
	  } else if (arr) {
	    cb.call(scope, arr);
	  }
	};

	module.exports = forEachAccumulated;

/***/ },
/* 40 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactEventEmitterMixin
	 */

	'use strict';

	var EventPluginHub = __webpack_require__(34);

	function runEventQueueInBatch(events) {
	  EventPluginHub.enqueueEvents(events);
	  EventPluginHub.processEventQueue(false);
	}

	var ReactEventEmitterMixin = {

	  /**
	   * Streams a fired top-level event to `EventPluginHub` where plugins have the
	   * opportunity to create `ReactEvent`s to be dispatched.
	   *
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {object} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native environment event.
	   */
	  handleTopLevel: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
	    var events = EventPluginHub.extractEvents(topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget);
	    runEventQueueInBatch(events);
	  }
	};

	module.exports = ReactEventEmitterMixin;

/***/ },
/* 41 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ViewportMetrics
	 */

	'use strict';

	var ViewportMetrics = {

	  currentScrollLeft: 0,

	  currentScrollTop: 0,

	  refreshScrollValues: function (scrollPosition) {
	    ViewportMetrics.currentScrollLeft = scrollPosition.x;
	    ViewportMetrics.currentScrollTop = scrollPosition.y;
	  }

	};

	module.exports = ViewportMetrics;

/***/ },
/* 42 */
/***/ function(module, exports) {

	/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule Object.assign
	 */

	// https://people.mozilla.org/~jorendorff/es6-draft.html#sec-object.assign

	'use strict';

	function assign(target, sources) {
	  if (target == null) {
	    throw new TypeError('Object.assign target cannot be null or undefined');
	  }

	  var to = Object(target);
	  var hasOwnProperty = Object.prototype.hasOwnProperty;

	  for (var nextIndex = 1; nextIndex < arguments.length; nextIndex++) {
	    var nextSource = arguments[nextIndex];
	    if (nextSource == null) {
	      continue;
	    }

	    var from = Object(nextSource);

	    // We don't currently support accessors nor proxies. Therefore this
	    // copy cannot throw. If we ever supported this then we must handle
	    // exceptions and side-effects. We don't support symbols so they won't
	    // be transferred.

	    for (var key in from) {
	      if (hasOwnProperty.call(from, key)) {
	        to[key] = from[key];
	      }
	    }
	  }

	  return to;
	}

	module.exports = assign;

/***/ },
/* 43 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule isEventSupported
	 */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(12);

	var useHasFeature;
	if (ExecutionEnvironment.canUseDOM) {
	  useHasFeature = document.implementation && document.implementation.hasFeature &&
	  // always returns true in newer browsers as per the standard.
	  // @see http://dom.spec.whatwg.org/#dom-domimplementation-hasfeature
	  document.implementation.hasFeature('', '') !== true;
	}

	/**
	 * Checks if an event is supported in the current execution environment.
	 *
	 * NOTE: This will not work correctly for non-generic events such as `change`,
	 * `reset`, `load`, `error`, and `select`.
	 *
	 * Borrows from Modernizr.
	 *
	 * @param {string} eventNameSuffix Event name, e.g. "click".
	 * @param {?boolean} capture Check if the capture phase is supported.
	 * @return {boolean} True if the event is supported.
	 * @internal
	 * @license Modernizr 3.0.0pre (Custom Build) | MIT
	 */
	function isEventSupported(eventNameSuffix, capture) {
	  if (!ExecutionEnvironment.canUseDOM || capture && !('addEventListener' in document)) {
	    return false;
	  }

	  var eventName = 'on' + eventNameSuffix;
	  var isSupported = (eventName in document);

	  if (!isSupported) {
	    var element = document.createElement('div');
	    element.setAttribute(eventName, 'return;');
	    isSupported = typeof element[eventName] === 'function';
	  }

	  if (!isSupported && useHasFeature && eventNameSuffix === 'wheel') {
	    // This is the only way to test support for the `wheel` event in IE9+.
	    isSupported = document.implementation.hasFeature('Events.wheel', '3.0');
	  }

	  return isSupported;
	}

	module.exports = isEventSupported;

/***/ },
/* 44 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMFeatureFlags
	 */

	'use strict';

	var ReactDOMFeatureFlags = {
	  useCreateElement: false
	};

	module.exports = ReactDOMFeatureFlags;

/***/ },
/* 45 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactElement
	 */

	'use strict';

	var ReactCurrentOwner = __webpack_require__(8);

	var assign = __webpack_require__(42);
	var canDefineProperty = __webpack_require__(46);

	// The Symbol used to tag the ReactElement type. If there is no native Symbol
	// nor polyfill, then a plain number is used for performance.
	var REACT_ELEMENT_TYPE = typeof Symbol === 'function' && Symbol['for'] && Symbol['for']('react.element') || 0xeac7;

	var RESERVED_PROPS = {
	  key: true,
	  ref: true,
	  __self: true,
	  __source: true
	};

	/**
	 * Base constructor for all React elements. This is only used to make this
	 * work with a dynamic instanceof check. Nothing should live on this prototype.
	 *
	 * @param {*} type
	 * @param {*} key
	 * @param {string|object} ref
	 * @param {*} self A *temporary* helper to detect places where `this` is
	 * different from the `owner` when React.createElement is called, so that we
	 * can warn. We want to get rid of owner and replace string `ref`s with arrow
	 * functions, and as long as `this` and owner are the same, there will be no
	 * change in behavior.
	 * @param {*} source An annotation object (added by a transpiler or otherwise)
	 * indicating filename, line number, and/or other information.
	 * @param {*} owner
	 * @param {*} props
	 * @internal
	 */
	var ReactElement = function (type, key, ref, self, source, owner, props) {
	  var element = {
	    // This tag allow us to uniquely identify this as a React Element
	    $$typeof: REACT_ELEMENT_TYPE,

	    // Built-in properties that belong on the element
	    type: type,
	    key: key,
	    ref: ref,
	    props: props,

	    // Record the component responsible for creating this element.
	    _owner: owner
	  };

	  if (process.env.NODE_ENV !== 'production') {
	    // The validation flag is currently mutative. We put it on
	    // an external backing store so that we can freeze the whole object.
	    // This can be replaced with a WeakMap once they are implemented in
	    // commonly used development environments.
	    element._store = {};

	    // To make comparing ReactElements easier for testing purposes, we make
	    // the validation flag non-enumerable (where possible, which should
	    // include every environment we run tests in), so the test framework
	    // ignores it.
	    if (canDefineProperty) {
	      Object.defineProperty(element._store, 'validated', {
	        configurable: false,
	        enumerable: false,
	        writable: true,
	        value: false
	      });
	      // self and source are DEV only properties.
	      Object.defineProperty(element, '_self', {
	        configurable: false,
	        enumerable: false,
	        writable: false,
	        value: self
	      });
	      // Two elements created in two different places should be considered
	      // equal for testing purposes and therefore we hide it from enumeration.
	      Object.defineProperty(element, '_source', {
	        configurable: false,
	        enumerable: false,
	        writable: false,
	        value: source
	      });
	    } else {
	      element._store.validated = false;
	      element._self = self;
	      element._source = source;
	    }
	    Object.freeze(element.props);
	    Object.freeze(element);
	  }

	  return element;
	};

	ReactElement.createElement = function (type, config, children) {
	  var propName;

	  // Reserved names are extracted
	  var props = {};

	  var key = null;
	  var ref = null;
	  var self = null;
	  var source = null;

	  if (config != null) {
	    ref = config.ref === undefined ? null : config.ref;
	    key = config.key === undefined ? null : '' + config.key;
	    self = config.__self === undefined ? null : config.__self;
	    source = config.__source === undefined ? null : config.__source;
	    // Remaining properties are added to a new props object
	    for (propName in config) {
	      if (config.hasOwnProperty(propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
	        props[propName] = config[propName];
	      }
	    }
	  }

	  // Children can be more than one argument, and those are transferred onto
	  // the newly allocated props object.
	  var childrenLength = arguments.length - 2;
	  if (childrenLength === 1) {
	    props.children = children;
	  } else if (childrenLength > 1) {
	    var childArray = Array(childrenLength);
	    for (var i = 0; i < childrenLength; i++) {
	      childArray[i] = arguments[i + 2];
	    }
	    props.children = childArray;
	  }

	  // Resolve default props
	  if (type && type.defaultProps) {
	    var defaultProps = type.defaultProps;
	    for (propName in defaultProps) {
	      if (typeof props[propName] === 'undefined') {
	        props[propName] = defaultProps[propName];
	      }
	    }
	  }

	  return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
	};

	ReactElement.createFactory = function (type) {
	  var factory = ReactElement.createElement.bind(null, type);
	  // Expose the type on the factory and the prototype so that it can be
	  // easily accessed on elements. E.g. `<Foo />.type === Foo`.
	  // This should not be named `constructor` since this may not be the function
	  // that created the element, and it may not even be a constructor.
	  // Legacy hook TODO: Warn if this is accessed
	  factory.type = type;
	  return factory;
	};

	ReactElement.cloneAndReplaceKey = function (oldElement, newKey) {
	  var newElement = ReactElement(oldElement.type, newKey, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, oldElement.props);

	  return newElement;
	};

	ReactElement.cloneAndReplaceProps = function (oldElement, newProps) {
	  var newElement = ReactElement(oldElement.type, oldElement.key, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, newProps);

	  if (process.env.NODE_ENV !== 'production') {
	    // If the key on the original is valid, then the clone is valid
	    newElement._store.validated = oldElement._store.validated;
	  }

	  return newElement;
	};

	ReactElement.cloneElement = function (element, config, children) {
	  var propName;

	  // Original props are copied
	  var props = assign({}, element.props);

	  // Reserved names are extracted
	  var key = element.key;
	  var ref = element.ref;
	  // Self is preserved since the owner is preserved.
	  var self = element._self;
	  // Source is preserved since cloneElement is unlikely to be targeted by a
	  // transpiler, and the original source is probably a better indicator of the
	  // true owner.
	  var source = element._source;

	  // Owner will be preserved, unless ref is overridden
	  var owner = element._owner;

	  if (config != null) {
	    if (config.ref !== undefined) {
	      // Silently steal the ref from the parent.
	      ref = config.ref;
	      owner = ReactCurrentOwner.current;
	    }
	    if (config.key !== undefined) {
	      key = '' + config.key;
	    }
	    // Remaining properties override existing props
	    for (propName in config) {
	      if (config.hasOwnProperty(propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
	        props[propName] = config[propName];
	      }
	    }
	  }

	  // Children can be more than one argument, and those are transferred onto
	  // the newly allocated props object.
	  var childrenLength = arguments.length - 2;
	  if (childrenLength === 1) {
	    props.children = children;
	  } else if (childrenLength > 1) {
	    var childArray = Array(childrenLength);
	    for (var i = 0; i < childrenLength; i++) {
	      childArray[i] = arguments[i + 2];
	    }
	    props.children = childArray;
	  }

	  return ReactElement(element.type, key, ref, self, source, owner, props);
	};

	/**
	 * @param {?object} object
	 * @return {boolean} True if `object` is a valid component.
	 * @final
	 */
	ReactElement.isValidElement = function (object) {
	  return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
	};

	module.exports = ReactElement;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 46 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule canDefineProperty
	 */

	'use strict';

	var canDefineProperty = false;
	if (process.env.NODE_ENV !== 'production') {
	  try {
	    Object.defineProperty({}, 'x', { get: function () {} });
	    canDefineProperty = true;
	  } catch (x) {
	    // IE will fail on defineProperty
	  }
	}

	module.exports = canDefineProperty;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 47 */
/***/ function(module, exports) {

	/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactEmptyComponentRegistry
	 */

	'use strict';

	// This registry keeps track of the React IDs of the components that rendered to
	// `null` (in reality a placeholder such as `noscript`)
	var nullComponentIDsRegistry = {};

	/**
	 * @param {string} id Component's `_rootNodeID`.
	 * @return {boolean} True if the component is rendered to null.
	 */
	function isNullComponentID(id) {
	  return !!nullComponentIDsRegistry[id];
	}

	/**
	 * Mark the component as having rendered to null.
	 * @param {string} id Component's `_rootNodeID`.
	 */
	function registerNullComponentID(id) {
	  nullComponentIDsRegistry[id] = true;
	}

	/**
	 * Unmark the component as having rendered to null: it renders to something now.
	 * @param {string} id Component's `_rootNodeID`.
	 */
	function deregisterNullComponentID(id) {
	  delete nullComponentIDsRegistry[id];
	}

	var ReactEmptyComponentRegistry = {
	  isNullComponentID: isNullComponentID,
	  registerNullComponentID: registerNullComponentID,
	  deregisterNullComponentID: deregisterNullComponentID
	};

	module.exports = ReactEmptyComponentRegistry;

/***/ },
/* 48 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactInstanceHandles
	 * @typechecks static-only
	 */

	'use strict';

	var ReactRootIndex = __webpack_require__(49);

	var invariant = __webpack_require__(16);

	var SEPARATOR = '.';
	var SEPARATOR_LENGTH = SEPARATOR.length;

	/**
	 * Maximum depth of traversals before we consider the possibility of a bad ID.
	 */
	var MAX_TREE_DEPTH = 10000;

	/**
	 * Creates a DOM ID prefix to use when mounting React components.
	 *
	 * @param {number} index A unique integer
	 * @return {string} React root ID.
	 * @internal
	 */
	function getReactRootIDString(index) {
	  return SEPARATOR + index.toString(36);
	}

	/**
	 * Checks if a character in the supplied ID is a separator or the end.
	 *
	 * @param {string} id A React DOM ID.
	 * @param {number} index Index of the character to check.
	 * @return {boolean} True if the character is a separator or end of the ID.
	 * @private
	 */
	function isBoundary(id, index) {
	  return id.charAt(index) === SEPARATOR || index === id.length;
	}

	/**
	 * Checks if the supplied string is a valid React DOM ID.
	 *
	 * @param {string} id A React DOM ID, maybe.
	 * @return {boolean} True if the string is a valid React DOM ID.
	 * @private
	 */
	function isValidID(id) {
	  return id === '' || id.charAt(0) === SEPARATOR && id.charAt(id.length - 1) !== SEPARATOR;
	}

	/**
	 * Checks if the first ID is an ancestor of or equal to the second ID.
	 *
	 * @param {string} ancestorID
	 * @param {string} descendantID
	 * @return {boolean} True if `ancestorID` is an ancestor of `descendantID`.
	 * @internal
	 */
	function isAncestorIDOf(ancestorID, descendantID) {
	  return descendantID.indexOf(ancestorID) === 0 && isBoundary(descendantID, ancestorID.length);
	}

	/**
	 * Gets the parent ID of the supplied React DOM ID, `id`.
	 *
	 * @param {string} id ID of a component.
	 * @return {string} ID of the parent, or an empty string.
	 * @private
	 */
	function getParentID(id) {
	  return id ? id.substr(0, id.lastIndexOf(SEPARATOR)) : '';
	}

	/**
	 * Gets the next DOM ID on the tree path from the supplied `ancestorID` to the
	 * supplied `destinationID`. If they are equal, the ID is returned.
	 *
	 * @param {string} ancestorID ID of an ancestor node of `destinationID`.
	 * @param {string} destinationID ID of the destination node.
	 * @return {string} Next ID on the path from `ancestorID` to `destinationID`.
	 * @private
	 */
	function getNextDescendantID(ancestorID, destinationID) {
	  !(isValidID(ancestorID) && isValidID(destinationID)) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'getNextDescendantID(%s, %s): Received an invalid React DOM ID.', ancestorID, destinationID) : invariant(false) : undefined;
	  !isAncestorIDOf(ancestorID, destinationID) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'getNextDescendantID(...): React has made an invalid assumption about ' + 'the DOM hierarchy. Expected `%s` to be an ancestor of `%s`.', ancestorID, destinationID) : invariant(false) : undefined;
	  if (ancestorID === destinationID) {
	    return ancestorID;
	  }
	  // Skip over the ancestor and the immediate separator. Traverse until we hit
	  // another separator or we reach the end of `destinationID`.
	  var start = ancestorID.length + SEPARATOR_LENGTH;
	  var i;
	  for (i = start; i < destinationID.length; i++) {
	    if (isBoundary(destinationID, i)) {
	      break;
	    }
	  }
	  return destinationID.substr(0, i);
	}

	/**
	 * Gets the nearest common ancestor ID of two IDs.
	 *
	 * Using this ID scheme, the nearest common ancestor ID is the longest common
	 * prefix of the two IDs that immediately preceded a "marker" in both strings.
	 *
	 * @param {string} oneID
	 * @param {string} twoID
	 * @return {string} Nearest common ancestor ID, or the empty string if none.
	 * @private
	 */
	function getFirstCommonAncestorID(oneID, twoID) {
	  var minLength = Math.min(oneID.length, twoID.length);
	  if (minLength === 0) {
	    return '';
	  }
	  var lastCommonMarkerIndex = 0;
	  // Use `<=` to traverse until the "EOL" of the shorter string.
	  for (var i = 0; i <= minLength; i++) {
	    if (isBoundary(oneID, i) && isBoundary(twoID, i)) {
	      lastCommonMarkerIndex = i;
	    } else if (oneID.charAt(i) !== twoID.charAt(i)) {
	      break;
	    }
	  }
	  var longestCommonID = oneID.substr(0, lastCommonMarkerIndex);
	  !isValidID(longestCommonID) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'getFirstCommonAncestorID(%s, %s): Expected a valid React DOM ID: %s', oneID, twoID, longestCommonID) : invariant(false) : undefined;
	  return longestCommonID;
	}

	/**
	 * Traverses the parent path between two IDs (either up or down). The IDs must
	 * not be the same, and there must exist a parent path between them. If the
	 * callback returns `false`, traversal is stopped.
	 *
	 * @param {?string} start ID at which to start traversal.
	 * @param {?string} stop ID at which to end traversal.
	 * @param {function} cb Callback to invoke each ID with.
	 * @param {*} arg Argument to invoke the callback with.
	 * @param {?boolean} skipFirst Whether or not to skip the first node.
	 * @param {?boolean} skipLast Whether or not to skip the last node.
	 * @private
	 */
	function traverseParentPath(start, stop, cb, arg, skipFirst, skipLast) {
	  start = start || '';
	  stop = stop || '';
	  !(start !== stop) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'traverseParentPath(...): Cannot traverse from and to the same ID, `%s`.', start) : invariant(false) : undefined;
	  var traverseUp = isAncestorIDOf(stop, start);
	  !(traverseUp || isAncestorIDOf(start, stop)) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'traverseParentPath(%s, %s, ...): Cannot traverse from two IDs that do ' + 'not have a parent path.', start, stop) : invariant(false) : undefined;
	  // Traverse from `start` to `stop` one depth at a time.
	  var depth = 0;
	  var traverse = traverseUp ? getParentID : getNextDescendantID;
	  for (var id = start;; /* until break */id = traverse(id, stop)) {
	    var ret;
	    if ((!skipFirst || id !== start) && (!skipLast || id !== stop)) {
	      ret = cb(id, traverseUp, arg);
	    }
	    if (ret === false || id === stop) {
	      // Only break //after// visiting `stop`.
	      break;
	    }
	    !(depth++ < MAX_TREE_DEPTH) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'traverseParentPath(%s, %s, ...): Detected an infinite loop while ' + 'traversing the React DOM ID tree. This may be due to malformed IDs: %s', start, stop, id) : invariant(false) : undefined;
	  }
	}

	/**
	 * Manages the IDs assigned to DOM representations of React components. This
	 * uses a specific scheme in order to traverse the DOM efficiently (e.g. in
	 * order to simulate events).
	 *
	 * @internal
	 */
	var ReactInstanceHandles = {

	  /**
	   * Constructs a React root ID
	   * @return {string} A React root ID.
	   */
	  createReactRootID: function () {
	    return getReactRootIDString(ReactRootIndex.createReactRootIndex());
	  },

	  /**
	   * Constructs a React ID by joining a root ID with a name.
	   *
	   * @param {string} rootID Root ID of a parent component.
	   * @param {string} name A component's name (as flattened children).
	   * @return {string} A React ID.
	   * @internal
	   */
	  createReactID: function (rootID, name) {
	    return rootID + name;
	  },

	  /**
	   * Gets the DOM ID of the React component that is the root of the tree that
	   * contains the React component with the supplied DOM ID.
	   *
	   * @param {string} id DOM ID of a React component.
	   * @return {?string} DOM ID of the React component that is the root.
	   * @internal
	   */
	  getReactRootIDFromNodeID: function (id) {
	    if (id && id.charAt(0) === SEPARATOR && id.length > 1) {
	      var index = id.indexOf(SEPARATOR, 1);
	      return index > -1 ? id.substr(0, index) : id;
	    }
	    return null;
	  },

	  /**
	   * Traverses the ID hierarchy and invokes the supplied `cb` on any IDs that
	   * should would receive a `mouseEnter` or `mouseLeave` event.
	   *
	   * NOTE: Does not invoke the callback on the nearest common ancestor because
	   * nothing "entered" or "left" that element.
	   *
	   * @param {string} leaveID ID being left.
	   * @param {string} enterID ID being entered.
	   * @param {function} cb Callback to invoke on each entered/left ID.
	   * @param {*} upArg Argument to invoke the callback with on left IDs.
	   * @param {*} downArg Argument to invoke the callback with on entered IDs.
	   * @internal
	   */
	  traverseEnterLeave: function (leaveID, enterID, cb, upArg, downArg) {
	    var ancestorID = getFirstCommonAncestorID(leaveID, enterID);
	    if (ancestorID !== leaveID) {
	      traverseParentPath(leaveID, ancestorID, cb, upArg, false, true);
	    }
	    if (ancestorID !== enterID) {
	      traverseParentPath(ancestorID, enterID, cb, downArg, true, false);
	    }
	  },

	  /**
	   * Simulates the traversal of a two-phase, capture/bubble event dispatch.
	   *
	   * NOTE: This traversal happens on IDs without touching the DOM.
	   *
	   * @param {string} targetID ID of the target node.
	   * @param {function} cb Callback to invoke.
	   * @param {*} arg Argument to invoke the callback with.
	   * @internal
	   */
	  traverseTwoPhase: function (targetID, cb, arg) {
	    if (targetID) {
	      traverseParentPath('', targetID, cb, arg, true, false);
	      traverseParentPath(targetID, '', cb, arg, false, true);
	    }
	  },

	  /**
	   * Same as `traverseTwoPhase` but skips the `targetID`.
	   */
	  traverseTwoPhaseSkipTarget: function (targetID, cb, arg) {
	    if (targetID) {
	      traverseParentPath('', targetID, cb, arg, true, true);
	      traverseParentPath(targetID, '', cb, arg, true, true);
	    }
	  },

	  /**
	   * Traverse a node ID, calling the supplied `cb` for each ancestor ID. For
	   * example, passing `.0.$row-0.1` would result in `cb` getting called
	   * with `.0`, `.0.$row-0`, and `.0.$row-0.1`.
	   *
	   * NOTE: This traversal happens on IDs without touching the DOM.
	   *
	   * @param {string} targetID ID of the target node.
	   * @param {function} cb Callback to invoke.
	   * @param {*} arg Argument to invoke the callback with.
	   * @internal
	   */
	  traverseAncestors: function (targetID, cb, arg) {
	    traverseParentPath('', targetID, cb, arg, true, false);
	  },

	  getFirstCommonAncestorID: getFirstCommonAncestorID,

	  /**
	   * Exposed for unit testing.
	   * @private
	   */
	  _getNextDescendantID: getNextDescendantID,

	  isAncestorIDOf: isAncestorIDOf,

	  SEPARATOR: SEPARATOR

	};

	module.exports = ReactInstanceHandles;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 49 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactRootIndex
	 * @typechecks
	 */

	'use strict';

	var ReactRootIndexInjection = {
	  /**
	   * @param {function} _createReactRootIndex
	   */
	  injectCreateReactRootIndex: function (_createReactRootIndex) {
	    ReactRootIndex.createReactRootIndex = _createReactRootIndex;
	  }
	};

	var ReactRootIndex = {
	  createReactRootIndex: null,
	  injection: ReactRootIndexInjection
	};

	module.exports = ReactRootIndex;

/***/ },
/* 50 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactInstanceMap
	 */

	'use strict';

	/**
	 * `ReactInstanceMap` maintains a mapping from a public facing stateful
	 * instance (key) and the internal representation (value). This allows public
	 * methods to accept the user facing instance as an argument and map them back
	 * to internal methods.
	 */

	// TODO: Replace this with ES6: var ReactInstanceMap = new Map();
	var ReactInstanceMap = {

	  /**
	   * This API should be called `delete` but we'd have to make sure to always
	   * transform these to strings for IE support. When this transform is fully
	   * supported we can rename it.
	   */
	  remove: function (key) {
	    key._reactInternalInstance = undefined;
	  },

	  get: function (key) {
	    return key._reactInternalInstance;
	  },

	  has: function (key) {
	    return key._reactInternalInstance !== undefined;
	  },

	  set: function (key, value) {
	    key._reactInternalInstance = value;
	  }

	};

	module.exports = ReactInstanceMap;

/***/ },
/* 51 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactMarkupChecksum
	 */

	'use strict';

	var adler32 = __webpack_require__(52);

	var TAG_END = /\/?>/;

	var ReactMarkupChecksum = {
	  CHECKSUM_ATTR_NAME: 'data-react-checksum',

	  /**
	   * @param {string} markup Markup string
	   * @return {string} Markup string with checksum attribute attached
	   */
	  addChecksumToMarkup: function (markup) {
	    var checksum = adler32(markup);

	    // Add checksum (handle both parent tags and self-closing tags)
	    return markup.replace(TAG_END, ' ' + ReactMarkupChecksum.CHECKSUM_ATTR_NAME + '="' + checksum + '"$&');
	  },

	  /**
	   * @param {string} markup to use
	   * @param {DOMElement} element root React element
	   * @returns {boolean} whether or not the markup is the same
	   */
	  canReuseMarkup: function (markup, element) {
	    var existingChecksum = element.getAttribute(ReactMarkupChecksum.CHECKSUM_ATTR_NAME);
	    existingChecksum = existingChecksum && parseInt(existingChecksum, 10);
	    var markupChecksum = adler32(markup);
	    return markupChecksum === existingChecksum;
	  }
	};

	module.exports = ReactMarkupChecksum;

/***/ },
/* 52 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule adler32
	 */

	'use strict';

	var MOD = 65521;

	// adler32 is not cryptographically strong, and is only used to sanity check that
	// markup generated on the server matches the markup generated on the client.
	// This implementation (a modified version of the SheetJS version) has been optimized
	// for our use case, at the expense of conforming to the adler32 specification
	// for non-ascii inputs.
	function adler32(data) {
	  var a = 1;
	  var b = 0;
	  var i = 0;
	  var l = data.length;
	  var m = l & ~0x3;
	  while (i < m) {
	    for (; i < Math.min(i + 4096, m); i += 4) {
	      b += (a += data.charCodeAt(i)) + (a += data.charCodeAt(i + 1)) + (a += data.charCodeAt(i + 2)) + (a += data.charCodeAt(i + 3));
	    }
	    a %= MOD;
	    b %= MOD;
	  }
	  for (; i < l; i++) {
	    b += a += data.charCodeAt(i);
	  }
	  a %= MOD;
	  b %= MOD;
	  return a | b << 16;
	}

	module.exports = adler32;

/***/ },
/* 53 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactReconciler
	 */

	'use strict';

	var ReactRef = __webpack_require__(54);

	/**
	 * Helper to call ReactRef.attachRefs with this composite component, split out
	 * to avoid allocations in the transaction mount-ready queue.
	 */
	function attachRefs() {
	  ReactRef.attachRefs(this, this._currentElement);
	}

	var ReactReconciler = {

	  /**
	   * Initializes the component, renders markup, and registers event listeners.
	   *
	   * @param {ReactComponent} internalInstance
	   * @param {string} rootID DOM ID of the root node.
	   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
	   * @return {?string} Rendered markup to be inserted into the DOM.
	   * @final
	   * @internal
	   */
	  mountComponent: function (internalInstance, rootID, transaction, context) {
	    var markup = internalInstance.mountComponent(rootID, transaction, context);
	    if (internalInstance._currentElement && internalInstance._currentElement.ref != null) {
	      transaction.getReactMountReady().enqueue(attachRefs, internalInstance);
	    }
	    return markup;
	  },

	  /**
	   * Releases any resources allocated by `mountComponent`.
	   *
	   * @final
	   * @internal
	   */
	  unmountComponent: function (internalInstance) {
	    ReactRef.detachRefs(internalInstance, internalInstance._currentElement);
	    internalInstance.unmountComponent();
	  },

	  /**
	   * Update a component using a new element.
	   *
	   * @param {ReactComponent} internalInstance
	   * @param {ReactElement} nextElement
	   * @param {ReactReconcileTransaction} transaction
	   * @param {object} context
	   * @internal
	   */
	  receiveComponent: function (internalInstance, nextElement, transaction, context) {
	    var prevElement = internalInstance._currentElement;

	    if (nextElement === prevElement && context === internalInstance._context) {
	      // Since elements are immutable after the owner is rendered,
	      // we can do a cheap identity compare here to determine if this is a
	      // superfluous reconcile. It's possible for state to be mutable but such
	      // change should trigger an update of the owner which would recreate
	      // the element. We explicitly check for the existence of an owner since
	      // it's possible for an element created outside a composite to be
	      // deeply mutated and reused.

	      // TODO: Bailing out early is just a perf optimization right?
	      // TODO: Removing the return statement should affect correctness?
	      return;
	    }

	    var refsChanged = ReactRef.shouldUpdateRefs(prevElement, nextElement);

	    if (refsChanged) {
	      ReactRef.detachRefs(internalInstance, prevElement);
	    }

	    internalInstance.receiveComponent(nextElement, transaction, context);

	    if (refsChanged && internalInstance._currentElement && internalInstance._currentElement.ref != null) {
	      transaction.getReactMountReady().enqueue(attachRefs, internalInstance);
	    }
	  },

	  /**
	   * Flush any dirty changes in a component.
	   *
	   * @param {ReactComponent} internalInstance
	   * @param {ReactReconcileTransaction} transaction
	   * @internal
	   */
	  performUpdateIfNecessary: function (internalInstance, transaction) {
	    internalInstance.performUpdateIfNecessary(transaction);
	  }

	};

	module.exports = ReactReconciler;

/***/ },
/* 54 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactRef
	 */

	'use strict';

	var ReactOwner = __webpack_require__(55);

	var ReactRef = {};

	function attachRef(ref, component, owner) {
	  if (typeof ref === 'function') {
	    ref(component.getPublicInstance());
	  } else {
	    // Legacy ref
	    ReactOwner.addComponentAsRefTo(component, ref, owner);
	  }
	}

	function detachRef(ref, component, owner) {
	  if (typeof ref === 'function') {
	    ref(null);
	  } else {
	    // Legacy ref
	    ReactOwner.removeComponentAsRefFrom(component, ref, owner);
	  }
	}

	ReactRef.attachRefs = function (instance, element) {
	  if (element === null || element === false) {
	    return;
	  }
	  var ref = element.ref;
	  if (ref != null) {
	    attachRef(ref, instance, element._owner);
	  }
	};

	ReactRef.shouldUpdateRefs = function (prevElement, nextElement) {
	  // If either the owner or a `ref` has changed, make sure the newest owner
	  // has stored a reference to `this`, and the previous owner (if different)
	  // has forgotten the reference to `this`. We use the element instead
	  // of the public this.props because the post processing cannot determine
	  // a ref. The ref conceptually lives on the element.

	  // TODO: Should this even be possible? The owner cannot change because
	  // it's forbidden by shouldUpdateReactComponent. The ref can change
	  // if you swap the keys of but not the refs. Reconsider where this check
	  // is made. It probably belongs where the key checking and
	  // instantiateReactComponent is done.

	  var prevEmpty = prevElement === null || prevElement === false;
	  var nextEmpty = nextElement === null || nextElement === false;

	  return(
	    // This has a few false positives w/r/t empty components.
	    prevEmpty || nextEmpty || nextElement._owner !== prevElement._owner || nextElement.ref !== prevElement.ref
	  );
	};

	ReactRef.detachRefs = function (instance, element) {
	  if (element === null || element === false) {
	    return;
	  }
	  var ref = element.ref;
	  if (ref != null) {
	    detachRef(ref, instance, element._owner);
	  }
	};

	module.exports = ReactRef;

/***/ },
/* 55 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactOwner
	 */

	'use strict';

	var invariant = __webpack_require__(16);

	/**
	 * ReactOwners are capable of storing references to owned components.
	 *
	 * All components are capable of //being// referenced by owner components, but
	 * only ReactOwner components are capable of //referencing// owned components.
	 * The named reference is known as a "ref".
	 *
	 * Refs are available when mounted and updated during reconciliation.
	 *
	 *   var MyComponent = React.createClass({
	 *     render: function() {
	 *       return (
	 *         <div onClick={this.handleClick}>
	 *           <CustomComponent ref="custom" />
	 *         </div>
	 *       );
	 *     },
	 *     handleClick: function() {
	 *       this.refs.custom.handleClick();
	 *     },
	 *     componentDidMount: function() {
	 *       this.refs.custom.initialize();
	 *     }
	 *   });
	 *
	 * Refs should rarely be used. When refs are used, they should only be done to
	 * control data that is not handled by React's data flow.
	 *
	 * @class ReactOwner
	 */
	var ReactOwner = {

	  /**
	   * @param {?object} object
	   * @return {boolean} True if `object` is a valid owner.
	   * @final
	   */
	  isValidOwner: function (object) {
	    return !!(object && typeof object.attachRef === 'function' && typeof object.detachRef === 'function');
	  },

	  /**
	   * Adds a component by ref to an owner component.
	   *
	   * @param {ReactComponent} component Component to reference.
	   * @param {string} ref Name by which to refer to the component.
	   * @param {ReactOwner} owner Component on which to record the ref.
	   * @final
	   * @internal
	   */
	  addComponentAsRefTo: function (component, ref, owner) {
	    !ReactOwner.isValidOwner(owner) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'addComponentAsRefTo(...): Only a ReactOwner can have refs. You might ' + 'be adding a ref to a component that was not created inside a component\'s ' + '`render` method, or you have multiple copies of React loaded ' + '(details: https://fb.me/react-refs-must-have-owner).') : invariant(false) : undefined;
	    owner.attachRef(ref, component);
	  },

	  /**
	   * Removes a component by ref from an owner component.
	   *
	   * @param {ReactComponent} component Component to dereference.
	   * @param {string} ref Name of the ref to remove.
	   * @param {ReactOwner} owner Component on which the ref is recorded.
	   * @final
	   * @internal
	   */
	  removeComponentAsRefFrom: function (component, ref, owner) {
	    !ReactOwner.isValidOwner(owner) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'removeComponentAsRefFrom(...): Only a ReactOwner can have refs. You might ' + 'be removing a ref to a component that was not created inside a component\'s ' + '`render` method, or you have multiple copies of React loaded ' + '(details: https://fb.me/react-refs-must-have-owner).') : invariant(false) : undefined;
	    // Check that `component` is still the current ref because we do not want to
	    // detach the ref if another component stole it.
	    if (owner.getPublicInstance().refs[ref] === component.getPublicInstance()) {
	      owner.detachRef(ref);
	    }
	  }

	};

	module.exports = ReactOwner;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 56 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactUpdateQueue
	 */

	'use strict';

	var ReactCurrentOwner = __webpack_require__(8);
	var ReactElement = __webpack_require__(45);
	var ReactInstanceMap = __webpack_require__(50);
	var ReactUpdates = __webpack_require__(57);

	var assign = __webpack_require__(42);
	var invariant = __webpack_require__(16);
	var warning = __webpack_require__(28);

	function enqueueUpdate(internalInstance) {
	  ReactUpdates.enqueueUpdate(internalInstance);
	}

	function getInternalInstanceReadyForUpdate(publicInstance, callerName) {
	  var internalInstance = ReactInstanceMap.get(publicInstance);
	  if (!internalInstance) {
	    if (process.env.NODE_ENV !== 'production') {
	      // Only warn when we have a callerName. Otherwise we should be silent.
	      // We're probably calling from enqueueCallback. We don't want to warn
	      // there because we already warned for the corresponding lifecycle method.
	      process.env.NODE_ENV !== 'production' ? warning(!callerName, '%s(...): Can only update a mounted or mounting component. ' + 'This usually means you called %s() on an unmounted component. ' + 'This is a no-op. Please check the code for the %s component.', callerName, callerName, publicInstance.constructor.displayName) : undefined;
	    }
	    return null;
	  }

	  if (process.env.NODE_ENV !== 'production') {
	    process.env.NODE_ENV !== 'production' ? warning(ReactCurrentOwner.current == null, '%s(...): Cannot update during an existing state transition ' + '(such as within `render`). Render methods should be a pure function ' + 'of props and state.', callerName) : undefined;
	  }

	  return internalInstance;
	}

	/**
	 * ReactUpdateQueue allows for state updates to be scheduled into a later
	 * reconciliation step.
	 */
	var ReactUpdateQueue = {

	  /**
	   * Checks whether or not this composite component is mounted.
	   * @param {ReactClass} publicInstance The instance we want to test.
	   * @return {boolean} True if mounted, false otherwise.
	   * @protected
	   * @final
	   */
	  isMounted: function (publicInstance) {
	    if (process.env.NODE_ENV !== 'production') {
	      var owner = ReactCurrentOwner.current;
	      if (owner !== null) {
	        process.env.NODE_ENV !== 'production' ? warning(owner._warnedAboutRefsInRender, '%s is accessing isMounted inside its render() function. ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', owner.getName() || 'A component') : undefined;
	        owner._warnedAboutRefsInRender = true;
	      }
	    }
	    var internalInstance = ReactInstanceMap.get(publicInstance);
	    if (internalInstance) {
	      // During componentWillMount and render this will still be null but after
	      // that will always render to something. At least for now. So we can use
	      // this hack.
	      return !!internalInstance._renderedComponent;
	    } else {
	      return false;
	    }
	  },

	  /**
	   * Enqueue a callback that will be executed after all the pending updates
	   * have processed.
	   *
	   * @param {ReactClass} publicInstance The instance to use as `this` context.
	   * @param {?function} callback Called after state is updated.
	   * @internal
	   */
	  enqueueCallback: function (publicInstance, callback) {
	    !(typeof callback === 'function') ? process.env.NODE_ENV !== 'production' ? invariant(false, 'enqueueCallback(...): You called `setProps`, `replaceProps`, ' + '`setState`, `replaceState`, or `forceUpdate` with a callback that ' + 'isn\'t callable.') : invariant(false) : undefined;
	    var internalInstance = getInternalInstanceReadyForUpdate(publicInstance);

	    // Previously we would throw an error if we didn't have an internal
	    // instance. Since we want to make it a no-op instead, we mirror the same
	    // behavior we have in other enqueue* methods.
	    // We also need to ignore callbacks in componentWillMount. See
	    // enqueueUpdates.
	    if (!internalInstance) {
	      return null;
	    }

	    if (internalInstance._pendingCallbacks) {
	      internalInstance._pendingCallbacks.push(callback);
	    } else {
	      internalInstance._pendingCallbacks = [callback];
	    }
	    // TODO: The callback here is ignored when setState is called from
	    // componentWillMount. Either fix it or disallow doing so completely in
	    // favor of getInitialState. Alternatively, we can disallow
	    // componentWillMount during server-side rendering.
	    enqueueUpdate(internalInstance);
	  },

	  enqueueCallbackInternal: function (internalInstance, callback) {
	    !(typeof callback === 'function') ? process.env.NODE_ENV !== 'production' ? invariant(false, 'enqueueCallback(...): You called `setProps`, `replaceProps`, ' + '`setState`, `replaceState`, or `forceUpdate` with a callback that ' + 'isn\'t callable.') : invariant(false) : undefined;
	    if (internalInstance._pendingCallbacks) {
	      internalInstance._pendingCallbacks.push(callback);
	    } else {
	      internalInstance._pendingCallbacks = [callback];
	    }
	    enqueueUpdate(internalInstance);
	  },

	  /**
	   * Forces an update. This should only be invoked when it is known with
	   * certainty that we are **not** in a DOM transaction.
	   *
	   * You may want to call this when you know that some deeper aspect of the
	   * component's state has changed but `setState` was not called.
	   *
	   * This will not invoke `shouldComponentUpdate`, but it will invoke
	   * `componentWillUpdate` and `componentDidUpdate`.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @internal
	   */
	  enqueueForceUpdate: function (publicInstance) {
	    var internalInstance = getInternalInstanceReadyForUpdate(publicInstance, 'forceUpdate');

	    if (!internalInstance) {
	      return;
	    }

	    internalInstance._pendingForceUpdate = true;

	    enqueueUpdate(internalInstance);
	  },

	  /**
	   * Replaces all of the state. Always use this or `setState` to mutate state.
	   * You should treat `this.state` as immutable.
	   *
	   * There is no guarantee that `this.state` will be immediately updated, so
	   * accessing `this.state` after calling this method may return the old value.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @param {object} completeState Next state.
	   * @internal
	   */
	  enqueueReplaceState: function (publicInstance, completeState) {
	    var internalInstance = getInternalInstanceReadyForUpdate(publicInstance, 'replaceState');

	    if (!internalInstance) {
	      return;
	    }

	    internalInstance._pendingStateQueue = [completeState];
	    internalInstance._pendingReplaceState = true;

	    enqueueUpdate(internalInstance);
	  },

	  /**
	   * Sets a subset of the state. This only exists because _pendingState is
	   * internal. This provides a merging strategy that is not available to deep
	   * properties which is confusing. TODO: Expose pendingState or don't use it
	   * during the merge.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @param {object} partialState Next partial state to be merged with state.
	   * @internal
	   */
	  enqueueSetState: function (publicInstance, partialState) {
	    var internalInstance = getInternalInstanceReadyForUpdate(publicInstance, 'setState');

	    if (!internalInstance) {
	      return;
	    }

	    var queue = internalInstance._pendingStateQueue || (internalInstance._pendingStateQueue = []);
	    queue.push(partialState);

	    enqueueUpdate(internalInstance);
	  },

	  /**
	   * Sets a subset of the props.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @param {object} partialProps Subset of the next props.
	   * @internal
	   */
	  enqueueSetProps: function (publicInstance, partialProps) {
	    var internalInstance = getInternalInstanceReadyForUpdate(publicInstance, 'setProps');
	    if (!internalInstance) {
	      return;
	    }
	    ReactUpdateQueue.enqueueSetPropsInternal(internalInstance, partialProps);
	  },

	  enqueueSetPropsInternal: function (internalInstance, partialProps) {
	    var topLevelWrapper = internalInstance._topLevelWrapper;
	    !topLevelWrapper ? process.env.NODE_ENV !== 'production' ? invariant(false, 'setProps(...): You called `setProps` on a ' + 'component with a parent. This is an anti-pattern since props will ' + 'get reactively updated when rendered. Instead, change the owner\'s ' + '`render` method to pass the correct value as props to the component ' + 'where it is created.') : invariant(false) : undefined;

	    // Merge with the pending element if it exists, otherwise with existing
	    // element props.
	    var wrapElement = topLevelWrapper._pendingElement || topLevelWrapper._currentElement;
	    var element = wrapElement.props;
	    var props = assign({}, element.props, partialProps);
	    topLevelWrapper._pendingElement = ReactElement.cloneAndReplaceProps(wrapElement, ReactElement.cloneAndReplaceProps(element, props));

	    enqueueUpdate(topLevelWrapper);
	  },

	  /**
	   * Replaces all of the props.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @param {object} props New props.
	   * @internal
	   */
	  enqueueReplaceProps: function (publicInstance, props) {
	    var internalInstance = getInternalInstanceReadyForUpdate(publicInstance, 'replaceProps');
	    if (!internalInstance) {
	      return;
	    }
	    ReactUpdateQueue.enqueueReplacePropsInternal(internalInstance, props);
	  },

	  enqueueReplacePropsInternal: function (internalInstance, props) {
	    var topLevelWrapper = internalInstance._topLevelWrapper;
	    !topLevelWrapper ? process.env.NODE_ENV !== 'production' ? invariant(false, 'replaceProps(...): You called `replaceProps` on a ' + 'component with a parent. This is an anti-pattern since props will ' + 'get reactively updated when rendered. Instead, change the owner\'s ' + '`render` method to pass the correct value as props to the component ' + 'where it is created.') : invariant(false) : undefined;

	    // Merge with the pending element if it exists, otherwise with existing
	    // element props.
	    var wrapElement = topLevelWrapper._pendingElement || topLevelWrapper._currentElement;
	    var element = wrapElement.props;
	    topLevelWrapper._pendingElement = ReactElement.cloneAndReplaceProps(wrapElement, ReactElement.cloneAndReplaceProps(element, props));

	    enqueueUpdate(topLevelWrapper);
	  },

	  enqueueElementInternal: function (internalInstance, newElement) {
	    internalInstance._pendingElement = newElement;
	    enqueueUpdate(internalInstance);
	  }

	};

	module.exports = ReactUpdateQueue;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 57 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactUpdates
	 */

	'use strict';

	var CallbackQueue = __webpack_require__(58);
	var PooledClass = __webpack_require__(59);
	var ReactPerf = __webpack_require__(21);
	var ReactReconciler = __webpack_require__(53);
	var Transaction = __webpack_require__(60);

	var assign = __webpack_require__(42);
	var invariant = __webpack_require__(16);

	var dirtyComponents = [];
	var asapCallbackQueue = CallbackQueue.getPooled();
	var asapEnqueued = false;

	var batchingStrategy = null;

	function ensureInjected() {
	  !(ReactUpdates.ReactReconcileTransaction && batchingStrategy) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactUpdates: must inject a reconcile transaction class and batching ' + 'strategy') : invariant(false) : undefined;
	}

	var NESTED_UPDATES = {
	  initialize: function () {
	    this.dirtyComponentsLength = dirtyComponents.length;
	  },
	  close: function () {
	    if (this.dirtyComponentsLength !== dirtyComponents.length) {
	      // Additional updates were enqueued by componentDidUpdate handlers or
	      // similar; before our own UPDATE_QUEUEING wrapper closes, we want to run
	      // these new updates so that if A's componentDidUpdate calls setState on
	      // B, B will update before the callback A's updater provided when calling
	      // setState.
	      dirtyComponents.splice(0, this.dirtyComponentsLength);
	      flushBatchedUpdates();
	    } else {
	      dirtyComponents.length = 0;
	    }
	  }
	};

	var UPDATE_QUEUEING = {
	  initialize: function () {
	    this.callbackQueue.reset();
	  },
	  close: function () {
	    this.callbackQueue.notifyAll();
	  }
	};

	var TRANSACTION_WRAPPERS = [NESTED_UPDATES, UPDATE_QUEUEING];

	function ReactUpdatesFlushTransaction() {
	  this.reinitializeTransaction();
	  this.dirtyComponentsLength = null;
	  this.callbackQueue = CallbackQueue.getPooled();
	  this.reconcileTransaction = ReactUpdates.ReactReconcileTransaction.getPooled( /* forceHTML */false);
	}

	assign(ReactUpdatesFlushTransaction.prototype, Transaction.Mixin, {
	  getTransactionWrappers: function () {
	    return TRANSACTION_WRAPPERS;
	  },

	  destructor: function () {
	    this.dirtyComponentsLength = null;
	    CallbackQueue.release(this.callbackQueue);
	    this.callbackQueue = null;
	    ReactUpdates.ReactReconcileTransaction.release(this.reconcileTransaction);
	    this.reconcileTransaction = null;
	  },

	  perform: function (method, scope, a) {
	    // Essentially calls `this.reconcileTransaction.perform(method, scope, a)`
	    // with this transaction's wrappers around it.
	    return Transaction.Mixin.perform.call(this, this.reconcileTransaction.perform, this.reconcileTransaction, method, scope, a);
	  }
	});

	PooledClass.addPoolingTo(ReactUpdatesFlushTransaction);

	function batchedUpdates(callback, a, b, c, d, e) {
	  ensureInjected();
	  batchingStrategy.batchedUpdates(callback, a, b, c, d, e);
	}

	/**
	 * Array comparator for ReactComponents by mount ordering.
	 *
	 * @param {ReactComponent} c1 first component you're comparing
	 * @param {ReactComponent} c2 second component you're comparing
	 * @return {number} Return value usable by Array.prototype.sort().
	 */
	function mountOrderComparator(c1, c2) {
	  return c1._mountOrder - c2._mountOrder;
	}

	function runBatchedUpdates(transaction) {
	  var len = transaction.dirtyComponentsLength;
	  !(len === dirtyComponents.length) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Expected flush transaction\'s stored dirty-components length (%s) to ' + 'match dirty-components array length (%s).', len, dirtyComponents.length) : invariant(false) : undefined;

	  // Since reconciling a component higher in the owner hierarchy usually (not
	  // always -- see shouldComponentUpdate()) will reconcile children, reconcile
	  // them before their children by sorting the array.
	  dirtyComponents.sort(mountOrderComparator);

	  for (var i = 0; i < len; i++) {
	    // If a component is unmounted before pending changes apply, it will still
	    // be here, but we assume that it has cleared its _pendingCallbacks and
	    // that performUpdateIfNecessary is a noop.
	    var component = dirtyComponents[i];

	    // If performUpdateIfNecessary happens to enqueue any new updates, we
	    // shouldn't execute the callbacks until the next render happens, so
	    // stash the callbacks first
	    var callbacks = component._pendingCallbacks;
	    component._pendingCallbacks = null;

	    ReactReconciler.performUpdateIfNecessary(component, transaction.reconcileTransaction);

	    if (callbacks) {
	      for (var j = 0; j < callbacks.length; j++) {
	        transaction.callbackQueue.enqueue(callbacks[j], component.getPublicInstance());
	      }
	    }
	  }
	}

	var flushBatchedUpdates = function () {
	  // ReactUpdatesFlushTransaction's wrappers will clear the dirtyComponents
	  // array and perform any updates enqueued by mount-ready handlers (i.e.,
	  // componentDidUpdate) but we need to check here too in order to catch
	  // updates enqueued by setState callbacks and asap calls.
	  while (dirtyComponents.length || asapEnqueued) {
	    if (dirtyComponents.length) {
	      var transaction = ReactUpdatesFlushTransaction.getPooled();
	      transaction.perform(runBatchedUpdates, null, transaction);
	      ReactUpdatesFlushTransaction.release(transaction);
	    }

	    if (asapEnqueued) {
	      asapEnqueued = false;
	      var queue = asapCallbackQueue;
	      asapCallbackQueue = CallbackQueue.getPooled();
	      queue.notifyAll();
	      CallbackQueue.release(queue);
	    }
	  }
	};
	flushBatchedUpdates = ReactPerf.measure('ReactUpdates', 'flushBatchedUpdates', flushBatchedUpdates);

	/**
	 * Mark a component as needing a rerender, adding an optional callback to a
	 * list of functions which will be executed once the rerender occurs.
	 */
	function enqueueUpdate(component) {
	  ensureInjected();

	  // Various parts of our code (such as ReactCompositeComponent's
	  // _renderValidatedComponent) assume that calls to render aren't nested;
	  // verify that that's the case. (This is called by each top-level update
	  // function, like setProps, setState, forceUpdate, etc.; creation and
	  // destruction of top-level components is guarded in ReactMount.)

	  if (!batchingStrategy.isBatchingUpdates) {
	    batchingStrategy.batchedUpdates(enqueueUpdate, component);
	    return;
	  }

	  dirtyComponents.push(component);
	}

	/**
	 * Enqueue a callback to be run at the end of the current batching cycle. Throws
	 * if no updates are currently being performed.
	 */
	function asap(callback, context) {
	  !batchingStrategy.isBatchingUpdates ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactUpdates.asap: Can\'t enqueue an asap callback in a context where' + 'updates are not being batched.') : invariant(false) : undefined;
	  asapCallbackQueue.enqueue(callback, context);
	  asapEnqueued = true;
	}

	var ReactUpdatesInjection = {
	  injectReconcileTransaction: function (ReconcileTransaction) {
	    !ReconcileTransaction ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactUpdates: must provide a reconcile transaction class') : invariant(false) : undefined;
	    ReactUpdates.ReactReconcileTransaction = ReconcileTransaction;
	  },

	  injectBatchingStrategy: function (_batchingStrategy) {
	    !_batchingStrategy ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactUpdates: must provide a batching strategy') : invariant(false) : undefined;
	    !(typeof _batchingStrategy.batchedUpdates === 'function') ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactUpdates: must provide a batchedUpdates() function') : invariant(false) : undefined;
	    !(typeof _batchingStrategy.isBatchingUpdates === 'boolean') ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactUpdates: must provide an isBatchingUpdates boolean attribute') : invariant(false) : undefined;
	    batchingStrategy = _batchingStrategy;
	  }
	};

	var ReactUpdates = {
	  /**
	   * React references `ReactReconcileTransaction` using this property in order
	   * to allow dependency injection.
	   *
	   * @internal
	   */
	  ReactReconcileTransaction: null,

	  batchedUpdates: batchedUpdates,
	  enqueueUpdate: enqueueUpdate,
	  flushBatchedUpdates: flushBatchedUpdates,
	  injection: ReactUpdatesInjection,
	  asap: asap
	};

	module.exports = ReactUpdates;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 58 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule CallbackQueue
	 */

	'use strict';

	var PooledClass = __webpack_require__(59);

	var assign = __webpack_require__(42);
	var invariant = __webpack_require__(16);

	/**
	 * A specialized pseudo-event module to help keep track of components waiting to
	 * be notified when their DOM representations are available for use.
	 *
	 * This implements `PooledClass`, so you should never need to instantiate this.
	 * Instead, use `CallbackQueue.getPooled()`.
	 *
	 * @class ReactMountReady
	 * @implements PooledClass
	 * @internal
	 */
	function CallbackQueue() {
	  this._callbacks = null;
	  this._contexts = null;
	}

	assign(CallbackQueue.prototype, {

	  /**
	   * Enqueues a callback to be invoked when `notifyAll` is invoked.
	   *
	   * @param {function} callback Invoked when `notifyAll` is invoked.
	   * @param {?object} context Context to call `callback` with.
	   * @internal
	   */
	  enqueue: function (callback, context) {
	    this._callbacks = this._callbacks || [];
	    this._contexts = this._contexts || [];
	    this._callbacks.push(callback);
	    this._contexts.push(context);
	  },

	  /**
	   * Invokes all enqueued callbacks and clears the queue. This is invoked after
	   * the DOM representation of a component has been created or updated.
	   *
	   * @internal
	   */
	  notifyAll: function () {
	    var callbacks = this._callbacks;
	    var contexts = this._contexts;
	    if (callbacks) {
	      !(callbacks.length === contexts.length) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Mismatched list of contexts in callback queue') : invariant(false) : undefined;
	      this._callbacks = null;
	      this._contexts = null;
	      for (var i = 0; i < callbacks.length; i++) {
	        callbacks[i].call(contexts[i]);
	      }
	      callbacks.length = 0;
	      contexts.length = 0;
	    }
	  },

	  /**
	   * Resets the internal queue.
	   *
	   * @internal
	   */
	  reset: function () {
	    this._callbacks = null;
	    this._contexts = null;
	  },

	  /**
	   * `PooledClass` looks for this.
	   */
	  destructor: function () {
	    this.reset();
	  }

	});

	PooledClass.addPoolingTo(CallbackQueue);

	module.exports = CallbackQueue;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 59 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule PooledClass
	 */

	'use strict';

	var invariant = __webpack_require__(16);

	/**
	 * Static poolers. Several custom versions for each potential number of
	 * arguments. A completely generic pooler is easy to implement, but would
	 * require accessing the `arguments` object. In each of these, `this` refers to
	 * the Class itself, not an instance. If any others are needed, simply add them
	 * here, or in their own files.
	 */
	var oneArgumentPooler = function (copyFieldsFrom) {
	  var Klass = this;
	  if (Klass.instancePool.length) {
	    var instance = Klass.instancePool.pop();
	    Klass.call(instance, copyFieldsFrom);
	    return instance;
	  } else {
	    return new Klass(copyFieldsFrom);
	  }
	};

	var twoArgumentPooler = function (a1, a2) {
	  var Klass = this;
	  if (Klass.instancePool.length) {
	    var instance = Klass.instancePool.pop();
	    Klass.call(instance, a1, a2);
	    return instance;
	  } else {
	    return new Klass(a1, a2);
	  }
	};

	var threeArgumentPooler = function (a1, a2, a3) {
	  var Klass = this;
	  if (Klass.instancePool.length) {
	    var instance = Klass.instancePool.pop();
	    Klass.call(instance, a1, a2, a3);
	    return instance;
	  } else {
	    return new Klass(a1, a2, a3);
	  }
	};

	var fourArgumentPooler = function (a1, a2, a3, a4) {
	  var Klass = this;
	  if (Klass.instancePool.length) {
	    var instance = Klass.instancePool.pop();
	    Klass.call(instance, a1, a2, a3, a4);
	    return instance;
	  } else {
	    return new Klass(a1, a2, a3, a4);
	  }
	};

	var fiveArgumentPooler = function (a1, a2, a3, a4, a5) {
	  var Klass = this;
	  if (Klass.instancePool.length) {
	    var instance = Klass.instancePool.pop();
	    Klass.call(instance, a1, a2, a3, a4, a5);
	    return instance;
	  } else {
	    return new Klass(a1, a2, a3, a4, a5);
	  }
	};

	var standardReleaser = function (instance) {
	  var Klass = this;
	  !(instance instanceof Klass) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Trying to release an instance into a pool of a different type.') : invariant(false) : undefined;
	  instance.destructor();
	  if (Klass.instancePool.length < Klass.poolSize) {
	    Klass.instancePool.push(instance);
	  }
	};

	var DEFAULT_POOL_SIZE = 10;
	var DEFAULT_POOLER = oneArgumentPooler;

	/**
	 * Augments `CopyConstructor` to be a poolable class, augmenting only the class
	 * itself (statically) not adding any prototypical fields. Any CopyConstructor
	 * you give this may have a `poolSize` property, and will look for a
	 * prototypical `destructor` on instances (optional).
	 *
	 * @param {Function} CopyConstructor Constructor that can be used to reset.
	 * @param {Function} pooler Customizable pooler.
	 */
	var addPoolingTo = function (CopyConstructor, pooler) {
	  var NewKlass = CopyConstructor;
	  NewKlass.instancePool = [];
	  NewKlass.getPooled = pooler || DEFAULT_POOLER;
	  if (!NewKlass.poolSize) {
	    NewKlass.poolSize = DEFAULT_POOL_SIZE;
	  }
	  NewKlass.release = standardReleaser;
	  return NewKlass;
	};

	var PooledClass = {
	  addPoolingTo: addPoolingTo,
	  oneArgumentPooler: oneArgumentPooler,
	  twoArgumentPooler: twoArgumentPooler,
	  threeArgumentPooler: threeArgumentPooler,
	  fourArgumentPooler: fourArgumentPooler,
	  fiveArgumentPooler: fiveArgumentPooler
	};

	module.exports = PooledClass;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 60 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule Transaction
	 */

	'use strict';

	var invariant = __webpack_require__(16);

	/**
	 * `Transaction` creates a black box that is able to wrap any method such that
	 * certain invariants are maintained before and after the method is invoked
	 * (Even if an exception is thrown while invoking the wrapped method). Whoever
	 * instantiates a transaction can provide enforcers of the invariants at
	 * creation time. The `Transaction` class itself will supply one additional
	 * automatic invariant for you - the invariant that any transaction instance
	 * should not be run while it is already being run. You would typically create a
	 * single instance of a `Transaction` for reuse multiple times, that potentially
	 * is used to wrap several different methods. Wrappers are extremely simple -
	 * they only require implementing two methods.
	 *
	 * <pre>
	 *                       wrappers (injected at creation time)
	 *                                      +        +
	 *                                      |        |
	 *                    +-----------------|--------|--------------+
	 *                    |                 v        |              |
	 *                    |      +---------------+   |              |
	 *                    |   +--|    wrapper1   |---|----+         |
	 *                    |   |  +---------------+   v    |         |
	 *                    |   |          +-------------+  |         |
	 *                    |   |     +----|   wrapper2  |--------+   |
	 *                    |   |     |    +-------------+  |     |   |
	 *                    |   |     |                     |     |   |
	 *                    |   v     v                     v     v   | wrapper
	 *                    | +---+ +---+   +---------+   +---+ +---+ | invariants
	 * perform(anyMethod) | |   | |   |   |         |   |   | |   | | maintained
	 * +----------------->|-|---|-|---|-->|anyMethod|---|---|-|---|-|-------->
	 *                    | |   | |   |   |         |   |   | |   | |
	 *                    | |   | |   |   |         |   |   | |   | |
	 *                    | |   | |   |   |         |   |   | |   | |
	 *                    | +---+ +---+   +---------+   +---+ +---+ |
	 *                    |  initialize                    close    |
	 *                    +-----------------------------------------+
	 * </pre>
	 *
	 * Use cases:
	 * - Preserving the input selection ranges before/after reconciliation.
	 *   Restoring selection even in the event of an unexpected error.
	 * - Deactivating events while rearranging the DOM, preventing blurs/focuses,
	 *   while guaranteeing that afterwards, the event system is reactivated.
	 * - Flushing a queue of collected DOM mutations to the main UI thread after a
	 *   reconciliation takes place in a worker thread.
	 * - Invoking any collected `componentDidUpdate` callbacks after rendering new
	 *   content.
	 * - (Future use case): Wrapping particular flushes of the `ReactWorker` queue
	 *   to preserve the `scrollTop` (an automatic scroll aware DOM).
	 * - (Future use case): Layout calculations before and after DOM updates.
	 *
	 * Transactional plugin API:
	 * - A module that has an `initialize` method that returns any precomputation.
	 * - and a `close` method that accepts the precomputation. `close` is invoked
	 *   when the wrapped process is completed, or has failed.
	 *
	 * @param {Array<TransactionalWrapper>} transactionWrapper Wrapper modules
	 * that implement `initialize` and `close`.
	 * @return {Transaction} Single transaction for reuse in thread.
	 *
	 * @class Transaction
	 */
	var Mixin = {
	  /**
	   * Sets up this instance so that it is prepared for collecting metrics. Does
	   * so such that this setup method may be used on an instance that is already
	   * initialized, in a way that does not consume additional memory upon reuse.
	   * That can be useful if you decide to make your subclass of this mixin a
	   * "PooledClass".
	   */
	  reinitializeTransaction: function () {
	    this.transactionWrappers = this.getTransactionWrappers();
	    if (this.wrapperInitData) {
	      this.wrapperInitData.length = 0;
	    } else {
	      this.wrapperInitData = [];
	    }
	    this._isInTransaction = false;
	  },

	  _isInTransaction: false,

	  /**
	   * @abstract
	   * @return {Array<TransactionWrapper>} Array of transaction wrappers.
	   */
	  getTransactionWrappers: null,

	  isInTransaction: function () {
	    return !!this._isInTransaction;
	  },

	  /**
	   * Executes the function within a safety window. Use this for the top level
	   * methods that result in large amounts of computation/mutations that would
	   * need to be safety checked. The optional arguments helps prevent the need
	   * to bind in many cases.
	   *
	   * @param {function} method Member of scope to call.
	   * @param {Object} scope Scope to invoke from.
	   * @param {Object?=} a Argument to pass to the method.
	   * @param {Object?=} b Argument to pass to the method.
	   * @param {Object?=} c Argument to pass to the method.
	   * @param {Object?=} d Argument to pass to the method.
	   * @param {Object?=} e Argument to pass to the method.
	   * @param {Object?=} f Argument to pass to the method.
	   *
	   * @return {*} Return value from `method`.
	   */
	  perform: function (method, scope, a, b, c, d, e, f) {
	    !!this.isInTransaction() ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Transaction.perform(...): Cannot initialize a transaction when there ' + 'is already an outstanding transaction.') : invariant(false) : undefined;
	    var errorThrown;
	    var ret;
	    try {
	      this._isInTransaction = true;
	      // Catching errors makes debugging more difficult, so we start with
	      // errorThrown set to true before setting it to false after calling
	      // close -- if it's still set to true in the finally block, it means
	      // one of these calls threw.
	      errorThrown = true;
	      this.initializeAll(0);
	      ret = method.call(scope, a, b, c, d, e, f);
	      errorThrown = false;
	    } finally {
	      try {
	        if (errorThrown) {
	          // If `method` throws, prefer to show that stack trace over any thrown
	          // by invoking `closeAll`.
	          try {
	            this.closeAll(0);
	          } catch (err) {}
	        } else {
	          // Since `method` didn't throw, we don't want to silence the exception
	          // here.
	          this.closeAll(0);
	        }
	      } finally {
	        this._isInTransaction = false;
	      }
	    }
	    return ret;
	  },

	  initializeAll: function (startIndex) {
	    var transactionWrappers = this.transactionWrappers;
	    for (var i = startIndex; i < transactionWrappers.length; i++) {
	      var wrapper = transactionWrappers[i];
	      try {
	        // Catching errors makes debugging more difficult, so we start with the
	        // OBSERVED_ERROR state before overwriting it with the real return value
	        // of initialize -- if it's still set to OBSERVED_ERROR in the finally
	        // block, it means wrapper.initialize threw.
	        this.wrapperInitData[i] = Transaction.OBSERVED_ERROR;
	        this.wrapperInitData[i] = wrapper.initialize ? wrapper.initialize.call(this) : null;
	      } finally {
	        if (this.wrapperInitData[i] === Transaction.OBSERVED_ERROR) {
	          // The initializer for wrapper i threw an error; initialize the
	          // remaining wrappers but silence any exceptions from them to ensure
	          // that the first error is the one to bubble up.
	          try {
	            this.initializeAll(i + 1);
	          } catch (err) {}
	        }
	      }
	    }
	  },

	  /**
	   * Invokes each of `this.transactionWrappers.close[i]` functions, passing into
	   * them the respective return values of `this.transactionWrappers.init[i]`
	   * (`close`rs that correspond to initializers that failed will not be
	   * invoked).
	   */
	  closeAll: function (startIndex) {
	    !this.isInTransaction() ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Transaction.closeAll(): Cannot close transaction when none are open.') : invariant(false) : undefined;
	    var transactionWrappers = this.transactionWrappers;
	    for (var i = startIndex; i < transactionWrappers.length; i++) {
	      var wrapper = transactionWrappers[i];
	      var initData = this.wrapperInitData[i];
	      var errorThrown;
	      try {
	        // Catching errors makes debugging more difficult, so we start with
	        // errorThrown set to true before setting it to false after calling
	        // close -- if it's still set to true in the finally block, it means
	        // wrapper.close threw.
	        errorThrown = true;
	        if (initData !== Transaction.OBSERVED_ERROR && wrapper.close) {
	          wrapper.close.call(this, initData);
	        }
	        errorThrown = false;
	      } finally {
	        if (errorThrown) {
	          // The closer for wrapper i threw an error; close the remaining
	          // wrappers but silence any exceptions from them to ensure that the
	          // first error is the one to bubble up.
	          try {
	            this.closeAll(i + 1);
	          } catch (e) {}
	        }
	      }
	    }
	    this.wrapperInitData.length = 0;
	  }
	};

	var Transaction = {

	  Mixin: Mixin,

	  /**
	   * Token to look for to determine if an error occurred.
	   */
	  OBSERVED_ERROR: {}

	};

	module.exports = Transaction;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 61 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule emptyObject
	 */

	'use strict';

	var emptyObject = {};

	if (process.env.NODE_ENV !== 'production') {
	  Object.freeze(emptyObject);
	}

	module.exports = emptyObject;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 62 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule containsNode
	 * @typechecks
	 */

	'use strict';

	var isTextNode = __webpack_require__(63);

	/*eslint-disable no-bitwise */

	/**
	 * Checks if a given DOM node contains or is another DOM node.
	 *
	 * @param {?DOMNode} outerNode Outer DOM node.
	 * @param {?DOMNode} innerNode Inner DOM node.
	 * @return {boolean} True if `outerNode` contains or is `innerNode`.
	 */
	function containsNode(_x, _x2) {
	  var _again = true;

	  _function: while (_again) {
	    var outerNode = _x,
	        innerNode = _x2;
	    _again = false;

	    if (!outerNode || !innerNode) {
	      return false;
	    } else if (outerNode === innerNode) {
	      return true;
	    } else if (isTextNode(outerNode)) {
	      return false;
	    } else if (isTextNode(innerNode)) {
	      _x = outerNode;
	      _x2 = innerNode.parentNode;
	      _again = true;
	      continue _function;
	    } else if (outerNode.contains) {
	      return outerNode.contains(innerNode);
	    } else if (outerNode.compareDocumentPosition) {
	      return !!(outerNode.compareDocumentPosition(innerNode) & 16);
	    } else {
	      return false;
	    }
	  }
	}

	module.exports = containsNode;

/***/ },
/* 63 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule isTextNode
	 * @typechecks
	 */

	'use strict';

	var isNode = __webpack_require__(64);

	/**
	 * @param {*} object The object to check.
	 * @return {boolean} Whether or not the object is a DOM text node.
	 */
	function isTextNode(object) {
	  return isNode(object) && object.nodeType == 3;
	}

	module.exports = isTextNode;

/***/ },
/* 64 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule isNode
	 * @typechecks
	 */

	/**
	 * @param {*} object The object to check.
	 * @return {boolean} Whether or not the object is a DOM node.
	 */
	'use strict';

	function isNode(object) {
	  return !!(object && (typeof Node === 'function' ? object instanceof Node : typeof object === 'object' && typeof object.nodeType === 'number' && typeof object.nodeName === 'string'));
	}

	module.exports = isNode;

/***/ },
/* 65 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule instantiateReactComponent
	 * @typechecks static-only
	 */

	'use strict';

	var ReactCompositeComponent = __webpack_require__(66);
	var ReactEmptyComponent = __webpack_require__(71);
	var ReactNativeComponent = __webpack_require__(72);

	var assign = __webpack_require__(42);
	var invariant = __webpack_require__(16);
	var warning = __webpack_require__(28);

	// To avoid a cyclic dependency, we create the final class in this module
	var ReactCompositeComponentWrapper = function () {};
	assign(ReactCompositeComponentWrapper.prototype, ReactCompositeComponent.Mixin, {
	  _instantiateReactComponent: instantiateReactComponent
	});

	function getDeclarationErrorAddendum(owner) {
	  if (owner) {
	    var name = owner.getName();
	    if (name) {
	      return ' Check the render method of `' + name + '`.';
	    }
	  }
	  return '';
	}

	/**
	 * Check if the type reference is a known internal type. I.e. not a user
	 * provided composite type.
	 *
	 * @param {function} type
	 * @return {boolean} Returns true if this is a valid internal type.
	 */
	function isInternalComponentType(type) {
	  return typeof type === 'function' && typeof type.prototype !== 'undefined' && typeof type.prototype.mountComponent === 'function' && typeof type.prototype.receiveComponent === 'function';
	}

	/**
	 * Given a ReactNode, create an instance that will actually be mounted.
	 *
	 * @param {ReactNode} node
	 * @return {object} A new instance of the element's constructor.
	 * @protected
	 */
	function instantiateReactComponent(node) {
	  var instance;

	  if (node === null || node === false) {
	    instance = new ReactEmptyComponent(instantiateReactComponent);
	  } else if (typeof node === 'object') {
	    var element = node;
	    !(element && (typeof element.type === 'function' || typeof element.type === 'string')) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Element type is invalid: expected a string (for built-in components) ' + 'or a class/function (for composite components) but got: %s.%s', element.type == null ? element.type : typeof element.type, getDeclarationErrorAddendum(element._owner)) : invariant(false) : undefined;

	    // Special case string values
	    if (typeof element.type === 'string') {
	      instance = ReactNativeComponent.createInternalComponent(element);
	    } else if (isInternalComponentType(element.type)) {
	      // This is temporarily available for custom components that are not string
	      // representations. I.e. ART. Once those are updated to use the string
	      // representation, we can drop this code path.
	      instance = new element.type(element);
	    } else {
	      instance = new ReactCompositeComponentWrapper();
	    }
	  } else if (typeof node === 'string' || typeof node === 'number') {
	    instance = ReactNativeComponent.createInstanceForText(node);
	  } else {
	     true ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Encountered invalid React node of type %s', typeof node) : invariant(false) : undefined;
	  }

	  if (process.env.NODE_ENV !== 'production') {
	    process.env.NODE_ENV !== 'production' ? warning(typeof instance.construct === 'function' && typeof instance.mountComponent === 'function' && typeof instance.receiveComponent === 'function' && typeof instance.unmountComponent === 'function', 'Only React Components can be mounted.') : undefined;
	  }

	  // Sets up the instance. This can probably just move into the constructor now.
	  instance.construct(node);

	  // These two fields are used by the DOM and ART diffing algorithms
	  // respectively. Instead of using expandos on components, we should be
	  // storing the state needed by the diffing algorithms elsewhere.
	  instance._mountIndex = 0;
	  instance._mountImage = null;

	  if (process.env.NODE_ENV !== 'production') {
	    instance._isOwnerNecessary = false;
	    instance._warnedAboutRefsInRender = false;
	  }

	  // Internal instances should fully constructed at this point, so they should
	  // not get any new fields added to them at this point.
	  if (process.env.NODE_ENV !== 'production') {
	    if (Object.preventExtensions) {
	      Object.preventExtensions(instance);
	    }
	  }

	  return instance;
	}

	module.exports = instantiateReactComponent;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 66 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactCompositeComponent
	 */

	'use strict';

	var ReactComponentEnvironment = __webpack_require__(67);
	var ReactCurrentOwner = __webpack_require__(8);
	var ReactElement = __webpack_require__(45);
	var ReactInstanceMap = __webpack_require__(50);
	var ReactPerf = __webpack_require__(21);
	var ReactPropTypeLocations = __webpack_require__(68);
	var ReactPropTypeLocationNames = __webpack_require__(69);
	var ReactReconciler = __webpack_require__(53);
	var ReactUpdateQueue = __webpack_require__(56);

	var assign = __webpack_require__(42);
	var emptyObject = __webpack_require__(61);
	var invariant = __webpack_require__(16);
	var shouldUpdateReactComponent = __webpack_require__(70);
	var warning = __webpack_require__(28);

	function getDeclarationErrorAddendum(component) {
	  var owner = component._currentElement._owner || null;
	  if (owner) {
	    var name = owner.getName();
	    if (name) {
	      return ' Check the render method of `' + name + '`.';
	    }
	  }
	  return '';
	}

	function StatelessComponent(Component) {}
	StatelessComponent.prototype.render = function () {
	  var Component = ReactInstanceMap.get(this)._currentElement.type;
	  return Component(this.props, this.context, this.updater);
	};

	/**
	 * ------------------ The Life-Cycle of a Composite Component ------------------
	 *
	 * - constructor: Initialization of state. The instance is now retained.
	 *   - componentWillMount
	 *   - render
	 *   - [children's constructors]
	 *     - [children's componentWillMount and render]
	 *     - [children's componentDidMount]
	 *     - componentDidMount
	 *
	 *       Update Phases:
	 *       - componentWillReceiveProps (only called if parent updated)
	 *       - shouldComponentUpdate
	 *         - componentWillUpdate
	 *           - render
	 *           - [children's constructors or receive props phases]
	 *         - componentDidUpdate
	 *
	 *     - componentWillUnmount
	 *     - [children's componentWillUnmount]
	 *   - [children destroyed]
	 * - (destroyed): The instance is now blank, released by React and ready for GC.
	 *
	 * -----------------------------------------------------------------------------
	 */

	/**
	 * An incrementing ID assigned to each component when it is mounted. This is
	 * used to enforce the order in which `ReactUpdates` updates dirty components.
	 *
	 * @private
	 */
	var nextMountID = 1;

	/**
	 * @lends {ReactCompositeComponent.prototype}
	 */
	var ReactCompositeComponentMixin = {

	  /**
	   * Base constructor for all composite component.
	   *
	   * @param {ReactElement} element
	   * @final
	   * @internal
	   */
	  construct: function (element) {
	    this._currentElement = element;
	    this._rootNodeID = null;
	    this._instance = null;

	    // See ReactUpdateQueue
	    this._pendingElement = null;
	    this._pendingStateQueue = null;
	    this._pendingReplaceState = false;
	    this._pendingForceUpdate = false;

	    this._renderedComponent = null;

	    this._context = null;
	    this._mountOrder = 0;
	    this._topLevelWrapper = null;

	    // See ReactUpdates and ReactUpdateQueue.
	    this._pendingCallbacks = null;
	  },

	  /**
	   * Initializes the component, renders markup, and registers event listeners.
	   *
	   * @param {string} rootID DOM ID of the root node.
	   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
	   * @return {?string} Rendered markup to be inserted into the DOM.
	   * @final
	   * @internal
	   */
	  mountComponent: function (rootID, transaction, context) {
	    this._context = context;
	    this._mountOrder = nextMountID++;
	    this._rootNodeID = rootID;

	    var publicProps = this._processProps(this._currentElement.props);
	    var publicContext = this._processContext(context);

	    var Component = this._currentElement.type;

	    // Initialize the public class
	    var inst;
	    var renderedElement;

	    // This is a way to detect if Component is a stateless arrow function
	    // component, which is not newable. It might not be 100% reliable but is
	    // something we can do until we start detecting that Component extends
	    // React.Component. We already assume that typeof Component === 'function'.
	    var canInstantiate = ('prototype' in Component);

	    if (canInstantiate) {
	      if (process.env.NODE_ENV !== 'production') {
	        ReactCurrentOwner.current = this;
	        try {
	          inst = new Component(publicProps, publicContext, ReactUpdateQueue);
	        } finally {
	          ReactCurrentOwner.current = null;
	        }
	      } else {
	        inst = new Component(publicProps, publicContext, ReactUpdateQueue);
	      }
	    }

	    if (!canInstantiate || inst === null || inst === false || ReactElement.isValidElement(inst)) {
	      renderedElement = inst;
	      inst = new StatelessComponent(Component);
	    }

	    if (process.env.NODE_ENV !== 'production') {
	      // This will throw later in _renderValidatedComponent, but add an early
	      // warning now to help debugging
	      if (inst.render == null) {
	        process.env.NODE_ENV !== 'production' ? warning(false, '%s(...): No `render` method found on the returned component ' + 'instance: you may have forgotten to define `render`, returned ' + 'null/false from a stateless component, or tried to render an ' + 'element whose type is a function that isn\'t a React component.', Component.displayName || Component.name || 'Component') : undefined;
	      } else {
	        // We support ES6 inheriting from React.Component, the module pattern,
	        // and stateless components, but not ES6 classes that don't extend
	        process.env.NODE_ENV !== 'production' ? warning(Component.prototype && Component.prototype.isReactComponent || !canInstantiate || !(inst instanceof Component), '%s(...): React component classes must extend React.Component.', Component.displayName || Component.name || 'Component') : undefined;
	      }
	    }

	    // These should be set up in the constructor, but as a convenience for
	    // simpler class abstractions, we set them up after the fact.
	    inst.props = publicProps;
	    inst.context = publicContext;
	    inst.refs = emptyObject;
	    inst.updater = ReactUpdateQueue;

	    this._instance = inst;

	    // Store a reference from the instance back to the internal representation
	    ReactInstanceMap.set(inst, this);

	    if (process.env.NODE_ENV !== 'production') {
	      // Since plain JS classes are defined without any special initialization
	      // logic, we can not catch common errors early. Therefore, we have to
	      // catch them here, at initialization time, instead.
	      process.env.NODE_ENV !== 'production' ? warning(!inst.getInitialState || inst.getInitialState.isReactClassApproved, 'getInitialState was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Did you mean to define a state property instead?', this.getName() || 'a component') : undefined;
	      process.env.NODE_ENV !== 'production' ? warning(!inst.getDefaultProps || inst.getDefaultProps.isReactClassApproved, 'getDefaultProps was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Use a static property to define defaultProps instead.', this.getName() || 'a component') : undefined;
	      process.env.NODE_ENV !== 'production' ? warning(!inst.propTypes, 'propTypes was defined as an instance property on %s. Use a static ' + 'property to define propTypes instead.', this.getName() || 'a component') : undefined;
	      process.env.NODE_ENV !== 'production' ? warning(!inst.contextTypes, 'contextTypes was defined as an instance property on %s. Use a ' + 'static property to define contextTypes instead.', this.getName() || 'a component') : undefined;
	      process.env.NODE_ENV !== 'production' ? warning(typeof inst.componentShouldUpdate !== 'function', '%s has a method called ' + 'componentShouldUpdate(). Did you mean shouldComponentUpdate()? ' + 'The name is phrased as a question because the function is ' + 'expected to return a value.', this.getName() || 'A component') : undefined;
	      process.env.NODE_ENV !== 'production' ? warning(typeof inst.componentDidUnmount !== 'function', '%s has a method called ' + 'componentDidUnmount(). But there is no such lifecycle method. ' + 'Did you mean componentWillUnmount()?', this.getName() || 'A component') : undefined;
	      process.env.NODE_ENV !== 'production' ? warning(typeof inst.componentWillRecieveProps !== 'function', '%s has a method called ' + 'componentWillRecieveProps(). Did you mean componentWillReceiveProps()?', this.getName() || 'A component') : undefined;
	    }

	    var initialState = inst.state;
	    if (initialState === undefined) {
	      inst.state = initialState = null;
	    }
	    !(typeof initialState === 'object' && !Array.isArray(initialState)) ? process.env.NODE_ENV !== 'production' ? invariant(false, '%s.state: must be set to an object or null', this.getName() || 'ReactCompositeComponent') : invariant(false) : undefined;

	    this._pendingStateQueue = null;
	    this._pendingReplaceState = false;
	    this._pendingForceUpdate = false;

	    if (inst.componentWillMount) {
	      inst.componentWillMount();
	      // When mounting, calls to `setState` by `componentWillMount` will set
	      // `this._pendingStateQueue` without triggering a re-render.
	      if (this._pendingStateQueue) {
	        inst.state = this._processPendingState(inst.props, inst.context);
	      }
	    }

	    // If not a stateless component, we now render
	    if (renderedElement === undefined) {
	      renderedElement = this._renderValidatedComponent();
	    }

	    this._renderedComponent = this._instantiateReactComponent(renderedElement);

	    var markup = ReactReconciler.mountComponent(this._renderedComponent, rootID, transaction, this._processChildContext(context));
	    if (inst.componentDidMount) {
	      transaction.getReactMountReady().enqueue(inst.componentDidMount, inst);
	    }

	    return markup;
	  },

	  /**
	   * Releases any resources allocated by `mountComponent`.
	   *
	   * @final
	   * @internal
	   */
	  unmountComponent: function () {
	    var inst = this._instance;

	    if (inst.componentWillUnmount) {
	      inst.componentWillUnmount();
	    }

	    ReactReconciler.unmountComponent(this._renderedComponent);
	    this._renderedComponent = null;
	    this._instance = null;

	    // Reset pending fields
	    // Even if this component is scheduled for another update in ReactUpdates,
	    // it would still be ignored because these fields are reset.
	    this._pendingStateQueue = null;
	    this._pendingReplaceState = false;
	    this._pendingForceUpdate = false;
	    this._pendingCallbacks = null;
	    this._pendingElement = null;

	    // These fields do not really need to be reset since this object is no
	    // longer accessible.
	    this._context = null;
	    this._rootNodeID = null;
	    this._topLevelWrapper = null;

	    // Delete the reference from the instance to this internal representation
	    // which allow the internals to be properly cleaned up even if the user
	    // leaks a reference to the public instance.
	    ReactInstanceMap.remove(inst);

	    // Some existing components rely on inst.props even after they've been
	    // destroyed (in event handlers).
	    // TODO: inst.props = null;
	    // TODO: inst.state = null;
	    // TODO: inst.context = null;
	  },

	  /**
	   * Filters the context object to only contain keys specified in
	   * `contextTypes`
	   *
	   * @param {object} context
	   * @return {?object}
	   * @private
	   */
	  _maskContext: function (context) {
	    var maskedContext = null;
	    var Component = this._currentElement.type;
	    var contextTypes = Component.contextTypes;
	    if (!contextTypes) {
	      return emptyObject;
	    }
	    maskedContext = {};
	    for (var contextName in contextTypes) {
	      maskedContext[contextName] = context[contextName];
	    }
	    return maskedContext;
	  },

	  /**
	   * Filters the context object to only contain keys specified in
	   * `contextTypes`, and asserts that they are valid.
	   *
	   * @param {object} context
	   * @return {?object}
	   * @private
	   */
	  _processContext: function (context) {
	    var maskedContext = this._maskContext(context);
	    if (process.env.NODE_ENV !== 'production') {
	      var Component = this._currentElement.type;
	      if (Component.contextTypes) {
	        this._checkPropTypes(Component.contextTypes, maskedContext, ReactPropTypeLocations.context);
	      }
	    }
	    return maskedContext;
	  },

	  /**
	   * @param {object} currentContext
	   * @return {object}
	   * @private
	   */
	  _processChildContext: function (currentContext) {
	    var Component = this._currentElement.type;
	    var inst = this._instance;
	    var childContext = inst.getChildContext && inst.getChildContext();
	    if (childContext) {
	      !(typeof Component.childContextTypes === 'object') ? process.env.NODE_ENV !== 'production' ? invariant(false, '%s.getChildContext(): childContextTypes must be defined in order to ' + 'use getChildContext().', this.getName() || 'ReactCompositeComponent') : invariant(false) : undefined;
	      if (process.env.NODE_ENV !== 'production') {
	        this._checkPropTypes(Component.childContextTypes, childContext, ReactPropTypeLocations.childContext);
	      }
	      for (var name in childContext) {
	        !(name in Component.childContextTypes) ? process.env.NODE_ENV !== 'production' ? invariant(false, '%s.getChildContext(): key "%s" is not defined in childContextTypes.', this.getName() || 'ReactCompositeComponent', name) : invariant(false) : undefined;
	      }
	      return assign({}, currentContext, childContext);
	    }
	    return currentContext;
	  },

	  /**
	   * Processes props by setting default values for unspecified props and
	   * asserting that the props are valid. Does not mutate its argument; returns
	   * a new props object with defaults merged in.
	   *
	   * @param {object} newProps
	   * @return {object}
	   * @private
	   */
	  _processProps: function (newProps) {
	    if (process.env.NODE_ENV !== 'production') {
	      var Component = this._currentElement.type;
	      if (Component.propTypes) {
	        this._checkPropTypes(Component.propTypes, newProps, ReactPropTypeLocations.prop);
	      }
	    }
	    return newProps;
	  },

	  /**
	   * Assert that the props are valid
	   *
	   * @param {object} propTypes Map of prop name to a ReactPropType
	   * @param {object} props
	   * @param {string} location e.g. "prop", "context", "child context"
	   * @private
	   */
	  _checkPropTypes: function (propTypes, props, location) {
	    // TODO: Stop validating prop types here and only use the element
	    // validation.
	    var componentName = this.getName();
	    for (var propName in propTypes) {
	      if (propTypes.hasOwnProperty(propName)) {
	        var error;
	        try {
	          // This is intentionally an invariant that gets caught. It's the same
	          // behavior as without this statement except with a better message.
	          !(typeof propTypes[propName] === 'function') ? process.env.NODE_ENV !== 'production' ? invariant(false, '%s: %s type `%s` is invalid; it must be a function, usually ' + 'from React.PropTypes.', componentName || 'React class', ReactPropTypeLocationNames[location], propName) : invariant(false) : undefined;
	          error = propTypes[propName](props, propName, componentName, location);
	        } catch (ex) {
	          error = ex;
	        }
	        if (error instanceof Error) {
	          // We may want to extend this logic for similar errors in
	          // top-level render calls, so I'm abstracting it away into
	          // a function to minimize refactoring in the future
	          var addendum = getDeclarationErrorAddendum(this);

	          if (location === ReactPropTypeLocations.prop) {
	            // Preface gives us something to blacklist in warning module
	            process.env.NODE_ENV !== 'production' ? warning(false, 'Failed Composite propType: %s%s', error.message, addendum) : undefined;
	          } else {
	            process.env.NODE_ENV !== 'production' ? warning(false, 'Failed Context Types: %s%s', error.message, addendum) : undefined;
	          }
	        }
	      }
	    }
	  },

	  receiveComponent: function (nextElement, transaction, nextContext) {
	    var prevElement = this._currentElement;
	    var prevContext = this._context;

	    this._pendingElement = null;

	    this.updateComponent(transaction, prevElement, nextElement, prevContext, nextContext);
	  },

	  /**
	   * If any of `_pendingElement`, `_pendingStateQueue`, or `_pendingForceUpdate`
	   * is set, update the component.
	   *
	   * @param {ReactReconcileTransaction} transaction
	   * @internal
	   */
	  performUpdateIfNecessary: function (transaction) {
	    if (this._pendingElement != null) {
	      ReactReconciler.receiveComponent(this, this._pendingElement || this._currentElement, transaction, this._context);
	    }

	    if (this._pendingStateQueue !== null || this._pendingForceUpdate) {
	      this.updateComponent(transaction, this._currentElement, this._currentElement, this._context, this._context);
	    }
	  },

	  /**
	   * Perform an update to a mounted component. The componentWillReceiveProps and
	   * shouldComponentUpdate methods are called, then (assuming the update isn't
	   * skipped) the remaining update lifecycle methods are called and the DOM
	   * representation is updated.
	   *
	   * By default, this implements React's rendering and reconciliation algorithm.
	   * Sophisticated clients may wish to override this.
	   *
	   * @param {ReactReconcileTransaction} transaction
	   * @param {ReactElement} prevParentElement
	   * @param {ReactElement} nextParentElement
	   * @internal
	   * @overridable
	   */
	  updateComponent: function (transaction, prevParentElement, nextParentElement, prevUnmaskedContext, nextUnmaskedContext) {
	    var inst = this._instance;

	    var nextContext = this._context === nextUnmaskedContext ? inst.context : this._processContext(nextUnmaskedContext);
	    var nextProps;

	    // Distinguish between a props update versus a simple state update
	    if (prevParentElement === nextParentElement) {
	      // Skip checking prop types again -- we don't read inst.props to avoid
	      // warning for DOM component props in this upgrade
	      nextProps = nextParentElement.props;
	    } else {
	      nextProps = this._processProps(nextParentElement.props);
	      // An update here will schedule an update but immediately set
	      // _pendingStateQueue which will ensure that any state updates gets
	      // immediately reconciled instead of waiting for the next batch.

	      if (inst.componentWillReceiveProps) {
	        inst.componentWillReceiveProps(nextProps, nextContext);
	      }
	    }

	    var nextState = this._processPendingState(nextProps, nextContext);

	    var shouldUpdate = this._pendingForceUpdate || !inst.shouldComponentUpdate || inst.shouldComponentUpdate(nextProps, nextState, nextContext);

	    if (process.env.NODE_ENV !== 'production') {
	      process.env.NODE_ENV !== 'production' ? warning(typeof shouldUpdate !== 'undefined', '%s.shouldComponentUpdate(): Returned undefined instead of a ' + 'boolean value. Make sure to return true or false.', this.getName() || 'ReactCompositeComponent') : undefined;
	    }

	    if (shouldUpdate) {
	      this._pendingForceUpdate = false;
	      // Will set `this.props`, `this.state` and `this.context`.
	      this._performComponentUpdate(nextParentElement, nextProps, nextState, nextContext, transaction, nextUnmaskedContext);
	    } else {
	      // If it's determined that a component should not update, we still want
	      // to set props and state but we shortcut the rest of the update.
	      this._currentElement = nextParentElement;
	      this._context = nextUnmaskedContext;
	      inst.props = nextProps;
	      inst.state = nextState;
	      inst.context = nextContext;
	    }
	  },

	  _processPendingState: function (props, context) {
	    var inst = this._instance;
	    var queue = this._pendingStateQueue;
	    var replace = this._pendingReplaceState;
	    this._pendingReplaceState = false;
	    this._pendingStateQueue = null;

	    if (!queue) {
	      return inst.state;
	    }

	    if (replace && queue.length === 1) {
	      return queue[0];
	    }

	    var nextState = assign({}, replace ? queue[0] : inst.state);
	    for (var i = replace ? 1 : 0; i < queue.length; i++) {
	      var partial = queue[i];
	      assign(nextState, typeof partial === 'function' ? partial.call(inst, nextState, props, context) : partial);
	    }

	    return nextState;
	  },

	  /**
	   * Merges new props and state, notifies delegate methods of update and
	   * performs update.
	   *
	   * @param {ReactElement} nextElement Next element
	   * @param {object} nextProps Next public object to set as properties.
	   * @param {?object} nextState Next object to set as state.
	   * @param {?object} nextContext Next public object to set as context.
	   * @param {ReactReconcileTransaction} transaction
	   * @param {?object} unmaskedContext
	   * @private
	   */
	  _performComponentUpdate: function (nextElement, nextProps, nextState, nextContext, transaction, unmaskedContext) {
	    var inst = this._instance;

	    var hasComponentDidUpdate = Boolean(inst.componentDidUpdate);
	    var prevProps;
	    var prevState;
	    var prevContext;
	    if (hasComponentDidUpdate) {
	      prevProps = inst.props;
	      prevState = inst.state;
	      prevContext = inst.context;
	    }

	    if (inst.componentWillUpdate) {
	      inst.componentWillUpdate(nextProps, nextState, nextContext);
	    }

	    this._currentElement = nextElement;
	    this._context = unmaskedContext;
	    inst.props = nextProps;
	    inst.state = nextState;
	    inst.context = nextContext;

	    this._updateRenderedComponent(transaction, unmaskedContext);

	    if (hasComponentDidUpdate) {
	      transaction.getReactMountReady().enqueue(inst.componentDidUpdate.bind(inst, prevProps, prevState, prevContext), inst);
	    }
	  },

	  /**
	   * Call the component's `render` method and update the DOM accordingly.
	   *
	   * @param {ReactReconcileTransaction} transaction
	   * @internal
	   */
	  _updateRenderedComponent: function (transaction, context) {
	    var prevComponentInstance = this._renderedComponent;
	    var prevRenderedElement = prevComponentInstance._currentElement;
	    var nextRenderedElement = this._renderValidatedComponent();
	    if (shouldUpdateReactComponent(prevRenderedElement, nextRenderedElement)) {
	      ReactReconciler.receiveComponent(prevComponentInstance, nextRenderedElement, transaction, this._processChildContext(context));
	    } else {
	      // These two IDs are actually the same! But nothing should rely on that.
	      var thisID = this._rootNodeID;
	      var prevComponentID = prevComponentInstance._rootNodeID;
	      ReactReconciler.unmountComponent(prevComponentInstance);

	      this._renderedComponent = this._instantiateReactComponent(nextRenderedElement);
	      var nextMarkup = ReactReconciler.mountComponent(this._renderedComponent, thisID, transaction, this._processChildContext(context));
	      this._replaceNodeWithMarkupByID(prevComponentID, nextMarkup);
	    }
	  },

	  /**
	   * @protected
	   */
	  _replaceNodeWithMarkupByID: function (prevComponentID, nextMarkup) {
	    ReactComponentEnvironment.replaceNodeWithMarkupByID(prevComponentID, nextMarkup);
	  },

	  /**
	   * @protected
	   */
	  _renderValidatedComponentWithoutOwnerOrContext: function () {
	    var inst = this._instance;
	    var renderedComponent = inst.render();
	    if (process.env.NODE_ENV !== 'production') {
	      // We allow auto-mocks to proceed as if they're returning null.
	      if (typeof renderedComponent === 'undefined' && inst.render._isMockFunction) {
	        // This is probably bad practice. Consider warning here and
	        // deprecating this convenience.
	        renderedComponent = null;
	      }
	    }

	    return renderedComponent;
	  },

	  /**
	   * @private
	   */
	  _renderValidatedComponent: function () {
	    var renderedComponent;
	    ReactCurrentOwner.current = this;
	    try {
	      renderedComponent = this._renderValidatedComponentWithoutOwnerOrContext();
	    } finally {
	      ReactCurrentOwner.current = null;
	    }
	    !(
	    // TODO: An `isValidNode` function would probably be more appropriate
	    renderedComponent === null || renderedComponent === false || ReactElement.isValidElement(renderedComponent)) ? process.env.NODE_ENV !== 'production' ? invariant(false, '%s.render(): A valid ReactComponent must be returned. You may have ' + 'returned undefined, an array or some other invalid object.', this.getName() || 'ReactCompositeComponent') : invariant(false) : undefined;
	    return renderedComponent;
	  },

	  /**
	   * Lazily allocates the refs object and stores `component` as `ref`.
	   *
	   * @param {string} ref Reference name.
	   * @param {component} component Component to store as `ref`.
	   * @final
	   * @private
	   */
	  attachRef: function (ref, component) {
	    var inst = this.getPublicInstance();
	    !(inst != null) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Stateless function components cannot have refs.') : invariant(false) : undefined;
	    var publicComponentInstance = component.getPublicInstance();
	    if (process.env.NODE_ENV !== 'production') {
	      var componentName = component && component.getName ? component.getName() : 'a component';
	      process.env.NODE_ENV !== 'production' ? warning(publicComponentInstance != null, 'Stateless function components cannot be given refs ' + '(See ref "%s" in %s created by %s). ' + 'Attempts to access this ref will fail.', ref, componentName, this.getName()) : undefined;
	    }
	    var refs = inst.refs === emptyObject ? inst.refs = {} : inst.refs;
	    refs[ref] = publicComponentInstance;
	  },

	  /**
	   * Detaches a reference name.
	   *
	   * @param {string} ref Name to dereference.
	   * @final
	   * @private
	   */
	  detachRef: function (ref) {
	    var refs = this.getPublicInstance().refs;
	    delete refs[ref];
	  },

	  /**
	   * Get a text description of the component that can be used to identify it
	   * in error messages.
	   * @return {string} The name or null.
	   * @internal
	   */
	  getName: function () {
	    var type = this._currentElement.type;
	    var constructor = this._instance && this._instance.constructor;
	    return type.displayName || constructor && constructor.displayName || type.name || constructor && constructor.name || null;
	  },

	  /**
	   * Get the publicly accessible representation of this component - i.e. what
	   * is exposed by refs and returned by render. Can be null for stateless
	   * components.
	   *
	   * @return {ReactComponent} the public component instance.
	   * @internal
	   */
	  getPublicInstance: function () {
	    var inst = this._instance;
	    if (inst instanceof StatelessComponent) {
	      return null;
	    }
	    return inst;
	  },

	  // Stub
	  _instantiateReactComponent: null

	};

	ReactPerf.measureMethods(ReactCompositeComponentMixin, 'ReactCompositeComponent', {
	  mountComponent: 'mountComponent',
	  updateComponent: 'updateComponent',
	  _renderValidatedComponent: '_renderValidatedComponent'
	});

	var ReactCompositeComponent = {

	  Mixin: ReactCompositeComponentMixin

	};

	module.exports = ReactCompositeComponent;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 67 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactComponentEnvironment
	 */

	'use strict';

	var invariant = __webpack_require__(16);

	var injected = false;

	var ReactComponentEnvironment = {

	  /**
	   * Optionally injectable environment dependent cleanup hook. (server vs.
	   * browser etc). Example: A browser system caches DOM nodes based on component
	   * ID and must remove that cache entry when this instance is unmounted.
	   */
	  unmountIDFromEnvironment: null,

	  /**
	   * Optionally injectable hook for swapping out mount images in the middle of
	   * the tree.
	   */
	  replaceNodeWithMarkupByID: null,

	  /**
	   * Optionally injectable hook for processing a queue of child updates. Will
	   * later move into MultiChildComponents.
	   */
	  processChildrenUpdates: null,

	  injection: {
	    injectEnvironment: function (environment) {
	      !!injected ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactCompositeComponent: injectEnvironment() can only be called once.') : invariant(false) : undefined;
	      ReactComponentEnvironment.unmountIDFromEnvironment = environment.unmountIDFromEnvironment;
	      ReactComponentEnvironment.replaceNodeWithMarkupByID = environment.replaceNodeWithMarkupByID;
	      ReactComponentEnvironment.processChildrenUpdates = environment.processChildrenUpdates;
	      injected = true;
	    }
	  }

	};

	module.exports = ReactComponentEnvironment;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 68 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactPropTypeLocations
	 */

	'use strict';

	var keyMirror = __webpack_require__(20);

	var ReactPropTypeLocations = keyMirror({
	  prop: null,
	  context: null,
	  childContext: null
	});

	module.exports = ReactPropTypeLocations;

/***/ },
/* 69 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactPropTypeLocationNames
	 */

	'use strict';

	var ReactPropTypeLocationNames = {};

	if (process.env.NODE_ENV !== 'production') {
	  ReactPropTypeLocationNames = {
	    prop: 'prop',
	    context: 'context',
	    childContext: 'child context'
	  };
	}

	module.exports = ReactPropTypeLocationNames;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 70 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule shouldUpdateReactComponent
	 * @typechecks static-only
	 */

	'use strict';

	/**
	 * Given a `prevElement` and `nextElement`, determines if the existing
	 * instance should be updated as opposed to being destroyed or replaced by a new
	 * instance. Both arguments are elements. This ensures that this logic can
	 * operate on stateless trees without any backing instance.
	 *
	 * @param {?object} prevElement
	 * @param {?object} nextElement
	 * @return {boolean} True if the existing instance should be updated.
	 * @protected
	 */
	function shouldUpdateReactComponent(prevElement, nextElement) {
	  var prevEmpty = prevElement === null || prevElement === false;
	  var nextEmpty = nextElement === null || nextElement === false;
	  if (prevEmpty || nextEmpty) {
	    return prevEmpty === nextEmpty;
	  }

	  var prevType = typeof prevElement;
	  var nextType = typeof nextElement;
	  if (prevType === 'string' || prevType === 'number') {
	    return nextType === 'string' || nextType === 'number';
	  } else {
	    return nextType === 'object' && prevElement.type === nextElement.type && prevElement.key === nextElement.key;
	  }
	  return false;
	}

	module.exports = shouldUpdateReactComponent;

/***/ },
/* 71 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactEmptyComponent
	 */

	'use strict';

	var ReactElement = __webpack_require__(45);
	var ReactEmptyComponentRegistry = __webpack_require__(47);
	var ReactReconciler = __webpack_require__(53);

	var assign = __webpack_require__(42);

	var placeholderElement;

	var ReactEmptyComponentInjection = {
	  injectEmptyComponent: function (component) {
	    placeholderElement = ReactElement.createElement(component);
	  }
	};

	function registerNullComponentID() {
	  ReactEmptyComponentRegistry.registerNullComponentID(this._rootNodeID);
	}

	var ReactEmptyComponent = function (instantiate) {
	  this._currentElement = null;
	  this._rootNodeID = null;
	  this._renderedComponent = instantiate(placeholderElement);
	};
	assign(ReactEmptyComponent.prototype, {
	  construct: function (element) {},
	  mountComponent: function (rootID, transaction, context) {
	    transaction.getReactMountReady().enqueue(registerNullComponentID, this);
	    this._rootNodeID = rootID;
	    return ReactReconciler.mountComponent(this._renderedComponent, rootID, transaction, context);
	  },
	  receiveComponent: function () {},
	  unmountComponent: function (rootID, transaction, context) {
	    ReactReconciler.unmountComponent(this._renderedComponent);
	    ReactEmptyComponentRegistry.deregisterNullComponentID(this._rootNodeID);
	    this._rootNodeID = null;
	    this._renderedComponent = null;
	  }
	});

	ReactEmptyComponent.injection = ReactEmptyComponentInjection;

	module.exports = ReactEmptyComponent;

/***/ },
/* 72 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactNativeComponent
	 */

	'use strict';

	var assign = __webpack_require__(42);
	var invariant = __webpack_require__(16);

	var autoGenerateWrapperClass = null;
	var genericComponentClass = null;
	// This registry keeps track of wrapper classes around native tags.
	var tagToComponentClass = {};
	var textComponentClass = null;

	var ReactNativeComponentInjection = {
	  // This accepts a class that receives the tag string. This is a catch all
	  // that can render any kind of tag.
	  injectGenericComponentClass: function (componentClass) {
	    genericComponentClass = componentClass;
	  },
	  // This accepts a text component class that takes the text string to be
	  // rendered as props.
	  injectTextComponentClass: function (componentClass) {
	    textComponentClass = componentClass;
	  },
	  // This accepts a keyed object with classes as values. Each key represents a
	  // tag. That particular tag will use this class instead of the generic one.
	  injectComponentClasses: function (componentClasses) {
	    assign(tagToComponentClass, componentClasses);
	  }
	};

	/**
	 * Get a composite component wrapper class for a specific tag.
	 *
	 * @param {ReactElement} element The tag for which to get the class.
	 * @return {function} The React class constructor function.
	 */
	function getComponentClassForElement(element) {
	  if (typeof element.type === 'function') {
	    return element.type;
	  }
	  var tag = element.type;
	  var componentClass = tagToComponentClass[tag];
	  if (componentClass == null) {
	    tagToComponentClass[tag] = componentClass = autoGenerateWrapperClass(tag);
	  }
	  return componentClass;
	}

	/**
	 * Get a native internal component class for a specific tag.
	 *
	 * @param {ReactElement} element The element to create.
	 * @return {function} The internal class constructor function.
	 */
	function createInternalComponent(element) {
	  !genericComponentClass ? process.env.NODE_ENV !== 'production' ? invariant(false, 'There is no registered component for the tag %s', element.type) : invariant(false) : undefined;
	  return new genericComponentClass(element.type, element.props);
	}

	/**
	 * @param {ReactText} text
	 * @return {ReactComponent}
	 */
	function createInstanceForText(text) {
	  return new textComponentClass(text);
	}

	/**
	 * @param {ReactComponent} component
	 * @return {boolean}
	 */
	function isTextComponent(component) {
	  return component instanceof textComponentClass;
	}

	var ReactNativeComponent = {
	  getComponentClassForElement: getComponentClassForElement,
	  createInternalComponent: createInternalComponent,
	  createInstanceForText: createInstanceForText,
	  isTextComponent: isTextComponent,
	  injection: ReactNativeComponentInjection
	};

	module.exports = ReactNativeComponent;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 73 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule validateDOMNesting
	 */

	'use strict';

	var assign = __webpack_require__(42);
	var emptyFunction = __webpack_require__(18);
	var warning = __webpack_require__(28);

	var validateDOMNesting = emptyFunction;

	if (process.env.NODE_ENV !== 'production') {
	  // This validation code was written based on the HTML5 parsing spec:
	  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
	  //
	  // Note: this does not catch all invalid nesting, nor does it try to (as it's
	  // not clear what practical benefit doing so provides); instead, we warn only
	  // for cases where the parser will give a parse tree differing from what React
	  // intended. For example, <b><div></div></b> is invalid but we don't warn
	  // because it still parses correctly; we do warn for other cases like nested
	  // <p> tags where the beginning of the second element implicitly closes the
	  // first, causing a confusing mess.

	  // https://html.spec.whatwg.org/multipage/syntax.html#special
	  var specialTags = ['address', 'applet', 'area', 'article', 'aside', 'base', 'basefont', 'bgsound', 'blockquote', 'body', 'br', 'button', 'caption', 'center', 'col', 'colgroup', 'dd', 'details', 'dir', 'div', 'dl', 'dt', 'embed', 'fieldset', 'figcaption', 'figure', 'footer', 'form', 'frame', 'frameset', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'head', 'header', 'hgroup', 'hr', 'html', 'iframe', 'img', 'input', 'isindex', 'li', 'link', 'listing', 'main', 'marquee', 'menu', 'menuitem', 'meta', 'nav', 'noembed', 'noframes', 'noscript', 'object', 'ol', 'p', 'param', 'plaintext', 'pre', 'script', 'section', 'select', 'source', 'style', 'summary', 'table', 'tbody', 'td', 'template', 'textarea', 'tfoot', 'th', 'thead', 'title', 'tr', 'track', 'ul', 'wbr', 'xmp'];

	  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
	  var inScopeTags = ['applet', 'caption', 'html', 'table', 'td', 'th', 'marquee', 'object', 'template',

	  // https://html.spec.whatwg.org/multipage/syntax.html#html-integration-point
	  // TODO: Distinguish by namespace here -- for <title>, including it here
	  // errs on the side of fewer warnings
	  'foreignObject', 'desc', 'title'];

	  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-button-scope
	  var buttonScopeTags = inScopeTags.concat(['button']);

	  // https://html.spec.whatwg.org/multipage/syntax.html#generate-implied-end-tags
	  var impliedEndTags = ['dd', 'dt', 'li', 'option', 'optgroup', 'p', 'rp', 'rt'];

	  var emptyAncestorInfo = {
	    parentTag: null,

	    formTag: null,
	    aTagInScope: null,
	    buttonTagInScope: null,
	    nobrTagInScope: null,
	    pTagInButtonScope: null,

	    listItemTagAutoclosing: null,
	    dlItemTagAutoclosing: null
	  };

	  var updatedAncestorInfo = function (oldInfo, tag, instance) {
	    var ancestorInfo = assign({}, oldInfo || emptyAncestorInfo);
	    var info = { tag: tag, instance: instance };

	    if (inScopeTags.indexOf(tag) !== -1) {
	      ancestorInfo.aTagInScope = null;
	      ancestorInfo.buttonTagInScope = null;
	      ancestorInfo.nobrTagInScope = null;
	    }
	    if (buttonScopeTags.indexOf(tag) !== -1) {
	      ancestorInfo.pTagInButtonScope = null;
	    }

	    // See rules for 'li', 'dd', 'dt' start tags in
	    // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody
	    if (specialTags.indexOf(tag) !== -1 && tag !== 'address' && tag !== 'div' && tag !== 'p') {
	      ancestorInfo.listItemTagAutoclosing = null;
	      ancestorInfo.dlItemTagAutoclosing = null;
	    }

	    ancestorInfo.parentTag = info;

	    if (tag === 'form') {
	      ancestorInfo.formTag = info;
	    }
	    if (tag === 'a') {
	      ancestorInfo.aTagInScope = info;
	    }
	    if (tag === 'button') {
	      ancestorInfo.buttonTagInScope = info;
	    }
	    if (tag === 'nobr') {
	      ancestorInfo.nobrTagInScope = info;
	    }
	    if (tag === 'p') {
	      ancestorInfo.pTagInButtonScope = info;
	    }
	    if (tag === 'li') {
	      ancestorInfo.listItemTagAutoclosing = info;
	    }
	    if (tag === 'dd' || tag === 'dt') {
	      ancestorInfo.dlItemTagAutoclosing = info;
	    }

	    return ancestorInfo;
	  };

	  /**
	   * Returns whether
	   */
	  var isTagValidWithParent = function (tag, parentTag) {
	    // First, let's check if we're in an unusual parsing mode...
	    switch (parentTag) {
	      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inselect
	      case 'select':
	        return tag === 'option' || tag === 'optgroup' || tag === '#text';
	      case 'optgroup':
	        return tag === 'option' || tag === '#text';
	      // Strictly speaking, seeing an <option> doesn't mean we're in a <select>
	      // but
	      case 'option':
	        return tag === '#text';

	      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intd
	      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incaption
	      // No special behavior since these rules fall back to "in body" mode for
	      // all except special table nodes which cause bad parsing behavior anyway.

	      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intr
	      case 'tr':
	        return tag === 'th' || tag === 'td' || tag === 'style' || tag === 'script' || tag === 'template';

	      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intbody
	      case 'tbody':
	      case 'thead':
	      case 'tfoot':
	        return tag === 'tr' || tag === 'style' || tag === 'script' || tag === 'template';

	      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incolgroup
	      case 'colgroup':
	        return tag === 'col' || tag === 'template';

	      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intable
	      case 'table':
	        return tag === 'caption' || tag === 'colgroup' || tag === 'tbody' || tag === 'tfoot' || tag === 'thead' || tag === 'style' || tag === 'script' || tag === 'template';

	      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inhead
	      case 'head':
	        return tag === 'base' || tag === 'basefont' || tag === 'bgsound' || tag === 'link' || tag === 'meta' || tag === 'title' || tag === 'noscript' || tag === 'noframes' || tag === 'style' || tag === 'script' || tag === 'template';

	      // https://html.spec.whatwg.org/multipage/semantics.html#the-html-element
	      case 'html':
	        return tag === 'head' || tag === 'body';
	    }

	    // Probably in the "in body" parsing mode, so we outlaw only tag combos
	    // where the parsing rules cause implicit opens or closes to be added.
	    // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody
	    switch (tag) {
	      case 'h1':
	      case 'h2':
	      case 'h3':
	      case 'h4':
	      case 'h5':
	      case 'h6':
	        return parentTag !== 'h1' && parentTag !== 'h2' && parentTag !== 'h3' && parentTag !== 'h4' && parentTag !== 'h5' && parentTag !== 'h6';

	      case 'rp':
	      case 'rt':
	        return impliedEndTags.indexOf(parentTag) === -1;

	      case 'caption':
	      case 'col':
	      case 'colgroup':
	      case 'frame':
	      case 'head':
	      case 'tbody':
	      case 'td':
	      case 'tfoot':
	      case 'th':
	      case 'thead':
	      case 'tr':
	        // These tags are only valid with a few parents that have special child
	        // parsing rules -- if we're down here, then none of those matched and
	        // so we allow it only if we don't know what the parent is, as all other
	        // cases are invalid.
	        return parentTag == null;
	    }

	    return true;
	  };

	  /**
	   * Returns whether
	   */
	  var findInvalidAncestorForTag = function (tag, ancestorInfo) {
	    switch (tag) {
	      case 'address':
	      case 'article':
	      case 'aside':
	      case 'blockquote':
	      case 'center':
	      case 'details':
	      case 'dialog':
	      case 'dir':
	      case 'div':
	      case 'dl':
	      case 'fieldset':
	      case 'figcaption':
	      case 'figure':
	      case 'footer':
	      case 'header':
	      case 'hgroup':
	      case 'main':
	      case 'menu':
	      case 'nav':
	      case 'ol':
	      case 'p':
	      case 'section':
	      case 'summary':
	      case 'ul':

	      case 'pre':
	      case 'listing':

	      case 'table':

	      case 'hr':

	      case 'xmp':

	      case 'h1':
	      case 'h2':
	      case 'h3':
	      case 'h4':
	      case 'h5':
	      case 'h6':
	        return ancestorInfo.pTagInButtonScope;

	      case 'form':
	        return ancestorInfo.formTag || ancestorInfo.pTagInButtonScope;

	      case 'li':
	        return ancestorInfo.listItemTagAutoclosing;

	      case 'dd':
	      case 'dt':
	        return ancestorInfo.dlItemTagAutoclosing;

	      case 'button':
	        return ancestorInfo.buttonTagInScope;

	      case 'a':
	        // Spec says something about storing a list of markers, but it sounds
	        // equivalent to this check.
	        return ancestorInfo.aTagInScope;

	      case 'nobr':
	        return ancestorInfo.nobrTagInScope;
	    }

	    return null;
	  };

	  /**
	   * Given a ReactCompositeComponent instance, return a list of its recursive
	   * owners, starting at the root and ending with the instance itself.
	   */
	  var findOwnerStack = function (instance) {
	    if (!instance) {
	      return [];
	    }

	    var stack = [];
	    /*eslint-disable space-after-keywords */
	    do {
	      /*eslint-enable space-after-keywords */
	      stack.push(instance);
	    } while (instance = instance._currentElement._owner);
	    stack.reverse();
	    return stack;
	  };

	  var didWarn = {};

	  validateDOMNesting = function (childTag, childInstance, ancestorInfo) {
	    ancestorInfo = ancestorInfo || emptyAncestorInfo;
	    var parentInfo = ancestorInfo.parentTag;
	    var parentTag = parentInfo && parentInfo.tag;

	    var invalidParent = isTagValidWithParent(childTag, parentTag) ? null : parentInfo;
	    var invalidAncestor = invalidParent ? null : findInvalidAncestorForTag(childTag, ancestorInfo);
	    var problematic = invalidParent || invalidAncestor;

	    if (problematic) {
	      var ancestorTag = problematic.tag;
	      var ancestorInstance = problematic.instance;

	      var childOwner = childInstance && childInstance._currentElement._owner;
	      var ancestorOwner = ancestorInstance && ancestorInstance._currentElement._owner;

	      var childOwners = findOwnerStack(childOwner);
	      var ancestorOwners = findOwnerStack(ancestorOwner);

	      var minStackLen = Math.min(childOwners.length, ancestorOwners.length);
	      var i;

	      var deepestCommon = -1;
	      for (i = 0; i < minStackLen; i++) {
	        if (childOwners[i] === ancestorOwners[i]) {
	          deepestCommon = i;
	        } else {
	          break;
	        }
	      }

	      var UNKNOWN = '(unknown)';
	      var childOwnerNames = childOwners.slice(deepestCommon + 1).map(function (inst) {
	        return inst.getName() || UNKNOWN;
	      });
	      var ancestorOwnerNames = ancestorOwners.slice(deepestCommon + 1).map(function (inst) {
	        return inst.getName() || UNKNOWN;
	      });
	      var ownerInfo = [].concat(
	      // If the parent and child instances have a common owner ancestor, start
	      // with that -- otherwise we just start with the parent's owners.
	      deepestCommon !== -1 ? childOwners[deepestCommon].getName() || UNKNOWN : [], ancestorOwnerNames, ancestorTag,
	      // If we're warning about an invalid (non-parent) ancestry, add '...'
	      invalidAncestor ? ['...'] : [], childOwnerNames, childTag).join(' > ');

	      var warnKey = !!invalidParent + '|' + childTag + '|' + ancestorTag + '|' + ownerInfo;
	      if (didWarn[warnKey]) {
	        return;
	      }
	      didWarn[warnKey] = true;

	      if (invalidParent) {
	        var info = '';
	        if (ancestorTag === 'table' && childTag === 'tr') {
	          info += ' Add a <tbody> to your code to match the DOM tree generated by ' + 'the browser.';
	        }
	        process.env.NODE_ENV !== 'production' ? warning(false, 'validateDOMNesting(...): <%s> cannot appear as a child of <%s>. ' + 'See %s.%s', childTag, ancestorTag, ownerInfo, info) : undefined;
	      } else {
	        process.env.NODE_ENV !== 'production' ? warning(false, 'validateDOMNesting(...): <%s> cannot appear as a descendant of ' + '<%s>. See %s.', childTag, ancestorTag, ownerInfo) : undefined;
	      }
	    }
	  };

	  validateDOMNesting.ancestorInfoContextKey = '__validateDOMNesting_ancestorInfo$' + Math.random().toString(36).slice(2);

	  validateDOMNesting.updatedAncestorInfo = updatedAncestorInfo;

	  // For testing
	  validateDOMNesting.isTagValidInContext = function (tag, ancestorInfo) {
	    ancestorInfo = ancestorInfo || emptyAncestorInfo;
	    var parentInfo = ancestorInfo.parentTag;
	    var parentTag = parentInfo && parentInfo.tag;
	    return isTagValidWithParent(tag, parentTag) && !findInvalidAncestorForTag(tag, ancestorInfo);
	  };
	}

	module.exports = validateDOMNesting;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 74 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDefaultInjection
	 */

	'use strict';

	var BeforeInputEventPlugin = __webpack_require__(75);
	var ChangeEventPlugin = __webpack_require__(83);
	var ClientReactRootIndex = __webpack_require__(86);
	var DefaultEventPluginOrder = __webpack_require__(87);
	var EnterLeaveEventPlugin = __webpack_require__(88);
	var ExecutionEnvironment = __webpack_require__(12);
	var HTMLDOMPropertyConfig = __webpack_require__(92);
	var ReactBrowserComponentMixin = __webpack_require__(93);
	var ReactComponentBrowserEnvironment = __webpack_require__(29);
	var ReactDefaultBatchingStrategy = __webpack_require__(95);
	var ReactDOMComponent = __webpack_require__(96);
	var ReactDOMTextComponent = __webpack_require__(9);
	var ReactEventListener = __webpack_require__(121);
	var ReactInjection = __webpack_require__(124);
	var ReactInstanceHandles = __webpack_require__(48);
	var ReactMount = __webpack_require__(31);
	var ReactReconcileTransaction = __webpack_require__(128);
	var SelectEventPlugin = __webpack_require__(133);
	var ServerReactRootIndex = __webpack_require__(134);
	var SimpleEventPlugin = __webpack_require__(135);
	var SVGDOMPropertyConfig = __webpack_require__(144);

	var alreadyInjected = false;

	function inject() {
	  if (alreadyInjected) {
	    // TODO: This is currently true because these injections are shared between
	    // the client and the server package. They should be built independently
	    // and not share any injection state. Then this problem will be solved.
	    return;
	  }
	  alreadyInjected = true;

	  ReactInjection.EventEmitter.injectReactEventListener(ReactEventListener);

	  /**
	   * Inject modules for resolving DOM hierarchy and plugin ordering.
	   */
	  ReactInjection.EventPluginHub.injectEventPluginOrder(DefaultEventPluginOrder);
	  ReactInjection.EventPluginHub.injectInstanceHandle(ReactInstanceHandles);
	  ReactInjection.EventPluginHub.injectMount(ReactMount);

	  /**
	   * Some important event plugins included by default (without having to require
	   * them).
	   */
	  ReactInjection.EventPluginHub.injectEventPluginsByName({
	    SimpleEventPlugin: SimpleEventPlugin,
	    EnterLeaveEventPlugin: EnterLeaveEventPlugin,
	    ChangeEventPlugin: ChangeEventPlugin,
	    SelectEventPlugin: SelectEventPlugin,
	    BeforeInputEventPlugin: BeforeInputEventPlugin
	  });

	  ReactInjection.NativeComponent.injectGenericComponentClass(ReactDOMComponent);

	  ReactInjection.NativeComponent.injectTextComponentClass(ReactDOMTextComponent);

	  ReactInjection.Class.injectMixin(ReactBrowserComponentMixin);

	  ReactInjection.DOMProperty.injectDOMPropertyConfig(HTMLDOMPropertyConfig);
	  ReactInjection.DOMProperty.injectDOMPropertyConfig(SVGDOMPropertyConfig);

	  ReactInjection.EmptyComponent.injectEmptyComponent('noscript');

	  ReactInjection.Updates.injectReconcileTransaction(ReactReconcileTransaction);
	  ReactInjection.Updates.injectBatchingStrategy(ReactDefaultBatchingStrategy);

	  ReactInjection.RootIndex.injectCreateReactRootIndex(ExecutionEnvironment.canUseDOM ? ClientReactRootIndex.createReactRootIndex : ServerReactRootIndex.createReactRootIndex);

	  ReactInjection.Component.injectEnvironment(ReactComponentBrowserEnvironment);

	  if (process.env.NODE_ENV !== 'production') {
	    var url = ExecutionEnvironment.canUseDOM && window.location.href || '';
	    if (/[?&]react_perf\b/.test(url)) {
	      var ReactDefaultPerf = __webpack_require__(145);
	      ReactDefaultPerf.start();
	    }
	  }
	}

	module.exports = {
	  inject: inject
	};
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 75 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015 Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule BeforeInputEventPlugin
	 * @typechecks static-only
	 */

	'use strict';

	var EventConstants = __webpack_require__(33);
	var EventPropagators = __webpack_require__(76);
	var ExecutionEnvironment = __webpack_require__(12);
	var FallbackCompositionState = __webpack_require__(77);
	var SyntheticCompositionEvent = __webpack_require__(79);
	var SyntheticInputEvent = __webpack_require__(81);

	var keyOf = __webpack_require__(82);

	var END_KEYCODES = [9, 13, 27, 32]; // Tab, Return, Esc, Space
	var START_KEYCODE = 229;

	var canUseCompositionEvent = ExecutionEnvironment.canUseDOM && 'CompositionEvent' in window;

	var documentMode = null;
	if (ExecutionEnvironment.canUseDOM && 'documentMode' in document) {
	  documentMode = document.documentMode;
	}

	// Webkit offers a very useful `textInput` event that can be used to
	// directly represent `beforeInput`. The IE `textinput` event is not as
	// useful, so we don't use it.
	var canUseTextInputEvent = ExecutionEnvironment.canUseDOM && 'TextEvent' in window && !documentMode && !isPresto();

	// In IE9+, we have access to composition events, but the data supplied
	// by the native compositionend event may be incorrect. Japanese ideographic
	// spaces, for instance (\u3000) are not recorded correctly.
	var useFallbackCompositionData = ExecutionEnvironment.canUseDOM && (!canUseCompositionEvent || documentMode && documentMode > 8 && documentMode <= 11);

	/**
	 * Opera <= 12 includes TextEvent in window, but does not fire
	 * text input events. Rely on keypress instead.
	 */
	function isPresto() {
	  var opera = window.opera;
	  return typeof opera === 'object' && typeof opera.version === 'function' && parseInt(opera.version(), 10) <= 12;
	}

	var SPACEBAR_CODE = 32;
	var SPACEBAR_CHAR = String.fromCharCode(SPACEBAR_CODE);

	var topLevelTypes = EventConstants.topLevelTypes;

	// Events and their corresponding property names.
	var eventTypes = {
	  beforeInput: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onBeforeInput: null }),
	      captured: keyOf({ onBeforeInputCapture: null })
	    },
	    dependencies: [topLevelTypes.topCompositionEnd, topLevelTypes.topKeyPress, topLevelTypes.topTextInput, topLevelTypes.topPaste]
	  },
	  compositionEnd: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onCompositionEnd: null }),
	      captured: keyOf({ onCompositionEndCapture: null })
	    },
	    dependencies: [topLevelTypes.topBlur, topLevelTypes.topCompositionEnd, topLevelTypes.topKeyDown, topLevelTypes.topKeyPress, topLevelTypes.topKeyUp, topLevelTypes.topMouseDown]
	  },
	  compositionStart: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onCompositionStart: null }),
	      captured: keyOf({ onCompositionStartCapture: null })
	    },
	    dependencies: [topLevelTypes.topBlur, topLevelTypes.topCompositionStart, topLevelTypes.topKeyDown, topLevelTypes.topKeyPress, topLevelTypes.topKeyUp, topLevelTypes.topMouseDown]
	  },
	  compositionUpdate: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onCompositionUpdate: null }),
	      captured: keyOf({ onCompositionUpdateCapture: null })
	    },
	    dependencies: [topLevelTypes.topBlur, topLevelTypes.topCompositionUpdate, topLevelTypes.topKeyDown, topLevelTypes.topKeyPress, topLevelTypes.topKeyUp, topLevelTypes.topMouseDown]
	  }
	};

	// Track whether we've ever handled a keypress on the space key.
	var hasSpaceKeypress = false;

	/**
	 * Return whether a native keypress event is assumed to be a command.
	 * This is required because Firefox fires `keypress` events for key commands
	 * (cut, copy, select-all, etc.) even though no character is inserted.
	 */
	function isKeypressCommand(nativeEvent) {
	  return (nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) &&
	  // ctrlKey && altKey is equivalent to AltGr, and is not a command.
	  !(nativeEvent.ctrlKey && nativeEvent.altKey);
	}

	/**
	 * Translate native top level events into event types.
	 *
	 * @param {string} topLevelType
	 * @return {object}
	 */
	function getCompositionEventType(topLevelType) {
	  switch (topLevelType) {
	    case topLevelTypes.topCompositionStart:
	      return eventTypes.compositionStart;
	    case topLevelTypes.topCompositionEnd:
	      return eventTypes.compositionEnd;
	    case topLevelTypes.topCompositionUpdate:
	      return eventTypes.compositionUpdate;
	  }
	}

	/**
	 * Does our fallback best-guess model think this event signifies that
	 * composition has begun?
	 *
	 * @param {string} topLevelType
	 * @param {object} nativeEvent
	 * @return {boolean}
	 */
	function isFallbackCompositionStart(topLevelType, nativeEvent) {
	  return topLevelType === topLevelTypes.topKeyDown && nativeEvent.keyCode === START_KEYCODE;
	}

	/**
	 * Does our fallback mode think that this event is the end of composition?
	 *
	 * @param {string} topLevelType
	 * @param {object} nativeEvent
	 * @return {boolean}
	 */
	function isFallbackCompositionEnd(topLevelType, nativeEvent) {
	  switch (topLevelType) {
	    case topLevelTypes.topKeyUp:
	      // Command keys insert or clear IME input.
	      return END_KEYCODES.indexOf(nativeEvent.keyCode) !== -1;
	    case topLevelTypes.topKeyDown:
	      // Expect IME keyCode on each keydown. If we get any other
	      // code we must have exited earlier.
	      return nativeEvent.keyCode !== START_KEYCODE;
	    case topLevelTypes.topKeyPress:
	    case topLevelTypes.topMouseDown:
	    case topLevelTypes.topBlur:
	      // Events are not possible without cancelling IME.
	      return true;
	    default:
	      return false;
	  }
	}

	/**
	 * Google Input Tools provides composition data via a CustomEvent,
	 * with the `data` property populated in the `detail` object. If this
	 * is available on the event object, use it. If not, this is a plain
	 * composition event and we have nothing special to extract.
	 *
	 * @param {object} nativeEvent
	 * @return {?string}
	 */
	function getDataFromCustomEvent(nativeEvent) {
	  var detail = nativeEvent.detail;
	  if (typeof detail === 'object' && 'data' in detail) {
	    return detail.data;
	  }
	  return null;
	}

	// Track the current IME composition fallback object, if any.
	var currentComposition = null;

	/**
	 * @param {string} topLevelType Record from `EventConstants`.
	 * @param {DOMEventTarget} topLevelTarget The listening component root node.
	 * @param {string} topLevelTargetID ID of `topLevelTarget`.
	 * @param {object} nativeEvent Native browser event.
	 * @return {?object} A SyntheticCompositionEvent.
	 */
	function extractCompositionEvent(topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
	  var eventType;
	  var fallbackData;

	  if (canUseCompositionEvent) {
	    eventType = getCompositionEventType(topLevelType);
	  } else if (!currentComposition) {
	    if (isFallbackCompositionStart(topLevelType, nativeEvent)) {
	      eventType = eventTypes.compositionStart;
	    }
	  } else if (isFallbackCompositionEnd(topLevelType, nativeEvent)) {
	    eventType = eventTypes.compositionEnd;
	  }

	  if (!eventType) {
	    return null;
	  }

	  if (useFallbackCompositionData) {
	    // The current composition is stored statically and must not be
	    // overwritten while composition continues.
	    if (!currentComposition && eventType === eventTypes.compositionStart) {
	      currentComposition = FallbackCompositionState.getPooled(topLevelTarget);
	    } else if (eventType === eventTypes.compositionEnd) {
	      if (currentComposition) {
	        fallbackData = currentComposition.getData();
	      }
	    }
	  }

	  var event = SyntheticCompositionEvent.getPooled(eventType, topLevelTargetID, nativeEvent, nativeEventTarget);

	  if (fallbackData) {
	    // Inject data generated from fallback path into the synthetic event.
	    // This matches the property of native CompositionEventInterface.
	    event.data = fallbackData;
	  } else {
	    var customData = getDataFromCustomEvent(nativeEvent);
	    if (customData !== null) {
	      event.data = customData;
	    }
	  }

	  EventPropagators.accumulateTwoPhaseDispatches(event);
	  return event;
	}

	/**
	 * @param {string} topLevelType Record from `EventConstants`.
	 * @param {object} nativeEvent Native browser event.
	 * @return {?string} The string corresponding to this `beforeInput` event.
	 */
	function getNativeBeforeInputChars(topLevelType, nativeEvent) {
	  switch (topLevelType) {
	    case topLevelTypes.topCompositionEnd:
	      return getDataFromCustomEvent(nativeEvent);
	    case topLevelTypes.topKeyPress:
	      /**
	       * If native `textInput` events are available, our goal is to make
	       * use of them. However, there is a special case: the spacebar key.
	       * In Webkit, preventing default on a spacebar `textInput` event
	       * cancels character insertion, but it *also* causes the browser
	       * to fall back to its default spacebar behavior of scrolling the
	       * page.
	       *
	       * Tracking at:
	       * https://code.google.com/p/chromium/issues/detail?id=355103
	       *
	       * To avoid this issue, use the keypress event as if no `textInput`
	       * event is available.
	       */
	      var which = nativeEvent.which;
	      if (which !== SPACEBAR_CODE) {
	        return null;
	      }

	      hasSpaceKeypress = true;
	      return SPACEBAR_CHAR;

	    case topLevelTypes.topTextInput:
	      // Record the characters to be added to the DOM.
	      var chars = nativeEvent.data;

	      // If it's a spacebar character, assume that we have already handled
	      // it at the keypress level and bail immediately. Android Chrome
	      // doesn't give us keycodes, so we need to blacklist it.
	      if (chars === SPACEBAR_CHAR && hasSpaceKeypress) {
	        return null;
	      }

	      return chars;

	    default:
	      // For other native event types, do nothing.
	      return null;
	  }
	}

	/**
	 * For browsers that do not provide the `textInput` event, extract the
	 * appropriate string to use for SyntheticInputEvent.
	 *
	 * @param {string} topLevelType Record from `EventConstants`.
	 * @param {object} nativeEvent Native browser event.
	 * @return {?string} The fallback string for this `beforeInput` event.
	 */
	function getFallbackBeforeInputChars(topLevelType, nativeEvent) {
	  // If we are currently composing (IME) and using a fallback to do so,
	  // try to extract the composed characters from the fallback object.
	  if (currentComposition) {
	    if (topLevelType === topLevelTypes.topCompositionEnd || isFallbackCompositionEnd(topLevelType, nativeEvent)) {
	      var chars = currentComposition.getData();
	      FallbackCompositionState.release(currentComposition);
	      currentComposition = null;
	      return chars;
	    }
	    return null;
	  }

	  switch (topLevelType) {
	    case topLevelTypes.topPaste:
	      // If a paste event occurs after a keypress, throw out the input
	      // chars. Paste events should not lead to BeforeInput events.
	      return null;
	    case topLevelTypes.topKeyPress:
	      /**
	       * As of v27, Firefox may fire keypress events even when no character
	       * will be inserted. A few possibilities:
	       *
	       * - `which` is `0`. Arrow keys, Esc key, etc.
	       *
	       * - `which` is the pressed key code, but no char is available.
	       *   Ex: 'AltGr + d` in Polish. There is no modified character for
	       *   this key combination and no character is inserted into the
	       *   document, but FF fires the keypress for char code `100` anyway.
	       *   No `input` event will occur.
	       *
	       * - `which` is the pressed key code, but a command combination is
	       *   being used. Ex: `Cmd+C`. No character is inserted, and no
	       *   `input` event will occur.
	       */
	      if (nativeEvent.which && !isKeypressCommand(nativeEvent)) {
	        return String.fromCharCode(nativeEvent.which);
	      }
	      return null;
	    case topLevelTypes.topCompositionEnd:
	      return useFallbackCompositionData ? null : nativeEvent.data;
	    default:
	      return null;
	  }
	}

	/**
	 * Extract a SyntheticInputEvent for `beforeInput`, based on either native
	 * `textInput` or fallback behavior.
	 *
	 * @param {string} topLevelType Record from `EventConstants`.
	 * @param {DOMEventTarget} topLevelTarget The listening component root node.
	 * @param {string} topLevelTargetID ID of `topLevelTarget`.
	 * @param {object} nativeEvent Native browser event.
	 * @return {?object} A SyntheticInputEvent.
	 */
	function extractBeforeInputEvent(topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
	  var chars;

	  if (canUseTextInputEvent) {
	    chars = getNativeBeforeInputChars(topLevelType, nativeEvent);
	  } else {
	    chars = getFallbackBeforeInputChars(topLevelType, nativeEvent);
	  }

	  // If no characters are being inserted, no BeforeInput event should
	  // be fired.
	  if (!chars) {
	    return null;
	  }

	  var event = SyntheticInputEvent.getPooled(eventTypes.beforeInput, topLevelTargetID, nativeEvent, nativeEventTarget);

	  event.data = chars;
	  EventPropagators.accumulateTwoPhaseDispatches(event);
	  return event;
	}

	/**
	 * Create an `onBeforeInput` event to match
	 * http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105/#events-inputevents.
	 *
	 * This event plugin is based on the native `textInput` event
	 * available in Chrome, Safari, Opera, and IE. This event fires after
	 * `onKeyPress` and `onCompositionEnd`, but before `onInput`.
	 *
	 * `beforeInput` is spec'd but not implemented in any browsers, and
	 * the `input` event does not provide any useful information about what has
	 * actually been added, contrary to the spec. Thus, `textInput` is the best
	 * available event to identify the characters that have actually been inserted
	 * into the target node.
	 *
	 * This plugin is also responsible for emitting `composition` events, thus
	 * allowing us to share composition fallback code for both `beforeInput` and
	 * `composition` event types.
	 */
	var BeforeInputEventPlugin = {

	  eventTypes: eventTypes,

	  /**
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {DOMEventTarget} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native browser event.
	   * @return {*} An accumulation of synthetic events.
	   * @see {EventPluginHub.extractEvents}
	   */
	  extractEvents: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
	    return [extractCompositionEvent(topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget), extractBeforeInputEvent(topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget)];
	  }
	};

	module.exports = BeforeInputEventPlugin;

/***/ },
/* 76 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule EventPropagators
	 */

	'use strict';

	var EventConstants = __webpack_require__(33);
	var EventPluginHub = __webpack_require__(34);

	var warning = __webpack_require__(28);

	var accumulateInto = __webpack_require__(38);
	var forEachAccumulated = __webpack_require__(39);

	var PropagationPhases = EventConstants.PropagationPhases;
	var getListener = EventPluginHub.getListener;

	/**
	 * Some event types have a notion of different registration names for different
	 * "phases" of propagation. This finds listeners by a given phase.
	 */
	function listenerAtPhase(id, event, propagationPhase) {
	  var registrationName = event.dispatchConfig.phasedRegistrationNames[propagationPhase];
	  return getListener(id, registrationName);
	}

	/**
	 * Tags a `SyntheticEvent` with dispatched listeners. Creating this function
	 * here, allows us to not have to bind or create functions for each event.
	 * Mutating the event's members allows us to not have to create a wrapping
	 * "dispatch" object that pairs the event with the listener.
	 */
	function accumulateDirectionalDispatches(domID, upwards, event) {
	  if (process.env.NODE_ENV !== 'production') {
	    process.env.NODE_ENV !== 'production' ? warning(domID, 'Dispatching id must not be null') : undefined;
	  }
	  var phase = upwards ? PropagationPhases.bubbled : PropagationPhases.captured;
	  var listener = listenerAtPhase(domID, event, phase);
	  if (listener) {
	    event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
	    event._dispatchIDs = accumulateInto(event._dispatchIDs, domID);
	  }
	}

	/**
	 * Collect dispatches (must be entirely collected before dispatching - see unit
	 * tests). Lazily allocate the array to conserve memory.  We must loop through
	 * each event and perform the traversal for each one. We cannot perform a
	 * single traversal for the entire collection of events because each event may
	 * have a different target.
	 */
	function accumulateTwoPhaseDispatchesSingle(event) {
	  if (event && event.dispatchConfig.phasedRegistrationNames) {
	    EventPluginHub.injection.getInstanceHandle().traverseTwoPhase(event.dispatchMarker, accumulateDirectionalDispatches, event);
	  }
	}

	/**
	 * Same as `accumulateTwoPhaseDispatchesSingle`, but skips over the targetID.
	 */
	function accumulateTwoPhaseDispatchesSingleSkipTarget(event) {
	  if (event && event.dispatchConfig.phasedRegistrationNames) {
	    EventPluginHub.injection.getInstanceHandle().traverseTwoPhaseSkipTarget(event.dispatchMarker, accumulateDirectionalDispatches, event);
	  }
	}

	/**
	 * Accumulates without regard to direction, does not look for phased
	 * registration names. Same as `accumulateDirectDispatchesSingle` but without
	 * requiring that the `dispatchMarker` be the same as the dispatched ID.
	 */
	function accumulateDispatches(id, ignoredDirection, event) {
	  if (event && event.dispatchConfig.registrationName) {
	    var registrationName = event.dispatchConfig.registrationName;
	    var listener = getListener(id, registrationName);
	    if (listener) {
	      event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
	      event._dispatchIDs = accumulateInto(event._dispatchIDs, id);
	    }
	  }
	}

	/**
	 * Accumulates dispatches on an `SyntheticEvent`, but only for the
	 * `dispatchMarker`.
	 * @param {SyntheticEvent} event
	 */
	function accumulateDirectDispatchesSingle(event) {
	  if (event && event.dispatchConfig.registrationName) {
	    accumulateDispatches(event.dispatchMarker, null, event);
	  }
	}

	function accumulateTwoPhaseDispatches(events) {
	  forEachAccumulated(events, accumulateTwoPhaseDispatchesSingle);
	}

	function accumulateTwoPhaseDispatchesSkipTarget(events) {
	  forEachAccumulated(events, accumulateTwoPhaseDispatchesSingleSkipTarget);
	}

	function accumulateEnterLeaveDispatches(leave, enter, fromID, toID) {
	  EventPluginHub.injection.getInstanceHandle().traverseEnterLeave(fromID, toID, accumulateDispatches, leave, enter);
	}

	function accumulateDirectDispatches(events) {
	  forEachAccumulated(events, accumulateDirectDispatchesSingle);
	}

	/**
	 * A small set of propagation patterns, each of which will accept a small amount
	 * of information, and generate a set of "dispatch ready event objects" - which
	 * are sets of events that have already been annotated with a set of dispatched
	 * listener functions/ids. The API is designed this way to discourage these
	 * propagation strategies from actually executing the dispatches, since we
	 * always want to collect the entire set of dispatches before executing event a
	 * single one.
	 *
	 * @constructor EventPropagators
	 */
	var EventPropagators = {
	  accumulateTwoPhaseDispatches: accumulateTwoPhaseDispatches,
	  accumulateTwoPhaseDispatchesSkipTarget: accumulateTwoPhaseDispatchesSkipTarget,
	  accumulateDirectDispatches: accumulateDirectDispatches,
	  accumulateEnterLeaveDispatches: accumulateEnterLeaveDispatches
	};

	module.exports = EventPropagators;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 77 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule FallbackCompositionState
	 * @typechecks static-only
	 */

	'use strict';

	var PooledClass = __webpack_require__(59);

	var assign = __webpack_require__(42);
	var getTextContentAccessor = __webpack_require__(78);

	/**
	 * This helper class stores information about text content of a target node,
	 * allowing comparison of content before and after a given event.
	 *
	 * Identify the node where selection currently begins, then observe
	 * both its text content and its current position in the DOM. Since the
	 * browser may natively replace the target node during composition, we can
	 * use its position to find its replacement.
	 *
	 * @param {DOMEventTarget} root
	 */
	function FallbackCompositionState(root) {
	  this._root = root;
	  this._startText = this.getText();
	  this._fallbackText = null;
	}

	assign(FallbackCompositionState.prototype, {
	  destructor: function () {
	    this._root = null;
	    this._startText = null;
	    this._fallbackText = null;
	  },

	  /**
	   * Get current text of input.
	   *
	   * @return {string}
	   */
	  getText: function () {
	    if ('value' in this._root) {
	      return this._root.value;
	    }
	    return this._root[getTextContentAccessor()];
	  },

	  /**
	   * Determine the differing substring between the initially stored
	   * text content and the current content.
	   *
	   * @return {string}
	   */
	  getData: function () {
	    if (this._fallbackText) {
	      return this._fallbackText;
	    }

	    var start;
	    var startValue = this._startText;
	    var startLength = startValue.length;
	    var end;
	    var endValue = this.getText();
	    var endLength = endValue.length;

	    for (start = 0; start < startLength; start++) {
	      if (startValue[start] !== endValue[start]) {
	        break;
	      }
	    }

	    var minEnd = startLength - start;
	    for (end = 1; end <= minEnd; end++) {
	      if (startValue[startLength - end] !== endValue[endLength - end]) {
	        break;
	      }
	    }

	    var sliceTail = end > 1 ? 1 - end : undefined;
	    this._fallbackText = endValue.slice(start, sliceTail);
	    return this._fallbackText;
	  }
	});

	PooledClass.addPoolingTo(FallbackCompositionState);

	module.exports = FallbackCompositionState;

/***/ },
/* 78 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getTextContentAccessor
	 */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(12);

	var contentKey = null;

	/**
	 * Gets the key used to access text content on a DOM node.
	 *
	 * @return {?string} Key used to access text content.
	 * @internal
	 */
	function getTextContentAccessor() {
	  if (!contentKey && ExecutionEnvironment.canUseDOM) {
	    // Prefer textContent to innerText because many browsers support both but
	    // SVG <text> elements don't support innerText even when <div> does.
	    contentKey = 'textContent' in document.documentElement ? 'textContent' : 'innerText';
	  }
	  return contentKey;
	}

	module.exports = getTextContentAccessor;

/***/ },
/* 79 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticCompositionEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticEvent = __webpack_require__(80);

	/**
	 * @interface Event
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/#events-compositionevents
	 */
	var CompositionEventInterface = {
	  data: null
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticCompositionEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
	  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
	}

	SyntheticEvent.augmentClass(SyntheticCompositionEvent, CompositionEventInterface);

	module.exports = SyntheticCompositionEvent;

/***/ },
/* 80 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticEvent
	 * @typechecks static-only
	 */

	'use strict';

	var PooledClass = __webpack_require__(59);

	var assign = __webpack_require__(42);
	var emptyFunction = __webpack_require__(18);
	var warning = __webpack_require__(28);

	/**
	 * @interface Event
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var EventInterface = {
	  type: null,
	  target: null,
	  // currentTarget is set when dispatching; no use in copying it here
	  currentTarget: emptyFunction.thatReturnsNull,
	  eventPhase: null,
	  bubbles: null,
	  cancelable: null,
	  timeStamp: function (event) {
	    return event.timeStamp || Date.now();
	  },
	  defaultPrevented: null,
	  isTrusted: null
	};

	/**
	 * Synthetic events are dispatched by event plugins, typically in response to a
	 * top-level event delegation handler.
	 *
	 * These systems should generally use pooling to reduce the frequency of garbage
	 * collection. The system should check `isPersistent` to determine whether the
	 * event should be released into the pool after being dispatched. Users that
	 * need a persisted event should invoke `persist`.
	 *
	 * Synthetic events (and subclasses) implement the DOM Level 3 Events API by
	 * normalizing browser quirks. Subclasses do not necessarily have to implement a
	 * DOM interface; custom application-specific events can also subclass this.
	 *
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 */
	function SyntheticEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
	  this.dispatchConfig = dispatchConfig;
	  this.dispatchMarker = dispatchMarker;
	  this.nativeEvent = nativeEvent;

	  var Interface = this.constructor.Interface;
	  for (var propName in Interface) {
	    if (!Interface.hasOwnProperty(propName)) {
	      continue;
	    }
	    var normalize = Interface[propName];
	    if (normalize) {
	      this[propName] = normalize(nativeEvent);
	    } else {
	      if (propName === 'target') {
	        this.target = nativeEventTarget;
	      } else {
	        this[propName] = nativeEvent[propName];
	      }
	    }
	  }

	  var defaultPrevented = nativeEvent.defaultPrevented != null ? nativeEvent.defaultPrevented : nativeEvent.returnValue === false;
	  if (defaultPrevented) {
	    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
	  } else {
	    this.isDefaultPrevented = emptyFunction.thatReturnsFalse;
	  }
	  this.isPropagationStopped = emptyFunction.thatReturnsFalse;
	}

	assign(SyntheticEvent.prototype, {

	  preventDefault: function () {
	    this.defaultPrevented = true;
	    var event = this.nativeEvent;
	    if (process.env.NODE_ENV !== 'production') {
	      process.env.NODE_ENV !== 'production' ? warning(event, 'This synthetic event is reused for performance reasons. If you\'re ' + 'seeing this, you\'re calling `preventDefault` on a ' + 'released/nullified synthetic event. This is a no-op. See ' + 'https://fb.me/react-event-pooling for more information.') : undefined;
	    }
	    if (!event) {
	      return;
	    }

	    if (event.preventDefault) {
	      event.preventDefault();
	    } else {
	      event.returnValue = false;
	    }
	    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
	  },

	  stopPropagation: function () {
	    var event = this.nativeEvent;
	    if (process.env.NODE_ENV !== 'production') {
	      process.env.NODE_ENV !== 'production' ? warning(event, 'This synthetic event is reused for performance reasons. If you\'re ' + 'seeing this, you\'re calling `stopPropagation` on a ' + 'released/nullified synthetic event. This is a no-op. See ' + 'https://fb.me/react-event-pooling for more information.') : undefined;
	    }
	    if (!event) {
	      return;
	    }

	    if (event.stopPropagation) {
	      event.stopPropagation();
	    } else {
	      event.cancelBubble = true;
	    }
	    this.isPropagationStopped = emptyFunction.thatReturnsTrue;
	  },

	  /**
	   * We release all dispatched `SyntheticEvent`s after each event loop, adding
	   * them back into the pool. This allows a way to hold onto a reference that
	   * won't be added back into the pool.
	   */
	  persist: function () {
	    this.isPersistent = emptyFunction.thatReturnsTrue;
	  },

	  /**
	   * Checks if this event should be released back into the pool.
	   *
	   * @return {boolean} True if this should not be released, false otherwise.
	   */
	  isPersistent: emptyFunction.thatReturnsFalse,

	  /**
	   * `PooledClass` looks for `destructor` on each instance it releases.
	   */
	  destructor: function () {
	    var Interface = this.constructor.Interface;
	    for (var propName in Interface) {
	      this[propName] = null;
	    }
	    this.dispatchConfig = null;
	    this.dispatchMarker = null;
	    this.nativeEvent = null;
	  }

	});

	SyntheticEvent.Interface = EventInterface;

	/**
	 * Helper to reduce boilerplate when creating subclasses.
	 *
	 * @param {function} Class
	 * @param {?object} Interface
	 */
	SyntheticEvent.augmentClass = function (Class, Interface) {
	  var Super = this;

	  var prototype = Object.create(Super.prototype);
	  assign(prototype, Class.prototype);
	  Class.prototype = prototype;
	  Class.prototype.constructor = Class;

	  Class.Interface = assign({}, Super.Interface, Interface);
	  Class.augmentClass = Super.augmentClass;

	  PooledClass.addPoolingTo(Class, PooledClass.fourArgumentPooler);
	};

	PooledClass.addPoolingTo(SyntheticEvent, PooledClass.fourArgumentPooler);

	module.exports = SyntheticEvent;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 81 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticInputEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticEvent = __webpack_require__(80);

	/**
	 * @interface Event
	 * @see http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105
	 *      /#events-inputevents
	 */
	var InputEventInterface = {
	  data: null
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticInputEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
	  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
	}

	SyntheticEvent.augmentClass(SyntheticInputEvent, InputEventInterface);

	module.exports = SyntheticInputEvent;

/***/ },
/* 82 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule keyOf
	 */

	/**
	 * Allows extraction of a minified key. Let's the build system minify keys
	 * without losing the ability to dynamically use key strings as values
	 * themselves. Pass in an object with a single key/val pair and it will return
	 * you the string key of that single record. Suppose you want to grab the
	 * value for a key 'className' inside of an object. Key/val minification may
	 * have aliased that key to be 'xa12'. keyOf({className: null}) will return
	 * 'xa12' in that case. Resolve keys you want to use once at startup time, then
	 * reuse those resolutions.
	 */
	"use strict";

	var keyOf = function (oneKeyObj) {
	  var key;
	  for (key in oneKeyObj) {
	    if (!oneKeyObj.hasOwnProperty(key)) {
	      continue;
	    }
	    return key;
	  }
	  return null;
	};

	module.exports = keyOf;

/***/ },
/* 83 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ChangeEventPlugin
	 */

	'use strict';

	var EventConstants = __webpack_require__(33);
	var EventPluginHub = __webpack_require__(34);
	var EventPropagators = __webpack_require__(76);
	var ExecutionEnvironment = __webpack_require__(12);
	var ReactUpdates = __webpack_require__(57);
	var SyntheticEvent = __webpack_require__(80);

	var getEventTarget = __webpack_require__(84);
	var isEventSupported = __webpack_require__(43);
	var isTextInputElement = __webpack_require__(85);
	var keyOf = __webpack_require__(82);

	var topLevelTypes = EventConstants.topLevelTypes;

	var eventTypes = {
	  change: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onChange: null }),
	      captured: keyOf({ onChangeCapture: null })
	    },
	    dependencies: [topLevelTypes.topBlur, topLevelTypes.topChange, topLevelTypes.topClick, topLevelTypes.topFocus, topLevelTypes.topInput, topLevelTypes.topKeyDown, topLevelTypes.topKeyUp, topLevelTypes.topSelectionChange]
	  }
	};

	/**
	 * For IE shims
	 */
	var activeElement = null;
	var activeElementID = null;
	var activeElementValue = null;
	var activeElementValueProp = null;

	/**
	 * SECTION: handle `change` event
	 */
	function shouldUseChangeEvent(elem) {
	  var nodeName = elem.nodeName && elem.nodeName.toLowerCase();
	  return nodeName === 'select' || nodeName === 'input' && elem.type === 'file';
	}

	var doesChangeEventBubble = false;
	if (ExecutionEnvironment.canUseDOM) {
	  // See `handleChange` comment below
	  doesChangeEventBubble = isEventSupported('change') && (!('documentMode' in document) || document.documentMode > 8);
	}

	function manualDispatchChangeEvent(nativeEvent) {
	  var event = SyntheticEvent.getPooled(eventTypes.change, activeElementID, nativeEvent, getEventTarget(nativeEvent));
	  EventPropagators.accumulateTwoPhaseDispatches(event);

	  // If change and propertychange bubbled, we'd just bind to it like all the
	  // other events and have it go through ReactBrowserEventEmitter. Since it
	  // doesn't, we manually listen for the events and so we have to enqueue and
	  // process the abstract event manually.
	  //
	  // Batching is necessary here in order to ensure that all event handlers run
	  // before the next rerender (including event handlers attached to ancestor
	  // elements instead of directly on the input). Without this, controlled
	  // components don't work properly in conjunction with event bubbling because
	  // the component is rerendered and the value reverted before all the event
	  // handlers can run. See https://github.com/facebook/react/issues/708.
	  ReactUpdates.batchedUpdates(runEventInBatch, event);
	}

	function runEventInBatch(event) {
	  EventPluginHub.enqueueEvents(event);
	  EventPluginHub.processEventQueue(false);
	}

	function startWatchingForChangeEventIE8(target, targetID) {
	  activeElement = target;
	  activeElementID = targetID;
	  activeElement.attachEvent('onchange', manualDispatchChangeEvent);
	}

	function stopWatchingForChangeEventIE8() {
	  if (!activeElement) {
	    return;
	  }
	  activeElement.detachEvent('onchange', manualDispatchChangeEvent);
	  activeElement = null;
	  activeElementID = null;
	}

	function getTargetIDForChangeEvent(topLevelType, topLevelTarget, topLevelTargetID) {
	  if (topLevelType === topLevelTypes.topChange) {
	    return topLevelTargetID;
	  }
	}
	function handleEventsForChangeEventIE8(topLevelType, topLevelTarget, topLevelTargetID) {
	  if (topLevelType === topLevelTypes.topFocus) {
	    // stopWatching() should be a noop here but we call it just in case we
	    // missed a blur event somehow.
	    stopWatchingForChangeEventIE8();
	    startWatchingForChangeEventIE8(topLevelTarget, topLevelTargetID);
	  } else if (topLevelType === topLevelTypes.topBlur) {
	    stopWatchingForChangeEventIE8();
	  }
	}

	/**
	 * SECTION: handle `input` event
	 */
	var isInputEventSupported = false;
	if (ExecutionEnvironment.canUseDOM) {
	  // IE9 claims to support the input event but fails to trigger it when
	  // deleting text, so we ignore its input events
	  isInputEventSupported = isEventSupported('input') && (!('documentMode' in document) || document.documentMode > 9);
	}

	/**
	 * (For old IE.) Replacement getter/setter for the `value` property that gets
	 * set on the active element.
	 */
	var newValueProp = {
	  get: function () {
	    return activeElementValueProp.get.call(this);
	  },
	  set: function (val) {
	    // Cast to a string so we can do equality checks.
	    activeElementValue = '' + val;
	    activeElementValueProp.set.call(this, val);
	  }
	};

	/**
	 * (For old IE.) Starts tracking propertychange events on the passed-in element
	 * and override the value property so that we can distinguish user events from
	 * value changes in JS.
	 */
	function startWatchingForValueChange(target, targetID) {
	  activeElement = target;
	  activeElementID = targetID;
	  activeElementValue = target.value;
	  activeElementValueProp = Object.getOwnPropertyDescriptor(target.constructor.prototype, 'value');

	  // Not guarded in a canDefineProperty check: IE8 supports defineProperty only
	  // on DOM elements
	  Object.defineProperty(activeElement, 'value', newValueProp);
	  activeElement.attachEvent('onpropertychange', handlePropertyChange);
	}

	/**
	 * (For old IE.) Removes the event listeners from the currently-tracked element,
	 * if any exists.
	 */
	function stopWatchingForValueChange() {
	  if (!activeElement) {
	    return;
	  }

	  // delete restores the original property definition
	  delete activeElement.value;
	  activeElement.detachEvent('onpropertychange', handlePropertyChange);

	  activeElement = null;
	  activeElementID = null;
	  activeElementValue = null;
	  activeElementValueProp = null;
	}

	/**
	 * (For old IE.) Handles a propertychange event, sending a `change` event if
	 * the value of the active element has changed.
	 */
	function handlePropertyChange(nativeEvent) {
	  if (nativeEvent.propertyName !== 'value') {
	    return;
	  }
	  var value = nativeEvent.srcElement.value;
	  if (value === activeElementValue) {
	    return;
	  }
	  activeElementValue = value;

	  manualDispatchChangeEvent(nativeEvent);
	}

	/**
	 * If a `change` event should be fired, returns the target's ID.
	 */
	function getTargetIDForInputEvent(topLevelType, topLevelTarget, topLevelTargetID) {
	  if (topLevelType === topLevelTypes.topInput) {
	    // In modern browsers (i.e., not IE8 or IE9), the input event is exactly
	    // what we want so fall through here and trigger an abstract event
	    return topLevelTargetID;
	  }
	}

	// For IE8 and IE9.
	function handleEventsForInputEventIE(topLevelType, topLevelTarget, topLevelTargetID) {
	  if (topLevelType === topLevelTypes.topFocus) {
	    // In IE8, we can capture almost all .value changes by adding a
	    // propertychange handler and looking for events with propertyName
	    // equal to 'value'
	    // In IE9, propertychange fires for most input events but is buggy and
	    // doesn't fire when text is deleted, but conveniently, selectionchange
	    // appears to fire in all of the remaining cases so we catch those and
	    // forward the event if the value has changed
	    // In either case, we don't want to call the event handler if the value
	    // is changed from JS so we redefine a setter for `.value` that updates
	    // our activeElementValue variable, allowing us to ignore those changes
	    //
	    // stopWatching() should be a noop here but we call it just in case we
	    // missed a blur event somehow.
	    stopWatchingForValueChange();
	    startWatchingForValueChange(topLevelTarget, topLevelTargetID);
	  } else if (topLevelType === topLevelTypes.topBlur) {
	    stopWatchingForValueChange();
	  }
	}

	// For IE8 and IE9.
	function getTargetIDForInputEventIE(topLevelType, topLevelTarget, topLevelTargetID) {
	  if (topLevelType === topLevelTypes.topSelectionChange || topLevelType === topLevelTypes.topKeyUp || topLevelType === topLevelTypes.topKeyDown) {
	    // On the selectionchange event, the target is just document which isn't
	    // helpful for us so just check activeElement instead.
	    //
	    // 99% of the time, keydown and keyup aren't necessary. IE8 fails to fire
	    // propertychange on the first input event after setting `value` from a
	    // script and fires only keydown, keypress, keyup. Catching keyup usually
	    // gets it and catching keydown lets us fire an event for the first
	    // keystroke if user does a key repeat (it'll be a little delayed: right
	    // before the second keystroke). Other input methods (e.g., paste) seem to
	    // fire selectionchange normally.
	    if (activeElement && activeElement.value !== activeElementValue) {
	      activeElementValue = activeElement.value;
	      return activeElementID;
	    }
	  }
	}

	/**
	 * SECTION: handle `click` event
	 */
	function shouldUseClickEvent(elem) {
	  // Use the `click` event to detect changes to checkbox and radio inputs.
	  // This approach works across all browsers, whereas `change` does not fire
	  // until `blur` in IE8.
	  return elem.nodeName && elem.nodeName.toLowerCase() === 'input' && (elem.type === 'checkbox' || elem.type === 'radio');
	}

	function getTargetIDForClickEvent(topLevelType, topLevelTarget, topLevelTargetID) {
	  if (topLevelType === topLevelTypes.topClick) {
	    return topLevelTargetID;
	  }
	}

	/**
	 * This plugin creates an `onChange` event that normalizes change events
	 * across form elements. This event fires at a time when it's possible to
	 * change the element's value without seeing a flicker.
	 *
	 * Supported elements are:
	 * - input (see `isTextInputElement`)
	 * - textarea
	 * - select
	 */
	var ChangeEventPlugin = {

	  eventTypes: eventTypes,

	  /**
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {DOMEventTarget} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native browser event.
	   * @return {*} An accumulation of synthetic events.
	   * @see {EventPluginHub.extractEvents}
	   */
	  extractEvents: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {

	    var getTargetIDFunc, handleEventFunc;
	    if (shouldUseChangeEvent(topLevelTarget)) {
	      if (doesChangeEventBubble) {
	        getTargetIDFunc = getTargetIDForChangeEvent;
	      } else {
	        handleEventFunc = handleEventsForChangeEventIE8;
	      }
	    } else if (isTextInputElement(topLevelTarget)) {
	      if (isInputEventSupported) {
	        getTargetIDFunc = getTargetIDForInputEvent;
	      } else {
	        getTargetIDFunc = getTargetIDForInputEventIE;
	        handleEventFunc = handleEventsForInputEventIE;
	      }
	    } else if (shouldUseClickEvent(topLevelTarget)) {
	      getTargetIDFunc = getTargetIDForClickEvent;
	    }

	    if (getTargetIDFunc) {
	      var targetID = getTargetIDFunc(topLevelType, topLevelTarget, topLevelTargetID);
	      if (targetID) {
	        var event = SyntheticEvent.getPooled(eventTypes.change, targetID, nativeEvent, nativeEventTarget);
	        event.type = 'change';
	        EventPropagators.accumulateTwoPhaseDispatches(event);
	        return event;
	      }
	    }

	    if (handleEventFunc) {
	      handleEventFunc(topLevelType, topLevelTarget, topLevelTargetID);
	    }
	  }

	};

	module.exports = ChangeEventPlugin;

/***/ },
/* 84 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getEventTarget
	 * @typechecks static-only
	 */

	'use strict';

	/**
	 * Gets the target node from a native browser event by accounting for
	 * inconsistencies in browser DOM APIs.
	 *
	 * @param {object} nativeEvent Native browser event.
	 * @return {DOMEventTarget} Target node.
	 */
	function getEventTarget(nativeEvent) {
	  var target = nativeEvent.target || nativeEvent.srcElement || window;
	  // Safari may fire events on text nodes (Node.TEXT_NODE is 3).
	  // @see http://www.quirksmode.org/js/events_properties.html
	  return target.nodeType === 3 ? target.parentNode : target;
	}

	module.exports = getEventTarget;

/***/ },
/* 85 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule isTextInputElement
	 */

	'use strict';

	/**
	 * @see http://www.whatwg.org/specs/web-apps/current-work/multipage/the-input-element.html#input-type-attr-summary
	 */
	var supportedInputTypes = {
	  'color': true,
	  'date': true,
	  'datetime': true,
	  'datetime-local': true,
	  'email': true,
	  'month': true,
	  'number': true,
	  'password': true,
	  'range': true,
	  'search': true,
	  'tel': true,
	  'text': true,
	  'time': true,
	  'url': true,
	  'week': true
	};

	function isTextInputElement(elem) {
	  var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
	  return nodeName && (nodeName === 'input' && supportedInputTypes[elem.type] || nodeName === 'textarea');
	}

	module.exports = isTextInputElement;

/***/ },
/* 86 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ClientReactRootIndex
	 * @typechecks
	 */

	'use strict';

	var nextReactRootIndex = 0;

	var ClientReactRootIndex = {
	  createReactRootIndex: function () {
	    return nextReactRootIndex++;
	  }
	};

	module.exports = ClientReactRootIndex;

/***/ },
/* 87 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule DefaultEventPluginOrder
	 */

	'use strict';

	var keyOf = __webpack_require__(82);

	/**
	 * Module that is injectable into `EventPluginHub`, that specifies a
	 * deterministic ordering of `EventPlugin`s. A convenient way to reason about
	 * plugins, without having to package every one of them. This is better than
	 * having plugins be ordered in the same order that they are injected because
	 * that ordering would be influenced by the packaging order.
	 * `ResponderEventPlugin` must occur before `SimpleEventPlugin` so that
	 * preventing default on events is convenient in `SimpleEventPlugin` handlers.
	 */
	var DefaultEventPluginOrder = [keyOf({ ResponderEventPlugin: null }), keyOf({ SimpleEventPlugin: null }), keyOf({ TapEventPlugin: null }), keyOf({ EnterLeaveEventPlugin: null }), keyOf({ ChangeEventPlugin: null }), keyOf({ SelectEventPlugin: null }), keyOf({ BeforeInputEventPlugin: null })];

	module.exports = DefaultEventPluginOrder;

/***/ },
/* 88 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule EnterLeaveEventPlugin
	 * @typechecks static-only
	 */

	'use strict';

	var EventConstants = __webpack_require__(33);
	var EventPropagators = __webpack_require__(76);
	var SyntheticMouseEvent = __webpack_require__(89);

	var ReactMount = __webpack_require__(31);
	var keyOf = __webpack_require__(82);

	var topLevelTypes = EventConstants.topLevelTypes;
	var getFirstReactDOM = ReactMount.getFirstReactDOM;

	var eventTypes = {
	  mouseEnter: {
	    registrationName: keyOf({ onMouseEnter: null }),
	    dependencies: [topLevelTypes.topMouseOut, topLevelTypes.topMouseOver]
	  },
	  mouseLeave: {
	    registrationName: keyOf({ onMouseLeave: null }),
	    dependencies: [topLevelTypes.topMouseOut, topLevelTypes.topMouseOver]
	  }
	};

	var extractedEvents = [null, null];

	var EnterLeaveEventPlugin = {

	  eventTypes: eventTypes,

	  /**
	   * For almost every interaction we care about, there will be both a top-level
	   * `mouseover` and `mouseout` event that occurs. Only use `mouseout` so that
	   * we do not extract duplicate events. However, moving the mouse into the
	   * browser from outside will not fire a `mouseout` event. In this case, we use
	   * the `mouseover` top-level event.
	   *
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {DOMEventTarget} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native browser event.
	   * @return {*} An accumulation of synthetic events.
	   * @see {EventPluginHub.extractEvents}
	   */
	  extractEvents: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
	    if (topLevelType === topLevelTypes.topMouseOver && (nativeEvent.relatedTarget || nativeEvent.fromElement)) {
	      return null;
	    }
	    if (topLevelType !== topLevelTypes.topMouseOut && topLevelType !== topLevelTypes.topMouseOver) {
	      // Must not be a mouse in or mouse out - ignoring.
	      return null;
	    }

	    var win;
	    if (topLevelTarget.window === topLevelTarget) {
	      // `topLevelTarget` is probably a window object.
	      win = topLevelTarget;
	    } else {
	      // TODO: Figure out why `ownerDocument` is sometimes undefined in IE8.
	      var doc = topLevelTarget.ownerDocument;
	      if (doc) {
	        win = doc.defaultView || doc.parentWindow;
	      } else {
	        win = window;
	      }
	    }

	    var from;
	    var to;
	    var fromID = '';
	    var toID = '';
	    if (topLevelType === topLevelTypes.topMouseOut) {
	      from = topLevelTarget;
	      fromID = topLevelTargetID;
	      to = getFirstReactDOM(nativeEvent.relatedTarget || nativeEvent.toElement);
	      if (to) {
	        toID = ReactMount.getID(to);
	      } else {
	        to = win;
	      }
	      to = to || win;
	    } else {
	      from = win;
	      to = topLevelTarget;
	      toID = topLevelTargetID;
	    }

	    if (from === to) {
	      // Nothing pertains to our managed components.
	      return null;
	    }

	    var leave = SyntheticMouseEvent.getPooled(eventTypes.mouseLeave, fromID, nativeEvent, nativeEventTarget);
	    leave.type = 'mouseleave';
	    leave.target = from;
	    leave.relatedTarget = to;

	    var enter = SyntheticMouseEvent.getPooled(eventTypes.mouseEnter, toID, nativeEvent, nativeEventTarget);
	    enter.type = 'mouseenter';
	    enter.target = to;
	    enter.relatedTarget = from;

	    EventPropagators.accumulateEnterLeaveDispatches(leave, enter, fromID, toID);

	    extractedEvents[0] = leave;
	    extractedEvents[1] = enter;

	    return extractedEvents;
	  }

	};

	module.exports = EnterLeaveEventPlugin;

/***/ },
/* 89 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticMouseEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticUIEvent = __webpack_require__(90);
	var ViewportMetrics = __webpack_require__(41);

	var getEventModifierState = __webpack_require__(91);

	/**
	 * @interface MouseEvent
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var MouseEventInterface = {
	  screenX: null,
	  screenY: null,
	  clientX: null,
	  clientY: null,
	  ctrlKey: null,
	  shiftKey: null,
	  altKey: null,
	  metaKey: null,
	  getModifierState: getEventModifierState,
	  button: function (event) {
	    // Webkit, Firefox, IE9+
	    // which:  1 2 3
	    // button: 0 1 2 (standard)
	    var button = event.button;
	    if ('which' in event) {
	      return button;
	    }
	    // IE<9
	    // which:  undefined
	    // button: 0 0 0
	    // button: 1 4 2 (onmouseup)
	    return button === 2 ? 2 : button === 4 ? 1 : 0;
	  },
	  buttons: null,
	  relatedTarget: function (event) {
	    return event.relatedTarget || (event.fromElement === event.srcElement ? event.toElement : event.fromElement);
	  },
	  // "Proprietary" Interface.
	  pageX: function (event) {
	    return 'pageX' in event ? event.pageX : event.clientX + ViewportMetrics.currentScrollLeft;
	  },
	  pageY: function (event) {
	    return 'pageY' in event ? event.pageY : event.clientY + ViewportMetrics.currentScrollTop;
	  }
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticMouseEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
	  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
	}

	SyntheticUIEvent.augmentClass(SyntheticMouseEvent, MouseEventInterface);

	module.exports = SyntheticMouseEvent;

/***/ },
/* 90 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticUIEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticEvent = __webpack_require__(80);

	var getEventTarget = __webpack_require__(84);

	/**
	 * @interface UIEvent
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var UIEventInterface = {
	  view: function (event) {
	    if (event.view) {
	      return event.view;
	    }

	    var target = getEventTarget(event);
	    if (target != null && target.window === target) {
	      // target is a window object
	      return target;
	    }

	    var doc = target.ownerDocument;
	    // TODO: Figure out why `ownerDocument` is sometimes undefined in IE8.
	    if (doc) {
	      return doc.defaultView || doc.parentWindow;
	    } else {
	      return window;
	    }
	  },
	  detail: function (event) {
	    return event.detail || 0;
	  }
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticEvent}
	 */
	function SyntheticUIEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
	  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
	}

	SyntheticEvent.augmentClass(SyntheticUIEvent, UIEventInterface);

	module.exports = SyntheticUIEvent;

/***/ },
/* 91 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getEventModifierState
	 * @typechecks static-only
	 */

	'use strict';

	/**
	 * Translation from modifier key to the associated property in the event.
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/#keys-Modifiers
	 */

	var modifierKeyToProp = {
	  'Alt': 'altKey',
	  'Control': 'ctrlKey',
	  'Meta': 'metaKey',
	  'Shift': 'shiftKey'
	};

	// IE8 does not implement getModifierState so we simply map it to the only
	// modifier keys exposed by the event itself, does not support Lock-keys.
	// Currently, all major browsers except Chrome seems to support Lock-keys.
	function modifierStateGetter(keyArg) {
	  var syntheticEvent = this;
	  var nativeEvent = syntheticEvent.nativeEvent;
	  if (nativeEvent.getModifierState) {
	    return nativeEvent.getModifierState(keyArg);
	  }
	  var keyProp = modifierKeyToProp[keyArg];
	  return keyProp ? !!nativeEvent[keyProp] : false;
	}

	function getEventModifierState(nativeEvent) {
	  return modifierStateGetter;
	}

	module.exports = getEventModifierState;

/***/ },
/* 92 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule HTMLDOMPropertyConfig
	 */

	'use strict';

	var DOMProperty = __webpack_require__(26);
	var ExecutionEnvironment = __webpack_require__(12);

	var MUST_USE_ATTRIBUTE = DOMProperty.injection.MUST_USE_ATTRIBUTE;
	var MUST_USE_PROPERTY = DOMProperty.injection.MUST_USE_PROPERTY;
	var HAS_BOOLEAN_VALUE = DOMProperty.injection.HAS_BOOLEAN_VALUE;
	var HAS_SIDE_EFFECTS = DOMProperty.injection.HAS_SIDE_EFFECTS;
	var HAS_NUMERIC_VALUE = DOMProperty.injection.HAS_NUMERIC_VALUE;
	var HAS_POSITIVE_NUMERIC_VALUE = DOMProperty.injection.HAS_POSITIVE_NUMERIC_VALUE;
	var HAS_OVERLOADED_BOOLEAN_VALUE = DOMProperty.injection.HAS_OVERLOADED_BOOLEAN_VALUE;

	var hasSVG;
	if (ExecutionEnvironment.canUseDOM) {
	  var implementation = document.implementation;
	  hasSVG = implementation && implementation.hasFeature && implementation.hasFeature('http://www.w3.org/TR/SVG11/feature#BasicStructure', '1.1');
	}

	var HTMLDOMPropertyConfig = {
	  isCustomAttribute: RegExp.prototype.test.bind(/^(data|aria)-[a-z_][a-z\d_.\-]*$/),
	  Properties: {
	    /**
	     * Standard Properties
	     */
	    accept: null,
	    acceptCharset: null,
	    accessKey: null,
	    action: null,
	    allowFullScreen: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
	    allowTransparency: MUST_USE_ATTRIBUTE,
	    alt: null,
	    async: HAS_BOOLEAN_VALUE,
	    autoComplete: null,
	    // autoFocus is polyfilled/normalized by AutoFocusUtils
	    // autoFocus: HAS_BOOLEAN_VALUE,
	    autoPlay: HAS_BOOLEAN_VALUE,
	    capture: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
	    cellPadding: null,
	    cellSpacing: null,
	    charSet: MUST_USE_ATTRIBUTE,
	    challenge: MUST_USE_ATTRIBUTE,
	    checked: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    classID: MUST_USE_ATTRIBUTE,
	    // To set className on SVG elements, it's necessary to use .setAttribute;
	    // this works on HTML elements too in all browsers except IE8. Conveniently,
	    // IE8 doesn't support SVG and so we can simply use the attribute in
	    // browsers that support SVG and the property in browsers that don't,
	    // regardless of whether the element is HTML or SVG.
	    className: hasSVG ? MUST_USE_ATTRIBUTE : MUST_USE_PROPERTY,
	    cols: MUST_USE_ATTRIBUTE | HAS_POSITIVE_NUMERIC_VALUE,
	    colSpan: null,
	    content: null,
	    contentEditable: null,
	    contextMenu: MUST_USE_ATTRIBUTE,
	    controls: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    coords: null,
	    crossOrigin: null,
	    data: null, // For `<object />` acts as `src`.
	    dateTime: MUST_USE_ATTRIBUTE,
	    'default': HAS_BOOLEAN_VALUE,
	    defer: HAS_BOOLEAN_VALUE,
	    dir: null,
	    disabled: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
	    download: HAS_OVERLOADED_BOOLEAN_VALUE,
	    draggable: null,
	    encType: null,
	    form: MUST_USE_ATTRIBUTE,
	    formAction: MUST_USE_ATTRIBUTE,
	    formEncType: MUST_USE_ATTRIBUTE,
	    formMethod: MUST_USE_ATTRIBUTE,
	    formNoValidate: HAS_BOOLEAN_VALUE,
	    formTarget: MUST_USE_ATTRIBUTE,
	    frameBorder: MUST_USE_ATTRIBUTE,
	    headers: null,
	    height: MUST_USE_ATTRIBUTE,
	    hidden: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
	    high: null,
	    href: null,
	    hrefLang: null,
	    htmlFor: null,
	    httpEquiv: null,
	    icon: null,
	    id: MUST_USE_PROPERTY,
	    inputMode: MUST_USE_ATTRIBUTE,
	    integrity: null,
	    is: MUST_USE_ATTRIBUTE,
	    keyParams: MUST_USE_ATTRIBUTE,
	    keyType: MUST_USE_ATTRIBUTE,
	    kind: null,
	    label: null,
	    lang: null,
	    list: MUST_USE_ATTRIBUTE,
	    loop: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    low: null,
	    manifest: MUST_USE_ATTRIBUTE,
	    marginHeight: null,
	    marginWidth: null,
	    max: null,
	    maxLength: MUST_USE_ATTRIBUTE,
	    media: MUST_USE_ATTRIBUTE,
	    mediaGroup: null,
	    method: null,
	    min: null,
	    minLength: MUST_USE_ATTRIBUTE,
	    multiple: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    muted: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    name: null,
	    nonce: MUST_USE_ATTRIBUTE,
	    noValidate: HAS_BOOLEAN_VALUE,
	    open: HAS_BOOLEAN_VALUE,
	    optimum: null,
	    pattern: null,
	    placeholder: null,
	    poster: null,
	    preload: null,
	    radioGroup: null,
	    readOnly: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    rel: null,
	    required: HAS_BOOLEAN_VALUE,
	    reversed: HAS_BOOLEAN_VALUE,
	    role: MUST_USE_ATTRIBUTE,
	    rows: MUST_USE_ATTRIBUTE | HAS_POSITIVE_NUMERIC_VALUE,
	    rowSpan: null,
	    sandbox: null,
	    scope: null,
	    scoped: HAS_BOOLEAN_VALUE,
	    scrolling: null,
	    seamless: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
	    selected: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    shape: null,
	    size: MUST_USE_ATTRIBUTE | HAS_POSITIVE_NUMERIC_VALUE,
	    sizes: MUST_USE_ATTRIBUTE,
	    span: HAS_POSITIVE_NUMERIC_VALUE,
	    spellCheck: null,
	    src: null,
	    srcDoc: MUST_USE_PROPERTY,
	    srcLang: null,
	    srcSet: MUST_USE_ATTRIBUTE,
	    start: HAS_NUMERIC_VALUE,
	    step: null,
	    style: null,
	    summary: null,
	    tabIndex: null,
	    target: null,
	    title: null,
	    type: null,
	    useMap: null,
	    value: MUST_USE_PROPERTY | HAS_SIDE_EFFECTS,
	    width: MUST_USE_ATTRIBUTE,
	    wmode: MUST_USE_ATTRIBUTE,
	    wrap: null,

	    /**
	     * RDFa Properties
	     */
	    about: MUST_USE_ATTRIBUTE,
	    datatype: MUST_USE_ATTRIBUTE,
	    inlist: MUST_USE_ATTRIBUTE,
	    prefix: MUST_USE_ATTRIBUTE,
	    // property is also supported for OpenGraph in meta tags.
	    property: MUST_USE_ATTRIBUTE,
	    resource: MUST_USE_ATTRIBUTE,
	    'typeof': MUST_USE_ATTRIBUTE,
	    vocab: MUST_USE_ATTRIBUTE,

	    /**
	     * Non-standard Properties
	     */
	    // autoCapitalize and autoCorrect are supported in Mobile Safari for
	    // keyboard hints.
	    autoCapitalize: MUST_USE_ATTRIBUTE,
	    autoCorrect: MUST_USE_ATTRIBUTE,
	    // autoSave allows WebKit/Blink to persist values of input fields on page reloads
	    autoSave: null,
	    // color is for Safari mask-icon link
	    color: null,
	    // itemProp, itemScope, itemType are for
	    // Microdata support. See http://schema.org/docs/gs.html
	    itemProp: MUST_USE_ATTRIBUTE,
	    itemScope: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
	    itemType: MUST_USE_ATTRIBUTE,
	    // itemID and itemRef are for Microdata support as well but
	    // only specified in the the WHATWG spec document. See
	    // https://html.spec.whatwg.org/multipage/microdata.html#microdata-dom-api
	    itemID: MUST_USE_ATTRIBUTE,
	    itemRef: MUST_USE_ATTRIBUTE,
	    // results show looking glass icon and recent searches on input
	    // search fields in WebKit/Blink
	    results: null,
	    // IE-only attribute that specifies security restrictions on an iframe
	    // as an alternative to the sandbox attribute on IE<10
	    security: MUST_USE_ATTRIBUTE,
	    // IE-only attribute that controls focus behavior
	    unselectable: MUST_USE_ATTRIBUTE
	  },
	  DOMAttributeNames: {
	    acceptCharset: 'accept-charset',
	    className: 'class',
	    htmlFor: 'for',
	    httpEquiv: 'http-equiv'
	  },
	  DOMPropertyNames: {
	    autoComplete: 'autocomplete',
	    autoFocus: 'autofocus',
	    autoPlay: 'autoplay',
	    autoSave: 'autosave',
	    // `encoding` is equivalent to `enctype`, IE8 lacks an `enctype` setter.
	    // http://www.w3.org/TR/html5/forms.html#dom-fs-encoding
	    encType: 'encoding',
	    hrefLang: 'hreflang',
	    radioGroup: 'radiogroup',
	    spellCheck: 'spellcheck',
	    srcDoc: 'srcdoc',
	    srcSet: 'srcset'
	  }
	};

	module.exports = HTMLDOMPropertyConfig;

/***/ },
/* 93 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactBrowserComponentMixin
	 */

	'use strict';

	var ReactInstanceMap = __webpack_require__(50);

	var findDOMNode = __webpack_require__(94);
	var warning = __webpack_require__(28);

	var didWarnKey = '_getDOMNodeDidWarn';

	var ReactBrowserComponentMixin = {
	  /**
	   * Returns the DOM node rendered by this component.
	   *
	   * @return {DOMElement} The root node of this component.
	   * @final
	   * @protected
	   */
	  getDOMNode: function () {
	    process.env.NODE_ENV !== 'production' ? warning(this.constructor[didWarnKey], '%s.getDOMNode(...) is deprecated. Please use ' + 'ReactDOM.findDOMNode(instance) instead.', ReactInstanceMap.get(this).getName() || this.tagName || 'Unknown') : undefined;
	    this.constructor[didWarnKey] = true;
	    return findDOMNode(this);
	  }
	};

	module.exports = ReactBrowserComponentMixin;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 94 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule findDOMNode
	 * @typechecks static-only
	 */

	'use strict';

	var ReactCurrentOwner = __webpack_require__(8);
	var ReactInstanceMap = __webpack_require__(50);
	var ReactMount = __webpack_require__(31);

	var invariant = __webpack_require__(16);
	var warning = __webpack_require__(28);

	/**
	 * Returns the DOM node rendered by this element.
	 *
	 * @param {ReactComponent|DOMElement} componentOrElement
	 * @return {?DOMElement} The root node of this element.
	 */
	function findDOMNode(componentOrElement) {
	  if (process.env.NODE_ENV !== 'production') {
	    var owner = ReactCurrentOwner.current;
	    if (owner !== null) {
	      process.env.NODE_ENV !== 'production' ? warning(owner._warnedAboutRefsInRender, '%s is accessing getDOMNode or findDOMNode inside its render(). ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', owner.getName() || 'A component') : undefined;
	      owner._warnedAboutRefsInRender = true;
	    }
	  }
	  if (componentOrElement == null) {
	    return null;
	  }
	  if (componentOrElement.nodeType === 1) {
	    return componentOrElement;
	  }
	  if (ReactInstanceMap.has(componentOrElement)) {
	    return ReactMount.getNodeFromInstance(componentOrElement);
	  }
	  !(componentOrElement.render == null || typeof componentOrElement.render !== 'function') ? process.env.NODE_ENV !== 'production' ? invariant(false, 'findDOMNode was called on an unmounted component.') : invariant(false) : undefined;
	   true ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Element appears to be neither ReactComponent nor DOMNode (keys: %s)', Object.keys(componentOrElement)) : invariant(false) : undefined;
	}

	module.exports = findDOMNode;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 95 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDefaultBatchingStrategy
	 */

	'use strict';

	var ReactUpdates = __webpack_require__(57);
	var Transaction = __webpack_require__(60);

	var assign = __webpack_require__(42);
	var emptyFunction = __webpack_require__(18);

	var RESET_BATCHED_UPDATES = {
	  initialize: emptyFunction,
	  close: function () {
	    ReactDefaultBatchingStrategy.isBatchingUpdates = false;
	  }
	};

	var FLUSH_BATCHED_UPDATES = {
	  initialize: emptyFunction,
	  close: ReactUpdates.flushBatchedUpdates.bind(ReactUpdates)
	};

	var TRANSACTION_WRAPPERS = [FLUSH_BATCHED_UPDATES, RESET_BATCHED_UPDATES];

	function ReactDefaultBatchingStrategyTransaction() {
	  this.reinitializeTransaction();
	}

	assign(ReactDefaultBatchingStrategyTransaction.prototype, Transaction.Mixin, {
	  getTransactionWrappers: function () {
	    return TRANSACTION_WRAPPERS;
	  }
	});

	var transaction = new ReactDefaultBatchingStrategyTransaction();

	var ReactDefaultBatchingStrategy = {
	  isBatchingUpdates: false,

	  /**
	   * Call the provided function in a context within which calls to `setState`
	   * and friends are batched such that components aren't updated unnecessarily.
	   */
	  batchedUpdates: function (callback, a, b, c, d, e) {
	    var alreadyBatchingUpdates = ReactDefaultBatchingStrategy.isBatchingUpdates;

	    ReactDefaultBatchingStrategy.isBatchingUpdates = true;

	    // The code is written this way to avoid extra allocations
	    if (alreadyBatchingUpdates) {
	      callback(a, b, c, d, e);
	    } else {
	      transaction.perform(callback, null, a, b, c, d, e);
	    }
	  }
	};

	module.exports = ReactDefaultBatchingStrategy;

/***/ },
/* 96 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMComponent
	 * @typechecks static-only
	 */

	/* global hasOwnProperty:true */

	'use strict';

	var AutoFocusUtils = __webpack_require__(97);
	var CSSPropertyOperations = __webpack_require__(99);
	var DOMProperty = __webpack_require__(26);
	var DOMPropertyOperations = __webpack_require__(25);
	var EventConstants = __webpack_require__(33);
	var ReactBrowserEventEmitter = __webpack_require__(32);
	var ReactComponentBrowserEnvironment = __webpack_require__(29);
	var ReactDOMButton = __webpack_require__(107);
	var ReactDOMInput = __webpack_require__(108);
	var ReactDOMOption = __webpack_require__(112);
	var ReactDOMSelect = __webpack_require__(115);
	var ReactDOMTextarea = __webpack_require__(116);
	var ReactMount = __webpack_require__(31);
	var ReactMultiChild = __webpack_require__(117);
	var ReactPerf = __webpack_require__(21);
	var ReactUpdateQueue = __webpack_require__(56);

	var assign = __webpack_require__(42);
	var canDefineProperty = __webpack_require__(46);
	var escapeTextContentForBrowser = __webpack_require__(24);
	var invariant = __webpack_require__(16);
	var isEventSupported = __webpack_require__(43);
	var keyOf = __webpack_require__(82);
	var setInnerHTML = __webpack_require__(22);
	var setTextContent = __webpack_require__(23);
	var shallowEqual = __webpack_require__(120);
	var validateDOMNesting = __webpack_require__(73);
	var warning = __webpack_require__(28);

	var deleteListener = ReactBrowserEventEmitter.deleteListener;
	var listenTo = ReactBrowserEventEmitter.listenTo;
	var registrationNameModules = ReactBrowserEventEmitter.registrationNameModules;

	// For quickly matching children type, to test if can be treated as content.
	var CONTENT_TYPES = { 'string': true, 'number': true };

	var CHILDREN = keyOf({ children: null });
	var STYLE = keyOf({ style: null });
	var HTML = keyOf({ __html: null });

	var ELEMENT_NODE_TYPE = 1;

	function getDeclarationErrorAddendum(internalInstance) {
	  if (internalInstance) {
	    var owner = internalInstance._currentElement._owner || null;
	    if (owner) {
	      var name = owner.getName();
	      if (name) {
	        return ' This DOM node was rendered by `' + name + '`.';
	      }
	    }
	  }
	  return '';
	}

	var legacyPropsDescriptor;
	if (process.env.NODE_ENV !== 'production') {
	  legacyPropsDescriptor = {
	    props: {
	      enumerable: false,
	      get: function () {
	        var component = this._reactInternalComponent;
	        process.env.NODE_ENV !== 'production' ? warning(false, 'ReactDOMComponent: Do not access .props of a DOM node; instead, ' + 'recreate the props as `render` did originally or read the DOM ' + 'properties/attributes directly from this node (e.g., ' + 'this.refs.box.className).%s', getDeclarationErrorAddendum(component)) : undefined;
	        return component._currentElement.props;
	      }
	    }
	  };
	}

	function legacyGetDOMNode() {
	  if (process.env.NODE_ENV !== 'production') {
	    var component = this._reactInternalComponent;
	    process.env.NODE_ENV !== 'production' ? warning(false, 'ReactDOMComponent: Do not access .getDOMNode() of a DOM node; ' + 'instead, use the node directly.%s', getDeclarationErrorAddendum(component)) : undefined;
	  }
	  return this;
	}

	function legacyIsMounted() {
	  var component = this._reactInternalComponent;
	  if (process.env.NODE_ENV !== 'production') {
	    process.env.NODE_ENV !== 'production' ? warning(false, 'ReactDOMComponent: Do not access .isMounted() of a DOM node.%s', getDeclarationErrorAddendum(component)) : undefined;
	  }
	  return !!component;
	}

	function legacySetStateEtc() {
	  if (process.env.NODE_ENV !== 'production') {
	    var component = this._reactInternalComponent;
	    process.env.NODE_ENV !== 'production' ? warning(false, 'ReactDOMComponent: Do not access .setState(), .replaceState(), or ' + '.forceUpdate() of a DOM node. This is a no-op.%s', getDeclarationErrorAddendum(component)) : undefined;
	  }
	}

	function legacySetProps(partialProps, callback) {
	  var component = this._reactInternalComponent;
	  if (process.env.NODE_ENV !== 'production') {
	    process.env.NODE_ENV !== 'production' ? warning(false, 'ReactDOMComponent: Do not access .setProps() of a DOM node. ' + 'Instead, call ReactDOM.render again at the top level.%s', getDeclarationErrorAddendum(component)) : undefined;
	  }
	  if (!component) {
	    return;
	  }
	  ReactUpdateQueue.enqueueSetPropsInternal(component, partialProps);
	  if (callback) {
	    ReactUpdateQueue.enqueueCallbackInternal(component, callback);
	  }
	}

	function legacyReplaceProps(partialProps, callback) {
	  var component = this._reactInternalComponent;
	  if (process.env.NODE_ENV !== 'production') {
	    process.env.NODE_ENV !== 'production' ? warning(false, 'ReactDOMComponent: Do not access .replaceProps() of a DOM node. ' + 'Instead, call ReactDOM.render again at the top level.%s', getDeclarationErrorAddendum(component)) : undefined;
	  }
	  if (!component) {
	    return;
	  }
	  ReactUpdateQueue.enqueueReplacePropsInternal(component, partialProps);
	  if (callback) {
	    ReactUpdateQueue.enqueueCallbackInternal(component, callback);
	  }
	}

	function friendlyStringify(obj) {
	  if (typeof obj === 'object') {
	    if (Array.isArray(obj)) {
	      return '[' + obj.map(friendlyStringify).join(', ') + ']';
	    } else {
	      var pairs = [];
	      for (var key in obj) {
	        if (Object.prototype.hasOwnProperty.call(obj, key)) {
	          var keyEscaped = /^[a-z$_][\w$_]*$/i.test(key) ? key : JSON.stringify(key);
	          pairs.push(keyEscaped + ': ' + friendlyStringify(obj[key]));
	        }
	      }
	      return '{' + pairs.join(', ') + '}';
	    }
	  } else if (typeof obj === 'string') {
	    return JSON.stringify(obj);
	  } else if (typeof obj === 'function') {
	    return '[function object]';
	  }
	  // Differs from JSON.stringify in that undefined becauses undefined and that
	  // inf and nan don't become null
	  return String(obj);
	}

	var styleMutationWarning = {};

	function checkAndWarnForMutatedStyle(style1, style2, component) {
	  if (style1 == null || style2 == null) {
	    return;
	  }
	  if (shallowEqual(style1, style2)) {
	    return;
	  }

	  var componentName = component._tag;
	  var owner = component._currentElement._owner;
	  var ownerName;
	  if (owner) {
	    ownerName = owner.getName();
	  }

	  var hash = ownerName + '|' + componentName;

	  if (styleMutationWarning.hasOwnProperty(hash)) {
	    return;
	  }

	  styleMutationWarning[hash] = true;

	  process.env.NODE_ENV !== 'production' ? warning(false, '`%s` was passed a style object that has previously been mutated. ' + 'Mutating `style` is deprecated. Consider cloning it beforehand. Check ' + 'the `render` %s. Previous style: %s. Mutated style: %s.', componentName, owner ? 'of `' + ownerName + '`' : 'using <' + componentName + '>', friendlyStringify(style1), friendlyStringify(style2)) : undefined;
	}

	/**
	 * @param {object} component
	 * @param {?object} props
	 */
	function assertValidProps(component, props) {
	  if (!props) {
	    return;
	  }
	  // Note the use of `==` which checks for null or undefined.
	  if (process.env.NODE_ENV !== 'production') {
	    if (voidElementTags[component._tag]) {
	      process.env.NODE_ENV !== 'production' ? warning(props.children == null && props.dangerouslySetInnerHTML == null, '%s is a void element tag and must not have `children` or ' + 'use `props.dangerouslySetInnerHTML`.%s', component._tag, component._currentElement._owner ? ' Check the render method of ' + component._currentElement._owner.getName() + '.' : '') : undefined;
	    }
	  }
	  if (props.dangerouslySetInnerHTML != null) {
	    !(props.children == null) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Can only set one of `children` or `props.dangerouslySetInnerHTML`.') : invariant(false) : undefined;
	    !(typeof props.dangerouslySetInnerHTML === 'object' && HTML in props.dangerouslySetInnerHTML) ? process.env.NODE_ENV !== 'production' ? invariant(false, '`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. ' + 'Please visit https://fb.me/react-invariant-dangerously-set-inner-html ' + 'for more information.') : invariant(false) : undefined;
	  }
	  if (process.env.NODE_ENV !== 'production') {
	    process.env.NODE_ENV !== 'production' ? warning(props.innerHTML == null, 'Directly setting property `innerHTML` is not permitted. ' + 'For more information, lookup documentation on `dangerouslySetInnerHTML`.') : undefined;
	    process.env.NODE_ENV !== 'production' ? warning(!props.contentEditable || props.children == null, 'A component is `contentEditable` and contains `children` managed by ' + 'React. It is now your responsibility to guarantee that none of ' + 'those nodes are unexpectedly modified or duplicated. This is ' + 'probably not intentional.') : undefined;
	  }
	  !(props.style == null || typeof props.style === 'object') ? process.env.NODE_ENV !== 'production' ? invariant(false, 'The `style` prop expects a mapping from style properties to values, ' + 'not a string. For example, style={{marginRight: spacing + \'em\'}} when ' + 'using JSX.%s', getDeclarationErrorAddendum(component)) : invariant(false) : undefined;
	}

	function enqueuePutListener(id, registrationName, listener, transaction) {
	  if (process.env.NODE_ENV !== 'production') {
	    // IE8 has no API for event capturing and the `onScroll` event doesn't
	    // bubble.
	    process.env.NODE_ENV !== 'production' ? warning(registrationName !== 'onScroll' || isEventSupported('scroll', true), 'This browser doesn\'t support the `onScroll` event') : undefined;
	  }
	  var container = ReactMount.findReactContainerForID(id);
	  if (container) {
	    var doc = container.nodeType === ELEMENT_NODE_TYPE ? container.ownerDocument : container;
	    listenTo(registrationName, doc);
	  }
	  transaction.getReactMountReady().enqueue(putListener, {
	    id: id,
	    registrationName: registrationName,
	    listener: listener
	  });
	}

	function putListener() {
	  var listenerToPut = this;
	  ReactBrowserEventEmitter.putListener(listenerToPut.id, listenerToPut.registrationName, listenerToPut.listener);
	}

	// There are so many media events, it makes sense to just
	// maintain a list rather than create a `trapBubbledEvent` for each
	var mediaEvents = {
	  topAbort: 'abort',
	  topCanPlay: 'canplay',
	  topCanPlayThrough: 'canplaythrough',
	  topDurationChange: 'durationchange',
	  topEmptied: 'emptied',
	  topEncrypted: 'encrypted',
	  topEnded: 'ended',
	  topError: 'error',
	  topLoadedData: 'loadeddata',
	  topLoadedMetadata: 'loadedmetadata',
	  topLoadStart: 'loadstart',
	  topPause: 'pause',
	  topPlay: 'play',
	  topPlaying: 'playing',
	  topProgress: 'progress',
	  topRateChange: 'ratechange',
	  topSeeked: 'seeked',
	  topSeeking: 'seeking',
	  topStalled: 'stalled',
	  topSuspend: 'suspend',
	  topTimeUpdate: 'timeupdate',
	  topVolumeChange: 'volumechange',
	  topWaiting: 'waiting'
	};

	function trapBubbledEventsLocal() {
	  var inst = this;
	  // If a component renders to null or if another component fatals and causes
	  // the state of the tree to be corrupted, `node` here can be null.
	  !inst._rootNodeID ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Must be mounted to trap events') : invariant(false) : undefined;
	  var node = ReactMount.getNode(inst._rootNodeID);
	  !node ? process.env.NODE_ENV !== 'production' ? invariant(false, 'trapBubbledEvent(...): Requires node to be rendered.') : invariant(false) : undefined;

	  switch (inst._tag) {
	    case 'iframe':
	      inst._wrapperState.listeners = [ReactBrowserEventEmitter.trapBubbledEvent(EventConstants.topLevelTypes.topLoad, 'load', node)];
	      break;
	    case 'video':
	    case 'audio':

	      inst._wrapperState.listeners = [];
	      // create listener for each media event
	      for (var event in mediaEvents) {
	        if (mediaEvents.hasOwnProperty(event)) {
	          inst._wrapperState.listeners.push(ReactBrowserEventEmitter.trapBubbledEvent(EventConstants.topLevelTypes[event], mediaEvents[event], node));
	        }
	      }

	      break;
	    case 'img':
	      inst._wrapperState.listeners = [ReactBrowserEventEmitter.trapBubbledEvent(EventConstants.topLevelTypes.topError, 'error', node), ReactBrowserEventEmitter.trapBubbledEvent(EventConstants.topLevelTypes.topLoad, 'load', node)];
	      break;
	    case 'form':
	      inst._wrapperState.listeners = [ReactBrowserEventEmitter.trapBubbledEvent(EventConstants.topLevelTypes.topReset, 'reset', node), ReactBrowserEventEmitter.trapBubbledEvent(EventConstants.topLevelTypes.topSubmit, 'submit', node)];
	      break;
	  }
	}

	function mountReadyInputWrapper() {
	  ReactDOMInput.mountReadyWrapper(this);
	}

	function postUpdateSelectWrapper() {
	  ReactDOMSelect.postUpdateWrapper(this);
	}

	// For HTML, certain tags should omit their close tag. We keep a whitelist for
	// those special cased tags.

	var omittedCloseTags = {
	  'area': true,
	  'base': true,
	  'br': true,
	  'col': true,
	  'embed': true,
	  'hr': true,
	  'img': true,
	  'input': true,
	  'keygen': true,
	  'link': true,
	  'meta': true,
	  'param': true,
	  'source': true,
	  'track': true,
	  'wbr': true
	};

	// NOTE: menuitem's close tag should be omitted, but that causes problems.
	var newlineEatingTags = {
	  'listing': true,
	  'pre': true,
	  'textarea': true
	};

	// For HTML, certain tags cannot have children. This has the same purpose as
	// `omittedCloseTags` except that `menuitem` should still have its closing tag.

	var voidElementTags = assign({
	  'menuitem': true
	}, omittedCloseTags);

	// We accept any tag to be rendered but since this gets injected into arbitrary
	// HTML, we want to make sure that it's a safe tag.
	// http://www.w3.org/TR/REC-xml/#NT-Name

	var VALID_TAG_REGEX = /^[a-zA-Z][a-zA-Z:_\.\-\d]*$/; // Simplified subset
	var validatedTagCache = {};
	var hasOwnProperty = ({}).hasOwnProperty;

	function validateDangerousTag(tag) {
	  if (!hasOwnProperty.call(validatedTagCache, tag)) {
	    !VALID_TAG_REGEX.test(tag) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Invalid tag: %s', tag) : invariant(false) : undefined;
	    validatedTagCache[tag] = true;
	  }
	}

	function processChildContextDev(context, inst) {
	  // Pass down our tag name to child components for validation purposes
	  context = assign({}, context);
	  var info = context[validateDOMNesting.ancestorInfoContextKey];
	  context[validateDOMNesting.ancestorInfoContextKey] = validateDOMNesting.updatedAncestorInfo(info, inst._tag, inst);
	  return context;
	}

	function isCustomComponent(tagName, props) {
	  return tagName.indexOf('-') >= 0 || props.is != null;
	}

	/**
	 * Creates a new React class that is idempotent and capable of containing other
	 * React components. It accepts event listeners and DOM properties that are
	 * valid according to `DOMProperty`.
	 *
	 *  - Event listeners: `onClick`, `onMouseDown`, etc.
	 *  - DOM properties: `className`, `name`, `title`, etc.
	 *
	 * The `style` property functions differently from the DOM API. It accepts an
	 * object mapping of style properties to values.
	 *
	 * @constructor ReactDOMComponent
	 * @extends ReactMultiChild
	 */
	function ReactDOMComponent(tag) {
	  validateDangerousTag(tag);
	  this._tag = tag.toLowerCase();
	  this._renderedChildren = null;
	  this._previousStyle = null;
	  this._previousStyleCopy = null;
	  this._rootNodeID = null;
	  this._wrapperState = null;
	  this._topLevelWrapper = null;
	  this._nodeWithLegacyProperties = null;
	  if (process.env.NODE_ENV !== 'production') {
	    this._unprocessedContextDev = null;
	    this._processedContextDev = null;
	  }
	}

	ReactDOMComponent.displayName = 'ReactDOMComponent';

	ReactDOMComponent.Mixin = {

	  construct: function (element) {
	    this._currentElement = element;
	  },

	  /**
	   * Generates root tag markup then recurses. This method has side effects and
	   * is not idempotent.
	   *
	   * @internal
	   * @param {string} rootID The root DOM ID for this node.
	   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
	   * @param {object} context
	   * @return {string} The computed markup.
	   */
	  mountComponent: function (rootID, transaction, context) {
	    this._rootNodeID = rootID;

	    var props = this._currentElement.props;

	    switch (this._tag) {
	      case 'iframe':
	      case 'img':
	      case 'form':
	      case 'video':
	      case 'audio':
	        this._wrapperState = {
	          listeners: null
	        };
	        transaction.getReactMountReady().enqueue(trapBubbledEventsLocal, this);
	        break;
	      case 'button':
	        props = ReactDOMButton.getNativeProps(this, props, context);
	        break;
	      case 'input':
	        ReactDOMInput.mountWrapper(this, props, context);
	        props = ReactDOMInput.getNativeProps(this, props, context);
	        break;
	      case 'option':
	        ReactDOMOption.mountWrapper(this, props, context);
	        props = ReactDOMOption.getNativeProps(this, props, context);
	        break;
	      case 'select':
	        ReactDOMSelect.mountWrapper(this, props, context);
	        props = ReactDOMSelect.getNativeProps(this, props, context);
	        context = ReactDOMSelect.processChildContext(this, props, context);
	        break;
	      case 'textarea':
	        ReactDOMTextarea.mountWrapper(this, props, context);
	        props = ReactDOMTextarea.getNativeProps(this, props, context);
	        break;
	    }

	    assertValidProps(this, props);
	    if (process.env.NODE_ENV !== 'production') {
	      if (context[validateDOMNesting.ancestorInfoContextKey]) {
	        validateDOMNesting(this._tag, this, context[validateDOMNesting.ancestorInfoContextKey]);
	      }
	    }

	    if (process.env.NODE_ENV !== 'production') {
	      this._unprocessedContextDev = context;
	      this._processedContextDev = processChildContextDev(context, this);
	      context = this._processedContextDev;
	    }

	    var mountImage;
	    if (transaction.useCreateElement) {
	      var ownerDocument = context[ReactMount.ownerDocumentContextKey];
	      var el = ownerDocument.createElement(this._currentElement.type);
	      DOMPropertyOperations.setAttributeForID(el, this._rootNodeID);
	      // Populate node cache
	      ReactMount.getID(el);
	      this._updateDOMProperties({}, props, transaction, el);
	      this._createInitialChildren(transaction, props, context, el);
	      mountImage = el;
	    } else {
	      var tagOpen = this._createOpenTagMarkupAndPutListeners(transaction, props);
	      var tagContent = this._createContentMarkup(transaction, props, context);
	      if (!tagContent && omittedCloseTags[this._tag]) {
	        mountImage = tagOpen + '/>';
	      } else {
	        mountImage = tagOpen + '>' + tagContent + '</' + this._currentElement.type + '>';
	      }
	    }

	    switch (this._tag) {
	      case 'input':
	        transaction.getReactMountReady().enqueue(mountReadyInputWrapper, this);
	      // falls through
	      case 'button':
	      case 'select':
	      case 'textarea':
	        if (props.autoFocus) {
	          transaction.getReactMountReady().enqueue(AutoFocusUtils.focusDOMComponent, this);
	        }
	        break;
	    }

	    return mountImage;
	  },

	  /**
	   * Creates markup for the open tag and all attributes.
	   *
	   * This method has side effects because events get registered.
	   *
	   * Iterating over object properties is faster than iterating over arrays.
	   * @see http://jsperf.com/obj-vs-arr-iteration
	   *
	   * @private
	   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
	   * @param {object} props
	   * @return {string} Markup of opening tag.
	   */
	  _createOpenTagMarkupAndPutListeners: function (transaction, props) {
	    var ret = '<' + this._currentElement.type;

	    for (var propKey in props) {
	      if (!props.hasOwnProperty(propKey)) {
	        continue;
	      }
	      var propValue = props[propKey];
	      if (propValue == null) {
	        continue;
	      }
	      if (registrationNameModules.hasOwnProperty(propKey)) {
	        if (propValue) {
	          enqueuePutListener(this._rootNodeID, propKey, propValue, transaction);
	        }
	      } else {
	        if (propKey === STYLE) {
	          if (propValue) {
	            if (process.env.NODE_ENV !== 'production') {
	              // See `_updateDOMProperties`. style block
	              this._previousStyle = propValue;
	            }
	            propValue = this._previousStyleCopy = assign({}, props.style);
	          }
	          propValue = CSSPropertyOperations.createMarkupForStyles(propValue);
	        }
	        var markup = null;
	        if (this._tag != null && isCustomComponent(this._tag, props)) {
	          if (propKey !== CHILDREN) {
	            markup = DOMPropertyOperations.createMarkupForCustomAttribute(propKey, propValue);
	          }
	        } else {
	          markup = DOMPropertyOperations.createMarkupForProperty(propKey, propValue);
	        }
	        if (markup) {
	          ret += ' ' + markup;
	        }
	      }
	    }

	    // For static pages, no need to put React ID and checksum. Saves lots of
	    // bytes.
	    if (transaction.renderToStaticMarkup) {
	      return ret;
	    }

	    var markupForID = DOMPropertyOperations.createMarkupForID(this._rootNodeID);
	    return ret + ' ' + markupForID;
	  },

	  /**
	   * Creates markup for the content between the tags.
	   *
	   * @private
	   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
	   * @param {object} props
	   * @param {object} context
	   * @return {string} Content markup.
	   */
	  _createContentMarkup: function (transaction, props, context) {
	    var ret = '';

	    // Intentional use of != to avoid catching zero/false.
	    var innerHTML = props.dangerouslySetInnerHTML;
	    if (innerHTML != null) {
	      if (innerHTML.__html != null) {
	        ret = innerHTML.__html;
	      }
	    } else {
	      var contentToUse = CONTENT_TYPES[typeof props.children] ? props.children : null;
	      var childrenToUse = contentToUse != null ? null : props.children;
	      if (contentToUse != null) {
	        // TODO: Validate that text is allowed as a child of this node
	        ret = escapeTextContentForBrowser(contentToUse);
	      } else if (childrenToUse != null) {
	        var mountImages = this.mountChildren(childrenToUse, transaction, context);
	        ret = mountImages.join('');
	      }
	    }
	    if (newlineEatingTags[this._tag] && ret.charAt(0) === '\n') {
	      // text/html ignores the first character in these tags if it's a newline
	      // Prefer to break application/xml over text/html (for now) by adding
	      // a newline specifically to get eaten by the parser. (Alternately for
	      // textareas, replacing "^\n" with "\r\n" doesn't get eaten, and the first
	      // \r is normalized out by HTMLTextAreaElement#value.)
	      // See: <http://www.w3.org/TR/html-polyglot/#newlines-in-textarea-and-pre>
	      // See: <http://www.w3.org/TR/html5/syntax.html#element-restrictions>
	      // See: <http://www.w3.org/TR/html5/syntax.html#newlines>
	      // See: Parsing of "textarea" "listing" and "pre" elements
	      //  from <http://www.w3.org/TR/html5/syntax.html#parsing-main-inbody>
	      return '\n' + ret;
	    } else {
	      return ret;
	    }
	  },

	  _createInitialChildren: function (transaction, props, context, el) {
	    // Intentional use of != to avoid catching zero/false.
	    var innerHTML = props.dangerouslySetInnerHTML;
	    if (innerHTML != null) {
	      if (innerHTML.__html != null) {
	        setInnerHTML(el, innerHTML.__html);
	      }
	    } else {
	      var contentToUse = CONTENT_TYPES[typeof props.children] ? props.children : null;
	      var childrenToUse = contentToUse != null ? null : props.children;
	      if (contentToUse != null) {
	        // TODO: Validate that text is allowed as a child of this node
	        setTextContent(el, contentToUse);
	      } else if (childrenToUse != null) {
	        var mountImages = this.mountChildren(childrenToUse, transaction, context);
	        for (var i = 0; i < mountImages.length; i++) {
	          el.appendChild(mountImages[i]);
	        }
	      }
	    }
	  },

	  /**
	   * Receives a next element and updates the component.
	   *
	   * @internal
	   * @param {ReactElement} nextElement
	   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
	   * @param {object} context
	   */
	  receiveComponent: function (nextElement, transaction, context) {
	    var prevElement = this._currentElement;
	    this._currentElement = nextElement;
	    this.updateComponent(transaction, prevElement, nextElement, context);
	  },

	  /**
	   * Updates a native DOM component after it has already been allocated and
	   * attached to the DOM. Reconciles the root DOM node, then recurses.
	   *
	   * @param {ReactReconcileTransaction} transaction
	   * @param {ReactElement} prevElement
	   * @param {ReactElement} nextElement
	   * @internal
	   * @overridable
	   */
	  updateComponent: function (transaction, prevElement, nextElement, context) {
	    var lastProps = prevElement.props;
	    var nextProps = this._currentElement.props;

	    switch (this._tag) {
	      case 'button':
	        lastProps = ReactDOMButton.getNativeProps(this, lastProps);
	        nextProps = ReactDOMButton.getNativeProps(this, nextProps);
	        break;
	      case 'input':
	        ReactDOMInput.updateWrapper(this);
	        lastProps = ReactDOMInput.getNativeProps(this, lastProps);
	        nextProps = ReactDOMInput.getNativeProps(this, nextProps);
	        break;
	      case 'option':
	        lastProps = ReactDOMOption.getNativeProps(this, lastProps);
	        nextProps = ReactDOMOption.getNativeProps(this, nextProps);
	        break;
	      case 'select':
	        lastProps = ReactDOMSelect.getNativeProps(this, lastProps);
	        nextProps = ReactDOMSelect.getNativeProps(this, nextProps);
	        break;
	      case 'textarea':
	        ReactDOMTextarea.updateWrapper(this);
	        lastProps = ReactDOMTextarea.getNativeProps(this, lastProps);
	        nextProps = ReactDOMTextarea.getNativeProps(this, nextProps);
	        break;
	    }

	    if (process.env.NODE_ENV !== 'production') {
	      // If the context is reference-equal to the old one, pass down the same
	      // processed object so the update bailout in ReactReconciler behaves
	      // correctly (and identically in dev and prod). See #5005.
	      if (this._unprocessedContextDev !== context) {
	        this._unprocessedContextDev = context;
	        this._processedContextDev = processChildContextDev(context, this);
	      }
	      context = this._processedContextDev;
	    }

	    assertValidProps(this, nextProps);
	    this._updateDOMProperties(lastProps, nextProps, transaction, null);
	    this._updateDOMChildren(lastProps, nextProps, transaction, context);

	    if (!canDefineProperty && this._nodeWithLegacyProperties) {
	      this._nodeWithLegacyProperties.props = nextProps;
	    }

	    if (this._tag === 'select') {
	      // <select> value update needs to occur after <option> children
	      // reconciliation
	      transaction.getReactMountReady().enqueue(postUpdateSelectWrapper, this);
	    }
	  },

	  /**
	   * Reconciles the properties by detecting differences in property values and
	   * updating the DOM as necessary. This function is probably the single most
	   * critical path for performance optimization.
	   *
	   * TODO: Benchmark whether checking for changed values in memory actually
	   *       improves performance (especially statically positioned elements).
	   * TODO: Benchmark the effects of putting this at the top since 99% of props
	   *       do not change for a given reconciliation.
	   * TODO: Benchmark areas that can be improved with caching.
	   *
	   * @private
	   * @param {object} lastProps
	   * @param {object} nextProps
	   * @param {ReactReconcileTransaction} transaction
	   * @param {?DOMElement} node
	   */
	  _updateDOMProperties: function (lastProps, nextProps, transaction, node) {
	    var propKey;
	    var styleName;
	    var styleUpdates;
	    for (propKey in lastProps) {
	      if (nextProps.hasOwnProperty(propKey) || !lastProps.hasOwnProperty(propKey)) {
	        continue;
	      }
	      if (propKey === STYLE) {
	        var lastStyle = this._previousStyleCopy;
	        for (styleName in lastStyle) {
	          if (lastStyle.hasOwnProperty(styleName)) {
	            styleUpdates = styleUpdates || {};
	            styleUpdates[styleName] = '';
	          }
	        }
	        this._previousStyleCopy = null;
	      } else if (registrationNameModules.hasOwnProperty(propKey)) {
	        if (lastProps[propKey]) {
	          // Only call deleteListener if there was a listener previously or
	          // else willDeleteListener gets called when there wasn't actually a
	          // listener (e.g., onClick={null})
	          deleteListener(this._rootNodeID, propKey);
	        }
	      } else if (DOMProperty.properties[propKey] || DOMProperty.isCustomAttribute(propKey)) {
	        if (!node) {
	          node = ReactMount.getNode(this._rootNodeID);
	        }
	        DOMPropertyOperations.deleteValueForProperty(node, propKey);
	      }
	    }
	    for (propKey in nextProps) {
	      var nextProp = nextProps[propKey];
	      var lastProp = propKey === STYLE ? this._previousStyleCopy : lastProps[propKey];
	      if (!nextProps.hasOwnProperty(propKey) || nextProp === lastProp) {
	        continue;
	      }
	      if (propKey === STYLE) {
	        if (nextProp) {
	          if (process.env.NODE_ENV !== 'production') {
	            checkAndWarnForMutatedStyle(this._previousStyleCopy, this._previousStyle, this);
	            this._previousStyle = nextProp;
	          }
	          nextProp = this._previousStyleCopy = assign({}, nextProp);
	        } else {
	          this._previousStyleCopy = null;
	        }
	        if (lastProp) {
	          // Unset styles on `lastProp` but not on `nextProp`.
	          for (styleName in lastProp) {
	            if (lastProp.hasOwnProperty(styleName) && (!nextProp || !nextProp.hasOwnProperty(styleName))) {
	              styleUpdates = styleUpdates || {};
	              styleUpdates[styleName] = '';
	            }
	          }
	          // Update styles that changed since `lastProp`.
	          for (styleName in nextProp) {
	            if (nextProp.hasOwnProperty(styleName) && lastProp[styleName] !== nextProp[styleName]) {
	              styleUpdates = styleUpdates || {};
	              styleUpdates[styleName] = nextProp[styleName];
	            }
	          }
	        } else {
	          // Relies on `updateStylesByID` not mutating `styleUpdates`.
	          styleUpdates = nextProp;
	        }
	      } else if (registrationNameModules.hasOwnProperty(propKey)) {
	        if (nextProp) {
	          enqueuePutListener(this._rootNodeID, propKey, nextProp, transaction);
	        } else if (lastProp) {
	          deleteListener(this._rootNodeID, propKey);
	        }
	      } else if (isCustomComponent(this._tag, nextProps)) {
	        if (!node) {
	          node = ReactMount.getNode(this._rootNodeID);
	        }
	        if (propKey === CHILDREN) {
	          nextProp = null;
	        }
	        DOMPropertyOperations.setValueForAttribute(node, propKey, nextProp);
	      } else if (DOMProperty.properties[propKey] || DOMProperty.isCustomAttribute(propKey)) {
	        if (!node) {
	          node = ReactMount.getNode(this._rootNodeID);
	        }
	        // If we're updating to null or undefined, we should remove the property
	        // from the DOM node instead of inadvertantly setting to a string. This
	        // brings us in line with the same behavior we have on initial render.
	        if (nextProp != null) {
	          DOMPropertyOperations.setValueForProperty(node, propKey, nextProp);
	        } else {
	          DOMPropertyOperations.deleteValueForProperty(node, propKey);
	        }
	      }
	    }
	    if (styleUpdates) {
	      if (!node) {
	        node = ReactMount.getNode(this._rootNodeID);
	      }
	      CSSPropertyOperations.setValueForStyles(node, styleUpdates);
	    }
	  },

	  /**
	   * Reconciles the children with the various properties that affect the
	   * children content.
	   *
	   * @param {object} lastProps
	   * @param {object} nextProps
	   * @param {ReactReconcileTransaction} transaction
	   * @param {object} context
	   */
	  _updateDOMChildren: function (lastProps, nextProps, transaction, context) {
	    var lastContent = CONTENT_TYPES[typeof lastProps.children] ? lastProps.children : null;
	    var nextContent = CONTENT_TYPES[typeof nextProps.children] ? nextProps.children : null;

	    var lastHtml = lastProps.dangerouslySetInnerHTML && lastProps.dangerouslySetInnerHTML.__html;
	    var nextHtml = nextProps.dangerouslySetInnerHTML && nextProps.dangerouslySetInnerHTML.__html;

	    // Note the use of `!=` which checks for null or undefined.
	    var lastChildren = lastContent != null ? null : lastProps.children;
	    var nextChildren = nextContent != null ? null : nextProps.children;

	    // If we're switching from children to content/html or vice versa, remove
	    // the old content
	    var lastHasContentOrHtml = lastContent != null || lastHtml != null;
	    var nextHasContentOrHtml = nextContent != null || nextHtml != null;
	    if (lastChildren != null && nextChildren == null) {
	      this.updateChildren(null, transaction, context);
	    } else if (lastHasContentOrHtml && !nextHasContentOrHtml) {
	      this.updateTextContent('');
	    }

	    if (nextContent != null) {
	      if (lastContent !== nextContent) {
	        this.updateTextContent('' + nextContent);
	      }
	    } else if (nextHtml != null) {
	      if (lastHtml !== nextHtml) {
	        this.updateMarkup('' + nextHtml);
	      }
	    } else if (nextChildren != null) {
	      this.updateChildren(nextChildren, transaction, context);
	    }
	  },

	  /**
	   * Destroys all event registrations for this instance. Does not remove from
	   * the DOM. That must be done by the parent.
	   *
	   * @internal
	   */
	  unmountComponent: function () {
	    switch (this._tag) {
	      case 'iframe':
	      case 'img':
	      case 'form':
	      case 'video':
	      case 'audio':
	        var listeners = this._wrapperState.listeners;
	        if (listeners) {
	          for (var i = 0; i < listeners.length; i++) {
	            listeners[i].remove();
	          }
	        }
	        break;
	      case 'input':
	        ReactDOMInput.unmountWrapper(this);
	        break;
	      case 'html':
	      case 'head':
	      case 'body':
	        /**
	         * Components like <html> <head> and <body> can't be removed or added
	         * easily in a cross-browser way, however it's valuable to be able to
	         * take advantage of React's reconciliation for styling and <title>
	         * management. So we just document it and throw in dangerous cases.
	         */
	         true ? process.env.NODE_ENV !== 'production' ? invariant(false, '<%s> tried to unmount. Because of cross-browser quirks it is ' + 'impossible to unmount some top-level components (eg <html>, ' + '<head>, and <body>) reliably and efficiently. To fix this, have a ' + 'single top-level component that never unmounts render these ' + 'elements.', this._tag) : invariant(false) : undefined;
	        break;
	    }

	    this.unmountChildren();
	    ReactBrowserEventEmitter.deleteAllListeners(this._rootNodeID);
	    ReactComponentBrowserEnvironment.unmountIDFromEnvironment(this._rootNodeID);
	    this._rootNodeID = null;
	    this._wrapperState = null;
	    if (this._nodeWithLegacyProperties) {
	      var node = this._nodeWithLegacyProperties;
	      node._reactInternalComponent = null;
	      this._nodeWithLegacyProperties = null;
	    }
	  },

	  getPublicInstance: function () {
	    if (!this._nodeWithLegacyProperties) {
	      var node = ReactMount.getNode(this._rootNodeID);

	      node._reactInternalComponent = this;
	      node.getDOMNode = legacyGetDOMNode;
	      node.isMounted = legacyIsMounted;
	      node.setState = legacySetStateEtc;
	      node.replaceState = legacySetStateEtc;
	      node.forceUpdate = legacySetStateEtc;
	      node.setProps = legacySetProps;
	      node.replaceProps = legacyReplaceProps;

	      if (process.env.NODE_ENV !== 'production') {
	        if (canDefineProperty) {
	          Object.defineProperties(node, legacyPropsDescriptor);
	        } else {
	          // updateComponent will update this property on subsequent renders
	          node.props = this._currentElement.props;
	        }
	      } else {
	        // updateComponent will update this property on subsequent renders
	        node.props = this._currentElement.props;
	      }

	      this._nodeWithLegacyProperties = node;
	    }
	    return this._nodeWithLegacyProperties;
	  }

	};

	ReactPerf.measureMethods(ReactDOMComponent, 'ReactDOMComponent', {
	  mountComponent: 'mountComponent',
	  updateComponent: 'updateComponent'
	});

	assign(ReactDOMComponent.prototype, ReactDOMComponent.Mixin, ReactMultiChild.Mixin);

	module.exports = ReactDOMComponent;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 97 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule AutoFocusUtils
	 * @typechecks static-only
	 */

	'use strict';

	var ReactMount = __webpack_require__(31);

	var findDOMNode = __webpack_require__(94);
	var focusNode = __webpack_require__(98);

	var Mixin = {
	  componentDidMount: function () {
	    if (this.props.autoFocus) {
	      focusNode(findDOMNode(this));
	    }
	  }
	};

	var AutoFocusUtils = {
	  Mixin: Mixin,

	  focusDOMComponent: function () {
	    focusNode(ReactMount.getNode(this._rootNodeID));
	  }
	};

	module.exports = AutoFocusUtils;

/***/ },
/* 98 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule focusNode
	 */

	'use strict';

	/**
	 * @param {DOMElement} node input/textarea to focus
	 */
	function focusNode(node) {
	  // IE8 can throw "Can't move focus to the control because it is invisible,
	  // not enabled, or of a type that does not accept the focus." for all kinds of
	  // reasons that are too expensive and fragile to test.
	  try {
	    node.focus();
	  } catch (e) {}
	}

	module.exports = focusNode;

/***/ },
/* 99 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule CSSPropertyOperations
	 * @typechecks static-only
	 */

	'use strict';

	var CSSProperty = __webpack_require__(100);
	var ExecutionEnvironment = __webpack_require__(12);
	var ReactPerf = __webpack_require__(21);

	var camelizeStyleName = __webpack_require__(101);
	var dangerousStyleValue = __webpack_require__(103);
	var hyphenateStyleName = __webpack_require__(104);
	var memoizeStringOnly = __webpack_require__(106);
	var warning = __webpack_require__(28);

	var processStyleName = memoizeStringOnly(function (styleName) {
	  return hyphenateStyleName(styleName);
	});

	var hasShorthandPropertyBug = false;
	var styleFloatAccessor = 'cssFloat';
	if (ExecutionEnvironment.canUseDOM) {
	  var tempStyle = document.createElement('div').style;
	  try {
	    // IE8 throws "Invalid argument." if resetting shorthand style properties.
	    tempStyle.font = '';
	  } catch (e) {
	    hasShorthandPropertyBug = true;
	  }
	  // IE8 only supports accessing cssFloat (standard) as styleFloat
	  if (document.documentElement.style.cssFloat === undefined) {
	    styleFloatAccessor = 'styleFloat';
	  }
	}

	if (process.env.NODE_ENV !== 'production') {
	  // 'msTransform' is correct, but the other prefixes should be capitalized
	  var badVendoredStyleNamePattern = /^(?:webkit|moz|o)[A-Z]/;

	  // style values shouldn't contain a semicolon
	  var badStyleValueWithSemicolonPattern = /;\s*$/;

	  var warnedStyleNames = {};
	  var warnedStyleValues = {};

	  var warnHyphenatedStyleName = function (name) {
	    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
	      return;
	    }

	    warnedStyleNames[name] = true;
	    process.env.NODE_ENV !== 'production' ? warning(false, 'Unsupported style property %s. Did you mean %s?', name, camelizeStyleName(name)) : undefined;
	  };

	  var warnBadVendoredStyleName = function (name) {
	    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
	      return;
	    }

	    warnedStyleNames[name] = true;
	    process.env.NODE_ENV !== 'production' ? warning(false, 'Unsupported vendor-prefixed style property %s. Did you mean %s?', name, name.charAt(0).toUpperCase() + name.slice(1)) : undefined;
	  };

	  var warnStyleValueWithSemicolon = function (name, value) {
	    if (warnedStyleValues.hasOwnProperty(value) && warnedStyleValues[value]) {
	      return;
	    }

	    warnedStyleValues[value] = true;
	    process.env.NODE_ENV !== 'production' ? warning(false, 'Style property values shouldn\'t contain a semicolon. ' + 'Try "%s: %s" instead.', name, value.replace(badStyleValueWithSemicolonPattern, '')) : undefined;
	  };

	  /**
	   * @param {string} name
	   * @param {*} value
	   */
	  var warnValidStyle = function (name, value) {
	    if (name.indexOf('-') > -1) {
	      warnHyphenatedStyleName(name);
	    } else if (badVendoredStyleNamePattern.test(name)) {
	      warnBadVendoredStyleName(name);
	    } else if (badStyleValueWithSemicolonPattern.test(value)) {
	      warnStyleValueWithSemicolon(name, value);
	    }
	  };
	}

	/**
	 * Operations for dealing with CSS properties.
	 */
	var CSSPropertyOperations = {

	  /**
	   * Serializes a mapping of style properties for use as inline styles:
	   *
	   *   > createMarkupForStyles({width: '200px', height: 0})
	   *   "width:200px;height:0;"
	   *
	   * Undefined values are ignored so that declarative programming is easier.
	   * The result should be HTML-escaped before insertion into the DOM.
	   *
	   * @param {object} styles
	   * @return {?string}
	   */
	  createMarkupForStyles: function (styles) {
	    var serialized = '';
	    for (var styleName in styles) {
	      if (!styles.hasOwnProperty(styleName)) {
	        continue;
	      }
	      var styleValue = styles[styleName];
	      if (process.env.NODE_ENV !== 'production') {
	        warnValidStyle(styleName, styleValue);
	      }
	      if (styleValue != null) {
	        serialized += processStyleName(styleName) + ':';
	        serialized += dangerousStyleValue(styleName, styleValue) + ';';
	      }
	    }
	    return serialized || null;
	  },

	  /**
	   * Sets the value for multiple styles on a node.  If a value is specified as
	   * '' (empty string), the corresponding style property will be unset.
	   *
	   * @param {DOMElement} node
	   * @param {object} styles
	   */
	  setValueForStyles: function (node, styles) {
	    var style = node.style;
	    for (var styleName in styles) {
	      if (!styles.hasOwnProperty(styleName)) {
	        continue;
	      }
	      if (process.env.NODE_ENV !== 'production') {
	        warnValidStyle(styleName, styles[styleName]);
	      }
	      var styleValue = dangerousStyleValue(styleName, styles[styleName]);
	      if (styleName === 'float') {
	        styleName = styleFloatAccessor;
	      }
	      if (styleValue) {
	        style[styleName] = styleValue;
	      } else {
	        var expansion = hasShorthandPropertyBug && CSSProperty.shorthandPropertyExpansions[styleName];
	        if (expansion) {
	          // Shorthand property that IE8 won't like unsetting, so unset each
	          // component to placate it
	          for (var individualStyleName in expansion) {
	            style[individualStyleName] = '';
	          }
	        } else {
	          style[styleName] = '';
	        }
	      }
	    }
	  }

	};

	ReactPerf.measureMethods(CSSPropertyOperations, 'CSSPropertyOperations', {
	  setValueForStyles: 'setValueForStyles'
	});

	module.exports = CSSPropertyOperations;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 100 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule CSSProperty
	 */

	'use strict';

	/**
	 * CSS properties which accept numbers but are not in units of "px".
	 */
	var isUnitlessNumber = {
	  animationIterationCount: true,
	  boxFlex: true,
	  boxFlexGroup: true,
	  boxOrdinalGroup: true,
	  columnCount: true,
	  flex: true,
	  flexGrow: true,
	  flexPositive: true,
	  flexShrink: true,
	  flexNegative: true,
	  flexOrder: true,
	  fontWeight: true,
	  lineClamp: true,
	  lineHeight: true,
	  opacity: true,
	  order: true,
	  orphans: true,
	  tabSize: true,
	  widows: true,
	  zIndex: true,
	  zoom: true,

	  // SVG-related properties
	  fillOpacity: true,
	  stopOpacity: true,
	  strokeDashoffset: true,
	  strokeOpacity: true,
	  strokeWidth: true
	};

	/**
	 * @param {string} prefix vendor-specific prefix, eg: Webkit
	 * @param {string} key style name, eg: transitionDuration
	 * @return {string} style name prefixed with `prefix`, properly camelCased, eg:
	 * WebkitTransitionDuration
	 */
	function prefixKey(prefix, key) {
	  return prefix + key.charAt(0).toUpperCase() + key.substring(1);
	}

	/**
	 * Support style names that may come passed in prefixed by adding permutations
	 * of vendor prefixes.
	 */
	var prefixes = ['Webkit', 'ms', 'Moz', 'O'];

	// Using Object.keys here, or else the vanilla for-in loop makes IE8 go into an
	// infinite loop, because it iterates over the newly added props too.
	Object.keys(isUnitlessNumber).forEach(function (prop) {
	  prefixes.forEach(function (prefix) {
	    isUnitlessNumber[prefixKey(prefix, prop)] = isUnitlessNumber[prop];
	  });
	});

	/**
	 * Most style properties can be unset by doing .style[prop] = '' but IE8
	 * doesn't like doing that with shorthand properties so for the properties that
	 * IE8 breaks on, which are listed here, we instead unset each of the
	 * individual properties. See http://bugs.jquery.com/ticket/12385.
	 * The 4-value 'clock' properties like margin, padding, border-width seem to
	 * behave without any problems. Curiously, list-style works too without any
	 * special prodding.
	 */
	var shorthandPropertyExpansions = {
	  background: {
	    backgroundAttachment: true,
	    backgroundColor: true,
	    backgroundImage: true,
	    backgroundPositionX: true,
	    backgroundPositionY: true,
	    backgroundRepeat: true
	  },
	  backgroundPosition: {
	    backgroundPositionX: true,
	    backgroundPositionY: true
	  },
	  border: {
	    borderWidth: true,
	    borderStyle: true,
	    borderColor: true
	  },
	  borderBottom: {
	    borderBottomWidth: true,
	    borderBottomStyle: true,
	    borderBottomColor: true
	  },
	  borderLeft: {
	    borderLeftWidth: true,
	    borderLeftStyle: true,
	    borderLeftColor: true
	  },
	  borderRight: {
	    borderRightWidth: true,
	    borderRightStyle: true,
	    borderRightColor: true
	  },
	  borderTop: {
	    borderTopWidth: true,
	    borderTopStyle: true,
	    borderTopColor: true
	  },
	  font: {
	    fontStyle: true,
	    fontVariant: true,
	    fontWeight: true,
	    fontSize: true,
	    lineHeight: true,
	    fontFamily: true
	  },
	  outline: {
	    outlineWidth: true,
	    outlineStyle: true,
	    outlineColor: true
	  }
	};

	var CSSProperty = {
	  isUnitlessNumber: isUnitlessNumber,
	  shorthandPropertyExpansions: shorthandPropertyExpansions
	};

	module.exports = CSSProperty;

/***/ },
/* 101 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule camelizeStyleName
	 * @typechecks
	 */

	'use strict';

	var camelize = __webpack_require__(102);

	var msPattern = /^-ms-/;

	/**
	 * Camelcases a hyphenated CSS property name, for example:
	 *
	 *   > camelizeStyleName('background-color')
	 *   < "backgroundColor"
	 *   > camelizeStyleName('-moz-transition')
	 *   < "MozTransition"
	 *   > camelizeStyleName('-ms-transition')
	 *   < "msTransition"
	 *
	 * As Andi Smith suggests
	 * (http://www.andismith.com/blog/2012/02/modernizr-prefixed/), an `-ms` prefix
	 * is converted to lowercase `ms`.
	 *
	 * @param {string} string
	 * @return {string}
	 */
	function camelizeStyleName(string) {
	  return camelize(string.replace(msPattern, 'ms-'));
	}

	module.exports = camelizeStyleName;

/***/ },
/* 102 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule camelize
	 * @typechecks
	 */

	"use strict";

	var _hyphenPattern = /-(.)/g;

	/**
	 * Camelcases a hyphenated string, for example:
	 *
	 *   > camelize('background-color')
	 *   < "backgroundColor"
	 *
	 * @param {string} string
	 * @return {string}
	 */
	function camelize(string) {
	  return string.replace(_hyphenPattern, function (_, character) {
	    return character.toUpperCase();
	  });
	}

	module.exports = camelize;

/***/ },
/* 103 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule dangerousStyleValue
	 * @typechecks static-only
	 */

	'use strict';

	var CSSProperty = __webpack_require__(100);

	var isUnitlessNumber = CSSProperty.isUnitlessNumber;

	/**
	 * Convert a value into the proper css writable value. The style name `name`
	 * should be logical (no hyphens), as specified
	 * in `CSSProperty.isUnitlessNumber`.
	 *
	 * @param {string} name CSS property name such as `topMargin`.
	 * @param {*} value CSS property value such as `10px`.
	 * @return {string} Normalized style value with dimensions applied.
	 */
	function dangerousStyleValue(name, value) {
	  // Note that we've removed escapeTextForBrowser() calls here since the
	  // whole string will be escaped when the attribute is injected into
	  // the markup. If you provide unsafe user data here they can inject
	  // arbitrary CSS which may be problematic (I couldn't repro this):
	  // https://www.owasp.org/index.php/XSS_Filter_Evasion_Cheat_Sheet
	  // http://www.thespanner.co.uk/2007/11/26/ultimate-xss-css-injection/
	  // This is not an XSS hole but instead a potential CSS injection issue
	  // which has lead to a greater discussion about how we're going to
	  // trust URLs moving forward. See #2115901

	  var isEmpty = value == null || typeof value === 'boolean' || value === '';
	  if (isEmpty) {
	    return '';
	  }

	  var isNonNumeric = isNaN(value);
	  if (isNonNumeric || value === 0 || isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name]) {
	    return '' + value; // cast to string
	  }

	  if (typeof value === 'string') {
	    value = value.trim();
	  }
	  return value + 'px';
	}

	module.exports = dangerousStyleValue;

/***/ },
/* 104 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule hyphenateStyleName
	 * @typechecks
	 */

	'use strict';

	var hyphenate = __webpack_require__(105);

	var msPattern = /^ms-/;

	/**
	 * Hyphenates a camelcased CSS property name, for example:
	 *
	 *   > hyphenateStyleName('backgroundColor')
	 *   < "background-color"
	 *   > hyphenateStyleName('MozTransition')
	 *   < "-moz-transition"
	 *   > hyphenateStyleName('msTransition')
	 *   < "-ms-transition"
	 *
	 * As Modernizr suggests (http://modernizr.com/docs/#prefixed), an `ms` prefix
	 * is converted to `-ms-`.
	 *
	 * @param {string} string
	 * @return {string}
	 */
	function hyphenateStyleName(string) {
	  return hyphenate(string).replace(msPattern, '-ms-');
	}

	module.exports = hyphenateStyleName;

/***/ },
/* 105 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule hyphenate
	 * @typechecks
	 */

	'use strict';

	var _uppercasePattern = /([A-Z])/g;

	/**
	 * Hyphenates a camelcased string, for example:
	 *
	 *   > hyphenate('backgroundColor')
	 *   < "background-color"
	 *
	 * For CSS style names, use `hyphenateStyleName` instead which works properly
	 * with all vendor prefixes, including `ms`.
	 *
	 * @param {string} string
	 * @return {string}
	 */
	function hyphenate(string) {
	  return string.replace(_uppercasePattern, '-$1').toLowerCase();
	}

	module.exports = hyphenate;

/***/ },
/* 106 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule memoizeStringOnly
	 * @typechecks static-only
	 */

	'use strict';

	/**
	 * Memoizes the return value of a function that accepts one string argument.
	 *
	 * @param {function} callback
	 * @return {function}
	 */
	function memoizeStringOnly(callback) {
	  var cache = {};
	  return function (string) {
	    if (!cache.hasOwnProperty(string)) {
	      cache[string] = callback.call(this, string);
	    }
	    return cache[string];
	  };
	}

	module.exports = memoizeStringOnly;

/***/ },
/* 107 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMButton
	 */

	'use strict';

	var mouseListenerNames = {
	  onClick: true,
	  onDoubleClick: true,
	  onMouseDown: true,
	  onMouseMove: true,
	  onMouseUp: true,

	  onClickCapture: true,
	  onDoubleClickCapture: true,
	  onMouseDownCapture: true,
	  onMouseMoveCapture: true,
	  onMouseUpCapture: true
	};

	/**
	 * Implements a <button> native component that does not receive mouse events
	 * when `disabled` is set.
	 */
	var ReactDOMButton = {
	  getNativeProps: function (inst, props, context) {
	    if (!props.disabled) {
	      return props;
	    }

	    // Copy the props, except the mouse listeners
	    var nativeProps = {};
	    for (var key in props) {
	      if (props.hasOwnProperty(key) && !mouseListenerNames[key]) {
	        nativeProps[key] = props[key];
	      }
	    }

	    return nativeProps;
	  }
	};

	module.exports = ReactDOMButton;

/***/ },
/* 108 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMInput
	 */

	'use strict';

	var ReactDOMIDOperations = __webpack_require__(30);
	var LinkedValueUtils = __webpack_require__(109);
	var ReactMount = __webpack_require__(31);
	var ReactUpdates = __webpack_require__(57);

	var assign = __webpack_require__(42);
	var invariant = __webpack_require__(16);

	var instancesByReactID = {};

	function forceUpdateIfMounted() {
	  if (this._rootNodeID) {
	    // DOM component is still mounted; update
	    ReactDOMInput.updateWrapper(this);
	  }
	}

	/**
	 * Implements an <input> native component that allows setting these optional
	 * props: `checked`, `value`, `defaultChecked`, and `defaultValue`.
	 *
	 * If `checked` or `value` are not supplied (or null/undefined), user actions
	 * that affect the checked state or value will trigger updates to the element.
	 *
	 * If they are supplied (and not null/undefined), the rendered element will not
	 * trigger updates to the element. Instead, the props must change in order for
	 * the rendered element to be updated.
	 *
	 * The rendered element will be initialized as unchecked (or `defaultChecked`)
	 * with an empty value (or `defaultValue`).
	 *
	 * @see http://www.w3.org/TR/2012/WD-html5-20121025/the-input-element.html
	 */
	var ReactDOMInput = {
	  getNativeProps: function (inst, props, context) {
	    var value = LinkedValueUtils.getValue(props);
	    var checked = LinkedValueUtils.getChecked(props);

	    var nativeProps = assign({}, props, {
	      defaultChecked: undefined,
	      defaultValue: undefined,
	      value: value != null ? value : inst._wrapperState.initialValue,
	      checked: checked != null ? checked : inst._wrapperState.initialChecked,
	      onChange: inst._wrapperState.onChange
	    });

	    return nativeProps;
	  },

	  mountWrapper: function (inst, props) {
	    if (process.env.NODE_ENV !== 'production') {
	      LinkedValueUtils.checkPropTypes('input', props, inst._currentElement._owner);
	    }

	    var defaultValue = props.defaultValue;
	    inst._wrapperState = {
	      initialChecked: props.defaultChecked || false,
	      initialValue: defaultValue != null ? defaultValue : null,
	      onChange: _handleChange.bind(inst)
	    };
	  },

	  mountReadyWrapper: function (inst) {
	    // Can't be in mountWrapper or else server rendering leaks.
	    instancesByReactID[inst._rootNodeID] = inst;
	  },

	  unmountWrapper: function (inst) {
	    delete instancesByReactID[inst._rootNodeID];
	  },

	  updateWrapper: function (inst) {
	    var props = inst._currentElement.props;

	    // TODO: Shouldn't this be getChecked(props)?
	    var checked = props.checked;
	    if (checked != null) {
	      ReactDOMIDOperations.updatePropertyByID(inst._rootNodeID, 'checked', checked || false);
	    }

	    var value = LinkedValueUtils.getValue(props);
	    if (value != null) {
	      // Cast `value` to a string to ensure the value is set correctly. While
	      // browsers typically do this as necessary, jsdom doesn't.
	      ReactDOMIDOperations.updatePropertyByID(inst._rootNodeID, 'value', '' + value);
	    }
	  }
	};

	function _handleChange(event) {
	  var props = this._currentElement.props;

	  var returnValue = LinkedValueUtils.executeOnChange(props, event);

	  // Here we use asap to wait until all updates have propagated, which
	  // is important when using controlled components within layers:
	  // https://github.com/facebook/react/issues/1698
	  ReactUpdates.asap(forceUpdateIfMounted, this);

	  var name = props.name;
	  if (props.type === 'radio' && name != null) {
	    var rootNode = ReactMount.getNode(this._rootNodeID);
	    var queryRoot = rootNode;

	    while (queryRoot.parentNode) {
	      queryRoot = queryRoot.parentNode;
	    }

	    // If `rootNode.form` was non-null, then we could try `form.elements`,
	    // but that sometimes behaves strangely in IE8. We could also try using
	    // `form.getElementsByName`, but that will only return direct children
	    // and won't include inputs that use the HTML5 `form=` attribute. Since
	    // the input might not even be in a form, let's just use the global
	    // `querySelectorAll` to ensure we don't miss anything.
	    var group = queryRoot.querySelectorAll('input[name=' + JSON.stringify('' + name) + '][type="radio"]');

	    for (var i = 0; i < group.length; i++) {
	      var otherNode = group[i];
	      if (otherNode === rootNode || otherNode.form !== rootNode.form) {
	        continue;
	      }
	      // This will throw if radio buttons rendered by different copies of React
	      // and the same name are rendered into the same form (same as #1939).
	      // That's probably okay; we don't support it just as we don't support
	      // mixing React with non-React.
	      var otherID = ReactMount.getID(otherNode);
	      !otherID ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactDOMInput: Mixing React and non-React radio inputs with the ' + 'same `name` is not supported.') : invariant(false) : undefined;
	      var otherInstance = instancesByReactID[otherID];
	      !otherInstance ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactDOMInput: Unknown radio button ID %s.', otherID) : invariant(false) : undefined;
	      // If this is a controlled radio button group, forcing the input that
	      // was previously checked to update will cause it to be come re-checked
	      // as appropriate.
	      ReactUpdates.asap(forceUpdateIfMounted, otherInstance);
	    }
	  }

	  return returnValue;
	}

	module.exports = ReactDOMInput;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 109 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule LinkedValueUtils
	 * @typechecks static-only
	 */

	'use strict';

	var ReactPropTypes = __webpack_require__(110);
	var ReactPropTypeLocations = __webpack_require__(68);

	var invariant = __webpack_require__(16);
	var warning = __webpack_require__(28);

	var hasReadOnlyValue = {
	  'button': true,
	  'checkbox': true,
	  'image': true,
	  'hidden': true,
	  'radio': true,
	  'reset': true,
	  'submit': true
	};

	function _assertSingleLink(inputProps) {
	  !(inputProps.checkedLink == null || inputProps.valueLink == null) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Cannot provide a checkedLink and a valueLink. If you want to use ' + 'checkedLink, you probably don\'t want to use valueLink and vice versa.') : invariant(false) : undefined;
	}
	function _assertValueLink(inputProps) {
	  _assertSingleLink(inputProps);
	  !(inputProps.value == null && inputProps.onChange == null) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Cannot provide a valueLink and a value or onChange event. If you want ' + 'to use value or onChange, you probably don\'t want to use valueLink.') : invariant(false) : undefined;
	}

	function _assertCheckedLink(inputProps) {
	  _assertSingleLink(inputProps);
	  !(inputProps.checked == null && inputProps.onChange == null) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Cannot provide a checkedLink and a checked property or onChange event. ' + 'If you want to use checked or onChange, you probably don\'t want to ' + 'use checkedLink') : invariant(false) : undefined;
	}

	var propTypes = {
	  value: function (props, propName, componentName) {
	    if (!props[propName] || hasReadOnlyValue[props.type] || props.onChange || props.readOnly || props.disabled) {
	      return null;
	    }
	    return new Error('You provided a `value` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultValue`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
	  },
	  checked: function (props, propName, componentName) {
	    if (!props[propName] || props.onChange || props.readOnly || props.disabled) {
	      return null;
	    }
	    return new Error('You provided a `checked` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultChecked`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
	  },
	  onChange: ReactPropTypes.func
	};

	var loggedTypeFailures = {};
	function getDeclarationErrorAddendum(owner) {
	  if (owner) {
	    var name = owner.getName();
	    if (name) {
	      return ' Check the render method of `' + name + '`.';
	    }
	  }
	  return '';
	}

	/**
	 * Provide a linked `value` attribute for controlled forms. You should not use
	 * this outside of the ReactDOM controlled form components.
	 */
	var LinkedValueUtils = {
	  checkPropTypes: function (tagName, props, owner) {
	    for (var propName in propTypes) {
	      if (propTypes.hasOwnProperty(propName)) {
	        var error = propTypes[propName](props, propName, tagName, ReactPropTypeLocations.prop);
	      }
	      if (error instanceof Error && !(error.message in loggedTypeFailures)) {
	        // Only monitor this failure once because there tends to be a lot of the
	        // same error.
	        loggedTypeFailures[error.message] = true;

	        var addendum = getDeclarationErrorAddendum(owner);
	        process.env.NODE_ENV !== 'production' ? warning(false, 'Failed form propType: %s%s', error.message, addendum) : undefined;
	      }
	    }
	  },

	  /**
	   * @param {object} inputProps Props for form component
	   * @return {*} current value of the input either from value prop or link.
	   */
	  getValue: function (inputProps) {
	    if (inputProps.valueLink) {
	      _assertValueLink(inputProps);
	      return inputProps.valueLink.value;
	    }
	    return inputProps.value;
	  },

	  /**
	   * @param {object} inputProps Props for form component
	   * @return {*} current checked status of the input either from checked prop
	   *             or link.
	   */
	  getChecked: function (inputProps) {
	    if (inputProps.checkedLink) {
	      _assertCheckedLink(inputProps);
	      return inputProps.checkedLink.value;
	    }
	    return inputProps.checked;
	  },

	  /**
	   * @param {object} inputProps Props for form component
	   * @param {SyntheticEvent} event change event to handle
	   */
	  executeOnChange: function (inputProps, event) {
	    if (inputProps.valueLink) {
	      _assertValueLink(inputProps);
	      return inputProps.valueLink.requestChange(event.target.value);
	    } else if (inputProps.checkedLink) {
	      _assertCheckedLink(inputProps);
	      return inputProps.checkedLink.requestChange(event.target.checked);
	    } else if (inputProps.onChange) {
	      return inputProps.onChange.call(undefined, event);
	    }
	  }
	};

	module.exports = LinkedValueUtils;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 110 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactPropTypes
	 */

	'use strict';

	var ReactElement = __webpack_require__(45);
	var ReactPropTypeLocationNames = __webpack_require__(69);

	var emptyFunction = __webpack_require__(18);
	var getIteratorFn = __webpack_require__(111);

	/**
	 * Collection of methods that allow declaration and validation of props that are
	 * supplied to React components. Example usage:
	 *
	 *   var Props = require('ReactPropTypes');
	 *   var MyArticle = React.createClass({
	 *     propTypes: {
	 *       // An optional string prop named "description".
	 *       description: Props.string,
	 *
	 *       // A required enum prop named "category".
	 *       category: Props.oneOf(['News','Photos']).isRequired,
	 *
	 *       // A prop named "dialog" that requires an instance of Dialog.
	 *       dialog: Props.instanceOf(Dialog).isRequired
	 *     },
	 *     render: function() { ... }
	 *   });
	 *
	 * A more formal specification of how these methods are used:
	 *
	 *   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
	 *   decl := ReactPropTypes.{type}(.isRequired)?
	 *
	 * Each and every declaration produces a function with the same signature. This
	 * allows the creation of custom validation functions. For example:
	 *
	 *  var MyLink = React.createClass({
	 *    propTypes: {
	 *      // An optional string or URI prop named "href".
	 *      href: function(props, propName, componentName) {
	 *        var propValue = props[propName];
	 *        if (propValue != null && typeof propValue !== 'string' &&
	 *            !(propValue instanceof URI)) {
	 *          return new Error(
	 *            'Expected a string or an URI for ' + propName + ' in ' +
	 *            componentName
	 *          );
	 *        }
	 *      }
	 *    },
	 *    render: function() {...}
	 *  });
	 *
	 * @internal
	 */

	var ANONYMOUS = '<<anonymous>>';

	var ReactPropTypes = {
	  array: createPrimitiveTypeChecker('array'),
	  bool: createPrimitiveTypeChecker('boolean'),
	  func: createPrimitiveTypeChecker('function'),
	  number: createPrimitiveTypeChecker('number'),
	  object: createPrimitiveTypeChecker('object'),
	  string: createPrimitiveTypeChecker('string'),

	  any: createAnyTypeChecker(),
	  arrayOf: createArrayOfTypeChecker,
	  element: createElementTypeChecker(),
	  instanceOf: createInstanceTypeChecker,
	  node: createNodeChecker(),
	  objectOf: createObjectOfTypeChecker,
	  oneOf: createEnumTypeChecker,
	  oneOfType: createUnionTypeChecker,
	  shape: createShapeTypeChecker
	};

	function createChainableTypeChecker(validate) {
	  function checkType(isRequired, props, propName, componentName, location, propFullName) {
	    componentName = componentName || ANONYMOUS;
	    propFullName = propFullName || propName;
	    if (props[propName] == null) {
	      var locationName = ReactPropTypeLocationNames[location];
	      if (isRequired) {
	        return new Error('Required ' + locationName + ' `' + propFullName + '` was not specified in ' + ('`' + componentName + '`.'));
	      }
	      return null;
	    } else {
	      return validate(props, propName, componentName, location, propFullName);
	    }
	  }

	  var chainedCheckType = checkType.bind(null, false);
	  chainedCheckType.isRequired = checkType.bind(null, true);

	  return chainedCheckType;
	}

	function createPrimitiveTypeChecker(expectedType) {
	  function validate(props, propName, componentName, location, propFullName) {
	    var propValue = props[propName];
	    var propType = getPropType(propValue);
	    if (propType !== expectedType) {
	      var locationName = ReactPropTypeLocationNames[location];
	      // `propValue` being instance of, say, date/regexp, pass the 'object'
	      // check, but we can offer a more precise error message here rather than
	      // 'of type `object`'.
	      var preciseType = getPreciseType(propValue);

	      return new Error('Invalid ' + locationName + ' `' + propFullName + '` of type ' + ('`' + preciseType + '` supplied to `' + componentName + '`, expected ') + ('`' + expectedType + '`.'));
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function createAnyTypeChecker() {
	  return createChainableTypeChecker(emptyFunction.thatReturns(null));
	}

	function createArrayOfTypeChecker(typeChecker) {
	  function validate(props, propName, componentName, location, propFullName) {
	    var propValue = props[propName];
	    if (!Array.isArray(propValue)) {
	      var locationName = ReactPropTypeLocationNames[location];
	      var propType = getPropType(propValue);
	      return new Error('Invalid ' + locationName + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an array.'));
	    }
	    for (var i = 0; i < propValue.length; i++) {
	      var error = typeChecker(propValue, i, componentName, location, propFullName + '[' + i + ']');
	      if (error instanceof Error) {
	        return error;
	      }
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function createElementTypeChecker() {
	  function validate(props, propName, componentName, location, propFullName) {
	    if (!ReactElement.isValidElement(props[propName])) {
	      var locationName = ReactPropTypeLocationNames[location];
	      return new Error('Invalid ' + locationName + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`, expected a single ReactElement.'));
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function createInstanceTypeChecker(expectedClass) {
	  function validate(props, propName, componentName, location, propFullName) {
	    if (!(props[propName] instanceof expectedClass)) {
	      var locationName = ReactPropTypeLocationNames[location];
	      var expectedClassName = expectedClass.name || ANONYMOUS;
	      var actualClassName = getClassName(props[propName]);
	      return new Error('Invalid ' + locationName + ' `' + propFullName + '` of type ' + ('`' + actualClassName + '` supplied to `' + componentName + '`, expected ') + ('instance of `' + expectedClassName + '`.'));
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function createEnumTypeChecker(expectedValues) {
	  if (!Array.isArray(expectedValues)) {
	    return createChainableTypeChecker(function () {
	      return new Error('Invalid argument supplied to oneOf, expected an instance of array.');
	    });
	  }

	  function validate(props, propName, componentName, location, propFullName) {
	    var propValue = props[propName];
	    for (var i = 0; i < expectedValues.length; i++) {
	      if (propValue === expectedValues[i]) {
	        return null;
	      }
	    }

	    var locationName = ReactPropTypeLocationNames[location];
	    var valuesString = JSON.stringify(expectedValues);
	    return new Error('Invalid ' + locationName + ' `' + propFullName + '` of value `' + propValue + '` ' + ('supplied to `' + componentName + '`, expected one of ' + valuesString + '.'));
	  }
	  return createChainableTypeChecker(validate);
	}

	function createObjectOfTypeChecker(typeChecker) {
	  function validate(props, propName, componentName, location, propFullName) {
	    var propValue = props[propName];
	    var propType = getPropType(propValue);
	    if (propType !== 'object') {
	      var locationName = ReactPropTypeLocationNames[location];
	      return new Error('Invalid ' + locationName + ' `' + propFullName + '` of type ' + ('`' + propType + '` supplied to `' + componentName + '`, expected an object.'));
	    }
	    for (var key in propValue) {
	      if (propValue.hasOwnProperty(key)) {
	        var error = typeChecker(propValue, key, componentName, location, propFullName + '.' + key);
	        if (error instanceof Error) {
	          return error;
	        }
	      }
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function createUnionTypeChecker(arrayOfTypeCheckers) {
	  if (!Array.isArray(arrayOfTypeCheckers)) {
	    return createChainableTypeChecker(function () {
	      return new Error('Invalid argument supplied to oneOfType, expected an instance of array.');
	    });
	  }

	  function validate(props, propName, componentName, location, propFullName) {
	    for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
	      var checker = arrayOfTypeCheckers[i];
	      if (checker(props, propName, componentName, location, propFullName) == null) {
	        return null;
	      }
	    }

	    var locationName = ReactPropTypeLocationNames[location];
	    return new Error('Invalid ' + locationName + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`.'));
	  }
	  return createChainableTypeChecker(validate);
	}

	function createNodeChecker() {
	  function validate(props, propName, componentName, location, propFullName) {
	    if (!isNode(props[propName])) {
	      var locationName = ReactPropTypeLocationNames[location];
	      return new Error('Invalid ' + locationName + ' `' + propFullName + '` supplied to ' + ('`' + componentName + '`, expected a ReactNode.'));
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function createShapeTypeChecker(shapeTypes) {
	  function validate(props, propName, componentName, location, propFullName) {
	    var propValue = props[propName];
	    var propType = getPropType(propValue);
	    if (propType !== 'object') {
	      var locationName = ReactPropTypeLocationNames[location];
	      return new Error('Invalid ' + locationName + ' `' + propFullName + '` of type `' + propType + '` ' + ('supplied to `' + componentName + '`, expected `object`.'));
	    }
	    for (var key in shapeTypes) {
	      var checker = shapeTypes[key];
	      if (!checker) {
	        continue;
	      }
	      var error = checker(propValue, key, componentName, location, propFullName + '.' + key);
	      if (error) {
	        return error;
	      }
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function isNode(propValue) {
	  switch (typeof propValue) {
	    case 'number':
	    case 'string':
	    case 'undefined':
	      return true;
	    case 'boolean':
	      return !propValue;
	    case 'object':
	      if (Array.isArray(propValue)) {
	        return propValue.every(isNode);
	      }
	      if (propValue === null || ReactElement.isValidElement(propValue)) {
	        return true;
	      }

	      var iteratorFn = getIteratorFn(propValue);
	      if (iteratorFn) {
	        var iterator = iteratorFn.call(propValue);
	        var step;
	        if (iteratorFn !== propValue.entries) {
	          while (!(step = iterator.next()).done) {
	            if (!isNode(step.value)) {
	              return false;
	            }
	          }
	        } else {
	          // Iterator will provide entry [k,v] tuples rather than values.
	          while (!(step = iterator.next()).done) {
	            var entry = step.value;
	            if (entry) {
	              if (!isNode(entry[1])) {
	                return false;
	              }
	            }
	          }
	        }
	      } else {
	        return false;
	      }

	      return true;
	    default:
	      return false;
	  }
	}

	// Equivalent of `typeof` but with special handling for array and regexp.
	function getPropType(propValue) {
	  var propType = typeof propValue;
	  if (Array.isArray(propValue)) {
	    return 'array';
	  }
	  if (propValue instanceof RegExp) {
	    // Old webkits (at least until Android 4.0) return 'function' rather than
	    // 'object' for typeof a RegExp. We'll normalize this here so that /bla/
	    // passes PropTypes.object.
	    return 'object';
	  }
	  return propType;
	}

	// This handles more types than `getPropType`. Only used for error messages.
	// See `createPrimitiveTypeChecker`.
	function getPreciseType(propValue) {
	  var propType = getPropType(propValue);
	  if (propType === 'object') {
	    if (propValue instanceof Date) {
	      return 'date';
	    } else if (propValue instanceof RegExp) {
	      return 'regexp';
	    }
	  }
	  return propType;
	}

	// Returns class name of the object, if any.
	function getClassName(propValue) {
	  if (!propValue.constructor || !propValue.constructor.name) {
	    return '<<anonymous>>';
	  }
	  return propValue.constructor.name;
	}

	module.exports = ReactPropTypes;

/***/ },
/* 111 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getIteratorFn
	 * @typechecks static-only
	 */

	'use strict';

	/* global Symbol */
	var ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
	var FAUX_ITERATOR_SYMBOL = '@@iterator'; // Before Symbol spec.

	/**
	 * Returns the iterator method function contained on the iterable object.
	 *
	 * Be sure to invoke the function with the iterable as context:
	 *
	 *     var iteratorFn = getIteratorFn(myIterable);
	 *     if (iteratorFn) {
	 *       var iterator = iteratorFn.call(myIterable);
	 *       ...
	 *     }
	 *
	 * @param {?object} maybeIterable
	 * @return {?function}
	 */
	function getIteratorFn(maybeIterable) {
	  var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
	  if (typeof iteratorFn === 'function') {
	    return iteratorFn;
	  }
	}

	module.exports = getIteratorFn;

/***/ },
/* 112 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMOption
	 */

	'use strict';

	var ReactChildren = __webpack_require__(113);
	var ReactDOMSelect = __webpack_require__(115);

	var assign = __webpack_require__(42);
	var warning = __webpack_require__(28);

	var valueContextKey = ReactDOMSelect.valueContextKey;

	/**
	 * Implements an <option> native component that warns when `selected` is set.
	 */
	var ReactDOMOption = {
	  mountWrapper: function (inst, props, context) {
	    // TODO (yungsters): Remove support for `selected` in <option>.
	    if (process.env.NODE_ENV !== 'production') {
	      process.env.NODE_ENV !== 'production' ? warning(props.selected == null, 'Use the `defaultValue` or `value` props on <select> instead of ' + 'setting `selected` on <option>.') : undefined;
	    }

	    // Look up whether this option is 'selected' via context
	    var selectValue = context[valueContextKey];

	    // If context key is null (e.g., no specified value or after initial mount)
	    // or missing (e.g., for <datalist>), we don't change props.selected
	    var selected = null;
	    if (selectValue != null) {
	      selected = false;
	      if (Array.isArray(selectValue)) {
	        // multiple
	        for (var i = 0; i < selectValue.length; i++) {
	          if ('' + selectValue[i] === '' + props.value) {
	            selected = true;
	            break;
	          }
	        }
	      } else {
	        selected = '' + selectValue === '' + props.value;
	      }
	    }

	    inst._wrapperState = { selected: selected };
	  },

	  getNativeProps: function (inst, props, context) {
	    var nativeProps = assign({ selected: undefined, children: undefined }, props);

	    // Read state only from initial mount because <select> updates value
	    // manually; we need the initial state only for server rendering
	    if (inst._wrapperState.selected != null) {
	      nativeProps.selected = inst._wrapperState.selected;
	    }

	    var content = '';

	    // Flatten children and warn if they aren't strings or numbers;
	    // invalid types are ignored.
	    ReactChildren.forEach(props.children, function (child) {
	      if (child == null) {
	        return;
	      }
	      if (typeof child === 'string' || typeof child === 'number') {
	        content += child;
	      } else {
	        process.env.NODE_ENV !== 'production' ? warning(false, 'Only strings and numbers are supported as <option> children.') : undefined;
	      }
	    });

	    if (content) {
	      nativeProps.children = content;
	    }

	    return nativeProps;
	  }

	};

	module.exports = ReactDOMOption;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 113 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactChildren
	 */

	'use strict';

	var PooledClass = __webpack_require__(59);
	var ReactElement = __webpack_require__(45);

	var emptyFunction = __webpack_require__(18);
	var traverseAllChildren = __webpack_require__(114);

	var twoArgumentPooler = PooledClass.twoArgumentPooler;
	var fourArgumentPooler = PooledClass.fourArgumentPooler;

	var userProvidedKeyEscapeRegex = /\/(?!\/)/g;
	function escapeUserProvidedKey(text) {
	  return ('' + text).replace(userProvidedKeyEscapeRegex, '//');
	}

	/**
	 * PooledClass representing the bookkeeping associated with performing a child
	 * traversal. Allows avoiding binding callbacks.
	 *
	 * @constructor ForEachBookKeeping
	 * @param {!function} forEachFunction Function to perform traversal with.
	 * @param {?*} forEachContext Context to perform context with.
	 */
	function ForEachBookKeeping(forEachFunction, forEachContext) {
	  this.func = forEachFunction;
	  this.context = forEachContext;
	  this.count = 0;
	}
	ForEachBookKeeping.prototype.destructor = function () {
	  this.func = null;
	  this.context = null;
	  this.count = 0;
	};
	PooledClass.addPoolingTo(ForEachBookKeeping, twoArgumentPooler);

	function forEachSingleChild(bookKeeping, child, name) {
	  var func = bookKeeping.func;
	  var context = bookKeeping.context;

	  func.call(context, child, bookKeeping.count++);
	}

	/**
	 * Iterates through children that are typically specified as `props.children`.
	 *
	 * The provided forEachFunc(child, index) will be called for each
	 * leaf child.
	 *
	 * @param {?*} children Children tree container.
	 * @param {function(*, int)} forEachFunc
	 * @param {*} forEachContext Context for forEachContext.
	 */
	function forEachChildren(children, forEachFunc, forEachContext) {
	  if (children == null) {
	    return children;
	  }
	  var traverseContext = ForEachBookKeeping.getPooled(forEachFunc, forEachContext);
	  traverseAllChildren(children, forEachSingleChild, traverseContext);
	  ForEachBookKeeping.release(traverseContext);
	}

	/**
	 * PooledClass representing the bookkeeping associated with performing a child
	 * mapping. Allows avoiding binding callbacks.
	 *
	 * @constructor MapBookKeeping
	 * @param {!*} mapResult Object containing the ordered map of results.
	 * @param {!function} mapFunction Function to perform mapping with.
	 * @param {?*} mapContext Context to perform mapping with.
	 */
	function MapBookKeeping(mapResult, keyPrefix, mapFunction, mapContext) {
	  this.result = mapResult;
	  this.keyPrefix = keyPrefix;
	  this.func = mapFunction;
	  this.context = mapContext;
	  this.count = 0;
	}
	MapBookKeeping.prototype.destructor = function () {
	  this.result = null;
	  this.keyPrefix = null;
	  this.func = null;
	  this.context = null;
	  this.count = 0;
	};
	PooledClass.addPoolingTo(MapBookKeeping, fourArgumentPooler);

	function mapSingleChildIntoContext(bookKeeping, child, childKey) {
	  var result = bookKeeping.result;
	  var keyPrefix = bookKeeping.keyPrefix;
	  var func = bookKeeping.func;
	  var context = bookKeeping.context;

	  var mappedChild = func.call(context, child, bookKeeping.count++);
	  if (Array.isArray(mappedChild)) {
	    mapIntoWithKeyPrefixInternal(mappedChild, result, childKey, emptyFunction.thatReturnsArgument);
	  } else if (mappedChild != null) {
	    if (ReactElement.isValidElement(mappedChild)) {
	      mappedChild = ReactElement.cloneAndReplaceKey(mappedChild,
	      // Keep both the (mapped) and old keys if they differ, just as
	      // traverseAllChildren used to do for objects as children
	      keyPrefix + (mappedChild !== child ? escapeUserProvidedKey(mappedChild.key || '') + '/' : '') + childKey);
	    }
	    result.push(mappedChild);
	  }
	}

	function mapIntoWithKeyPrefixInternal(children, array, prefix, func, context) {
	  var escapedPrefix = '';
	  if (prefix != null) {
	    escapedPrefix = escapeUserProvidedKey(prefix) + '/';
	  }
	  var traverseContext = MapBookKeeping.getPooled(array, escapedPrefix, func, context);
	  traverseAllChildren(children, mapSingleChildIntoContext, traverseContext);
	  MapBookKeeping.release(traverseContext);
	}

	/**
	 * Maps children that are typically specified as `props.children`.
	 *
	 * The provided mapFunction(child, key, index) will be called for each
	 * leaf child.
	 *
	 * @param {?*} children Children tree container.
	 * @param {function(*, int)} func The map function.
	 * @param {*} context Context for mapFunction.
	 * @return {object} Object containing the ordered map of results.
	 */
	function mapChildren(children, func, context) {
	  if (children == null) {
	    return children;
	  }
	  var result = [];
	  mapIntoWithKeyPrefixInternal(children, result, null, func, context);
	  return result;
	}

	function forEachSingleChildDummy(traverseContext, child, name) {
	  return null;
	}

	/**
	 * Count the number of children that are typically specified as
	 * `props.children`.
	 *
	 * @param {?*} children Children tree container.
	 * @return {number} The number of children.
	 */
	function countChildren(children, context) {
	  return traverseAllChildren(children, forEachSingleChildDummy, null);
	}

	/**
	 * Flatten a children object (typically specified as `props.children`) and
	 * return an array with appropriately re-keyed children.
	 */
	function toArray(children) {
	  var result = [];
	  mapIntoWithKeyPrefixInternal(children, result, null, emptyFunction.thatReturnsArgument);
	  return result;
	}

	var ReactChildren = {
	  forEach: forEachChildren,
	  map: mapChildren,
	  mapIntoWithKeyPrefixInternal: mapIntoWithKeyPrefixInternal,
	  count: countChildren,
	  toArray: toArray
	};

	module.exports = ReactChildren;

/***/ },
/* 114 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule traverseAllChildren
	 */

	'use strict';

	var ReactCurrentOwner = __webpack_require__(8);
	var ReactElement = __webpack_require__(45);
	var ReactInstanceHandles = __webpack_require__(48);

	var getIteratorFn = __webpack_require__(111);
	var invariant = __webpack_require__(16);
	var warning = __webpack_require__(28);

	var SEPARATOR = ReactInstanceHandles.SEPARATOR;
	var SUBSEPARATOR = ':';

	/**
	 * TODO: Test that a single child and an array with one item have the same key
	 * pattern.
	 */

	var userProvidedKeyEscaperLookup = {
	  '=': '=0',
	  '.': '=1',
	  ':': '=2'
	};

	var userProvidedKeyEscapeRegex = /[=.:]/g;

	var didWarnAboutMaps = false;

	function userProvidedKeyEscaper(match) {
	  return userProvidedKeyEscaperLookup[match];
	}

	/**
	 * Generate a key string that identifies a component within a set.
	 *
	 * @param {*} component A component that could contain a manual key.
	 * @param {number} index Index that is used if a manual key is not provided.
	 * @return {string}
	 */
	function getComponentKey(component, index) {
	  if (component && component.key != null) {
	    // Explicit key
	    return wrapUserProvidedKey(component.key);
	  }
	  // Implicit key determined by the index in the set
	  return index.toString(36);
	}

	/**
	 * Escape a component key so that it is safe to use in a reactid.
	 *
	 * @param {*} text Component key to be escaped.
	 * @return {string} An escaped string.
	 */
	function escapeUserProvidedKey(text) {
	  return ('' + text).replace(userProvidedKeyEscapeRegex, userProvidedKeyEscaper);
	}

	/**
	 * Wrap a `key` value explicitly provided by the user to distinguish it from
	 * implicitly-generated keys generated by a component's index in its parent.
	 *
	 * @param {string} key Value of a user-provided `key` attribute
	 * @return {string}
	 */
	function wrapUserProvidedKey(key) {
	  return '$' + escapeUserProvidedKey(key);
	}

	/**
	 * @param {?*} children Children tree container.
	 * @param {!string} nameSoFar Name of the key path so far.
	 * @param {!function} callback Callback to invoke with each child found.
	 * @param {?*} traverseContext Used to pass information throughout the traversal
	 * process.
	 * @return {!number} The number of children in this subtree.
	 */
	function traverseAllChildrenImpl(children, nameSoFar, callback, traverseContext) {
	  var type = typeof children;

	  if (type === 'undefined' || type === 'boolean') {
	    // All of the above are perceived as null.
	    children = null;
	  }

	  if (children === null || type === 'string' || type === 'number' || ReactElement.isValidElement(children)) {
	    callback(traverseContext, children,
	    // If it's the only child, treat the name as if it was wrapped in an array
	    // so that it's consistent if the number of children grows.
	    nameSoFar === '' ? SEPARATOR + getComponentKey(children, 0) : nameSoFar);
	    return 1;
	  }

	  var child;
	  var nextName;
	  var subtreeCount = 0; // Count of children found in the current subtree.
	  var nextNamePrefix = nameSoFar === '' ? SEPARATOR : nameSoFar + SUBSEPARATOR;

	  if (Array.isArray(children)) {
	    for (var i = 0; i < children.length; i++) {
	      child = children[i];
	      nextName = nextNamePrefix + getComponentKey(child, i);
	      subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
	    }
	  } else {
	    var iteratorFn = getIteratorFn(children);
	    if (iteratorFn) {
	      var iterator = iteratorFn.call(children);
	      var step;
	      if (iteratorFn !== children.entries) {
	        var ii = 0;
	        while (!(step = iterator.next()).done) {
	          child = step.value;
	          nextName = nextNamePrefix + getComponentKey(child, ii++);
	          subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
	        }
	      } else {
	        if (process.env.NODE_ENV !== 'production') {
	          process.env.NODE_ENV !== 'production' ? warning(didWarnAboutMaps, 'Using Maps as children is not yet fully supported. It is an ' + 'experimental feature that might be removed. Convert it to a ' + 'sequence / iterable of keyed ReactElements instead.') : undefined;
	          didWarnAboutMaps = true;
	        }
	        // Iterator will provide entry [k,v] tuples rather than values.
	        while (!(step = iterator.next()).done) {
	          var entry = step.value;
	          if (entry) {
	            child = entry[1];
	            nextName = nextNamePrefix + wrapUserProvidedKey(entry[0]) + SUBSEPARATOR + getComponentKey(child, 0);
	            subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
	          }
	        }
	      }
	    } else if (type === 'object') {
	      var addendum = '';
	      if (process.env.NODE_ENV !== 'production') {
	        addendum = ' If you meant to render a collection of children, use an array ' + 'instead or wrap the object using createFragment(object) from the ' + 'React add-ons.';
	        if (children._isReactElement) {
	          addendum = ' It looks like you\'re using an element created by a different ' + 'version of React. Make sure to use only one copy of React.';
	        }
	        if (ReactCurrentOwner.current) {
	          var name = ReactCurrentOwner.current.getName();
	          if (name) {
	            addendum += ' Check the render method of `' + name + '`.';
	          }
	        }
	      }
	      var childrenString = String(children);
	       true ? process.env.NODE_ENV !== 'production' ? invariant(false, 'Objects are not valid as a React child (found: %s).%s', childrenString === '[object Object]' ? 'object with keys {' + Object.keys(children).join(', ') + '}' : childrenString, addendum) : invariant(false) : undefined;
	    }
	  }

	  return subtreeCount;
	}

	/**
	 * Traverses children that are typically specified as `props.children`, but
	 * might also be specified through attributes:
	 *
	 * - `traverseAllChildren(this.props.children, ...)`
	 * - `traverseAllChildren(this.props.leftPanelChildren, ...)`
	 *
	 * The `traverseContext` is an optional argument that is passed through the
	 * entire traversal. It can be used to store accumulations or anything else that
	 * the callback might find relevant.
	 *
	 * @param {?*} children Children tree object.
	 * @param {!function} callback To invoke upon traversing each child.
	 * @param {?*} traverseContext Context for traversal.
	 * @return {!number} The number of children in this subtree.
	 */
	function traverseAllChildren(children, callback, traverseContext) {
	  if (children == null) {
	    return 0;
	  }

	  return traverseAllChildrenImpl(children, '', callback, traverseContext);
	}

	module.exports = traverseAllChildren;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 115 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMSelect
	 */

	'use strict';

	var LinkedValueUtils = __webpack_require__(109);
	var ReactMount = __webpack_require__(31);
	var ReactUpdates = __webpack_require__(57);

	var assign = __webpack_require__(42);
	var warning = __webpack_require__(28);

	var valueContextKey = '__ReactDOMSelect_value$' + Math.random().toString(36).slice(2);

	function updateOptionsIfPendingUpdateAndMounted() {
	  if (this._rootNodeID && this._wrapperState.pendingUpdate) {
	    this._wrapperState.pendingUpdate = false;

	    var props = this._currentElement.props;
	    var value = LinkedValueUtils.getValue(props);

	    if (value != null) {
	      updateOptions(this, Boolean(props.multiple), value);
	    }
	  }
	}

	function getDeclarationErrorAddendum(owner) {
	  if (owner) {
	    var name = owner.getName();
	    if (name) {
	      return ' Check the render method of `' + name + '`.';
	    }
	  }
	  return '';
	}

	var valuePropNames = ['value', 'defaultValue'];

	/**
	 * Validation function for `value` and `defaultValue`.
	 * @private
	 */
	function checkSelectPropTypes(inst, props) {
	  var owner = inst._currentElement._owner;
	  LinkedValueUtils.checkPropTypes('select', props, owner);

	  for (var i = 0; i < valuePropNames.length; i++) {
	    var propName = valuePropNames[i];
	    if (props[propName] == null) {
	      continue;
	    }
	    if (props.multiple) {
	      process.env.NODE_ENV !== 'production' ? warning(Array.isArray(props[propName]), 'The `%s` prop supplied to <select> must be an array if ' + '`multiple` is true.%s', propName, getDeclarationErrorAddendum(owner)) : undefined;
	    } else {
	      process.env.NODE_ENV !== 'production' ? warning(!Array.isArray(props[propName]), 'The `%s` prop supplied to <select> must be a scalar ' + 'value if `multiple` is false.%s', propName, getDeclarationErrorAddendum(owner)) : undefined;
	    }
	  }
	}

	/**
	 * @param {ReactDOMComponent} inst
	 * @param {boolean} multiple
	 * @param {*} propValue A stringable (with `multiple`, a list of stringables).
	 * @private
	 */
	function updateOptions(inst, multiple, propValue) {
	  var selectedValue, i;
	  var options = ReactMount.getNode(inst._rootNodeID).options;

	  if (multiple) {
	    selectedValue = {};
	    for (i = 0; i < propValue.length; i++) {
	      selectedValue['' + propValue[i]] = true;
	    }
	    for (i = 0; i < options.length; i++) {
	      var selected = selectedValue.hasOwnProperty(options[i].value);
	      if (options[i].selected !== selected) {
	        options[i].selected = selected;
	      }
	    }
	  } else {
	    // Do not set `select.value` as exact behavior isn't consistent across all
	    // browsers for all cases.
	    selectedValue = '' + propValue;
	    for (i = 0; i < options.length; i++) {
	      if (options[i].value === selectedValue) {
	        options[i].selected = true;
	        return;
	      }
	    }
	    if (options.length) {
	      options[0].selected = true;
	    }
	  }
	}

	/**
	 * Implements a <select> native component that allows optionally setting the
	 * props `value` and `defaultValue`. If `multiple` is false, the prop must be a
	 * stringable. If `multiple` is true, the prop must be an array of stringables.
	 *
	 * If `value` is not supplied (or null/undefined), user actions that change the
	 * selected option will trigger updates to the rendered options.
	 *
	 * If it is supplied (and not null/undefined), the rendered options will not
	 * update in response to user actions. Instead, the `value` prop must change in
	 * order for the rendered options to update.
	 *
	 * If `defaultValue` is provided, any options with the supplied values will be
	 * selected.
	 */
	var ReactDOMSelect = {
	  valueContextKey: valueContextKey,

	  getNativeProps: function (inst, props, context) {
	    return assign({}, props, {
	      onChange: inst._wrapperState.onChange,
	      value: undefined
	    });
	  },

	  mountWrapper: function (inst, props) {
	    if (process.env.NODE_ENV !== 'production') {
	      checkSelectPropTypes(inst, props);
	    }

	    var value = LinkedValueUtils.getValue(props);
	    inst._wrapperState = {
	      pendingUpdate: false,
	      initialValue: value != null ? value : props.defaultValue,
	      onChange: _handleChange.bind(inst),
	      wasMultiple: Boolean(props.multiple)
	    };
	  },

	  processChildContext: function (inst, props, context) {
	    // Pass down initial value so initial generated markup has correct
	    // `selected` attributes
	    var childContext = assign({}, context);
	    childContext[valueContextKey] = inst._wrapperState.initialValue;
	    return childContext;
	  },

	  postUpdateWrapper: function (inst) {
	    var props = inst._currentElement.props;

	    // After the initial mount, we control selected-ness manually so don't pass
	    // the context value down
	    inst._wrapperState.initialValue = undefined;

	    var wasMultiple = inst._wrapperState.wasMultiple;
	    inst._wrapperState.wasMultiple = Boolean(props.multiple);

	    var value = LinkedValueUtils.getValue(props);
	    if (value != null) {
	      inst._wrapperState.pendingUpdate = false;
	      updateOptions(inst, Boolean(props.multiple), value);
	    } else if (wasMultiple !== Boolean(props.multiple)) {
	      // For simplicity, reapply `defaultValue` if `multiple` is toggled.
	      if (props.defaultValue != null) {
	        updateOptions(inst, Boolean(props.multiple), props.defaultValue);
	      } else {
	        // Revert the select back to its default unselected state.
	        updateOptions(inst, Boolean(props.multiple), props.multiple ? [] : '');
	      }
	    }
	  }
	};

	function _handleChange(event) {
	  var props = this._currentElement.props;
	  var returnValue = LinkedValueUtils.executeOnChange(props, event);

	  this._wrapperState.pendingUpdate = true;
	  ReactUpdates.asap(updateOptionsIfPendingUpdateAndMounted, this);
	  return returnValue;
	}

	module.exports = ReactDOMSelect;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 116 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMTextarea
	 */

	'use strict';

	var LinkedValueUtils = __webpack_require__(109);
	var ReactDOMIDOperations = __webpack_require__(30);
	var ReactUpdates = __webpack_require__(57);

	var assign = __webpack_require__(42);
	var invariant = __webpack_require__(16);
	var warning = __webpack_require__(28);

	function forceUpdateIfMounted() {
	  if (this._rootNodeID) {
	    // DOM component is still mounted; update
	    ReactDOMTextarea.updateWrapper(this);
	  }
	}

	/**
	 * Implements a <textarea> native component that allows setting `value`, and
	 * `defaultValue`. This differs from the traditional DOM API because value is
	 * usually set as PCDATA children.
	 *
	 * If `value` is not supplied (or null/undefined), user actions that affect the
	 * value will trigger updates to the element.
	 *
	 * If `value` is supplied (and not null/undefined), the rendered element will
	 * not trigger updates to the element. Instead, the `value` prop must change in
	 * order for the rendered element to be updated.
	 *
	 * The rendered element will be initialized with an empty value, the prop
	 * `defaultValue` if specified, or the children content (deprecated).
	 */
	var ReactDOMTextarea = {
	  getNativeProps: function (inst, props, context) {
	    !(props.dangerouslySetInnerHTML == null) ? process.env.NODE_ENV !== 'production' ? invariant(false, '`dangerouslySetInnerHTML` does not make sense on <textarea>.') : invariant(false) : undefined;

	    // Always set children to the same thing. In IE9, the selection range will
	    // get reset if `textContent` is mutated.
	    var nativeProps = assign({}, props, {
	      defaultValue: undefined,
	      value: undefined,
	      children: inst._wrapperState.initialValue,
	      onChange: inst._wrapperState.onChange
	    });

	    return nativeProps;
	  },

	  mountWrapper: function (inst, props) {
	    if (process.env.NODE_ENV !== 'production') {
	      LinkedValueUtils.checkPropTypes('textarea', props, inst._currentElement._owner);
	    }

	    var defaultValue = props.defaultValue;
	    // TODO (yungsters): Remove support for children content in <textarea>.
	    var children = props.children;
	    if (children != null) {
	      if (process.env.NODE_ENV !== 'production') {
	        process.env.NODE_ENV !== 'production' ? warning(false, 'Use the `defaultValue` or `value` props instead of setting ' + 'children on <textarea>.') : undefined;
	      }
	      !(defaultValue == null) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'If you supply `defaultValue` on a <textarea>, do not pass children.') : invariant(false) : undefined;
	      if (Array.isArray(children)) {
	        !(children.length <= 1) ? process.env.NODE_ENV !== 'production' ? invariant(false, '<textarea> can only have at most one child.') : invariant(false) : undefined;
	        children = children[0];
	      }

	      defaultValue = '' + children;
	    }
	    if (defaultValue == null) {
	      defaultValue = '';
	    }
	    var value = LinkedValueUtils.getValue(props);

	    inst._wrapperState = {
	      // We save the initial value so that `ReactDOMComponent` doesn't update
	      // `textContent` (unnecessary since we update value).
	      // The initial value can be a boolean or object so that's why it's
	      // forced to be a string.
	      initialValue: '' + (value != null ? value : defaultValue),
	      onChange: _handleChange.bind(inst)
	    };
	  },

	  updateWrapper: function (inst) {
	    var props = inst._currentElement.props;
	    var value = LinkedValueUtils.getValue(props);
	    if (value != null) {
	      // Cast `value` to a string to ensure the value is set correctly. While
	      // browsers typically do this as necessary, jsdom doesn't.
	      ReactDOMIDOperations.updatePropertyByID(inst._rootNodeID, 'value', '' + value);
	    }
	  }
	};

	function _handleChange(event) {
	  var props = this._currentElement.props;
	  var returnValue = LinkedValueUtils.executeOnChange(props, event);
	  ReactUpdates.asap(forceUpdateIfMounted, this);
	  return returnValue;
	}

	module.exports = ReactDOMTextarea;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 117 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactMultiChild
	 * @typechecks static-only
	 */

	'use strict';

	var ReactComponentEnvironment = __webpack_require__(67);
	var ReactMultiChildUpdateTypes = __webpack_require__(19);

	var ReactCurrentOwner = __webpack_require__(8);
	var ReactReconciler = __webpack_require__(53);
	var ReactChildReconciler = __webpack_require__(118);

	var flattenChildren = __webpack_require__(119);

	/**
	 * Updating children of a component may trigger recursive updates. The depth is
	 * used to batch recursive updates to render markup more efficiently.
	 *
	 * @type {number}
	 * @private
	 */
	var updateDepth = 0;

	/**
	 * Queue of update configuration objects.
	 *
	 * Each object has a `type` property that is in `ReactMultiChildUpdateTypes`.
	 *
	 * @type {array<object>}
	 * @private
	 */
	var updateQueue = [];

	/**
	 * Queue of markup to be rendered.
	 *
	 * @type {array<string>}
	 * @private
	 */
	var markupQueue = [];

	/**
	 * Enqueues markup to be rendered and inserted at a supplied index.
	 *
	 * @param {string} parentID ID of the parent component.
	 * @param {string} markup Markup that renders into an element.
	 * @param {number} toIndex Destination index.
	 * @private
	 */
	function enqueueInsertMarkup(parentID, markup, toIndex) {
	  // NOTE: Null values reduce hidden classes.
	  updateQueue.push({
	    parentID: parentID,
	    parentNode: null,
	    type: ReactMultiChildUpdateTypes.INSERT_MARKUP,
	    markupIndex: markupQueue.push(markup) - 1,
	    content: null,
	    fromIndex: null,
	    toIndex: toIndex
	  });
	}

	/**
	 * Enqueues moving an existing element to another index.
	 *
	 * @param {string} parentID ID of the parent component.
	 * @param {number} fromIndex Source index of the existing element.
	 * @param {number} toIndex Destination index of the element.
	 * @private
	 */
	function enqueueMove(parentID, fromIndex, toIndex) {
	  // NOTE: Null values reduce hidden classes.
	  updateQueue.push({
	    parentID: parentID,
	    parentNode: null,
	    type: ReactMultiChildUpdateTypes.MOVE_EXISTING,
	    markupIndex: null,
	    content: null,
	    fromIndex: fromIndex,
	    toIndex: toIndex
	  });
	}

	/**
	 * Enqueues removing an element at an index.
	 *
	 * @param {string} parentID ID of the parent component.
	 * @param {number} fromIndex Index of the element to remove.
	 * @private
	 */
	function enqueueRemove(parentID, fromIndex) {
	  // NOTE: Null values reduce hidden classes.
	  updateQueue.push({
	    parentID: parentID,
	    parentNode: null,
	    type: ReactMultiChildUpdateTypes.REMOVE_NODE,
	    markupIndex: null,
	    content: null,
	    fromIndex: fromIndex,
	    toIndex: null
	  });
	}

	/**
	 * Enqueues setting the markup of a node.
	 *
	 * @param {string} parentID ID of the parent component.
	 * @param {string} markup Markup that renders into an element.
	 * @private
	 */
	function enqueueSetMarkup(parentID, markup) {
	  // NOTE: Null values reduce hidden classes.
	  updateQueue.push({
	    parentID: parentID,
	    parentNode: null,
	    type: ReactMultiChildUpdateTypes.SET_MARKUP,
	    markupIndex: null,
	    content: markup,
	    fromIndex: null,
	    toIndex: null
	  });
	}

	/**
	 * Enqueues setting the text content.
	 *
	 * @param {string} parentID ID of the parent component.
	 * @param {string} textContent Text content to set.
	 * @private
	 */
	function enqueueTextContent(parentID, textContent) {
	  // NOTE: Null values reduce hidden classes.
	  updateQueue.push({
	    parentID: parentID,
	    parentNode: null,
	    type: ReactMultiChildUpdateTypes.TEXT_CONTENT,
	    markupIndex: null,
	    content: textContent,
	    fromIndex: null,
	    toIndex: null
	  });
	}

	/**
	 * Processes any enqueued updates.
	 *
	 * @private
	 */
	function processQueue() {
	  if (updateQueue.length) {
	    ReactComponentEnvironment.processChildrenUpdates(updateQueue, markupQueue);
	    clearQueue();
	  }
	}

	/**
	 * Clears any enqueued updates.
	 *
	 * @private
	 */
	function clearQueue() {
	  updateQueue.length = 0;
	  markupQueue.length = 0;
	}

	/**
	 * ReactMultiChild are capable of reconciling multiple children.
	 *
	 * @class ReactMultiChild
	 * @internal
	 */
	var ReactMultiChild = {

	  /**
	   * Provides common functionality for components that must reconcile multiple
	   * children. This is used by `ReactDOMComponent` to mount, update, and
	   * unmount child components.
	   *
	   * @lends {ReactMultiChild.prototype}
	   */
	  Mixin: {

	    _reconcilerInstantiateChildren: function (nestedChildren, transaction, context) {
	      if (process.env.NODE_ENV !== 'production') {
	        if (this._currentElement) {
	          try {
	            ReactCurrentOwner.current = this._currentElement._owner;
	            return ReactChildReconciler.instantiateChildren(nestedChildren, transaction, context);
	          } finally {
	            ReactCurrentOwner.current = null;
	          }
	        }
	      }
	      return ReactChildReconciler.instantiateChildren(nestedChildren, transaction, context);
	    },

	    _reconcilerUpdateChildren: function (prevChildren, nextNestedChildrenElements, transaction, context) {
	      var nextChildren;
	      if (process.env.NODE_ENV !== 'production') {
	        if (this._currentElement) {
	          try {
	            ReactCurrentOwner.current = this._currentElement._owner;
	            nextChildren = flattenChildren(nextNestedChildrenElements);
	          } finally {
	            ReactCurrentOwner.current = null;
	          }
	          return ReactChildReconciler.updateChildren(prevChildren, nextChildren, transaction, context);
	        }
	      }
	      nextChildren = flattenChildren(nextNestedChildrenElements);
	      return ReactChildReconciler.updateChildren(prevChildren, nextChildren, transaction, context);
	    },

	    /**
	     * Generates a "mount image" for each of the supplied children. In the case
	     * of `ReactDOMComponent`, a mount image is a string of markup.
	     *
	     * @param {?object} nestedChildren Nested child maps.
	     * @return {array} An array of mounted representations.
	     * @internal
	     */
	    mountChildren: function (nestedChildren, transaction, context) {
	      var children = this._reconcilerInstantiateChildren(nestedChildren, transaction, context);
	      this._renderedChildren = children;
	      var mountImages = [];
	      var index = 0;
	      for (var name in children) {
	        if (children.hasOwnProperty(name)) {
	          var child = children[name];
	          // Inlined for performance, see `ReactInstanceHandles.createReactID`.
	          var rootID = this._rootNodeID + name;
	          var mountImage = ReactReconciler.mountComponent(child, rootID, transaction, context);
	          child._mountIndex = index++;
	          mountImages.push(mountImage);
	        }
	      }
	      return mountImages;
	    },

	    /**
	     * Replaces any rendered children with a text content string.
	     *
	     * @param {string} nextContent String of content.
	     * @internal
	     */
	    updateTextContent: function (nextContent) {
	      updateDepth++;
	      var errorThrown = true;
	      try {
	        var prevChildren = this._renderedChildren;
	        // Remove any rendered children.
	        ReactChildReconciler.unmountChildren(prevChildren);
	        // TODO: The setTextContent operation should be enough
	        for (var name in prevChildren) {
	          if (prevChildren.hasOwnProperty(name)) {
	            this._unmountChild(prevChildren[name]);
	          }
	        }
	        // Set new text content.
	        this.setTextContent(nextContent);
	        errorThrown = false;
	      } finally {
	        updateDepth--;
	        if (!updateDepth) {
	          if (errorThrown) {
	            clearQueue();
	          } else {
	            processQueue();
	          }
	        }
	      }
	    },

	    /**
	     * Replaces any rendered children with a markup string.
	     *
	     * @param {string} nextMarkup String of markup.
	     * @internal
	     */
	    updateMarkup: function (nextMarkup) {
	      updateDepth++;
	      var errorThrown = true;
	      try {
	        var prevChildren = this._renderedChildren;
	        // Remove any rendered children.
	        ReactChildReconciler.unmountChildren(prevChildren);
	        for (var name in prevChildren) {
	          if (prevChildren.hasOwnProperty(name)) {
	            this._unmountChildByName(prevChildren[name], name);
	          }
	        }
	        this.setMarkup(nextMarkup);
	        errorThrown = false;
	      } finally {
	        updateDepth--;
	        if (!updateDepth) {
	          if (errorThrown) {
	            clearQueue();
	          } else {
	            processQueue();
	          }
	        }
	      }
	    },

	    /**
	     * Updates the rendered children with new children.
	     *
	     * @param {?object} nextNestedChildrenElements Nested child element maps.
	     * @param {ReactReconcileTransaction} transaction
	     * @internal
	     */
	    updateChildren: function (nextNestedChildrenElements, transaction, context) {
	      updateDepth++;
	      var errorThrown = true;
	      try {
	        this._updateChildren(nextNestedChildrenElements, transaction, context);
	        errorThrown = false;
	      } finally {
	        updateDepth--;
	        if (!updateDepth) {
	          if (errorThrown) {
	            clearQueue();
	          } else {
	            processQueue();
	          }
	        }
	      }
	    },

	    /**
	     * Improve performance by isolating this hot code path from the try/catch
	     * block in `updateChildren`.
	     *
	     * @param {?object} nextNestedChildrenElements Nested child element maps.
	     * @param {ReactReconcileTransaction} transaction
	     * @final
	     * @protected
	     */
	    _updateChildren: function (nextNestedChildrenElements, transaction, context) {
	      var prevChildren = this._renderedChildren;
	      var nextChildren = this._reconcilerUpdateChildren(prevChildren, nextNestedChildrenElements, transaction, context);
	      this._renderedChildren = nextChildren;
	      if (!nextChildren && !prevChildren) {
	        return;
	      }
	      var name;
	      // `nextIndex` will increment for each child in `nextChildren`, but
	      // `lastIndex` will be the last index visited in `prevChildren`.
	      var lastIndex = 0;
	      var nextIndex = 0;
	      for (name in nextChildren) {
	        if (!nextChildren.hasOwnProperty(name)) {
	          continue;
	        }
	        var prevChild = prevChildren && prevChildren[name];
	        var nextChild = nextChildren[name];
	        if (prevChild === nextChild) {
	          this.moveChild(prevChild, nextIndex, lastIndex);
	          lastIndex = Math.max(prevChild._mountIndex, lastIndex);
	          prevChild._mountIndex = nextIndex;
	        } else {
	          if (prevChild) {
	            // Update `lastIndex` before `_mountIndex` gets unset by unmounting.
	            lastIndex = Math.max(prevChild._mountIndex, lastIndex);
	            this._unmountChild(prevChild);
	          }
	          // The child must be instantiated before it's mounted.
	          this._mountChildByNameAtIndex(nextChild, name, nextIndex, transaction, context);
	        }
	        nextIndex++;
	      }
	      // Remove children that are no longer present.
	      for (name in prevChildren) {
	        if (prevChildren.hasOwnProperty(name) && !(nextChildren && nextChildren.hasOwnProperty(name))) {
	          this._unmountChild(prevChildren[name]);
	        }
	      }
	    },

	    /**
	     * Unmounts all rendered children. This should be used to clean up children
	     * when this component is unmounted.
	     *
	     * @internal
	     */
	    unmountChildren: function () {
	      var renderedChildren = this._renderedChildren;
	      ReactChildReconciler.unmountChildren(renderedChildren);
	      this._renderedChildren = null;
	    },

	    /**
	     * Moves a child component to the supplied index.
	     *
	     * @param {ReactComponent} child Component to move.
	     * @param {number} toIndex Destination index of the element.
	     * @param {number} lastIndex Last index visited of the siblings of `child`.
	     * @protected
	     */
	    moveChild: function (child, toIndex, lastIndex) {
	      // If the index of `child` is less than `lastIndex`, then it needs to
	      // be moved. Otherwise, we do not need to move it because a child will be
	      // inserted or moved before `child`.
	      if (child._mountIndex < lastIndex) {
	        enqueueMove(this._rootNodeID, child._mountIndex, toIndex);
	      }
	    },

	    /**
	     * Creates a child component.
	     *
	     * @param {ReactComponent} child Component to create.
	     * @param {string} mountImage Markup to insert.
	     * @protected
	     */
	    createChild: function (child, mountImage) {
	      enqueueInsertMarkup(this._rootNodeID, mountImage, child._mountIndex);
	    },

	    /**
	     * Removes a child component.
	     *
	     * @param {ReactComponent} child Child to remove.
	     * @protected
	     */
	    removeChild: function (child) {
	      enqueueRemove(this._rootNodeID, child._mountIndex);
	    },

	    /**
	     * Sets this text content string.
	     *
	     * @param {string} textContent Text content to set.
	     * @protected
	     */
	    setTextContent: function (textContent) {
	      enqueueTextContent(this._rootNodeID, textContent);
	    },

	    /**
	     * Sets this markup string.
	     *
	     * @param {string} markup Markup to set.
	     * @protected
	     */
	    setMarkup: function (markup) {
	      enqueueSetMarkup(this._rootNodeID, markup);
	    },

	    /**
	     * Mounts a child with the supplied name.
	     *
	     * NOTE: This is part of `updateChildren` and is here for readability.
	     *
	     * @param {ReactComponent} child Component to mount.
	     * @param {string} name Name of the child.
	     * @param {number} index Index at which to insert the child.
	     * @param {ReactReconcileTransaction} transaction
	     * @private
	     */
	    _mountChildByNameAtIndex: function (child, name, index, transaction, context) {
	      // Inlined for performance, see `ReactInstanceHandles.createReactID`.
	      var rootID = this._rootNodeID + name;
	      var mountImage = ReactReconciler.mountComponent(child, rootID, transaction, context);
	      child._mountIndex = index;
	      this.createChild(child, mountImage);
	    },

	    /**
	     * Unmounts a rendered child.
	     *
	     * NOTE: This is part of `updateChildren` and is here for readability.
	     *
	     * @param {ReactComponent} child Component to unmount.
	     * @private
	     */
	    _unmountChild: function (child) {
	      this.removeChild(child);
	      child._mountIndex = null;
	    }

	  }

	};

	module.exports = ReactMultiChild;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 118 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactChildReconciler
	 * @typechecks static-only
	 */

	'use strict';

	var ReactReconciler = __webpack_require__(53);

	var instantiateReactComponent = __webpack_require__(65);
	var shouldUpdateReactComponent = __webpack_require__(70);
	var traverseAllChildren = __webpack_require__(114);
	var warning = __webpack_require__(28);

	function instantiateChild(childInstances, child, name) {
	  // We found a component instance.
	  var keyUnique = childInstances[name] === undefined;
	  if (process.env.NODE_ENV !== 'production') {
	    process.env.NODE_ENV !== 'production' ? warning(keyUnique, 'flattenChildren(...): Encountered two children with the same key, ' + '`%s`. Child keys must be unique; when two children share a key, only ' + 'the first child will be used.', name) : undefined;
	  }
	  if (child != null && keyUnique) {
	    childInstances[name] = instantiateReactComponent(child, null);
	  }
	}

	/**
	 * ReactChildReconciler provides helpers for initializing or updating a set of
	 * children. Its output is suitable for passing it onto ReactMultiChild which
	 * does diffed reordering and insertion.
	 */
	var ReactChildReconciler = {
	  /**
	   * Generates a "mount image" for each of the supplied children. In the case
	   * of `ReactDOMComponent`, a mount image is a string of markup.
	   *
	   * @param {?object} nestedChildNodes Nested child maps.
	   * @return {?object} A set of child instances.
	   * @internal
	   */
	  instantiateChildren: function (nestedChildNodes, transaction, context) {
	    if (nestedChildNodes == null) {
	      return null;
	    }
	    var childInstances = {};
	    traverseAllChildren(nestedChildNodes, instantiateChild, childInstances);
	    return childInstances;
	  },

	  /**
	   * Updates the rendered children and returns a new set of children.
	   *
	   * @param {?object} prevChildren Previously initialized set of children.
	   * @param {?object} nextChildren Flat child element maps.
	   * @param {ReactReconcileTransaction} transaction
	   * @param {object} context
	   * @return {?object} A new set of child instances.
	   * @internal
	   */
	  updateChildren: function (prevChildren, nextChildren, transaction, context) {
	    // We currently don't have a way to track moves here but if we use iterators
	    // instead of for..in we can zip the iterators and check if an item has
	    // moved.
	    // TODO: If nothing has changed, return the prevChildren object so that we
	    // can quickly bailout if nothing has changed.
	    if (!nextChildren && !prevChildren) {
	      return null;
	    }
	    var name;
	    for (name in nextChildren) {
	      if (!nextChildren.hasOwnProperty(name)) {
	        continue;
	      }
	      var prevChild = prevChildren && prevChildren[name];
	      var prevElement = prevChild && prevChild._currentElement;
	      var nextElement = nextChildren[name];
	      if (prevChild != null && shouldUpdateReactComponent(prevElement, nextElement)) {
	        ReactReconciler.receiveComponent(prevChild, nextElement, transaction, context);
	        nextChildren[name] = prevChild;
	      } else {
	        if (prevChild) {
	          ReactReconciler.unmountComponent(prevChild, name);
	        }
	        // The child must be instantiated before it's mounted.
	        var nextChildInstance = instantiateReactComponent(nextElement, null);
	        nextChildren[name] = nextChildInstance;
	      }
	    }
	    // Unmount children that are no longer present.
	    for (name in prevChildren) {
	      if (prevChildren.hasOwnProperty(name) && !(nextChildren && nextChildren.hasOwnProperty(name))) {
	        ReactReconciler.unmountComponent(prevChildren[name]);
	      }
	    }
	    return nextChildren;
	  },

	  /**
	   * Unmounts all rendered children. This should be used to clean up children
	   * when this component is unmounted.
	   *
	   * @param {?object} renderedChildren Previously initialized set of children.
	   * @internal
	   */
	  unmountChildren: function (renderedChildren) {
	    for (var name in renderedChildren) {
	      if (renderedChildren.hasOwnProperty(name)) {
	        var renderedChild = renderedChildren[name];
	        ReactReconciler.unmountComponent(renderedChild);
	      }
	    }
	  }

	};

	module.exports = ReactChildReconciler;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 119 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule flattenChildren
	 */

	'use strict';

	var traverseAllChildren = __webpack_require__(114);
	var warning = __webpack_require__(28);

	/**
	 * @param {function} traverseContext Context passed through traversal.
	 * @param {?ReactComponent} child React child component.
	 * @param {!string} name String name of key path to child.
	 */
	function flattenSingleChildIntoContext(traverseContext, child, name) {
	  // We found a component instance.
	  var result = traverseContext;
	  var keyUnique = result[name] === undefined;
	  if (process.env.NODE_ENV !== 'production') {
	    process.env.NODE_ENV !== 'production' ? warning(keyUnique, 'flattenChildren(...): Encountered two children with the same key, ' + '`%s`. Child keys must be unique; when two children share a key, only ' + 'the first child will be used.', name) : undefined;
	  }
	  if (keyUnique && child != null) {
	    result[name] = child;
	  }
	}

	/**
	 * Flattens children that are typically specified as `props.children`. Any null
	 * children will not be included in the resulting object.
	 * @return {!object} flattened children keyed by name.
	 */
	function flattenChildren(children) {
	  if (children == null) {
	    return children;
	  }
	  var result = {};
	  traverseAllChildren(children, flattenSingleChildIntoContext, result);
	  return result;
	}

	module.exports = flattenChildren;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 120 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule shallowEqual
	 * @typechecks
	 * 
	 */

	'use strict';

	var hasOwnProperty = Object.prototype.hasOwnProperty;

	/**
	 * Performs equality by iterating through keys on an object and returning false
	 * when any key has values which are not strictly equal between the arguments.
	 * Returns true when the values of all keys are strictly equal.
	 */
	function shallowEqual(objA, objB) {
	  if (objA === objB) {
	    return true;
	  }

	  if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) {
	    return false;
	  }

	  var keysA = Object.keys(objA);
	  var keysB = Object.keys(objB);

	  if (keysA.length !== keysB.length) {
	    return false;
	  }

	  // Test for A's keys different from B.
	  var bHasOwnProperty = hasOwnProperty.bind(objB);
	  for (var i = 0; i < keysA.length; i++) {
	    if (!bHasOwnProperty(keysA[i]) || objA[keysA[i]] !== objB[keysA[i]]) {
	      return false;
	    }
	  }

	  return true;
	}

	module.exports = shallowEqual;

/***/ },
/* 121 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactEventListener
	 * @typechecks static-only
	 */

	'use strict';

	var EventListener = __webpack_require__(122);
	var ExecutionEnvironment = __webpack_require__(12);
	var PooledClass = __webpack_require__(59);
	var ReactInstanceHandles = __webpack_require__(48);
	var ReactMount = __webpack_require__(31);
	var ReactUpdates = __webpack_require__(57);

	var assign = __webpack_require__(42);
	var getEventTarget = __webpack_require__(84);
	var getUnboundedScrollPosition = __webpack_require__(123);

	var DOCUMENT_FRAGMENT_NODE_TYPE = 11;

	/**
	 * Finds the parent React component of `node`.
	 *
	 * @param {*} node
	 * @return {?DOMEventTarget} Parent container, or `null` if the specified node
	 *                           is not nested.
	 */
	function findParent(node) {
	  // TODO: It may be a good idea to cache this to prevent unnecessary DOM
	  // traversal, but caching is difficult to do correctly without using a
	  // mutation observer to listen for all DOM changes.
	  var nodeID = ReactMount.getID(node);
	  var rootID = ReactInstanceHandles.getReactRootIDFromNodeID(nodeID);
	  var container = ReactMount.findReactContainerForID(rootID);
	  var parent = ReactMount.getFirstReactDOM(container);
	  return parent;
	}

	// Used to store ancestor hierarchy in top level callback
	function TopLevelCallbackBookKeeping(topLevelType, nativeEvent) {
	  this.topLevelType = topLevelType;
	  this.nativeEvent = nativeEvent;
	  this.ancestors = [];
	}
	assign(TopLevelCallbackBookKeeping.prototype, {
	  destructor: function () {
	    this.topLevelType = null;
	    this.nativeEvent = null;
	    this.ancestors.length = 0;
	  }
	});
	PooledClass.addPoolingTo(TopLevelCallbackBookKeeping, PooledClass.twoArgumentPooler);

	function handleTopLevelImpl(bookKeeping) {
	  // TODO: Re-enable event.path handling
	  //
	  // if (bookKeeping.nativeEvent.path && bookKeeping.nativeEvent.path.length > 1) {
	  //   // New browsers have a path attribute on native events
	  //   handleTopLevelWithPath(bookKeeping);
	  // } else {
	  //   // Legacy browsers don't have a path attribute on native events
	  //   handleTopLevelWithoutPath(bookKeeping);
	  // }

	  void handleTopLevelWithPath; // temporarily unused
	  handleTopLevelWithoutPath(bookKeeping);
	}

	// Legacy browsers don't have a path attribute on native events
	function handleTopLevelWithoutPath(bookKeeping) {
	  var topLevelTarget = ReactMount.getFirstReactDOM(getEventTarget(bookKeeping.nativeEvent)) || window;

	  // Loop through the hierarchy, in case there's any nested components.
	  // It's important that we build the array of ancestors before calling any
	  // event handlers, because event handlers can modify the DOM, leading to
	  // inconsistencies with ReactMount's node cache. See #1105.
	  var ancestor = topLevelTarget;
	  while (ancestor) {
	    bookKeeping.ancestors.push(ancestor);
	    ancestor = findParent(ancestor);
	  }

	  for (var i = 0; i < bookKeeping.ancestors.length; i++) {
	    topLevelTarget = bookKeeping.ancestors[i];
	    var topLevelTargetID = ReactMount.getID(topLevelTarget) || '';
	    ReactEventListener._handleTopLevel(bookKeeping.topLevelType, topLevelTarget, topLevelTargetID, bookKeeping.nativeEvent, getEventTarget(bookKeeping.nativeEvent));
	  }
	}

	// New browsers have a path attribute on native events
	function handleTopLevelWithPath(bookKeeping) {
	  var path = bookKeeping.nativeEvent.path;
	  var currentNativeTarget = path[0];
	  var eventsFired = 0;
	  for (var i = 0; i < path.length; i++) {
	    var currentPathElement = path[i];
	    if (currentPathElement.nodeType === DOCUMENT_FRAGMENT_NODE_TYPE) {
	      currentNativeTarget = path[i + 1];
	    }
	    // TODO: slow
	    var reactParent = ReactMount.getFirstReactDOM(currentPathElement);
	    if (reactParent === currentPathElement) {
	      var currentPathElementID = ReactMount.getID(currentPathElement);
	      var newRootID = ReactInstanceHandles.getReactRootIDFromNodeID(currentPathElementID);
	      bookKeeping.ancestors.push(currentPathElement);

	      var topLevelTargetID = ReactMount.getID(currentPathElement) || '';
	      eventsFired++;
	      ReactEventListener._handleTopLevel(bookKeeping.topLevelType, currentPathElement, topLevelTargetID, bookKeeping.nativeEvent, currentNativeTarget);

	      // Jump to the root of this React render tree
	      while (currentPathElementID !== newRootID) {
	        i++;
	        currentPathElement = path[i];
	        currentPathElementID = ReactMount.getID(currentPathElement);
	      }
	    }
	  }
	  if (eventsFired === 0) {
	    ReactEventListener._handleTopLevel(bookKeeping.topLevelType, window, '', bookKeeping.nativeEvent, getEventTarget(bookKeeping.nativeEvent));
	  }
	}

	function scrollValueMonitor(cb) {
	  var scrollPosition = getUnboundedScrollPosition(window);
	  cb(scrollPosition);
	}

	var ReactEventListener = {
	  _enabled: true,
	  _handleTopLevel: null,

	  WINDOW_HANDLE: ExecutionEnvironment.canUseDOM ? window : null,

	  setHandleTopLevel: function (handleTopLevel) {
	    ReactEventListener._handleTopLevel = handleTopLevel;
	  },

	  setEnabled: function (enabled) {
	    ReactEventListener._enabled = !!enabled;
	  },

	  isEnabled: function () {
	    return ReactEventListener._enabled;
	  },

	  /**
	   * Traps top-level events by using event bubbling.
	   *
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {string} handlerBaseName Event name (e.g. "click").
	   * @param {object} handle Element on which to attach listener.
	   * @return {?object} An object with a remove function which will forcefully
	   *                  remove the listener.
	   * @internal
	   */
	  trapBubbledEvent: function (topLevelType, handlerBaseName, handle) {
	    var element = handle;
	    if (!element) {
	      return null;
	    }
	    return EventListener.listen(element, handlerBaseName, ReactEventListener.dispatchEvent.bind(null, topLevelType));
	  },

	  /**
	   * Traps a top-level event by using event capturing.
	   *
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {string} handlerBaseName Event name (e.g. "click").
	   * @param {object} handle Element on which to attach listener.
	   * @return {?object} An object with a remove function which will forcefully
	   *                  remove the listener.
	   * @internal
	   */
	  trapCapturedEvent: function (topLevelType, handlerBaseName, handle) {
	    var element = handle;
	    if (!element) {
	      return null;
	    }
	    return EventListener.capture(element, handlerBaseName, ReactEventListener.dispatchEvent.bind(null, topLevelType));
	  },

	  monitorScrollValue: function (refresh) {
	    var callback = scrollValueMonitor.bind(null, refresh);
	    EventListener.listen(window, 'scroll', callback);
	  },

	  dispatchEvent: function (topLevelType, nativeEvent) {
	    if (!ReactEventListener._enabled) {
	      return;
	    }

	    var bookKeeping = TopLevelCallbackBookKeeping.getPooled(topLevelType, nativeEvent);
	    try {
	      // Event queue being processed in the same cycle allows
	      // `preventDefault`.
	      ReactUpdates.batchedUpdates(handleTopLevelImpl, bookKeeping);
	    } finally {
	      TopLevelCallbackBookKeeping.release(bookKeeping);
	    }
	  }
	};

	module.exports = ReactEventListener;

/***/ },
/* 122 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 *
	 * Licensed under the Apache License, Version 2.0 (the "License");
	 * you may not use this file except in compliance with the License.
	 * You may obtain a copy of the License at
	 *
	 * http://www.apache.org/licenses/LICENSE-2.0
	 *
	 * Unless required by applicable law or agreed to in writing, software
	 * distributed under the License is distributed on an "AS IS" BASIS,
	 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
	 * See the License for the specific language governing permissions and
	 * limitations under the License.
	 *
	 * @providesModule EventListener
	 * @typechecks
	 */

	'use strict';

	var emptyFunction = __webpack_require__(18);

	/**
	 * Upstream version of event listener. Does not take into account specific
	 * nature of platform.
	 */
	var EventListener = {
	  /**
	   * Listen to DOM events during the bubble phase.
	   *
	   * @param {DOMEventTarget} target DOM element to register listener on.
	   * @param {string} eventType Event type, e.g. 'click' or 'mouseover'.
	   * @param {function} callback Callback function.
	   * @return {object} Object with a `remove` method.
	   */
	  listen: function (target, eventType, callback) {
	    if (target.addEventListener) {
	      target.addEventListener(eventType, callback, false);
	      return {
	        remove: function () {
	          target.removeEventListener(eventType, callback, false);
	        }
	      };
	    } else if (target.attachEvent) {
	      target.attachEvent('on' + eventType, callback);
	      return {
	        remove: function () {
	          target.detachEvent('on' + eventType, callback);
	        }
	      };
	    }
	  },

	  /**
	   * Listen to DOM events during the capture phase.
	   *
	   * @param {DOMEventTarget} target DOM element to register listener on.
	   * @param {string} eventType Event type, e.g. 'click' or 'mouseover'.
	   * @param {function} callback Callback function.
	   * @return {object} Object with a `remove` method.
	   */
	  capture: function (target, eventType, callback) {
	    if (target.addEventListener) {
	      target.addEventListener(eventType, callback, true);
	      return {
	        remove: function () {
	          target.removeEventListener(eventType, callback, true);
	        }
	      };
	    } else {
	      if (process.env.NODE_ENV !== 'production') {
	        console.error('Attempted to listen to events during the capture phase on a ' + 'browser that does not support the capture phase. Your application ' + 'will not receive some events.');
	      }
	      return {
	        remove: emptyFunction
	      };
	    }
	  },

	  registerDefault: function () {}
	};

	module.exports = EventListener;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 123 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getUnboundedScrollPosition
	 * @typechecks
	 */

	'use strict';

	/**
	 * Gets the scroll position of the supplied element or window.
	 *
	 * The return values are unbounded, unlike `getScrollPosition`. This means they
	 * may be negative or exceed the element boundaries (which is possible using
	 * inertial scrolling).
	 *
	 * @param {DOMWindow|DOMElement} scrollable
	 * @return {object} Map with `x` and `y` keys.
	 */
	function getUnboundedScrollPosition(scrollable) {
	  if (scrollable === window) {
	    return {
	      x: window.pageXOffset || document.documentElement.scrollLeft,
	      y: window.pageYOffset || document.documentElement.scrollTop
	    };
	  }
	  return {
	    x: scrollable.scrollLeft,
	    y: scrollable.scrollTop
	  };
	}

	module.exports = getUnboundedScrollPosition;

/***/ },
/* 124 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactInjection
	 */

	'use strict';

	var DOMProperty = __webpack_require__(26);
	var EventPluginHub = __webpack_require__(34);
	var ReactComponentEnvironment = __webpack_require__(67);
	var ReactClass = __webpack_require__(125);
	var ReactEmptyComponent = __webpack_require__(71);
	var ReactBrowserEventEmitter = __webpack_require__(32);
	var ReactNativeComponent = __webpack_require__(72);
	var ReactPerf = __webpack_require__(21);
	var ReactRootIndex = __webpack_require__(49);
	var ReactUpdates = __webpack_require__(57);

	var ReactInjection = {
	  Component: ReactComponentEnvironment.injection,
	  Class: ReactClass.injection,
	  DOMProperty: DOMProperty.injection,
	  EmptyComponent: ReactEmptyComponent.injection,
	  EventPluginHub: EventPluginHub.injection,
	  EventEmitter: ReactBrowserEventEmitter.injection,
	  NativeComponent: ReactNativeComponent.injection,
	  Perf: ReactPerf.injection,
	  RootIndex: ReactRootIndex.injection,
	  Updates: ReactUpdates.injection
	};

	module.exports = ReactInjection;

/***/ },
/* 125 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactClass
	 */

	'use strict';

	var ReactComponent = __webpack_require__(126);
	var ReactElement = __webpack_require__(45);
	var ReactPropTypeLocations = __webpack_require__(68);
	var ReactPropTypeLocationNames = __webpack_require__(69);
	var ReactNoopUpdateQueue = __webpack_require__(127);

	var assign = __webpack_require__(42);
	var emptyObject = __webpack_require__(61);
	var invariant = __webpack_require__(16);
	var keyMirror = __webpack_require__(20);
	var keyOf = __webpack_require__(82);
	var warning = __webpack_require__(28);

	var MIXINS_KEY = keyOf({ mixins: null });

	/**
	 * Policies that describe methods in `ReactClassInterface`.
	 */
	var SpecPolicy = keyMirror({
	  /**
	   * These methods may be defined only once by the class specification or mixin.
	   */
	  DEFINE_ONCE: null,
	  /**
	   * These methods may be defined by both the class specification and mixins.
	   * Subsequent definitions will be chained. These methods must return void.
	   */
	  DEFINE_MANY: null,
	  /**
	   * These methods are overriding the base class.
	   */
	  OVERRIDE_BASE: null,
	  /**
	   * These methods are similar to DEFINE_MANY, except we assume they return
	   * objects. We try to merge the keys of the return values of all the mixed in
	   * functions. If there is a key conflict we throw.
	   */
	  DEFINE_MANY_MERGED: null
	});

	var injectedMixins = [];

	var warnedSetProps = false;
	function warnSetProps() {
	  if (!warnedSetProps) {
	    warnedSetProps = true;
	    process.env.NODE_ENV !== 'production' ? warning(false, 'setProps(...) and replaceProps(...) are deprecated. ' + 'Instead, call render again at the top level.') : undefined;
	  }
	}

	/**
	 * Composite components are higher-level components that compose other composite
	 * or native components.
	 *
	 * To create a new type of `ReactClass`, pass a specification of
	 * your new class to `React.createClass`. The only requirement of your class
	 * specification is that you implement a `render` method.
	 *
	 *   var MyComponent = React.createClass({
	 *     render: function() {
	 *       return <div>Hello World</div>;
	 *     }
	 *   });
	 *
	 * The class specification supports a specific protocol of methods that have
	 * special meaning (e.g. `render`). See `ReactClassInterface` for
	 * more the comprehensive protocol. Any other properties and methods in the
	 * class specification will be available on the prototype.
	 *
	 * @interface ReactClassInterface
	 * @internal
	 */
	var ReactClassInterface = {

	  /**
	   * An array of Mixin objects to include when defining your component.
	   *
	   * @type {array}
	   * @optional
	   */
	  mixins: SpecPolicy.DEFINE_MANY,

	  /**
	   * An object containing properties and methods that should be defined on
	   * the component's constructor instead of its prototype (static methods).
	   *
	   * @type {object}
	   * @optional
	   */
	  statics: SpecPolicy.DEFINE_MANY,

	  /**
	   * Definition of prop types for this component.
	   *
	   * @type {object}
	   * @optional
	   */
	  propTypes: SpecPolicy.DEFINE_MANY,

	  /**
	   * Definition of context types for this component.
	   *
	   * @type {object}
	   * @optional
	   */
	  contextTypes: SpecPolicy.DEFINE_MANY,

	  /**
	   * Definition of context types this component sets for its children.
	   *
	   * @type {object}
	   * @optional
	   */
	  childContextTypes: SpecPolicy.DEFINE_MANY,

	  // ==== Definition methods ====

	  /**
	   * Invoked when the component is mounted. Values in the mapping will be set on
	   * `this.props` if that prop is not specified (i.e. using an `in` check).
	   *
	   * This method is invoked before `getInitialState` and therefore cannot rely
	   * on `this.state` or use `this.setState`.
	   *
	   * @return {object}
	   * @optional
	   */
	  getDefaultProps: SpecPolicy.DEFINE_MANY_MERGED,

	  /**
	   * Invoked once before the component is mounted. The return value will be used
	   * as the initial value of `this.state`.
	   *
	   *   getInitialState: function() {
	   *     return {
	   *       isOn: false,
	   *       fooBaz: new BazFoo()
	   *     }
	   *   }
	   *
	   * @return {object}
	   * @optional
	   */
	  getInitialState: SpecPolicy.DEFINE_MANY_MERGED,

	  /**
	   * @return {object}
	   * @optional
	   */
	  getChildContext: SpecPolicy.DEFINE_MANY_MERGED,

	  /**
	   * Uses props from `this.props` and state from `this.state` to render the
	   * structure of the component.
	   *
	   * No guarantees are made about when or how often this method is invoked, so
	   * it must not have side effects.
	   *
	   *   render: function() {
	   *     var name = this.props.name;
	   *     return <div>Hello, {name}!</div>;
	   *   }
	   *
	   * @return {ReactComponent}
	   * @nosideeffects
	   * @required
	   */
	  render: SpecPolicy.DEFINE_ONCE,

	  // ==== Delegate methods ====

	  /**
	   * Invoked when the component is initially created and about to be mounted.
	   * This may have side effects, but any external subscriptions or data created
	   * by this method must be cleaned up in `componentWillUnmount`.
	   *
	   * @optional
	   */
	  componentWillMount: SpecPolicy.DEFINE_MANY,

	  /**
	   * Invoked when the component has been mounted and has a DOM representation.
	   * However, there is no guarantee that the DOM node is in the document.
	   *
	   * Use this as an opportunity to operate on the DOM when the component has
	   * been mounted (initialized and rendered) for the first time.
	   *
	   * @param {DOMElement} rootNode DOM element representing the component.
	   * @optional
	   */
	  componentDidMount: SpecPolicy.DEFINE_MANY,

	  /**
	   * Invoked before the component receives new props.
	   *
	   * Use this as an opportunity to react to a prop transition by updating the
	   * state using `this.setState`. Current props are accessed via `this.props`.
	   *
	   *   componentWillReceiveProps: function(nextProps, nextContext) {
	   *     this.setState({
	   *       likesIncreasing: nextProps.likeCount > this.props.likeCount
	   *     });
	   *   }
	   *
	   * NOTE: There is no equivalent `componentWillReceiveState`. An incoming prop
	   * transition may cause a state change, but the opposite is not true. If you
	   * need it, you are probably looking for `componentWillUpdate`.
	   *
	   * @param {object} nextProps
	   * @optional
	   */
	  componentWillReceiveProps: SpecPolicy.DEFINE_MANY,

	  /**
	   * Invoked while deciding if the component should be updated as a result of
	   * receiving new props, state and/or context.
	   *
	   * Use this as an opportunity to `return false` when you're certain that the
	   * transition to the new props/state/context will not require a component
	   * update.
	   *
	   *   shouldComponentUpdate: function(nextProps, nextState, nextContext) {
	   *     return !equal(nextProps, this.props) ||
	   *       !equal(nextState, this.state) ||
	   *       !equal(nextContext, this.context);
	   *   }
	   *
	   * @param {object} nextProps
	   * @param {?object} nextState
	   * @param {?object} nextContext
	   * @return {boolean} True if the component should update.
	   * @optional
	   */
	  shouldComponentUpdate: SpecPolicy.DEFINE_ONCE,

	  /**
	   * Invoked when the component is about to update due to a transition from
	   * `this.props`, `this.state` and `this.context` to `nextProps`, `nextState`
	   * and `nextContext`.
	   *
	   * Use this as an opportunity to perform preparation before an update occurs.
	   *
	   * NOTE: You **cannot** use `this.setState()` in this method.
	   *
	   * @param {object} nextProps
	   * @param {?object} nextState
	   * @param {?object} nextContext
	   * @param {ReactReconcileTransaction} transaction
	   * @optional
	   */
	  componentWillUpdate: SpecPolicy.DEFINE_MANY,

	  /**
	   * Invoked when the component's DOM representation has been updated.
	   *
	   * Use this as an opportunity to operate on the DOM when the component has
	   * been updated.
	   *
	   * @param {object} prevProps
	   * @param {?object} prevState
	   * @param {?object} prevContext
	   * @param {DOMElement} rootNode DOM element representing the component.
	   * @optional
	   */
	  componentDidUpdate: SpecPolicy.DEFINE_MANY,

	  /**
	   * Invoked when the component is about to be removed from its parent and have
	   * its DOM representation destroyed.
	   *
	   * Use this as an opportunity to deallocate any external resources.
	   *
	   * NOTE: There is no `componentDidUnmount` since your component will have been
	   * destroyed by that point.
	   *
	   * @optional
	   */
	  componentWillUnmount: SpecPolicy.DEFINE_MANY,

	  // ==== Advanced methods ====

	  /**
	   * Updates the component's currently mounted DOM representation.
	   *
	   * By default, this implements React's rendering and reconciliation algorithm.
	   * Sophisticated clients may wish to override this.
	   *
	   * @param {ReactReconcileTransaction} transaction
	   * @internal
	   * @overridable
	   */
	  updateComponent: SpecPolicy.OVERRIDE_BASE

	};

	/**
	 * Mapping from class specification keys to special processing functions.
	 *
	 * Although these are declared like instance properties in the specification
	 * when defining classes using `React.createClass`, they are actually static
	 * and are accessible on the constructor instead of the prototype. Despite
	 * being static, they must be defined outside of the "statics" key under
	 * which all other static methods are defined.
	 */
	var RESERVED_SPEC_KEYS = {
	  displayName: function (Constructor, displayName) {
	    Constructor.displayName = displayName;
	  },
	  mixins: function (Constructor, mixins) {
	    if (mixins) {
	      for (var i = 0; i < mixins.length; i++) {
	        mixSpecIntoComponent(Constructor, mixins[i]);
	      }
	    }
	  },
	  childContextTypes: function (Constructor, childContextTypes) {
	    if (process.env.NODE_ENV !== 'production') {
	      validateTypeDef(Constructor, childContextTypes, ReactPropTypeLocations.childContext);
	    }
	    Constructor.childContextTypes = assign({}, Constructor.childContextTypes, childContextTypes);
	  },
	  contextTypes: function (Constructor, contextTypes) {
	    if (process.env.NODE_ENV !== 'production') {
	      validateTypeDef(Constructor, contextTypes, ReactPropTypeLocations.context);
	    }
	    Constructor.contextTypes = assign({}, Constructor.contextTypes, contextTypes);
	  },
	  /**
	   * Special case getDefaultProps which should move into statics but requires
	   * automatic merging.
	   */
	  getDefaultProps: function (Constructor, getDefaultProps) {
	    if (Constructor.getDefaultProps) {
	      Constructor.getDefaultProps = createMergedResultFunction(Constructor.getDefaultProps, getDefaultProps);
	    } else {
	      Constructor.getDefaultProps = getDefaultProps;
	    }
	  },
	  propTypes: function (Constructor, propTypes) {
	    if (process.env.NODE_ENV !== 'production') {
	      validateTypeDef(Constructor, propTypes, ReactPropTypeLocations.prop);
	    }
	    Constructor.propTypes = assign({}, Constructor.propTypes, propTypes);
	  },
	  statics: function (Constructor, statics) {
	    mixStaticSpecIntoComponent(Constructor, statics);
	  },
	  autobind: function () {} };

	// noop
	function validateTypeDef(Constructor, typeDef, location) {
	  for (var propName in typeDef) {
	    if (typeDef.hasOwnProperty(propName)) {
	      // use a warning instead of an invariant so components
	      // don't show up in prod but not in __DEV__
	      process.env.NODE_ENV !== 'production' ? warning(typeof typeDef[propName] === 'function', '%s: %s type `%s` is invalid; it must be a function, usually from ' + 'React.PropTypes.', Constructor.displayName || 'ReactClass', ReactPropTypeLocationNames[location], propName) : undefined;
	    }
	  }
	}

	function validateMethodOverride(proto, name) {
	  var specPolicy = ReactClassInterface.hasOwnProperty(name) ? ReactClassInterface[name] : null;

	  // Disallow overriding of base class methods unless explicitly allowed.
	  if (ReactClassMixin.hasOwnProperty(name)) {
	    !(specPolicy === SpecPolicy.OVERRIDE_BASE) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactClassInterface: You are attempting to override ' + '`%s` from your class specification. Ensure that your method names ' + 'do not overlap with React methods.', name) : invariant(false) : undefined;
	  }

	  // Disallow defining methods more than once unless explicitly allowed.
	  if (proto.hasOwnProperty(name)) {
	    !(specPolicy === SpecPolicy.DEFINE_MANY || specPolicy === SpecPolicy.DEFINE_MANY_MERGED) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactClassInterface: You are attempting to define ' + '`%s` on your component more than once. This conflict may be due ' + 'to a mixin.', name) : invariant(false) : undefined;
	  }
	}

	/**
	 * Mixin helper which handles policy validation and reserved
	 * specification keys when building React classses.
	 */
	function mixSpecIntoComponent(Constructor, spec) {
	  if (!spec) {
	    return;
	  }

	  !(typeof spec !== 'function') ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactClass: You\'re attempting to ' + 'use a component class as a mixin. Instead, just use a regular object.') : invariant(false) : undefined;
	  !!ReactElement.isValidElement(spec) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactClass: You\'re attempting to ' + 'use a component as a mixin. Instead, just use a regular object.') : invariant(false) : undefined;

	  var proto = Constructor.prototype;

	  // By handling mixins before any other properties, we ensure the same
	  // chaining order is applied to methods with DEFINE_MANY policy, whether
	  // mixins are listed before or after these methods in the spec.
	  if (spec.hasOwnProperty(MIXINS_KEY)) {
	    RESERVED_SPEC_KEYS.mixins(Constructor, spec.mixins);
	  }

	  for (var name in spec) {
	    if (!spec.hasOwnProperty(name)) {
	      continue;
	    }

	    if (name === MIXINS_KEY) {
	      // We have already handled mixins in a special case above.
	      continue;
	    }

	    var property = spec[name];
	    validateMethodOverride(proto, name);

	    if (RESERVED_SPEC_KEYS.hasOwnProperty(name)) {
	      RESERVED_SPEC_KEYS[name](Constructor, property);
	    } else {
	      // Setup methods on prototype:
	      // The following member methods should not be automatically bound:
	      // 1. Expected ReactClass methods (in the "interface").
	      // 2. Overridden methods (that were mixed in).
	      var isReactClassMethod = ReactClassInterface.hasOwnProperty(name);
	      var isAlreadyDefined = proto.hasOwnProperty(name);
	      var isFunction = typeof property === 'function';
	      var shouldAutoBind = isFunction && !isReactClassMethod && !isAlreadyDefined && spec.autobind !== false;

	      if (shouldAutoBind) {
	        if (!proto.__reactAutoBindMap) {
	          proto.__reactAutoBindMap = {};
	        }
	        proto.__reactAutoBindMap[name] = property;
	        proto[name] = property;
	      } else {
	        if (isAlreadyDefined) {
	          var specPolicy = ReactClassInterface[name];

	          // These cases should already be caught by validateMethodOverride.
	          !(isReactClassMethod && (specPolicy === SpecPolicy.DEFINE_MANY_MERGED || specPolicy === SpecPolicy.DEFINE_MANY)) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactClass: Unexpected spec policy %s for key %s ' + 'when mixing in component specs.', specPolicy, name) : invariant(false) : undefined;

	          // For methods which are defined more than once, call the existing
	          // methods before calling the new property, merging if appropriate.
	          if (specPolicy === SpecPolicy.DEFINE_MANY_MERGED) {
	            proto[name] = createMergedResultFunction(proto[name], property);
	          } else if (specPolicy === SpecPolicy.DEFINE_MANY) {
	            proto[name] = createChainedFunction(proto[name], property);
	          }
	        } else {
	          proto[name] = property;
	          if (process.env.NODE_ENV !== 'production') {
	            // Add verbose displayName to the function, which helps when looking
	            // at profiling tools.
	            if (typeof property === 'function' && spec.displayName) {
	              proto[name].displayName = spec.displayName + '_' + name;
	            }
	          }
	        }
	      }
	    }
	  }
	}

	function mixStaticSpecIntoComponent(Constructor, statics) {
	  if (!statics) {
	    return;
	  }
	  for (var name in statics) {
	    var property = statics[name];
	    if (!statics.hasOwnProperty(name)) {
	      continue;
	    }

	    var isReserved = (name in RESERVED_SPEC_KEYS);
	    !!isReserved ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactClass: You are attempting to define a reserved ' + 'property, `%s`, that shouldn\'t be on the "statics" key. Define it ' + 'as an instance property instead; it will still be accessible on the ' + 'constructor.', name) : invariant(false) : undefined;

	    var isInherited = (name in Constructor);
	    !!isInherited ? process.env.NODE_ENV !== 'production' ? invariant(false, 'ReactClass: You are attempting to define ' + '`%s` on your component more than once. This conflict may be ' + 'due to a mixin.', name) : invariant(false) : undefined;
	    Constructor[name] = property;
	  }
	}

	/**
	 * Merge two objects, but throw if both contain the same key.
	 *
	 * @param {object} one The first object, which is mutated.
	 * @param {object} two The second object
	 * @return {object} one after it has been mutated to contain everything in two.
	 */
	function mergeIntoWithNoDuplicateKeys(one, two) {
	  !(one && two && typeof one === 'object' && typeof two === 'object') ? process.env.NODE_ENV !== 'production' ? invariant(false, 'mergeIntoWithNoDuplicateKeys(): Cannot merge non-objects.') : invariant(false) : undefined;

	  for (var key in two) {
	    if (two.hasOwnProperty(key)) {
	      !(one[key] === undefined) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'mergeIntoWithNoDuplicateKeys(): ' + 'Tried to merge two objects with the same key: `%s`. This conflict ' + 'may be due to a mixin; in particular, this may be caused by two ' + 'getInitialState() or getDefaultProps() methods returning objects ' + 'with clashing keys.', key) : invariant(false) : undefined;
	      one[key] = two[key];
	    }
	  }
	  return one;
	}

	/**
	 * Creates a function that invokes two functions and merges their return values.
	 *
	 * @param {function} one Function to invoke first.
	 * @param {function} two Function to invoke second.
	 * @return {function} Function that invokes the two argument functions.
	 * @private
	 */
	function createMergedResultFunction(one, two) {
	  return function mergedResult() {
	    var a = one.apply(this, arguments);
	    var b = two.apply(this, arguments);
	    if (a == null) {
	      return b;
	    } else if (b == null) {
	      return a;
	    }
	    var c = {};
	    mergeIntoWithNoDuplicateKeys(c, a);
	    mergeIntoWithNoDuplicateKeys(c, b);
	    return c;
	  };
	}

	/**
	 * Creates a function that invokes two functions and ignores their return vales.
	 *
	 * @param {function} one Function to invoke first.
	 * @param {function} two Function to invoke second.
	 * @return {function} Function that invokes the two argument functions.
	 * @private
	 */
	function createChainedFunction(one, two) {
	  return function chainedFunction() {
	    one.apply(this, arguments);
	    two.apply(this, arguments);
	  };
	}

	/**
	 * Binds a method to the component.
	 *
	 * @param {object} component Component whose method is going to be bound.
	 * @param {function} method Method to be bound.
	 * @return {function} The bound method.
	 */
	function bindAutoBindMethod(component, method) {
	  var boundMethod = method.bind(component);
	  if (process.env.NODE_ENV !== 'production') {
	    boundMethod.__reactBoundContext = component;
	    boundMethod.__reactBoundMethod = method;
	    boundMethod.__reactBoundArguments = null;
	    var componentName = component.constructor.displayName;
	    var _bind = boundMethod.bind;
	    /* eslint-disable block-scoped-var, no-undef */
	    boundMethod.bind = function (newThis) {
	      for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
	        args[_key - 1] = arguments[_key];
	      }

	      // User is trying to bind() an autobound method; we effectively will
	      // ignore the value of "this" that the user is trying to use, so
	      // let's warn.
	      if (newThis !== component && newThis !== null) {
	        process.env.NODE_ENV !== 'production' ? warning(false, 'bind(): React component methods may only be bound to the ' + 'component instance. See %s', componentName) : undefined;
	      } else if (!args.length) {
	        process.env.NODE_ENV !== 'production' ? warning(false, 'bind(): You are binding a component method to the component. ' + 'React does this for you automatically in a high-performance ' + 'way, so you can safely remove this call. See %s', componentName) : undefined;
	        return boundMethod;
	      }
	      var reboundMethod = _bind.apply(boundMethod, arguments);
	      reboundMethod.__reactBoundContext = component;
	      reboundMethod.__reactBoundMethod = method;
	      reboundMethod.__reactBoundArguments = args;
	      return reboundMethod;
	      /* eslint-enable */
	    };
	  }
	  return boundMethod;
	}

	/**
	 * Binds all auto-bound methods in a component.
	 *
	 * @param {object} component Component whose method is going to be bound.
	 */
	function bindAutoBindMethods(component) {
	  for (var autoBindKey in component.__reactAutoBindMap) {
	    if (component.__reactAutoBindMap.hasOwnProperty(autoBindKey)) {
	      var method = component.__reactAutoBindMap[autoBindKey];
	      component[autoBindKey] = bindAutoBindMethod(component, method);
	    }
	  }
	}

	/**
	 * Add more to the ReactClass base class. These are all legacy features and
	 * therefore not already part of the modern ReactComponent.
	 */
	var ReactClassMixin = {

	  /**
	   * TODO: This will be deprecated because state should always keep a consistent
	   * type signature and the only use case for this, is to avoid that.
	   */
	  replaceState: function (newState, callback) {
	    this.updater.enqueueReplaceState(this, newState);
	    if (callback) {
	      this.updater.enqueueCallback(this, callback);
	    }
	  },

	  /**
	   * Checks whether or not this composite component is mounted.
	   * @return {boolean} True if mounted, false otherwise.
	   * @protected
	   * @final
	   */
	  isMounted: function () {
	    return this.updater.isMounted(this);
	  },

	  /**
	   * Sets a subset of the props.
	   *
	   * @param {object} partialProps Subset of the next props.
	   * @param {?function} callback Called after props are updated.
	   * @final
	   * @public
	   * @deprecated
	   */
	  setProps: function (partialProps, callback) {
	    if (process.env.NODE_ENV !== 'production') {
	      warnSetProps();
	    }
	    this.updater.enqueueSetProps(this, partialProps);
	    if (callback) {
	      this.updater.enqueueCallback(this, callback);
	    }
	  },

	  /**
	   * Replace all the props.
	   *
	   * @param {object} newProps Subset of the next props.
	   * @param {?function} callback Called after props are updated.
	   * @final
	   * @public
	   * @deprecated
	   */
	  replaceProps: function (newProps, callback) {
	    if (process.env.NODE_ENV !== 'production') {
	      warnSetProps();
	    }
	    this.updater.enqueueReplaceProps(this, newProps);
	    if (callback) {
	      this.updater.enqueueCallback(this, callback);
	    }
	  }
	};

	var ReactClassComponent = function () {};
	assign(ReactClassComponent.prototype, ReactComponent.prototype, ReactClassMixin);

	/**
	 * Module for creating composite components.
	 *
	 * @class ReactClass
	 */
	var ReactClass = {

	  /**
	   * Creates a composite component class given a class specification.
	   *
	   * @param {object} spec Class specification (which must define `render`).
	   * @return {function} Component constructor function.
	   * @public
	   */
	  createClass: function (spec) {
	    var Constructor = function (props, context, updater) {
	      // This constructor is overridden by mocks. The argument is used
	      // by mocks to assert on what gets mounted.

	      if (process.env.NODE_ENV !== 'production') {
	        process.env.NODE_ENV !== 'production' ? warning(this instanceof Constructor, 'Something is calling a React component directly. Use a factory or ' + 'JSX instead. See: https://fb.me/react-legacyfactory') : undefined;
	      }

	      // Wire up auto-binding
	      if (this.__reactAutoBindMap) {
	        bindAutoBindMethods(this);
	      }

	      this.props = props;
	      this.context = context;
	      this.refs = emptyObject;
	      this.updater = updater || ReactNoopUpdateQueue;

	      this.state = null;

	      // ReactClasses doesn't have constructors. Instead, they use the
	      // getInitialState and componentWillMount methods for initialization.

	      var initialState = this.getInitialState ? this.getInitialState() : null;
	      if (process.env.NODE_ENV !== 'production') {
	        // We allow auto-mocks to proceed as if they're returning null.
	        if (typeof initialState === 'undefined' && this.getInitialState._isMockFunction) {
	          // This is probably bad practice. Consider warning here and
	          // deprecating this convenience.
	          initialState = null;
	        }
	      }
	      !(typeof initialState === 'object' && !Array.isArray(initialState)) ? process.env.NODE_ENV !== 'production' ? invariant(false, '%s.getInitialState(): must return an object or null', Constructor.displayName || 'ReactCompositeComponent') : invariant(false) : undefined;

	      this.state = initialState;
	    };
	    Constructor.prototype = new ReactClassComponent();
	    Constructor.prototype.constructor = Constructor;

	    injectedMixins.forEach(mixSpecIntoComponent.bind(null, Constructor));

	    mixSpecIntoComponent(Constructor, spec);

	    // Initialize the defaultProps property after all mixins have been merged.
	    if (Constructor.getDefaultProps) {
	      Constructor.defaultProps = Constructor.getDefaultProps();
	    }

	    if (process.env.NODE_ENV !== 'production') {
	      // This is a tag to indicate that the use of these method names is ok,
	      // since it's used with createClass. If it's not, then it's likely a
	      // mistake so we'll warn you to use the static property, property
	      // initializer or constructor respectively.
	      if (Constructor.getDefaultProps) {
	        Constructor.getDefaultProps.isReactClassApproved = {};
	      }
	      if (Constructor.prototype.getInitialState) {
	        Constructor.prototype.getInitialState.isReactClassApproved = {};
	      }
	    }

	    !Constructor.prototype.render ? process.env.NODE_ENV !== 'production' ? invariant(false, 'createClass(...): Class specification must implement a `render` method.') : invariant(false) : undefined;

	    if (process.env.NODE_ENV !== 'production') {
	      process.env.NODE_ENV !== 'production' ? warning(!Constructor.prototype.componentShouldUpdate, '%s has a method called ' + 'componentShouldUpdate(). Did you mean shouldComponentUpdate()? ' + 'The name is phrased as a question because the function is ' + 'expected to return a value.', spec.displayName || 'A component') : undefined;
	      process.env.NODE_ENV !== 'production' ? warning(!Constructor.prototype.componentWillRecieveProps, '%s has a method called ' + 'componentWillRecieveProps(). Did you mean componentWillReceiveProps()?', spec.displayName || 'A component') : undefined;
	    }

	    // Reduce time spent doing lookups by setting these on the prototype.
	    for (var methodName in ReactClassInterface) {
	      if (!Constructor.prototype[methodName]) {
	        Constructor.prototype[methodName] = null;
	      }
	    }

	    return Constructor;
	  },

	  injection: {
	    injectMixin: function (mixin) {
	      injectedMixins.push(mixin);
	    }
	  }

	};

	module.exports = ReactClass;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 126 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactComponent
	 */

	'use strict';

	var ReactNoopUpdateQueue = __webpack_require__(127);

	var canDefineProperty = __webpack_require__(46);
	var emptyObject = __webpack_require__(61);
	var invariant = __webpack_require__(16);
	var warning = __webpack_require__(28);

	/**
	 * Base class helpers for the updating state of a component.
	 */
	function ReactComponent(props, context, updater) {
	  this.props = props;
	  this.context = context;
	  this.refs = emptyObject;
	  // We initialize the default updater but the real one gets injected by the
	  // renderer.
	  this.updater = updater || ReactNoopUpdateQueue;
	}

	ReactComponent.prototype.isReactComponent = {};

	/**
	 * Sets a subset of the state. Always use this to mutate
	 * state. You should treat `this.state` as immutable.
	 *
	 * There is no guarantee that `this.state` will be immediately updated, so
	 * accessing `this.state` after calling this method may return the old value.
	 *
	 * There is no guarantee that calls to `setState` will run synchronously,
	 * as they may eventually be batched together.  You can provide an optional
	 * callback that will be executed when the call to setState is actually
	 * completed.
	 *
	 * When a function is provided to setState, it will be called at some point in
	 * the future (not synchronously). It will be called with the up to date
	 * component arguments (state, props, context). These values can be different
	 * from this.* because your function may be called after receiveProps but before
	 * shouldComponentUpdate, and this new state, props, and context will not yet be
	 * assigned to this.
	 *
	 * @param {object|function} partialState Next partial state or function to
	 *        produce next partial state to be merged with current state.
	 * @param {?function} callback Called after state is updated.
	 * @final
	 * @protected
	 */
	ReactComponent.prototype.setState = function (partialState, callback) {
	  !(typeof partialState === 'object' || typeof partialState === 'function' || partialState == null) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'setState(...): takes an object of state variables to update or a ' + 'function which returns an object of state variables.') : invariant(false) : undefined;
	  if (process.env.NODE_ENV !== 'production') {
	    process.env.NODE_ENV !== 'production' ? warning(partialState != null, 'setState(...): You passed an undefined or null state object; ' + 'instead, use forceUpdate().') : undefined;
	  }
	  this.updater.enqueueSetState(this, partialState);
	  if (callback) {
	    this.updater.enqueueCallback(this, callback);
	  }
	};

	/**
	 * Forces an update. This should only be invoked when it is known with
	 * certainty that we are **not** in a DOM transaction.
	 *
	 * You may want to call this when you know that some deeper aspect of the
	 * component's state has changed but `setState` was not called.
	 *
	 * This will not invoke `shouldComponentUpdate`, but it will invoke
	 * `componentWillUpdate` and `componentDidUpdate`.
	 *
	 * @param {?function} callback Called after update is complete.
	 * @final
	 * @protected
	 */
	ReactComponent.prototype.forceUpdate = function (callback) {
	  this.updater.enqueueForceUpdate(this);
	  if (callback) {
	    this.updater.enqueueCallback(this, callback);
	  }
	};

	/**
	 * Deprecated APIs. These APIs used to exist on classic React classes but since
	 * we would like to deprecate them, we're not going to move them over to this
	 * modern base class. Instead, we define a getter that warns if it's accessed.
	 */
	if (process.env.NODE_ENV !== 'production') {
	  var deprecatedAPIs = {
	    getDOMNode: ['getDOMNode', 'Use ReactDOM.findDOMNode(component) instead.'],
	    isMounted: ['isMounted', 'Instead, make sure to clean up subscriptions and pending requests in ' + 'componentWillUnmount to prevent memory leaks.'],
	    replaceProps: ['replaceProps', 'Instead, call render again at the top level.'],
	    replaceState: ['replaceState', 'Refactor your code to use setState instead (see ' + 'https://github.com/facebook/react/issues/3236).'],
	    setProps: ['setProps', 'Instead, call render again at the top level.']
	  };
	  var defineDeprecationWarning = function (methodName, info) {
	    if (canDefineProperty) {
	      Object.defineProperty(ReactComponent.prototype, methodName, {
	        get: function () {
	          process.env.NODE_ENV !== 'production' ? warning(false, '%s(...) is deprecated in plain JavaScript React classes. %s', info[0], info[1]) : undefined;
	          return undefined;
	        }
	      });
	    }
	  };
	  for (var fnName in deprecatedAPIs) {
	    if (deprecatedAPIs.hasOwnProperty(fnName)) {
	      defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);
	    }
	  }
	}

	module.exports = ReactComponent;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 127 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactNoopUpdateQueue
	 */

	'use strict';

	var warning = __webpack_require__(28);

	function warnTDZ(publicInstance, callerName) {
	  if (process.env.NODE_ENV !== 'production') {
	    process.env.NODE_ENV !== 'production' ? warning(false, '%s(...): Can only update a mounted or mounting component. ' + 'This usually means you called %s() on an unmounted component. ' + 'This is a no-op. Please check the code for the %s component.', callerName, callerName, publicInstance.constructor && publicInstance.constructor.displayName || '') : undefined;
	  }
	}

	/**
	 * This is the abstract API for an update queue.
	 */
	var ReactNoopUpdateQueue = {

	  /**
	   * Checks whether or not this composite component is mounted.
	   * @param {ReactClass} publicInstance The instance we want to test.
	   * @return {boolean} True if mounted, false otherwise.
	   * @protected
	   * @final
	   */
	  isMounted: function (publicInstance) {
	    return false;
	  },

	  /**
	   * Enqueue a callback that will be executed after all the pending updates
	   * have processed.
	   *
	   * @param {ReactClass} publicInstance The instance to use as `this` context.
	   * @param {?function} callback Called after state is updated.
	   * @internal
	   */
	  enqueueCallback: function (publicInstance, callback) {},

	  /**
	   * Forces an update. This should only be invoked when it is known with
	   * certainty that we are **not** in a DOM transaction.
	   *
	   * You may want to call this when you know that some deeper aspect of the
	   * component's state has changed but `setState` was not called.
	   *
	   * This will not invoke `shouldComponentUpdate`, but it will invoke
	   * `componentWillUpdate` and `componentDidUpdate`.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @internal
	   */
	  enqueueForceUpdate: function (publicInstance) {
	    warnTDZ(publicInstance, 'forceUpdate');
	  },

	  /**
	   * Replaces all of the state. Always use this or `setState` to mutate state.
	   * You should treat `this.state` as immutable.
	   *
	   * There is no guarantee that `this.state` will be immediately updated, so
	   * accessing `this.state` after calling this method may return the old value.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @param {object} completeState Next state.
	   * @internal
	   */
	  enqueueReplaceState: function (publicInstance, completeState) {
	    warnTDZ(publicInstance, 'replaceState');
	  },

	  /**
	   * Sets a subset of the state. This only exists because _pendingState is
	   * internal. This provides a merging strategy that is not available to deep
	   * properties which is confusing. TODO: Expose pendingState or don't use it
	   * during the merge.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @param {object} partialState Next partial state to be merged with state.
	   * @internal
	   */
	  enqueueSetState: function (publicInstance, partialState) {
	    warnTDZ(publicInstance, 'setState');
	  },

	  /**
	   * Sets a subset of the props.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @param {object} partialProps Subset of the next props.
	   * @internal
	   */
	  enqueueSetProps: function (publicInstance, partialProps) {
	    warnTDZ(publicInstance, 'setProps');
	  },

	  /**
	   * Replaces all of the props.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @param {object} props New props.
	   * @internal
	   */
	  enqueueReplaceProps: function (publicInstance, props) {
	    warnTDZ(publicInstance, 'replaceProps');
	  }

	};

	module.exports = ReactNoopUpdateQueue;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 128 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactReconcileTransaction
	 * @typechecks static-only
	 */

	'use strict';

	var CallbackQueue = __webpack_require__(58);
	var PooledClass = __webpack_require__(59);
	var ReactBrowserEventEmitter = __webpack_require__(32);
	var ReactDOMFeatureFlags = __webpack_require__(44);
	var ReactInputSelection = __webpack_require__(129);
	var Transaction = __webpack_require__(60);

	var assign = __webpack_require__(42);

	/**
	 * Ensures that, when possible, the selection range (currently selected text
	 * input) is not disturbed by performing the transaction.
	 */
	var SELECTION_RESTORATION = {
	  /**
	   * @return {Selection} Selection information.
	   */
	  initialize: ReactInputSelection.getSelectionInformation,
	  /**
	   * @param {Selection} sel Selection information returned from `initialize`.
	   */
	  close: ReactInputSelection.restoreSelection
	};

	/**
	 * Suppresses events (blur/focus) that could be inadvertently dispatched due to
	 * high level DOM manipulations (like temporarily removing a text input from the
	 * DOM).
	 */
	var EVENT_SUPPRESSION = {
	  /**
	   * @return {boolean} The enabled status of `ReactBrowserEventEmitter` before
	   * the reconciliation.
	   */
	  initialize: function () {
	    var currentlyEnabled = ReactBrowserEventEmitter.isEnabled();
	    ReactBrowserEventEmitter.setEnabled(false);
	    return currentlyEnabled;
	  },

	  /**
	   * @param {boolean} previouslyEnabled Enabled status of
	   *   `ReactBrowserEventEmitter` before the reconciliation occurred. `close`
	   *   restores the previous value.
	   */
	  close: function (previouslyEnabled) {
	    ReactBrowserEventEmitter.setEnabled(previouslyEnabled);
	  }
	};

	/**
	 * Provides a queue for collecting `componentDidMount` and
	 * `componentDidUpdate` callbacks during the the transaction.
	 */
	var ON_DOM_READY_QUEUEING = {
	  /**
	   * Initializes the internal `onDOMReady` queue.
	   */
	  initialize: function () {
	    this.reactMountReady.reset();
	  },

	  /**
	   * After DOM is flushed, invoke all registered `onDOMReady` callbacks.
	   */
	  close: function () {
	    this.reactMountReady.notifyAll();
	  }
	};

	/**
	 * Executed within the scope of the `Transaction` instance. Consider these as
	 * being member methods, but with an implied ordering while being isolated from
	 * each other.
	 */
	var TRANSACTION_WRAPPERS = [SELECTION_RESTORATION, EVENT_SUPPRESSION, ON_DOM_READY_QUEUEING];

	/**
	 * Currently:
	 * - The order that these are listed in the transaction is critical:
	 * - Suppresses events.
	 * - Restores selection range.
	 *
	 * Future:
	 * - Restore document/overflow scroll positions that were unintentionally
	 *   modified via DOM insertions above the top viewport boundary.
	 * - Implement/integrate with customized constraint based layout system and keep
	 *   track of which dimensions must be remeasured.
	 *
	 * @class ReactReconcileTransaction
	 */
	function ReactReconcileTransaction(forceHTML) {
	  this.reinitializeTransaction();
	  // Only server-side rendering really needs this option (see
	  // `ReactServerRendering`), but server-side uses
	  // `ReactServerRenderingTransaction` instead. This option is here so that it's
	  // accessible and defaults to false when `ReactDOMComponent` and
	  // `ReactTextComponent` checks it in `mountComponent`.`
	  this.renderToStaticMarkup = false;
	  this.reactMountReady = CallbackQueue.getPooled(null);
	  this.useCreateElement = !forceHTML && ReactDOMFeatureFlags.useCreateElement;
	}

	var Mixin = {
	  /**
	   * @see Transaction
	   * @abstract
	   * @final
	   * @return {array<object>} List of operation wrap procedures.
	   *   TODO: convert to array<TransactionWrapper>
	   */
	  getTransactionWrappers: function () {
	    return TRANSACTION_WRAPPERS;
	  },

	  /**
	   * @return {object} The queue to collect `onDOMReady` callbacks with.
	   */
	  getReactMountReady: function () {
	    return this.reactMountReady;
	  },

	  /**
	   * `PooledClass` looks for this, and will invoke this before allowing this
	   * instance to be reused.
	   */
	  destructor: function () {
	    CallbackQueue.release(this.reactMountReady);
	    this.reactMountReady = null;
	  }
	};

	assign(ReactReconcileTransaction.prototype, Transaction.Mixin, Mixin);

	PooledClass.addPoolingTo(ReactReconcileTransaction);

	module.exports = ReactReconcileTransaction;

/***/ },
/* 129 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactInputSelection
	 */

	'use strict';

	var ReactDOMSelection = __webpack_require__(130);

	var containsNode = __webpack_require__(62);
	var focusNode = __webpack_require__(98);
	var getActiveElement = __webpack_require__(132);

	function isInDocument(node) {
	  return containsNode(document.documentElement, node);
	}

	/**
	 * @ReactInputSelection: React input selection module. Based on Selection.js,
	 * but modified to be suitable for react and has a couple of bug fixes (doesn't
	 * assume buttons have range selections allowed).
	 * Input selection module for React.
	 */
	var ReactInputSelection = {

	  hasSelectionCapabilities: function (elem) {
	    var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
	    return nodeName && (nodeName === 'input' && elem.type === 'text' || nodeName === 'textarea' || elem.contentEditable === 'true');
	  },

	  getSelectionInformation: function () {
	    var focusedElem = getActiveElement();
	    return {
	      focusedElem: focusedElem,
	      selectionRange: ReactInputSelection.hasSelectionCapabilities(focusedElem) ? ReactInputSelection.getSelection(focusedElem) : null
	    };
	  },

	  /**
	   * @restoreSelection: If any selection information was potentially lost,
	   * restore it. This is useful when performing operations that could remove dom
	   * nodes and place them back in, resulting in focus being lost.
	   */
	  restoreSelection: function (priorSelectionInformation) {
	    var curFocusedElem = getActiveElement();
	    var priorFocusedElem = priorSelectionInformation.focusedElem;
	    var priorSelectionRange = priorSelectionInformation.selectionRange;
	    if (curFocusedElem !== priorFocusedElem && isInDocument(priorFocusedElem)) {
	      if (ReactInputSelection.hasSelectionCapabilities(priorFocusedElem)) {
	        ReactInputSelection.setSelection(priorFocusedElem, priorSelectionRange);
	      }
	      focusNode(priorFocusedElem);
	    }
	  },

	  /**
	   * @getSelection: Gets the selection bounds of a focused textarea, input or
	   * contentEditable node.
	   * -@input: Look up selection bounds of this input
	   * -@return {start: selectionStart, end: selectionEnd}
	   */
	  getSelection: function (input) {
	    var selection;

	    if ('selectionStart' in input) {
	      // Modern browser with input or textarea.
	      selection = {
	        start: input.selectionStart,
	        end: input.selectionEnd
	      };
	    } else if (document.selection && (input.nodeName && input.nodeName.toLowerCase() === 'input')) {
	      // IE8 input.
	      var range = document.selection.createRange();
	      // There can only be one selection per document in IE, so it must
	      // be in our element.
	      if (range.parentElement() === input) {
	        selection = {
	          start: -range.moveStart('character', -input.value.length),
	          end: -range.moveEnd('character', -input.value.length)
	        };
	      }
	    } else {
	      // Content editable or old IE textarea.
	      selection = ReactDOMSelection.getOffsets(input);
	    }

	    return selection || { start: 0, end: 0 };
	  },

	  /**
	   * @setSelection: Sets the selection bounds of a textarea or input and focuses
	   * the input.
	   * -@input     Set selection bounds of this input or textarea
	   * -@offsets   Object of same form that is returned from get*
	   */
	  setSelection: function (input, offsets) {
	    var start = offsets.start;
	    var end = offsets.end;
	    if (typeof end === 'undefined') {
	      end = start;
	    }

	    if ('selectionStart' in input) {
	      input.selectionStart = start;
	      input.selectionEnd = Math.min(end, input.value.length);
	    } else if (document.selection && (input.nodeName && input.nodeName.toLowerCase() === 'input')) {
	      var range = input.createTextRange();
	      range.collapse(true);
	      range.moveStart('character', start);
	      range.moveEnd('character', end - start);
	      range.select();
	    } else {
	      ReactDOMSelection.setOffsets(input, offsets);
	    }
	  }
	};

	module.exports = ReactInputSelection;

/***/ },
/* 130 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMSelection
	 */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(12);

	var getNodeForCharacterOffset = __webpack_require__(131);
	var getTextContentAccessor = __webpack_require__(78);

	/**
	 * While `isCollapsed` is available on the Selection object and `collapsed`
	 * is available on the Range object, IE11 sometimes gets them wrong.
	 * If the anchor/focus nodes and offsets are the same, the range is collapsed.
	 */
	function isCollapsed(anchorNode, anchorOffset, focusNode, focusOffset) {
	  return anchorNode === focusNode && anchorOffset === focusOffset;
	}

	/**
	 * Get the appropriate anchor and focus node/offset pairs for IE.
	 *
	 * The catch here is that IE's selection API doesn't provide information
	 * about whether the selection is forward or backward, so we have to
	 * behave as though it's always forward.
	 *
	 * IE text differs from modern selection in that it behaves as though
	 * block elements end with a new line. This means character offsets will
	 * differ between the two APIs.
	 *
	 * @param {DOMElement} node
	 * @return {object}
	 */
	function getIEOffsets(node) {
	  var selection = document.selection;
	  var selectedRange = selection.createRange();
	  var selectedLength = selectedRange.text.length;

	  // Duplicate selection so we can move range without breaking user selection.
	  var fromStart = selectedRange.duplicate();
	  fromStart.moveToElementText(node);
	  fromStart.setEndPoint('EndToStart', selectedRange);

	  var startOffset = fromStart.text.length;
	  var endOffset = startOffset + selectedLength;

	  return {
	    start: startOffset,
	    end: endOffset
	  };
	}

	/**
	 * @param {DOMElement} node
	 * @return {?object}
	 */
	function getModernOffsets(node) {
	  var selection = window.getSelection && window.getSelection();

	  if (!selection || selection.rangeCount === 0) {
	    return null;
	  }

	  var anchorNode = selection.anchorNode;
	  var anchorOffset = selection.anchorOffset;
	  var focusNode = selection.focusNode;
	  var focusOffset = selection.focusOffset;

	  var currentRange = selection.getRangeAt(0);

	  // In Firefox, range.startContainer and range.endContainer can be "anonymous
	  // divs", e.g. the up/down buttons on an <input type="number">. Anonymous
	  // divs do not seem to expose properties, triggering a "Permission denied
	  // error" if any of its properties are accessed. The only seemingly possible
	  // way to avoid erroring is to access a property that typically works for
	  // non-anonymous divs and catch any error that may otherwise arise. See
	  // https://bugzilla.mozilla.org/show_bug.cgi?id=208427
	  try {
	    /* eslint-disable no-unused-expressions */
	    currentRange.startContainer.nodeType;
	    currentRange.endContainer.nodeType;
	    /* eslint-enable no-unused-expressions */
	  } catch (e) {
	    return null;
	  }

	  // If the node and offset values are the same, the selection is collapsed.
	  // `Selection.isCollapsed` is available natively, but IE sometimes gets
	  // this value wrong.
	  var isSelectionCollapsed = isCollapsed(selection.anchorNode, selection.anchorOffset, selection.focusNode, selection.focusOffset);

	  var rangeLength = isSelectionCollapsed ? 0 : currentRange.toString().length;

	  var tempRange = currentRange.cloneRange();
	  tempRange.selectNodeContents(node);
	  tempRange.setEnd(currentRange.startContainer, currentRange.startOffset);

	  var isTempRangeCollapsed = isCollapsed(tempRange.startContainer, tempRange.startOffset, tempRange.endContainer, tempRange.endOffset);

	  var start = isTempRangeCollapsed ? 0 : tempRange.toString().length;
	  var end = start + rangeLength;

	  // Detect whether the selection is backward.
	  var detectionRange = document.createRange();
	  detectionRange.setStart(anchorNode, anchorOffset);
	  detectionRange.setEnd(focusNode, focusOffset);
	  var isBackward = detectionRange.collapsed;

	  return {
	    start: isBackward ? end : start,
	    end: isBackward ? start : end
	  };
	}

	/**
	 * @param {DOMElement|DOMTextNode} node
	 * @param {object} offsets
	 */
	function setIEOffsets(node, offsets) {
	  var range = document.selection.createRange().duplicate();
	  var start, end;

	  if (typeof offsets.end === 'undefined') {
	    start = offsets.start;
	    end = start;
	  } else if (offsets.start > offsets.end) {
	    start = offsets.end;
	    end = offsets.start;
	  } else {
	    start = offsets.start;
	    end = offsets.end;
	  }

	  range.moveToElementText(node);
	  range.moveStart('character', start);
	  range.setEndPoint('EndToStart', range);
	  range.moveEnd('character', end - start);
	  range.select();
	}

	/**
	 * In modern non-IE browsers, we can support both forward and backward
	 * selections.
	 *
	 * Note: IE10+ supports the Selection object, but it does not support
	 * the `extend` method, which means that even in modern IE, it's not possible
	 * to programatically create a backward selection. Thus, for all IE
	 * versions, we use the old IE API to create our selections.
	 *
	 * @param {DOMElement|DOMTextNode} node
	 * @param {object} offsets
	 */
	function setModernOffsets(node, offsets) {
	  if (!window.getSelection) {
	    return;
	  }

	  var selection = window.getSelection();
	  var length = node[getTextContentAccessor()].length;
	  var start = Math.min(offsets.start, length);
	  var end = typeof offsets.end === 'undefined' ? start : Math.min(offsets.end, length);

	  // IE 11 uses modern selection, but doesn't support the extend method.
	  // Flip backward selections, so we can set with a single range.
	  if (!selection.extend && start > end) {
	    var temp = end;
	    end = start;
	    start = temp;
	  }

	  var startMarker = getNodeForCharacterOffset(node, start);
	  var endMarker = getNodeForCharacterOffset(node, end);

	  if (startMarker && endMarker) {
	    var range = document.createRange();
	    range.setStart(startMarker.node, startMarker.offset);
	    selection.removeAllRanges();

	    if (start > end) {
	      selection.addRange(range);
	      selection.extend(endMarker.node, endMarker.offset);
	    } else {
	      range.setEnd(endMarker.node, endMarker.offset);
	      selection.addRange(range);
	    }
	  }
	}

	var useIEOffsets = ExecutionEnvironment.canUseDOM && 'selection' in document && !('getSelection' in window);

	var ReactDOMSelection = {
	  /**
	   * @param {DOMElement} node
	   */
	  getOffsets: useIEOffsets ? getIEOffsets : getModernOffsets,

	  /**
	   * @param {DOMElement|DOMTextNode} node
	   * @param {object} offsets
	   */
	  setOffsets: useIEOffsets ? setIEOffsets : setModernOffsets
	};

	module.exports = ReactDOMSelection;

/***/ },
/* 131 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getNodeForCharacterOffset
	 */

	'use strict';

	/**
	 * Given any node return the first leaf node without children.
	 *
	 * @param {DOMElement|DOMTextNode} node
	 * @return {DOMElement|DOMTextNode}
	 */
	function getLeafNode(node) {
	  while (node && node.firstChild) {
	    node = node.firstChild;
	  }
	  return node;
	}

	/**
	 * Get the next sibling within a container. This will walk up the
	 * DOM if a node's siblings have been exhausted.
	 *
	 * @param {DOMElement|DOMTextNode} node
	 * @return {?DOMElement|DOMTextNode}
	 */
	function getSiblingNode(node) {
	  while (node) {
	    if (node.nextSibling) {
	      return node.nextSibling;
	    }
	    node = node.parentNode;
	  }
	}

	/**
	 * Get object describing the nodes which contain characters at offset.
	 *
	 * @param {DOMElement|DOMTextNode} root
	 * @param {number} offset
	 * @return {?object}
	 */
	function getNodeForCharacterOffset(root, offset) {
	  var node = getLeafNode(root);
	  var nodeStart = 0;
	  var nodeEnd = 0;

	  while (node) {
	    if (node.nodeType === 3) {
	      nodeEnd = nodeStart + node.textContent.length;

	      if (nodeStart <= offset && nodeEnd >= offset) {
	        return {
	          node: node,
	          offset: offset - nodeStart
	        };
	      }

	      nodeStart = nodeEnd;
	    }

	    node = getLeafNode(getSiblingNode(node));
	  }
	}

	module.exports = getNodeForCharacterOffset;

/***/ },
/* 132 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getActiveElement
	 * @typechecks
	 */

	/* eslint-disable fb-www/typeof-undefined */

	/**
	 * Same as document.activeElement but wraps in a try-catch block. In IE it is
	 * not safe to call document.activeElement if there is nothing focused.
	 *
	 * The activeElement will be null only if the document or document body is not
	 * yet defined.
	 */
	'use strict';

	function getActiveElement() /*?DOMElement*/{
	  if (typeof document === 'undefined') {
	    return null;
	  }
	  try {
	    return document.activeElement || document.body;
	  } catch (e) {
	    return document.body;
	  }
	}

	module.exports = getActiveElement;

/***/ },
/* 133 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SelectEventPlugin
	 */

	'use strict';

	var EventConstants = __webpack_require__(33);
	var EventPropagators = __webpack_require__(76);
	var ExecutionEnvironment = __webpack_require__(12);
	var ReactInputSelection = __webpack_require__(129);
	var SyntheticEvent = __webpack_require__(80);

	var getActiveElement = __webpack_require__(132);
	var isTextInputElement = __webpack_require__(85);
	var keyOf = __webpack_require__(82);
	var shallowEqual = __webpack_require__(120);

	var topLevelTypes = EventConstants.topLevelTypes;

	var skipSelectionChangeEvent = ExecutionEnvironment.canUseDOM && 'documentMode' in document && document.documentMode <= 11;

	var eventTypes = {
	  select: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onSelect: null }),
	      captured: keyOf({ onSelectCapture: null })
	    },
	    dependencies: [topLevelTypes.topBlur, topLevelTypes.topContextMenu, topLevelTypes.topFocus, topLevelTypes.topKeyDown, topLevelTypes.topMouseDown, topLevelTypes.topMouseUp, topLevelTypes.topSelectionChange]
	  }
	};

	var activeElement = null;
	var activeElementID = null;
	var lastSelection = null;
	var mouseDown = false;

	// Track whether a listener exists for this plugin. If none exist, we do
	// not extract events.
	var hasListener = false;
	var ON_SELECT_KEY = keyOf({ onSelect: null });

	/**
	 * Get an object which is a unique representation of the current selection.
	 *
	 * The return value will not be consistent across nodes or browsers, but
	 * two identical selections on the same node will return identical objects.
	 *
	 * @param {DOMElement} node
	 * @return {object}
	 */
	function getSelection(node) {
	  if ('selectionStart' in node && ReactInputSelection.hasSelectionCapabilities(node)) {
	    return {
	      start: node.selectionStart,
	      end: node.selectionEnd
	    };
	  } else if (window.getSelection) {
	    var selection = window.getSelection();
	    return {
	      anchorNode: selection.anchorNode,
	      anchorOffset: selection.anchorOffset,
	      focusNode: selection.focusNode,
	      focusOffset: selection.focusOffset
	    };
	  } else if (document.selection) {
	    var range = document.selection.createRange();
	    return {
	      parentElement: range.parentElement(),
	      text: range.text,
	      top: range.boundingTop,
	      left: range.boundingLeft
	    };
	  }
	}

	/**
	 * Poll selection to see whether it's changed.
	 *
	 * @param {object} nativeEvent
	 * @return {?SyntheticEvent}
	 */
	function constructSelectEvent(nativeEvent, nativeEventTarget) {
	  // Ensure we have the right element, and that the user is not dragging a
	  // selection (this matches native `select` event behavior). In HTML5, select
	  // fires only on input and textarea thus if there's no focused element we
	  // won't dispatch.
	  if (mouseDown || activeElement == null || activeElement !== getActiveElement()) {
	    return null;
	  }

	  // Only fire when selection has actually changed.
	  var currentSelection = getSelection(activeElement);
	  if (!lastSelection || !shallowEqual(lastSelection, currentSelection)) {
	    lastSelection = currentSelection;

	    var syntheticEvent = SyntheticEvent.getPooled(eventTypes.select, activeElementID, nativeEvent, nativeEventTarget);

	    syntheticEvent.type = 'select';
	    syntheticEvent.target = activeElement;

	    EventPropagators.accumulateTwoPhaseDispatches(syntheticEvent);

	    return syntheticEvent;
	  }

	  return null;
	}

	/**
	 * This plugin creates an `onSelect` event that normalizes select events
	 * across form elements.
	 *
	 * Supported elements are:
	 * - input (see `isTextInputElement`)
	 * - textarea
	 * - contentEditable
	 *
	 * This differs from native browser implementations in the following ways:
	 * - Fires on contentEditable fields as well as inputs.
	 * - Fires for collapsed selection.
	 * - Fires after user input.
	 */
	var SelectEventPlugin = {

	  eventTypes: eventTypes,

	  /**
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {DOMEventTarget} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native browser event.
	   * @return {*} An accumulation of synthetic events.
	   * @see {EventPluginHub.extractEvents}
	   */
	  extractEvents: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
	    if (!hasListener) {
	      return null;
	    }

	    switch (topLevelType) {
	      // Track the input node that has focus.
	      case topLevelTypes.topFocus:
	        if (isTextInputElement(topLevelTarget) || topLevelTarget.contentEditable === 'true') {
	          activeElement = topLevelTarget;
	          activeElementID = topLevelTargetID;
	          lastSelection = null;
	        }
	        break;
	      case topLevelTypes.topBlur:
	        activeElement = null;
	        activeElementID = null;
	        lastSelection = null;
	        break;

	      // Don't fire the event while the user is dragging. This matches the
	      // semantics of the native select event.
	      case topLevelTypes.topMouseDown:
	        mouseDown = true;
	        break;
	      case topLevelTypes.topContextMenu:
	      case topLevelTypes.topMouseUp:
	        mouseDown = false;
	        return constructSelectEvent(nativeEvent, nativeEventTarget);

	      // Chrome and IE fire non-standard event when selection is changed (and
	      // sometimes when it hasn't). IE's event fires out of order with respect
	      // to key and input events on deletion, so we discard it.
	      //
	      // Firefox doesn't support selectionchange, so check selection status
	      // after each key entry. The selection changes after keydown and before
	      // keyup, but we check on keydown as well in the case of holding down a
	      // key, when multiple keydown events are fired but only one keyup is.
	      // This is also our approach for IE handling, for the reason above.
	      case topLevelTypes.topSelectionChange:
	        if (skipSelectionChangeEvent) {
	          break;
	        }
	      // falls through
	      case topLevelTypes.topKeyDown:
	      case topLevelTypes.topKeyUp:
	        return constructSelectEvent(nativeEvent, nativeEventTarget);
	    }

	    return null;
	  },

	  didPutListener: function (id, registrationName, listener) {
	    if (registrationName === ON_SELECT_KEY) {
	      hasListener = true;
	    }
	  }
	};

	module.exports = SelectEventPlugin;

/***/ },
/* 134 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ServerReactRootIndex
	 * @typechecks
	 */

	'use strict';

	/**
	 * Size of the reactRoot ID space. We generate random numbers for React root
	 * IDs and if there's a collision the events and DOM update system will
	 * get confused. In the future we need a way to generate GUIDs but for
	 * now this will work on a smaller scale.
	 */
	var GLOBAL_MOUNT_POINT_MAX = Math.pow(2, 53);

	var ServerReactRootIndex = {
	  createReactRootIndex: function () {
	    return Math.ceil(Math.random() * GLOBAL_MOUNT_POINT_MAX);
	  }
	};

	module.exports = ServerReactRootIndex;

/***/ },
/* 135 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SimpleEventPlugin
	 */

	'use strict';

	var EventConstants = __webpack_require__(33);
	var EventListener = __webpack_require__(122);
	var EventPropagators = __webpack_require__(76);
	var ReactMount = __webpack_require__(31);
	var SyntheticClipboardEvent = __webpack_require__(136);
	var SyntheticEvent = __webpack_require__(80);
	var SyntheticFocusEvent = __webpack_require__(137);
	var SyntheticKeyboardEvent = __webpack_require__(138);
	var SyntheticMouseEvent = __webpack_require__(89);
	var SyntheticDragEvent = __webpack_require__(141);
	var SyntheticTouchEvent = __webpack_require__(142);
	var SyntheticUIEvent = __webpack_require__(90);
	var SyntheticWheelEvent = __webpack_require__(143);

	var emptyFunction = __webpack_require__(18);
	var getEventCharCode = __webpack_require__(139);
	var invariant = __webpack_require__(16);
	var keyOf = __webpack_require__(82);

	var topLevelTypes = EventConstants.topLevelTypes;

	var eventTypes = {
	  abort: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onAbort: true }),
	      captured: keyOf({ onAbortCapture: true })
	    }
	  },
	  blur: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onBlur: true }),
	      captured: keyOf({ onBlurCapture: true })
	    }
	  },
	  canPlay: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onCanPlay: true }),
	      captured: keyOf({ onCanPlayCapture: true })
	    }
	  },
	  canPlayThrough: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onCanPlayThrough: true }),
	      captured: keyOf({ onCanPlayThroughCapture: true })
	    }
	  },
	  click: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onClick: true }),
	      captured: keyOf({ onClickCapture: true })
	    }
	  },
	  contextMenu: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onContextMenu: true }),
	      captured: keyOf({ onContextMenuCapture: true })
	    }
	  },
	  copy: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onCopy: true }),
	      captured: keyOf({ onCopyCapture: true })
	    }
	  },
	  cut: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onCut: true }),
	      captured: keyOf({ onCutCapture: true })
	    }
	  },
	  doubleClick: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onDoubleClick: true }),
	      captured: keyOf({ onDoubleClickCapture: true })
	    }
	  },
	  drag: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onDrag: true }),
	      captured: keyOf({ onDragCapture: true })
	    }
	  },
	  dragEnd: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onDragEnd: true }),
	      captured: keyOf({ onDragEndCapture: true })
	    }
	  },
	  dragEnter: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onDragEnter: true }),
	      captured: keyOf({ onDragEnterCapture: true })
	    }
	  },
	  dragExit: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onDragExit: true }),
	      captured: keyOf({ onDragExitCapture: true })
	    }
	  },
	  dragLeave: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onDragLeave: true }),
	      captured: keyOf({ onDragLeaveCapture: true })
	    }
	  },
	  dragOver: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onDragOver: true }),
	      captured: keyOf({ onDragOverCapture: true })
	    }
	  },
	  dragStart: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onDragStart: true }),
	      captured: keyOf({ onDragStartCapture: true })
	    }
	  },
	  drop: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onDrop: true }),
	      captured: keyOf({ onDropCapture: true })
	    }
	  },
	  durationChange: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onDurationChange: true }),
	      captured: keyOf({ onDurationChangeCapture: true })
	    }
	  },
	  emptied: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onEmptied: true }),
	      captured: keyOf({ onEmptiedCapture: true })
	    }
	  },
	  encrypted: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onEncrypted: true }),
	      captured: keyOf({ onEncryptedCapture: true })
	    }
	  },
	  ended: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onEnded: true }),
	      captured: keyOf({ onEndedCapture: true })
	    }
	  },
	  error: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onError: true }),
	      captured: keyOf({ onErrorCapture: true })
	    }
	  },
	  focus: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onFocus: true }),
	      captured: keyOf({ onFocusCapture: true })
	    }
	  },
	  input: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onInput: true }),
	      captured: keyOf({ onInputCapture: true })
	    }
	  },
	  keyDown: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onKeyDown: true }),
	      captured: keyOf({ onKeyDownCapture: true })
	    }
	  },
	  keyPress: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onKeyPress: true }),
	      captured: keyOf({ onKeyPressCapture: true })
	    }
	  },
	  keyUp: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onKeyUp: true }),
	      captured: keyOf({ onKeyUpCapture: true })
	    }
	  },
	  load: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onLoad: true }),
	      captured: keyOf({ onLoadCapture: true })
	    }
	  },
	  loadedData: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onLoadedData: true }),
	      captured: keyOf({ onLoadedDataCapture: true })
	    }
	  },
	  loadedMetadata: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onLoadedMetadata: true }),
	      captured: keyOf({ onLoadedMetadataCapture: true })
	    }
	  },
	  loadStart: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onLoadStart: true }),
	      captured: keyOf({ onLoadStartCapture: true })
	    }
	  },
	  // Note: We do not allow listening to mouseOver events. Instead, use the
	  // onMouseEnter/onMouseLeave created by `EnterLeaveEventPlugin`.
	  mouseDown: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onMouseDown: true }),
	      captured: keyOf({ onMouseDownCapture: true })
	    }
	  },
	  mouseMove: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onMouseMove: true }),
	      captured: keyOf({ onMouseMoveCapture: true })
	    }
	  },
	  mouseOut: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onMouseOut: true }),
	      captured: keyOf({ onMouseOutCapture: true })
	    }
	  },
	  mouseOver: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onMouseOver: true }),
	      captured: keyOf({ onMouseOverCapture: true })
	    }
	  },
	  mouseUp: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onMouseUp: true }),
	      captured: keyOf({ onMouseUpCapture: true })
	    }
	  },
	  paste: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onPaste: true }),
	      captured: keyOf({ onPasteCapture: true })
	    }
	  },
	  pause: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onPause: true }),
	      captured: keyOf({ onPauseCapture: true })
	    }
	  },
	  play: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onPlay: true }),
	      captured: keyOf({ onPlayCapture: true })
	    }
	  },
	  playing: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onPlaying: true }),
	      captured: keyOf({ onPlayingCapture: true })
	    }
	  },
	  progress: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onProgress: true }),
	      captured: keyOf({ onProgressCapture: true })
	    }
	  },
	  rateChange: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onRateChange: true }),
	      captured: keyOf({ onRateChangeCapture: true })
	    }
	  },
	  reset: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onReset: true }),
	      captured: keyOf({ onResetCapture: true })
	    }
	  },
	  scroll: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onScroll: true }),
	      captured: keyOf({ onScrollCapture: true })
	    }
	  },
	  seeked: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onSeeked: true }),
	      captured: keyOf({ onSeekedCapture: true })
	    }
	  },
	  seeking: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onSeeking: true }),
	      captured: keyOf({ onSeekingCapture: true })
	    }
	  },
	  stalled: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onStalled: true }),
	      captured: keyOf({ onStalledCapture: true })
	    }
	  },
	  submit: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onSubmit: true }),
	      captured: keyOf({ onSubmitCapture: true })
	    }
	  },
	  suspend: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onSuspend: true }),
	      captured: keyOf({ onSuspendCapture: true })
	    }
	  },
	  timeUpdate: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onTimeUpdate: true }),
	      captured: keyOf({ onTimeUpdateCapture: true })
	    }
	  },
	  touchCancel: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onTouchCancel: true }),
	      captured: keyOf({ onTouchCancelCapture: true })
	    }
	  },
	  touchEnd: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onTouchEnd: true }),
	      captured: keyOf({ onTouchEndCapture: true })
	    }
	  },
	  touchMove: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onTouchMove: true }),
	      captured: keyOf({ onTouchMoveCapture: true })
	    }
	  },
	  touchStart: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onTouchStart: true }),
	      captured: keyOf({ onTouchStartCapture: true })
	    }
	  },
	  volumeChange: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onVolumeChange: true }),
	      captured: keyOf({ onVolumeChangeCapture: true })
	    }
	  },
	  waiting: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onWaiting: true }),
	      captured: keyOf({ onWaitingCapture: true })
	    }
	  },
	  wheel: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({ onWheel: true }),
	      captured: keyOf({ onWheelCapture: true })
	    }
	  }
	};

	var topLevelEventsToDispatchConfig = {
	  topAbort: eventTypes.abort,
	  topBlur: eventTypes.blur,
	  topCanPlay: eventTypes.canPlay,
	  topCanPlayThrough: eventTypes.canPlayThrough,
	  topClick: eventTypes.click,
	  topContextMenu: eventTypes.contextMenu,
	  topCopy: eventTypes.copy,
	  topCut: eventTypes.cut,
	  topDoubleClick: eventTypes.doubleClick,
	  topDrag: eventTypes.drag,
	  topDragEnd: eventTypes.dragEnd,
	  topDragEnter: eventTypes.dragEnter,
	  topDragExit: eventTypes.dragExit,
	  topDragLeave: eventTypes.dragLeave,
	  topDragOver: eventTypes.dragOver,
	  topDragStart: eventTypes.dragStart,
	  topDrop: eventTypes.drop,
	  topDurationChange: eventTypes.durationChange,
	  topEmptied: eventTypes.emptied,
	  topEncrypted: eventTypes.encrypted,
	  topEnded: eventTypes.ended,
	  topError: eventTypes.error,
	  topFocus: eventTypes.focus,
	  topInput: eventTypes.input,
	  topKeyDown: eventTypes.keyDown,
	  topKeyPress: eventTypes.keyPress,
	  topKeyUp: eventTypes.keyUp,
	  topLoad: eventTypes.load,
	  topLoadedData: eventTypes.loadedData,
	  topLoadedMetadata: eventTypes.loadedMetadata,
	  topLoadStart: eventTypes.loadStart,
	  topMouseDown: eventTypes.mouseDown,
	  topMouseMove: eventTypes.mouseMove,
	  topMouseOut: eventTypes.mouseOut,
	  topMouseOver: eventTypes.mouseOver,
	  topMouseUp: eventTypes.mouseUp,
	  topPaste: eventTypes.paste,
	  topPause: eventTypes.pause,
	  topPlay: eventTypes.play,
	  topPlaying: eventTypes.playing,
	  topProgress: eventTypes.progress,
	  topRateChange: eventTypes.rateChange,
	  topReset: eventTypes.reset,
	  topScroll: eventTypes.scroll,
	  topSeeked: eventTypes.seeked,
	  topSeeking: eventTypes.seeking,
	  topStalled: eventTypes.stalled,
	  topSubmit: eventTypes.submit,
	  topSuspend: eventTypes.suspend,
	  topTimeUpdate: eventTypes.timeUpdate,
	  topTouchCancel: eventTypes.touchCancel,
	  topTouchEnd: eventTypes.touchEnd,
	  topTouchMove: eventTypes.touchMove,
	  topTouchStart: eventTypes.touchStart,
	  topVolumeChange: eventTypes.volumeChange,
	  topWaiting: eventTypes.waiting,
	  topWheel: eventTypes.wheel
	};

	for (var type in topLevelEventsToDispatchConfig) {
	  topLevelEventsToDispatchConfig[type].dependencies = [type];
	}

	var ON_CLICK_KEY = keyOf({ onClick: null });
	var onClickListeners = {};

	var SimpleEventPlugin = {

	  eventTypes: eventTypes,

	  /**
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {DOMEventTarget} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native browser event.
	   * @return {*} An accumulation of synthetic events.
	   * @see {EventPluginHub.extractEvents}
	   */
	  extractEvents: function (topLevelType, topLevelTarget, topLevelTargetID, nativeEvent, nativeEventTarget) {
	    var dispatchConfig = topLevelEventsToDispatchConfig[topLevelType];
	    if (!dispatchConfig) {
	      return null;
	    }
	    var EventConstructor;
	    switch (topLevelType) {
	      case topLevelTypes.topAbort:
	      case topLevelTypes.topCanPlay:
	      case topLevelTypes.topCanPlayThrough:
	      case topLevelTypes.topDurationChange:
	      case topLevelTypes.topEmptied:
	      case topLevelTypes.topEncrypted:
	      case topLevelTypes.topEnded:
	      case topLevelTypes.topError:
	      case topLevelTypes.topInput:
	      case topLevelTypes.topLoad:
	      case topLevelTypes.topLoadedData:
	      case topLevelTypes.topLoadedMetadata:
	      case topLevelTypes.topLoadStart:
	      case topLevelTypes.topPause:
	      case topLevelTypes.topPlay:
	      case topLevelTypes.topPlaying:
	      case topLevelTypes.topProgress:
	      case topLevelTypes.topRateChange:
	      case topLevelTypes.topReset:
	      case topLevelTypes.topSeeked:
	      case topLevelTypes.topSeeking:
	      case topLevelTypes.topStalled:
	      case topLevelTypes.topSubmit:
	      case topLevelTypes.topSuspend:
	      case topLevelTypes.topTimeUpdate:
	      case topLevelTypes.topVolumeChange:
	      case topLevelTypes.topWaiting:
	        // HTML Events
	        // @see http://www.w3.org/TR/html5/index.html#events-0
	        EventConstructor = SyntheticEvent;
	        break;
	      case topLevelTypes.topKeyPress:
	        // FireFox creates a keypress event for function keys too. This removes
	        // the unwanted keypress events. Enter is however both printable and
	        // non-printable. One would expect Tab to be as well (but it isn't).
	        if (getEventCharCode(nativeEvent) === 0) {
	          return null;
	        }
	      /* falls through */
	      case topLevelTypes.topKeyDown:
	      case topLevelTypes.topKeyUp:
	        EventConstructor = SyntheticKeyboardEvent;
	        break;
	      case topLevelTypes.topBlur:
	      case topLevelTypes.topFocus:
	        EventConstructor = SyntheticFocusEvent;
	        break;
	      case topLevelTypes.topClick:
	        // Firefox creates a click event on right mouse clicks. This removes the
	        // unwanted click events.
	        if (nativeEvent.button === 2) {
	          return null;
	        }
	      /* falls through */
	      case topLevelTypes.topContextMenu:
	      case topLevelTypes.topDoubleClick:
	      case topLevelTypes.topMouseDown:
	      case topLevelTypes.topMouseMove:
	      case topLevelTypes.topMouseOut:
	      case topLevelTypes.topMouseOver:
	      case topLevelTypes.topMouseUp:
	        EventConstructor = SyntheticMouseEvent;
	        break;
	      case topLevelTypes.topDrag:
	      case topLevelTypes.topDragEnd:
	      case topLevelTypes.topDragEnter:
	      case topLevelTypes.topDragExit:
	      case topLevelTypes.topDragLeave:
	      case topLevelTypes.topDragOver:
	      case topLevelTypes.topDragStart:
	      case topLevelTypes.topDrop:
	        EventConstructor = SyntheticDragEvent;
	        break;
	      case topLevelTypes.topTouchCancel:
	      case topLevelTypes.topTouchEnd:
	      case topLevelTypes.topTouchMove:
	      case topLevelTypes.topTouchStart:
	        EventConstructor = SyntheticTouchEvent;
	        break;
	      case topLevelTypes.topScroll:
	        EventConstructor = SyntheticUIEvent;
	        break;
	      case topLevelTypes.topWheel:
	        EventConstructor = SyntheticWheelEvent;
	        break;
	      case topLevelTypes.topCopy:
	      case topLevelTypes.topCut:
	      case topLevelTypes.topPaste:
	        EventConstructor = SyntheticClipboardEvent;
	        break;
	    }
	    !EventConstructor ? process.env.NODE_ENV !== 'production' ? invariant(false, 'SimpleEventPlugin: Unhandled event type, `%s`.', topLevelType) : invariant(false) : undefined;
	    var event = EventConstructor.getPooled(dispatchConfig, topLevelTargetID, nativeEvent, nativeEventTarget);
	    EventPropagators.accumulateTwoPhaseDispatches(event);
	    return event;
	  },

	  didPutListener: function (id, registrationName, listener) {
	    // Mobile Safari does not fire properly bubble click events on
	    // non-interactive elements, which means delegated click listeners do not
	    // fire. The workaround for this bug involves attaching an empty click
	    // listener on the target node.
	    if (registrationName === ON_CLICK_KEY) {
	      var node = ReactMount.getNode(id);
	      if (!onClickListeners[id]) {
	        onClickListeners[id] = EventListener.listen(node, 'click', emptyFunction);
	      }
	    }
	  },

	  willDeleteListener: function (id, registrationName) {
	    if (registrationName === ON_CLICK_KEY) {
	      onClickListeners[id].remove();
	      delete onClickListeners[id];
	    }
	  }

	};

	module.exports = SimpleEventPlugin;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 136 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticClipboardEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticEvent = __webpack_require__(80);

	/**
	 * @interface Event
	 * @see http://www.w3.org/TR/clipboard-apis/
	 */
	var ClipboardEventInterface = {
	  clipboardData: function (event) {
	    return 'clipboardData' in event ? event.clipboardData : window.clipboardData;
	  }
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticClipboardEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
	  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
	}

	SyntheticEvent.augmentClass(SyntheticClipboardEvent, ClipboardEventInterface);

	module.exports = SyntheticClipboardEvent;

/***/ },
/* 137 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticFocusEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticUIEvent = __webpack_require__(90);

	/**
	 * @interface FocusEvent
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var FocusEventInterface = {
	  relatedTarget: null
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticFocusEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
	  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
	}

	SyntheticUIEvent.augmentClass(SyntheticFocusEvent, FocusEventInterface);

	module.exports = SyntheticFocusEvent;

/***/ },
/* 138 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticKeyboardEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticUIEvent = __webpack_require__(90);

	var getEventCharCode = __webpack_require__(139);
	var getEventKey = __webpack_require__(140);
	var getEventModifierState = __webpack_require__(91);

	/**
	 * @interface KeyboardEvent
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var KeyboardEventInterface = {
	  key: getEventKey,
	  location: null,
	  ctrlKey: null,
	  shiftKey: null,
	  altKey: null,
	  metaKey: null,
	  repeat: null,
	  locale: null,
	  getModifierState: getEventModifierState,
	  // Legacy Interface
	  charCode: function (event) {
	    // `charCode` is the result of a KeyPress event and represents the value of
	    // the actual printable character.

	    // KeyPress is deprecated, but its replacement is not yet final and not
	    // implemented in any major browser. Only KeyPress has charCode.
	    if (event.type === 'keypress') {
	      return getEventCharCode(event);
	    }
	    return 0;
	  },
	  keyCode: function (event) {
	    // `keyCode` is the result of a KeyDown/Up event and represents the value of
	    // physical keyboard key.

	    // The actual meaning of the value depends on the users' keyboard layout
	    // which cannot be detected. Assuming that it is a US keyboard layout
	    // provides a surprisingly accurate mapping for US and European users.
	    // Due to this, it is left to the user to implement at this time.
	    if (event.type === 'keydown' || event.type === 'keyup') {
	      return event.keyCode;
	    }
	    return 0;
	  },
	  which: function (event) {
	    // `which` is an alias for either `keyCode` or `charCode` depending on the
	    // type of the event.
	    if (event.type === 'keypress') {
	      return getEventCharCode(event);
	    }
	    if (event.type === 'keydown' || event.type === 'keyup') {
	      return event.keyCode;
	    }
	    return 0;
	  }
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticKeyboardEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
	  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
	}

	SyntheticUIEvent.augmentClass(SyntheticKeyboardEvent, KeyboardEventInterface);

	module.exports = SyntheticKeyboardEvent;

/***/ },
/* 139 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getEventCharCode
	 * @typechecks static-only
	 */

	'use strict';

	/**
	 * `charCode` represents the actual "character code" and is safe to use with
	 * `String.fromCharCode`. As such, only keys that correspond to printable
	 * characters produce a valid `charCode`, the only exception to this is Enter.
	 * The Tab-key is considered non-printable and does not have a `charCode`,
	 * presumably because it does not produce a tab-character in browsers.
	 *
	 * @param {object} nativeEvent Native browser event.
	 * @return {number} Normalized `charCode` property.
	 */
	function getEventCharCode(nativeEvent) {
	  var charCode;
	  var keyCode = nativeEvent.keyCode;

	  if ('charCode' in nativeEvent) {
	    charCode = nativeEvent.charCode;

	    // FF does not set `charCode` for the Enter-key, check against `keyCode`.
	    if (charCode === 0 && keyCode === 13) {
	      charCode = 13;
	    }
	  } else {
	    // IE8 does not implement `charCode`, but `keyCode` has the correct value.
	    charCode = keyCode;
	  }

	  // Some non-printable keys are reported in `charCode`/`keyCode`, discard them.
	  // Must not discard the (non-)printable Enter-key.
	  if (charCode >= 32 || charCode === 13) {
	    return charCode;
	  }

	  return 0;
	}

	module.exports = getEventCharCode;

/***/ },
/* 140 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getEventKey
	 * @typechecks static-only
	 */

	'use strict';

	var getEventCharCode = __webpack_require__(139);

	/**
	 * Normalization of deprecated HTML5 `key` values
	 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
	 */
	var normalizeKey = {
	  'Esc': 'Escape',
	  'Spacebar': ' ',
	  'Left': 'ArrowLeft',
	  'Up': 'ArrowUp',
	  'Right': 'ArrowRight',
	  'Down': 'ArrowDown',
	  'Del': 'Delete',
	  'Win': 'OS',
	  'Menu': 'ContextMenu',
	  'Apps': 'ContextMenu',
	  'Scroll': 'ScrollLock',
	  'MozPrintableKey': 'Unidentified'
	};

	/**
	 * Translation from legacy `keyCode` to HTML5 `key`
	 * Only special keys supported, all others depend on keyboard layout or browser
	 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
	 */
	var translateToKey = {
	  8: 'Backspace',
	  9: 'Tab',
	  12: 'Clear',
	  13: 'Enter',
	  16: 'Shift',
	  17: 'Control',
	  18: 'Alt',
	  19: 'Pause',
	  20: 'CapsLock',
	  27: 'Escape',
	  32: ' ',
	  33: 'PageUp',
	  34: 'PageDown',
	  35: 'End',
	  36: 'Home',
	  37: 'ArrowLeft',
	  38: 'ArrowUp',
	  39: 'ArrowRight',
	  40: 'ArrowDown',
	  45: 'Insert',
	  46: 'Delete',
	  112: 'F1', 113: 'F2', 114: 'F3', 115: 'F4', 116: 'F5', 117: 'F6',
	  118: 'F7', 119: 'F8', 120: 'F9', 121: 'F10', 122: 'F11', 123: 'F12',
	  144: 'NumLock',
	  145: 'ScrollLock',
	  224: 'Meta'
	};

	/**
	 * @param {object} nativeEvent Native browser event.
	 * @return {string} Normalized `key` property.
	 */
	function getEventKey(nativeEvent) {
	  if (nativeEvent.key) {
	    // Normalize inconsistent values reported by browsers due to
	    // implementations of a working draft specification.

	    // FireFox implements `key` but returns `MozPrintableKey` for all
	    // printable characters (normalized to `Unidentified`), ignore it.
	    var key = normalizeKey[nativeEvent.key] || nativeEvent.key;
	    if (key !== 'Unidentified') {
	      return key;
	    }
	  }

	  // Browser does not implement `key`, polyfill as much of it as we can.
	  if (nativeEvent.type === 'keypress') {
	    var charCode = getEventCharCode(nativeEvent);

	    // The enter-key is technically both printable and non-printable and can
	    // thus be captured by `keypress`, no other non-printable key should.
	    return charCode === 13 ? 'Enter' : String.fromCharCode(charCode);
	  }
	  if (nativeEvent.type === 'keydown' || nativeEvent.type === 'keyup') {
	    // While user keyboard layout determines the actual meaning of each
	    // `keyCode` value, almost all function keys have a universal value.
	    return translateToKey[nativeEvent.keyCode] || 'Unidentified';
	  }
	  return '';
	}

	module.exports = getEventKey;

/***/ },
/* 141 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticDragEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticMouseEvent = __webpack_require__(89);

	/**
	 * @interface DragEvent
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var DragEventInterface = {
	  dataTransfer: null
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticDragEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
	  SyntheticMouseEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
	}

	SyntheticMouseEvent.augmentClass(SyntheticDragEvent, DragEventInterface);

	module.exports = SyntheticDragEvent;

/***/ },
/* 142 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticTouchEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticUIEvent = __webpack_require__(90);

	var getEventModifierState = __webpack_require__(91);

	/**
	 * @interface TouchEvent
	 * @see http://www.w3.org/TR/touch-events/
	 */
	var TouchEventInterface = {
	  touches: null,
	  targetTouches: null,
	  changedTouches: null,
	  altKey: null,
	  metaKey: null,
	  ctrlKey: null,
	  shiftKey: null,
	  getModifierState: getEventModifierState
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticTouchEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
	  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
	}

	SyntheticUIEvent.augmentClass(SyntheticTouchEvent, TouchEventInterface);

	module.exports = SyntheticTouchEvent;

/***/ },
/* 143 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticWheelEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticMouseEvent = __webpack_require__(89);

	/**
	 * @interface WheelEvent
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var WheelEventInterface = {
	  deltaX: function (event) {
	    return 'deltaX' in event ? event.deltaX :
	    // Fallback to `wheelDeltaX` for Webkit and normalize (right is positive).
	    'wheelDeltaX' in event ? -event.wheelDeltaX : 0;
	  },
	  deltaY: function (event) {
	    return 'deltaY' in event ? event.deltaY :
	    // Fallback to `wheelDeltaY` for Webkit and normalize (down is positive).
	    'wheelDeltaY' in event ? -event.wheelDeltaY :
	    // Fallback to `wheelDelta` for IE<9 and normalize (down is positive).
	    'wheelDelta' in event ? -event.wheelDelta : 0;
	  },
	  deltaZ: null,

	  // Browsers without "deltaMode" is reporting in raw wheel delta where one
	  // notch on the scroll is always +/- 120, roughly equivalent to pixels.
	  // A good approximation of DOM_DELTA_LINE (1) is 5% of viewport size or
	  // ~40 pixels, for DOM_DELTA_SCREEN (2) it is 87.5% of viewport size.
	  deltaMode: null
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticMouseEvent}
	 */
	function SyntheticWheelEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
	  SyntheticMouseEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
	}

	SyntheticMouseEvent.augmentClass(SyntheticWheelEvent, WheelEventInterface);

	module.exports = SyntheticWheelEvent;

/***/ },
/* 144 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SVGDOMPropertyConfig
	 */

	'use strict';

	var DOMProperty = __webpack_require__(26);

	var MUST_USE_ATTRIBUTE = DOMProperty.injection.MUST_USE_ATTRIBUTE;

	var NS = {
	  xlink: 'http://www.w3.org/1999/xlink',
	  xml: 'http://www.w3.org/XML/1998/namespace'
	};

	var SVGDOMPropertyConfig = {
	  Properties: {
	    clipPath: MUST_USE_ATTRIBUTE,
	    cx: MUST_USE_ATTRIBUTE,
	    cy: MUST_USE_ATTRIBUTE,
	    d: MUST_USE_ATTRIBUTE,
	    dx: MUST_USE_ATTRIBUTE,
	    dy: MUST_USE_ATTRIBUTE,
	    fill: MUST_USE_ATTRIBUTE,
	    fillOpacity: MUST_USE_ATTRIBUTE,
	    fontFamily: MUST_USE_ATTRIBUTE,
	    fontSize: MUST_USE_ATTRIBUTE,
	    fx: MUST_USE_ATTRIBUTE,
	    fy: MUST_USE_ATTRIBUTE,
	    gradientTransform: MUST_USE_ATTRIBUTE,
	    gradientUnits: MUST_USE_ATTRIBUTE,
	    markerEnd: MUST_USE_ATTRIBUTE,
	    markerMid: MUST_USE_ATTRIBUTE,
	    markerStart: MUST_USE_ATTRIBUTE,
	    offset: MUST_USE_ATTRIBUTE,
	    opacity: MUST_USE_ATTRIBUTE,
	    patternContentUnits: MUST_USE_ATTRIBUTE,
	    patternUnits: MUST_USE_ATTRIBUTE,
	    points: MUST_USE_ATTRIBUTE,
	    preserveAspectRatio: MUST_USE_ATTRIBUTE,
	    r: MUST_USE_ATTRIBUTE,
	    rx: MUST_USE_ATTRIBUTE,
	    ry: MUST_USE_ATTRIBUTE,
	    spreadMethod: MUST_USE_ATTRIBUTE,
	    stopColor: MUST_USE_ATTRIBUTE,
	    stopOpacity: MUST_USE_ATTRIBUTE,
	    stroke: MUST_USE_ATTRIBUTE,
	    strokeDasharray: MUST_USE_ATTRIBUTE,
	    strokeLinecap: MUST_USE_ATTRIBUTE,
	    strokeOpacity: MUST_USE_ATTRIBUTE,
	    strokeWidth: MUST_USE_ATTRIBUTE,
	    textAnchor: MUST_USE_ATTRIBUTE,
	    transform: MUST_USE_ATTRIBUTE,
	    version: MUST_USE_ATTRIBUTE,
	    viewBox: MUST_USE_ATTRIBUTE,
	    x1: MUST_USE_ATTRIBUTE,
	    x2: MUST_USE_ATTRIBUTE,
	    x: MUST_USE_ATTRIBUTE,
	    xlinkActuate: MUST_USE_ATTRIBUTE,
	    xlinkArcrole: MUST_USE_ATTRIBUTE,
	    xlinkHref: MUST_USE_ATTRIBUTE,
	    xlinkRole: MUST_USE_ATTRIBUTE,
	    xlinkShow: MUST_USE_ATTRIBUTE,
	    xlinkTitle: MUST_USE_ATTRIBUTE,
	    xlinkType: MUST_USE_ATTRIBUTE,
	    xmlBase: MUST_USE_ATTRIBUTE,
	    xmlLang: MUST_USE_ATTRIBUTE,
	    xmlSpace: MUST_USE_ATTRIBUTE,
	    y1: MUST_USE_ATTRIBUTE,
	    y2: MUST_USE_ATTRIBUTE,
	    y: MUST_USE_ATTRIBUTE
	  },
	  DOMAttributeNamespaces: {
	    xlinkActuate: NS.xlink,
	    xlinkArcrole: NS.xlink,
	    xlinkHref: NS.xlink,
	    xlinkRole: NS.xlink,
	    xlinkShow: NS.xlink,
	    xlinkTitle: NS.xlink,
	    xlinkType: NS.xlink,
	    xmlBase: NS.xml,
	    xmlLang: NS.xml,
	    xmlSpace: NS.xml
	  },
	  DOMAttributeNames: {
	    clipPath: 'clip-path',
	    fillOpacity: 'fill-opacity',
	    fontFamily: 'font-family',
	    fontSize: 'font-size',
	    gradientTransform: 'gradientTransform',
	    gradientUnits: 'gradientUnits',
	    markerEnd: 'marker-end',
	    markerMid: 'marker-mid',
	    markerStart: 'marker-start',
	    patternContentUnits: 'patternContentUnits',
	    patternUnits: 'patternUnits',
	    preserveAspectRatio: 'preserveAspectRatio',
	    spreadMethod: 'spreadMethod',
	    stopColor: 'stop-color',
	    stopOpacity: 'stop-opacity',
	    strokeDasharray: 'stroke-dasharray',
	    strokeLinecap: 'stroke-linecap',
	    strokeOpacity: 'stroke-opacity',
	    strokeWidth: 'stroke-width',
	    textAnchor: 'text-anchor',
	    viewBox: 'viewBox',
	    xlinkActuate: 'xlink:actuate',
	    xlinkArcrole: 'xlink:arcrole',
	    xlinkHref: 'xlink:href',
	    xlinkRole: 'xlink:role',
	    xlinkShow: 'xlink:show',
	    xlinkTitle: 'xlink:title',
	    xlinkType: 'xlink:type',
	    xmlBase: 'xml:base',
	    xmlLang: 'xml:lang',
	    xmlSpace: 'xml:space'
	  }
	};

	module.exports = SVGDOMPropertyConfig;

/***/ },
/* 145 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDefaultPerf
	 * @typechecks static-only
	 */

	'use strict';

	var DOMProperty = __webpack_require__(26);
	var ReactDefaultPerfAnalysis = __webpack_require__(146);
	var ReactMount = __webpack_require__(31);
	var ReactPerf = __webpack_require__(21);

	var performanceNow = __webpack_require__(147);

	function roundFloat(val) {
	  return Math.floor(val * 100) / 100;
	}

	function addValue(obj, key, val) {
	  obj[key] = (obj[key] || 0) + val;
	}

	var ReactDefaultPerf = {
	  _allMeasurements: [], // last item in the list is the current one
	  _mountStack: [0],
	  _injected: false,

	  start: function () {
	    if (!ReactDefaultPerf._injected) {
	      ReactPerf.injection.injectMeasure(ReactDefaultPerf.measure);
	    }

	    ReactDefaultPerf._allMeasurements.length = 0;
	    ReactPerf.enableMeasure = true;
	  },

	  stop: function () {
	    ReactPerf.enableMeasure = false;
	  },

	  getLastMeasurements: function () {
	    return ReactDefaultPerf._allMeasurements;
	  },

	  printExclusive: function (measurements) {
	    measurements = measurements || ReactDefaultPerf._allMeasurements;
	    var summary = ReactDefaultPerfAnalysis.getExclusiveSummary(measurements);
	    console.table(summary.map(function (item) {
	      return {
	        'Component class name': item.componentName,
	        'Total inclusive time (ms)': roundFloat(item.inclusive),
	        'Exclusive mount time (ms)': roundFloat(item.exclusive),
	        'Exclusive render time (ms)': roundFloat(item.render),
	        'Mount time per instance (ms)': roundFloat(item.exclusive / item.count),
	        'Render time per instance (ms)': roundFloat(item.render / item.count),
	        'Instances': item.count
	      };
	    }));
	    // TODO: ReactDefaultPerfAnalysis.getTotalTime() does not return the correct
	    // number.
	  },

	  printInclusive: function (measurements) {
	    measurements = measurements || ReactDefaultPerf._allMeasurements;
	    var summary = ReactDefaultPerfAnalysis.getInclusiveSummary(measurements);
	    console.table(summary.map(function (item) {
	      return {
	        'Owner > component': item.componentName,
	        'Inclusive time (ms)': roundFloat(item.time),
	        'Instances': item.count
	      };
	    }));
	    console.log('Total time:', ReactDefaultPerfAnalysis.getTotalTime(measurements).toFixed(2) + ' ms');
	  },

	  getMeasurementsSummaryMap: function (measurements) {
	    var summary = ReactDefaultPerfAnalysis.getInclusiveSummary(measurements, true);
	    return summary.map(function (item) {
	      return {
	        'Owner > component': item.componentName,
	        'Wasted time (ms)': item.time,
	        'Instances': item.count
	      };
	    });
	  },

	  printWasted: function (measurements) {
	    measurements = measurements || ReactDefaultPerf._allMeasurements;
	    console.table(ReactDefaultPerf.getMeasurementsSummaryMap(measurements));
	    console.log('Total time:', ReactDefaultPerfAnalysis.getTotalTime(measurements).toFixed(2) + ' ms');
	  },

	  printDOM: function (measurements) {
	    measurements = measurements || ReactDefaultPerf._allMeasurements;
	    var summary = ReactDefaultPerfAnalysis.getDOMSummary(measurements);
	    console.table(summary.map(function (item) {
	      var result = {};
	      result[DOMProperty.ID_ATTRIBUTE_NAME] = item.id;
	      result.type = item.type;
	      result.args = JSON.stringify(item.args);
	      return result;
	    }));
	    console.log('Total time:', ReactDefaultPerfAnalysis.getTotalTime(measurements).toFixed(2) + ' ms');
	  },

	  _recordWrite: function (id, fnName, totalTime, args) {
	    // TODO: totalTime isn't that useful since it doesn't count paints/reflows
	    var writes = ReactDefaultPerf._allMeasurements[ReactDefaultPerf._allMeasurements.length - 1].writes;
	    writes[id] = writes[id] || [];
	    writes[id].push({
	      type: fnName,
	      time: totalTime,
	      args: args
	    });
	  },

	  measure: function (moduleName, fnName, func) {
	    return function () {
	      for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
	        args[_key] = arguments[_key];
	      }

	      var totalTime;
	      var rv;
	      var start;

	      if (fnName === '_renderNewRootComponent' || fnName === 'flushBatchedUpdates') {
	        // A "measurement" is a set of metrics recorded for each flush. We want
	        // to group the metrics for a given flush together so we can look at the
	        // components that rendered and the DOM operations that actually
	        // happened to determine the amount of "wasted work" performed.
	        ReactDefaultPerf._allMeasurements.push({
	          exclusive: {},
	          inclusive: {},
	          render: {},
	          counts: {},
	          writes: {},
	          displayNames: {},
	          totalTime: 0,
	          created: {}
	        });
	        start = performanceNow();
	        rv = func.apply(this, args);
	        ReactDefaultPerf._allMeasurements[ReactDefaultPerf._allMeasurements.length - 1].totalTime = performanceNow() - start;
	        return rv;
	      } else if (fnName === '_mountImageIntoNode' || moduleName === 'ReactBrowserEventEmitter' || moduleName === 'ReactDOMIDOperations' || moduleName === 'CSSPropertyOperations' || moduleName === 'DOMChildrenOperations' || moduleName === 'DOMPropertyOperations') {
	        start = performanceNow();
	        rv = func.apply(this, args);
	        totalTime = performanceNow() - start;

	        if (fnName === '_mountImageIntoNode') {
	          var mountID = ReactMount.getID(args[1]);
	          ReactDefaultPerf._recordWrite(mountID, fnName, totalTime, args[0]);
	        } else if (fnName === 'dangerouslyProcessChildrenUpdates') {
	          // special format
	          args[0].forEach(function (update) {
	            var writeArgs = {};
	            if (update.fromIndex !== null) {
	              writeArgs.fromIndex = update.fromIndex;
	            }
	            if (update.toIndex !== null) {
	              writeArgs.toIndex = update.toIndex;
	            }
	            if (update.textContent !== null) {
	              writeArgs.textContent = update.textContent;
	            }
	            if (update.markupIndex !== null) {
	              writeArgs.markup = args[1][update.markupIndex];
	            }
	            ReactDefaultPerf._recordWrite(update.parentID, update.type, totalTime, writeArgs);
	          });
	        } else {
	          // basic format
	          var id = args[0];
	          if (typeof id === 'object') {
	            id = ReactMount.getID(args[0]);
	          }
	          ReactDefaultPerf._recordWrite(id, fnName, totalTime, Array.prototype.slice.call(args, 1));
	        }
	        return rv;
	      } else if (moduleName === 'ReactCompositeComponent' && (fnName === 'mountComponent' || fnName === 'updateComponent' || // TODO: receiveComponent()?
	      fnName === '_renderValidatedComponent')) {

	        if (this._currentElement.type === ReactMount.TopLevelWrapper) {
	          return func.apply(this, args);
	        }

	        var rootNodeID = fnName === 'mountComponent' ? args[0] : this._rootNodeID;
	        var isRender = fnName === '_renderValidatedComponent';
	        var isMount = fnName === 'mountComponent';

	        var mountStack = ReactDefaultPerf._mountStack;
	        var entry = ReactDefaultPerf._allMeasurements[ReactDefaultPerf._allMeasurements.length - 1];

	        if (isRender) {
	          addValue(entry.counts, rootNodeID, 1);
	        } else if (isMount) {
	          entry.created[rootNodeID] = true;
	          mountStack.push(0);
	        }

	        start = performanceNow();
	        rv = func.apply(this, args);
	        totalTime = performanceNow() - start;

	        if (isRender) {
	          addValue(entry.render, rootNodeID, totalTime);
	        } else if (isMount) {
	          var subMountTime = mountStack.pop();
	          mountStack[mountStack.length - 1] += totalTime;
	          addValue(entry.exclusive, rootNodeID, totalTime - subMountTime);
	          addValue(entry.inclusive, rootNodeID, totalTime);
	        } else {
	          addValue(entry.inclusive, rootNodeID, totalTime);
	        }

	        entry.displayNames[rootNodeID] = {
	          current: this.getName(),
	          owner: this._currentElement._owner ? this._currentElement._owner.getName() : '<root>'
	        };

	        return rv;
	      } else {
	        return func.apply(this, args);
	      }
	    };
	  }
	};

	module.exports = ReactDefaultPerf;

/***/ },
/* 146 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDefaultPerfAnalysis
	 */

	'use strict';

	var assign = __webpack_require__(42);

	// Don't try to save users less than 1.2ms (a number I made up)
	var DONT_CARE_THRESHOLD = 1.2;
	var DOM_OPERATION_TYPES = {
	  '_mountImageIntoNode': 'set innerHTML',
	  INSERT_MARKUP: 'set innerHTML',
	  MOVE_EXISTING: 'move',
	  REMOVE_NODE: 'remove',
	  SET_MARKUP: 'set innerHTML',
	  TEXT_CONTENT: 'set textContent',
	  'setValueForProperty': 'update attribute',
	  'setValueForAttribute': 'update attribute',
	  'deleteValueForProperty': 'remove attribute',
	  'setValueForStyles': 'update styles',
	  'replaceNodeWithMarkup': 'replace',
	  'updateTextContent': 'set textContent'
	};

	function getTotalTime(measurements) {
	  // TODO: return number of DOM ops? could be misleading.
	  // TODO: measure dropped frames after reconcile?
	  // TODO: log total time of each reconcile and the top-level component
	  // class that triggered it.
	  var totalTime = 0;
	  for (var i = 0; i < measurements.length; i++) {
	    var measurement = measurements[i];
	    totalTime += measurement.totalTime;
	  }
	  return totalTime;
	}

	function getDOMSummary(measurements) {
	  var items = [];
	  measurements.forEach(function (measurement) {
	    Object.keys(measurement.writes).forEach(function (id) {
	      measurement.writes[id].forEach(function (write) {
	        items.push({
	          id: id,
	          type: DOM_OPERATION_TYPES[write.type] || write.type,
	          args: write.args
	        });
	      });
	    });
	  });
	  return items;
	}

	function getExclusiveSummary(measurements) {
	  var candidates = {};
	  var displayName;

	  for (var i = 0; i < measurements.length; i++) {
	    var measurement = measurements[i];
	    var allIDs = assign({}, measurement.exclusive, measurement.inclusive);

	    for (var id in allIDs) {
	      displayName = measurement.displayNames[id].current;

	      candidates[displayName] = candidates[displayName] || {
	        componentName: displayName,
	        inclusive: 0,
	        exclusive: 0,
	        render: 0,
	        count: 0
	      };
	      if (measurement.render[id]) {
	        candidates[displayName].render += measurement.render[id];
	      }
	      if (measurement.exclusive[id]) {
	        candidates[displayName].exclusive += measurement.exclusive[id];
	      }
	      if (measurement.inclusive[id]) {
	        candidates[displayName].inclusive += measurement.inclusive[id];
	      }
	      if (measurement.counts[id]) {
	        candidates[displayName].count += measurement.counts[id];
	      }
	    }
	  }

	  // Now make a sorted array with the results.
	  var arr = [];
	  for (displayName in candidates) {
	    if (candidates[displayName].exclusive >= DONT_CARE_THRESHOLD) {
	      arr.push(candidates[displayName]);
	    }
	  }

	  arr.sort(function (a, b) {
	    return b.exclusive - a.exclusive;
	  });

	  return arr;
	}

	function getInclusiveSummary(measurements, onlyClean) {
	  var candidates = {};
	  var inclusiveKey;

	  for (var i = 0; i < measurements.length; i++) {
	    var measurement = measurements[i];
	    var allIDs = assign({}, measurement.exclusive, measurement.inclusive);
	    var cleanComponents;

	    if (onlyClean) {
	      cleanComponents = getUnchangedComponents(measurement);
	    }

	    for (var id in allIDs) {
	      if (onlyClean && !cleanComponents[id]) {
	        continue;
	      }

	      var displayName = measurement.displayNames[id];

	      // Inclusive time is not useful for many components without knowing where
	      // they are instantiated. So we aggregate inclusive time with both the
	      // owner and current displayName as the key.
	      inclusiveKey = displayName.owner + ' > ' + displayName.current;

	      candidates[inclusiveKey] = candidates[inclusiveKey] || {
	        componentName: inclusiveKey,
	        time: 0,
	        count: 0
	      };

	      if (measurement.inclusive[id]) {
	        candidates[inclusiveKey].time += measurement.inclusive[id];
	      }
	      if (measurement.counts[id]) {
	        candidates[inclusiveKey].count += measurement.counts[id];
	      }
	    }
	  }

	  // Now make a sorted array with the results.
	  var arr = [];
	  for (inclusiveKey in candidates) {
	    if (candidates[inclusiveKey].time >= DONT_CARE_THRESHOLD) {
	      arr.push(candidates[inclusiveKey]);
	    }
	  }

	  arr.sort(function (a, b) {
	    return b.time - a.time;
	  });

	  return arr;
	}

	function getUnchangedComponents(measurement) {
	  // For a given reconcile, look at which components did not actually
	  // render anything to the DOM and return a mapping of their ID to
	  // the amount of time it took to render the entire subtree.
	  var cleanComponents = {};
	  var dirtyLeafIDs = Object.keys(measurement.writes);
	  var allIDs = assign({}, measurement.exclusive, measurement.inclusive);

	  for (var id in allIDs) {
	    var isDirty = false;
	    // For each component that rendered, see if a component that triggered
	    // a DOM op is in its subtree.
	    for (var i = 0; i < dirtyLeafIDs.length; i++) {
	      if (dirtyLeafIDs[i].indexOf(id) === 0) {
	        isDirty = true;
	        break;
	      }
	    }
	    // check if component newly created
	    if (measurement.created[id]) {
	      isDirty = true;
	    }
	    if (!isDirty && measurement.counts[id] > 0) {
	      cleanComponents[id] = true;
	    }
	  }
	  return cleanComponents;
	}

	var ReactDefaultPerfAnalysis = {
	  getExclusiveSummary: getExclusiveSummary,
	  getInclusiveSummary: getInclusiveSummary,
	  getDOMSummary: getDOMSummary,
	  getTotalTime: getTotalTime
	};

	module.exports = ReactDefaultPerfAnalysis;

/***/ },
/* 147 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule performanceNow
	 * @typechecks
	 */

	'use strict';

	var performance = __webpack_require__(148);

	var performanceNow;

	/**
	 * Detect if we can use `window.performance.now()` and gracefully fallback to
	 * `Date.now()` if it doesn't exist. We need to support Firefox < 15 for now
	 * because of Facebook's testing infrastructure.
	 */
	if (performance.now) {
	  performanceNow = function () {
	    return performance.now();
	  };
	} else {
	  performanceNow = function () {
	    return Date.now();
	  };
	}

	module.exports = performanceNow;

/***/ },
/* 148 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule performance
	 * @typechecks
	 */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(12);

	var performance;

	if (ExecutionEnvironment.canUseDOM) {
	  performance = window.performance || window.msPerformance || window.webkitPerformance;
	}

	module.exports = performance || {};

/***/ },
/* 149 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactVersion
	 */

	'use strict';

	module.exports = '0.14.8';

/***/ },
/* 150 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	* @providesModule renderSubtreeIntoContainer
	*/

	'use strict';

	var ReactMount = __webpack_require__(31);

	module.exports = ReactMount.renderSubtreeIntoContainer;

/***/ },
/* 151 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMServer
	 */

	'use strict';

	var ReactDefaultInjection = __webpack_require__(74);
	var ReactServerRendering = __webpack_require__(152);
	var ReactVersion = __webpack_require__(149);

	ReactDefaultInjection.inject();

	var ReactDOMServer = {
	  renderToString: ReactServerRendering.renderToString,
	  renderToStaticMarkup: ReactServerRendering.renderToStaticMarkup,
	  version: ReactVersion
	};

	module.exports = ReactDOMServer;

/***/ },
/* 152 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @typechecks static-only
	 * @providesModule ReactServerRendering
	 */
	'use strict';

	var ReactDefaultBatchingStrategy = __webpack_require__(95);
	var ReactElement = __webpack_require__(45);
	var ReactInstanceHandles = __webpack_require__(48);
	var ReactMarkupChecksum = __webpack_require__(51);
	var ReactServerBatchingStrategy = __webpack_require__(153);
	var ReactServerRenderingTransaction = __webpack_require__(154);
	var ReactUpdates = __webpack_require__(57);

	var emptyObject = __webpack_require__(61);
	var instantiateReactComponent = __webpack_require__(65);
	var invariant = __webpack_require__(16);

	/**
	 * @param {ReactElement} element
	 * @return {string} the HTML markup
	 */
	function renderToString(element) {
	  !ReactElement.isValidElement(element) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'renderToString(): You must pass a valid ReactElement.') : invariant(false) : undefined;

	  var transaction;
	  try {
	    ReactUpdates.injection.injectBatchingStrategy(ReactServerBatchingStrategy);

	    var id = ReactInstanceHandles.createReactRootID();
	    transaction = ReactServerRenderingTransaction.getPooled(false);

	    return transaction.perform(function () {
	      var componentInstance = instantiateReactComponent(element, null);
	      var markup = componentInstance.mountComponent(id, transaction, emptyObject);
	      return ReactMarkupChecksum.addChecksumToMarkup(markup);
	    }, null);
	  } finally {
	    ReactServerRenderingTransaction.release(transaction);
	    // Revert to the DOM batching strategy since these two renderers
	    // currently share these stateful modules.
	    ReactUpdates.injection.injectBatchingStrategy(ReactDefaultBatchingStrategy);
	  }
	}

	/**
	 * @param {ReactElement} element
	 * @return {string} the HTML markup, without the extra React ID and checksum
	 * (for generating static pages)
	 */
	function renderToStaticMarkup(element) {
	  !ReactElement.isValidElement(element) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'renderToStaticMarkup(): You must pass a valid ReactElement.') : invariant(false) : undefined;

	  var transaction;
	  try {
	    ReactUpdates.injection.injectBatchingStrategy(ReactServerBatchingStrategy);

	    var id = ReactInstanceHandles.createReactRootID();
	    transaction = ReactServerRenderingTransaction.getPooled(true);

	    return transaction.perform(function () {
	      var componentInstance = instantiateReactComponent(element, null);
	      return componentInstance.mountComponent(id, transaction, emptyObject);
	    }, null);
	  } finally {
	    ReactServerRenderingTransaction.release(transaction);
	    // Revert to the DOM batching strategy since these two renderers
	    // currently share these stateful modules.
	    ReactUpdates.injection.injectBatchingStrategy(ReactDefaultBatchingStrategy);
	  }
	}

	module.exports = {
	  renderToString: renderToString,
	  renderToStaticMarkup: renderToStaticMarkup
	};
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 153 */
/***/ function(module, exports) {

	/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactServerBatchingStrategy
	 * @typechecks
	 */

	'use strict';

	var ReactServerBatchingStrategy = {
	  isBatchingUpdates: false,
	  batchedUpdates: function (callback) {
	    // Don't do anything here. During the server rendering we don't want to
	    // schedule any updates. We will simply ignore them.
	  }
	};

	module.exports = ReactServerBatchingStrategy;

/***/ },
/* 154 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactServerRenderingTransaction
	 * @typechecks
	 */

	'use strict';

	var PooledClass = __webpack_require__(59);
	var CallbackQueue = __webpack_require__(58);
	var Transaction = __webpack_require__(60);

	var assign = __webpack_require__(42);
	var emptyFunction = __webpack_require__(18);

	/**
	 * Provides a `CallbackQueue` queue for collecting `onDOMReady` callbacks
	 * during the performing of the transaction.
	 */
	var ON_DOM_READY_QUEUEING = {
	  /**
	   * Initializes the internal `onDOMReady` queue.
	   */
	  initialize: function () {
	    this.reactMountReady.reset();
	  },

	  close: emptyFunction
	};

	/**
	 * Executed within the scope of the `Transaction` instance. Consider these as
	 * being member methods, but with an implied ordering while being isolated from
	 * each other.
	 */
	var TRANSACTION_WRAPPERS = [ON_DOM_READY_QUEUEING];

	/**
	 * @class ReactServerRenderingTransaction
	 * @param {boolean} renderToStaticMarkup
	 */
	function ReactServerRenderingTransaction(renderToStaticMarkup) {
	  this.reinitializeTransaction();
	  this.renderToStaticMarkup = renderToStaticMarkup;
	  this.reactMountReady = CallbackQueue.getPooled(null);
	  this.useCreateElement = false;
	}

	var Mixin = {
	  /**
	   * @see Transaction
	   * @abstract
	   * @final
	   * @return {array} Empty list of operation wrap procedures.
	   */
	  getTransactionWrappers: function () {
	    return TRANSACTION_WRAPPERS;
	  },

	  /**
	   * @return {object} The queue to collect `onDOMReady` callbacks with.
	   */
	  getReactMountReady: function () {
	    return this.reactMountReady;
	  },

	  /**
	   * `PooledClass` looks for this, and will invoke this before allowing this
	   * instance to be reused.
	   */
	  destructor: function () {
	    CallbackQueue.release(this.reactMountReady);
	    this.reactMountReady = null;
	  }
	};

	assign(ReactServerRenderingTransaction.prototype, Transaction.Mixin, Mixin);

	PooledClass.addPoolingTo(ReactServerRenderingTransaction);

	module.exports = ReactServerRenderingTransaction;

/***/ },
/* 155 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactIsomorphic
	 */

	'use strict';

	var ReactChildren = __webpack_require__(113);
	var ReactComponent = __webpack_require__(126);
	var ReactClass = __webpack_require__(125);
	var ReactDOMFactories = __webpack_require__(156);
	var ReactElement = __webpack_require__(45);
	var ReactElementValidator = __webpack_require__(157);
	var ReactPropTypes = __webpack_require__(110);
	var ReactVersion = __webpack_require__(149);

	var assign = __webpack_require__(42);
	var onlyChild = __webpack_require__(159);

	var createElement = ReactElement.createElement;
	var createFactory = ReactElement.createFactory;
	var cloneElement = ReactElement.cloneElement;

	if (process.env.NODE_ENV !== 'production') {
	  createElement = ReactElementValidator.createElement;
	  createFactory = ReactElementValidator.createFactory;
	  cloneElement = ReactElementValidator.cloneElement;
	}

	var React = {

	  // Modern

	  Children: {
	    map: ReactChildren.map,
	    forEach: ReactChildren.forEach,
	    count: ReactChildren.count,
	    toArray: ReactChildren.toArray,
	    only: onlyChild
	  },

	  Component: ReactComponent,

	  createElement: createElement,
	  cloneElement: cloneElement,
	  isValidElement: ReactElement.isValidElement,

	  // Classic

	  PropTypes: ReactPropTypes,
	  createClass: ReactClass.createClass,
	  createFactory: createFactory,
	  createMixin: function (mixin) {
	    // Currently a noop. Will be used to validate and trace mixins.
	    return mixin;
	  },

	  // This looks DOM specific but these are actually isomorphic helpers
	  // since they are just generating DOM strings.
	  DOM: ReactDOMFactories,

	  version: ReactVersion,

	  // Hook for JSX spread, don't use this for anything else.
	  __spread: assign
	};

	module.exports = React;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 156 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMFactories
	 * @typechecks static-only
	 */

	'use strict';

	var ReactElement = __webpack_require__(45);
	var ReactElementValidator = __webpack_require__(157);

	var mapObject = __webpack_require__(158);

	/**
	 * Create a factory that creates HTML tag elements.
	 *
	 * @param {string} tag Tag name (e.g. `div`).
	 * @private
	 */
	function createDOMFactory(tag) {
	  if (process.env.NODE_ENV !== 'production') {
	    return ReactElementValidator.createFactory(tag);
	  }
	  return ReactElement.createFactory(tag);
	}

	/**
	 * Creates a mapping from supported HTML tags to `ReactDOMComponent` classes.
	 * This is also accessible via `React.DOM`.
	 *
	 * @public
	 */
	var ReactDOMFactories = mapObject({
	  a: 'a',
	  abbr: 'abbr',
	  address: 'address',
	  area: 'area',
	  article: 'article',
	  aside: 'aside',
	  audio: 'audio',
	  b: 'b',
	  base: 'base',
	  bdi: 'bdi',
	  bdo: 'bdo',
	  big: 'big',
	  blockquote: 'blockquote',
	  body: 'body',
	  br: 'br',
	  button: 'button',
	  canvas: 'canvas',
	  caption: 'caption',
	  cite: 'cite',
	  code: 'code',
	  col: 'col',
	  colgroup: 'colgroup',
	  data: 'data',
	  datalist: 'datalist',
	  dd: 'dd',
	  del: 'del',
	  details: 'details',
	  dfn: 'dfn',
	  dialog: 'dialog',
	  div: 'div',
	  dl: 'dl',
	  dt: 'dt',
	  em: 'em',
	  embed: 'embed',
	  fieldset: 'fieldset',
	  figcaption: 'figcaption',
	  figure: 'figure',
	  footer: 'footer',
	  form: 'form',
	  h1: 'h1',
	  h2: 'h2',
	  h3: 'h3',
	  h4: 'h4',
	  h5: 'h5',
	  h6: 'h6',
	  head: 'head',
	  header: 'header',
	  hgroup: 'hgroup',
	  hr: 'hr',
	  html: 'html',
	  i: 'i',
	  iframe: 'iframe',
	  img: 'img',
	  input: 'input',
	  ins: 'ins',
	  kbd: 'kbd',
	  keygen: 'keygen',
	  label: 'label',
	  legend: 'legend',
	  li: 'li',
	  link: 'link',
	  main: 'main',
	  map: 'map',
	  mark: 'mark',
	  menu: 'menu',
	  menuitem: 'menuitem',
	  meta: 'meta',
	  meter: 'meter',
	  nav: 'nav',
	  noscript: 'noscript',
	  object: 'object',
	  ol: 'ol',
	  optgroup: 'optgroup',
	  option: 'option',
	  output: 'output',
	  p: 'p',
	  param: 'param',
	  picture: 'picture',
	  pre: 'pre',
	  progress: 'progress',
	  q: 'q',
	  rp: 'rp',
	  rt: 'rt',
	  ruby: 'ruby',
	  s: 's',
	  samp: 'samp',
	  script: 'script',
	  section: 'section',
	  select: 'select',
	  small: 'small',
	  source: 'source',
	  span: 'span',
	  strong: 'strong',
	  style: 'style',
	  sub: 'sub',
	  summary: 'summary',
	  sup: 'sup',
	  table: 'table',
	  tbody: 'tbody',
	  td: 'td',
	  textarea: 'textarea',
	  tfoot: 'tfoot',
	  th: 'th',
	  thead: 'thead',
	  time: 'time',
	  title: 'title',
	  tr: 'tr',
	  track: 'track',
	  u: 'u',
	  ul: 'ul',
	  'var': 'var',
	  video: 'video',
	  wbr: 'wbr',

	  // SVG
	  circle: 'circle',
	  clipPath: 'clipPath',
	  defs: 'defs',
	  ellipse: 'ellipse',
	  g: 'g',
	  image: 'image',
	  line: 'line',
	  linearGradient: 'linearGradient',
	  mask: 'mask',
	  path: 'path',
	  pattern: 'pattern',
	  polygon: 'polygon',
	  polyline: 'polyline',
	  radialGradient: 'radialGradient',
	  rect: 'rect',
	  stop: 'stop',
	  svg: 'svg',
	  text: 'text',
	  tspan: 'tspan'

	}, createDOMFactory);

	module.exports = ReactDOMFactories;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 157 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactElementValidator
	 */

	/**
	 * ReactElementValidator provides a wrapper around a element factory
	 * which validates the props passed to the element. This is intended to be
	 * used only in DEV and could be replaced by a static type checker for languages
	 * that support it.
	 */

	'use strict';

	var ReactElement = __webpack_require__(45);
	var ReactPropTypeLocations = __webpack_require__(68);
	var ReactPropTypeLocationNames = __webpack_require__(69);
	var ReactCurrentOwner = __webpack_require__(8);

	var canDefineProperty = __webpack_require__(46);
	var getIteratorFn = __webpack_require__(111);
	var invariant = __webpack_require__(16);
	var warning = __webpack_require__(28);

	function getDeclarationErrorAddendum() {
	  if (ReactCurrentOwner.current) {
	    var name = ReactCurrentOwner.current.getName();
	    if (name) {
	      return ' Check the render method of `' + name + '`.';
	    }
	  }
	  return '';
	}

	/**
	 * Warn if there's no key explicitly set on dynamic arrays of children or
	 * object keys are not valid. This allows us to keep track of children between
	 * updates.
	 */
	var ownerHasKeyUseWarning = {};

	var loggedTypeFailures = {};

	/**
	 * Warn if the element doesn't have an explicit key assigned to it.
	 * This element is in an array. The array could grow and shrink or be
	 * reordered. All children that haven't already been validated are required to
	 * have a "key" property assigned to it.
	 *
	 * @internal
	 * @param {ReactElement} element Element that requires a key.
	 * @param {*} parentType element's parent's type.
	 */
	function validateExplicitKey(element, parentType) {
	  if (!element._store || element._store.validated || element.key != null) {
	    return;
	  }
	  element._store.validated = true;

	  var addenda = getAddendaForKeyUse('uniqueKey', element, parentType);
	  if (addenda === null) {
	    // we already showed the warning
	    return;
	  }
	  process.env.NODE_ENV !== 'production' ? warning(false, 'Each child in an array or iterator should have a unique "key" prop.' + '%s%s%s', addenda.parentOrOwner || '', addenda.childOwner || '', addenda.url || '') : undefined;
	}

	/**
	 * Shared warning and monitoring code for the key warnings.
	 *
	 * @internal
	 * @param {string} messageType A key used for de-duping warnings.
	 * @param {ReactElement} element Component that requires a key.
	 * @param {*} parentType element's parent's type.
	 * @returns {?object} A set of addenda to use in the warning message, or null
	 * if the warning has already been shown before (and shouldn't be shown again).
	 */
	function getAddendaForKeyUse(messageType, element, parentType) {
	  var addendum = getDeclarationErrorAddendum();
	  if (!addendum) {
	    var parentName = typeof parentType === 'string' ? parentType : parentType.displayName || parentType.name;
	    if (parentName) {
	      addendum = ' Check the top-level render call using <' + parentName + '>.';
	    }
	  }

	  var memoizer = ownerHasKeyUseWarning[messageType] || (ownerHasKeyUseWarning[messageType] = {});
	  if (memoizer[addendum]) {
	    return null;
	  }
	  memoizer[addendum] = true;

	  var addenda = {
	    parentOrOwner: addendum,
	    url: ' See https://fb.me/react-warning-keys for more information.',
	    childOwner: null
	  };

	  // Usually the current owner is the offender, but if it accepts children as a
	  // property, it may be the creator of the child that's responsible for
	  // assigning it a key.
	  if (element && element._owner && element._owner !== ReactCurrentOwner.current) {
	    // Give the component that originally created this child.
	    addenda.childOwner = ' It was passed a child from ' + element._owner.getName() + '.';
	  }

	  return addenda;
	}

	/**
	 * Ensure that every element either is passed in a static location, in an
	 * array with an explicit keys property defined, or in an object literal
	 * with valid key property.
	 *
	 * @internal
	 * @param {ReactNode} node Statically passed child of any type.
	 * @param {*} parentType node's parent's type.
	 */
	function validateChildKeys(node, parentType) {
	  if (typeof node !== 'object') {
	    return;
	  }
	  if (Array.isArray(node)) {
	    for (var i = 0; i < node.length; i++) {
	      var child = node[i];
	      if (ReactElement.isValidElement(child)) {
	        validateExplicitKey(child, parentType);
	      }
	    }
	  } else if (ReactElement.isValidElement(node)) {
	    // This element was passed in a valid location.
	    if (node._store) {
	      node._store.validated = true;
	    }
	  } else if (node) {
	    var iteratorFn = getIteratorFn(node);
	    // Entry iterators provide implicit keys.
	    if (iteratorFn) {
	      if (iteratorFn !== node.entries) {
	        var iterator = iteratorFn.call(node);
	        var step;
	        while (!(step = iterator.next()).done) {
	          if (ReactElement.isValidElement(step.value)) {
	            validateExplicitKey(step.value, parentType);
	          }
	        }
	      }
	    }
	  }
	}

	/**
	 * Assert that the props are valid
	 *
	 * @param {string} componentName Name of the component for error messages.
	 * @param {object} propTypes Map of prop name to a ReactPropType
	 * @param {object} props
	 * @param {string} location e.g. "prop", "context", "child context"
	 * @private
	 */
	function checkPropTypes(componentName, propTypes, props, location) {
	  for (var propName in propTypes) {
	    if (propTypes.hasOwnProperty(propName)) {
	      var error;
	      // Prop type validation may throw. In case they do, we don't want to
	      // fail the render phase where it didn't fail before. So we log it.
	      // After these have been cleaned up, we'll let them throw.
	      try {
	        // This is intentionally an invariant that gets caught. It's the same
	        // behavior as without this statement except with a better message.
	        !(typeof propTypes[propName] === 'function') ? process.env.NODE_ENV !== 'production' ? invariant(false, '%s: %s type `%s` is invalid; it must be a function, usually from ' + 'React.PropTypes.', componentName || 'React class', ReactPropTypeLocationNames[location], propName) : invariant(false) : undefined;
	        error = propTypes[propName](props, propName, componentName, location);
	      } catch (ex) {
	        error = ex;
	      }
	      process.env.NODE_ENV !== 'production' ? warning(!error || error instanceof Error, '%s: type specification of %s `%s` is invalid; the type checker ' + 'function must return `null` or an `Error` but returned a %s. ' + 'You may have forgotten to pass an argument to the type checker ' + 'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' + 'shape all require an argument).', componentName || 'React class', ReactPropTypeLocationNames[location], propName, typeof error) : undefined;
	      if (error instanceof Error && !(error.message in loggedTypeFailures)) {
	        // Only monitor this failure once because there tends to be a lot of the
	        // same error.
	        loggedTypeFailures[error.message] = true;

	        var addendum = getDeclarationErrorAddendum();
	        process.env.NODE_ENV !== 'production' ? warning(false, 'Failed propType: %s%s', error.message, addendum) : undefined;
	      }
	    }
	  }
	}

	/**
	 * Given an element, validate that its props follow the propTypes definition,
	 * provided by the type.
	 *
	 * @param {ReactElement} element
	 */
	function validatePropTypes(element) {
	  var componentClass = element.type;
	  if (typeof componentClass !== 'function') {
	    return;
	  }
	  var name = componentClass.displayName || componentClass.name;
	  if (componentClass.propTypes) {
	    checkPropTypes(name, componentClass.propTypes, element.props, ReactPropTypeLocations.prop);
	  }
	  if (typeof componentClass.getDefaultProps === 'function') {
	    process.env.NODE_ENV !== 'production' ? warning(componentClass.getDefaultProps.isReactClassApproved, 'getDefaultProps is only used on classic React.createClass ' + 'definitions. Use a static property named `defaultProps` instead.') : undefined;
	  }
	}

	var ReactElementValidator = {

	  createElement: function (type, props, children) {
	    var validType = typeof type === 'string' || typeof type === 'function';
	    // We warn in this case but don't throw. We expect the element creation to
	    // succeed and there will likely be errors in render.
	    process.env.NODE_ENV !== 'production' ? warning(validType, 'React.createElement: type should not be null, undefined, boolean, or ' + 'number. It should be a string (for DOM elements) or a ReactClass ' + '(for composite components).%s', getDeclarationErrorAddendum()) : undefined;

	    var element = ReactElement.createElement.apply(this, arguments);

	    // The result can be nullish if a mock or a custom function is used.
	    // TODO: Drop this when these are no longer allowed as the type argument.
	    if (element == null) {
	      return element;
	    }

	    // Skip key warning if the type isn't valid since our key validation logic
	    // doesn't expect a non-string/function type and can throw confusing errors.
	    // We don't want exception behavior to differ between dev and prod.
	    // (Rendering will throw with a helpful message and as soon as the type is
	    // fixed, the key warnings will appear.)
	    if (validType) {
	      for (var i = 2; i < arguments.length; i++) {
	        validateChildKeys(arguments[i], type);
	      }
	    }

	    validatePropTypes(element);

	    return element;
	  },

	  createFactory: function (type) {
	    var validatedFactory = ReactElementValidator.createElement.bind(null, type);
	    // Legacy hook TODO: Warn if this is accessed
	    validatedFactory.type = type;

	    if (process.env.NODE_ENV !== 'production') {
	      if (canDefineProperty) {
	        Object.defineProperty(validatedFactory, 'type', {
	          enumerable: false,
	          get: function () {
	            process.env.NODE_ENV !== 'production' ? warning(false, 'Factory.type is deprecated. Access the class directly ' + 'before passing it to createFactory.') : undefined;
	            Object.defineProperty(this, 'type', {
	              value: type
	            });
	            return type;
	          }
	        });
	      }
	    }

	    return validatedFactory;
	  },

	  cloneElement: function (element, props, children) {
	    var newElement = ReactElement.cloneElement.apply(this, arguments);
	    for (var i = 2; i < arguments.length; i++) {
	      validateChildKeys(arguments[i], newElement.type);
	    }
	    validatePropTypes(newElement);
	    return newElement;
	  }

	};

	module.exports = ReactElementValidator;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 158 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule mapObject
	 */

	'use strict';

	var hasOwnProperty = Object.prototype.hasOwnProperty;

	/**
	 * Executes the provided `callback` once for each enumerable own property in the
	 * object and constructs a new object from the results. The `callback` is
	 * invoked with three arguments:
	 *
	 *  - the property value
	 *  - the property name
	 *  - the object being traversed
	 *
	 * Properties that are added after the call to `mapObject` will not be visited
	 * by `callback`. If the values of existing properties are changed, the value
	 * passed to `callback` will be the value at the time `mapObject` visits them.
	 * Properties that are deleted before being visited are not visited.
	 *
	 * @grep function objectMap()
	 * @grep function objMap()
	 *
	 * @param {?object} object
	 * @param {function} callback
	 * @param {*} context
	 * @return {?object}
	 */
	function mapObject(object, callback, context) {
	  if (!object) {
	    return null;
	  }
	  var result = {};
	  for (var name in object) {
	    if (hasOwnProperty.call(object, name)) {
	      result[name] = callback.call(context, object[name], name, object);
	    }
	  }
	  return result;
	}

	module.exports = mapObject;

/***/ },
/* 159 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule onlyChild
	 */
	'use strict';

	var ReactElement = __webpack_require__(45);

	var invariant = __webpack_require__(16);

	/**
	 * Returns the first child in a collection of children and verifies that there
	 * is only one child in the collection. The current implementation of this
	 * function assumes that a single child gets passed without a wrapper, but the
	 * purpose of this helper function is to abstract away the particular structure
	 * of children.
	 *
	 * @param {?object} children Child collection structure.
	 * @return {ReactComponent} The first and only `ReactComponent` contained in the
	 * structure.
	 */
	function onlyChild(children) {
	  !ReactElement.isValidElement(children) ? process.env.NODE_ENV !== 'production' ? invariant(false, 'onlyChild must be passed a children with exactly one child.') : invariant(false) : undefined;
	  return children;
	}

	module.exports = onlyChild;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 160 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule deprecated
	 */

	'use strict';

	var assign = __webpack_require__(42);
	var warning = __webpack_require__(28);

	/**
	 * This will log a single deprecation notice per function and forward the call
	 * on to the new API.
	 *
	 * @param {string} fnName The name of the function
	 * @param {string} newModule The module that fn will exist in
	 * @param {string} newPackage The module that fn will exist in
	 * @param {*} ctx The context this forwarded call should run in
	 * @param {function} fn The function to forward on to
	 * @return {function} The function that will warn once and then call fn
	 */
	function deprecated(fnName, newModule, newPackage, ctx, fn) {
	  var warned = false;
	  if (process.env.NODE_ENV !== 'production') {
	    var newFn = function () {
	      process.env.NODE_ENV !== 'production' ? warning(warned,
	      // Require examples in this string must be split to prevent React's
	      // build tools from mistaking them for real requires.
	      // Otherwise the build tools will attempt to build a '%s' module.
	      'React.%s is deprecated. Please use %s.%s from require' + '(\'%s\') ' + 'instead.', fnName, newModule, fnName, newPackage) : undefined;
	      warned = true;
	      return fn.apply(ctx, arguments);
	    };
	    // We need to make sure all properties of the original fn are copied over.
	    // In particular, this is needed to support PropTypes
	    return assign(newFn, fn);
	  }

	  return fn;
	}

	module.exports = deprecated;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 161 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	module.exports = __webpack_require__(151);


/***/ },
/* 162 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	exports.DefaultRoute = __webpack_require__(163);
	exports.Link = __webpack_require__(178);
	exports.NotFoundRoute = __webpack_require__(179);
	exports.Redirect = __webpack_require__(180);
	exports.Route = __webpack_require__(177);
	exports.ActiveHandler = __webpack_require__(175);
	exports.RouteHandler = exports.ActiveHandler;

	exports.HashLocation = __webpack_require__(181);
	exports.HistoryLocation = __webpack_require__(185);
	exports.RefreshLocation = __webpack_require__(186);
	exports.StaticLocation = __webpack_require__(187);
	exports.TestLocation = __webpack_require__(188);

	exports.ImitateBrowserBehavior = __webpack_require__(189);
	exports.ScrollToTopBehavior = __webpack_require__(190);

	exports.History = __webpack_require__(183);
	exports.Navigation = __webpack_require__(191);
	exports.State = __webpack_require__(192);

	exports.createRoute = __webpack_require__(165).createRoute;
	exports.createDefaultRoute = __webpack_require__(165).createDefaultRoute;
	exports.createNotFoundRoute = __webpack_require__(165).createNotFoundRoute;
	exports.createRedirect = __webpack_require__(165).createRedirect;
	exports.createRoutesFromReactChildren = __webpack_require__(193);

	exports.create = __webpack_require__(194);
	exports.run = __webpack_require__(203);

/***/ },
/* 163 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _get = function get(_x, _x2, _x3) { var _again = true; _function: while (_again) { var object = _x, property = _x2, receiver = _x3; _again = false; if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { _x = parent; _x2 = property; _x3 = receiver; _again = true; desc = parent = undefined; continue _function; } } else if ('value' in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } } };

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	function _inherits(subClass, superClass) { if (typeof superClass !== 'function' && superClass !== null) { throw new TypeError('Super expression must either be null or a function, not ' + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var PropTypes = __webpack_require__(164);
	var RouteHandler = __webpack_require__(175);
	var Route = __webpack_require__(177);

	/**
	 * A <DefaultRoute> component is a special kind of <Route> that
	 * renders when its parent matches but none of its siblings do.
	 * Only one such route may be used at any given level in the
	 * route hierarchy.
	 */

	var DefaultRoute = (function (_Route) {
	  _inherits(DefaultRoute, _Route);

	  function DefaultRoute() {
	    _classCallCheck(this, DefaultRoute);

	    _get(Object.getPrototypeOf(DefaultRoute.prototype), 'constructor', this).apply(this, arguments);
	  }

	  // TODO: Include these in the above class definition
	  // once we can use ES7 property initializers.
	  // https://github.com/babel/babel/issues/619

	  return DefaultRoute;
	})(Route);

	DefaultRoute.propTypes = {
	  name: PropTypes.string,
	  path: PropTypes.falsy,
	  children: PropTypes.falsy,
	  handler: PropTypes.func.isRequired
	};

	DefaultRoute.defaultProps = {
	  handler: RouteHandler
	};

	module.exports = DefaultRoute;

/***/ },
/* 164 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var assign = __webpack_require__(42);
	var ReactPropTypes = __webpack_require__(4).PropTypes;
	var Route = __webpack_require__(165);

	var PropTypes = assign({}, ReactPropTypes, {

	  /**
	   * Indicates that a prop should be falsy.
	   */
	  falsy: function falsy(props, propName, componentName) {
	    if (props[propName]) return new Error('<' + componentName + '> should not have a "' + propName + '" prop');
	  },

	  /**
	   * Indicates that a prop should be a Route object.
	   */
	  route: ReactPropTypes.instanceOf(Route),

	  /**
	   * Indicates that a prop should be a Router object.
	   */
	  //router: ReactPropTypes.instanceOf(Router) // TODO
	  router: ReactPropTypes.func

	});

	module.exports = PropTypes;

/***/ },
/* 165 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _createClass = (function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ('value' in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; })();

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	var assign = __webpack_require__(42);
	var invariant = __webpack_require__(166);
	var warning = __webpack_require__(167);
	var PathUtils = __webpack_require__(168);

	var _currentRoute;

	var Route = (function () {
	  _createClass(Route, null, [{
	    key: 'createRoute',

	    /**
	     * Creates and returns a new route. Options may be a URL pathname string
	     * with placeholders for named params or an object with any of the following
	     * properties:
	     *
	     * - name                     The name of the route. This is used to lookup a
	     *                            route relative to its parent route and should be
	     *                            unique among all child routes of the same parent
	     * - path                     A URL pathname string with optional placeholders
	     *                            that specify the names of params to extract from
	     *                            the URL when the path matches. Defaults to `/${name}`
	     *                            when there is a name given, or the path of the parent
	     *                            route, or /
	     * - ignoreScrollBehavior     True to make this route (and all descendants) ignore
	     *                            the scroll behavior of the router
	     * - isDefault                True to make this route the default route among all
	     *                            its siblings
	     * - isNotFound               True to make this route the "not found" route among
	     *                            all its siblings
	     * - onEnter                  A transition hook that will be called when the
	     *                            router is going to enter this route
	     * - onLeave                  A transition hook that will be called when the
	     *                            router is going to leave this route
	     * - handler                  A React component that will be rendered when
	     *                            this route is active
	     * - parentRoute              The parent route to use for this route. This option
	     *                            is automatically supplied when creating routes inside
	     *                            the callback to another invocation of createRoute. You
	     *                            only ever need to use this when declaring routes
	     *                            independently of one another to manually piece together
	     *                            the route hierarchy
	     *
	     * The callback may be used to structure your route hierarchy. Any call to
	     * createRoute, createDefaultRoute, createNotFoundRoute, or createRedirect
	     * inside the callback automatically uses this route as its parent.
	     */
	    value: function createRoute(options, callback) {
	      options = options || {};

	      if (typeof options === 'string') options = { path: options };

	      var parentRoute = _currentRoute;

	      if (parentRoute) {
	        warning(options.parentRoute == null || options.parentRoute === parentRoute, 'You should not use parentRoute with createRoute inside another route\'s child callback; it is ignored');
	      } else {
	        parentRoute = options.parentRoute;
	      }

	      var name = options.name;
	      var path = options.path || name;

	      if (path && !(options.isDefault || options.isNotFound)) {
	        if (PathUtils.isAbsolute(path)) {
	          if (parentRoute) {
	            invariant(path === parentRoute.path || parentRoute.paramNames.length === 0, 'You cannot nest path "%s" inside "%s"; the parent requires URL parameters', path, parentRoute.path);
	          }
	        } else if (parentRoute) {
	          // Relative paths extend their parent.
	          path = PathUtils.join(parentRoute.path, path);
	        } else {
	          path = '/' + path;
	        }
	      } else {
	        path = parentRoute ? parentRoute.path : '/';
	      }

	      if (options.isNotFound && !/\*$/.test(path)) path += '*'; // Auto-append * to the path of not found routes.

	      var route = new Route(name, path, options.ignoreScrollBehavior, options.isDefault, options.isNotFound, options.onEnter, options.onLeave, options.handler);

	      if (parentRoute) {
	        if (route.isDefault) {
	          invariant(parentRoute.defaultRoute == null, '%s may not have more than one default route', parentRoute);

	          parentRoute.defaultRoute = route;
	        } else if (route.isNotFound) {
	          invariant(parentRoute.notFoundRoute == null, '%s may not have more than one not found route', parentRoute);

	          parentRoute.notFoundRoute = route;
	        }

	        parentRoute.appendChild(route);
	      }

	      // Any routes created in the callback
	      // use this route as their parent.
	      if (typeof callback === 'function') {
	        var currentRoute = _currentRoute;
	        _currentRoute = route;
	        callback.call(route, route);
	        _currentRoute = currentRoute;
	      }

	      return route;
	    }

	    /**
	     * Creates and returns a route that is rendered when its parent matches
	     * the current URL.
	     */
	  }, {
	    key: 'createDefaultRoute',
	    value: function createDefaultRoute(options) {
	      return Route.createRoute(assign({}, options, { isDefault: true }));
	    }

	    /**
	     * Creates and returns a route that is rendered when its parent matches
	     * the current URL but none of its siblings do.
	     */
	  }, {
	    key: 'createNotFoundRoute',
	    value: function createNotFoundRoute(options) {
	      return Route.createRoute(assign({}, options, { isNotFound: true }));
	    }

	    /**
	     * Creates and returns a route that automatically redirects the transition
	     * to another route. In addition to the normal options to createRoute, this
	     * function accepts the following options:
	     *
	     * - from         An alias for the `path` option. Defaults to *
	     * - to           The path/route/route name to redirect to
	     * - params       The params to use in the redirect URL. Defaults
	     *                to using the current params
	     * - query        The query to use in the redirect URL. Defaults
	     *                to using the current query
	     */
	  }, {
	    key: 'createRedirect',
	    value: function createRedirect(options) {
	      return Route.createRoute(assign({}, options, {
	        path: options.path || options.from || '*',
	        onEnter: function onEnter(transition, params, query) {
	          transition.redirect(options.to, options.params || params, options.query || query);
	        }
	      }));
	    }
	  }]);

	  function Route(name, path, ignoreScrollBehavior, isDefault, isNotFound, onEnter, onLeave, handler) {
	    _classCallCheck(this, Route);

	    this.name = name;
	    this.path = path;
	    this.paramNames = PathUtils.extractParamNames(this.path);
	    this.ignoreScrollBehavior = !!ignoreScrollBehavior;
	    this.isDefault = !!isDefault;
	    this.isNotFound = !!isNotFound;
	    this.onEnter = onEnter;
	    this.onLeave = onLeave;
	    this.handler = handler;
	  }

	  /**
	   * Appends the given route to this route's child routes.
	   */

	  _createClass(Route, [{
	    key: 'appendChild',
	    value: function appendChild(route) {
	      invariant(route instanceof Route, 'route.appendChild must use a valid Route');

	      if (!this.childRoutes) this.childRoutes = [];

	      this.childRoutes.push(route);
	    }
	  }, {
	    key: 'toString',
	    value: function toString() {
	      var string = '<Route';

	      if (this.name) string += ' name="' + this.name + '"';

	      string += ' path="' + this.path + '">';

	      return string;
	    }
	  }]);

	  return Route;
	})();

	module.exports = Route;

/***/ },
/* 166 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 */

	'use strict';

	/**
	 * Use invariant() to assert state which your program assumes to be true.
	 *
	 * Provide sprintf-style format (only %s is supported) and arguments
	 * to provide information about what broke and what you were
	 * expecting.
	 *
	 * The invariant message will be stripped in production, but the invariant
	 * will remain to ensure logic does not differ in production.
	 */

	var invariant = function(condition, format, a, b, c, d, e, f) {
	  if (process.env.NODE_ENV !== 'production') {
	    if (format === undefined) {
	      throw new Error('invariant requires an error message argument');
	    }
	  }

	  if (!condition) {
	    var error;
	    if (format === undefined) {
	      error = new Error(
	        'Minified exception occurred; use the non-minified dev environment ' +
	        'for the full error message and additional helpful warnings.'
	      );
	    } else {
	      var args = [a, b, c, d, e, f];
	      var argIndex = 0;
	      error = new Error(
	        format.replace(/%s/g, function() { return args[argIndex++]; })
	      );
	      error.name = 'Invariant Violation';
	    }

	    error.framesToPop = 1; // we don't care about invariant's own frame
	    throw error;
	  }
	};

	module.exports = invariant;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 167 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of https://github.com/facebook/react/tree/0.13-stable.
	 * An additional grant of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule warning
	 */

	"use strict";

	/**
	 * Similar to invariant but only logs a warning if the condition is not met.
	 * This can be used to log issues in development environments in critical
	 * paths. Removing the logging code for production environments will keep the
	 * same logic and follow the same code paths.
	 */

	var __DEV__ = process.env.NODE_ENV !== 'production';

	var warning = function warning() {};

	if (__DEV__) {
	  warning = function (condition, format) {
	    for (var _len = arguments.length, args = Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
	      args[_key - 2] = arguments[_key];
	    }

	    if (format === undefined) {
	      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
	    }

	    if (format.length < 10 || /^[s\W]*$/.test(format)) {
	      throw new Error('The warning format should be able to uniquely identify this ' + 'warning. Please, use a more descriptive format than: ' + format);
	    }

	    if (format.indexOf('Failed Composite propType: ') === 0) {
	      return; // Ignore CompositeComponent proptype check.
	    }

	    if (!condition) {
	      var argIndex = 0;
	      var message = 'Warning: ' + format.replace(/%s/g, function () {
	        return args[argIndex++];
	      });
	      console.warn(message);
	      try {
	        // --- Welcome to debugging React ---
	        // This error was thrown as a convenience so that you can use this stack
	        // to find the callsite that caused this warning to fire.
	        throw new Error(message);
	      } catch (x) {}
	    }
	  };
	}

	module.exports = warning;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 168 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var invariant = __webpack_require__(166);
	var assign = __webpack_require__(169);
	var qs = __webpack_require__(170);

	var paramCompileMatcher = /:([a-zA-Z_$][a-zA-Z0-9_$]*)|[*.()\[\]\\+|{}^$]/g;
	var paramInjectMatcher = /:([a-zA-Z_$][a-zA-Z0-9_$?]*[?]?)|[*]/g;
	var paramInjectTrailingSlashMatcher = /\/\/\?|\/\?\/|\/\?(?![^\/=]+=.*$)/g;
	var queryMatcher = /\?(.*)$/;

	var _compiledPatterns = {};

	function compilePattern(pattern) {
	  if (!(pattern in _compiledPatterns)) {
	    var paramNames = [];
	    var source = pattern.replace(paramCompileMatcher, function (match, paramName) {
	      if (paramName) {
	        paramNames.push(paramName);
	        return '([^/?#]+)';
	      } else if (match === '*') {
	        paramNames.push('splat');
	        return '(.*?)';
	      } else {
	        return '\\' + match;
	      }
	    });

	    _compiledPatterns[pattern] = {
	      matcher: new RegExp('^' + source + '$', 'i'),
	      paramNames: paramNames
	    };
	  }

	  return _compiledPatterns[pattern];
	}

	var PathUtils = {

	  /**
	   * Returns true if the given path is absolute.
	   */
	  isAbsolute: function isAbsolute(path) {
	    return path.charAt(0) === '/';
	  },

	  /**
	   * Joins two URL paths together.
	   */
	  join: function join(a, b) {
	    return a.replace(/\/*$/, '/') + b;
	  },

	  /**
	   * Returns an array of the names of all parameters in the given pattern.
	   */
	  extractParamNames: function extractParamNames(pattern) {
	    return compilePattern(pattern).paramNames;
	  },

	  /**
	   * Extracts the portions of the given URL path that match the given pattern
	   * and returns an object of param name => value pairs. Returns null if the
	   * pattern does not match the given path.
	   */
	  extractParams: function extractParams(pattern, path) {
	    var _compilePattern = compilePattern(pattern);

	    var matcher = _compilePattern.matcher;
	    var paramNames = _compilePattern.paramNames;

	    var match = path.match(matcher);

	    if (!match) return null;

	    var params = {};

	    paramNames.forEach(function (paramName, index) {
	      params[paramName] = match[index + 1];
	    });

	    return params;
	  },

	  /**
	   * Returns a version of the given route path with params interpolated. Throws
	   * if there is a dynamic segment of the route path for which there is no param.
	   */
	  injectParams: function injectParams(pattern, params) {
	    params = params || {};

	    var splatIndex = 0;

	    return pattern.replace(paramInjectMatcher, function (match, paramName) {
	      paramName = paramName || 'splat';

	      // If param is optional don't check for existence
	      if (paramName.slice(-1) === '?') {
	        paramName = paramName.slice(0, -1);

	        if (params[paramName] == null) return '';
	      } else {
	        invariant(params[paramName] != null, 'Missing "%s" parameter for path "%s"', paramName, pattern);
	      }

	      var segment;
	      if (paramName === 'splat' && Array.isArray(params[paramName])) {
	        segment = params[paramName][splatIndex++];

	        invariant(segment != null, 'Missing splat # %s for path "%s"', splatIndex, pattern);
	      } else {
	        segment = params[paramName];
	      }

	      return segment;
	    }).replace(paramInjectTrailingSlashMatcher, '/');
	  },

	  /**
	   * Returns an object that is the result of parsing any query string contained
	   * in the given path, null if the path contains no query string.
	   */
	  extractQuery: function extractQuery(path) {
	    var match = path.match(queryMatcher);
	    return match && qs.parse(match[1]);
	  },

	  /**
	   * Returns a version of the given path without the query string.
	   */
	  withoutQuery: function withoutQuery(path) {
	    return path.replace(queryMatcher, '');
	  },

	  /**
	   * Returns a version of the given path with the parameters in the given
	   * query merged into the query string.
	   */
	  withQuery: function withQuery(path, query) {
	    var existingQuery = PathUtils.extractQuery(path);

	    if (existingQuery) query = query ? assign(existingQuery, query) : existingQuery;

	    var queryString = qs.stringify(query, { arrayFormat: 'brackets' });

	    if (queryString) return PathUtils.withoutQuery(path) + '?' + queryString;

	    return PathUtils.withoutQuery(path);
	  }

	};

	module.exports = PathUtils;

/***/ },
/* 169 */
/***/ function(module, exports) {

	'use strict';

	function ToObject(val) {
		if (val == null) {
			throw new TypeError('Object.assign cannot be called with null or undefined');
		}

		return Object(val);
	}

	module.exports = Object.assign || function (target, source) {
		var from;
		var keys;
		var to = ToObject(target);

		for (var s = 1; s < arguments.length; s++) {
			from = arguments[s];
			keys = Object.keys(Object(from));

			for (var i = 0; i < keys.length; i++) {
				to[keys[i]] = from[keys[i]];
			}
		}

		return to;
	};


/***/ },
/* 170 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__(171);


/***/ },
/* 171 */
/***/ function(module, exports, __webpack_require__) {

	// Load modules

	var Stringify = __webpack_require__(172);
	var Parse = __webpack_require__(174);


	// Declare internals

	var internals = {};


	module.exports = {
	    stringify: Stringify,
	    parse: Parse
	};


/***/ },
/* 172 */
/***/ function(module, exports, __webpack_require__) {

	// Load modules

	var Utils = __webpack_require__(173);


	// Declare internals

	var internals = {
	    delimiter: '&',
	    arrayPrefixGenerators: {
	        brackets: function (prefix, key) {
	            return prefix + '[]';
	        },
	        indices: function (prefix, key) {
	            return prefix + '[' + key + ']';
	        },
	        repeat: function (prefix, key) {
	            return prefix;
	        }
	    }
	};


	internals.stringify = function (obj, prefix, generateArrayPrefix) {

	    if (Utils.isBuffer(obj)) {
	        obj = obj.toString();
	    }
	    else if (obj instanceof Date) {
	        obj = obj.toISOString();
	    }
	    else if (obj === null) {
	        obj = '';
	    }

	    if (typeof obj === 'string' ||
	        typeof obj === 'number' ||
	        typeof obj === 'boolean') {

	        return [encodeURIComponent(prefix) + '=' + encodeURIComponent(obj)];
	    }

	    var values = [];

	    if (typeof obj === 'undefined') {
	        return values;
	    }

	    var objKeys = Object.keys(obj);
	    for (var i = 0, il = objKeys.length; i < il; ++i) {
	        var key = objKeys[i];
	        if (Array.isArray(obj)) {
	            values = values.concat(internals.stringify(obj[key], generateArrayPrefix(prefix, key), generateArrayPrefix));
	        }
	        else {
	            values = values.concat(internals.stringify(obj[key], prefix + '[' + key + ']', generateArrayPrefix));
	        }
	    }

	    return values;
	};


	module.exports = function (obj, options) {

	    options = options || {};
	    var delimiter = typeof options.delimiter === 'undefined' ? internals.delimiter : options.delimiter;

	    var keys = [];

	    if (typeof obj !== 'object' ||
	        obj === null) {

	        return '';
	    }

	    var arrayFormat;
	    if (options.arrayFormat in internals.arrayPrefixGenerators) {
	        arrayFormat = options.arrayFormat;
	    }
	    else if ('indices' in options) {
	        arrayFormat = options.indices ? 'indices' : 'repeat';
	    }
	    else {
	        arrayFormat = 'indices';
	    }

	    var generateArrayPrefix = internals.arrayPrefixGenerators[arrayFormat];

	    var objKeys = Object.keys(obj);
	    for (var i = 0, il = objKeys.length; i < il; ++i) {
	        var key = objKeys[i];
	        keys = keys.concat(internals.stringify(obj[key], key, generateArrayPrefix));
	    }

	    return keys.join(delimiter);
	};


/***/ },
/* 173 */
/***/ function(module, exports) {

	// Load modules


	// Declare internals

	var internals = {};


	exports.arrayToObject = function (source) {

	    var obj = {};
	    for (var i = 0, il = source.length; i < il; ++i) {
	        if (typeof source[i] !== 'undefined') {

	            obj[i] = source[i];
	        }
	    }

	    return obj;
	};


	exports.merge = function (target, source) {

	    if (!source) {
	        return target;
	    }

	    if (typeof source !== 'object') {
	        if (Array.isArray(target)) {
	            target.push(source);
	        }
	        else {
	            target[source] = true;
	        }

	        return target;
	    }

	    if (typeof target !== 'object') {
	        target = [target].concat(source);
	        return target;
	    }

	    if (Array.isArray(target) &&
	        !Array.isArray(source)) {

	        target = exports.arrayToObject(target);
	    }

	    var keys = Object.keys(source);
	    for (var k = 0, kl = keys.length; k < kl; ++k) {
	        var key = keys[k];
	        var value = source[key];

	        if (!target[key]) {
	            target[key] = value;
	        }
	        else {
	            target[key] = exports.merge(target[key], value);
	        }
	    }

	    return target;
	};


	exports.decode = function (str) {

	    try {
	        return decodeURIComponent(str.replace(/\+/g, ' '));
	    } catch (e) {
	        return str;
	    }
	};


	exports.compact = function (obj, refs) {

	    if (typeof obj !== 'object' ||
	        obj === null) {

	        return obj;
	    }

	    refs = refs || [];
	    var lookup = refs.indexOf(obj);
	    if (lookup !== -1) {
	        return refs[lookup];
	    }

	    refs.push(obj);

	    if (Array.isArray(obj)) {
	        var compacted = [];

	        for (var i = 0, il = obj.length; i < il; ++i) {
	            if (typeof obj[i] !== 'undefined') {
	                compacted.push(obj[i]);
	            }
	        }

	        return compacted;
	    }

	    var keys = Object.keys(obj);
	    for (i = 0, il = keys.length; i < il; ++i) {
	        var key = keys[i];
	        obj[key] = exports.compact(obj[key], refs);
	    }

	    return obj;
	};


	exports.isRegExp = function (obj) {
	    return Object.prototype.toString.call(obj) === '[object RegExp]';
	};


	exports.isBuffer = function (obj) {

	    if (obj === null ||
	        typeof obj === 'undefined') {

	        return false;
	    }

	    return !!(obj.constructor &&
	        obj.constructor.isBuffer &&
	        obj.constructor.isBuffer(obj));
	};


/***/ },
/* 174 */
/***/ function(module, exports, __webpack_require__) {

	// Load modules

	var Utils = __webpack_require__(173);


	// Declare internals

	var internals = {
	    delimiter: '&',
	    depth: 5,
	    arrayLimit: 20,
	    parameterLimit: 1000
	};


	internals.parseValues = function (str, options) {

	    var obj = {};
	    var parts = str.split(options.delimiter, options.parameterLimit === Infinity ? undefined : options.parameterLimit);

	    for (var i = 0, il = parts.length; i < il; ++i) {
	        var part = parts[i];
	        var pos = part.indexOf(']=') === -1 ? part.indexOf('=') : part.indexOf(']=') + 1;

	        if (pos === -1) {
	            obj[Utils.decode(part)] = '';
	        }
	        else {
	            var key = Utils.decode(part.slice(0, pos));
	            var val = Utils.decode(part.slice(pos + 1));

	            if (Object.prototype.hasOwnProperty(key)) {
	                continue;
	            }

	            if (!obj.hasOwnProperty(key)) {
	                obj[key] = val;
	            }
	            else {
	                obj[key] = [].concat(obj[key]).concat(val);
	            }
	        }
	    }

	    return obj;
	};


	internals.parseObject = function (chain, val, options) {

	    if (!chain.length) {
	        return val;
	    }

	    var root = chain.shift();

	    var obj = {};
	    if (root === '[]') {
	        obj = [];
	        obj = obj.concat(internals.parseObject(chain, val, options));
	    }
	    else {
	        var cleanRoot = root[0] === '[' && root[root.length - 1] === ']' ? root.slice(1, root.length - 1) : root;
	        var index = parseInt(cleanRoot, 10);
	        var indexString = '' + index;
	        if (!isNaN(index) &&
	            root !== cleanRoot &&
	            indexString === cleanRoot &&
	            index >= 0 &&
	            index <= options.arrayLimit) {

	            obj = [];
	            obj[index] = internals.parseObject(chain, val, options);
	        }
	        else {
	            obj[cleanRoot] = internals.parseObject(chain, val, options);
	        }
	    }

	    return obj;
	};


	internals.parseKeys = function (key, val, options) {

	    if (!key) {
	        return;
	    }

	    // The regex chunks

	    var parent = /^([^\[\]]*)/;
	    var child = /(\[[^\[\]]*\])/g;

	    // Get the parent

	    var segment = parent.exec(key);

	    // Don't allow them to overwrite object prototype properties

	    if (Object.prototype.hasOwnProperty(segment[1])) {
	        return;
	    }

	    // Stash the parent if it exists

	    var keys = [];
	    if (segment[1]) {
	        keys.push(segment[1]);
	    }

	    // Loop through children appending to the array until we hit depth

	    var i = 0;
	    while ((segment = child.exec(key)) !== null && i < options.depth) {

	        ++i;
	        if (!Object.prototype.hasOwnProperty(segment[1].replace(/\[|\]/g, ''))) {
	            keys.push(segment[1]);
	        }
	    }

	    // If there's a remainder, just add whatever is left

	    if (segment) {
	        keys.push('[' + key.slice(segment.index) + ']');
	    }

	    return internals.parseObject(keys, val, options);
	};


	module.exports = function (str, options) {

	    if (str === '' ||
	        str === null ||
	        typeof str === 'undefined') {

	        return {};
	    }

	    options = options || {};
	    options.delimiter = typeof options.delimiter === 'string' || Utils.isRegExp(options.delimiter) ? options.delimiter : internals.delimiter;
	    options.depth = typeof options.depth === 'number' ? options.depth : internals.depth;
	    options.arrayLimit = typeof options.arrayLimit === 'number' ? options.arrayLimit : internals.arrayLimit;
	    options.parameterLimit = typeof options.parameterLimit === 'number' ? options.parameterLimit : internals.parameterLimit;

	    var tempObj = typeof str === 'string' ? internals.parseValues(str, options) : str;
	    var obj = {};

	    // Iterate over the keys and setup the new object

	    var keys = Object.keys(tempObj);
	    for (var i = 0, il = keys.length; i < il; ++i) {
	        var key = keys[i];
	        var newObj = internals.parseKeys(key, tempObj[key], options);
	        obj = Utils.merge(obj, newObj);
	    }

	    return Utils.compact(obj);
	};


/***/ },
/* 175 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _createClass = (function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ('value' in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; })();

	var _get = function get(_x, _x2, _x3) { var _again = true; _function: while (_again) { var object = _x, property = _x2, receiver = _x3; _again = false; if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { _x = parent; _x2 = property; _x3 = receiver; _again = true; desc = parent = undefined; continue _function; } } else if ('value' in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } } };

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	function _inherits(subClass, superClass) { if (typeof superClass !== 'function' && superClass !== null) { throw new TypeError('Super expression must either be null or a function, not ' + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var React = __webpack_require__(4);
	var ContextWrapper = __webpack_require__(176);
	var assign = __webpack_require__(42);
	var PropTypes = __webpack_require__(164);

	var REF_NAME = '__routeHandler__';

	/**
	 * A <RouteHandler> component renders the active child route handler
	 * when routes are nested.
	 */

	var RouteHandler = (function (_React$Component) {
	  _inherits(RouteHandler, _React$Component);

	  function RouteHandler() {
	    _classCallCheck(this, RouteHandler);

	    _get(Object.getPrototypeOf(RouteHandler.prototype), 'constructor', this).apply(this, arguments);
	  }

	  // TODO: Include these in the above class definition
	  // once we can use ES7 property initializers.
	  // https://github.com/babel/babel/issues/619

	  _createClass(RouteHandler, [{
	    key: 'getChildContext',
	    value: function getChildContext() {
	      return {
	        routeDepth: this.context.routeDepth + 1
	      };
	    }
	  }, {
	    key: 'componentDidMount',
	    value: function componentDidMount() {
	      this._updateRouteComponent(this.refs[REF_NAME]);
	    }
	  }, {
	    key: 'componentDidUpdate',
	    value: function componentDidUpdate() {
	      this._updateRouteComponent(this.refs[REF_NAME]);
	    }
	  }, {
	    key: 'componentWillUnmount',
	    value: function componentWillUnmount() {
	      this._updateRouteComponent(null);
	    }
	  }, {
	    key: '_updateRouteComponent',
	    value: function _updateRouteComponent(component) {
	      this.context.router.setRouteComponentAtDepth(this.getRouteDepth(), component);
	    }
	  }, {
	    key: 'getRouteDepth',
	    value: function getRouteDepth() {
	      return this.context.routeDepth;
	    }
	  }, {
	    key: 'createChildRouteHandler',
	    value: function createChildRouteHandler(props) {
	      var route = this.context.router.getRouteAtDepth(this.getRouteDepth());

	      if (route == null) return null;

	      var childProps = assign({}, props || this.props, {
	        ref: REF_NAME,
	        params: this.context.router.getCurrentParams(),
	        query: this.context.router.getCurrentQuery()
	      });

	      return React.createElement(route.handler, childProps);
	    }
	  }, {
	    key: 'render',
	    value: function render() {
	      var handler = this.createChildRouteHandler();
	      // <script/> for things like <CSSTransitionGroup/> that don't like null
	      return handler ? React.createElement(
	        ContextWrapper,
	        null,
	        handler
	      ) : React.createElement('script', null);
	    }
	  }]);

	  return RouteHandler;
	})(React.Component);

	RouteHandler.contextTypes = {
	  routeDepth: PropTypes.number.isRequired,
	  router: PropTypes.router.isRequired
	};

	RouteHandler.childContextTypes = {
	  routeDepth: PropTypes.number.isRequired
	};

	module.exports = RouteHandler;

/***/ },
/* 176 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * This component is necessary to get around a context warning
	 * present in React 0.13.0. It sovles this by providing a separation
	 * between the "owner" and "parent" contexts.
	 */

	'use strict';

	var _createClass = (function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ('value' in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; })();

	var _get = function get(_x, _x2, _x3) { var _again = true; _function: while (_again) { var object = _x, property = _x2, receiver = _x3; _again = false; if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { _x = parent; _x2 = property; _x3 = receiver; _again = true; desc = parent = undefined; continue _function; } } else if ('value' in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } } };

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	function _inherits(subClass, superClass) { if (typeof superClass !== 'function' && superClass !== null) { throw new TypeError('Super expression must either be null or a function, not ' + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var React = __webpack_require__(4);

	var ContextWrapper = (function (_React$Component) {
	  _inherits(ContextWrapper, _React$Component);

	  function ContextWrapper() {
	    _classCallCheck(this, ContextWrapper);

	    _get(Object.getPrototypeOf(ContextWrapper.prototype), 'constructor', this).apply(this, arguments);
	  }

	  _createClass(ContextWrapper, [{
	    key: 'render',
	    value: function render() {
	      return this.props.children;
	    }
	  }]);

	  return ContextWrapper;
	})(React.Component);

	module.exports = ContextWrapper;

/***/ },
/* 177 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _createClass = (function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ('value' in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; })();

	var _get = function get(_x, _x2, _x3) { var _again = true; _function: while (_again) { var object = _x, property = _x2, receiver = _x3; _again = false; if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { _x = parent; _x2 = property; _x3 = receiver; _again = true; desc = parent = undefined; continue _function; } } else if ('value' in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } } };

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	function _inherits(subClass, superClass) { if (typeof superClass !== 'function' && superClass !== null) { throw new TypeError('Super expression must either be null or a function, not ' + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var React = __webpack_require__(4);
	var invariant = __webpack_require__(166);
	var PropTypes = __webpack_require__(164);
	var RouteHandler = __webpack_require__(175);

	/**
	 * <Route> components specify components that are rendered to the page when the
	 * URL matches a given pattern.
	 *
	 * Routes are arranged in a nested tree structure. When a new URL is requested,
	 * the tree is searched depth-first to find a route whose path matches the URL.
	 * When one is found, all routes in the tree that lead to it are considered
	 * "active" and their components are rendered into the DOM, nested in the same
	 * order as they are in the tree.
	 *
	 * The preferred way to configure a router is using JSX. The XML-like syntax is
	 * a great way to visualize how routes are laid out in an application.
	 *
	 *   var routes = [
	 *     <Route handler={App}>
	 *       <Route name="login" handler={Login}/>
	 *       <Route name="logout" handler={Logout}/>
	 *       <Route name="about" handler={About}/>
	 *     </Route>
	 *   ];
	 *   
	 *   Router.run(routes, function (Handler) {
	 *     React.render(<Handler/>, document.body);
	 *   });
	 *
	 * Handlers for Route components that contain children can render their active
	 * child route using a <RouteHandler> element.
	 *
	 *   var App = React.createClass({
	 *     render: function () {
	 *       return (
	 *         <div className="application">
	 *           <RouteHandler/>
	 *         </div>
	 *       );
	 *     }
	 *   });
	 *
	 * If no handler is provided for the route, it will render a matched child route.
	 */

	var Route = (function (_React$Component) {
	  _inherits(Route, _React$Component);

	  function Route() {
	    _classCallCheck(this, Route);

	    _get(Object.getPrototypeOf(Route.prototype), 'constructor', this).apply(this, arguments);
	  }

	  // TODO: Include these in the above class definition
	  // once we can use ES7 property initializers.
	  // https://github.com/babel/babel/issues/619

	  _createClass(Route, [{
	    key: 'render',
	    value: function render() {
	      invariant(false, '%s elements are for router configuration only and should not be rendered', this.constructor.name);
	    }
	  }]);

	  return Route;
	})(React.Component);

	Route.propTypes = {
	  name: PropTypes.string,
	  path: PropTypes.string,
	  handler: PropTypes.func,
	  ignoreScrollBehavior: PropTypes.bool
	};

	Route.defaultProps = {
	  handler: RouteHandler
	};

	module.exports = Route;

/***/ },
/* 178 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _createClass = (function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ('value' in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; })();

	var _get = function get(_x, _x2, _x3) { var _again = true; _function: while (_again) { var object = _x, property = _x2, receiver = _x3; _again = false; if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { _x = parent; _x2 = property; _x3 = receiver; _again = true; desc = parent = undefined; continue _function; } } else if ('value' in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } } };

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	function _inherits(subClass, superClass) { if (typeof superClass !== 'function' && superClass !== null) { throw new TypeError('Super expression must either be null or a function, not ' + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var React = __webpack_require__(4);
	var assign = __webpack_require__(42);
	var PropTypes = __webpack_require__(164);

	function isLeftClickEvent(event) {
	  return event.button === 0;
	}

	function isModifiedEvent(event) {
	  return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
	}

	/**
	 * <Link> components are used to create an <a> element that links to a route.
	 * When that route is active, the link gets an "active" class name (or the
	 * value of its `activeClassName` prop).
	 *
	 * For example, assuming you have the following route:
	 *
	 *   <Route name="showPost" path="/posts/:postID" handler={Post}/>
	 *
	 * You could use the following component to link to that route:
	 *
	 *   <Link to="showPost" params={{ postID: "123" }} />
	 *
	 * In addition to params, links may pass along query string parameters
	 * using the `query` prop.
	 *
	 *   <Link to="showPost" params={{ postID: "123" }} query={{ show:true }}/>
	 */

	var Link = (function (_React$Component) {
	  _inherits(Link, _React$Component);

	  function Link() {
	    _classCallCheck(this, Link);

	    _get(Object.getPrototypeOf(Link.prototype), 'constructor', this).apply(this, arguments);
	  }

	  // TODO: Include these in the above class definition
	  // once we can use ES7 property initializers.
	  // https://github.com/babel/babel/issues/619

	  _createClass(Link, [{
	    key: 'handleClick',
	    value: function handleClick(event) {
	      var allowTransition = true;
	      var clickResult;

	      if (this.props.onClick) clickResult = this.props.onClick(event);

	      if (isModifiedEvent(event) || !isLeftClickEvent(event)) return;

	      if (clickResult === false || event.defaultPrevented === true) allowTransition = false;

	      event.preventDefault();

	      if (allowTransition) this.context.router.transitionTo(this.props.to, this.props.params, this.props.query);
	    }

	    /**
	     * Returns the value of the "href" attribute to use on the DOM element.
	     */
	  }, {
	    key: 'getHref',
	    value: function getHref() {
	      return this.context.router.makeHref(this.props.to, this.props.params, this.props.query);
	    }

	    /**
	     * Returns the value of the "class" attribute to use on the DOM element, which contains
	     * the value of the activeClassName property when this <Link> is active.
	     */
	  }, {
	    key: 'getClassName',
	    value: function getClassName() {
	      var className = this.props.className;

	      if (this.getActiveState()) className += ' ' + this.props.activeClassName;

	      return className;
	    }
	  }, {
	    key: 'getActiveState',
	    value: function getActiveState() {
	      return this.context.router.isActive(this.props.to, this.props.params, this.props.query);
	    }
	  }, {
	    key: 'render',
	    value: function render() {
	      var props = assign({}, this.props, {
	        href: this.getHref(),
	        className: this.getClassName(),
	        onClick: this.handleClick.bind(this)
	      });

	      if (props.activeStyle && this.getActiveState()) props.style = props.activeStyle;

	      return React.DOM.a(props, this.props.children);
	    }
	  }]);

	  return Link;
	})(React.Component);

	Link.contextTypes = {
	  router: PropTypes.router.isRequired
	};

	Link.propTypes = {
	  activeClassName: PropTypes.string.isRequired,
	  to: PropTypes.oneOfType([PropTypes.string, PropTypes.route]).isRequired,
	  params: PropTypes.object,
	  query: PropTypes.object,
	  activeStyle: PropTypes.object,
	  onClick: PropTypes.func
	};

	Link.defaultProps = {
	  activeClassName: 'active',
	  className: ''
	};

	module.exports = Link;

/***/ },
/* 179 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _get = function get(_x, _x2, _x3) { var _again = true; _function: while (_again) { var object = _x, property = _x2, receiver = _x3; _again = false; if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { _x = parent; _x2 = property; _x3 = receiver; _again = true; desc = parent = undefined; continue _function; } } else if ('value' in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } } };

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	function _inherits(subClass, superClass) { if (typeof superClass !== 'function' && superClass !== null) { throw new TypeError('Super expression must either be null or a function, not ' + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var PropTypes = __webpack_require__(164);
	var RouteHandler = __webpack_require__(175);
	var Route = __webpack_require__(177);

	/**
	 * A <NotFoundRoute> is a special kind of <Route> that
	 * renders when the beginning of its parent's path matches
	 * but none of its siblings do, including any <DefaultRoute>.
	 * Only one such route may be used at any given level in the
	 * route hierarchy.
	 */

	var NotFoundRoute = (function (_Route) {
	  _inherits(NotFoundRoute, _Route);

	  function NotFoundRoute() {
	    _classCallCheck(this, NotFoundRoute);

	    _get(Object.getPrototypeOf(NotFoundRoute.prototype), 'constructor', this).apply(this, arguments);
	  }

	  // TODO: Include these in the above class definition
	  // once we can use ES7 property initializers.
	  // https://github.com/babel/babel/issues/619

	  return NotFoundRoute;
	})(Route);

	NotFoundRoute.propTypes = {
	  name: PropTypes.string,
	  path: PropTypes.falsy,
	  children: PropTypes.falsy,
	  handler: PropTypes.func.isRequired
	};

	NotFoundRoute.defaultProps = {
	  handler: RouteHandler
	};

	module.exports = NotFoundRoute;

/***/ },
/* 180 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _get = function get(_x, _x2, _x3) { var _again = true; _function: while (_again) { var object = _x, property = _x2, receiver = _x3; _again = false; if (object === null) object = Function.prototype; var desc = Object.getOwnPropertyDescriptor(object, property); if (desc === undefined) { var parent = Object.getPrototypeOf(object); if (parent === null) { return undefined; } else { _x = parent; _x2 = property; _x3 = receiver; _again = true; desc = parent = undefined; continue _function; } } else if ('value' in desc) { return desc.value; } else { var getter = desc.get; if (getter === undefined) { return undefined; } return getter.call(receiver); } } };

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	function _inherits(subClass, superClass) { if (typeof superClass !== 'function' && superClass !== null) { throw new TypeError('Super expression must either be null or a function, not ' + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var PropTypes = __webpack_require__(164);
	var Route = __webpack_require__(177);

	/**
	 * A <Redirect> component is a special kind of <Route> that always
	 * redirects to another route when it matches.
	 */

	var Redirect = (function (_Route) {
	  _inherits(Redirect, _Route);

	  function Redirect() {
	    _classCallCheck(this, Redirect);

	    _get(Object.getPrototypeOf(Redirect.prototype), 'constructor', this).apply(this, arguments);
	  }

	  // TODO: Include these in the above class definition
	  // once we can use ES7 property initializers.
	  // https://github.com/babel/babel/issues/619

	  return Redirect;
	})(Route);

	Redirect.propTypes = {
	  path: PropTypes.string,
	  from: PropTypes.string, // Alias for path.
	  to: PropTypes.string,
	  handler: PropTypes.falsy
	};

	// Redirects should not have a default handler
	Redirect.defaultProps = {};

	module.exports = Redirect;

/***/ },
/* 181 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var LocationActions = __webpack_require__(182);
	var History = __webpack_require__(183);

	var _listeners = [];
	var _isListening = false;
	var _actionType;

	function notifyChange(type) {
	  if (type === LocationActions.PUSH) History.length += 1;

	  var change = {
	    path: HashLocation.getCurrentPath(),
	    type: type
	  };

	  _listeners.forEach(function (listener) {
	    listener.call(HashLocation, change);
	  });
	}

	function ensureSlash() {
	  var path = HashLocation.getCurrentPath();

	  if (path.charAt(0) === '/') return true;

	  HashLocation.replace('/' + path);

	  return false;
	}

	function onHashChange() {
	  if (ensureSlash()) {
	    // If we don't have an _actionType then all we know is the hash
	    // changed. It was probably caused by the user clicking the Back
	    // button, but may have also been the Forward button or manual
	    // manipulation. So just guess 'pop'.
	    var curActionType = _actionType;
	    _actionType = null;
	    notifyChange(curActionType || LocationActions.POP);
	  }
	}

	/**
	 * A Location that uses `window.location.hash`.
	 */
	var HashLocation = {

	  addChangeListener: function addChangeListener(listener) {
	    _listeners.push(listener);

	    // Do this BEFORE listening for hashchange.
	    ensureSlash();

	    if (!_isListening) {
	      if (window.addEventListener) {
	        window.addEventListener('hashchange', onHashChange, false);
	      } else {
	        window.attachEvent('onhashchange', onHashChange);
	      }

	      _isListening = true;
	    }
	  },

	  removeChangeListener: function removeChangeListener(listener) {
	    _listeners = _listeners.filter(function (l) {
	      return l !== listener;
	    });

	    if (_listeners.length === 0) {
	      if (window.removeEventListener) {
	        window.removeEventListener('hashchange', onHashChange, false);
	      } else {
	        window.removeEvent('onhashchange', onHashChange);
	      }

	      _isListening = false;
	    }
	  },

	  push: function push(path) {
	    _actionType = LocationActions.PUSH;
	    window.location.hash = path;
	  },

	  replace: function replace(path) {
	    _actionType = LocationActions.REPLACE;
	    window.location.replace(window.location.pathname + window.location.search + '#' + path);
	  },

	  pop: function pop() {
	    _actionType = LocationActions.POP;
	    History.back();
	  },

	  getCurrentPath: function getCurrentPath() {
	    return decodeURI(
	    // We can't use window.location.hash here because it's not
	    // consistent across browsers - Firefox will pre-decode it!
	    window.location.href.split('#')[1] || '');
	  },

	  toString: function toString() {
	    return '<HashLocation>';
	  }

	};

	module.exports = HashLocation;

/***/ },
/* 182 */
/***/ function(module, exports) {

	/**
	 * Actions that modify the URL.
	 */
	'use strict';

	var LocationActions = {

	  /**
	   * Indicates a new location is being pushed to the history stack.
	   */
	  PUSH: 'push',

	  /**
	   * Indicates the current location should be replaced.
	   */
	  REPLACE: 'replace',

	  /**
	   * Indicates the most recent entry should be removed from the history stack.
	   */
	  POP: 'pop'

	};

	module.exports = LocationActions;

/***/ },
/* 183 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var invariant = __webpack_require__(166);
	var canUseDOM = __webpack_require__(184);

	var History = {

	  /**
	   * The current number of entries in the history.
	   *
	   * Note: This property is read-only.
	   */
	  length: 1,

	  /**
	   * Sends the browser back one entry in the history.
	   */
	  back: function back() {
	    invariant(canUseDOM, 'Cannot use History.back without a DOM');

	    // Do this first so that History.length will
	    // be accurate in location change listeners.
	    History.length -= 1;

	    window.history.back();
	  }

	};

	module.exports = History;

/***/ },
/* 184 */
/***/ function(module, exports) {

	var canUseDOM = !!(
	  typeof window !== 'undefined' &&
	  window.document &&
	  window.document.createElement
	);

	module.exports = canUseDOM;

/***/ },
/* 185 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var LocationActions = __webpack_require__(182);
	var History = __webpack_require__(183);

	var _listeners = [];
	var _isListening = false;

	function notifyChange(type) {
	  var change = {
	    path: HistoryLocation.getCurrentPath(),
	    type: type
	  };

	  _listeners.forEach(function (listener) {
	    listener.call(HistoryLocation, change);
	  });
	}

	function onPopState(event) {
	  if (event.state === undefined) return; // Ignore extraneous popstate events in WebKit.

	  notifyChange(LocationActions.POP);
	}

	/**
	 * A Location that uses HTML5 history.
	 */
	var HistoryLocation = {

	  addChangeListener: function addChangeListener(listener) {
	    _listeners.push(listener);

	    if (!_isListening) {
	      if (window.addEventListener) {
	        window.addEventListener('popstate', onPopState, false);
	      } else {
	        window.attachEvent('onpopstate', onPopState);
	      }

	      _isListening = true;
	    }
	  },

	  removeChangeListener: function removeChangeListener(listener) {
	    _listeners = _listeners.filter(function (l) {
	      return l !== listener;
	    });

	    if (_listeners.length === 0) {
	      if (window.addEventListener) {
	        window.removeEventListener('popstate', onPopState, false);
	      } else {
	        window.removeEvent('onpopstate', onPopState);
	      }

	      _isListening = false;
	    }
	  },

	  push: function push(path) {
	    window.history.pushState({ path: path }, '', path);
	    History.length += 1;
	    notifyChange(LocationActions.PUSH);
	  },

	  replace: function replace(path) {
	    window.history.replaceState({ path: path }, '', path);
	    notifyChange(LocationActions.REPLACE);
	  },

	  pop: History.back,

	  getCurrentPath: function getCurrentPath() {
	    return decodeURI(window.location.pathname + window.location.search);
	  },

	  toString: function toString() {
	    return '<HistoryLocation>';
	  }

	};

	module.exports = HistoryLocation;

/***/ },
/* 186 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var HistoryLocation = __webpack_require__(185);
	var History = __webpack_require__(183);

	/**
	 * A Location that uses full page refreshes. This is used as
	 * the fallback for HistoryLocation in browsers that do not
	 * support the HTML5 history API.
	 */
	var RefreshLocation = {

	  push: function push(path) {
	    window.location = path;
	  },

	  replace: function replace(path) {
	    window.location.replace(path);
	  },

	  pop: History.back,

	  getCurrentPath: HistoryLocation.getCurrentPath,

	  toString: function toString() {
	    return '<RefreshLocation>';
	  }

	};

	module.exports = RefreshLocation;

/***/ },
/* 187 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _createClass = (function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ('value' in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; })();

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	var invariant = __webpack_require__(166);

	function throwCannotModify() {
	  invariant(false, 'You cannot modify a static location');
	}

	/**
	 * A location that only ever contains a single path. Useful in
	 * stateless environments like servers where there is no path history,
	 * only the path that was used in the request.
	 */

	var StaticLocation = (function () {
	  function StaticLocation(path) {
	    _classCallCheck(this, StaticLocation);

	    this.path = path;
	  }

	  // TODO: Include these in the above class definition
	  // once we can use ES7 property initializers.
	  // https://github.com/babel/babel/issues/619

	  _createClass(StaticLocation, [{
	    key: 'getCurrentPath',
	    value: function getCurrentPath() {
	      return this.path;
	    }
	  }, {
	    key: 'toString',
	    value: function toString() {
	      return '<StaticLocation path="' + this.path + '">';
	    }
	  }]);

	  return StaticLocation;
	})();

	StaticLocation.prototype.push = throwCannotModify;
	StaticLocation.prototype.replace = throwCannotModify;
	StaticLocation.prototype.pop = throwCannotModify;

	module.exports = StaticLocation;

/***/ },
/* 188 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _createClass = (function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ('value' in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; })();

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	var invariant = __webpack_require__(166);
	var LocationActions = __webpack_require__(182);
	var History = __webpack_require__(183);

	/**
	 * A location that is convenient for testing and does not require a DOM.
	 */

	var TestLocation = (function () {
	  function TestLocation(history) {
	    _classCallCheck(this, TestLocation);

	    this.history = history || [];
	    this.listeners = [];
	    this.needsDOM = false;
	    this._updateHistoryLength();
	  }

	  _createClass(TestLocation, [{
	    key: '_updateHistoryLength',
	    value: function _updateHistoryLength() {
	      History.length = this.history.length;
	    }
	  }, {
	    key: '_notifyChange',
	    value: function _notifyChange(type) {
	      var change = {
	        path: this.getCurrentPath(),
	        type: type
	      };

	      for (var i = 0, len = this.listeners.length; i < len; ++i) this.listeners[i].call(this, change);
	    }
	  }, {
	    key: 'addChangeListener',
	    value: function addChangeListener(listener) {
	      this.listeners.push(listener);
	    }
	  }, {
	    key: 'removeChangeListener',
	    value: function removeChangeListener(listener) {
	      this.listeners = this.listeners.filter(function (l) {
	        return l !== listener;
	      });
	    }
	  }, {
	    key: 'push',
	    value: function push(path) {
	      this.history.push(path);
	      this._updateHistoryLength();
	      this._notifyChange(LocationActions.PUSH);
	    }
	  }, {
	    key: 'replace',
	    value: function replace(path) {
	      invariant(this.history.length, 'You cannot replace the current path with no history');

	      this.history[this.history.length - 1] = path;

	      this._notifyChange(LocationActions.REPLACE);
	    }
	  }, {
	    key: 'pop',
	    value: function pop() {
	      this.history.pop();
	      this._updateHistoryLength();
	      this._notifyChange(LocationActions.POP);
	    }
	  }, {
	    key: 'getCurrentPath',
	    value: function getCurrentPath() {
	      return this.history[this.history.length - 1];
	    }
	  }, {
	    key: 'toString',
	    value: function toString() {
	      return '<TestLocation>';
	    }
	  }]);

	  return TestLocation;
	})();

	module.exports = TestLocation;

/***/ },
/* 189 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var LocationActions = __webpack_require__(182);

	/**
	 * A scroll behavior that attempts to imitate the default behavior
	 * of modern browsers.
	 */
	var ImitateBrowserBehavior = {

	  updateScrollPosition: function updateScrollPosition(position, actionType) {
	    switch (actionType) {
	      case LocationActions.PUSH:
	      case LocationActions.REPLACE:
	        window.scrollTo(0, 0);
	        break;
	      case LocationActions.POP:
	        if (position) {
	          window.scrollTo(position.x, position.y);
	        } else {
	          window.scrollTo(0, 0);
	        }
	        break;
	    }
	  }

	};

	module.exports = ImitateBrowserBehavior;

/***/ },
/* 190 */
/***/ function(module, exports) {

	/**
	 * A scroll behavior that always scrolls to the top of the page
	 * after a transition.
	 */
	"use strict";

	var ScrollToTopBehavior = {

	  updateScrollPosition: function updateScrollPosition() {
	    window.scrollTo(0, 0);
	  }

	};

	module.exports = ScrollToTopBehavior;

/***/ },
/* 191 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var PropTypes = __webpack_require__(164);

	/**
	 * A mixin for components that modify the URL.
	 *
	 * Example:
	 *
	 *   var MyLink = React.createClass({
	 *     mixins: [ Router.Navigation ],
	 *     handleClick(event) {
	 *       event.preventDefault();
	 *       this.transitionTo('aRoute', { the: 'params' }, { the: 'query' });
	 *     },
	 *     render() {
	 *       return (
	 *         <a onClick={this.handleClick}>Click me!</a>
	 *       );
	 *     }
	 *   });
	 */
	var Navigation = {

	  contextTypes: {
	    router: PropTypes.router.isRequired
	  },

	  /**
	   * Returns an absolute URL path created from the given route
	   * name, URL parameters, and query values.
	   */
	  makePath: function makePath(to, params, query) {
	    return this.context.router.makePath(to, params, query);
	  },

	  /**
	   * Returns a string that may safely be used as the href of a
	   * link to the route with the given name.
	   */
	  makeHref: function makeHref(to, params, query) {
	    return this.context.router.makeHref(to, params, query);
	  },

	  /**
	   * Transitions to the URL specified in the arguments by pushing
	   * a new URL onto the history stack.
	   */
	  transitionTo: function transitionTo(to, params, query) {
	    this.context.router.transitionTo(to, params, query);
	  },

	  /**
	   * Transitions to the URL specified in the arguments by replacing
	   * the current URL in the history stack.
	   */
	  replaceWith: function replaceWith(to, params, query) {
	    this.context.router.replaceWith(to, params, query);
	  },

	  /**
	   * Transitions to the previous URL.
	   */
	  goBack: function goBack() {
	    return this.context.router.goBack();
	  }

	};

	module.exports = Navigation;

/***/ },
/* 192 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var PropTypes = __webpack_require__(164);

	/**
	 * A mixin for components that need to know the path, routes, URL
	 * params and query that are currently active.
	 *
	 * Example:
	 *
	 *   var AboutLink = React.createClass({
	 *     mixins: [ Router.State ],
	 *     render() {
	 *       var className = this.props.className;
	 *
	 *       if (this.isActive('about'))
	 *         className += ' is-active';
	 *
	 *       return React.DOM.a({ className: className }, this.props.children);
	 *     }
	 *   });
	 */
	var State = {

	  contextTypes: {
	    router: PropTypes.router.isRequired
	  },

	  /**
	   * Returns the current URL path.
	   */
	  getPath: function getPath() {
	    return this.context.router.getCurrentPath();
	  },

	  /**
	   * Returns the current URL path without the query string.
	   */
	  getPathname: function getPathname() {
	    return this.context.router.getCurrentPathname();
	  },

	  /**
	   * Returns an object of the URL params that are currently active.
	   */
	  getParams: function getParams() {
	    return this.context.router.getCurrentParams();
	  },

	  /**
	   * Returns an object of the query params that are currently active.
	   */
	  getQuery: function getQuery() {
	    return this.context.router.getCurrentQuery();
	  },

	  /**
	   * Returns an array of the routes that are currently active.
	   */
	  getRoutes: function getRoutes() {
	    return this.context.router.getCurrentRoutes();
	  },

	  /**
	   * A helper method to determine if a given route, params, and query
	   * are active.
	   */
	  isActive: function isActive(to, params, query) {
	    return this.context.router.isActive(to, params, query);
	  }

	};

	module.exports = State;

/***/ },
/* 193 */
/***/ function(module, exports, __webpack_require__) {

	/* jshint -W084 */
	'use strict';

	var React = __webpack_require__(4);
	var assign = __webpack_require__(42);
	var warning = __webpack_require__(167);
	var DefaultRoute = __webpack_require__(163);
	var NotFoundRoute = __webpack_require__(179);
	var Redirect = __webpack_require__(180);
	var Route = __webpack_require__(165);

	function checkPropTypes(componentName, propTypes, props) {
	  componentName = componentName || 'UnknownComponent';

	  for (var propName in propTypes) {
	    if (propTypes.hasOwnProperty(propName)) {
	      var error = propTypes[propName](props, propName, componentName);

	      if (error instanceof Error) warning(false, error.message);
	    }
	  }
	}

	function createRouteOptions(props) {
	  var options = assign({}, props);
	  var handler = options.handler;

	  if (handler) {
	    options.onEnter = handler.willTransitionTo;
	    options.onLeave = handler.willTransitionFrom;
	  }

	  return options;
	}

	function createRouteFromReactElement(element) {
	  if (!React.isValidElement(element)) return;

	  var type = element.type;
	  var props = assign({}, type.defaultProps, element.props);

	  if (type.propTypes) checkPropTypes(type.displayName, type.propTypes, props);

	  if (type === DefaultRoute) return Route.createDefaultRoute(createRouteOptions(props));

	  if (type === NotFoundRoute) return Route.createNotFoundRoute(createRouteOptions(props));

	  if (type === Redirect) return Route.createRedirect(createRouteOptions(props));

	  return Route.createRoute(createRouteOptions(props), function () {
	    if (props.children) createRoutesFromReactChildren(props.children);
	  });
	}

	/**
	 * Creates and returns an array of routes created from the given
	 * ReactChildren, all of which should be one of <Route>, <DefaultRoute>,
	 * <NotFoundRoute>, or <Redirect>, e.g.:
	 *
	 *   var { createRoutesFromReactChildren, Route, Redirect } = require('react-router');
	 *
	 *   var routes = createRoutesFromReactChildren(
	 *     <Route path="/" handler={App}>
	 *       <Route name="user" path="/user/:userId" handler={User}>
	 *         <Route name="task" path="tasks/:taskId" handler={Task}/>
	 *         <Redirect from="todos/:taskId" to="task"/>
	 *       </Route>
	 *     </Route>
	 *   );
	 */
	function createRoutesFromReactChildren(children) {
	  var routes = [];

	  React.Children.forEach(children, function (child) {
	    if (child = createRouteFromReactElement(child)) routes.push(child);
	  });

	  return routes;
	}

	module.exports = createRoutesFromReactChildren;

/***/ },
/* 194 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/* jshint -W058 */
	'use strict';

	var React = __webpack_require__(4);
	var warning = __webpack_require__(167);
	var invariant = __webpack_require__(166);
	var canUseDOM = __webpack_require__(184);
	var LocationActions = __webpack_require__(182);
	var ImitateBrowserBehavior = __webpack_require__(189);
	var HashLocation = __webpack_require__(181);
	var HistoryLocation = __webpack_require__(185);
	var RefreshLocation = __webpack_require__(186);
	var StaticLocation = __webpack_require__(187);
	var ScrollHistory = __webpack_require__(195);
	var createRoutesFromReactChildren = __webpack_require__(193);
	var isReactChildren = __webpack_require__(197);
	var Transition = __webpack_require__(198);
	var PropTypes = __webpack_require__(164);
	var Redirect = __webpack_require__(200);
	var History = __webpack_require__(183);
	var Cancellation = __webpack_require__(199);
	var Match = __webpack_require__(201);
	var Route = __webpack_require__(165);
	var supportsHistory = __webpack_require__(202);
	var PathUtils = __webpack_require__(168);

	/**
	 * The default location for new routers.
	 */
	var DEFAULT_LOCATION = canUseDOM ? HashLocation : '/';

	/**
	 * The default scroll behavior for new routers.
	 */
	var DEFAULT_SCROLL_BEHAVIOR = canUseDOM ? ImitateBrowserBehavior : null;

	function hasProperties(object, properties) {
	  for (var propertyName in properties) if (properties.hasOwnProperty(propertyName) && object[propertyName] !== properties[propertyName]) return false;

	  return true;
	}

	function hasMatch(routes, route, prevParams, nextParams, prevQuery, nextQuery) {
	  return routes.some(function (r) {
	    if (r !== route) return false;

	    var paramNames = route.paramNames;
	    var paramName;

	    // Ensure that all params the route cares about did not change.
	    for (var i = 0, len = paramNames.length; i < len; ++i) {
	      paramName = paramNames[i];

	      if (nextParams[paramName] !== prevParams[paramName]) return false;
	    }

	    // Ensure the query hasn't changed.
	    return hasProperties(prevQuery, nextQuery) && hasProperties(nextQuery, prevQuery);
	  });
	}

	function addRoutesToNamedRoutes(routes, namedRoutes) {
	  var route;
	  for (var i = 0, len = routes.length; i < len; ++i) {
	    route = routes[i];

	    if (route.name) {
	      invariant(namedRoutes[route.name] == null, 'You may not have more than one route named "%s"', route.name);

	      namedRoutes[route.name] = route;
	    }

	    if (route.childRoutes) addRoutesToNamedRoutes(route.childRoutes, namedRoutes);
	  }
	}

	function routeIsActive(activeRoutes, routeName) {
	  return activeRoutes.some(function (route) {
	    return route.name === routeName;
	  });
	}

	function paramsAreActive(activeParams, params) {
	  for (var property in params) if (String(activeParams[property]) !== String(params[property])) return false;

	  return true;
	}

	function queryIsActive(activeQuery, query) {
	  for (var property in query) if (String(activeQuery[property]) !== String(query[property])) return false;

	  return true;
	}

	/**
	 * Creates and returns a new router using the given options. A router
	 * is a ReactComponent class that knows how to react to changes in the
	 * URL and keep the contents of the page in sync.
	 *
	 * Options may be any of the following:
	 *
	 * - routes           (required) The route config
	 * - location         The location to use. Defaults to HashLocation when
	 *                    the DOM is available, "/" otherwise
	 * - scrollBehavior   The scroll behavior to use. Defaults to ImitateBrowserBehavior
	 *                    when the DOM is available, null otherwise
	 * - onError          A function that is used to handle errors
	 * - onAbort          A function that is used to handle aborted transitions
	 *
	 * When rendering in a server-side environment, the location should simply
	 * be the URL path that was used in the request, including the query string.
	 */
	function createRouter(options) {
	  options = options || {};

	  if (isReactChildren(options)) options = { routes: options };

	  var mountedComponents = [];
	  var location = options.location || DEFAULT_LOCATION;
	  var scrollBehavior = options.scrollBehavior || DEFAULT_SCROLL_BEHAVIOR;
	  var state = {};
	  var nextState = {};
	  var pendingTransition = null;
	  var dispatchHandler = null;

	  if (typeof location === 'string') location = new StaticLocation(location);

	  if (location instanceof StaticLocation) {
	    warning(!canUseDOM || process.env.NODE_ENV === 'test', 'You should not use a static location in a DOM environment because ' + 'the router will not be kept in sync with the current URL');
	  } else {
	    invariant(canUseDOM || location.needsDOM === false, 'You cannot use %s without a DOM', location);
	  }

	  // Automatically fall back to full page refreshes in
	  // browsers that don't support the HTML history API.
	  if (location === HistoryLocation && !supportsHistory()) location = RefreshLocation;

	  var Router = React.createClass({

	    displayName: 'Router',

	    statics: {

	      isRunning: false,

	      cancelPendingTransition: function cancelPendingTransition() {
	        if (pendingTransition) {
	          pendingTransition.cancel();
	          pendingTransition = null;
	        }
	      },

	      clearAllRoutes: function clearAllRoutes() {
	        Router.cancelPendingTransition();
	        Router.namedRoutes = {};
	        Router.routes = [];
	      },

	      /**
	       * Adds routes to this router from the given children object (see ReactChildren).
	       */
	      addRoutes: function addRoutes(routes) {
	        if (isReactChildren(routes)) routes = createRoutesFromReactChildren(routes);

	        addRoutesToNamedRoutes(routes, Router.namedRoutes);

	        Router.routes.push.apply(Router.routes, routes);
	      },

	      /**
	       * Replaces routes of this router from the given children object (see ReactChildren).
	       */
	      replaceRoutes: function replaceRoutes(routes) {
	        Router.clearAllRoutes();
	        Router.addRoutes(routes);
	        Router.refresh();
	      },

	      /**
	       * Performs a match of the given path against this router and returns an object
	       * with the { routes, params, pathname, query } that match. Returns null if no
	       * match can be made.
	       */
	      match: function match(path) {
	        return Match.findMatch(Router.routes, path);
	      },

	      /**
	       * Returns an absolute URL path created from the given route
	       * name, URL parameters, and query.
	       */
	      makePath: function makePath(to, params, query) {
	        var path;
	        if (PathUtils.isAbsolute(to)) {
	          path = to;
	        } else {
	          var route = to instanceof Route ? to : Router.namedRoutes[to];

	          invariant(route instanceof Route, 'Cannot find a route named "%s"', to);

	          path = route.path;
	        }

	        return PathUtils.withQuery(PathUtils.injectParams(path, params), query);
	      },

	      /**
	       * Returns a string that may safely be used as the href of a link
	       * to the route with the given name, URL parameters, and query.
	       */
	      makeHref: function makeHref(to, params, query) {
	        var path = Router.makePath(to, params, query);
	        return location === HashLocation ? '#' + path : path;
	      },

	      /**
	       * Transitions to the URL specified in the arguments by pushing
	       * a new URL onto the history stack.
	       */
	      transitionTo: function transitionTo(to, params, query) {
	        var path = Router.makePath(to, params, query);

	        if (pendingTransition) {
	          // Replace so pending location does not stay in history.
	          location.replace(path);
	        } else {
	          location.push(path);
	        }
	      },

	      /**
	       * Transitions to the URL specified in the arguments by replacing
	       * the current URL in the history stack.
	       */
	      replaceWith: function replaceWith(to, params, query) {
	        location.replace(Router.makePath(to, params, query));
	      },

	      /**
	       * Transitions to the previous URL if one is available. Returns true if the
	       * router was able to go back, false otherwise.
	       *
	       * Note: The router only tracks history entries in your application, not the
	       * current browser session, so you can safely call this function without guarding
	       * against sending the user back to some other site. However, when using
	       * RefreshLocation (which is the fallback for HistoryLocation in browsers that
	       * don't support HTML5 history) this method will *always* send the client back
	       * because we cannot reliably track history length.
	       */
	      goBack: function goBack() {
	        if (History.length > 1 || location === RefreshLocation) {
	          location.pop();
	          return true;
	        }

	        warning(false, 'goBack() was ignored because there is no router history');

	        return false;
	      },

	      handleAbort: options.onAbort || function (abortReason) {
	        if (location instanceof StaticLocation) throw new Error('Unhandled aborted transition! Reason: ' + abortReason);

	        if (abortReason instanceof Cancellation) {
	          return;
	        } else if (abortReason instanceof Redirect) {
	          location.replace(Router.makePath(abortReason.to, abortReason.params, abortReason.query));
	        } else {
	          location.pop();
	        }
	      },

	      handleError: options.onError || function (error) {
	        // Throw so we don't silently swallow async errors.
	        throw error; // This error probably originated in a transition hook.
	      },

	      handleLocationChange: function handleLocationChange(change) {
	        Router.dispatch(change.path, change.type);
	      },

	      /**
	       * Performs a transition to the given path and calls callback(error, abortReason)
	       * when the transition is finished. If both arguments are null the router's state
	       * was updated. Otherwise the transition did not complete.
	       *
	       * In a transition, a router first determines which routes are involved by beginning
	       * with the current route, up the route tree to the first parent route that is shared
	       * with the destination route, and back down the tree to the destination route. The
	       * willTransitionFrom hook is invoked on all route handlers we're transitioning away
	       * from, in reverse nesting order. Likewise, the willTransitionTo hook is invoked on
	       * all route handlers we're transitioning to.
	       *
	       * Both willTransitionFrom and willTransitionTo hooks may either abort or redirect the
	       * transition. To resolve asynchronously, they may use the callback argument. If no
	       * hooks wait, the transition is fully synchronous.
	       */
	      dispatch: function dispatch(path, action) {
	        Router.cancelPendingTransition();

	        var prevPath = state.path;
	        var isRefreshing = action == null;

	        if (prevPath === path && !isRefreshing) return; // Nothing to do!

	        // Record the scroll position as early as possible to
	        // get it before browsers try update it automatically.
	        if (prevPath && action === LocationActions.PUSH) Router.recordScrollPosition(prevPath);

	        var match = Router.match(path);

	        warning(match != null, 'No route matches path "%s". Make sure you have <Route path="%s"> somewhere in your routes', path, path);

	        if (match == null) match = {};

	        var prevRoutes = state.routes || [];
	        var prevParams = state.params || {};
	        var prevQuery = state.query || {};

	        var nextRoutes = match.routes || [];
	        var nextParams = match.params || {};
	        var nextQuery = match.query || {};

	        var fromRoutes, toRoutes;
	        if (prevRoutes.length) {
	          fromRoutes = prevRoutes.filter(function (route) {
	            return !hasMatch(nextRoutes, route, prevParams, nextParams, prevQuery, nextQuery);
	          });

	          toRoutes = nextRoutes.filter(function (route) {
	            return !hasMatch(prevRoutes, route, prevParams, nextParams, prevQuery, nextQuery);
	          });
	        } else {
	          fromRoutes = [];
	          toRoutes = nextRoutes;
	        }

	        var transition = new Transition(path, Router.replaceWith.bind(Router, path));
	        pendingTransition = transition;

	        var fromComponents = mountedComponents.slice(prevRoutes.length - fromRoutes.length);

	        Transition.from(transition, fromRoutes, fromComponents, function (error) {
	          if (error || transition.abortReason) return dispatchHandler.call(Router, error, transition); // No need to continue.

	          Transition.to(transition, toRoutes, nextParams, nextQuery, function (error) {
	            dispatchHandler.call(Router, error, transition, {
	              path: path,
	              action: action,
	              pathname: match.pathname,
	              routes: nextRoutes,
	              params: nextParams,
	              query: nextQuery
	            });
	          });
	        });
	      },

	      /**
	       * Starts this router and calls callback(router, state) when the route changes.
	       *
	       * If the router's location is static (i.e. a URL path in a server environment)
	       * the callback is called only once. Otherwise, the location should be one of the
	       * Router.*Location objects (e.g. Router.HashLocation or Router.HistoryLocation).
	       */
	      run: function run(callback) {
	        invariant(!Router.isRunning, 'Router is already running');

	        dispatchHandler = function (error, transition, newState) {
	          if (error) Router.handleError(error);

	          if (pendingTransition !== transition) return;

	          pendingTransition = null;

	          if (transition.abortReason) {
	            Router.handleAbort(transition.abortReason);
	          } else {
	            callback.call(Router, Router, nextState = newState);
	          }
	        };

	        if (!(location instanceof StaticLocation)) {
	          if (location.addChangeListener) location.addChangeListener(Router.handleLocationChange);

	          Router.isRunning = true;
	        }

	        // Bootstrap using the current path.
	        Router.refresh();
	      },

	      refresh: function refresh() {
	        Router.dispatch(location.getCurrentPath(), null);
	      },

	      stop: function stop() {
	        Router.cancelPendingTransition();

	        if (location.removeChangeListener) location.removeChangeListener(Router.handleLocationChange);

	        Router.isRunning = false;
	      },

	      getLocation: function getLocation() {
	        return location;
	      },

	      getScrollBehavior: function getScrollBehavior() {
	        return scrollBehavior;
	      },

	      getRouteAtDepth: function getRouteAtDepth(routeDepth) {
	        var routes = state.routes;
	        return routes && routes[routeDepth];
	      },

	      setRouteComponentAtDepth: function setRouteComponentAtDepth(routeDepth, component) {
	        mountedComponents[routeDepth] = component;
	      },

	      /**
	       * Returns the current URL path + query string.
	       */
	      getCurrentPath: function getCurrentPath() {
	        return state.path;
	      },

	      /**
	       * Returns the current URL path without the query string.
	       */
	      getCurrentPathname: function getCurrentPathname() {
	        return state.pathname;
	      },

	      /**
	       * Returns an object of the currently active URL parameters.
	       */
	      getCurrentParams: function getCurrentParams() {
	        return state.params;
	      },

	      /**
	       * Returns an object of the currently active query parameters.
	       */
	      getCurrentQuery: function getCurrentQuery() {
	        return state.query;
	      },

	      /**
	       * Returns an array of the currently active routes.
	       */
	      getCurrentRoutes: function getCurrentRoutes() {
	        return state.routes;
	      },

	      /**
	       * Returns true if the given route, params, and query are active.
	       */
	      isActive: function isActive(to, params, query) {
	        if (PathUtils.isAbsolute(to)) return to === state.path;

	        return routeIsActive(state.routes, to) && paramsAreActive(state.params, params) && (query == null || queryIsActive(state.query, query));
	      }

	    },

	    mixins: [ScrollHistory],

	    propTypes: {
	      children: PropTypes.falsy
	    },

	    childContextTypes: {
	      routeDepth: PropTypes.number.isRequired,
	      router: PropTypes.router.isRequired
	    },

	    getChildContext: function getChildContext() {
	      return {
	        routeDepth: 1,
	        router: Router
	      };
	    },

	    getInitialState: function getInitialState() {
	      return state = nextState;
	    },

	    componentWillReceiveProps: function componentWillReceiveProps() {
	      this.setState(state = nextState);
	    },

	    componentWillUnmount: function componentWillUnmount() {
	      Router.stop();
	    },

	    render: function render() {
	      var route = Router.getRouteAtDepth(0);
	      return route ? React.createElement(route.handler, this.props) : null;
	    }

	  });

	  Router.clearAllRoutes();

	  if (options.routes) Router.addRoutes(options.routes);

	  return Router;
	}

	module.exports = createRouter;
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(7)))

/***/ },
/* 195 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var invariant = __webpack_require__(166);
	var canUseDOM = __webpack_require__(184);
	var getWindowScrollPosition = __webpack_require__(196);

	function shouldUpdateScroll(state, prevState) {
	  if (!prevState) return true;

	  // Don't update scroll position when only the query has changed.
	  if (state.pathname === prevState.pathname) return false;

	  var routes = state.routes;
	  var prevRoutes = prevState.routes;

	  var sharedAncestorRoutes = routes.filter(function (route) {
	    return prevRoutes.indexOf(route) !== -1;
	  });

	  return !sharedAncestorRoutes.some(function (route) {
	    return route.ignoreScrollBehavior;
	  });
	}

	/**
	 * Provides the router with the ability to manage window scroll position
	 * according to its scroll behavior.
	 */
	var ScrollHistory = {

	  statics: {

	    /**
	     * Records curent scroll position as the last known position for the given URL path.
	     */
	    recordScrollPosition: function recordScrollPosition(path) {
	      if (!this.scrollHistory) this.scrollHistory = {};

	      this.scrollHistory[path] = getWindowScrollPosition();
	    },

	    /**
	     * Returns the last known scroll position for the given URL path.
	     */
	    getScrollPosition: function getScrollPosition(path) {
	      if (!this.scrollHistory) this.scrollHistory = {};

	      return this.scrollHistory[path] || null;
	    }

	  },

	  componentWillMount: function componentWillMount() {
	    invariant(this.constructor.getScrollBehavior() == null || canUseDOM, 'Cannot use scroll behavior without a DOM');
	  },

	  componentDidMount: function componentDidMount() {
	    this._updateScroll();
	  },

	  componentDidUpdate: function componentDidUpdate(prevProps, prevState) {
	    this._updateScroll(prevState);
	  },

	  _updateScroll: function _updateScroll(prevState) {
	    if (!shouldUpdateScroll(this.state, prevState)) return;

	    var scrollBehavior = this.constructor.getScrollBehavior();

	    if (scrollBehavior) scrollBehavior.updateScrollPosition(this.constructor.getScrollPosition(this.state.path), this.state.action);
	  }

	};

	module.exports = ScrollHistory;

/***/ },
/* 196 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var invariant = __webpack_require__(166);
	var canUseDOM = __webpack_require__(184);

	/**
	 * Returns the current scroll position of the window as { x, y }.
	 */
	function getWindowScrollPosition() {
	  invariant(canUseDOM, 'Cannot get current scroll position without a DOM');

	  return {
	    x: window.pageXOffset || document.documentElement.scrollLeft,
	    y: window.pageYOffset || document.documentElement.scrollTop
	  };
	}

	module.exports = getWindowScrollPosition;

/***/ },
/* 197 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var React = __webpack_require__(4);

	function isValidChild(object) {
	  return object == null || React.isValidElement(object);
	}

	function isReactChildren(object) {
	  return isValidChild(object) || Array.isArray(object) && object.every(isValidChild);
	}

	module.exports = isReactChildren;

/***/ },
/* 198 */
/***/ function(module, exports, __webpack_require__) {

	/* jshint -W058 */

	'use strict';

	var Cancellation = __webpack_require__(199);
	var Redirect = __webpack_require__(200);

	/**
	 * Encapsulates a transition to a given path.
	 *
	 * The willTransitionTo and willTransitionFrom handlers receive
	 * an instance of this class as their first argument.
	 */
	function Transition(path, retry) {
	  this.path = path;
	  this.abortReason = null;
	  // TODO: Change this to router.retryTransition(transition)
	  this.retry = retry.bind(this);
	}

	Transition.prototype.abort = function (reason) {
	  if (this.abortReason == null) this.abortReason = reason || 'ABORT';
	};

	Transition.prototype.redirect = function (to, params, query) {
	  this.abort(new Redirect(to, params, query));
	};

	Transition.prototype.cancel = function () {
	  this.abort(new Cancellation());
	};

	Transition.from = function (transition, routes, components, callback) {
	  routes.reduce(function (callback, route, index) {
	    return function (error) {
	      if (error || transition.abortReason) {
	        callback(error);
	      } else if (route.onLeave) {
	        try {
	          route.onLeave(transition, components[index], callback);

	          // If there is no callback in the argument list, call it automatically.
	          if (route.onLeave.length < 3) callback();
	        } catch (e) {
	          callback(e);
	        }
	      } else {
	        callback();
	      }
	    };
	  }, callback)();
	};

	Transition.to = function (transition, routes, params, query, callback) {
	  routes.reduceRight(function (callback, route) {
	    return function (error) {
	      if (error || transition.abortReason) {
	        callback(error);
	      } else if (route.onEnter) {
	        try {
	          route.onEnter(transition, params, query, callback);

	          // If there is no callback in the argument list, call it automatically.
	          if (route.onEnter.length < 4) callback();
	        } catch (e) {
	          callback(e);
	        }
	      } else {
	        callback();
	      }
	    };
	  }, callback)();
	};

	module.exports = Transition;

/***/ },
/* 199 */
/***/ function(module, exports) {

	/**
	 * Represents a cancellation caused by navigating away
	 * before the previous transition has fully resolved.
	 */
	"use strict";

	function Cancellation() {}

	module.exports = Cancellation;

/***/ },
/* 200 */
/***/ function(module, exports) {

	/**
	 * Encapsulates a redirect to the given route.
	 */
	"use strict";

	function Redirect(to, params, query) {
	  this.to = to;
	  this.params = params;
	  this.query = query;
	}

	module.exports = Redirect;

/***/ },
/* 201 */
/***/ function(module, exports, __webpack_require__) {

	/* jshint -W084 */
	'use strict';

	var _createClass = (function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ('value' in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; })();

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	var PathUtils = __webpack_require__(168);

	function deepSearch(route, pathname, query) {
	  // Check the subtree first to find the most deeply-nested match.
	  var childRoutes = route.childRoutes;
	  if (childRoutes) {
	    var match, childRoute;
	    for (var i = 0, len = childRoutes.length; i < len; ++i) {
	      childRoute = childRoutes[i];

	      if (childRoute.isDefault || childRoute.isNotFound) continue; // Check these in order later.

	      if (match = deepSearch(childRoute, pathname, query)) {
	        // A route in the subtree matched! Add this route and we're done.
	        match.routes.unshift(route);
	        return match;
	      }
	    }
	  }

	  // No child routes matched; try the default route.
	  var defaultRoute = route.defaultRoute;
	  if (defaultRoute && (params = PathUtils.extractParams(defaultRoute.path, pathname))) return new Match(pathname, params, query, [route, defaultRoute]);

	  // Does the "not found" route match?
	  var notFoundRoute = route.notFoundRoute;
	  if (notFoundRoute && (params = PathUtils.extractParams(notFoundRoute.path, pathname))) return new Match(pathname, params, query, [route, notFoundRoute]);

	  // Last attempt: check this route.
	  var params = PathUtils.extractParams(route.path, pathname);
	  if (params) return new Match(pathname, params, query, [route]);

	  return null;
	}

	var Match = (function () {
	  _createClass(Match, null, [{
	    key: 'findMatch',

	    /**
	     * Attempts to match depth-first a route in the given route's
	     * subtree against the given path and returns the match if it
	     * succeeds, null if no match can be made.
	     */
	    value: function findMatch(routes, path) {
	      var pathname = PathUtils.withoutQuery(path);
	      var query = PathUtils.extractQuery(path);
	      var match = null;

	      for (var i = 0, len = routes.length; match == null && i < len; ++i) match = deepSearch(routes[i], pathname, query);

	      return match;
	    }
	  }]);

	  function Match(pathname, params, query, routes) {
	    _classCallCheck(this, Match);

	    this.pathname = pathname;
	    this.params = params;
	    this.query = query;
	    this.routes = routes;
	  }

	  return Match;
	})();

	module.exports = Match;

/***/ },
/* 202 */
/***/ function(module, exports) {

	'use strict';

	function supportsHistory() {
	  /*! taken from modernizr
	   * https://github.com/Modernizr/Modernizr/blob/master/LICENSE
	   * https://github.com/Modernizr/Modernizr/blob/master/feature-detects/history.js
	   * changed to avoid false negatives for Windows Phones: https://github.com/rackt/react-router/issues/586
	   */
	  var ua = navigator.userAgent;
	  if ((ua.indexOf('Android 2.') !== -1 || ua.indexOf('Android 4.0') !== -1) && ua.indexOf('Mobile Safari') !== -1 && ua.indexOf('Chrome') === -1 && ua.indexOf('Windows Phone') === -1) {
	    return false;
	  }
	  return window.history && 'pushState' in window.history;
	}

	module.exports = supportsHistory;

/***/ },
/* 203 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var createRouter = __webpack_require__(194);

	/**
	 * A high-level convenience method that creates, configures, and
	 * runs a router in one shot. The method signature is:
	 *
	 *   Router.run(routes[, location ], callback);
	 *
	 * Using `window.location.hash` to manage the URL, you could do:
	 *
	 *   Router.run(routes, function (Handler) {
	 *     React.render(<Handler/>, document.body);
	 *   });
	 * 
	 * Using HTML5 history and a custom "cursor" prop:
	 * 
	 *   Router.run(routes, Router.HistoryLocation, function (Handler) {
	 *     React.render(<Handler cursor={cursor}/>, document.body);
	 *   });
	 *
	 * Returns the newly created router.
	 *
	 * Note: If you need to specify further options for your router such
	 * as error/abort handling or custom scroll behavior, use Router.create
	 * instead.
	 *
	 *   var router = Router.create(options);
	 *   router.run(function (Handler) {
	 *     // ...
	 *   });
	 */
	function runRouter(routes, location, callback) {
	  if (typeof location === 'function') {
	    callback = location;
	    location = null;
	  }

	  var router = createRouter({
	    routes: routes,
	    location: location
	  });

	  router.run(callback);

	  return router;
	}

	module.exports = runRouter;

/***/ },
/* 204 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	exports.getHandler = undefined;

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _reactRouter = __webpack_require__(162);

	var _reactRouter2 = _interopRequireDefault(_reactRouter);

	var _Root = __webpack_require__(205);

	var _Root2 = _interopRequireDefault(_Root);

	var _Index = __webpack_require__(212);

	var _Index2 = _interopRequireDefault(_Index);

	var _Introduction = __webpack_require__(317);

	var _Introduction2 = _interopRequireDefault(_Introduction);

	var _Tagging = __webpack_require__(319);

	var _Tagging2 = _interopRequireDefault(_Tagging);

	var _InferringDocumentStructure = __webpack_require__(320);

	var _InferringDocumentStructure2 = _interopRequireDefault(_InferringDocumentStructure);

	var _Dissemination = __webpack_require__(321);

	var _Dissemination2 = _interopRequireDefault(_Dissemination);

	var _chapters = __webpack_require__(217);

	var _chapters2 = _interopRequireDefault(_chapters);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	var Route = _reactRouter2.default.Route;
	var DefaultRoute = _reactRouter2.default.DefaultRoute;

	var Routes = _react2.default.createElement(
	    Route,
	    { handler: _Root2.default, path: '/' },
	    _react2.default.createElement(DefaultRoute, { handler: _Index2.default }),
	    _chapters2.default.inOrder.map(function (chapter) {
	        return _react2.default.createElement(Route, { path: chapter.route, handler: getHandler(chapter.route) });
	    }),
	    ')}'
	);

	function getHandler(route) {
	    switch (route) {
	        case _chapters2.default.introduction.route:
	            return _Introduction2.default;
	        case _chapters2.default.tagging.route:
	            return _Tagging2.default;
	        case _chapters2.default.documentStructure.route:
	            return _InferringDocumentStructure2.default;
	        case _chapters2.default.dissemination.route:
	            return _Dissemination2.default;
	        default:
	            throw Error("No handler for route " + route + ". You should edit Routes.jsx.");
	    }
	}

	exports.getHandler = getHandler;
	exports.default = Routes;

/***/ },
/* 205 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _reactRouter = __webpack_require__(162);

	var _reactRouter2 = _interopRequireDefault(_reactRouter);

	var _Header = __webpack_require__(206);

	var _Header2 = _interopRequireDefault(_Header);

	var _GoogleAnalytics = __webpack_require__(207);

	var _GoogleAnalytics2 = _interopRequireDefault(_GoogleAnalytics);

	var _Bibliography = __webpack_require__(209);

	var _Bibliography2 = _interopRequireDefault(_Bibliography);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }
	// import config from '../../config';


	var RouteHandler = _reactRouter2.default.RouteHandler;

	var Root = function (_React$Component) {
	    _inherits(Root, _React$Component);

	    function Root(props) {
	        _classCallCheck(this, Root);

	        //noinspection UnnecessaryLabelJS

	        var _this = _possibleConstructorReturn(this, Object.getPrototypeOf(Root).call(this, props));

	        title: //noinspection BadExpressionStatementJS
	        props.string;
	        return _this;
	    }

	    _createClass(Root, [{
	        key: 'render',
	        value: function render() {
	            var initialProps = {
	                __html: safeStringify(this.props)
	            };

	            var relativeToRoot = this.props.path.match(/\//g).slice(1).map(function (a) {
	                return "../";
	            }).join("");

	            return _react2.default.createElement(
	                'html',
	                null,
	                _react2.default.createElement(
	                    'head',
	                    null,
	                    _react2.default.createElement('meta', { charSet: 'utf-8' }),
	                    _react2.default.createElement('meta', { httpEquiv: 'X-UA-Compatible', content: 'IE=edge' }),
	                    _react2.default.createElement(
	                        'title',
	                        null,
	                        this.props.title
	                    ),
	                    _react2.default.createElement('meta', { name: 'description', content: this.props.description }),
	                    _react2.default.createElement('meta', { name: 'viewport', content: 'width=device-width, initial-scale=1' }),
	                    _react2.default.createElement('link', { rel: 'apple-touch-icon', href: 'apple-touch-icon.png' }),
	                    _react2.default.createElement('link', { rel: 'stylesheet', href: 'https://fonts.googleapis.com/css?family=Noto+Sans' }),
	                    _react2.default.createElement('link', { rel: 'stylesheet', href: 'https://cdnjs.cloudflare.com/ajax/libs/KaTeX/0.6.0/katex.min.css' }),
	                    _react2.default.createElement('link', { rel: 'stylesheet', href: relativeToRoot + "style.css" })
	                ),
	                _react2.default.createElement(
	                    'body',
	                    { className: 'p2' },
	                    _react2.default.createElement(_Header2.default, this.props),
	                    _react2.default.createElement(RouteHandler, this.props),
	                    _react2.default.createElement(_Bibliography2.default, null),
	                    _react2.default.createElement('script', {
	                        id: 'initial-props',
	                        type: 'application/json',
	                        dangerouslySetInnerHTML: initialProps }),
	                    _react2.default.createElement('script', { src: relativeToRoot + 'bundle.js' }),
	                    _react2.default.createElement(_GoogleAnalytics2.default, null)
	                )
	            );
	        }
	    }]);

	    return Root;
	}(_react2.default.Component);
	// <Footer/> //TODO

	// Copy all properties except webpackStats (added in by Webpack), which is huge,
	// has circular references and will not be used by the React components anyways.
	// Make regex replacements to not break the HTML as well.


	function safeStringify(obj) {
	    var objNoStats = {};
	    for (var key in obj) {
	        if (obj.hasOwnProperty(key) && key != "webpackStats") {
	            objNoStats[key] = obj[key];
	        }
	    }
	    return JSON.stringify(objNoStats).replace(/<\/script/g, '<\\/script').replace(/<!--/g, '<\\!--');
	}
	Root.propTypes = {
	    path: _react2.default.PropTypes.string.isRequired
	};
	exports.default = Root;
	// Html.propTypes = {
	//     title: PropTypes.string,
	//     description: PropTypes.string,
	//     body: PropTypes.string.isRequired,
	//     debug: PropTypes.bool.isRequired,
	// };
	//
	// export default Html;

/***/ },
/* 206 */
/***/ function(module, exports, __webpack_require__) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var Header = function (_React$Component) {
	    _inherits(Header, _React$Component);

	    function Header(props) {
	        _classCallCheck(this, Header);

	        var _this = _possibleConstructorReturn(this, Object.getPrototypeOf(Header).call(this, props));

	        title: props.string;
	        return _this;
	    }

	    _createClass(Header, [{
	        key: "render",
	        value: function render() {
	            var relativeToRoot = this.props.path.match(/\//g).slice(1).map(function (a) {
	                return "../";
	            }).join("");

	            return _react2.default.createElement(
	                "header",
	                { className: "py2" },
	                _react2.default.createElement(
	                    "h1",
	                    { className: "mt0" },
	                    this.props.title
	                )
	            );
	        }
	    }]);

	    return Header;
	}(_react2.default.Component);

	exports.default = Header;

/***/ },
/* 207 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _config = __webpack_require__(208);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	var trackingCode = { __html: '(function(b,o,i,l,e,r){b.GoogleAnalyticsObject=l;b[l]||(b[l]=' + 'function(){(b[l].q=b[l].q||[]).push(arguments)});b[l].l=+new Date;' + 'e=o.createElement(i);r=o.getElementsByTagName(i)[0];' + 'e.src=\'https://www.google-analytics.com/analytics.js\';' + 'r.parentNode.insertBefore(e,r)}(window,document,\'script\',\'ga\'));' + ('ga(\'create\',\'' + _config.googleAnalyticsId + '\',\'auto\');')
	};

	function GoogleAnalytics() {
	  return _react2.default.createElement('script', { dangerouslySetInnerHTML: trackingCode });
	}

	exports.default = GoogleAnalytics;

/***/ },
/* 208 */
/***/ function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	var googleAnalyticsId = "";
	exports.googleAnalyticsId = googleAnalyticsId;

/***/ },
/* 209 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _underscore = __webpack_require__(210);

	var _underscore2 = _interopRequireDefault(_underscore);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var Bibl = function (_Component) {
	  _inherits(Bibl, _Component);

	  function Bibl() {
	    _classCallCheck(this, Bibl);

	    return _possibleConstructorReturn(this, Object.getPrototypeOf(Bibl).apply(this, arguments));
	  }

	  _createClass(Bibl, [{
	    key: 'render',
	    value: function render() {
	      return _react2.default.createElement(
	        'section',
	        { id: 'bibliography' },
	        _react2.default.createElement(
	          'h2',
	          null,
	          'References'
	        ),
	        _react2.default.createElement(
	          'ol',
	          { id: 'reference-list' },
	          _underscore2.default.map(_bib2.default, function (citation, i) {
	            var nameStr = citation.author.full;
	            if (!citation.author.full) {
	              nameStr = citation.author.lastName + (citation.author.firstName ? ", " + citation.author.firstName : "");
	            }
	            if (nameStr) {
	              nameStr = _react2.default.createElement(
	                'span',
	                { className: 'ref-author' },
	                nameStr
	              );
	            }
	            var publication = citation.journal ? _react2.default.createElement(
	              'span',
	              null,
	              _react2.default.createElement(
	                'span',
	                { className: 'ref-journal' },
	                citation.journal
	              ),
	              '.'
	            ) : "";

	            return _react2.default.createElement(
	              'li',
	              { itemProp: 'citation',
	                itemScope: true,
	                itemType: 'http://schema.org/CreativeWork',
	                key: i,
	                id: citation.id.toString(),
	                className: 'ref' },
	              nameStr,
	              ' (',
	              _react2.default.createElement(
	                'time',
	                { dateTime: citation.year, className: 'ref-year' },
	                citation.year
	              ),
	              '). ',
	              _react2.default.createElement(
	                'cite',
	                null,
	                _react2.default.createElement(
	                  'a',
	                  { href: citation.href },
	                  _react2.default.createElement(
	                    'span',
	                    { itemProp: 'name' },
	                    citation.title
	                  )
	                )
	              ),
	              '. ',
	              publication
	            );
	          })
	        )
	      );
	    }
	  }]);

	  return Bibl;
	}(_react.Component);

	exports.default = Bibl;

/***/ },
/* 210 */
/***/ function(module, exports, __webpack_require__) {

	var __WEBPACK_AMD_DEFINE_ARRAY__, __WEBPACK_AMD_DEFINE_RESULT__;//     Underscore.js 1.8.3
	//     http://underscorejs.org
	//     (c) 2009-2015 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
	//     Underscore may be freely distributed under the MIT license.

	(function() {

	  // Baseline setup
	  // --------------

	  // Establish the root object, `window` in the browser, or `exports` on the server.
	  var root = this;

	  // Save the previous value of the `_` variable.
	  var previousUnderscore = root._;

	  // Save bytes in the minified (but not gzipped) version:
	  var ArrayProto = Array.prototype, ObjProto = Object.prototype, FuncProto = Function.prototype;

	  // Create quick reference variables for speed access to core prototypes.
	  var
	    push             = ArrayProto.push,
	    slice            = ArrayProto.slice,
	    toString         = ObjProto.toString,
	    hasOwnProperty   = ObjProto.hasOwnProperty;

	  // All **ECMAScript 5** native function implementations that we hope to use
	  // are declared here.
	  var
	    nativeIsArray      = Array.isArray,
	    nativeKeys         = Object.keys,
	    nativeBind         = FuncProto.bind,
	    nativeCreate       = Object.create;

	  // Naked function reference for surrogate-prototype-swapping.
	  var Ctor = function(){};

	  // Create a safe reference to the Underscore object for use below.
	  var _ = function(obj) {
	    if (obj instanceof _) return obj;
	    if (!(this instanceof _)) return new _(obj);
	    this._wrapped = obj;
	  };

	  // Export the Underscore object for **Node.js**, with
	  // backwards-compatibility for the old `require()` API. If we're in
	  // the browser, add `_` as a global object.
	  if (true) {
	    if (typeof module !== 'undefined' && module.exports) {
	      exports = module.exports = _;
	    }
	    exports._ = _;
	  } else {
	    root._ = _;
	  }

	  // Current version.
	  _.VERSION = '1.8.3';

	  // Internal function that returns an efficient (for current engines) version
	  // of the passed-in callback, to be repeatedly applied in other Underscore
	  // functions.
	  var optimizeCb = function(func, context, argCount) {
	    if (context === void 0) return func;
	    switch (argCount == null ? 3 : argCount) {
	      case 1: return function(value) {
	        return func.call(context, value);
	      };
	      case 2: return function(value, other) {
	        return func.call(context, value, other);
	      };
	      case 3: return function(value, index, collection) {
	        return func.call(context, value, index, collection);
	      };
	      case 4: return function(accumulator, value, index, collection) {
	        return func.call(context, accumulator, value, index, collection);
	      };
	    }
	    return function() {
	      return func.apply(context, arguments);
	    };
	  };

	  // A mostly-internal function to generate callbacks that can be applied
	  // to each element in a collection, returning the desired result — either
	  // identity, an arbitrary callback, a property matcher, or a property accessor.
	  var cb = function(value, context, argCount) {
	    if (value == null) return _.identity;
	    if (_.isFunction(value)) return optimizeCb(value, context, argCount);
	    if (_.isObject(value)) return _.matcher(value);
	    return _.property(value);
	  };
	  _.iteratee = function(value, context) {
	    return cb(value, context, Infinity);
	  };

	  // An internal function for creating assigner functions.
	  var createAssigner = function(keysFunc, undefinedOnly) {
	    return function(obj) {
	      var length = arguments.length;
	      if (length < 2 || obj == null) return obj;
	      for (var index = 1; index < length; index++) {
	        var source = arguments[index],
	            keys = keysFunc(source),
	            l = keys.length;
	        for (var i = 0; i < l; i++) {
	          var key = keys[i];
	          if (!undefinedOnly || obj[key] === void 0) obj[key] = source[key];
	        }
	      }
	      return obj;
	    };
	  };

	  // An internal function for creating a new object that inherits from another.
	  var baseCreate = function(prototype) {
	    if (!_.isObject(prototype)) return {};
	    if (nativeCreate) return nativeCreate(prototype);
	    Ctor.prototype = prototype;
	    var result = new Ctor;
	    Ctor.prototype = null;
	    return result;
	  };

	  var property = function(key) {
	    return function(obj) {
	      return obj == null ? void 0 : obj[key];
	    };
	  };

	  // Helper for collection methods to determine whether a collection
	  // should be iterated as an array or as an object
	  // Related: http://people.mozilla.org/~jorendorff/es6-draft.html#sec-tolength
	  // Avoids a very nasty iOS 8 JIT bug on ARM-64. #2094
	  var MAX_ARRAY_INDEX = Math.pow(2, 53) - 1;
	  var getLength = property('length');
	  var isArrayLike = function(collection) {
	    var length = getLength(collection);
	    return typeof length == 'number' && length >= 0 && length <= MAX_ARRAY_INDEX;
	  };

	  // Collection Functions
	  // --------------------

	  // The cornerstone, an `each` implementation, aka `forEach`.
	  // Handles raw objects in addition to array-likes. Treats all
	  // sparse array-likes as if they were dense.
	  _.each = _.forEach = function(obj, iteratee, context) {
	    iteratee = optimizeCb(iteratee, context);
	    var i, length;
	    if (isArrayLike(obj)) {
	      for (i = 0, length = obj.length; i < length; i++) {
	        iteratee(obj[i], i, obj);
	      }
	    } else {
	      var keys = _.keys(obj);
	      for (i = 0, length = keys.length; i < length; i++) {
	        iteratee(obj[keys[i]], keys[i], obj);
	      }
	    }
	    return obj;
	  };

	  // Return the results of applying the iteratee to each element.
	  _.map = _.collect = function(obj, iteratee, context) {
	    iteratee = cb(iteratee, context);
	    var keys = !isArrayLike(obj) && _.keys(obj),
	        length = (keys || obj).length,
	        results = Array(length);
	    for (var index = 0; index < length; index++) {
	      var currentKey = keys ? keys[index] : index;
	      results[index] = iteratee(obj[currentKey], currentKey, obj);
	    }
	    return results;
	  };

	  // Create a reducing function iterating left or right.
	  function createReduce(dir) {
	    // Optimized iterator function as using arguments.length
	    // in the main function will deoptimize the, see #1991.
	    function iterator(obj, iteratee, memo, keys, index, length) {
	      for (; index >= 0 && index < length; index += dir) {
	        var currentKey = keys ? keys[index] : index;
	        memo = iteratee(memo, obj[currentKey], currentKey, obj);
	      }
	      return memo;
	    }

	    return function(obj, iteratee, memo, context) {
	      iteratee = optimizeCb(iteratee, context, 4);
	      var keys = !isArrayLike(obj) && _.keys(obj),
	          length = (keys || obj).length,
	          index = dir > 0 ? 0 : length - 1;
	      // Determine the initial value if none is provided.
	      if (arguments.length < 3) {
	        memo = obj[keys ? keys[index] : index];
	        index += dir;
	      }
	      return iterator(obj, iteratee, memo, keys, index, length);
	    };
	  }

	  // **Reduce** builds up a single result from a list of values, aka `inject`,
	  // or `foldl`.
	  _.reduce = _.foldl = _.inject = createReduce(1);

	  // The right-associative version of reduce, also known as `foldr`.
	  _.reduceRight = _.foldr = createReduce(-1);

	  // Return the first value which passes a truth test. Aliased as `detect`.
	  _.find = _.detect = function(obj, predicate, context) {
	    var key;
	    if (isArrayLike(obj)) {
	      key = _.findIndex(obj, predicate, context);
	    } else {
	      key = _.findKey(obj, predicate, context);
	    }
	    if (key !== void 0 && key !== -1) return obj[key];
	  };

	  // Return all the elements that pass a truth test.
	  // Aliased as `select`.
	  _.filter = _.select = function(obj, predicate, context) {
	    var results = [];
	    predicate = cb(predicate, context);
	    _.each(obj, function(value, index, list) {
	      if (predicate(value, index, list)) results.push(value);
	    });
	    return results;
	  };

	  // Return all the elements for which a truth test fails.
	  _.reject = function(obj, predicate, context) {
	    return _.filter(obj, _.negate(cb(predicate)), context);
	  };

	  // Determine whether all of the elements match a truth test.
	  // Aliased as `all`.
	  _.every = _.all = function(obj, predicate, context) {
	    predicate = cb(predicate, context);
	    var keys = !isArrayLike(obj) && _.keys(obj),
	        length = (keys || obj).length;
	    for (var index = 0; index < length; index++) {
	      var currentKey = keys ? keys[index] : index;
	      if (!predicate(obj[currentKey], currentKey, obj)) return false;
	    }
	    return true;
	  };

	  // Determine if at least one element in the object matches a truth test.
	  // Aliased as `any`.
	  _.some = _.any = function(obj, predicate, context) {
	    predicate = cb(predicate, context);
	    var keys = !isArrayLike(obj) && _.keys(obj),
	        length = (keys || obj).length;
	    for (var index = 0; index < length; index++) {
	      var currentKey = keys ? keys[index] : index;
	      if (predicate(obj[currentKey], currentKey, obj)) return true;
	    }
	    return false;
	  };

	  // Determine if the array or object contains a given item (using `===`).
	  // Aliased as `includes` and `include`.
	  _.contains = _.includes = _.include = function(obj, item, fromIndex, guard) {
	    if (!isArrayLike(obj)) obj = _.values(obj);
	    if (typeof fromIndex != 'number' || guard) fromIndex = 0;
	    return _.indexOf(obj, item, fromIndex) >= 0;
	  };

	  // Invoke a method (with arguments) on every item in a collection.
	  _.invoke = function(obj, method) {
	    var args = slice.call(arguments, 2);
	    var isFunc = _.isFunction(method);
	    return _.map(obj, function(value) {
	      var func = isFunc ? method : value[method];
	      return func == null ? func : func.apply(value, args);
	    });
	  };

	  // Convenience version of a common use case of `map`: fetching a property.
	  _.pluck = function(obj, key) {
	    return _.map(obj, _.property(key));
	  };

	  // Convenience version of a common use case of `filter`: selecting only objects
	  // containing specific `key:value` pairs.
	  _.where = function(obj, attrs) {
	    return _.filter(obj, _.matcher(attrs));
	  };

	  // Convenience version of a common use case of `find`: getting the first object
	  // containing specific `key:value` pairs.
	  _.findWhere = function(obj, attrs) {
	    return _.find(obj, _.matcher(attrs));
	  };

	  // Return the maximum element (or element-based computation).
	  _.max = function(obj, iteratee, context) {
	    var result = -Infinity, lastComputed = -Infinity,
	        value, computed;
	    if (iteratee == null && obj != null) {
	      obj = isArrayLike(obj) ? obj : _.values(obj);
	      for (var i = 0, length = obj.length; i < length; i++) {
	        value = obj[i];
	        if (value > result) {
	          result = value;
	        }
	      }
	    } else {
	      iteratee = cb(iteratee, context);
	      _.each(obj, function(value, index, list) {
	        computed = iteratee(value, index, list);
	        if (computed > lastComputed || computed === -Infinity && result === -Infinity) {
	          result = value;
	          lastComputed = computed;
	        }
	      });
	    }
	    return result;
	  };

	  // Return the minimum element (or element-based computation).
	  _.min = function(obj, iteratee, context) {
	    var result = Infinity, lastComputed = Infinity,
	        value, computed;
	    if (iteratee == null && obj != null) {
	      obj = isArrayLike(obj) ? obj : _.values(obj);
	      for (var i = 0, length = obj.length; i < length; i++) {
	        value = obj[i];
	        if (value < result) {
	          result = value;
	        }
	      }
	    } else {
	      iteratee = cb(iteratee, context);
	      _.each(obj, function(value, index, list) {
	        computed = iteratee(value, index, list);
	        if (computed < lastComputed || computed === Infinity && result === Infinity) {
	          result = value;
	          lastComputed = computed;
	        }
	      });
	    }
	    return result;
	  };

	  // Shuffle a collection, using the modern version of the
	  // [Fisher-Yates shuffle](http://en.wikipedia.org/wiki/Fisher–Yates_shuffle).
	  _.shuffle = function(obj) {
	    var set = isArrayLike(obj) ? obj : _.values(obj);
	    var length = set.length;
	    var shuffled = Array(length);
	    for (var index = 0, rand; index < length; index++) {
	      rand = _.random(0, index);
	      if (rand !== index) shuffled[index] = shuffled[rand];
	      shuffled[rand] = set[index];
	    }
	    return shuffled;
	  };

	  // Sample **n** random values from a collection.
	  // If **n** is not specified, returns a single random element.
	  // The internal `guard` argument allows it to work with `map`.
	  _.sample = function(obj, n, guard) {
	    if (n == null || guard) {
	      if (!isArrayLike(obj)) obj = _.values(obj);
	      return obj[_.random(obj.length - 1)];
	    }
	    return _.shuffle(obj).slice(0, Math.max(0, n));
	  };

	  // Sort the object's values by a criterion produced by an iteratee.
	  _.sortBy = function(obj, iteratee, context) {
	    iteratee = cb(iteratee, context);
	    return _.pluck(_.map(obj, function(value, index, list) {
	      return {
	        value: value,
	        index: index,
	        criteria: iteratee(value, index, list)
	      };
	    }).sort(function(left, right) {
	      var a = left.criteria;
	      var b = right.criteria;
	      if (a !== b) {
	        if (a > b || a === void 0) return 1;
	        if (a < b || b === void 0) return -1;
	      }
	      return left.index - right.index;
	    }), 'value');
	  };

	  // An internal function used for aggregate "group by" operations.
	  var group = function(behavior) {
	    return function(obj, iteratee, context) {
	      var result = {};
	      iteratee = cb(iteratee, context);
	      _.each(obj, function(value, index) {
	        var key = iteratee(value, index, obj);
	        behavior(result, value, key);
	      });
	      return result;
	    };
	  };

	  // Groups the object's values by a criterion. Pass either a string attribute
	  // to group by, or a function that returns the criterion.
	  _.groupBy = group(function(result, value, key) {
	    if (_.has(result, key)) result[key].push(value); else result[key] = [value];
	  });

	  // Indexes the object's values by a criterion, similar to `groupBy`, but for
	  // when you know that your index values will be unique.
	  _.indexBy = group(function(result, value, key) {
	    result[key] = value;
	  });

	  // Counts instances of an object that group by a certain criterion. Pass
	  // either a string attribute to count by, or a function that returns the
	  // criterion.
	  _.countBy = group(function(result, value, key) {
	    if (_.has(result, key)) result[key]++; else result[key] = 1;
	  });

	  // Safely create a real, live array from anything iterable.
	  _.toArray = function(obj) {
	    if (!obj) return [];
	    if (_.isArray(obj)) return slice.call(obj);
	    if (isArrayLike(obj)) return _.map(obj, _.identity);
	    return _.values(obj);
	  };

	  // Return the number of elements in an object.
	  _.size = function(obj) {
	    if (obj == null) return 0;
	    return isArrayLike(obj) ? obj.length : _.keys(obj).length;
	  };

	  // Split a collection into two arrays: one whose elements all satisfy the given
	  // predicate, and one whose elements all do not satisfy the predicate.
	  _.partition = function(obj, predicate, context) {
	    predicate = cb(predicate, context);
	    var pass = [], fail = [];
	    _.each(obj, function(value, key, obj) {
	      (predicate(value, key, obj) ? pass : fail).push(value);
	    });
	    return [pass, fail];
	  };

	  // Array Functions
	  // ---------------

	  // Get the first element of an array. Passing **n** will return the first N
	  // values in the array. Aliased as `head` and `take`. The **guard** check
	  // allows it to work with `_.map`.
	  _.first = _.head = _.take = function(array, n, guard) {
	    if (array == null) return void 0;
	    if (n == null || guard) return array[0];
	    return _.initial(array, array.length - n);
	  };

	  // Returns everything but the last entry of the array. Especially useful on
	  // the arguments object. Passing **n** will return all the values in
	  // the array, excluding the last N.
	  _.initial = function(array, n, guard) {
	    return slice.call(array, 0, Math.max(0, array.length - (n == null || guard ? 1 : n)));
	  };

	  // Get the last element of an array. Passing **n** will return the last N
	  // values in the array.
	  _.last = function(array, n, guard) {
	    if (array == null) return void 0;
	    if (n == null || guard) return array[array.length - 1];
	    return _.rest(array, Math.max(0, array.length - n));
	  };

	  // Returns everything but the first entry of the array. Aliased as `tail` and `drop`.
	  // Especially useful on the arguments object. Passing an **n** will return
	  // the rest N values in the array.
	  _.rest = _.tail = _.drop = function(array, n, guard) {
	    return slice.call(array, n == null || guard ? 1 : n);
	  };

	  // Trim out all falsy values from an array.
	  _.compact = function(array) {
	    return _.filter(array, _.identity);
	  };

	  // Internal implementation of a recursive `flatten` function.
	  var flatten = function(input, shallow, strict, startIndex) {
	    var output = [], idx = 0;
	    for (var i = startIndex || 0, length = getLength(input); i < length; i++) {
	      var value = input[i];
	      if (isArrayLike(value) && (_.isArray(value) || _.isArguments(value))) {
	        //flatten current level of array or arguments object
	        if (!shallow) value = flatten(value, shallow, strict);
	        var j = 0, len = value.length;
	        output.length += len;
	        while (j < len) {
	          output[idx++] = value[j++];
	        }
	      } else if (!strict) {
	        output[idx++] = value;
	      }
	    }
	    return output;
	  };

	  // Flatten out an array, either recursively (by default), or just one level.
	  _.flatten = function(array, shallow) {
	    return flatten(array, shallow, false);
	  };

	  // Return a version of the array that does not contain the specified value(s).
	  _.without = function(array) {
	    return _.difference(array, slice.call(arguments, 1));
	  };

	  // Produce a duplicate-free version of the array. If the array has already
	  // been sorted, you have the option of using a faster algorithm.
	  // Aliased as `unique`.
	  _.uniq = _.unique = function(array, isSorted, iteratee, context) {
	    if (!_.isBoolean(isSorted)) {
	      context = iteratee;
	      iteratee = isSorted;
	      isSorted = false;
	    }
	    if (iteratee != null) iteratee = cb(iteratee, context);
	    var result = [];
	    var seen = [];
	    for (var i = 0, length = getLength(array); i < length; i++) {
	      var value = array[i],
	          computed = iteratee ? iteratee(value, i, array) : value;
	      if (isSorted) {
	        if (!i || seen !== computed) result.push(value);
	        seen = computed;
	      } else if (iteratee) {
	        if (!_.contains(seen, computed)) {
	          seen.push(computed);
	          result.push(value);
	        }
	      } else if (!_.contains(result, value)) {
	        result.push(value);
	      }
	    }
	    return result;
	  };

	  // Produce an array that contains the union: each distinct element from all of
	  // the passed-in arrays.
	  _.union = function() {
	    return _.uniq(flatten(arguments, true, true));
	  };

	  // Produce an array that contains every item shared between all the
	  // passed-in arrays.
	  _.intersection = function(array) {
	    var result = [];
	    var argsLength = arguments.length;
	    for (var i = 0, length = getLength(array); i < length; i++) {
	      var item = array[i];
	      if (_.contains(result, item)) continue;
	      for (var j = 1; j < argsLength; j++) {
	        if (!_.contains(arguments[j], item)) break;
	      }
	      if (j === argsLength) result.push(item);
	    }
	    return result;
	  };

	  // Take the difference between one array and a number of other arrays.
	  // Only the elements present in just the first array will remain.
	  _.difference = function(array) {
	    var rest = flatten(arguments, true, true, 1);
	    return _.filter(array, function(value){
	      return !_.contains(rest, value);
	    });
	  };

	  // Zip together multiple lists into a single array -- elements that share
	  // an index go together.
	  _.zip = function() {
	    return _.unzip(arguments);
	  };

	  // Complement of _.zip. Unzip accepts an array of arrays and groups
	  // each array's elements on shared indices
	  _.unzip = function(array) {
	    var length = array && _.max(array, getLength).length || 0;
	    var result = Array(length);

	    for (var index = 0; index < length; index++) {
	      result[index] = _.pluck(array, index);
	    }
	    return result;
	  };

	  // Converts lists into objects. Pass either a single array of `[key, value]`
	  // pairs, or two parallel arrays of the same length -- one of keys, and one of
	  // the corresponding values.
	  _.object = function(list, values) {
	    var result = {};
	    for (var i = 0, length = getLength(list); i < length; i++) {
	      if (values) {
	        result[list[i]] = values[i];
	      } else {
	        result[list[i][0]] = list[i][1];
	      }
	    }
	    return result;
	  };

	  // Generator function to create the findIndex and findLastIndex functions
	  function createPredicateIndexFinder(dir) {
	    return function(array, predicate, context) {
	      predicate = cb(predicate, context);
	      var length = getLength(array);
	      var index = dir > 0 ? 0 : length - 1;
	      for (; index >= 0 && index < length; index += dir) {
	        if (predicate(array[index], index, array)) return index;
	      }
	      return -1;
	    };
	  }

	  // Returns the first index on an array-like that passes a predicate test
	  _.findIndex = createPredicateIndexFinder(1);
	  _.findLastIndex = createPredicateIndexFinder(-1);

	  // Use a comparator function to figure out the smallest index at which
	  // an object should be inserted so as to maintain order. Uses binary search.
	  _.sortedIndex = function(array, obj, iteratee, context) {
	    iteratee = cb(iteratee, context, 1);
	    var value = iteratee(obj);
	    var low = 0, high = getLength(array);
	    while (low < high) {
	      var mid = Math.floor((low + high) / 2);
	      if (iteratee(array[mid]) < value) low = mid + 1; else high = mid;
	    }
	    return low;
	  };

	  // Generator function to create the indexOf and lastIndexOf functions
	  function createIndexFinder(dir, predicateFind, sortedIndex) {
	    return function(array, item, idx) {
	      var i = 0, length = getLength(array);
	      if (typeof idx == 'number') {
	        if (dir > 0) {
	            i = idx >= 0 ? idx : Math.max(idx + length, i);
	        } else {
	            length = idx >= 0 ? Math.min(idx + 1, length) : idx + length + 1;
	        }
	      } else if (sortedIndex && idx && length) {
	        idx = sortedIndex(array, item);
	        return array[idx] === item ? idx : -1;
	      }
	      if (item !== item) {
	        idx = predicateFind(slice.call(array, i, length), _.isNaN);
	        return idx >= 0 ? idx + i : -1;
	      }
	      for (idx = dir > 0 ? i : length - 1; idx >= 0 && idx < length; idx += dir) {
	        if (array[idx] === item) return idx;
	      }
	      return -1;
	    };
	  }

	  // Return the position of the first occurrence of an item in an array,
	  // or -1 if the item is not included in the array.
	  // If the array is large and already in sort order, pass `true`
	  // for **isSorted** to use binary search.
	  _.indexOf = createIndexFinder(1, _.findIndex, _.sortedIndex);
	  _.lastIndexOf = createIndexFinder(-1, _.findLastIndex);

	  // Generate an integer Array containing an arithmetic progression. A port of
	  // the native Python `range()` function. See
	  // [the Python documentation](http://docs.python.org/library/functions.html#range).
	  _.range = function(start, stop, step) {
	    if (stop == null) {
	      stop = start || 0;
	      start = 0;
	    }
	    step = step || 1;

	    var length = Math.max(Math.ceil((stop - start) / step), 0);
	    var range = Array(length);

	    for (var idx = 0; idx < length; idx++, start += step) {
	      range[idx] = start;
	    }

	    return range;
	  };

	  // Function (ahem) Functions
	  // ------------------

	  // Determines whether to execute a function as a constructor
	  // or a normal function with the provided arguments
	  var executeBound = function(sourceFunc, boundFunc, context, callingContext, args) {
	    if (!(callingContext instanceof boundFunc)) return sourceFunc.apply(context, args);
	    var self = baseCreate(sourceFunc.prototype);
	    var result = sourceFunc.apply(self, args);
	    if (_.isObject(result)) return result;
	    return self;
	  };

	  // Create a function bound to a given object (assigning `this`, and arguments,
	  // optionally). Delegates to **ECMAScript 5**'s native `Function.bind` if
	  // available.
	  _.bind = function(func, context) {
	    if (nativeBind && func.bind === nativeBind) return nativeBind.apply(func, slice.call(arguments, 1));
	    if (!_.isFunction(func)) throw new TypeError('Bind must be called on a function');
	    var args = slice.call(arguments, 2);
	    var bound = function() {
	      return executeBound(func, bound, context, this, args.concat(slice.call(arguments)));
	    };
	    return bound;
	  };

	  // Partially apply a function by creating a version that has had some of its
	  // arguments pre-filled, without changing its dynamic `this` context. _ acts
	  // as a placeholder, allowing any combination of arguments to be pre-filled.
	  _.partial = function(func) {
	    var boundArgs = slice.call(arguments, 1);
	    var bound = function() {
	      var position = 0, length = boundArgs.length;
	      var args = Array(length);
	      for (var i = 0; i < length; i++) {
	        args[i] = boundArgs[i] === _ ? arguments[position++] : boundArgs[i];
	      }
	      while (position < arguments.length) args.push(arguments[position++]);
	      return executeBound(func, bound, this, this, args);
	    };
	    return bound;
	  };

	  // Bind a number of an object's methods to that object. Remaining arguments
	  // are the method names to be bound. Useful for ensuring that all callbacks
	  // defined on an object belong to it.
	  _.bindAll = function(obj) {
	    var i, length = arguments.length, key;
	    if (length <= 1) throw new Error('bindAll must be passed function names');
	    for (i = 1; i < length; i++) {
	      key = arguments[i];
	      obj[key] = _.bind(obj[key], obj);
	    }
	    return obj;
	  };

	  // Memoize an expensive function by storing its results.
	  _.memoize = function(func, hasher) {
	    var memoize = function(key) {
	      var cache = memoize.cache;
	      var address = '' + (hasher ? hasher.apply(this, arguments) : key);
	      if (!_.has(cache, address)) cache[address] = func.apply(this, arguments);
	      return cache[address];
	    };
	    memoize.cache = {};
	    return memoize;
	  };

	  // Delays a function for the given number of milliseconds, and then calls
	  // it with the arguments supplied.
	  _.delay = function(func, wait) {
	    var args = slice.call(arguments, 2);
	    return setTimeout(function(){
	      return func.apply(null, args);
	    }, wait);
	  };

	  // Defers a function, scheduling it to run after the current call stack has
	  // cleared.
	  _.defer = _.partial(_.delay, _, 1);

	  // Returns a function, that, when invoked, will only be triggered at most once
	  // during a given window of time. Normally, the throttled function will run
	  // as much as it can, without ever going more than once per `wait` duration;
	  // but if you'd like to disable the execution on the leading edge, pass
	  // `{leading: false}`. To disable execution on the trailing edge, ditto.
	  _.throttle = function(func, wait, options) {
	    var context, args, result;
	    var timeout = null;
	    var previous = 0;
	    if (!options) options = {};
	    var later = function() {
	      previous = options.leading === false ? 0 : _.now();
	      timeout = null;
	      result = func.apply(context, args);
	      if (!timeout) context = args = null;
	    };
	    return function() {
	      var now = _.now();
	      if (!previous && options.leading === false) previous = now;
	      var remaining = wait - (now - previous);
	      context = this;
	      args = arguments;
	      if (remaining <= 0 || remaining > wait) {
	        if (timeout) {
	          clearTimeout(timeout);
	          timeout = null;
	        }
	        previous = now;
	        result = func.apply(context, args);
	        if (!timeout) context = args = null;
	      } else if (!timeout && options.trailing !== false) {
	        timeout = setTimeout(later, remaining);
	      }
	      return result;
	    };
	  };

	  // Returns a function, that, as long as it continues to be invoked, will not
	  // be triggered. The function will be called after it stops being called for
	  // N milliseconds. If `immediate` is passed, trigger the function on the
	  // leading edge, instead of the trailing.
	  _.debounce = function(func, wait, immediate) {
	    var timeout, args, context, timestamp, result;

	    var later = function() {
	      var last = _.now() - timestamp;

	      if (last < wait && last >= 0) {
	        timeout = setTimeout(later, wait - last);
	      } else {
	        timeout = null;
	        if (!immediate) {
	          result = func.apply(context, args);
	          if (!timeout) context = args = null;
	        }
	      }
	    };

	    return function() {
	      context = this;
	      args = arguments;
	      timestamp = _.now();
	      var callNow = immediate && !timeout;
	      if (!timeout) timeout = setTimeout(later, wait);
	      if (callNow) {
	        result = func.apply(context, args);
	        context = args = null;
	      }

	      return result;
	    };
	  };

	  // Returns the first function passed as an argument to the second,
	  // allowing you to adjust arguments, run code before and after, and
	  // conditionally execute the original function.
	  _.wrap = function(func, wrapper) {
	    return _.partial(wrapper, func);
	  };

	  // Returns a negated version of the passed-in predicate.
	  _.negate = function(predicate) {
	    return function() {
	      return !predicate.apply(this, arguments);
	    };
	  };

	  // Returns a function that is the composition of a list of functions, each
	  // consuming the return value of the function that follows.
	  _.compose = function() {
	    var args = arguments;
	    var start = args.length - 1;
	    return function() {
	      var i = start;
	      var result = args[start].apply(this, arguments);
	      while (i--) result = args[i].call(this, result);
	      return result;
	    };
	  };

	  // Returns a function that will only be executed on and after the Nth call.
	  _.after = function(times, func) {
	    return function() {
	      if (--times < 1) {
	        return func.apply(this, arguments);
	      }
	    };
	  };

	  // Returns a function that will only be executed up to (but not including) the Nth call.
	  _.before = function(times, func) {
	    var memo;
	    return function() {
	      if (--times > 0) {
	        memo = func.apply(this, arguments);
	      }
	      if (times <= 1) func = null;
	      return memo;
	    };
	  };

	  // Returns a function that will be executed at most one time, no matter how
	  // often you call it. Useful for lazy initialization.
	  _.once = _.partial(_.before, 2);

	  // Object Functions
	  // ----------------

	  // Keys in IE < 9 that won't be iterated by `for key in ...` and thus missed.
	  var hasEnumBug = !{toString: null}.propertyIsEnumerable('toString');
	  var nonEnumerableProps = ['valueOf', 'isPrototypeOf', 'toString',
	                      'propertyIsEnumerable', 'hasOwnProperty', 'toLocaleString'];

	  function collectNonEnumProps(obj, keys) {
	    var nonEnumIdx = nonEnumerableProps.length;
	    var constructor = obj.constructor;
	    var proto = (_.isFunction(constructor) && constructor.prototype) || ObjProto;

	    // Constructor is a special case.
	    var prop = 'constructor';
	    if (_.has(obj, prop) && !_.contains(keys, prop)) keys.push(prop);

	    while (nonEnumIdx--) {
	      prop = nonEnumerableProps[nonEnumIdx];
	      if (prop in obj && obj[prop] !== proto[prop] && !_.contains(keys, prop)) {
	        keys.push(prop);
	      }
	    }
	  }

	  // Retrieve the names of an object's own properties.
	  // Delegates to **ECMAScript 5**'s native `Object.keys`
	  _.keys = function(obj) {
	    if (!_.isObject(obj)) return [];
	    if (nativeKeys) return nativeKeys(obj);
	    var keys = [];
	    for (var key in obj) if (_.has(obj, key)) keys.push(key);
	    // Ahem, IE < 9.
	    if (hasEnumBug) collectNonEnumProps(obj, keys);
	    return keys;
	  };

	  // Retrieve all the property names of an object.
	  _.allKeys = function(obj) {
	    if (!_.isObject(obj)) return [];
	    var keys = [];
	    for (var key in obj) keys.push(key);
	    // Ahem, IE < 9.
	    if (hasEnumBug) collectNonEnumProps(obj, keys);
	    return keys;
	  };

	  // Retrieve the values of an object's properties.
	  _.values = function(obj) {
	    var keys = _.keys(obj);
	    var length = keys.length;
	    var values = Array(length);
	    for (var i = 0; i < length; i++) {
	      values[i] = obj[keys[i]];
	    }
	    return values;
	  };

	  // Returns the results of applying the iteratee to each element of the object
	  // In contrast to _.map it returns an object
	  _.mapObject = function(obj, iteratee, context) {
	    iteratee = cb(iteratee, context);
	    var keys =  _.keys(obj),
	          length = keys.length,
	          results = {},
	          currentKey;
	      for (var index = 0; index < length; index++) {
	        currentKey = keys[index];
	        results[currentKey] = iteratee(obj[currentKey], currentKey, obj);
	      }
	      return results;
	  };

	  // Convert an object into a list of `[key, value]` pairs.
	  _.pairs = function(obj) {
	    var keys = _.keys(obj);
	    var length = keys.length;
	    var pairs = Array(length);
	    for (var i = 0; i < length; i++) {
	      pairs[i] = [keys[i], obj[keys[i]]];
	    }
	    return pairs;
	  };

	  // Invert the keys and values of an object. The values must be serializable.
	  _.invert = function(obj) {
	    var result = {};
	    var keys = _.keys(obj);
	    for (var i = 0, length = keys.length; i < length; i++) {
	      result[obj[keys[i]]] = keys[i];
	    }
	    return result;
	  };

	  // Return a sorted list of the function names available on the object.
	  // Aliased as `methods`
	  _.functions = _.methods = function(obj) {
	    var names = [];
	    for (var key in obj) {
	      if (_.isFunction(obj[key])) names.push(key);
	    }
	    return names.sort();
	  };

	  // Extend a given object with all the properties in passed-in object(s).
	  _.extend = createAssigner(_.allKeys);

	  // Assigns a given object with all the own properties in the passed-in object(s)
	  // (https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object/assign)
	  _.extendOwn = _.assign = createAssigner(_.keys);

	  // Returns the first key on an object that passes a predicate test
	  _.findKey = function(obj, predicate, context) {
	    predicate = cb(predicate, context);
	    var keys = _.keys(obj), key;
	    for (var i = 0, length = keys.length; i < length; i++) {
	      key = keys[i];
	      if (predicate(obj[key], key, obj)) return key;
	    }
	  };

	  // Return a copy of the object only containing the whitelisted properties.
	  _.pick = function(object, oiteratee, context) {
	    var result = {}, obj = object, iteratee, keys;
	    if (obj == null) return result;
	    if (_.isFunction(oiteratee)) {
	      keys = _.allKeys(obj);
	      iteratee = optimizeCb(oiteratee, context);
	    } else {
	      keys = flatten(arguments, false, false, 1);
	      iteratee = function(value, key, obj) { return key in obj; };
	      obj = Object(obj);
	    }
	    for (var i = 0, length = keys.length; i < length; i++) {
	      var key = keys[i];
	      var value = obj[key];
	      if (iteratee(value, key, obj)) result[key] = value;
	    }
	    return result;
	  };

	   // Return a copy of the object without the blacklisted properties.
	  _.omit = function(obj, iteratee, context) {
	    if (_.isFunction(iteratee)) {
	      iteratee = _.negate(iteratee);
	    } else {
	      var keys = _.map(flatten(arguments, false, false, 1), String);
	      iteratee = function(value, key) {
	        return !_.contains(keys, key);
	      };
	    }
	    return _.pick(obj, iteratee, context);
	  };

	  // Fill in a given object with default properties.
	  _.defaults = createAssigner(_.allKeys, true);

	  // Creates an object that inherits from the given prototype object.
	  // If additional properties are provided then they will be added to the
	  // created object.
	  _.create = function(prototype, props) {
	    var result = baseCreate(prototype);
	    if (props) _.extendOwn(result, props);
	    return result;
	  };

	  // Create a (shallow-cloned) duplicate of an object.
	  _.clone = function(obj) {
	    if (!_.isObject(obj)) return obj;
	    return _.isArray(obj) ? obj.slice() : _.extend({}, obj);
	  };

	  // Invokes interceptor with the obj, and then returns obj.
	  // The primary purpose of this method is to "tap into" a method chain, in
	  // order to perform operations on intermediate results within the chain.
	  _.tap = function(obj, interceptor) {
	    interceptor(obj);
	    return obj;
	  };

	  // Returns whether an object has a given set of `key:value` pairs.
	  _.isMatch = function(object, attrs) {
	    var keys = _.keys(attrs), length = keys.length;
	    if (object == null) return !length;
	    var obj = Object(object);
	    for (var i = 0; i < length; i++) {
	      var key = keys[i];
	      if (attrs[key] !== obj[key] || !(key in obj)) return false;
	    }
	    return true;
	  };


	  // Internal recursive comparison function for `isEqual`.
	  var eq = function(a, b, aStack, bStack) {
	    // Identical objects are equal. `0 === -0`, but they aren't identical.
	    // See the [Harmony `egal` proposal](http://wiki.ecmascript.org/doku.php?id=harmony:egal).
	    if (a === b) return a !== 0 || 1 / a === 1 / b;
	    // A strict comparison is necessary because `null == undefined`.
	    if (a == null || b == null) return a === b;
	    // Unwrap any wrapped objects.
	    if (a instanceof _) a = a._wrapped;
	    if (b instanceof _) b = b._wrapped;
	    // Compare `[[Class]]` names.
	    var className = toString.call(a);
	    if (className !== toString.call(b)) return false;
	    switch (className) {
	      // Strings, numbers, regular expressions, dates, and booleans are compared by value.
	      case '[object RegExp]':
	      // RegExps are coerced to strings for comparison (Note: '' + /a/i === '/a/i')
	      case '[object String]':
	        // Primitives and their corresponding object wrappers are equivalent; thus, `"5"` is
	        // equivalent to `new String("5")`.
	        return '' + a === '' + b;
	      case '[object Number]':
	        // `NaN`s are equivalent, but non-reflexive.
	        // Object(NaN) is equivalent to NaN
	        if (+a !== +a) return +b !== +b;
	        // An `egal` comparison is performed for other numeric values.
	        return +a === 0 ? 1 / +a === 1 / b : +a === +b;
	      case '[object Date]':
	      case '[object Boolean]':
	        // Coerce dates and booleans to numeric primitive values. Dates are compared by their
	        // millisecond representations. Note that invalid dates with millisecond representations
	        // of `NaN` are not equivalent.
	        return +a === +b;
	    }

	    var areArrays = className === '[object Array]';
	    if (!areArrays) {
	      if (typeof a != 'object' || typeof b != 'object') return false;

	      // Objects with different constructors are not equivalent, but `Object`s or `Array`s
	      // from different frames are.
	      var aCtor = a.constructor, bCtor = b.constructor;
	      if (aCtor !== bCtor && !(_.isFunction(aCtor) && aCtor instanceof aCtor &&
	                               _.isFunction(bCtor) && bCtor instanceof bCtor)
	                          && ('constructor' in a && 'constructor' in b)) {
	        return false;
	      }
	    }
	    // Assume equality for cyclic structures. The algorithm for detecting cyclic
	    // structures is adapted from ES 5.1 section 15.12.3, abstract operation `JO`.

	    // Initializing stack of traversed objects.
	    // It's done here since we only need them for objects and arrays comparison.
	    aStack = aStack || [];
	    bStack = bStack || [];
	    var length = aStack.length;
	    while (length--) {
	      // Linear search. Performance is inversely proportional to the number of
	      // unique nested structures.
	      if (aStack[length] === a) return bStack[length] === b;
	    }

	    // Add the first object to the stack of traversed objects.
	    aStack.push(a);
	    bStack.push(b);

	    // Recursively compare objects and arrays.
	    if (areArrays) {
	      // Compare array lengths to determine if a deep comparison is necessary.
	      length = a.length;
	      if (length !== b.length) return false;
	      // Deep compare the contents, ignoring non-numeric properties.
	      while (length--) {
	        if (!eq(a[length], b[length], aStack, bStack)) return false;
	      }
	    } else {
	      // Deep compare objects.
	      var keys = _.keys(a), key;
	      length = keys.length;
	      // Ensure that both objects contain the same number of properties before comparing deep equality.
	      if (_.keys(b).length !== length) return false;
	      while (length--) {
	        // Deep compare each member
	        key = keys[length];
	        if (!(_.has(b, key) && eq(a[key], b[key], aStack, bStack))) return false;
	      }
	    }
	    // Remove the first object from the stack of traversed objects.
	    aStack.pop();
	    bStack.pop();
	    return true;
	  };

	  // Perform a deep comparison to check if two objects are equal.
	  _.isEqual = function(a, b) {
	    return eq(a, b);
	  };

	  // Is a given array, string, or object empty?
	  // An "empty" object has no enumerable own-properties.
	  _.isEmpty = function(obj) {
	    if (obj == null) return true;
	    if (isArrayLike(obj) && (_.isArray(obj) || _.isString(obj) || _.isArguments(obj))) return obj.length === 0;
	    return _.keys(obj).length === 0;
	  };

	  // Is a given value a DOM element?
	  _.isElement = function(obj) {
	    return !!(obj && obj.nodeType === 1);
	  };

	  // Is a given value an array?
	  // Delegates to ECMA5's native Array.isArray
	  _.isArray = nativeIsArray || function(obj) {
	    return toString.call(obj) === '[object Array]';
	  };

	  // Is a given variable an object?
	  _.isObject = function(obj) {
	    var type = typeof obj;
	    return type === 'function' || type === 'object' && !!obj;
	  };

	  // Add some isType methods: isArguments, isFunction, isString, isNumber, isDate, isRegExp, isError.
	  _.each(['Arguments', 'Function', 'String', 'Number', 'Date', 'RegExp', 'Error'], function(name) {
	    _['is' + name] = function(obj) {
	      return toString.call(obj) === '[object ' + name + ']';
	    };
	  });

	  // Define a fallback version of the method in browsers (ahem, IE < 9), where
	  // there isn't any inspectable "Arguments" type.
	  if (!_.isArguments(arguments)) {
	    _.isArguments = function(obj) {
	      return _.has(obj, 'callee');
	    };
	  }

	  // Optimize `isFunction` if appropriate. Work around some typeof bugs in old v8,
	  // IE 11 (#1621), and in Safari 8 (#1929).
	  if (typeof /./ != 'function' && typeof Int8Array != 'object') {
	    _.isFunction = function(obj) {
	      return typeof obj == 'function' || false;
	    };
	  }

	  // Is a given object a finite number?
	  _.isFinite = function(obj) {
	    return isFinite(obj) && !isNaN(parseFloat(obj));
	  };

	  // Is the given value `NaN`? (NaN is the only number which does not equal itself).
	  _.isNaN = function(obj) {
	    return _.isNumber(obj) && obj !== +obj;
	  };

	  // Is a given value a boolean?
	  _.isBoolean = function(obj) {
	    return obj === true || obj === false || toString.call(obj) === '[object Boolean]';
	  };

	  // Is a given value equal to null?
	  _.isNull = function(obj) {
	    return obj === null;
	  };

	  // Is a given variable undefined?
	  _.isUndefined = function(obj) {
	    return obj === void 0;
	  };

	  // Shortcut function for checking if an object has a given property directly
	  // on itself (in other words, not on a prototype).
	  _.has = function(obj, key) {
	    return obj != null && hasOwnProperty.call(obj, key);
	  };

	  // Utility Functions
	  // -----------------

	  // Run Underscore.js in *noConflict* mode, returning the `_` variable to its
	  // previous owner. Returns a reference to the Underscore object.
	  _.noConflict = function() {
	    root._ = previousUnderscore;
	    return this;
	  };

	  // Keep the identity function around for default iteratees.
	  _.identity = function(value) {
	    return value;
	  };

	  // Predicate-generating functions. Often useful outside of Underscore.
	  _.constant = function(value) {
	    return function() {
	      return value;
	    };
	  };

	  _.noop = function(){};

	  _.property = property;

	  // Generates a function for a given object that returns a given property.
	  _.propertyOf = function(obj) {
	    return obj == null ? function(){} : function(key) {
	      return obj[key];
	    };
	  };

	  // Returns a predicate for checking whether an object has a given set of
	  // `key:value` pairs.
	  _.matcher = _.matches = function(attrs) {
	    attrs = _.extendOwn({}, attrs);
	    return function(obj) {
	      return _.isMatch(obj, attrs);
	    };
	  };

	  // Run a function **n** times.
	  _.times = function(n, iteratee, context) {
	    var accum = Array(Math.max(0, n));
	    iteratee = optimizeCb(iteratee, context, 1);
	    for (var i = 0; i < n; i++) accum[i] = iteratee(i);
	    return accum;
	  };

	  // Return a random integer between min and max (inclusive).
	  _.random = function(min, max) {
	    if (max == null) {
	      max = min;
	      min = 0;
	    }
	    return min + Math.floor(Math.random() * (max - min + 1));
	  };

	  // A (possibly faster) way to get the current timestamp as an integer.
	  _.now = Date.now || function() {
	    return new Date().getTime();
	  };

	   // List of HTML entities for escaping.
	  var escapeMap = {
	    '&': '&amp;',
	    '<': '&lt;',
	    '>': '&gt;',
	    '"': '&quot;',
	    "'": '&#x27;',
	    '`': '&#x60;'
	  };
	  var unescapeMap = _.invert(escapeMap);

	  // Functions for escaping and unescaping strings to/from HTML interpolation.
	  var createEscaper = function(map) {
	    var escaper = function(match) {
	      return map[match];
	    };
	    // Regexes for identifying a key that needs to be escaped
	    var source = '(?:' + _.keys(map).join('|') + ')';
	    var testRegexp = RegExp(source);
	    var replaceRegexp = RegExp(source, 'g');
	    return function(string) {
	      string = string == null ? '' : '' + string;
	      return testRegexp.test(string) ? string.replace(replaceRegexp, escaper) : string;
	    };
	  };
	  _.escape = createEscaper(escapeMap);
	  _.unescape = createEscaper(unescapeMap);

	  // If the value of the named `property` is a function then invoke it with the
	  // `object` as context; otherwise, return it.
	  _.result = function(object, property, fallback) {
	    var value = object == null ? void 0 : object[property];
	    if (value === void 0) {
	      value = fallback;
	    }
	    return _.isFunction(value) ? value.call(object) : value;
	  };

	  // Generate a unique integer id (unique within the entire client session).
	  // Useful for temporary DOM ids.
	  var idCounter = 0;
	  _.uniqueId = function(prefix) {
	    var id = ++idCounter + '';
	    return prefix ? prefix + id : id;
	  };

	  // By default, Underscore uses ERB-style template delimiters, change the
	  // following template settings to use alternative delimiters.
	  _.templateSettings = {
	    evaluate    : /<%([\s\S]+?)%>/g,
	    interpolate : /<%=([\s\S]+?)%>/g,
	    escape      : /<%-([\s\S]+?)%>/g
	  };

	  // When customizing `templateSettings`, if you don't want to define an
	  // interpolation, evaluation or escaping regex, we need one that is
	  // guaranteed not to match.
	  var noMatch = /(.)^/;

	  // Certain characters need to be escaped so that they can be put into a
	  // string literal.
	  var escapes = {
	    "'":      "'",
	    '\\':     '\\',
	    '\r':     'r',
	    '\n':     'n',
	    '\u2028': 'u2028',
	    '\u2029': 'u2029'
	  };

	  var escaper = /\\|'|\r|\n|\u2028|\u2029/g;

	  var escapeChar = function(match) {
	    return '\\' + escapes[match];
	  };

	  // JavaScript micro-templating, similar to John Resig's implementation.
	  // Underscore templating handles arbitrary delimiters, preserves whitespace,
	  // and correctly escapes quotes within interpolated code.
	  // NB: `oldSettings` only exists for backwards compatibility.
	  _.template = function(text, settings, oldSettings) {
	    if (!settings && oldSettings) settings = oldSettings;
	    settings = _.defaults({}, settings, _.templateSettings);

	    // Combine delimiters into one regular expression via alternation.
	    var matcher = RegExp([
	      (settings.escape || noMatch).source,
	      (settings.interpolate || noMatch).source,
	      (settings.evaluate || noMatch).source
	    ].join('|') + '|$', 'g');

	    // Compile the template source, escaping string literals appropriately.
	    var index = 0;
	    var source = "__p+='";
	    text.replace(matcher, function(match, escape, interpolate, evaluate, offset) {
	      source += text.slice(index, offset).replace(escaper, escapeChar);
	      index = offset + match.length;

	      if (escape) {
	        source += "'+\n((__t=(" + escape + "))==null?'':_.escape(__t))+\n'";
	      } else if (interpolate) {
	        source += "'+\n((__t=(" + interpolate + "))==null?'':__t)+\n'";
	      } else if (evaluate) {
	        source += "';\n" + evaluate + "\n__p+='";
	      }

	      // Adobe VMs need the match returned to produce the correct offest.
	      return match;
	    });
	    source += "';\n";

	    // If a variable is not specified, place data values in local scope.
	    if (!settings.variable) source = 'with(obj||{}){\n' + source + '}\n';

	    source = "var __t,__p='',__j=Array.prototype.join," +
	      "print=function(){__p+=__j.call(arguments,'');};\n" +
	      source + 'return __p;\n';

	    try {
	      var render = new Function(settings.variable || 'obj', '_', source);
	    } catch (e) {
	      e.source = source;
	      throw e;
	    }

	    var template = function(data) {
	      return render.call(this, data, _);
	    };

	    // Provide the compiled source as a convenience for precompilation.
	    var argument = settings.variable || 'obj';
	    template.source = 'function(' + argument + '){\n' + source + '}';

	    return template;
	  };

	  // Add a "chain" function. Start chaining a wrapped Underscore object.
	  _.chain = function(obj) {
	    var instance = _(obj);
	    instance._chain = true;
	    return instance;
	  };

	  // OOP
	  // ---------------
	  // If Underscore is called as a function, it returns a wrapped object that
	  // can be used OO-style. This wrapper holds altered versions of all the
	  // underscore functions. Wrapped objects may be chained.

	  // Helper function to continue chaining intermediate results.
	  var result = function(instance, obj) {
	    return instance._chain ? _(obj).chain() : obj;
	  };

	  // Add your own custom functions to the Underscore object.
	  _.mixin = function(obj) {
	    _.each(_.functions(obj), function(name) {
	      var func = _[name] = obj[name];
	      _.prototype[name] = function() {
	        var args = [this._wrapped];
	        push.apply(args, arguments);
	        return result(this, func.apply(_, args));
	      };
	    });
	  };

	  // Add all of the Underscore functions to the wrapper object.
	  _.mixin(_);

	  // Add all mutator Array functions to the wrapper.
	  _.each(['pop', 'push', 'reverse', 'shift', 'sort', 'splice', 'unshift'], function(name) {
	    var method = ArrayProto[name];
	    _.prototype[name] = function() {
	      var obj = this._wrapped;
	      method.apply(obj, arguments);
	      if ((name === 'shift' || name === 'splice') && obj.length === 0) delete obj[0];
	      return result(this, obj);
	    };
	  });

	  // Add all accessor Array functions to the wrapper.
	  _.each(['concat', 'join', 'slice'], function(name) {
	    var method = ArrayProto[name];
	    _.prototype[name] = function() {
	      return result(this, method.apply(this._wrapped, arguments));
	    };
	  });

	  // Extracts the result from a wrapped and chained object.
	  _.prototype.value = function() {
	    return this._wrapped;
	  };

	  // Provide unwrapping proxy for some methods used in engine operations
	  // such as arithmetic and JSON stringification.
	  _.prototype.valueOf = _.prototype.toJSON = _.prototype.value;

	  _.prototype.toString = function() {
	    return '' + this._wrapped;
	  };

	  // AMD registration happens at the end for compatibility with AMD loaders
	  // that may not enforce next-turn semantics on modules. Even though general
	  // practice for AMD registration is to be anonymous, underscore registers
	  // as a named module because, like jQuery, it is a base library that is
	  // popular enough to be bundled in a third party lib, but not be part of
	  // an AMD load request. Those cases could generate an error when an
	  // anonymous define() is called outside of a loader request.
	  if (true) {
	    !(__WEBPACK_AMD_DEFINE_ARRAY__ = [], __WEBPACK_AMD_DEFINE_RESULT__ = function() {
	      return _;
	    }.apply(exports, __WEBPACK_AMD_DEFINE_ARRAY__), __WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));
	  }
	}.call(this));


/***/ },
/* 211 */
/***/ function(module, exports) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	var bib = {
	    bradley2010learning: {
	        id: 'bradley2010learning', type: 'inproceedings',
	        title: "Learning tree conditional random fields",
	        author: {
	            abbr: "Bradley & Guestrin",
	            full: "Bradley, Joseph K and Guestrin, Carlos"
	        },
	        booktitle: "Proceedings of the 27th International Conference on Machine Learning (ICML-10)",
	        pages: "127—134",
	        year: 2010
	    },
	    jordan2002discriminative: {
	        id: 'jordan2002discriminative',
	        title: "On discriminative vs. generative classifiers: A comparison of logistic regression and naive bayes",
	        author: {
	            abbr: "Ng & Jordan",
	            full: "Ng, Andrew and Jordan, Michael"
	        },
	        journal: "Advances in neural information processing systems",
	        volume: "14",
	        pages: "841",
	        year: 2002
	    },
	    younger1967recognition: {
	        id: 'younger1967recognition',
	        title: 'Recognition and parsing of context-free languages in time n³',
	        author: {
	            firstName: "Daniel",
	            lastName: "Younger"
	        },
	        journal: 'Information and control',
	        volume: '10',
	        number: '2',
	        pages: '189—208',
	        year: 1967,
	        publisher: 'Elsevier',
	        type: 'article'
	    },

	    kasami1965efficient: {
	        type: 'techreport', id: 'kasami1965efficient',
	        title: 'AN EFFICIENT RECOGNITION AND SYNTAX ANALYSIS ALGORITHM FOR CONTEXT-FREE LANGUAGES',
	        author: {
	            firstName: "Tadao",
	            lastName: "Kasami"
	        },
	        year: '1965',
	        institution: 'DTIC Document'
	    },

	    sipser2006introduction: {
	        id: 'sipser2006introduction',
	        type: 'book',
	        title: 'Introduction to the Theory of Computation',
	        author: { lastName: 'Sipser', firstName: 'Michael' },
	        volume: '2',
	        year: '2006',
	        publisher: 'Thomson Course Technology Boston'
	    },

	    huang1971stochastic: {
	        id: 'huang1971stochastic',
	        type: 'article',
	        title: 'On stochastic context-free languages',
	        author: 'Huang, Teddy and Fu, King Sun',
	        journal: 'Information Sciences',
	        volume: '3',
	        number: '3',
	        pages: '201—224',
	        year: '1971',
	        publisher: 'Elsevier'
	    },
	    lange2009cnf: {
	        id: 'lange2009cnf',
	        type: 'article',
	        title: 'To CNF or not to CNF? An efficient yet presentable version of the CYK algorithm',
	        author: {
	            abbr: "Lange and Leiß",
	            full: "Lange, Martin and Leiß, Hans"
	        },
	        journal: 'Informatica Didactica',
	        volume: '8',
	        pages: '2008—2010',
	        year: '2009'
	    },
	    cocke1969programming: {
	        id: 'cocke1969programming',
	        type: 'article',
	        title: 'Programming languages and their compilers: Preliminary notes',
	        author: { lastName: 'Cocke', firstName: 'John' },
	        year: '1969',
	        publisher: 'Courant Institute of Mathematical Sciences, New York University'
	    },

	    vanopijnen2014: {
	        id: "vanopijnen2014",
	        type: "article",
	        title: "Op en in het web: Hoe de toegankelijkheid van rechterlijke uitspraken kan worden verbeterd",
	        href: "http://dare.uva.nl/record/1/408882",
	        author: {
	            firstName: "Marc",
	            lastName: "van Opijnen"
	        },
	        year: 2014
	    },
	    trompper2014: {
	        id: "trompper2014",
	        type: "web",
	        title: "NL open legal data survey: Rechtspraak.nl",
	        href: "http://leibniz-internship-report.herokuapp.com/eu-legal-data-survey/nl#rechtspraak.nl",
	        author: {
	            lastName: "Trompper"
	        },
	        year: 2014
	    },
	    mccallum2000maximum: {
	        id: "mccallum2000maximum",
	        title: "Maximum Entropy Markov Models for Information Extraction and Segmentation",
	        author: {
	            abbr: "Freitag et al.",
	            full: "McCallum, Andrew and Freitag, Dayne and Pereira, Fernando"
	        },
	        booktitle: "ICML",
	        volume: "17",
	        pages: "591—598",
	        year: 2000,
	        href: "http://cseweb.ucsd.edu/~elkan/254spring02/gidofalvi.pdf"
	    },
	    mallet: {
	        id: 'mallet',
	        author: {
	            full: "McCallum, Andrew Kachites",
	            abbr: "McCallum"
	        },
	        title: "MALLET: A Machine Learning for Language Toolkit",
	        href: "http://mallet.cs.umass.edu",
	        year: 2002
	    },
	    sutton2006introduction: {
	        id: 'sutton2006introduction',
	        type: "article",
	        title: "An introduction to conditional random fields for relational learning",
	        author: {
	            abbr: "McCallum & Sutton",
	            full: "Sutton, Charles and McCallum, Andrew"
	        },
	        journal: "Introduction to statistical relational learning",
	        pages: "93—128",
	        year: 2006,
	        publisher: "MIT press"
	    },
	    finkel2004exploiting: {
	        id: 'finkel2004exploiting',
	        type: "inproceedings",
	        title: "Exploiting context for biomedical entity recognition: from syntax to the web",
	        author: {
	            full: "Finkel, Jenny and Dingare, Shipra and Nguyen, Huy and Nissim, Malvina and Manning, Christopher and Sinclair, Gail",
	            abbr: "Finkel et al."
	        },
	        journal: "Proceedings of the International Joint Workshop on Natural Language Processing in Biomedicine and its Applications",
	        pages: "88—91",
	        year: 2004,
	        publisher: "Association for Computational Linguistics"
	    },
	    klinger2009feature: {
	        id: 'klinger2009feature',
	        type: "inproceedings",
	        title: "Feature Subset Selection in Conditional Random Fields for Named Entity Recognition",
	        author: {
	            full: "Klinger, Roman and Friedrich, Christoph",
	            abbr: "Klinger et al."
	        },
	        journal: "RANLP",
	        pages: "185—191",
	        year: 2009
	    },
	    krishnan2006effective: {
	        id: 'krishnan2006effective',
	        type: "inproceedings",
	        title: "An effective two-stage model for exploiting non-local dependencies in named entity recognition",
	        author: {
	            full: "Krishnan, Vijay and Manning, Christopher D",
	            abbr: "Krishnan et al."
	        },
	        journal: "Proceedings of the 21st International Conference on Computational Linguistics and the 44th annual meeting of the Association for Computational Linguistics",
	        pages: "1121-1128",
	        year: 2006,
	        publisher: "Association for Computational Linguistics"
	    }

	};
	exports.default = bib;

	//html4: {
	//    id: 'html4',
	//    type: "web",
	//    href: 'https://www.w3.org/TR/html4/',
	//    title: "HTML 4.01 Specification",
	//    author: {
	//        abbr: "McCallum & Sutton",
	//        full: "Sutton, Charles and McCallum, Andrew"
	//    },
	//    journal: "Introduction to statistical relational learning",
	//    pages: "93—128",
	//    year: 2006,
	//    publisher: "MIT press"
	//}

/***/ },
/* 212 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _BrowserCheck = __webpack_require__(213);

	var _BrowserCheck2 = _interopRequireDefault(_BrowserCheck);

	var _Author = __webpack_require__(214);

	var _Author2 = _interopRequireDefault(_Author);

	var _AbstractContent = __webpack_require__(215);

	var _AbstractContent2 = _interopRequireDefault(_AbstractContent);

	var _ToC = __webpack_require__(216);

	var _ToC2 = _interopRequireDefault(_ToC);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var Index = function (_Component) {
	    _inherits(Index, _Component);

	    function Index() {
	        _classCallCheck(this, Index);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Index).apply(this, arguments));
	    }

	    _createClass(Index, [{
	        key: 'render',
	        value: function render() {
	            // <h1 itemProp="name">{this.props.title}</h1>
	            return _react2.default.createElement(
	                'div',
	                {
	                    itemProp: 'mainEntity',
	                    itemScope: true,
	                    itemType: 'http://schema.org/Thesis' },
	                _react2.default.createElement(_BrowserCheck2.default, null),
	                _react2.default.createElement(
	                    'header',
	                    { style: { marginBottom: "20px" } },
	                    _react2.default.createElement(_Author2.default, null)
	                ),
	                _react2.default.createElement(
	                    'section',
	                    null,
	                    _react2.default.createElement(
	                        'h2',
	                        null,
	                        'Abstract'
	                    ),
	                    _react2.default.createElement(_AbstractContent2.default, null)
	                ),
	                _react2.default.createElement(
	                    'section',
	                    null,
	                    _react2.default.createElement(
	                        'h2',
	                        null,
	                        'Table of Contents'
	                    ),
	                    _react2.default.createElement(_ToC2.default, this.props)
	                )
	            );
	        }
	    }]);

	    return Index;
	}(_react.Component);

	exports.default = Index;

/***/ },
/* 213 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var BrowserCheck = function (_Component) {
	    _inherits(BrowserCheck, _Component);

	    function BrowserCheck() {
	        _classCallCheck(this, BrowserCheck);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(BrowserCheck).apply(this, arguments));
	    }

	    _createClass(BrowserCheck, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement('div', { dangerouslySetInnerHTML: { __html: "<!--[if lt IE 8]>" + '<p class="browserupgrade">You are using an ' + '<strong>outdated</strong>' + ' browser. ' + 'Please ' + '<a href="http://browsehappy.com/">upgrade your browser</a>' + ' to improve your experience.</p>' + "<![endif]-->" } });
	        }
	    }]);

	    return BrowserCheck;
	}(_react.Component);

	exports.default = BrowserCheck;

/***/ },
/* 214 */
/***/ function(module, exports, __webpack_require__) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var ghUrl = "https://github.com/digitalheir";

	var AuthorData = function (_Component) {
	  _inherits(AuthorData, _Component);

	  function AuthorData() {
	    _classCallCheck(this, AuthorData);

	    return _possibleConstructorReturn(this, Object.getPrototypeOf(AuthorData).apply(this, arguments));
	  }

	  _createClass(AuthorData, [{
	    key: "render",
	    value: function render() {
	      var ghUrl = "https://github.com/digitalheir";
	      return _react2.default.createElement(
	        "div",
	        { itemProp: "author copyrightHolder", itemScope: true,
	          itemType: "http://schema.org/Person", className: "author-data" },
	        _react2.default.createElement("meta", { itemProp: "url", content: ghUrl }),
	        _react2.default.createElement(
	          "div",
	          { id: "github-link", className: "author-line" },
	          _react2.default.createElement(
	            "a",
	            { href: ghUrl,
	              className: "icon no-decoration" },
	            _react2.default.createElement(
	              "i",
	              {
	                className: "fa fa-github" },
	              " "
	            )
	          ),
	          _react2.default.createElement(
	            "a",
	            {
	              href: ghUrl, className: "author-value" },
	            "digitalheir"
	          )
	        ),
	        _react2.default.createElement(
	          "div",
	          { id: "mail-link", className: "author-line" },
	          _react2.default.createElement(
	            "a",
	            { href: ghUrl,
	              className: "icon no-decoration" },
	            _react2.default.createElement(
	              "i",
	              {
	                className: "fa fa-envelope" },
	              " "
	            )
	          ),
	          _react2.default.createElement(
	            "a",
	            {
	              href: "m.f.a.trompper@uva.nl", itemProp: "email", className: "author-value" },
	            "m.f.a.trompper@uva.nl"
	          )
	        )
	      );
	    }
	  }]);

	  return AuthorData;
	}(_react.Component);

	exports.default = AuthorData;

/***/ },
/* 215 */
/***/ function(module, exports, __webpack_require__) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var Header = function (_React$Component) {
	    _inherits(Header, _React$Component);

	    function Header() {
	        _classCallCheck(this, Header);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Header).apply(this, arguments));
	    }

	    _createClass(Header, [{
	        key: "render",
	        value: function render() {
	            //var relativeToRoot = this.props.path.match(/\//g).slice(1).map(a=>"../").join("");
	            return _react2.default.createElement(
	                "div",
	                null,
	                _react2.default.createElement(
	                    "p",
	                    null,
	                    "A growing amount of Dutch case law is openly distributed on ",
	                    _react2.default.createElement(
	                        "a",
	                        { href: "http://www.rechtspraak.nl/" },
	                        "Rechtspraak.nl"
	                    ),
	                    ". Currently, many documents are not marked up or marked up only very sparsely, hampering our ability to process these documents automatically."
	                ),
	                _react2.default.createElement(
	                    "p",
	                    null,
	                    "In this thesis, we explore the problem of automatically assigning a section structure to the texts of Dutch court judgments. To this end, we develop a database that mirrors the XML data offering of Rechtspraak.nl. We experiment with Linear-Chain Conditional Random Fields to label text elements with their role in the document (text, title or numbering). Afterwards, we experiment with stochastic context free grammars to generate a parse tree to model section nesting."
	                ),
	                _react2.default.createElement(
	                    "p",
	                    null,
	                    "TODO: results"
	                )
	            );
	        }
	    }]);

	    return Header;
	}(_react2.default.Component);

	exports.default = Header;

/***/ },
/* 216 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _chapters = __webpack_require__(217);

	var _chapters2 = _interopRequireDefault(_chapters);

	var _Routes = __webpack_require__(204);

	var _getSectionComponent = __webpack_require__(218);

	var _getSectionComponent2 = _interopRequireDefault(_getSectionComponent);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }
	// import Router from 'react-router'

	// let Link = Router.Link;


	var ToC = function (_React$Component) {
	    _inherits(ToC, _React$Component);

	    function ToC() {
	        _classCallCheck(this, ToC);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(ToC).apply(this, arguments));
	    }

	    _createClass(ToC, [{
	        key: 'render',
	        value: function render() {
	            var relativeToRoot = this.props.path.match(/\//g).slice(1).map(function (_) {
	                return "../";
	            }).join("");
	            var path = this.props.path;

	            return _react2.default.createElement(
	                'nav',
	                { className: 'toc' },
	                _react2.default.createElement(
	                    'ol',
	                    null,
	                    this.props.showHome ? _react2.default.createElement(
	                        'a',
	                        { href: relativeToRoot },
	                        'Home'
	                    ) : "",
	                    _chapters2.default.inOrder.map(function (chapter) {
	                        var urlSection = relativeToRoot + chapter.route.replace('/', '');

	                        return _react2.default.createElement(
	                            'li',
	                            { key: chapter.route },
	                            path == chapter.route ? _react2.default.createElement(
	                                'strong',
	                                null,
	                                chapter.title
	                            ) : _react2.default.createElement(
	                                'a',
	                                { href: urlSection,
	                                    className: 'nav-link' },
	                                chapter.title
	                            ),
	                            ToC.getSubSections((0, _Routes.getHandler)(chapter.route), urlSection, 1)
	                        );
	                    })
	                )
	            );
	        }
	    }], [{
	        key: 'getSubSections',
	        value: function getSubSections(chapter, urlSection, depth) {
	            if (chapter.getSections && chapter.getSections()) {
	                // console.log(chapter.getSections());
	                return _react2.default.createElement(
	                    'ol',
	                    null,
	                    chapter.getSections().inOrder.map(function (section) {

	                        if (!!section) return _react2.default.createElement(
	                            'li',
	                            null,
	                            _react2.default.createElement(
	                                'a',
	                                { href: urlSection + "#" + section.id },
	                                section.title
	                            ),
	                            ToC.getSubSections((0, _getSectionComponent2.default)(section.id), urlSection, depth + 1)
	                        );else throw Error("Null section found in " + JSON.stringify(chapter.getSections()));
	                    })
	                );
	            } else {
	                return "";
	            }
	        }
	    }]);

	    return ToC;
	}(_react2.default.Component);

	ToC.propTypes = {
	    path: _react2.default.PropTypes.string.isRequired
	};
	exports.default = ToC;

/***/ },
/* 217 */
/***/ function(module, exports) {

	"use strict";

	var chapters = {
	    introduction: {
	        title: "Introduction",
	        route: '/introduction/'
	    },
	    rechtspraakNl: {
	        title: "Rechtspraak.nl Data Set",
	        route: '/rechtspraak-nl/'
	    },
	    tagging: {
	        title: "Tagging Elements",
	        route: '/tagging/'
	    },
	    documentStructure: {
	        title: "Inferring a Document Structure",
	        route: '/document-structure/'
	    },
	    dissemination: {
	        title: "Dissemination of Enriched Dataset",
	        route: '/dissemination/'
	    },
	    // presentation: {
	    //     title: "Dissemination",
	    //     route: '/dissemination/'
	    // }

	    pathTo: function pathTo(fromPath, toChapter) {
	        var relativeToRoot = fromPath.match(/\//g).slice(1).map(function (_) {
	            return "../";
	        }).join("");
	        return relativeToRoot + toChapter.route.replace('/', '');
	    }
	};

	chapters.inOrder = [chapters.introduction,
	// chapters.rechtspraakNl,
	chapters.tagging, chapters.documentStructure, chapters.dissemination];

	chapters.inOrder.forEach(function (ch) {
	    if (!ch) throw new Error("Chapters object contains null or undefined item.");
	});

	module.exports = chapters;

/***/ },
/* 218 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	exports.default = function (route) {
	    switch (route) {
	        //Introduction
	        case _sections2.default.rechtspraakNl.id:
	            return _IntroRechtspraakNl2.default;
	        case _sections2.default.rechtspraakNlMarkup.id:
	            return _RechtspraakNlMarkup2.default;
	        case _sections2.default.importing.id:
	            return _Importing2.default;

	        // Tagging
	        case _sections4.default.taggingIntroduction.id:
	            return _Introduction2.default;
	        case _sections4.default.taggingMethods.id:
	            return _Methods2.default;
	        case _sections6.default.featureSelection.id:
	            return _FeatureSelection2.default;
	        case _sections6.default.crf.id:
	            return _CRF2.default;
	        case _sections6.default.deterministic.id:
	            return _DeterministicTagger2.default;
	        case _sections6.default.manual.id:
	            return _ManualTagger2.default;
	        case _sections4.default.taggingDiscussion.id:
	            return _Discussion2.default;
	        case _sections4.default.taggingResults.id:
	            return _Results2.default;

	        // InferringDocumentStructure
	        case _sections8.default.intro.id:
	            return _Introduction4.default;
	        case _sections8.default.methods.id:
	            return _Methods4.default;
	        case _sections10.default.scfg.id:
	            return _ContextFreeGrammars2.default;
	        case _sections10.default.cyk.id:
	            return _CYK2.default;
	        case _sections8.default.evaluation.id:
	            return _Evaluation2.default;

	        //  Dissemination
	        case _sections12.default.additionalEnrichment.id:
	            return _AdditionalEnrichment2.default;

	        default:
	            throw Error("No handler for route " + route + ". You should edit getSectionComponent.js");
	    }
	};

	var _IntroRechtspraakNl = __webpack_require__(219);

	var _IntroRechtspraakNl2 = _interopRequireDefault(_IntroRechtspraakNl);

	var _RechtspraakNlMarkup = __webpack_require__(247);

	var _RechtspraakNlMarkup2 = _interopRequireDefault(_RechtspraakNlMarkup);

	var _Importing = __webpack_require__(248);

	var _Importing2 = _interopRequireDefault(_Importing);

	var _sections = __webpack_require__(246);

	var _sections2 = _interopRequireDefault(_sections);

	var _sections3 = __webpack_require__(249);

	var _sections4 = _interopRequireDefault(_sections3);

	var _sections5 = __webpack_require__(250);

	var _sections6 = _interopRequireDefault(_sections5);

	var _sections7 = __webpack_require__(251);

	var _sections8 = _interopRequireDefault(_sections7);

	var _sections9 = __webpack_require__(252);

	var _sections10 = _interopRequireDefault(_sections9);

	var _Results = __webpack_require__(253);

	var _Results2 = _interopRequireDefault(_Results);

	var _Discussion = __webpack_require__(254);

	var _Discussion2 = _interopRequireDefault(_Discussion);

	var _Methods = __webpack_require__(255);

	var _Methods2 = _interopRequireDefault(_Methods);

	var _Introduction = __webpack_require__(257);

	var _Introduction2 = _interopRequireDefault(_Introduction);

	var _ManualTagger = __webpack_require__(258);

	var _ManualTagger2 = _interopRequireDefault(_ManualTagger);

	var _DeterministicTagger = __webpack_require__(284);

	var _DeterministicTagger2 = _interopRequireDefault(_DeterministicTagger);

	var _CRF = __webpack_require__(285);

	var _CRF2 = _interopRequireDefault(_CRF);

	var _FeatureSelection = __webpack_require__(290);

	var _FeatureSelection2 = _interopRequireDefault(_FeatureSelection);

	var _Introduction3 = __webpack_require__(310);

	var _Introduction4 = _interopRequireDefault(_Introduction3);

	var _Methods3 = __webpack_require__(311);

	var _Methods4 = _interopRequireDefault(_Methods3);

	var _Evaluation = __webpack_require__(312);

	var _Evaluation2 = _interopRequireDefault(_Evaluation);

	var _ContextFreeGrammars = __webpack_require__(313);

	var _ContextFreeGrammars2 = _interopRequireDefault(_ContextFreeGrammars);

	var _AdditionalEnrichment = __webpack_require__(314);

	var _AdditionalEnrichment2 = _interopRequireDefault(_AdditionalEnrichment);

	var _CYK = __webpack_require__(315);

	var _CYK2 = _interopRequireDefault(_CYK);

	var _sections11 = __webpack_require__(316);

	var _sections12 = _interopRequireDefault(_sections11);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/***/ },
/* 219 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _Source = __webpack_require__(220);

	var _Source2 = _interopRequireDefault(_Source);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	var _MarkupStatsFigure = __webpack_require__(223);

	var _MarkupStatsFigure2 = _interopRequireDefault(_MarkupStatsFigure);

	var _FigRef = __webpack_require__(245);

	var _FigRef2 = _interopRequireDefault(_FigRef);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _chapters = __webpack_require__(217);

	var _chapters2 = _interopRequireDefault(_chapters);

	var _sections = __webpack_require__(246);

	var _sections2 = _interopRequireDefault(_sections);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var Introduction = function (_Component) {
	    _inherits(Introduction, _Component);

	    function Introduction() {
	        _classCallCheck(this, Introduction);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Introduction).apply(this, arguments));
	    }

	    _createClass(Introduction, [{
	        key: 'render',

	        //pre>\nfunction (doc)
	        //
	        //    var Snowball = require('views/lib/snowball');
	        //    if (doc['corpus'] == 'Rechtspraak.nl' && doc['tokens']) {
	        //    for (var i in doc['tokens']) {
	        //    for (var j in doc['tokens'][i]) {
	        //    var token = doc['tokens'][i][j];
	        //    var stemmer = new Snowball('Dutch');
	        //    stemmer.setCurrent(token);
	        //    stemmer.stem();
	        //    emit([stemmer.getCurrent(), token], 1);
	        //}
	        //}
	        //}
	        // }/pre>
	        //                </section:h2>
	        //                <section:h2>-</section:h2>
	        //                <section:h2>Data set statistics
	        //                    <p>
	        //                        In this section, we explore some basic statistics to get a feel for the database. A conversation that has
	        //                        repeatedly come up is whether
	        //                    </p>
	        //                    <div id="canvas-svg"></div>
	        //                </section:h2>
	        //                <section>
	        //                    <h2>Counting terms</h2>
	        //                    <p>Initial tokenization is performed server-side, using Alpino.</p>
	        //                    <p>Then, in a MapReduce task we reduce these inflected words to lemmas and count lemma occurrences for each document. We use a<a href="https://github.com/fortnightlabs/snowball-js">Javascript port</a> of the <a href="http://snowball.tartarus.org/algorithms/dutch/stemmer.html">
	        //                        Snowball stemming algorithm for
	        //                        Dutch</a>, along with some additional normalization rules which mainly handle special characters (consider,
	        //                        for example, that <code>'s ochtends</code> represents the same phrase as <code>`s ochtends</code>
	        //                    </p>). For
	        //                    this, we use a slug-generator algorithm.
	        //                    <p>The stemming phase is implemented as a CommonJS module in the map function for our term count:</p><code>term_document_count</code><strong>Map</strong>
	        //  <pre>\nfunction (doc) {"{"}
	        //      var Snowball = require('views/lib/snowball');
	        //      if (doc['corpus'] == 'Rechtspraak.nl' && doc['tokens']) {"{"}
	        //      for (var i in doc['tokens']) {"{"}
	        //      for (var j in doc['tokens'][i]) {"{"}
	        //      var token = doc['tokens'][i][j];
	        //      var stemmer = new Snowball('Dutch');
	        //      stemmer.setCurrent(token);
	        //      stemmer.stem();
	        //      emit([stemmer.getCurrent(), doc['_id']], 1);
	        //  {"}"}
	        //  {"}"}
	        //  {"}"}
	        //  {"}"}</pre><strong>Reduce</strong>
	        //                    <pre>\n_sum</pre><code>term_token_count</code><strong>Map</strong>
	        //  <strong>Reduce</strong>
	        //                    <pre>\n_sum</pre>
	        //                    <p>The difference between <code>term_document_count</code> and <code>term_token_count</code> is the keys they produce in the map function:<code>term_document_count</code> groups stemmed terms together with their document identifier, where <code>term_token_count</code> groups
	        //                        these terms with their original token.
	        //                    </p>
	        //                    <p><code>term_document_count</code> is useful to .</p>    https://en.wikipedia.org/wiki/Latent_Dirichlet_allocation
	        //                    <p><code>term_token_count</code> is useful when we want to know where a stemmed term comes
	        //                        from, i.e., its manifestation in the source documents.
	        //                    </p>    Refer to <a href="http://rechtspraak.cloudant.com/ecli/_design/term_frequency">http://rechtspraak.cloudant.com/ecli/_design/term_frequency</a>    for the implemented design document.
	        //                </section>
	        value: function render() {

	            return _react2.default.createElement(
	                'div',
	                null,
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    _react2.default.createElement(
	                        'a',
	                        { href: 'http://www.rechtspraak.nl/' },
	                        'Rechtspraak.nl'
	                    ),
	                    ' is the official website of the Dutch judiciary. The website hosts an open data portal for Dutch case law, containing metadata for about 2 million court judgments',
	                    _react2.default.createElement(_Source2.default, { href: 'http://data.rechtspraak.nl/uitspraken/zoeken?' }),
	                    ' and judgments texts for about 350.000 judgments in XML',
	                    _react2.default.createElement(_Source2.default, {
	                        href: 'http://data.rechtspraak.nl/uitspraken/zoeken?return=doc' }),
	                    '. In this thesis, we are interested in markup, so we only consider those documents that contain text. As ',
	                    _references2.default.cite(_bib2.default.vanopijnen2014),
	                    ' explains, the full data set of Rechtspraak.nl contains only a fraction of all court judgments that exist in the Netherlands, but the collection is curated so that it is representative of case law in the Netherlands.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'If we turn to ',
	                    _react2.default.createElement(_FigRef2.default, {
	                        fig: _figs2.default.markupStats }),
	                    ', we see that there is a recent trend for more richly marked up documents. However, a overwhelmingly large portion of older documents remains that contain no or only sparse markup. To illustrate: at the time of writing, 78.7% of all judgment texts on Rechtspraak.nl do not contain any ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'section'
	                    ),
	                    ' tag, implying that a large amount of documents are barely marked up. This is unfortunate, because having proper markup makes documents better searchable and more easy to style.'
	                ),
	                _react2.default.createElement(_MarkupStatsFigure2.default, null),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'The problem that we investigate in this thesis, then, is whether we can enrich the markup of documents in Rechtspraak.nl by automatically assigning a section hierarchy among the text elements. This task is split in the following subtasks:'
	                ),
	                _react2.default.createElement(
	                    'ol',
	                    null,
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(
	                            'a',
	                            { href: "#" + _sections2.default.importing },
	                            'importing documents from the Rechtspraak.nl web service;'
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(
	                            'a',
	                            { href: "#" + _sections2.default.importing },
	                            'tokenizing relevant text elements;'
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(
	                            'a',
	                            { href: _chapters2.default.pathTo(this.props.path, _chapters2.default.tagging) },
	                            'labeling these text elements with their respective roles (e.g., "',
	                            _react2.default.createElement(
	                                'code',
	                                null,
	                                'section title'
	                            ),
	                            '" and "',
	                            _react2.default.createElement(
	                                'code',
	                                null,
	                                'text block'
	                            ),
	                            '");'
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(
	                            'a',
	                            { href: _chapters2.default.pathTo(this.props.path, _chapters2.default.documentStructure) },
	                            'combining the tokens in such a way that they represent the most likely section hierarchy;'
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        'publishing the resulting documents so that search engines can make use of the enriched markup'
	                    )
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'The project in published as two separate Java libraries: one for importing and enriching documents from Rechtspraak.nl (',
	                    _react2.default.createElement(
	                        'a',
	                        { href: 'https://github.com/digitalheir/java-rechtspraak-library' },
	                        'source code on GitHub'
	                    ),
	                    '), and one for mirroring the Rechtspraak.nl corpus on a CouchDB database (',
	                    _react2.default.createElement(
	                        'a',
	                        { href: 'https://github.com/digitalheir/dutch-case-law-to-couchdb' },
	                        'source code on GitHub'
	                    ),
	                    ').'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'For a comprehensive study on the legal and technical background of the digital publication of Dutch case law, see ',
	                    _references2.default.cite(_bib2.default.vanopijnen2014),
	                    '. For a general overview of Rechtspraak.nl\'s web service, see ',
	                    _references2.default.cite(_bib2.default.trompper2014),
	                    '.'
	                )
	            );
	        }
	    }]);

	    return Introduction;
	}(_react.Component);

	exports.default = Introduction;

/***/ },
/* 220 */
/***/ function(module, exports, __webpack_require__) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var Source = function (_Component) {
	  _inherits(Source, _Component);

	  function Source() {
	    _classCallCheck(this, Source);

	    return _possibleConstructorReturn(this, Object.getPrototypeOf(Source).apply(this, arguments));
	  }

	  _createClass(Source, [{
	    key: "render",
	    value: function render() {
	      return _react2.default.createElement(
	        "sup",
	        { className: "data-source", style: this.props.styles },
	        _react2.default.createElement(
	          "a",
	          {
	            href: this.props.href },
	          "[source]"
	        )
	      );
	    }
	  }]);

	  return Source;
	}(_react.Component);

	Source.defaultProps = {
	  href: undefined,
	  styles: {}
	};

	exports.default = Source;

/***/ },
/* 221 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _ParentheticalReference = __webpack_require__(222);

	var _ParentheticalReference2 = _interopRequireDefault(_ParentheticalReference);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	exports.default = {
	  ref: _bib2.default,
	  cite: function cite(refId, page) {
	    return _react2.default.createElement(_ParentheticalReference2.default, {
	      refId: refId,
	      page: page
	    });
	  }
	};
	//noinspection JSUnresolvedVariable

/***/ },
/* 222 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var bibliography = __webpack_require__(211);

	var _class = function (_Component) {
	    _inherits(_class, _Component);

	    function _class() {
	        _classCallCheck(this, _class);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(_class).apply(this, arguments));
	    }

	    _createClass(_class, [{
	        key: 'render',
	        value: function render() {
	            var refId = this.props.refId;
	            var page = this.props.page;

	            var reference = refId;
	            if (typeof refId == 'string') {
	                reference = bibliography[refId];
	            }

	            if (!reference) {
	                throw new Error("Reference not found: " + refId);
	            }

	            // Create string for page
	            var strPage = reference.pages ? ", pp. " + reference.pages : "";
	            page = page ? page : reference.page;
	            if (page) {
	                strPage = ", p. " + page;
	            }

	            //Create string for name
	            var name = reference.author.lastName ? reference.author.lastName.toString() : reference.author.abbr;

	            //Create element
	            return _react2.default.createElement(
	                'a',
	                { className: 'inline-citation',
	                    href: "#" + reference.id.toString()
	                },
	                name,
	                ' (',
	                reference.year.toString(),
	                strPage.toString(),
	                ')'
	            );
	        }
	    }]);

	    return _class;
	}(_react.Component);

	exports.default = _class;

/***/ },
/* 223 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _StackedBarChart = __webpack_require__(226);

	var _StackedBarChart2 = _interopRequireDefault(_StackedBarChart);

	var _getData = __webpack_require__(243);

	var _getData2 = _interopRequireDefault(_getData);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var _class = function (_Component) {
	    _inherits(_class, _Component);

	    function _class() {
	        _classCallCheck(this, _class);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(_class).apply(this, arguments));
	    }

	    _createClass(_class, [{
	        key: 'render',
	        value: function render() {
	            // console.log(data)
	            //   console.log(figs.markupStats);
	            return _react2.default.createElement(
	                'figure',
	                { id: _figs2.default.markupStats.id },
	                _react2.default.createElement(
	                    'div',
	                    { className: 'figure-container' },
	                    _react2.default.createElement(_StackedBarChart2.default, {
	                        data: _getData2.default,
	                        sourceHref: _figs2.default.markupStats.url
	                    })
	                ),
	                _react2.default.createElement(
	                    'figcaption',
	                    null,
	                    _react2.default.createElement(
	                        'span',
	                        { className: 'figure-number' },
	                        'Fig ',
	                        _figs2.default.markupStats.num,
	                        '.'
	                    ),
	                    ' Chart showing the number documents with different kinds of markup. In particular, we are interested in the number of ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        '*.info'
	                    ),
	                    ' tags, which are headers that contain metadata about the case, and ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'section'
	                    ),
	                    ' tags, denoting sections.'
	                )
	            );
	        }
	    }]);

	    return _class;
	}(_react.Component);

	exports.default = _class;

/***/ },
/* 224 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _figs;

	var _data = __webpack_require__(225);

	function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

	var figs = (_figs = {
	    markupStats: {
	        id: 'markup-stats',
	        num: 1,
	        url: _data.url
	    },

	    titleTf: {
	        id: "fig-title-tf",
	        num: 3
	    },
	    figTitleWordCount: {
	        id: "fig-title-word-count",
	        num: 3
	    },
	    titleRelativeWordCount: {
	        id: "fig-title-relative-word-count",
	        num: 3
	    },

	    tfidf: {
	        id: "fig-title-tf-idf",
	        num: 4
	    },

	    figTitleNumbering: {
	        id: "fig-title-numbering",
	        num: 2
	    },
	    figTitleTreemap: {
	        id: "fig-title-patterns",
	        num: 2
	    },

	    hmm: function () {
	        var id = "fig-hmm";
	        var src = "/img/hmm.svg"; // TODO relativeurl
	        var alt = "Graphical representation of a Hidden Markov Model";
	        var num = 1;
	        return {
	            id: id,
	            src: src,
	            alt: alt,
	            num: num
	        };
	    }(),

	    graphicalModels: function () {
	        var id = "fig-models-relationships";
	        var src = "/img/graphical-models.svg"; // TODO relativeurl
	        var alt = "Diagram of the relationship between naive Bayes, logistic regression, HMMs, linear-chain CRFs, " + "generative models, and general CRFs";
	        var num = 1;
	        return {
	            id: id,
	            src: src,
	            alt: alt,
	            num: num
	        };
	    }()

	}, _defineProperty(_figs, 'tfidf', function () {
	    var id = "tfidf";
	    var num = 1;

	    return {
	        id: id,
	        num: num
	    };
	}()), _defineProperty(_figs, 'factorGraph', function () {
	    var id = "fig-factor-graph";
	    var src = "/img/factor-graph.svg"; //// TODO relativeurl
	    var alt = "Illustration of a factor graph. The set V represents all variable nodes; the set F represents all function nodes.";
	    var num = 1;

	    return {
	        id: id,
	        src: src,
	        alt: alt,
	        num: num
	    };
	}()), _figs);

	exports.default = figs;

/***/ },
/* 225 */
/***/ function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	var url = "http://rechtspraak.cloudant.com/docs/_design/stats/_view/richness_of_markup?group_level=2&stale=ok";
	exports.url = url;
	exports.default = {
	  rows: [{
	    key: [[0, "Contains <*.info> tag and <section> tag"], 1970],
	    value: 1
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 1991],
	    value: 1
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 1997],
	    value: 2
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2000],
	    value: 3
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2001],
	    value: 4
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2002],
	    value: 6
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2003],
	    value: 5
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2004],
	    value: 3
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2005],
	    value: 4
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2006],
	    value: 5
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2007],
	    value: 15
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2008],
	    value: 19
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2009],
	    value: 42
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2010],
	    value: 56
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2011],
	    value: 102
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2012],
	    value: 417
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2013],
	    value: 10162
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2014],
	    value: 17360
	  }, {
	    key: [[0, "Contains <*.info> tag and <section> tag"], 2015],
	    value: 15060
	  }, {
	    key: [[1, "Contains only <section> tag"], 1989],
	    value: 1
	  }, {
	    key: [[1, "Contains only <section> tag"], 1990],
	    value: 1
	  }, {
	    key: [[1, "Contains only <section> tag"], 1995],
	    value: 1
	  }, {
	    key: [[1, "Contains only <section> tag"], 1999],
	    value: 1
	  }, {
	    key: [[1, "Contains only <section> tag"], 2000],
	    value: 5
	  }, {
	    key: [[1, "Contains only <section> tag"], 2002],
	    value: 2
	  }, {
	    key: [[1, "Contains only <section> tag"], 2003],
	    value: 3
	  }, {
	    key: [[1, "Contains only <section> tag"], 2004],
	    value: 4
	  }, {
	    key: [[1, "Contains only <section> tag"], 2005],
	    value: 1
	  }, {
	    key: [[1, "Contains only <section> tag"], 2006],
	    value: 2
	  }, {
	    key: [[1, "Contains only <section> tag"], 2007],
	    value: 4
	  }, {
	    key: [[1, "Contains only <section> tag"], 2008],
	    value: 2
	  }, {
	    key: [[1, "Contains only <section> tag"], 2009],
	    value: 11
	  }, {
	    key: [[1, "Contains only <section> tag"], 2010],
	    value: 18
	  }, {
	    key: [[1, "Contains only <section> tag"], 2011],
	    value: 16
	  }, {
	    key: [[1, "Contains only <section> tag"], 2012],
	    value: 93
	  }, {
	    key: [[1, "Contains only <section> tag"], 2013],
	    value: 4515
	  }, {
	    key: [[1, "Contains only <section> tag"], 2014],
	    value: 7944
	  }, {
	    key: [[1, "Contains only <section> tag"], 2015],
	    value: 6361
	  }, {
	    key: [[2, "Contains <*.info> tag"], 2012],
	    value: 1
	  }, {
	    key: [[2, "Contains <*.info> tag"], 2013],
	    value: 3
	  }, {
	    key: [[2, "Contains <*.info> tag"], 2014],
	    value: 9
	  }, {
	    key: [[2, "Contains <*.info> tag"], 2015],
	    value: 10
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1976],
	    value: 1
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1977],
	    value: 2
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1979],
	    value: 1
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1982],
	    value: 1
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1983],
	    value: 4
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1984],
	    value: 7
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1985],
	    value: 9
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1986],
	    value: 14
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1987],
	    value: 14
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1988],
	    value: 12
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1989],
	    value: 17
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1990],
	    value: 13
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1991],
	    value: 9
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1992],
	    value: 18
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1993],
	    value: 27
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1994],
	    value: 151
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1995],
	    value: 530
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1996],
	    value: 568
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1997],
	    value: 723
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1998],
	    value: 1048
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 1999],
	    value: 2259
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2000],
	    value: 5443
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2001],
	    value: 7759
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2002],
	    value: 11218
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2003],
	    value: 12732
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2004],
	    value: 15310
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2005],
	    value: 17308
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2006],
	    value: 20582
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2007],
	    value: 23215
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2008],
	    value: 25906
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2009],
	    value: 28059
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2010],
	    value: 29870
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2011],
	    value: 32258
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2012],
	    value: 31073
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2013],
	    value: 16378
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2014],
	    value: 5669
	  }, {
	    key: [[3, "Contains neither <*.info> tag nor <section> tag"], 2015],
	    value: 4011
	  }]
	};

/***/ },
/* 226 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _underscore = __webpack_require__(210);

	var _underscore2 = _interopRequireDefault(_underscore);

	var _d = __webpack_require__(227);

	var _d2 = _interopRequireDefault(_d);

	var _Bar = __webpack_require__(228);

	var _Bar2 = _interopRequireDefault(_Bar);

	var _reactFauxDom = __webpack_require__(229);

	var _reactFauxDom2 = _interopRequireDefault(_reactFauxDom);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var StackedBarChart = function (_Component) {
	  _inherits(StackedBarChart, _Component);

	  function StackedBarChart() {
	    _classCallCheck(this, StackedBarChart);

	    return _possibleConstructorReturn(this, Object.getPrototypeOf(StackedBarChart).apply(this, arguments));
	  }

	  _createClass(StackedBarChart, [{
	    key: 'getHorizontalGridlines',
	    value: function getHorizontalGridlines(yScale, margin, innerWidth) {
	      var g = _reactFauxDom2.default.createElement('g');
	      _d2.default.select(g).selectAll("line.horizontalGrid").data(yScale.ticks(5)).enter().append("line").attr({
	        "class": "horizontalGrid",
	        "x1": margin.right,
	        "x2": innerWidth,
	        "y1": function y1(d) {
	          return yScale(d);
	        },
	        "y2": function y2(d) {
	          return yScale(d);
	        },

	        "fill": "none",
	        "shape-rendering": "crispEdges",
	        "stroke": "lightgray",
	        "stroke-width": "1px"
	      });
	      return _react2.default.createElement('g', {}, g.toReact());
	    }
	  }, {
	    key: 'getSeriesFunction',
	    value: function getSeriesFunction(color, xScale, yScale, innerHeight) {
	      return function (series, i) {
	        // console.log(color);
	        // console.log(series);
	        var groupColor = color(i);
	        // console.log(groupColor);
	        //console.log("series " + i + ", bars: " + series.length + ", colour: " + groupColor);
	        var bars = _underscore2.default.map(series, function (d, barIndex) {
	          return _react2.default.createElement(_Bar2.default, {
	            key: barIndex + "-" + d.x + "-" + d.y,
	            xScale: xScale,
	            yScale: yScale,
	            fill: groupColor,
	            height: innerHeight - yScale(d.y),
	            data: d,
	            i: barIndex
	          });
	        });

	        return _react2.default.createElement(
	          'g',
	          { key: i, className: 'bar-group' },
	          bars
	        );
	      };
	    }
	  }, {
	    key: 'createYAxis',
	    value: function createYAxis(yScale) {
	      var yAxis = _d2.default.svg.axis().scale(yScale).orient("left").ticks(5);

	      var yAxisEl = _reactFauxDom2.default.createElement("g");
	      _d2.default.select(yAxisEl).attr("class", "y axis").call(yAxis).append("text").attr("transform", "rotate(-90)").attr("y", 6).attr("dy", ".71em").style("text-anchor", "end").text("Document count");
	      return _react2.default.createElement('g', { className: "axis" }, yAxisEl.toReact());
	    }
	  }, {
	    key: 'createLegend',
	    value: function createLegend(dataStack, outerMargin, stackLabels, textSize, color) {
	      var legendHeight = 15;
	      var legendWidth = 25;
	      var legendSpacing = 4;
	      var labelMargin = {
	        top: 5,
	        right: 5,
	        bottom: 5,
	        left: 5
	      };
	      var labelOffsetLeft = outerMargin.right + labelMargin.left;
	      var labelOffsetTop = labelMargin.top;

	      var legendItems = _underscore2.default.map(dataStack, function (series, i) {
	        var yPos = i * (legendHeight + legendSpacing) + labelOffsetTop;

	        return _react2.default.createElement(
	          'g',
	          {
	            key: i,
	            className: "label series-" + (i + 1) },
	          _react2.default.createElement('rect', {
	            x: labelOffsetLeft,
	            y: yPos,
	            fill: color(i),
	            width: legendWidth,
	            height: legendHeight,
	            className: "series-" + (i + 1) }),
	          _react2.default.createElement(
	            'text',
	            { fill: '#000',
	              x: labelOffsetLeft + legendWidth + 5,
	              y: yPos + textSize
	            },
	            stackLabels[i]
	          )
	        );
	      });

	      return _react2.default.createElement(
	        'g',
	        { className: 'legend' },
	        _react2.default.createElement('rect', {
	          width: 115,
	          x: outerMargin.right,
	          height: (legendSpacing + legendHeight) * legendItems.length - legendSpacing + labelMargin.top + labelMargin.bottom,
	          fill: '#FFFFFF'
	        }),
	        legendItems
	      );
	    }
	  }, {
	    key: 'createXAxis',
	    value: function createXAxis(xScale, innerHeight) {
	      var xAxis = _d2.default.svg.axis().scale(xScale).orient("bottom").tickFormat(function (d, i) {
	        if (i % 3 == 1) return d;else return null;
	      });
	      //.ticks(d3.time.year, 10)
	      //.tickFormat("%Y")

	      var xAxisEl = _reactFauxDom2.default.createElement("g");

	      _d2.default.select(xAxisEl).attr("class", "x axis").attr("transform", "translate(0," + innerHeight + ")").call(xAxis);
	      return _react2.default.createElement('g', { className: "axis" }, xAxisEl.toReact());
	    }
	  }, {
	    key: 'render',
	    value: function render() {
	      var stackLabels = [];
	      var preStack = [];

	      //console.log(this.props.data)
	      // Split in two variables for use in d3
	      _underscore2.default.each(this.props.data, function (bucket) {
	        //console.log(bucket.data);
	        preStack.push(bucket.data);
	        stackLabels.push(bucket.title);
	      });

	      // Stacklabels like
	      // [<label>]

	      //console.log(stackLabels);
	      //console.log(this.props.data);
	      // console.log(preStack);

	      var dataStack = _d2.default.layout.stack()(preStack);
	      //console.log(dataStack);
	      var size = { width: this.props.width, height: this.props.height };
	      var margin = {
	        top: 35, right: 20, bottom: 30, left: 50
	      };
	      var textSize = 10;
	      var innerWidth = this.props.width - margin.left - margin.right;
	      var innerHeight = this.props.height - margin.top - margin.bottom;

	      var xValues = _underscore2.default.sortBy(_underscore2.default.uniq(_underscore2.default.map(_underscore2.default.flatten(dataStack), function (point) {
	        return point.x;
	      })), function (p) {
	        return p.x;
	      });

	      // console.log(this.props.colorRange);
	      // console.log(d3.keys(dataStack));
	      var color = _d2.default.scale.ordinal().range(this.props.colorRange).domain(_d2.default.keys(dataStack));

	      var xScale = _d2.default.scale.ordinal().domain(xValues).rangeRoundBands([0, innerWidth], .1);

	      var yVals = [];
	      var yMax = 0;
	      _underscore2.default.forEach(_underscore2.default.flatten(dataStack), function (e) {
	        if (typeof e.y != 'number') throw new Error("Expected numbers, but got " + e.y);
	        yVals[e.x] = (yVals[e.x] || 0) + e.y;
	        if (yVals[e.x] > yMax) yMax = yVals[e.x];
	      });
	      var yScale = _d2.default.scale.linear().domain([yMax, 0]).range([0, innerHeight]);

	      // Add series
	      var getSeriesAsReactElement = this.getSeriesFunction(color, xScale, yScale, innerHeight);

	      var barGroups = _underscore2.default.map(dataStack, getSeriesAsReactElement);

	      // Add horizontal grid lines
	      var gridLines = this.getHorizontalGridlines(yScale, margin, innerWidth);

	      var ax1 = this.createXAxis(xScale, innerHeight);
	      var ax2 = this.createYAxis(yScale);
	      var legend = stackLabels ? this.createLegend(dataStack, margin, stackLabels, textSize, color) : '';

	      //console.log(this.props.sourceHref);
	      var sourceAnchor = _react2.default.createElement(
	        'a',
	        { xlinkHref: this.props.sourceHref
	        },
	        _react2.default.createElement(
	          'text',
	          {
	            style: { textDecoration: "underline", fill: "#0000EE" },
	            textAnchor: 'start', x: 12, y: 20 },
	          'Data source'
	        )
	      );

	      //console.log("Rendered "+this.props.sourceHref);
	      return _react2.default.createElement(
	        'svg',
	        {
	          className: 'chart',
	          width: size.width,
	          height: size.height
	        },
	        _react2.default.createElement(
	          'g',
	          { transform: "translate(" + margin.left + "," + margin.top + ")" },
	          ax1,
	          ax2,
	          gridLines,
	          legend,
	          _react2.default.createElement(
	            'g',
	            null,
	            barGroups
	          )
	        ),
	        sourceAnchor
	      );
	    }
	  }]);

	  return StackedBarChart;
	}(_react.Component);

	StackedBarChart.defaultProps = {
	  alignLegend: 'left',
	  sourceHref: undefined,
	  width: 600,
	  height: 300,
	  stackLabels: undefined,
	  colorRange: ["#1E405C", "#2C70A9", "#56a0dd", "#9DCBF1", "#6b486b", "#a05d56", "#d0743c", "#ff8c00"]
	};
	StackedBarChart.propTypes = {
	  // Looks like
	  // [{
	  //   title: string,
	  //   data{
	  //       x: any,
	  //       y: any,
	  //   },*
	  // },*]
	  data: _react2.default.PropTypes.array.isRequired
	};
	exports.default = StackedBarChart;

/***/ },
/* 227 */
/***/ function(module, exports, __webpack_require__) {

	var __WEBPACK_AMD_DEFINE_FACTORY__, __WEBPACK_AMD_DEFINE_RESULT__;!function() {
	  var d3 = {
	    version: "3.5.17"
	  };
	  var d3_arraySlice = [].slice, d3_array = function(list) {
	    return d3_arraySlice.call(list);
	  };
	  var d3_document = this.document;
	  function d3_documentElement(node) {
	    return node && (node.ownerDocument || node.document || node).documentElement;
	  }
	  function d3_window(node) {
	    return node && (node.ownerDocument && node.ownerDocument.defaultView || node.document && node || node.defaultView);
	  }
	  if (d3_document) {
	    try {
	      d3_array(d3_document.documentElement.childNodes)[0].nodeType;
	    } catch (e) {
	      d3_array = function(list) {
	        var i = list.length, array = new Array(i);
	        while (i--) array[i] = list[i];
	        return array;
	      };
	    }
	  }
	  if (!Date.now) Date.now = function() {
	    return +new Date();
	  };
	  if (d3_document) {
	    try {
	      d3_document.createElement("DIV").style.setProperty("opacity", 0, "");
	    } catch (error) {
	      var d3_element_prototype = this.Element.prototype, d3_element_setAttribute = d3_element_prototype.setAttribute, d3_element_setAttributeNS = d3_element_prototype.setAttributeNS, d3_style_prototype = this.CSSStyleDeclaration.prototype, d3_style_setProperty = d3_style_prototype.setProperty;
	      d3_element_prototype.setAttribute = function(name, value) {
	        d3_element_setAttribute.call(this, name, value + "");
	      };
	      d3_element_prototype.setAttributeNS = function(space, local, value) {
	        d3_element_setAttributeNS.call(this, space, local, value + "");
	      };
	      d3_style_prototype.setProperty = function(name, value, priority) {
	        d3_style_setProperty.call(this, name, value + "", priority);
	      };
	    }
	  }
	  d3.ascending = d3_ascending;
	  function d3_ascending(a, b) {
	    return a < b ? -1 : a > b ? 1 : a >= b ? 0 : NaN;
	  }
	  d3.descending = function(a, b) {
	    return b < a ? -1 : b > a ? 1 : b >= a ? 0 : NaN;
	  };
	  d3.min = function(array, f) {
	    var i = -1, n = array.length, a, b;
	    if (arguments.length === 1) {
	      while (++i < n) if ((b = array[i]) != null && b >= b) {
	        a = b;
	        break;
	      }
	      while (++i < n) if ((b = array[i]) != null && a > b) a = b;
	    } else {
	      while (++i < n) if ((b = f.call(array, array[i], i)) != null && b >= b) {
	        a = b;
	        break;
	      }
	      while (++i < n) if ((b = f.call(array, array[i], i)) != null && a > b) a = b;
	    }
	    return a;
	  };
	  d3.max = function(array, f) {
	    var i = -1, n = array.length, a, b;
	    if (arguments.length === 1) {
	      while (++i < n) if ((b = array[i]) != null && b >= b) {
	        a = b;
	        break;
	      }
	      while (++i < n) if ((b = array[i]) != null && b > a) a = b;
	    } else {
	      while (++i < n) if ((b = f.call(array, array[i], i)) != null && b >= b) {
	        a = b;
	        break;
	      }
	      while (++i < n) if ((b = f.call(array, array[i], i)) != null && b > a) a = b;
	    }
	    return a;
	  };
	  d3.extent = function(array, f) {
	    var i = -1, n = array.length, a, b, c;
	    if (arguments.length === 1) {
	      while (++i < n) if ((b = array[i]) != null && b >= b) {
	        a = c = b;
	        break;
	      }
	      while (++i < n) if ((b = array[i]) != null) {
	        if (a > b) a = b;
	        if (c < b) c = b;
	      }
	    } else {
	      while (++i < n) if ((b = f.call(array, array[i], i)) != null && b >= b) {
	        a = c = b;
	        break;
	      }
	      while (++i < n) if ((b = f.call(array, array[i], i)) != null) {
	        if (a > b) a = b;
	        if (c < b) c = b;
	      }
	    }
	    return [ a, c ];
	  };
	  function d3_number(x) {
	    return x === null ? NaN : +x;
	  }
	  function d3_numeric(x) {
	    return !isNaN(x);
	  }
	  d3.sum = function(array, f) {
	    var s = 0, n = array.length, a, i = -1;
	    if (arguments.length === 1) {
	      while (++i < n) if (d3_numeric(a = +array[i])) s += a;
	    } else {
	      while (++i < n) if (d3_numeric(a = +f.call(array, array[i], i))) s += a;
	    }
	    return s;
	  };
	  d3.mean = function(array, f) {
	    var s = 0, n = array.length, a, i = -1, j = n;
	    if (arguments.length === 1) {
	      while (++i < n) if (d3_numeric(a = d3_number(array[i]))) s += a; else --j;
	    } else {
	      while (++i < n) if (d3_numeric(a = d3_number(f.call(array, array[i], i)))) s += a; else --j;
	    }
	    if (j) return s / j;
	  };
	  d3.quantile = function(values, p) {
	    var H = (values.length - 1) * p + 1, h = Math.floor(H), v = +values[h - 1], e = H - h;
	    return e ? v + e * (values[h] - v) : v;
	  };
	  d3.median = function(array, f) {
	    var numbers = [], n = array.length, a, i = -1;
	    if (arguments.length === 1) {
	      while (++i < n) if (d3_numeric(a = d3_number(array[i]))) numbers.push(a);
	    } else {
	      while (++i < n) if (d3_numeric(a = d3_number(f.call(array, array[i], i)))) numbers.push(a);
	    }
	    if (numbers.length) return d3.quantile(numbers.sort(d3_ascending), .5);
	  };
	  d3.variance = function(array, f) {
	    var n = array.length, m = 0, a, d, s = 0, i = -1, j = 0;
	    if (arguments.length === 1) {
	      while (++i < n) {
	        if (d3_numeric(a = d3_number(array[i]))) {
	          d = a - m;
	          m += d / ++j;
	          s += d * (a - m);
	        }
	      }
	    } else {
	      while (++i < n) {
	        if (d3_numeric(a = d3_number(f.call(array, array[i], i)))) {
	          d = a - m;
	          m += d / ++j;
	          s += d * (a - m);
	        }
	      }
	    }
	    if (j > 1) return s / (j - 1);
	  };
	  d3.deviation = function() {
	    var v = d3.variance.apply(this, arguments);
	    return v ? Math.sqrt(v) : v;
	  };
	  function d3_bisector(compare) {
	    return {
	      left: function(a, x, lo, hi) {
	        if (arguments.length < 3) lo = 0;
	        if (arguments.length < 4) hi = a.length;
	        while (lo < hi) {
	          var mid = lo + hi >>> 1;
	          if (compare(a[mid], x) < 0) lo = mid + 1; else hi = mid;
	        }
	        return lo;
	      },
	      right: function(a, x, lo, hi) {
	        if (arguments.length < 3) lo = 0;
	        if (arguments.length < 4) hi = a.length;
	        while (lo < hi) {
	          var mid = lo + hi >>> 1;
	          if (compare(a[mid], x) > 0) hi = mid; else lo = mid + 1;
	        }
	        return lo;
	      }
	    };
	  }
	  var d3_bisect = d3_bisector(d3_ascending);
	  d3.bisectLeft = d3_bisect.left;
	  d3.bisect = d3.bisectRight = d3_bisect.right;
	  d3.bisector = function(f) {
	    return d3_bisector(f.length === 1 ? function(d, x) {
	      return d3_ascending(f(d), x);
	    } : f);
	  };
	  d3.shuffle = function(array, i0, i1) {
	    if ((m = arguments.length) < 3) {
	      i1 = array.length;
	      if (m < 2) i0 = 0;
	    }
	    var m = i1 - i0, t, i;
	    while (m) {
	      i = Math.random() * m-- | 0;
	      t = array[m + i0], array[m + i0] = array[i + i0], array[i + i0] = t;
	    }
	    return array;
	  };
	  d3.permute = function(array, indexes) {
	    var i = indexes.length, permutes = new Array(i);
	    while (i--) permutes[i] = array[indexes[i]];
	    return permutes;
	  };
	  d3.pairs = function(array) {
	    var i = 0, n = array.length - 1, p0, p1 = array[0], pairs = new Array(n < 0 ? 0 : n);
	    while (i < n) pairs[i] = [ p0 = p1, p1 = array[++i] ];
	    return pairs;
	  };
	  d3.transpose = function(matrix) {
	    if (!(n = matrix.length)) return [];
	    for (var i = -1, m = d3.min(matrix, d3_transposeLength), transpose = new Array(m); ++i < m; ) {
	      for (var j = -1, n, row = transpose[i] = new Array(n); ++j < n; ) {
	        row[j] = matrix[j][i];
	      }
	    }
	    return transpose;
	  };
	  function d3_transposeLength(d) {
	    return d.length;
	  }
	  d3.zip = function() {
	    return d3.transpose(arguments);
	  };
	  d3.keys = function(map) {
	    var keys = [];
	    for (var key in map) keys.push(key);
	    return keys;
	  };
	  d3.values = function(map) {
	    var values = [];
	    for (var key in map) values.push(map[key]);
	    return values;
	  };
	  d3.entries = function(map) {
	    var entries = [];
	    for (var key in map) entries.push({
	      key: key,
	      value: map[key]
	    });
	    return entries;
	  };
	  d3.merge = function(arrays) {
	    var n = arrays.length, m, i = -1, j = 0, merged, array;
	    while (++i < n) j += arrays[i].length;
	    merged = new Array(j);
	    while (--n >= 0) {
	      array = arrays[n];
	      m = array.length;
	      while (--m >= 0) {
	        merged[--j] = array[m];
	      }
	    }
	    return merged;
	  };
	  var abs = Math.abs;
	  d3.range = function(start, stop, step) {
	    if (arguments.length < 3) {
	      step = 1;
	      if (arguments.length < 2) {
	        stop = start;
	        start = 0;
	      }
	    }
	    if ((stop - start) / step === Infinity) throw new Error("infinite range");
	    var range = [], k = d3_range_integerScale(abs(step)), i = -1, j;
	    start *= k, stop *= k, step *= k;
	    if (step < 0) while ((j = start + step * ++i) > stop) range.push(j / k); else while ((j = start + step * ++i) < stop) range.push(j / k);
	    return range;
	  };
	  function d3_range_integerScale(x) {
	    var k = 1;
	    while (x * k % 1) k *= 10;
	    return k;
	  }
	  function d3_class(ctor, properties) {
	    for (var key in properties) {
	      Object.defineProperty(ctor.prototype, key, {
	        value: properties[key],
	        enumerable: false
	      });
	    }
	  }
	  d3.map = function(object, f) {
	    var map = new d3_Map();
	    if (object instanceof d3_Map) {
	      object.forEach(function(key, value) {
	        map.set(key, value);
	      });
	    } else if (Array.isArray(object)) {
	      var i = -1, n = object.length, o;
	      if (arguments.length === 1) while (++i < n) map.set(i, object[i]); else while (++i < n) map.set(f.call(object, o = object[i], i), o);
	    } else {
	      for (var key in object) map.set(key, object[key]);
	    }
	    return map;
	  };
	  function d3_Map() {
	    this._ = Object.create(null);
	  }
	  var d3_map_proto = "__proto__", d3_map_zero = "\x00";
	  d3_class(d3_Map, {
	    has: d3_map_has,
	    get: function(key) {
	      return this._[d3_map_escape(key)];
	    },
	    set: function(key, value) {
	      return this._[d3_map_escape(key)] = value;
	    },
	    remove: d3_map_remove,
	    keys: d3_map_keys,
	    values: function() {
	      var values = [];
	      for (var key in this._) values.push(this._[key]);
	      return values;
	    },
	    entries: function() {
	      var entries = [];
	      for (var key in this._) entries.push({
	        key: d3_map_unescape(key),
	        value: this._[key]
	      });
	      return entries;
	    },
	    size: d3_map_size,
	    empty: d3_map_empty,
	    forEach: function(f) {
	      for (var key in this._) f.call(this, d3_map_unescape(key), this._[key]);
	    }
	  });
	  function d3_map_escape(key) {
	    return (key += "") === d3_map_proto || key[0] === d3_map_zero ? d3_map_zero + key : key;
	  }
	  function d3_map_unescape(key) {
	    return (key += "")[0] === d3_map_zero ? key.slice(1) : key;
	  }
	  function d3_map_has(key) {
	    return d3_map_escape(key) in this._;
	  }
	  function d3_map_remove(key) {
	    return (key = d3_map_escape(key)) in this._ && delete this._[key];
	  }
	  function d3_map_keys() {
	    var keys = [];
	    for (var key in this._) keys.push(d3_map_unescape(key));
	    return keys;
	  }
	  function d3_map_size() {
	    var size = 0;
	    for (var key in this._) ++size;
	    return size;
	  }
	  function d3_map_empty() {
	    for (var key in this._) return false;
	    return true;
	  }
	  d3.nest = function() {
	    var nest = {}, keys = [], sortKeys = [], sortValues, rollup;
	    function map(mapType, array, depth) {
	      if (depth >= keys.length) return rollup ? rollup.call(nest, array) : sortValues ? array.sort(sortValues) : array;
	      var i = -1, n = array.length, key = keys[depth++], keyValue, object, setter, valuesByKey = new d3_Map(), values;
	      while (++i < n) {
	        if (values = valuesByKey.get(keyValue = key(object = array[i]))) {
	          values.push(object);
	        } else {
	          valuesByKey.set(keyValue, [ object ]);
	        }
	      }
	      if (mapType) {
	        object = mapType();
	        setter = function(keyValue, values) {
	          object.set(keyValue, map(mapType, values, depth));
	        };
	      } else {
	        object = {};
	        setter = function(keyValue, values) {
	          object[keyValue] = map(mapType, values, depth);
	        };
	      }
	      valuesByKey.forEach(setter);
	      return object;
	    }
	    function entries(map, depth) {
	      if (depth >= keys.length) return map;
	      var array = [], sortKey = sortKeys[depth++];
	      map.forEach(function(key, keyMap) {
	        array.push({
	          key: key,
	          values: entries(keyMap, depth)
	        });
	      });
	      return sortKey ? array.sort(function(a, b) {
	        return sortKey(a.key, b.key);
	      }) : array;
	    }
	    nest.map = function(array, mapType) {
	      return map(mapType, array, 0);
	    };
	    nest.entries = function(array) {
	      return entries(map(d3.map, array, 0), 0);
	    };
	    nest.key = function(d) {
	      keys.push(d);
	      return nest;
	    };
	    nest.sortKeys = function(order) {
	      sortKeys[keys.length - 1] = order;
	      return nest;
	    };
	    nest.sortValues = function(order) {
	      sortValues = order;
	      return nest;
	    };
	    nest.rollup = function(f) {
	      rollup = f;
	      return nest;
	    };
	    return nest;
	  };
	  d3.set = function(array) {
	    var set = new d3_Set();
	    if (array) for (var i = 0, n = array.length; i < n; ++i) set.add(array[i]);
	    return set;
	  };
	  function d3_Set() {
	    this._ = Object.create(null);
	  }
	  d3_class(d3_Set, {
	    has: d3_map_has,
	    add: function(key) {
	      this._[d3_map_escape(key += "")] = true;
	      return key;
	    },
	    remove: d3_map_remove,
	    values: d3_map_keys,
	    size: d3_map_size,
	    empty: d3_map_empty,
	    forEach: function(f) {
	      for (var key in this._) f.call(this, d3_map_unescape(key));
	    }
	  });
	  d3.behavior = {};
	  function d3_identity(d) {
	    return d;
	  }
	  d3.rebind = function(target, source) {
	    var i = 1, n = arguments.length, method;
	    while (++i < n) target[method = arguments[i]] = d3_rebind(target, source, source[method]);
	    return target;
	  };
	  function d3_rebind(target, source, method) {
	    return function() {
	      var value = method.apply(source, arguments);
	      return value === source ? target : value;
	    };
	  }
	  function d3_vendorSymbol(object, name) {
	    if (name in object) return name;
	    name = name.charAt(0).toUpperCase() + name.slice(1);
	    for (var i = 0, n = d3_vendorPrefixes.length; i < n; ++i) {
	      var prefixName = d3_vendorPrefixes[i] + name;
	      if (prefixName in object) return prefixName;
	    }
	  }
	  var d3_vendorPrefixes = [ "webkit", "ms", "moz", "Moz", "o", "O" ];
	  function d3_noop() {}
	  d3.dispatch = function() {
	    var dispatch = new d3_dispatch(), i = -1, n = arguments.length;
	    while (++i < n) dispatch[arguments[i]] = d3_dispatch_event(dispatch);
	    return dispatch;
	  };
	  function d3_dispatch() {}
	  d3_dispatch.prototype.on = function(type, listener) {
	    var i = type.indexOf("."), name = "";
	    if (i >= 0) {
	      name = type.slice(i + 1);
	      type = type.slice(0, i);
	    }
	    if (type) return arguments.length < 2 ? this[type].on(name) : this[type].on(name, listener);
	    if (arguments.length === 2) {
	      if (listener == null) for (type in this) {
	        if (this.hasOwnProperty(type)) this[type].on(name, null);
	      }
	      return this;
	    }
	  };
	  function d3_dispatch_event(dispatch) {
	    var listeners = [], listenerByName = new d3_Map();
	    function event() {
	      var z = listeners, i = -1, n = z.length, l;
	      while (++i < n) if (l = z[i].on) l.apply(this, arguments);
	      return dispatch;
	    }
	    event.on = function(name, listener) {
	      var l = listenerByName.get(name), i;
	      if (arguments.length < 2) return l && l.on;
	      if (l) {
	        l.on = null;
	        listeners = listeners.slice(0, i = listeners.indexOf(l)).concat(listeners.slice(i + 1));
	        listenerByName.remove(name);
	      }
	      if (listener) listeners.push(listenerByName.set(name, {
	        on: listener
	      }));
	      return dispatch;
	    };
	    return event;
	  }
	  d3.event = null;
	  function d3_eventPreventDefault() {
	    d3.event.preventDefault();
	  }
	  function d3_eventSource() {
	    var e = d3.event, s;
	    while (s = e.sourceEvent) e = s;
	    return e;
	  }
	  function d3_eventDispatch(target) {
	    var dispatch = new d3_dispatch(), i = 0, n = arguments.length;
	    while (++i < n) dispatch[arguments[i]] = d3_dispatch_event(dispatch);
	    dispatch.of = function(thiz, argumentz) {
	      return function(e1) {
	        try {
	          var e0 = e1.sourceEvent = d3.event;
	          e1.target = target;
	          d3.event = e1;
	          dispatch[e1.type].apply(thiz, argumentz);
	        } finally {
	          d3.event = e0;
	        }
	      };
	    };
	    return dispatch;
	  }
	  d3.requote = function(s) {
	    return s.replace(d3_requote_re, "\\$&");
	  };
	  var d3_requote_re = /[\\\^\$\*\+\?\|\[\]\(\)\.\{\}]/g;
	  var d3_subclass = {}.__proto__ ? function(object, prototype) {
	    object.__proto__ = prototype;
	  } : function(object, prototype) {
	    for (var property in prototype) object[property] = prototype[property];
	  };
	  function d3_selection(groups) {
	    d3_subclass(groups, d3_selectionPrototype);
	    return groups;
	  }
	  var d3_select = function(s, n) {
	    return n.querySelector(s);
	  }, d3_selectAll = function(s, n) {
	    return n.querySelectorAll(s);
	  }, d3_selectMatches = function(n, s) {
	    var d3_selectMatcher = n.matches || n[d3_vendorSymbol(n, "matchesSelector")];
	    d3_selectMatches = function(n, s) {
	      return d3_selectMatcher.call(n, s);
	    };
	    return d3_selectMatches(n, s);
	  };
	  if (typeof Sizzle === "function") {
	    d3_select = function(s, n) {
	      return Sizzle(s, n)[0] || null;
	    };
	    d3_selectAll = Sizzle;
	    d3_selectMatches = Sizzle.matchesSelector;
	  }
	  d3.selection = function() {
	    return d3.select(d3_document.documentElement);
	  };
	  var d3_selectionPrototype = d3.selection.prototype = [];
	  d3_selectionPrototype.select = function(selector) {
	    var subgroups = [], subgroup, subnode, group, node;
	    selector = d3_selection_selector(selector);
	    for (var j = -1, m = this.length; ++j < m; ) {
	      subgroups.push(subgroup = []);
	      subgroup.parentNode = (group = this[j]).parentNode;
	      for (var i = -1, n = group.length; ++i < n; ) {
	        if (node = group[i]) {
	          subgroup.push(subnode = selector.call(node, node.__data__, i, j));
	          if (subnode && "__data__" in node) subnode.__data__ = node.__data__;
	        } else {
	          subgroup.push(null);
	        }
	      }
	    }
	    return d3_selection(subgroups);
	  };
	  function d3_selection_selector(selector) {
	    return typeof selector === "function" ? selector : function() {
	      return d3_select(selector, this);
	    };
	  }
	  d3_selectionPrototype.selectAll = function(selector) {
	    var subgroups = [], subgroup, node;
	    selector = d3_selection_selectorAll(selector);
	    for (var j = -1, m = this.length; ++j < m; ) {
	      for (var group = this[j], i = -1, n = group.length; ++i < n; ) {
	        if (node = group[i]) {
	          subgroups.push(subgroup = d3_array(selector.call(node, node.__data__, i, j)));
	          subgroup.parentNode = node;
	        }
	      }
	    }
	    return d3_selection(subgroups);
	  };
	  function d3_selection_selectorAll(selector) {
	    return typeof selector === "function" ? selector : function() {
	      return d3_selectAll(selector, this);
	    };
	  }
	  var d3_nsXhtml = "http://www.w3.org/1999/xhtml";
	  var d3_nsPrefix = {
	    svg: "http://www.w3.org/2000/svg",
	    xhtml: d3_nsXhtml,
	    xlink: "http://www.w3.org/1999/xlink",
	    xml: "http://www.w3.org/XML/1998/namespace",
	    xmlns: "http://www.w3.org/2000/xmlns/"
	  };
	  d3.ns = {
	    prefix: d3_nsPrefix,
	    qualify: function(name) {
	      var i = name.indexOf(":"), prefix = name;
	      if (i >= 0 && (prefix = name.slice(0, i)) !== "xmlns") name = name.slice(i + 1);
	      return d3_nsPrefix.hasOwnProperty(prefix) ? {
	        space: d3_nsPrefix[prefix],
	        local: name
	      } : name;
	    }
	  };
	  d3_selectionPrototype.attr = function(name, value) {
	    if (arguments.length < 2) {
	      if (typeof name === "string") {
	        var node = this.node();
	        name = d3.ns.qualify(name);
	        return name.local ? node.getAttributeNS(name.space, name.local) : node.getAttribute(name);
	      }
	      for (value in name) this.each(d3_selection_attr(value, name[value]));
	      return this;
	    }
	    return this.each(d3_selection_attr(name, value));
	  };
	  function d3_selection_attr(name, value) {
	    name = d3.ns.qualify(name);
	    function attrNull() {
	      this.removeAttribute(name);
	    }
	    function attrNullNS() {
	      this.removeAttributeNS(name.space, name.local);
	    }
	    function attrConstant() {
	      this.setAttribute(name, value);
	    }
	    function attrConstantNS() {
	      this.setAttributeNS(name.space, name.local, value);
	    }
	    function attrFunction() {
	      var x = value.apply(this, arguments);
	      if (x == null) this.removeAttribute(name); else this.setAttribute(name, x);
	    }
	    function attrFunctionNS() {
	      var x = value.apply(this, arguments);
	      if (x == null) this.removeAttributeNS(name.space, name.local); else this.setAttributeNS(name.space, name.local, x);
	    }
	    return value == null ? name.local ? attrNullNS : attrNull : typeof value === "function" ? name.local ? attrFunctionNS : attrFunction : name.local ? attrConstantNS : attrConstant;
	  }
	  function d3_collapse(s) {
	    return s.trim().replace(/\s+/g, " ");
	  }
	  d3_selectionPrototype.classed = function(name, value) {
	    if (arguments.length < 2) {
	      if (typeof name === "string") {
	        var node = this.node(), n = (name = d3_selection_classes(name)).length, i = -1;
	        if (value = node.classList) {
	          while (++i < n) if (!value.contains(name[i])) return false;
	        } else {
	          value = node.getAttribute("class");
	          while (++i < n) if (!d3_selection_classedRe(name[i]).test(value)) return false;
	        }
	        return true;
	      }
	      for (value in name) this.each(d3_selection_classed(value, name[value]));
	      return this;
	    }
	    return this.each(d3_selection_classed(name, value));
	  };
	  function d3_selection_classedRe(name) {
	    return new RegExp("(?:^|\\s+)" + d3.requote(name) + "(?:\\s+|$)", "g");
	  }
	  function d3_selection_classes(name) {
	    return (name + "").trim().split(/^|\s+/);
	  }
	  function d3_selection_classed(name, value) {
	    name = d3_selection_classes(name).map(d3_selection_classedName);
	    var n = name.length;
	    function classedConstant() {
	      var i = -1;
	      while (++i < n) name[i](this, value);
	    }
	    function classedFunction() {
	      var i = -1, x = value.apply(this, arguments);
	      while (++i < n) name[i](this, x);
	    }
	    return typeof value === "function" ? classedFunction : classedConstant;
	  }
	  function d3_selection_classedName(name) {
	    var re = d3_selection_classedRe(name);
	    return function(node, value) {
	      if (c = node.classList) return value ? c.add(name) : c.remove(name);
	      var c = node.getAttribute("class") || "";
	      if (value) {
	        re.lastIndex = 0;
	        if (!re.test(c)) node.setAttribute("class", d3_collapse(c + " " + name));
	      } else {
	        node.setAttribute("class", d3_collapse(c.replace(re, " ")));
	      }
	    };
	  }
	  d3_selectionPrototype.style = function(name, value, priority) {
	    var n = arguments.length;
	    if (n < 3) {
	      if (typeof name !== "string") {
	        if (n < 2) value = "";
	        for (priority in name) this.each(d3_selection_style(priority, name[priority], value));
	        return this;
	      }
	      if (n < 2) {
	        var node = this.node();
	        return d3_window(node).getComputedStyle(node, null).getPropertyValue(name);
	      }
	      priority = "";
	    }
	    return this.each(d3_selection_style(name, value, priority));
	  };
	  function d3_selection_style(name, value, priority) {
	    function styleNull() {
	      this.style.removeProperty(name);
	    }
	    function styleConstant() {
	      this.style.setProperty(name, value, priority);
	    }
	    function styleFunction() {
	      var x = value.apply(this, arguments);
	      if (x == null) this.style.removeProperty(name); else this.style.setProperty(name, x, priority);
	    }
	    return value == null ? styleNull : typeof value === "function" ? styleFunction : styleConstant;
	  }
	  d3_selectionPrototype.property = function(name, value) {
	    if (arguments.length < 2) {
	      if (typeof name === "string") return this.node()[name];
	      for (value in name) this.each(d3_selection_property(value, name[value]));
	      return this;
	    }
	    return this.each(d3_selection_property(name, value));
	  };
	  function d3_selection_property(name, value) {
	    function propertyNull() {
	      delete this[name];
	    }
	    function propertyConstant() {
	      this[name] = value;
	    }
	    function propertyFunction() {
	      var x = value.apply(this, arguments);
	      if (x == null) delete this[name]; else this[name] = x;
	    }
	    return value == null ? propertyNull : typeof value === "function" ? propertyFunction : propertyConstant;
	  }
	  d3_selectionPrototype.text = function(value) {
	    return arguments.length ? this.each(typeof value === "function" ? function() {
	      var v = value.apply(this, arguments);
	      this.textContent = v == null ? "" : v;
	    } : value == null ? function() {
	      this.textContent = "";
	    } : function() {
	      this.textContent = value;
	    }) : this.node().textContent;
	  };
	  d3_selectionPrototype.html = function(value) {
	    return arguments.length ? this.each(typeof value === "function" ? function() {
	      var v = value.apply(this, arguments);
	      this.innerHTML = v == null ? "" : v;
	    } : value == null ? function() {
	      this.innerHTML = "";
	    } : function() {
	      this.innerHTML = value;
	    }) : this.node().innerHTML;
	  };
	  d3_selectionPrototype.append = function(name) {
	    name = d3_selection_creator(name);
	    return this.select(function() {
	      return this.appendChild(name.apply(this, arguments));
	    });
	  };
	  function d3_selection_creator(name) {
	    function create() {
	      var document = this.ownerDocument, namespace = this.namespaceURI;
	      return namespace === d3_nsXhtml && document.documentElement.namespaceURI === d3_nsXhtml ? document.createElement(name) : document.createElementNS(namespace, name);
	    }
	    function createNS() {
	      return this.ownerDocument.createElementNS(name.space, name.local);
	    }
	    return typeof name === "function" ? name : (name = d3.ns.qualify(name)).local ? createNS : create;
	  }
	  d3_selectionPrototype.insert = function(name, before) {
	    name = d3_selection_creator(name);
	    before = d3_selection_selector(before);
	    return this.select(function() {
	      return this.insertBefore(name.apply(this, arguments), before.apply(this, arguments) || null);
	    });
	  };
	  d3_selectionPrototype.remove = function() {
	    return this.each(d3_selectionRemove);
	  };
	  function d3_selectionRemove() {
	    var parent = this.parentNode;
	    if (parent) parent.removeChild(this);
	  }
	  d3_selectionPrototype.data = function(value, key) {
	    var i = -1, n = this.length, group, node;
	    if (!arguments.length) {
	      value = new Array(n = (group = this[0]).length);
	      while (++i < n) {
	        if (node = group[i]) {
	          value[i] = node.__data__;
	        }
	      }
	      return value;
	    }
	    function bind(group, groupData) {
	      var i, n = group.length, m = groupData.length, n0 = Math.min(n, m), updateNodes = new Array(m), enterNodes = new Array(m), exitNodes = new Array(n), node, nodeData;
	      if (key) {
	        var nodeByKeyValue = new d3_Map(), keyValues = new Array(n), keyValue;
	        for (i = -1; ++i < n; ) {
	          if (node = group[i]) {
	            if (nodeByKeyValue.has(keyValue = key.call(node, node.__data__, i))) {
	              exitNodes[i] = node;
	            } else {
	              nodeByKeyValue.set(keyValue, node);
	            }
	            keyValues[i] = keyValue;
	          }
	        }
	        for (i = -1; ++i < m; ) {
	          if (!(node = nodeByKeyValue.get(keyValue = key.call(groupData, nodeData = groupData[i], i)))) {
	            enterNodes[i] = d3_selection_dataNode(nodeData);
	          } else if (node !== true) {
	            updateNodes[i] = node;
	            node.__data__ = nodeData;
	          }
	          nodeByKeyValue.set(keyValue, true);
	        }
	        for (i = -1; ++i < n; ) {
	          if (i in keyValues && nodeByKeyValue.get(keyValues[i]) !== true) {
	            exitNodes[i] = group[i];
	          }
	        }
	      } else {
	        for (i = -1; ++i < n0; ) {
	          node = group[i];
	          nodeData = groupData[i];
	          if (node) {
	            node.__data__ = nodeData;
	            updateNodes[i] = node;
	          } else {
	            enterNodes[i] = d3_selection_dataNode(nodeData);
	          }
	        }
	        for (;i < m; ++i) {
	          enterNodes[i] = d3_selection_dataNode(groupData[i]);
	        }
	        for (;i < n; ++i) {
	          exitNodes[i] = group[i];
	        }
	      }
	      enterNodes.update = updateNodes;
	      enterNodes.parentNode = updateNodes.parentNode = exitNodes.parentNode = group.parentNode;
	      enter.push(enterNodes);
	      update.push(updateNodes);
	      exit.push(exitNodes);
	    }
	    var enter = d3_selection_enter([]), update = d3_selection([]), exit = d3_selection([]);
	    if (typeof value === "function") {
	      while (++i < n) {
	        bind(group = this[i], value.call(group, group.parentNode.__data__, i));
	      }
	    } else {
	      while (++i < n) {
	        bind(group = this[i], value);
	      }
	    }
	    update.enter = function() {
	      return enter;
	    };
	    update.exit = function() {
	      return exit;
	    };
	    return update;
	  };
	  function d3_selection_dataNode(data) {
	    return {
	      __data__: data
	    };
	  }
	  d3_selectionPrototype.datum = function(value) {
	    return arguments.length ? this.property("__data__", value) : this.property("__data__");
	  };
	  d3_selectionPrototype.filter = function(filter) {
	    var subgroups = [], subgroup, group, node;
	    if (typeof filter !== "function") filter = d3_selection_filter(filter);
	    for (var j = 0, m = this.length; j < m; j++) {
	      subgroups.push(subgroup = []);
	      subgroup.parentNode = (group = this[j]).parentNode;
	      for (var i = 0, n = group.length; i < n; i++) {
	        if ((node = group[i]) && filter.call(node, node.__data__, i, j)) {
	          subgroup.push(node);
	        }
	      }
	    }
	    return d3_selection(subgroups);
	  };
	  function d3_selection_filter(selector) {
	    return function() {
	      return d3_selectMatches(this, selector);
	    };
	  }
	  d3_selectionPrototype.order = function() {
	    for (var j = -1, m = this.length; ++j < m; ) {
	      for (var group = this[j], i = group.length - 1, next = group[i], node; --i >= 0; ) {
	        if (node = group[i]) {
	          if (next && next !== node.nextSibling) next.parentNode.insertBefore(node, next);
	          next = node;
	        }
	      }
	    }
	    return this;
	  };
	  d3_selectionPrototype.sort = function(comparator) {
	    comparator = d3_selection_sortComparator.apply(this, arguments);
	    for (var j = -1, m = this.length; ++j < m; ) this[j].sort(comparator);
	    return this.order();
	  };
	  function d3_selection_sortComparator(comparator) {
	    if (!arguments.length) comparator = d3_ascending;
	    return function(a, b) {
	      return a && b ? comparator(a.__data__, b.__data__) : !a - !b;
	    };
	  }
	  d3_selectionPrototype.each = function(callback) {
	    return d3_selection_each(this, function(node, i, j) {
	      callback.call(node, node.__data__, i, j);
	    });
	  };
	  function d3_selection_each(groups, callback) {
	    for (var j = 0, m = groups.length; j < m; j++) {
	      for (var group = groups[j], i = 0, n = group.length, node; i < n; i++) {
	        if (node = group[i]) callback(node, i, j);
	      }
	    }
	    return groups;
	  }
	  d3_selectionPrototype.call = function(callback) {
	    var args = d3_array(arguments);
	    callback.apply(args[0] = this, args);
	    return this;
	  };
	  d3_selectionPrototype.empty = function() {
	    return !this.node();
	  };
	  d3_selectionPrototype.node = function() {
	    for (var j = 0, m = this.length; j < m; j++) {
	      for (var group = this[j], i = 0, n = group.length; i < n; i++) {
	        var node = group[i];
	        if (node) return node;
	      }
	    }
	    return null;
	  };
	  d3_selectionPrototype.size = function() {
	    var n = 0;
	    d3_selection_each(this, function() {
	      ++n;
	    });
	    return n;
	  };
	  function d3_selection_enter(selection) {
	    d3_subclass(selection, d3_selection_enterPrototype);
	    return selection;
	  }
	  var d3_selection_enterPrototype = [];
	  d3.selection.enter = d3_selection_enter;
	  d3.selection.enter.prototype = d3_selection_enterPrototype;
	  d3_selection_enterPrototype.append = d3_selectionPrototype.append;
	  d3_selection_enterPrototype.empty = d3_selectionPrototype.empty;
	  d3_selection_enterPrototype.node = d3_selectionPrototype.node;
	  d3_selection_enterPrototype.call = d3_selectionPrototype.call;
	  d3_selection_enterPrototype.size = d3_selectionPrototype.size;
	  d3_selection_enterPrototype.select = function(selector) {
	    var subgroups = [], subgroup, subnode, upgroup, group, node;
	    for (var j = -1, m = this.length; ++j < m; ) {
	      upgroup = (group = this[j]).update;
	      subgroups.push(subgroup = []);
	      subgroup.parentNode = group.parentNode;
	      for (var i = -1, n = group.length; ++i < n; ) {
	        if (node = group[i]) {
	          subgroup.push(upgroup[i] = subnode = selector.call(group.parentNode, node.__data__, i, j));
	          subnode.__data__ = node.__data__;
	        } else {
	          subgroup.push(null);
	        }
	      }
	    }
	    return d3_selection(subgroups);
	  };
	  d3_selection_enterPrototype.insert = function(name, before) {
	    if (arguments.length < 2) before = d3_selection_enterInsertBefore(this);
	    return d3_selectionPrototype.insert.call(this, name, before);
	  };
	  function d3_selection_enterInsertBefore(enter) {
	    var i0, j0;
	    return function(d, i, j) {
	      var group = enter[j].update, n = group.length, node;
	      if (j != j0) j0 = j, i0 = 0;
	      if (i >= i0) i0 = i + 1;
	      while (!(node = group[i0]) && ++i0 < n) ;
	      return node;
	    };
	  }
	  d3.select = function(node) {
	    var group;
	    if (typeof node === "string") {
	      group = [ d3_select(node, d3_document) ];
	      group.parentNode = d3_document.documentElement;
	    } else {
	      group = [ node ];
	      group.parentNode = d3_documentElement(node);
	    }
	    return d3_selection([ group ]);
	  };
	  d3.selectAll = function(nodes) {
	    var group;
	    if (typeof nodes === "string") {
	      group = d3_array(d3_selectAll(nodes, d3_document));
	      group.parentNode = d3_document.documentElement;
	    } else {
	      group = d3_array(nodes);
	      group.parentNode = null;
	    }
	    return d3_selection([ group ]);
	  };
	  d3_selectionPrototype.on = function(type, listener, capture) {
	    var n = arguments.length;
	    if (n < 3) {
	      if (typeof type !== "string") {
	        if (n < 2) listener = false;
	        for (capture in type) this.each(d3_selection_on(capture, type[capture], listener));
	        return this;
	      }
	      if (n < 2) return (n = this.node()["__on" + type]) && n._;
	      capture = false;
	    }
	    return this.each(d3_selection_on(type, listener, capture));
	  };
	  function d3_selection_on(type, listener, capture) {
	    var name = "__on" + type, i = type.indexOf("."), wrap = d3_selection_onListener;
	    if (i > 0) type = type.slice(0, i);
	    var filter = d3_selection_onFilters.get(type);
	    if (filter) type = filter, wrap = d3_selection_onFilter;
	    function onRemove() {
	      var l = this[name];
	      if (l) {
	        this.removeEventListener(type, l, l.$);
	        delete this[name];
	      }
	    }
	    function onAdd() {
	      var l = wrap(listener, d3_array(arguments));
	      onRemove.call(this);
	      this.addEventListener(type, this[name] = l, l.$ = capture);
	      l._ = listener;
	    }
	    function removeAll() {
	      var re = new RegExp("^__on([^.]+)" + d3.requote(type) + "$"), match;
	      for (var name in this) {
	        if (match = name.match(re)) {
	          var l = this[name];
	          this.removeEventListener(match[1], l, l.$);
	          delete this[name];
	        }
	      }
	    }
	    return i ? listener ? onAdd : onRemove : listener ? d3_noop : removeAll;
	  }
	  var d3_selection_onFilters = d3.map({
	    mouseenter: "mouseover",
	    mouseleave: "mouseout"
	  });
	  if (d3_document) {
	    d3_selection_onFilters.forEach(function(k) {
	      if ("on" + k in d3_document) d3_selection_onFilters.remove(k);
	    });
	  }
	  function d3_selection_onListener(listener, argumentz) {
	    return function(e) {
	      var o = d3.event;
	      d3.event = e;
	      argumentz[0] = this.__data__;
	      try {
	        listener.apply(this, argumentz);
	      } finally {
	        d3.event = o;
	      }
	    };
	  }
	  function d3_selection_onFilter(listener, argumentz) {
	    var l = d3_selection_onListener(listener, argumentz);
	    return function(e) {
	      var target = this, related = e.relatedTarget;
	      if (!related || related !== target && !(related.compareDocumentPosition(target) & 8)) {
	        l.call(target, e);
	      }
	    };
	  }
	  var d3_event_dragSelect, d3_event_dragId = 0;
	  function d3_event_dragSuppress(node) {
	    var name = ".dragsuppress-" + ++d3_event_dragId, click = "click" + name, w = d3.select(d3_window(node)).on("touchmove" + name, d3_eventPreventDefault).on("dragstart" + name, d3_eventPreventDefault).on("selectstart" + name, d3_eventPreventDefault);
	    if (d3_event_dragSelect == null) {
	      d3_event_dragSelect = "onselectstart" in node ? false : d3_vendorSymbol(node.style, "userSelect");
	    }
	    if (d3_event_dragSelect) {
	      var style = d3_documentElement(node).style, select = style[d3_event_dragSelect];
	      style[d3_event_dragSelect] = "none";
	    }
	    return function(suppressClick) {
	      w.on(name, null);
	      if (d3_event_dragSelect) style[d3_event_dragSelect] = select;
	      if (suppressClick) {
	        var off = function() {
	          w.on(click, null);
	        };
	        w.on(click, function() {
	          d3_eventPreventDefault();
	          off();
	        }, true);
	        setTimeout(off, 0);
	      }
	    };
	  }
	  d3.mouse = function(container) {
	    return d3_mousePoint(container, d3_eventSource());
	  };
	  var d3_mouse_bug44083 = this.navigator && /WebKit/.test(this.navigator.userAgent) ? -1 : 0;
	  function d3_mousePoint(container, e) {
	    if (e.changedTouches) e = e.changedTouches[0];
	    var svg = container.ownerSVGElement || container;
	    if (svg.createSVGPoint) {
	      var point = svg.createSVGPoint();
	      if (d3_mouse_bug44083 < 0) {
	        var window = d3_window(container);
	        if (window.scrollX || window.scrollY) {
	          svg = d3.select("body").append("svg").style({
	            position: "absolute",
	            top: 0,
	            left: 0,
	            margin: 0,
	            padding: 0,
	            border: "none"
	          }, "important");
	          var ctm = svg[0][0].getScreenCTM();
	          d3_mouse_bug44083 = !(ctm.f || ctm.e);
	          svg.remove();
	        }
	      }
	      if (d3_mouse_bug44083) point.x = e.pageX, point.y = e.pageY; else point.x = e.clientX, 
	      point.y = e.clientY;
	      point = point.matrixTransform(container.getScreenCTM().inverse());
	      return [ point.x, point.y ];
	    }
	    var rect = container.getBoundingClientRect();
	    return [ e.clientX - rect.left - container.clientLeft, e.clientY - rect.top - container.clientTop ];
	  }
	  d3.touch = function(container, touches, identifier) {
	    if (arguments.length < 3) identifier = touches, touches = d3_eventSource().changedTouches;
	    if (touches) for (var i = 0, n = touches.length, touch; i < n; ++i) {
	      if ((touch = touches[i]).identifier === identifier) {
	        return d3_mousePoint(container, touch);
	      }
	    }
	  };
	  d3.behavior.drag = function() {
	    var event = d3_eventDispatch(drag, "drag", "dragstart", "dragend"), origin = null, mousedown = dragstart(d3_noop, d3.mouse, d3_window, "mousemove", "mouseup"), touchstart = dragstart(d3_behavior_dragTouchId, d3.touch, d3_identity, "touchmove", "touchend");
	    function drag() {
	      this.on("mousedown.drag", mousedown).on("touchstart.drag", touchstart);
	    }
	    function dragstart(id, position, subject, move, end) {
	      return function() {
	        var that = this, target = d3.event.target.correspondingElement || d3.event.target, parent = that.parentNode, dispatch = event.of(that, arguments), dragged = 0, dragId = id(), dragName = ".drag" + (dragId == null ? "" : "-" + dragId), dragOffset, dragSubject = d3.select(subject(target)).on(move + dragName, moved).on(end + dragName, ended), dragRestore = d3_event_dragSuppress(target), position0 = position(parent, dragId);
	        if (origin) {
	          dragOffset = origin.apply(that, arguments);
	          dragOffset = [ dragOffset.x - position0[0], dragOffset.y - position0[1] ];
	        } else {
	          dragOffset = [ 0, 0 ];
	        }
	        dispatch({
	          type: "dragstart"
	        });
	        function moved() {
	          var position1 = position(parent, dragId), dx, dy;
	          if (!position1) return;
	          dx = position1[0] - position0[0];
	          dy = position1[1] - position0[1];
	          dragged |= dx | dy;
	          position0 = position1;
	          dispatch({
	            type: "drag",
	            x: position1[0] + dragOffset[0],
	            y: position1[1] + dragOffset[1],
	            dx: dx,
	            dy: dy
	          });
	        }
	        function ended() {
	          if (!position(parent, dragId)) return;
	          dragSubject.on(move + dragName, null).on(end + dragName, null);
	          dragRestore(dragged);
	          dispatch({
	            type: "dragend"
	          });
	        }
	      };
	    }
	    drag.origin = function(x) {
	      if (!arguments.length) return origin;
	      origin = x;
	      return drag;
	    };
	    return d3.rebind(drag, event, "on");
	  };
	  function d3_behavior_dragTouchId() {
	    return d3.event.changedTouches[0].identifier;
	  }
	  d3.touches = function(container, touches) {
	    if (arguments.length < 2) touches = d3_eventSource().touches;
	    return touches ? d3_array(touches).map(function(touch) {
	      var point = d3_mousePoint(container, touch);
	      point.identifier = touch.identifier;
	      return point;
	    }) : [];
	  };
	  var ε = 1e-6, ε2 = ε * ε, π = Math.PI, τ = 2 * π, τε = τ - ε, halfπ = π / 2, d3_radians = π / 180, d3_degrees = 180 / π;
	  function d3_sgn(x) {
	    return x > 0 ? 1 : x < 0 ? -1 : 0;
	  }
	  function d3_cross2d(a, b, c) {
	    return (b[0] - a[0]) * (c[1] - a[1]) - (b[1] - a[1]) * (c[0] - a[0]);
	  }
	  function d3_acos(x) {
	    return x > 1 ? 0 : x < -1 ? π : Math.acos(x);
	  }
	  function d3_asin(x) {
	    return x > 1 ? halfπ : x < -1 ? -halfπ : Math.asin(x);
	  }
	  function d3_sinh(x) {
	    return ((x = Math.exp(x)) - 1 / x) / 2;
	  }
	  function d3_cosh(x) {
	    return ((x = Math.exp(x)) + 1 / x) / 2;
	  }
	  function d3_tanh(x) {
	    return ((x = Math.exp(2 * x)) - 1) / (x + 1);
	  }
	  function d3_haversin(x) {
	    return (x = Math.sin(x / 2)) * x;
	  }
	  var ρ = Math.SQRT2, ρ2 = 2, ρ4 = 4;
	  d3.interpolateZoom = function(p0, p1) {
	    var ux0 = p0[0], uy0 = p0[1], w0 = p0[2], ux1 = p1[0], uy1 = p1[1], w1 = p1[2], dx = ux1 - ux0, dy = uy1 - uy0, d2 = dx * dx + dy * dy, i, S;
	    if (d2 < ε2) {
	      S = Math.log(w1 / w0) / ρ;
	      i = function(t) {
	        return [ ux0 + t * dx, uy0 + t * dy, w0 * Math.exp(ρ * t * S) ];
	      };
	    } else {
	      var d1 = Math.sqrt(d2), b0 = (w1 * w1 - w0 * w0 + ρ4 * d2) / (2 * w0 * ρ2 * d1), b1 = (w1 * w1 - w0 * w0 - ρ4 * d2) / (2 * w1 * ρ2 * d1), r0 = Math.log(Math.sqrt(b0 * b0 + 1) - b0), r1 = Math.log(Math.sqrt(b1 * b1 + 1) - b1);
	      S = (r1 - r0) / ρ;
	      i = function(t) {
	        var s = t * S, coshr0 = d3_cosh(r0), u = w0 / (ρ2 * d1) * (coshr0 * d3_tanh(ρ * s + r0) - d3_sinh(r0));
	        return [ ux0 + u * dx, uy0 + u * dy, w0 * coshr0 / d3_cosh(ρ * s + r0) ];
	      };
	    }
	    i.duration = S * 1e3;
	    return i;
	  };
	  d3.behavior.zoom = function() {
	    var view = {
	      x: 0,
	      y: 0,
	      k: 1
	    }, translate0, center0, center, size = [ 960, 500 ], scaleExtent = d3_behavior_zoomInfinity, duration = 250, zooming = 0, mousedown = "mousedown.zoom", mousemove = "mousemove.zoom", mouseup = "mouseup.zoom", mousewheelTimer, touchstart = "touchstart.zoom", touchtime, event = d3_eventDispatch(zoom, "zoomstart", "zoom", "zoomend"), x0, x1, y0, y1;
	    if (!d3_behavior_zoomWheel) {
	      d3_behavior_zoomWheel = "onwheel" in d3_document ? (d3_behavior_zoomDelta = function() {
	        return -d3.event.deltaY * (d3.event.deltaMode ? 120 : 1);
	      }, "wheel") : "onmousewheel" in d3_document ? (d3_behavior_zoomDelta = function() {
	        return d3.event.wheelDelta;
	      }, "mousewheel") : (d3_behavior_zoomDelta = function() {
	        return -d3.event.detail;
	      }, "MozMousePixelScroll");
	    }
	    function zoom(g) {
	      g.on(mousedown, mousedowned).on(d3_behavior_zoomWheel + ".zoom", mousewheeled).on("dblclick.zoom", dblclicked).on(touchstart, touchstarted);
	    }
	    zoom.event = function(g) {
	      g.each(function() {
	        var dispatch = event.of(this, arguments), view1 = view;
	        if (d3_transitionInheritId) {
	          d3.select(this).transition().each("start.zoom", function() {
	            view = this.__chart__ || {
	              x: 0,
	              y: 0,
	              k: 1
	            };
	            zoomstarted(dispatch);
	          }).tween("zoom:zoom", function() {
	            var dx = size[0], dy = size[1], cx = center0 ? center0[0] : dx / 2, cy = center0 ? center0[1] : dy / 2, i = d3.interpolateZoom([ (cx - view.x) / view.k, (cy - view.y) / view.k, dx / view.k ], [ (cx - view1.x) / view1.k, (cy - view1.y) / view1.k, dx / view1.k ]);
	            return function(t) {
	              var l = i(t), k = dx / l[2];
	              this.__chart__ = view = {
	                x: cx - l[0] * k,
	                y: cy - l[1] * k,
	                k: k
	              };
	              zoomed(dispatch);
	            };
	          }).each("interrupt.zoom", function() {
	            zoomended(dispatch);
	          }).each("end.zoom", function() {
	            zoomended(dispatch);
	          });
	        } else {
	          this.__chart__ = view;
	          zoomstarted(dispatch);
	          zoomed(dispatch);
	          zoomended(dispatch);
	        }
	      });
	    };
	    zoom.translate = function(_) {
	      if (!arguments.length) return [ view.x, view.y ];
	      view = {
	        x: +_[0],
	        y: +_[1],
	        k: view.k
	      };
	      rescale();
	      return zoom;
	    };
	    zoom.scale = function(_) {
	      if (!arguments.length) return view.k;
	      view = {
	        x: view.x,
	        y: view.y,
	        k: null
	      };
	      scaleTo(+_);
	      rescale();
	      return zoom;
	    };
	    zoom.scaleExtent = function(_) {
	      if (!arguments.length) return scaleExtent;
	      scaleExtent = _ == null ? d3_behavior_zoomInfinity : [ +_[0], +_[1] ];
	      return zoom;
	    };
	    zoom.center = function(_) {
	      if (!arguments.length) return center;
	      center = _ && [ +_[0], +_[1] ];
	      return zoom;
	    };
	    zoom.size = function(_) {
	      if (!arguments.length) return size;
	      size = _ && [ +_[0], +_[1] ];
	      return zoom;
	    };
	    zoom.duration = function(_) {
	      if (!arguments.length) return duration;
	      duration = +_;
	      return zoom;
	    };
	    zoom.x = function(z) {
	      if (!arguments.length) return x1;
	      x1 = z;
	      x0 = z.copy();
	      view = {
	        x: 0,
	        y: 0,
	        k: 1
	      };
	      return zoom;
	    };
	    zoom.y = function(z) {
	      if (!arguments.length) return y1;
	      y1 = z;
	      y0 = z.copy();
	      view = {
	        x: 0,
	        y: 0,
	        k: 1
	      };
	      return zoom;
	    };
	    function location(p) {
	      return [ (p[0] - view.x) / view.k, (p[1] - view.y) / view.k ];
	    }
	    function point(l) {
	      return [ l[0] * view.k + view.x, l[1] * view.k + view.y ];
	    }
	    function scaleTo(s) {
	      view.k = Math.max(scaleExtent[0], Math.min(scaleExtent[1], s));
	    }
	    function translateTo(p, l) {
	      l = point(l);
	      view.x += p[0] - l[0];
	      view.y += p[1] - l[1];
	    }
	    function zoomTo(that, p, l, k) {
	      that.__chart__ = {
	        x: view.x,
	        y: view.y,
	        k: view.k
	      };
	      scaleTo(Math.pow(2, k));
	      translateTo(center0 = p, l);
	      that = d3.select(that);
	      if (duration > 0) that = that.transition().duration(duration);
	      that.call(zoom.event);
	    }
	    function rescale() {
	      if (x1) x1.domain(x0.range().map(function(x) {
	        return (x - view.x) / view.k;
	      }).map(x0.invert));
	      if (y1) y1.domain(y0.range().map(function(y) {
	        return (y - view.y) / view.k;
	      }).map(y0.invert));
	    }
	    function zoomstarted(dispatch) {
	      if (!zooming++) dispatch({
	        type: "zoomstart"
	      });
	    }
	    function zoomed(dispatch) {
	      rescale();
	      dispatch({
	        type: "zoom",
	        scale: view.k,
	        translate: [ view.x, view.y ]
	      });
	    }
	    function zoomended(dispatch) {
	      if (!--zooming) dispatch({
	        type: "zoomend"
	      }), center0 = null;
	    }
	    function mousedowned() {
	      var that = this, dispatch = event.of(that, arguments), dragged = 0, subject = d3.select(d3_window(that)).on(mousemove, moved).on(mouseup, ended), location0 = location(d3.mouse(that)), dragRestore = d3_event_dragSuppress(that);
	      d3_selection_interrupt.call(that);
	      zoomstarted(dispatch);
	      function moved() {
	        dragged = 1;
	        translateTo(d3.mouse(that), location0);
	        zoomed(dispatch);
	      }
	      function ended() {
	        subject.on(mousemove, null).on(mouseup, null);
	        dragRestore(dragged);
	        zoomended(dispatch);
	      }
	    }
	    function touchstarted() {
	      var that = this, dispatch = event.of(that, arguments), locations0 = {}, distance0 = 0, scale0, zoomName = ".zoom-" + d3.event.changedTouches[0].identifier, touchmove = "touchmove" + zoomName, touchend = "touchend" + zoomName, targets = [], subject = d3.select(that), dragRestore = d3_event_dragSuppress(that);
	      started();
	      zoomstarted(dispatch);
	      subject.on(mousedown, null).on(touchstart, started);
	      function relocate() {
	        var touches = d3.touches(that);
	        scale0 = view.k;
	        touches.forEach(function(t) {
	          if (t.identifier in locations0) locations0[t.identifier] = location(t);
	        });
	        return touches;
	      }
	      function started() {
	        var target = d3.event.target;
	        d3.select(target).on(touchmove, moved).on(touchend, ended);
	        targets.push(target);
	        var changed = d3.event.changedTouches;
	        for (var i = 0, n = changed.length; i < n; ++i) {
	          locations0[changed[i].identifier] = null;
	        }
	        var touches = relocate(), now = Date.now();
	        if (touches.length === 1) {
	          if (now - touchtime < 500) {
	            var p = touches[0];
	            zoomTo(that, p, locations0[p.identifier], Math.floor(Math.log(view.k) / Math.LN2) + 1);
	            d3_eventPreventDefault();
	          }
	          touchtime = now;
	        } else if (touches.length > 1) {
	          var p = touches[0], q = touches[1], dx = p[0] - q[0], dy = p[1] - q[1];
	          distance0 = dx * dx + dy * dy;
	        }
	      }
	      function moved() {
	        var touches = d3.touches(that), p0, l0, p1, l1;
	        d3_selection_interrupt.call(that);
	        for (var i = 0, n = touches.length; i < n; ++i, l1 = null) {
	          p1 = touches[i];
	          if (l1 = locations0[p1.identifier]) {
	            if (l0) break;
	            p0 = p1, l0 = l1;
	          }
	        }
	        if (l1) {
	          var distance1 = (distance1 = p1[0] - p0[0]) * distance1 + (distance1 = p1[1] - p0[1]) * distance1, scale1 = distance0 && Math.sqrt(distance1 / distance0);
	          p0 = [ (p0[0] + p1[0]) / 2, (p0[1] + p1[1]) / 2 ];
	          l0 = [ (l0[0] + l1[0]) / 2, (l0[1] + l1[1]) / 2 ];
	          scaleTo(scale1 * scale0);
	        }
	        touchtime = null;
	        translateTo(p0, l0);
	        zoomed(dispatch);
	      }
	      function ended() {
	        if (d3.event.touches.length) {
	          var changed = d3.event.changedTouches;
	          for (var i = 0, n = changed.length; i < n; ++i) {
	            delete locations0[changed[i].identifier];
	          }
	          for (var identifier in locations0) {
	            return void relocate();
	          }
	        }
	        d3.selectAll(targets).on(zoomName, null);
	        subject.on(mousedown, mousedowned).on(touchstart, touchstarted);
	        dragRestore();
	        zoomended(dispatch);
	      }
	    }
	    function mousewheeled() {
	      var dispatch = event.of(this, arguments);
	      if (mousewheelTimer) clearTimeout(mousewheelTimer); else d3_selection_interrupt.call(this), 
	      translate0 = location(center0 = center || d3.mouse(this)), zoomstarted(dispatch);
	      mousewheelTimer = setTimeout(function() {
	        mousewheelTimer = null;
	        zoomended(dispatch);
	      }, 50);
	      d3_eventPreventDefault();
	      scaleTo(Math.pow(2, d3_behavior_zoomDelta() * .002) * view.k);
	      translateTo(center0, translate0);
	      zoomed(dispatch);
	    }
	    function dblclicked() {
	      var p = d3.mouse(this), k = Math.log(view.k) / Math.LN2;
	      zoomTo(this, p, location(p), d3.event.shiftKey ? Math.ceil(k) - 1 : Math.floor(k) + 1);
	    }
	    return d3.rebind(zoom, event, "on");
	  };
	  var d3_behavior_zoomInfinity = [ 0, Infinity ], d3_behavior_zoomDelta, d3_behavior_zoomWheel;
	  d3.color = d3_color;
	  function d3_color() {}
	  d3_color.prototype.toString = function() {
	    return this.rgb() + "";
	  };
	  d3.hsl = d3_hsl;
	  function d3_hsl(h, s, l) {
	    return this instanceof d3_hsl ? void (this.h = +h, this.s = +s, this.l = +l) : arguments.length < 2 ? h instanceof d3_hsl ? new d3_hsl(h.h, h.s, h.l) : d3_rgb_parse("" + h, d3_rgb_hsl, d3_hsl) : new d3_hsl(h, s, l);
	  }
	  var d3_hslPrototype = d3_hsl.prototype = new d3_color();
	  d3_hslPrototype.brighter = function(k) {
	    k = Math.pow(.7, arguments.length ? k : 1);
	    return new d3_hsl(this.h, this.s, this.l / k);
	  };
	  d3_hslPrototype.darker = function(k) {
	    k = Math.pow(.7, arguments.length ? k : 1);
	    return new d3_hsl(this.h, this.s, k * this.l);
	  };
	  d3_hslPrototype.rgb = function() {
	    return d3_hsl_rgb(this.h, this.s, this.l);
	  };
	  function d3_hsl_rgb(h, s, l) {
	    var m1, m2;
	    h = isNaN(h) ? 0 : (h %= 360) < 0 ? h + 360 : h;
	    s = isNaN(s) ? 0 : s < 0 ? 0 : s > 1 ? 1 : s;
	    l = l < 0 ? 0 : l > 1 ? 1 : l;
	    m2 = l <= .5 ? l * (1 + s) : l + s - l * s;
	    m1 = 2 * l - m2;
	    function v(h) {
	      if (h > 360) h -= 360; else if (h < 0) h += 360;
	      if (h < 60) return m1 + (m2 - m1) * h / 60;
	      if (h < 180) return m2;
	      if (h < 240) return m1 + (m2 - m1) * (240 - h) / 60;
	      return m1;
	    }
	    function vv(h) {
	      return Math.round(v(h) * 255);
	    }
	    return new d3_rgb(vv(h + 120), vv(h), vv(h - 120));
	  }
	  d3.hcl = d3_hcl;
	  function d3_hcl(h, c, l) {
	    return this instanceof d3_hcl ? void (this.h = +h, this.c = +c, this.l = +l) : arguments.length < 2 ? h instanceof d3_hcl ? new d3_hcl(h.h, h.c, h.l) : h instanceof d3_lab ? d3_lab_hcl(h.l, h.a, h.b) : d3_lab_hcl((h = d3_rgb_lab((h = d3.rgb(h)).r, h.g, h.b)).l, h.a, h.b) : new d3_hcl(h, c, l);
	  }
	  var d3_hclPrototype = d3_hcl.prototype = new d3_color();
	  d3_hclPrototype.brighter = function(k) {
	    return new d3_hcl(this.h, this.c, Math.min(100, this.l + d3_lab_K * (arguments.length ? k : 1)));
	  };
	  d3_hclPrototype.darker = function(k) {
	    return new d3_hcl(this.h, this.c, Math.max(0, this.l - d3_lab_K * (arguments.length ? k : 1)));
	  };
	  d3_hclPrototype.rgb = function() {
	    return d3_hcl_lab(this.h, this.c, this.l).rgb();
	  };
	  function d3_hcl_lab(h, c, l) {
	    if (isNaN(h)) h = 0;
	    if (isNaN(c)) c = 0;
	    return new d3_lab(l, Math.cos(h *= d3_radians) * c, Math.sin(h) * c);
	  }
	  d3.lab = d3_lab;
	  function d3_lab(l, a, b) {
	    return this instanceof d3_lab ? void (this.l = +l, this.a = +a, this.b = +b) : arguments.length < 2 ? l instanceof d3_lab ? new d3_lab(l.l, l.a, l.b) : l instanceof d3_hcl ? d3_hcl_lab(l.h, l.c, l.l) : d3_rgb_lab((l = d3_rgb(l)).r, l.g, l.b) : new d3_lab(l, a, b);
	  }
	  var d3_lab_K = 18;
	  var d3_lab_X = .95047, d3_lab_Y = 1, d3_lab_Z = 1.08883;
	  var d3_labPrototype = d3_lab.prototype = new d3_color();
	  d3_labPrototype.brighter = function(k) {
	    return new d3_lab(Math.min(100, this.l + d3_lab_K * (arguments.length ? k : 1)), this.a, this.b);
	  };
	  d3_labPrototype.darker = function(k) {
	    return new d3_lab(Math.max(0, this.l - d3_lab_K * (arguments.length ? k : 1)), this.a, this.b);
	  };
	  d3_labPrototype.rgb = function() {
	    return d3_lab_rgb(this.l, this.a, this.b);
	  };
	  function d3_lab_rgb(l, a, b) {
	    var y = (l + 16) / 116, x = y + a / 500, z = y - b / 200;
	    x = d3_lab_xyz(x) * d3_lab_X;
	    y = d3_lab_xyz(y) * d3_lab_Y;
	    z = d3_lab_xyz(z) * d3_lab_Z;
	    return new d3_rgb(d3_xyz_rgb(3.2404542 * x - 1.5371385 * y - .4985314 * z), d3_xyz_rgb(-.969266 * x + 1.8760108 * y + .041556 * z), d3_xyz_rgb(.0556434 * x - .2040259 * y + 1.0572252 * z));
	  }
	  function d3_lab_hcl(l, a, b) {
	    return l > 0 ? new d3_hcl(Math.atan2(b, a) * d3_degrees, Math.sqrt(a * a + b * b), l) : new d3_hcl(NaN, NaN, l);
	  }
	  function d3_lab_xyz(x) {
	    return x > .206893034 ? x * x * x : (x - 4 / 29) / 7.787037;
	  }
	  function d3_xyz_lab(x) {
	    return x > .008856 ? Math.pow(x, 1 / 3) : 7.787037 * x + 4 / 29;
	  }
	  function d3_xyz_rgb(r) {
	    return Math.round(255 * (r <= .00304 ? 12.92 * r : 1.055 * Math.pow(r, 1 / 2.4) - .055));
	  }
	  d3.rgb = d3_rgb;
	  function d3_rgb(r, g, b) {
	    return this instanceof d3_rgb ? void (this.r = ~~r, this.g = ~~g, this.b = ~~b) : arguments.length < 2 ? r instanceof d3_rgb ? new d3_rgb(r.r, r.g, r.b) : d3_rgb_parse("" + r, d3_rgb, d3_hsl_rgb) : new d3_rgb(r, g, b);
	  }
	  function d3_rgbNumber(value) {
	    return new d3_rgb(value >> 16, value >> 8 & 255, value & 255);
	  }
	  function d3_rgbString(value) {
	    return d3_rgbNumber(value) + "";
	  }
	  var d3_rgbPrototype = d3_rgb.prototype = new d3_color();
	  d3_rgbPrototype.brighter = function(k) {
	    k = Math.pow(.7, arguments.length ? k : 1);
	    var r = this.r, g = this.g, b = this.b, i = 30;
	    if (!r && !g && !b) return new d3_rgb(i, i, i);
	    if (r && r < i) r = i;
	    if (g && g < i) g = i;
	    if (b && b < i) b = i;
	    return new d3_rgb(Math.min(255, r / k), Math.min(255, g / k), Math.min(255, b / k));
	  };
	  d3_rgbPrototype.darker = function(k) {
	    k = Math.pow(.7, arguments.length ? k : 1);
	    return new d3_rgb(k * this.r, k * this.g, k * this.b);
	  };
	  d3_rgbPrototype.hsl = function() {
	    return d3_rgb_hsl(this.r, this.g, this.b);
	  };
	  d3_rgbPrototype.toString = function() {
	    return "#" + d3_rgb_hex(this.r) + d3_rgb_hex(this.g) + d3_rgb_hex(this.b);
	  };
	  function d3_rgb_hex(v) {
	    return v < 16 ? "0" + Math.max(0, v).toString(16) : Math.min(255, v).toString(16);
	  }
	  function d3_rgb_parse(format, rgb, hsl) {
	    var r = 0, g = 0, b = 0, m1, m2, color;
	    m1 = /([a-z]+)\((.*)\)/.exec(format = format.toLowerCase());
	    if (m1) {
	      m2 = m1[2].split(",");
	      switch (m1[1]) {
	       case "hsl":
	        {
	          return hsl(parseFloat(m2[0]), parseFloat(m2[1]) / 100, parseFloat(m2[2]) / 100);
	        }

	       case "rgb":
	        {
	          return rgb(d3_rgb_parseNumber(m2[0]), d3_rgb_parseNumber(m2[1]), d3_rgb_parseNumber(m2[2]));
	        }
	      }
	    }
	    if (color = d3_rgb_names.get(format)) {
	      return rgb(color.r, color.g, color.b);
	    }
	    if (format != null && format.charAt(0) === "#" && !isNaN(color = parseInt(format.slice(1), 16))) {
	      if (format.length === 4) {
	        r = (color & 3840) >> 4;
	        r = r >> 4 | r;
	        g = color & 240;
	        g = g >> 4 | g;
	        b = color & 15;
	        b = b << 4 | b;
	      } else if (format.length === 7) {
	        r = (color & 16711680) >> 16;
	        g = (color & 65280) >> 8;
	        b = color & 255;
	      }
	    }
	    return rgb(r, g, b);
	  }
	  function d3_rgb_hsl(r, g, b) {
	    var min = Math.min(r /= 255, g /= 255, b /= 255), max = Math.max(r, g, b), d = max - min, h, s, l = (max + min) / 2;
	    if (d) {
	      s = l < .5 ? d / (max + min) : d / (2 - max - min);
	      if (r == max) h = (g - b) / d + (g < b ? 6 : 0); else if (g == max) h = (b - r) / d + 2; else h = (r - g) / d + 4;
	      h *= 60;
	    } else {
	      h = NaN;
	      s = l > 0 && l < 1 ? 0 : h;
	    }
	    return new d3_hsl(h, s, l);
	  }
	  function d3_rgb_lab(r, g, b) {
	    r = d3_rgb_xyz(r);
	    g = d3_rgb_xyz(g);
	    b = d3_rgb_xyz(b);
	    var x = d3_xyz_lab((.4124564 * r + .3575761 * g + .1804375 * b) / d3_lab_X), y = d3_xyz_lab((.2126729 * r + .7151522 * g + .072175 * b) / d3_lab_Y), z = d3_xyz_lab((.0193339 * r + .119192 * g + .9503041 * b) / d3_lab_Z);
	    return d3_lab(116 * y - 16, 500 * (x - y), 200 * (y - z));
	  }
	  function d3_rgb_xyz(r) {
	    return (r /= 255) <= .04045 ? r / 12.92 : Math.pow((r + .055) / 1.055, 2.4);
	  }
	  function d3_rgb_parseNumber(c) {
	    var f = parseFloat(c);
	    return c.charAt(c.length - 1) === "%" ? Math.round(f * 2.55) : f;
	  }
	  var d3_rgb_names = d3.map({
	    aliceblue: 15792383,
	    antiquewhite: 16444375,
	    aqua: 65535,
	    aquamarine: 8388564,
	    azure: 15794175,
	    beige: 16119260,
	    bisque: 16770244,
	    black: 0,
	    blanchedalmond: 16772045,
	    blue: 255,
	    blueviolet: 9055202,
	    brown: 10824234,
	    burlywood: 14596231,
	    cadetblue: 6266528,
	    chartreuse: 8388352,
	    chocolate: 13789470,
	    coral: 16744272,
	    cornflowerblue: 6591981,
	    cornsilk: 16775388,
	    crimson: 14423100,
	    cyan: 65535,
	    darkblue: 139,
	    darkcyan: 35723,
	    darkgoldenrod: 12092939,
	    darkgray: 11119017,
	    darkgreen: 25600,
	    darkgrey: 11119017,
	    darkkhaki: 12433259,
	    darkmagenta: 9109643,
	    darkolivegreen: 5597999,
	    darkorange: 16747520,
	    darkorchid: 10040012,
	    darkred: 9109504,
	    darksalmon: 15308410,
	    darkseagreen: 9419919,
	    darkslateblue: 4734347,
	    darkslategray: 3100495,
	    darkslategrey: 3100495,
	    darkturquoise: 52945,
	    darkviolet: 9699539,
	    deeppink: 16716947,
	    deepskyblue: 49151,
	    dimgray: 6908265,
	    dimgrey: 6908265,
	    dodgerblue: 2003199,
	    firebrick: 11674146,
	    floralwhite: 16775920,
	    forestgreen: 2263842,
	    fuchsia: 16711935,
	    gainsboro: 14474460,
	    ghostwhite: 16316671,
	    gold: 16766720,
	    goldenrod: 14329120,
	    gray: 8421504,
	    green: 32768,
	    greenyellow: 11403055,
	    grey: 8421504,
	    honeydew: 15794160,
	    hotpink: 16738740,
	    indianred: 13458524,
	    indigo: 4915330,
	    ivory: 16777200,
	    khaki: 15787660,
	    lavender: 15132410,
	    lavenderblush: 16773365,
	    lawngreen: 8190976,
	    lemonchiffon: 16775885,
	    lightblue: 11393254,
	    lightcoral: 15761536,
	    lightcyan: 14745599,
	    lightgoldenrodyellow: 16448210,
	    lightgray: 13882323,
	    lightgreen: 9498256,
	    lightgrey: 13882323,
	    lightpink: 16758465,
	    lightsalmon: 16752762,
	    lightseagreen: 2142890,
	    lightskyblue: 8900346,
	    lightslategray: 7833753,
	    lightslategrey: 7833753,
	    lightsteelblue: 11584734,
	    lightyellow: 16777184,
	    lime: 65280,
	    limegreen: 3329330,
	    linen: 16445670,
	    magenta: 16711935,
	    maroon: 8388608,
	    mediumaquamarine: 6737322,
	    mediumblue: 205,
	    mediumorchid: 12211667,
	    mediumpurple: 9662683,
	    mediumseagreen: 3978097,
	    mediumslateblue: 8087790,
	    mediumspringgreen: 64154,
	    mediumturquoise: 4772300,
	    mediumvioletred: 13047173,
	    midnightblue: 1644912,
	    mintcream: 16121850,
	    mistyrose: 16770273,
	    moccasin: 16770229,
	    navajowhite: 16768685,
	    navy: 128,
	    oldlace: 16643558,
	    olive: 8421376,
	    olivedrab: 7048739,
	    orange: 16753920,
	    orangered: 16729344,
	    orchid: 14315734,
	    palegoldenrod: 15657130,
	    palegreen: 10025880,
	    paleturquoise: 11529966,
	    palevioletred: 14381203,
	    papayawhip: 16773077,
	    peachpuff: 16767673,
	    peru: 13468991,
	    pink: 16761035,
	    plum: 14524637,
	    powderblue: 11591910,
	    purple: 8388736,
	    rebeccapurple: 6697881,
	    red: 16711680,
	    rosybrown: 12357519,
	    royalblue: 4286945,
	    saddlebrown: 9127187,
	    salmon: 16416882,
	    sandybrown: 16032864,
	    seagreen: 3050327,
	    seashell: 16774638,
	    sienna: 10506797,
	    silver: 12632256,
	    skyblue: 8900331,
	    slateblue: 6970061,
	    slategray: 7372944,
	    slategrey: 7372944,
	    snow: 16775930,
	    springgreen: 65407,
	    steelblue: 4620980,
	    tan: 13808780,
	    teal: 32896,
	    thistle: 14204888,
	    tomato: 16737095,
	    turquoise: 4251856,
	    violet: 15631086,
	    wheat: 16113331,
	    white: 16777215,
	    whitesmoke: 16119285,
	    yellow: 16776960,
	    yellowgreen: 10145074
	  });
	  d3_rgb_names.forEach(function(key, value) {
	    d3_rgb_names.set(key, d3_rgbNumber(value));
	  });
	  function d3_functor(v) {
	    return typeof v === "function" ? v : function() {
	      return v;
	    };
	  }
	  d3.functor = d3_functor;
	  d3.xhr = d3_xhrType(d3_identity);
	  function d3_xhrType(response) {
	    return function(url, mimeType, callback) {
	      if (arguments.length === 2 && typeof mimeType === "function") callback = mimeType, 
	      mimeType = null;
	      return d3_xhr(url, mimeType, response, callback);
	    };
	  }
	  function d3_xhr(url, mimeType, response, callback) {
	    var xhr = {}, dispatch = d3.dispatch("beforesend", "progress", "load", "error"), headers = {}, request = new XMLHttpRequest(), responseType = null;
	    if (this.XDomainRequest && !("withCredentials" in request) && /^(http(s)?:)?\/\//.test(url)) request = new XDomainRequest();
	    "onload" in request ? request.onload = request.onerror = respond : request.onreadystatechange = function() {
	      request.readyState > 3 && respond();
	    };
	    function respond() {
	      var status = request.status, result;
	      if (!status && d3_xhrHasResponse(request) || status >= 200 && status < 300 || status === 304) {
	        try {
	          result = response.call(xhr, request);
	        } catch (e) {
	          dispatch.error.call(xhr, e);
	          return;
	        }
	        dispatch.load.call(xhr, result);
	      } else {
	        dispatch.error.call(xhr, request);
	      }
	    }
	    request.onprogress = function(event) {
	      var o = d3.event;
	      d3.event = event;
	      try {
	        dispatch.progress.call(xhr, request);
	      } finally {
	        d3.event = o;
	      }
	    };
	    xhr.header = function(name, value) {
	      name = (name + "").toLowerCase();
	      if (arguments.length < 2) return headers[name];
	      if (value == null) delete headers[name]; else headers[name] = value + "";
	      return xhr;
	    };
	    xhr.mimeType = function(value) {
	      if (!arguments.length) return mimeType;
	      mimeType = value == null ? null : value + "";
	      return xhr;
	    };
	    xhr.responseType = function(value) {
	      if (!arguments.length) return responseType;
	      responseType = value;
	      return xhr;
	    };
	    xhr.response = function(value) {
	      response = value;
	      return xhr;
	    };
	    [ "get", "post" ].forEach(function(method) {
	      xhr[method] = function() {
	        return xhr.send.apply(xhr, [ method ].concat(d3_array(arguments)));
	      };
	    });
	    xhr.send = function(method, data, callback) {
	      if (arguments.length === 2 && typeof data === "function") callback = data, data = null;
	      request.open(method, url, true);
	      if (mimeType != null && !("accept" in headers)) headers["accept"] = mimeType + ",*/*";
	      if (request.setRequestHeader) for (var name in headers) request.setRequestHeader(name, headers[name]);
	      if (mimeType != null && request.overrideMimeType) request.overrideMimeType(mimeType);
	      if (responseType != null) request.responseType = responseType;
	      if (callback != null) xhr.on("error", callback).on("load", function(request) {
	        callback(null, request);
	      });
	      dispatch.beforesend.call(xhr, request);
	      request.send(data == null ? null : data);
	      return xhr;
	    };
	    xhr.abort = function() {
	      request.abort();
	      return xhr;
	    };
	    d3.rebind(xhr, dispatch, "on");
	    return callback == null ? xhr : xhr.get(d3_xhr_fixCallback(callback));
	  }
	  function d3_xhr_fixCallback(callback) {
	    return callback.length === 1 ? function(error, request) {
	      callback(error == null ? request : null);
	    } : callback;
	  }
	  function d3_xhrHasResponse(request) {
	    var type = request.responseType;
	    return type && type !== "text" ? request.response : request.responseText;
	  }
	  d3.dsv = function(delimiter, mimeType) {
	    var reFormat = new RegExp('["' + delimiter + "\n]"), delimiterCode = delimiter.charCodeAt(0);
	    function dsv(url, row, callback) {
	      if (arguments.length < 3) callback = row, row = null;
	      var xhr = d3_xhr(url, mimeType, row == null ? response : typedResponse(row), callback);
	      xhr.row = function(_) {
	        return arguments.length ? xhr.response((row = _) == null ? response : typedResponse(_)) : row;
	      };
	      return xhr;
	    }
	    function response(request) {
	      return dsv.parse(request.responseText);
	    }
	    function typedResponse(f) {
	      return function(request) {
	        return dsv.parse(request.responseText, f);
	      };
	    }
	    dsv.parse = function(text, f) {
	      var o;
	      return dsv.parseRows(text, function(row, i) {
	        if (o) return o(row, i - 1);
	        var a = new Function("d", "return {" + row.map(function(name, i) {
	          return JSON.stringify(name) + ": d[" + i + "]";
	        }).join(",") + "}");
	        o = f ? function(row, i) {
	          return f(a(row), i);
	        } : a;
	      });
	    };
	    dsv.parseRows = function(text, f) {
	      var EOL = {}, EOF = {}, rows = [], N = text.length, I = 0, n = 0, t, eol;
	      function token() {
	        if (I >= N) return EOF;
	        if (eol) return eol = false, EOL;
	        var j = I;
	        if (text.charCodeAt(j) === 34) {
	          var i = j;
	          while (i++ < N) {
	            if (text.charCodeAt(i) === 34) {
	              if (text.charCodeAt(i + 1) !== 34) break;
	              ++i;
	            }
	          }
	          I = i + 2;
	          var c = text.charCodeAt(i + 1);
	          if (c === 13) {
	            eol = true;
	            if (text.charCodeAt(i + 2) === 10) ++I;
	          } else if (c === 10) {
	            eol = true;
	          }
	          return text.slice(j + 1, i).replace(/""/g, '"');
	        }
	        while (I < N) {
	          var c = text.charCodeAt(I++), k = 1;
	          if (c === 10) eol = true; else if (c === 13) {
	            eol = true;
	            if (text.charCodeAt(I) === 10) ++I, ++k;
	          } else if (c !== delimiterCode) continue;
	          return text.slice(j, I - k);
	        }
	        return text.slice(j);
	      }
	      while ((t = token()) !== EOF) {
	        var a = [];
	        while (t !== EOL && t !== EOF) {
	          a.push(t);
	          t = token();
	        }
	        if (f && (a = f(a, n++)) == null) continue;
	        rows.push(a);
	      }
	      return rows;
	    };
	    dsv.format = function(rows) {
	      if (Array.isArray(rows[0])) return dsv.formatRows(rows);
	      var fieldSet = new d3_Set(), fields = [];
	      rows.forEach(function(row) {
	        for (var field in row) {
	          if (!fieldSet.has(field)) {
	            fields.push(fieldSet.add(field));
	          }
	        }
	      });
	      return [ fields.map(formatValue).join(delimiter) ].concat(rows.map(function(row) {
	        return fields.map(function(field) {
	          return formatValue(row[field]);
	        }).join(delimiter);
	      })).join("\n");
	    };
	    dsv.formatRows = function(rows) {
	      return rows.map(formatRow).join("\n");
	    };
	    function formatRow(row) {
	      return row.map(formatValue).join(delimiter);
	    }
	    function formatValue(text) {
	      return reFormat.test(text) ? '"' + text.replace(/\"/g, '""') + '"' : text;
	    }
	    return dsv;
	  };
	  d3.csv = d3.dsv(",", "text/csv");
	  d3.tsv = d3.dsv("	", "text/tab-separated-values");
	  var d3_timer_queueHead, d3_timer_queueTail, d3_timer_interval, d3_timer_timeout, d3_timer_frame = this[d3_vendorSymbol(this, "requestAnimationFrame")] || function(callback) {
	    setTimeout(callback, 17);
	  };
	  d3.timer = function() {
	    d3_timer.apply(this, arguments);
	  };
	  function d3_timer(callback, delay, then) {
	    var n = arguments.length;
	    if (n < 2) delay = 0;
	    if (n < 3) then = Date.now();
	    var time = then + delay, timer = {
	      c: callback,
	      t: time,
	      n: null
	    };
	    if (d3_timer_queueTail) d3_timer_queueTail.n = timer; else d3_timer_queueHead = timer;
	    d3_timer_queueTail = timer;
	    if (!d3_timer_interval) {
	      d3_timer_timeout = clearTimeout(d3_timer_timeout);
	      d3_timer_interval = 1;
	      d3_timer_frame(d3_timer_step);
	    }
	    return timer;
	  }
	  function d3_timer_step() {
	    var now = d3_timer_mark(), delay = d3_timer_sweep() - now;
	    if (delay > 24) {
	      if (isFinite(delay)) {
	        clearTimeout(d3_timer_timeout);
	        d3_timer_timeout = setTimeout(d3_timer_step, delay);
	      }
	      d3_timer_interval = 0;
	    } else {
	      d3_timer_interval = 1;
	      d3_timer_frame(d3_timer_step);
	    }
	  }
	  d3.timer.flush = function() {
	    d3_timer_mark();
	    d3_timer_sweep();
	  };
	  function d3_timer_mark() {
	    var now = Date.now(), timer = d3_timer_queueHead;
	    while (timer) {
	      if (now >= timer.t && timer.c(now - timer.t)) timer.c = null;
	      timer = timer.n;
	    }
	    return now;
	  }
	  function d3_timer_sweep() {
	    var t0, t1 = d3_timer_queueHead, time = Infinity;
	    while (t1) {
	      if (t1.c) {
	        if (t1.t < time) time = t1.t;
	        t1 = (t0 = t1).n;
	      } else {
	        t1 = t0 ? t0.n = t1.n : d3_timer_queueHead = t1.n;
	      }
	    }
	    d3_timer_queueTail = t0;
	    return time;
	  }
	  function d3_format_precision(x, p) {
	    return p - (x ? Math.ceil(Math.log(x) / Math.LN10) : 1);
	  }
	  d3.round = function(x, n) {
	    return n ? Math.round(x * (n = Math.pow(10, n))) / n : Math.round(x);
	  };
	  var d3_formatPrefixes = [ "y", "z", "a", "f", "p", "n", "µ", "m", "", "k", "M", "G", "T", "P", "E", "Z", "Y" ].map(d3_formatPrefix);
	  d3.formatPrefix = function(value, precision) {
	    var i = 0;
	    if (value = +value) {
	      if (value < 0) value *= -1;
	      if (precision) value = d3.round(value, d3_format_precision(value, precision));
	      i = 1 + Math.floor(1e-12 + Math.log(value) / Math.LN10);
	      i = Math.max(-24, Math.min(24, Math.floor((i - 1) / 3) * 3));
	    }
	    return d3_formatPrefixes[8 + i / 3];
	  };
	  function d3_formatPrefix(d, i) {
	    var k = Math.pow(10, abs(8 - i) * 3);
	    return {
	      scale: i > 8 ? function(d) {
	        return d / k;
	      } : function(d) {
	        return d * k;
	      },
	      symbol: d
	    };
	  }
	  function d3_locale_numberFormat(locale) {
	    var locale_decimal = locale.decimal, locale_thousands = locale.thousands, locale_grouping = locale.grouping, locale_currency = locale.currency, formatGroup = locale_grouping && locale_thousands ? function(value, width) {
	      var i = value.length, t = [], j = 0, g = locale_grouping[0], length = 0;
	      while (i > 0 && g > 0) {
	        if (length + g + 1 > width) g = Math.max(1, width - length);
	        t.push(value.substring(i -= g, i + g));
	        if ((length += g + 1) > width) break;
	        g = locale_grouping[j = (j + 1) % locale_grouping.length];
	      }
	      return t.reverse().join(locale_thousands);
	    } : d3_identity;
	    return function(specifier) {
	      var match = d3_format_re.exec(specifier), fill = match[1] || " ", align = match[2] || ">", sign = match[3] || "-", symbol = match[4] || "", zfill = match[5], width = +match[6], comma = match[7], precision = match[8], type = match[9], scale = 1, prefix = "", suffix = "", integer = false, exponent = true;
	      if (precision) precision = +precision.substring(1);
	      if (zfill || fill === "0" && align === "=") {
	        zfill = fill = "0";
	        align = "=";
	      }
	      switch (type) {
	       case "n":
	        comma = true;
	        type = "g";
	        break;

	       case "%":
	        scale = 100;
	        suffix = "%";
	        type = "f";
	        break;

	       case "p":
	        scale = 100;
	        suffix = "%";
	        type = "r";
	        break;

	       case "b":
	       case "o":
	       case "x":
	       case "X":
	        if (symbol === "#") prefix = "0" + type.toLowerCase();

	       case "c":
	        exponent = false;

	       case "d":
	        integer = true;
	        precision = 0;
	        break;

	       case "s":
	        scale = -1;
	        type = "r";
	        break;
	      }
	      if (symbol === "$") prefix = locale_currency[0], suffix = locale_currency[1];
	      if (type == "r" && !precision) type = "g";
	      if (precision != null) {
	        if (type == "g") precision = Math.max(1, Math.min(21, precision)); else if (type == "e" || type == "f") precision = Math.max(0, Math.min(20, precision));
	      }
	      type = d3_format_types.get(type) || d3_format_typeDefault;
	      var zcomma = zfill && comma;
	      return function(value) {
	        var fullSuffix = suffix;
	        if (integer && value % 1) return "";
	        var negative = value < 0 || value === 0 && 1 / value < 0 ? (value = -value, "-") : sign === "-" ? "" : sign;
	        if (scale < 0) {
	          var unit = d3.formatPrefix(value, precision);
	          value = unit.scale(value);
	          fullSuffix = unit.symbol + suffix;
	        } else {
	          value *= scale;
	        }
	        value = type(value, precision);
	        var i = value.lastIndexOf("."), before, after;
	        if (i < 0) {
	          var j = exponent ? value.lastIndexOf("e") : -1;
	          if (j < 0) before = value, after = ""; else before = value.substring(0, j), after = value.substring(j);
	        } else {
	          before = value.substring(0, i);
	          after = locale_decimal + value.substring(i + 1);
	        }
	        if (!zfill && comma) before = formatGroup(before, Infinity);
	        var length = prefix.length + before.length + after.length + (zcomma ? 0 : negative.length), padding = length < width ? new Array(length = width - length + 1).join(fill) : "";
	        if (zcomma) before = formatGroup(padding + before, padding.length ? width - after.length : Infinity);
	        negative += prefix;
	        value = before + after;
	        return (align === "<" ? negative + value + padding : align === ">" ? padding + negative + value : align === "^" ? padding.substring(0, length >>= 1) + negative + value + padding.substring(length) : negative + (zcomma ? value : padding + value)) + fullSuffix;
	      };
	    };
	  }
	  var d3_format_re = /(?:([^{])?([<>=^]))?([+\- ])?([$#])?(0)?(\d+)?(,)?(\.-?\d+)?([a-z%])?/i;
	  var d3_format_types = d3.map({
	    b: function(x) {
	      return x.toString(2);
	    },
	    c: function(x) {
	      return String.fromCharCode(x);
	    },
	    o: function(x) {
	      return x.toString(8);
	    },
	    x: function(x) {
	      return x.toString(16);
	    },
	    X: function(x) {
	      return x.toString(16).toUpperCase();
	    },
	    g: function(x, p) {
	      return x.toPrecision(p);
	    },
	    e: function(x, p) {
	      return x.toExponential(p);
	    },
	    f: function(x, p) {
	      return x.toFixed(p);
	    },
	    r: function(x, p) {
	      return (x = d3.round(x, d3_format_precision(x, p))).toFixed(Math.max(0, Math.min(20, d3_format_precision(x * (1 + 1e-15), p))));
	    }
	  });
	  function d3_format_typeDefault(x) {
	    return x + "";
	  }
	  var d3_time = d3.time = {}, d3_date = Date;
	  function d3_date_utc() {
	    this._ = new Date(arguments.length > 1 ? Date.UTC.apply(this, arguments) : arguments[0]);
	  }
	  d3_date_utc.prototype = {
	    getDate: function() {
	      return this._.getUTCDate();
	    },
	    getDay: function() {
	      return this._.getUTCDay();
	    },
	    getFullYear: function() {
	      return this._.getUTCFullYear();
	    },
	    getHours: function() {
	      return this._.getUTCHours();
	    },
	    getMilliseconds: function() {
	      return this._.getUTCMilliseconds();
	    },
	    getMinutes: function() {
	      return this._.getUTCMinutes();
	    },
	    getMonth: function() {
	      return this._.getUTCMonth();
	    },
	    getSeconds: function() {
	      return this._.getUTCSeconds();
	    },
	    getTime: function() {
	      return this._.getTime();
	    },
	    getTimezoneOffset: function() {
	      return 0;
	    },
	    valueOf: function() {
	      return this._.valueOf();
	    },
	    setDate: function() {
	      d3_time_prototype.setUTCDate.apply(this._, arguments);
	    },
	    setDay: function() {
	      d3_time_prototype.setUTCDay.apply(this._, arguments);
	    },
	    setFullYear: function() {
	      d3_time_prototype.setUTCFullYear.apply(this._, arguments);
	    },
	    setHours: function() {
	      d3_time_prototype.setUTCHours.apply(this._, arguments);
	    },
	    setMilliseconds: function() {
	      d3_time_prototype.setUTCMilliseconds.apply(this._, arguments);
	    },
	    setMinutes: function() {
	      d3_time_prototype.setUTCMinutes.apply(this._, arguments);
	    },
	    setMonth: function() {
	      d3_time_prototype.setUTCMonth.apply(this._, arguments);
	    },
	    setSeconds: function() {
	      d3_time_prototype.setUTCSeconds.apply(this._, arguments);
	    },
	    setTime: function() {
	      d3_time_prototype.setTime.apply(this._, arguments);
	    }
	  };
	  var d3_time_prototype = Date.prototype;
	  function d3_time_interval(local, step, number) {
	    function round(date) {
	      var d0 = local(date), d1 = offset(d0, 1);
	      return date - d0 < d1 - date ? d0 : d1;
	    }
	    function ceil(date) {
	      step(date = local(new d3_date(date - 1)), 1);
	      return date;
	    }
	    function offset(date, k) {
	      step(date = new d3_date(+date), k);
	      return date;
	    }
	    function range(t0, t1, dt) {
	      var time = ceil(t0), times = [];
	      if (dt > 1) {
	        while (time < t1) {
	          if (!(number(time) % dt)) times.push(new Date(+time));
	          step(time, 1);
	        }
	      } else {
	        while (time < t1) times.push(new Date(+time)), step(time, 1);
	      }
	      return times;
	    }
	    function range_utc(t0, t1, dt) {
	      try {
	        d3_date = d3_date_utc;
	        var utc = new d3_date_utc();
	        utc._ = t0;
	        return range(utc, t1, dt);
	      } finally {
	        d3_date = Date;
	      }
	    }
	    local.floor = local;
	    local.round = round;
	    local.ceil = ceil;
	    local.offset = offset;
	    local.range = range;
	    var utc = local.utc = d3_time_interval_utc(local);
	    utc.floor = utc;
	    utc.round = d3_time_interval_utc(round);
	    utc.ceil = d3_time_interval_utc(ceil);
	    utc.offset = d3_time_interval_utc(offset);
	    utc.range = range_utc;
	    return local;
	  }
	  function d3_time_interval_utc(method) {
	    return function(date, k) {
	      try {
	        d3_date = d3_date_utc;
	        var utc = new d3_date_utc();
	        utc._ = date;
	        return method(utc, k)._;
	      } finally {
	        d3_date = Date;
	      }
	    };
	  }
	  d3_time.year = d3_time_interval(function(date) {
	    date = d3_time.day(date);
	    date.setMonth(0, 1);
	    return date;
	  }, function(date, offset) {
	    date.setFullYear(date.getFullYear() + offset);
	  }, function(date) {
	    return date.getFullYear();
	  });
	  d3_time.years = d3_time.year.range;
	  d3_time.years.utc = d3_time.year.utc.range;
	  d3_time.day = d3_time_interval(function(date) {
	    var day = new d3_date(2e3, 0);
	    day.setFullYear(date.getFullYear(), date.getMonth(), date.getDate());
	    return day;
	  }, function(date, offset) {
	    date.setDate(date.getDate() + offset);
	  }, function(date) {
	    return date.getDate() - 1;
	  });
	  d3_time.days = d3_time.day.range;
	  d3_time.days.utc = d3_time.day.utc.range;
	  d3_time.dayOfYear = function(date) {
	    var year = d3_time.year(date);
	    return Math.floor((date - year - (date.getTimezoneOffset() - year.getTimezoneOffset()) * 6e4) / 864e5);
	  };
	  [ "sunday", "monday", "tuesday", "wednesday", "thursday", "friday", "saturday" ].forEach(function(day, i) {
	    i = 7 - i;
	    var interval = d3_time[day] = d3_time_interval(function(date) {
	      (date = d3_time.day(date)).setDate(date.getDate() - (date.getDay() + i) % 7);
	      return date;
	    }, function(date, offset) {
	      date.setDate(date.getDate() + Math.floor(offset) * 7);
	    }, function(date) {
	      var day = d3_time.year(date).getDay();
	      return Math.floor((d3_time.dayOfYear(date) + (day + i) % 7) / 7) - (day !== i);
	    });
	    d3_time[day + "s"] = interval.range;
	    d3_time[day + "s"].utc = interval.utc.range;
	    d3_time[day + "OfYear"] = function(date) {
	      var day = d3_time.year(date).getDay();
	      return Math.floor((d3_time.dayOfYear(date) + (day + i) % 7) / 7);
	    };
	  });
	  d3_time.week = d3_time.sunday;
	  d3_time.weeks = d3_time.sunday.range;
	  d3_time.weeks.utc = d3_time.sunday.utc.range;
	  d3_time.weekOfYear = d3_time.sundayOfYear;
	  function d3_locale_timeFormat(locale) {
	    var locale_dateTime = locale.dateTime, locale_date = locale.date, locale_time = locale.time, locale_periods = locale.periods, locale_days = locale.days, locale_shortDays = locale.shortDays, locale_months = locale.months, locale_shortMonths = locale.shortMonths;
	    function d3_time_format(template) {
	      var n = template.length;
	      function format(date) {
	        var string = [], i = -1, j = 0, c, p, f;
	        while (++i < n) {
	          if (template.charCodeAt(i) === 37) {
	            string.push(template.slice(j, i));
	            if ((p = d3_time_formatPads[c = template.charAt(++i)]) != null) c = template.charAt(++i);
	            if (f = d3_time_formats[c]) c = f(date, p == null ? c === "e" ? " " : "0" : p);
	            string.push(c);
	            j = i + 1;
	          }
	        }
	        string.push(template.slice(j, i));
	        return string.join("");
	      }
	      format.parse = function(string) {
	        var d = {
	          y: 1900,
	          m: 0,
	          d: 1,
	          H: 0,
	          M: 0,
	          S: 0,
	          L: 0,
	          Z: null
	        }, i = d3_time_parse(d, template, string, 0);
	        if (i != string.length) return null;
	        if ("p" in d) d.H = d.H % 12 + d.p * 12;
	        var localZ = d.Z != null && d3_date !== d3_date_utc, date = new (localZ ? d3_date_utc : d3_date)();
	        if ("j" in d) date.setFullYear(d.y, 0, d.j); else if ("W" in d || "U" in d) {
	          if (!("w" in d)) d.w = "W" in d ? 1 : 0;
	          date.setFullYear(d.y, 0, 1);
	          date.setFullYear(d.y, 0, "W" in d ? (d.w + 6) % 7 + d.W * 7 - (date.getDay() + 5) % 7 : d.w + d.U * 7 - (date.getDay() + 6) % 7);
	        } else date.setFullYear(d.y, d.m, d.d);
	        date.setHours(d.H + (d.Z / 100 | 0), d.M + d.Z % 100, d.S, d.L);
	        return localZ ? date._ : date;
	      };
	      format.toString = function() {
	        return template;
	      };
	      return format;
	    }
	    function d3_time_parse(date, template, string, j) {
	      var c, p, t, i = 0, n = template.length, m = string.length;
	      while (i < n) {
	        if (j >= m) return -1;
	        c = template.charCodeAt(i++);
	        if (c === 37) {
	          t = template.charAt(i++);
	          p = d3_time_parsers[t in d3_time_formatPads ? template.charAt(i++) : t];
	          if (!p || (j = p(date, string, j)) < 0) return -1;
	        } else if (c != string.charCodeAt(j++)) {
	          return -1;
	        }
	      }
	      return j;
	    }
	    d3_time_format.utc = function(template) {
	      var local = d3_time_format(template);
	      function format(date) {
	        try {
	          d3_date = d3_date_utc;
	          var utc = new d3_date();
	          utc._ = date;
	          return local(utc);
	        } finally {
	          d3_date = Date;
	        }
	      }
	      format.parse = function(string) {
	        try {
	          d3_date = d3_date_utc;
	          var date = local.parse(string);
	          return date && date._;
	        } finally {
	          d3_date = Date;
	        }
	      };
	      format.toString = local.toString;
	      return format;
	    };
	    d3_time_format.multi = d3_time_format.utc.multi = d3_time_formatMulti;
	    var d3_time_periodLookup = d3.map(), d3_time_dayRe = d3_time_formatRe(locale_days), d3_time_dayLookup = d3_time_formatLookup(locale_days), d3_time_dayAbbrevRe = d3_time_formatRe(locale_shortDays), d3_time_dayAbbrevLookup = d3_time_formatLookup(locale_shortDays), d3_time_monthRe = d3_time_formatRe(locale_months), d3_time_monthLookup = d3_time_formatLookup(locale_months), d3_time_monthAbbrevRe = d3_time_formatRe(locale_shortMonths), d3_time_monthAbbrevLookup = d3_time_formatLookup(locale_shortMonths);
	    locale_periods.forEach(function(p, i) {
	      d3_time_periodLookup.set(p.toLowerCase(), i);
	    });
	    var d3_time_formats = {
	      a: function(d) {
	        return locale_shortDays[d.getDay()];
	      },
	      A: function(d) {
	        return locale_days[d.getDay()];
	      },
	      b: function(d) {
	        return locale_shortMonths[d.getMonth()];
	      },
	      B: function(d) {
	        return locale_months[d.getMonth()];
	      },
	      c: d3_time_format(locale_dateTime),
	      d: function(d, p) {
	        return d3_time_formatPad(d.getDate(), p, 2);
	      },
	      e: function(d, p) {
	        return d3_time_formatPad(d.getDate(), p, 2);
	      },
	      H: function(d, p) {
	        return d3_time_formatPad(d.getHours(), p, 2);
	      },
	      I: function(d, p) {
	        return d3_time_formatPad(d.getHours() % 12 || 12, p, 2);
	      },
	      j: function(d, p) {
	        return d3_time_formatPad(1 + d3_time.dayOfYear(d), p, 3);
	      },
	      L: function(d, p) {
	        return d3_time_formatPad(d.getMilliseconds(), p, 3);
	      },
	      m: function(d, p) {
	        return d3_time_formatPad(d.getMonth() + 1, p, 2);
	      },
	      M: function(d, p) {
	        return d3_time_formatPad(d.getMinutes(), p, 2);
	      },
	      p: function(d) {
	        return locale_periods[+(d.getHours() >= 12)];
	      },
	      S: function(d, p) {
	        return d3_time_formatPad(d.getSeconds(), p, 2);
	      },
	      U: function(d, p) {
	        return d3_time_formatPad(d3_time.sundayOfYear(d), p, 2);
	      },
	      w: function(d) {
	        return d.getDay();
	      },
	      W: function(d, p) {
	        return d3_time_formatPad(d3_time.mondayOfYear(d), p, 2);
	      },
	      x: d3_time_format(locale_date),
	      X: d3_time_format(locale_time),
	      y: function(d, p) {
	        return d3_time_formatPad(d.getFullYear() % 100, p, 2);
	      },
	      Y: function(d, p) {
	        return d3_time_formatPad(d.getFullYear() % 1e4, p, 4);
	      },
	      Z: d3_time_zone,
	      "%": function() {
	        return "%";
	      }
	    };
	    var d3_time_parsers = {
	      a: d3_time_parseWeekdayAbbrev,
	      A: d3_time_parseWeekday,
	      b: d3_time_parseMonthAbbrev,
	      B: d3_time_parseMonth,
	      c: d3_time_parseLocaleFull,
	      d: d3_time_parseDay,
	      e: d3_time_parseDay,
	      H: d3_time_parseHour24,
	      I: d3_time_parseHour24,
	      j: d3_time_parseDayOfYear,
	      L: d3_time_parseMilliseconds,
	      m: d3_time_parseMonthNumber,
	      M: d3_time_parseMinutes,
	      p: d3_time_parseAmPm,
	      S: d3_time_parseSeconds,
	      U: d3_time_parseWeekNumberSunday,
	      w: d3_time_parseWeekdayNumber,
	      W: d3_time_parseWeekNumberMonday,
	      x: d3_time_parseLocaleDate,
	      X: d3_time_parseLocaleTime,
	      y: d3_time_parseYear,
	      Y: d3_time_parseFullYear,
	      Z: d3_time_parseZone,
	      "%": d3_time_parseLiteralPercent
	    };
	    function d3_time_parseWeekdayAbbrev(date, string, i) {
	      d3_time_dayAbbrevRe.lastIndex = 0;
	      var n = d3_time_dayAbbrevRe.exec(string.slice(i));
	      return n ? (date.w = d3_time_dayAbbrevLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
	    }
	    function d3_time_parseWeekday(date, string, i) {
	      d3_time_dayRe.lastIndex = 0;
	      var n = d3_time_dayRe.exec(string.slice(i));
	      return n ? (date.w = d3_time_dayLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
	    }
	    function d3_time_parseMonthAbbrev(date, string, i) {
	      d3_time_monthAbbrevRe.lastIndex = 0;
	      var n = d3_time_monthAbbrevRe.exec(string.slice(i));
	      return n ? (date.m = d3_time_monthAbbrevLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
	    }
	    function d3_time_parseMonth(date, string, i) {
	      d3_time_monthRe.lastIndex = 0;
	      var n = d3_time_monthRe.exec(string.slice(i));
	      return n ? (date.m = d3_time_monthLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
	    }
	    function d3_time_parseLocaleFull(date, string, i) {
	      return d3_time_parse(date, d3_time_formats.c.toString(), string, i);
	    }
	    function d3_time_parseLocaleDate(date, string, i) {
	      return d3_time_parse(date, d3_time_formats.x.toString(), string, i);
	    }
	    function d3_time_parseLocaleTime(date, string, i) {
	      return d3_time_parse(date, d3_time_formats.X.toString(), string, i);
	    }
	    function d3_time_parseAmPm(date, string, i) {
	      var n = d3_time_periodLookup.get(string.slice(i, i += 2).toLowerCase());
	      return n == null ? -1 : (date.p = n, i);
	    }
	    return d3_time_format;
	  }
	  var d3_time_formatPads = {
	    "-": "",
	    _: " ",
	    "0": "0"
	  }, d3_time_numberRe = /^\s*\d+/, d3_time_percentRe = /^%/;
	  function d3_time_formatPad(value, fill, width) {
	    var sign = value < 0 ? "-" : "", string = (sign ? -value : value) + "", length = string.length;
	    return sign + (length < width ? new Array(width - length + 1).join(fill) + string : string);
	  }
	  function d3_time_formatRe(names) {
	    return new RegExp("^(?:" + names.map(d3.requote).join("|") + ")", "i");
	  }
	  function d3_time_formatLookup(names) {
	    var map = new d3_Map(), i = -1, n = names.length;
	    while (++i < n) map.set(names[i].toLowerCase(), i);
	    return map;
	  }
	  function d3_time_parseWeekdayNumber(date, string, i) {
	    d3_time_numberRe.lastIndex = 0;
	    var n = d3_time_numberRe.exec(string.slice(i, i + 1));
	    return n ? (date.w = +n[0], i + n[0].length) : -1;
	  }
	  function d3_time_parseWeekNumberSunday(date, string, i) {
	    d3_time_numberRe.lastIndex = 0;
	    var n = d3_time_numberRe.exec(string.slice(i));
	    return n ? (date.U = +n[0], i + n[0].length) : -1;
	  }
	  function d3_time_parseWeekNumberMonday(date, string, i) {
	    d3_time_numberRe.lastIndex = 0;
	    var n = d3_time_numberRe.exec(string.slice(i));
	    return n ? (date.W = +n[0], i + n[0].length) : -1;
	  }
	  function d3_time_parseFullYear(date, string, i) {
	    d3_time_numberRe.lastIndex = 0;
	    var n = d3_time_numberRe.exec(string.slice(i, i + 4));
	    return n ? (date.y = +n[0], i + n[0].length) : -1;
	  }
	  function d3_time_parseYear(date, string, i) {
	    d3_time_numberRe.lastIndex = 0;
	    var n = d3_time_numberRe.exec(string.slice(i, i + 2));
	    return n ? (date.y = d3_time_expandYear(+n[0]), i + n[0].length) : -1;
	  }
	  function d3_time_parseZone(date, string, i) {
	    return /^[+-]\d{4}$/.test(string = string.slice(i, i + 5)) ? (date.Z = -string, 
	    i + 5) : -1;
	  }
	  function d3_time_expandYear(d) {
	    return d + (d > 68 ? 1900 : 2e3);
	  }
	  function d3_time_parseMonthNumber(date, string, i) {
	    d3_time_numberRe.lastIndex = 0;
	    var n = d3_time_numberRe.exec(string.slice(i, i + 2));
	    return n ? (date.m = n[0] - 1, i + n[0].length) : -1;
	  }
	  function d3_time_parseDay(date, string, i) {
	    d3_time_numberRe.lastIndex = 0;
	    var n = d3_time_numberRe.exec(string.slice(i, i + 2));
	    return n ? (date.d = +n[0], i + n[0].length) : -1;
	  }
	  function d3_time_parseDayOfYear(date, string, i) {
	    d3_time_numberRe.lastIndex = 0;
	    var n = d3_time_numberRe.exec(string.slice(i, i + 3));
	    return n ? (date.j = +n[0], i + n[0].length) : -1;
	  }
	  function d3_time_parseHour24(date, string, i) {
	    d3_time_numberRe.lastIndex = 0;
	    var n = d3_time_numberRe.exec(string.slice(i, i + 2));
	    return n ? (date.H = +n[0], i + n[0].length) : -1;
	  }
	  function d3_time_parseMinutes(date, string, i) {
	    d3_time_numberRe.lastIndex = 0;
	    var n = d3_time_numberRe.exec(string.slice(i, i + 2));
	    return n ? (date.M = +n[0], i + n[0].length) : -1;
	  }
	  function d3_time_parseSeconds(date, string, i) {
	    d3_time_numberRe.lastIndex = 0;
	    var n = d3_time_numberRe.exec(string.slice(i, i + 2));
	    return n ? (date.S = +n[0], i + n[0].length) : -1;
	  }
	  function d3_time_parseMilliseconds(date, string, i) {
	    d3_time_numberRe.lastIndex = 0;
	    var n = d3_time_numberRe.exec(string.slice(i, i + 3));
	    return n ? (date.L = +n[0], i + n[0].length) : -1;
	  }
	  function d3_time_zone(d) {
	    var z = d.getTimezoneOffset(), zs = z > 0 ? "-" : "+", zh = abs(z) / 60 | 0, zm = abs(z) % 60;
	    return zs + d3_time_formatPad(zh, "0", 2) + d3_time_formatPad(zm, "0", 2);
	  }
	  function d3_time_parseLiteralPercent(date, string, i) {
	    d3_time_percentRe.lastIndex = 0;
	    var n = d3_time_percentRe.exec(string.slice(i, i + 1));
	    return n ? i + n[0].length : -1;
	  }
	  function d3_time_formatMulti(formats) {
	    var n = formats.length, i = -1;
	    while (++i < n) formats[i][0] = this(formats[i][0]);
	    return function(date) {
	      var i = 0, f = formats[i];
	      while (!f[1](date)) f = formats[++i];
	      return f[0](date);
	    };
	  }
	  d3.locale = function(locale) {
	    return {
	      numberFormat: d3_locale_numberFormat(locale),
	      timeFormat: d3_locale_timeFormat(locale)
	    };
	  };
	  var d3_locale_enUS = d3.locale({
	    decimal: ".",
	    thousands: ",",
	    grouping: [ 3 ],
	    currency: [ "$", "" ],
	    dateTime: "%a %b %e %X %Y",
	    date: "%m/%d/%Y",
	    time: "%H:%M:%S",
	    periods: [ "AM", "PM" ],
	    days: [ "Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday" ],
	    shortDays: [ "Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat" ],
	    months: [ "January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December" ],
	    shortMonths: [ "Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec" ]
	  });
	  d3.format = d3_locale_enUS.numberFormat;
	  d3.geo = {};
	  function d3_adder() {}
	  d3_adder.prototype = {
	    s: 0,
	    t: 0,
	    add: function(y) {
	      d3_adderSum(y, this.t, d3_adderTemp);
	      d3_adderSum(d3_adderTemp.s, this.s, this);
	      if (this.s) this.t += d3_adderTemp.t; else this.s = d3_adderTemp.t;
	    },
	    reset: function() {
	      this.s = this.t = 0;
	    },
	    valueOf: function() {
	      return this.s;
	    }
	  };
	  var d3_adderTemp = new d3_adder();
	  function d3_adderSum(a, b, o) {
	    var x = o.s = a + b, bv = x - a, av = x - bv;
	    o.t = a - av + (b - bv);
	  }
	  d3.geo.stream = function(object, listener) {
	    if (object && d3_geo_streamObjectType.hasOwnProperty(object.type)) {
	      d3_geo_streamObjectType[object.type](object, listener);
	    } else {
	      d3_geo_streamGeometry(object, listener);
	    }
	  };
	  function d3_geo_streamGeometry(geometry, listener) {
	    if (geometry && d3_geo_streamGeometryType.hasOwnProperty(geometry.type)) {
	      d3_geo_streamGeometryType[geometry.type](geometry, listener);
	    }
	  }
	  var d3_geo_streamObjectType = {
	    Feature: function(feature, listener) {
	      d3_geo_streamGeometry(feature.geometry, listener);
	    },
	    FeatureCollection: function(object, listener) {
	      var features = object.features, i = -1, n = features.length;
	      while (++i < n) d3_geo_streamGeometry(features[i].geometry, listener);
	    }
	  };
	  var d3_geo_streamGeometryType = {
	    Sphere: function(object, listener) {
	      listener.sphere();
	    },
	    Point: function(object, listener) {
	      object = object.coordinates;
	      listener.point(object[0], object[1], object[2]);
	    },
	    MultiPoint: function(object, listener) {
	      var coordinates = object.coordinates, i = -1, n = coordinates.length;
	      while (++i < n) object = coordinates[i], listener.point(object[0], object[1], object[2]);
	    },
	    LineString: function(object, listener) {
	      d3_geo_streamLine(object.coordinates, listener, 0);
	    },
	    MultiLineString: function(object, listener) {
	      var coordinates = object.coordinates, i = -1, n = coordinates.length;
	      while (++i < n) d3_geo_streamLine(coordinates[i], listener, 0);
	    },
	    Polygon: function(object, listener) {
	      d3_geo_streamPolygon(object.coordinates, listener);
	    },
	    MultiPolygon: function(object, listener) {
	      var coordinates = object.coordinates, i = -1, n = coordinates.length;
	      while (++i < n) d3_geo_streamPolygon(coordinates[i], listener);
	    },
	    GeometryCollection: function(object, listener) {
	      var geometries = object.geometries, i = -1, n = geometries.length;
	      while (++i < n) d3_geo_streamGeometry(geometries[i], listener);
	    }
	  };
	  function d3_geo_streamLine(coordinates, listener, closed) {
	    var i = -1, n = coordinates.length - closed, coordinate;
	    listener.lineStart();
	    while (++i < n) coordinate = coordinates[i], listener.point(coordinate[0], coordinate[1], coordinate[2]);
	    listener.lineEnd();
	  }
	  function d3_geo_streamPolygon(coordinates, listener) {
	    var i = -1, n = coordinates.length;
	    listener.polygonStart();
	    while (++i < n) d3_geo_streamLine(coordinates[i], listener, 1);
	    listener.polygonEnd();
	  }
	  d3.geo.area = function(object) {
	    d3_geo_areaSum = 0;
	    d3.geo.stream(object, d3_geo_area);
	    return d3_geo_areaSum;
	  };
	  var d3_geo_areaSum, d3_geo_areaRingSum = new d3_adder();
	  var d3_geo_area = {
	    sphere: function() {
	      d3_geo_areaSum += 4 * π;
	    },
	    point: d3_noop,
	    lineStart: d3_noop,
	    lineEnd: d3_noop,
	    polygonStart: function() {
	      d3_geo_areaRingSum.reset();
	      d3_geo_area.lineStart = d3_geo_areaRingStart;
	    },
	    polygonEnd: function() {
	      var area = 2 * d3_geo_areaRingSum;
	      d3_geo_areaSum += area < 0 ? 4 * π + area : area;
	      d3_geo_area.lineStart = d3_geo_area.lineEnd = d3_geo_area.point = d3_noop;
	    }
	  };
	  function d3_geo_areaRingStart() {
	    var λ00, φ00, λ0, cosφ0, sinφ0;
	    d3_geo_area.point = function(λ, φ) {
	      d3_geo_area.point = nextPoint;
	      λ0 = (λ00 = λ) * d3_radians, cosφ0 = Math.cos(φ = (φ00 = φ) * d3_radians / 2 + π / 4), 
	      sinφ0 = Math.sin(φ);
	    };
	    function nextPoint(λ, φ) {
	      λ *= d3_radians;
	      φ = φ * d3_radians / 2 + π / 4;
	      var dλ = λ - λ0, sdλ = dλ >= 0 ? 1 : -1, adλ = sdλ * dλ, cosφ = Math.cos(φ), sinφ = Math.sin(φ), k = sinφ0 * sinφ, u = cosφ0 * cosφ + k * Math.cos(adλ), v = k * sdλ * Math.sin(adλ);
	      d3_geo_areaRingSum.add(Math.atan2(v, u));
	      λ0 = λ, cosφ0 = cosφ, sinφ0 = sinφ;
	    }
	    d3_geo_area.lineEnd = function() {
	      nextPoint(λ00, φ00);
	    };
	  }
	  function d3_geo_cartesian(spherical) {
	    var λ = spherical[0], φ = spherical[1], cosφ = Math.cos(φ);
	    return [ cosφ * Math.cos(λ), cosφ * Math.sin(λ), Math.sin(φ) ];
	  }
	  function d3_geo_cartesianDot(a, b) {
	    return a[0] * b[0] + a[1] * b[1] + a[2] * b[2];
	  }
	  function d3_geo_cartesianCross(a, b) {
	    return [ a[1] * b[2] - a[2] * b[1], a[2] * b[0] - a[0] * b[2], a[0] * b[1] - a[1] * b[0] ];
	  }
	  function d3_geo_cartesianAdd(a, b) {
	    a[0] += b[0];
	    a[1] += b[1];
	    a[2] += b[2];
	  }
	  function d3_geo_cartesianScale(vector, k) {
	    return [ vector[0] * k, vector[1] * k, vector[2] * k ];
	  }
	  function d3_geo_cartesianNormalize(d) {
	    var l = Math.sqrt(d[0] * d[0] + d[1] * d[1] + d[2] * d[2]);
	    d[0] /= l;
	    d[1] /= l;
	    d[2] /= l;
	  }
	  function d3_geo_spherical(cartesian) {
	    return [ Math.atan2(cartesian[1], cartesian[0]), d3_asin(cartesian[2]) ];
	  }
	  function d3_geo_sphericalEqual(a, b) {
	    return abs(a[0] - b[0]) < ε && abs(a[1] - b[1]) < ε;
	  }
	  d3.geo.bounds = function() {
	    var λ0, φ0, λ1, φ1, λ_, λ__, φ__, p0, dλSum, ranges, range;
	    var bound = {
	      point: point,
	      lineStart: lineStart,
	      lineEnd: lineEnd,
	      polygonStart: function() {
	        bound.point = ringPoint;
	        bound.lineStart = ringStart;
	        bound.lineEnd = ringEnd;
	        dλSum = 0;
	        d3_geo_area.polygonStart();
	      },
	      polygonEnd: function() {
	        d3_geo_area.polygonEnd();
	        bound.point = point;
	        bound.lineStart = lineStart;
	        bound.lineEnd = lineEnd;
	        if (d3_geo_areaRingSum < 0) λ0 = -(λ1 = 180), φ0 = -(φ1 = 90); else if (dλSum > ε) φ1 = 90; else if (dλSum < -ε) φ0 = -90;
	        range[0] = λ0, range[1] = λ1;
	      }
	    };
	    function point(λ, φ) {
	      ranges.push(range = [ λ0 = λ, λ1 = λ ]);
	      if (φ < φ0) φ0 = φ;
	      if (φ > φ1) φ1 = φ;
	    }
	    function linePoint(λ, φ) {
	      var p = d3_geo_cartesian([ λ * d3_radians, φ * d3_radians ]);
	      if (p0) {
	        var normal = d3_geo_cartesianCross(p0, p), equatorial = [ normal[1], -normal[0], 0 ], inflection = d3_geo_cartesianCross(equatorial, normal);
	        d3_geo_cartesianNormalize(inflection);
	        inflection = d3_geo_spherical(inflection);
	        var dλ = λ - λ_, s = dλ > 0 ? 1 : -1, λi = inflection[0] * d3_degrees * s, antimeridian = abs(dλ) > 180;
	        if (antimeridian ^ (s * λ_ < λi && λi < s * λ)) {
	          var φi = inflection[1] * d3_degrees;
	          if (φi > φ1) φ1 = φi;
	        } else if (λi = (λi + 360) % 360 - 180, antimeridian ^ (s * λ_ < λi && λi < s * λ)) {
	          var φi = -inflection[1] * d3_degrees;
	          if (φi < φ0) φ0 = φi;
	        } else {
	          if (φ < φ0) φ0 = φ;
	          if (φ > φ1) φ1 = φ;
	        }
	        if (antimeridian) {
	          if (λ < λ_) {
	            if (angle(λ0, λ) > angle(λ0, λ1)) λ1 = λ;
	          } else {
	            if (angle(λ, λ1) > angle(λ0, λ1)) λ0 = λ;
	          }
	        } else {
	          if (λ1 >= λ0) {
	            if (λ < λ0) λ0 = λ;
	            if (λ > λ1) λ1 = λ;
	          } else {
	            if (λ > λ_) {
	              if (angle(λ0, λ) > angle(λ0, λ1)) λ1 = λ;
	            } else {
	              if (angle(λ, λ1) > angle(λ0, λ1)) λ0 = λ;
	            }
	          }
	        }
	      } else {
	        point(λ, φ);
	      }
	      p0 = p, λ_ = λ;
	    }
	    function lineStart() {
	      bound.point = linePoint;
	    }
	    function lineEnd() {
	      range[0] = λ0, range[1] = λ1;
	      bound.point = point;
	      p0 = null;
	    }
	    function ringPoint(λ, φ) {
	      if (p0) {
	        var dλ = λ - λ_;
	        dλSum += abs(dλ) > 180 ? dλ + (dλ > 0 ? 360 : -360) : dλ;
	      } else λ__ = λ, φ__ = φ;
	      d3_geo_area.point(λ, φ);
	      linePoint(λ, φ);
	    }
	    function ringStart() {
	      d3_geo_area.lineStart();
	    }
	    function ringEnd() {
	      ringPoint(λ__, φ__);
	      d3_geo_area.lineEnd();
	      if (abs(dλSum) > ε) λ0 = -(λ1 = 180);
	      range[0] = λ0, range[1] = λ1;
	      p0 = null;
	    }
	    function angle(λ0, λ1) {
	      return (λ1 -= λ0) < 0 ? λ1 + 360 : λ1;
	    }
	    function compareRanges(a, b) {
	      return a[0] - b[0];
	    }
	    function withinRange(x, range) {
	      return range[0] <= range[1] ? range[0] <= x && x <= range[1] : x < range[0] || range[1] < x;
	    }
	    return function(feature) {
	      φ1 = λ1 = -(λ0 = φ0 = Infinity);
	      ranges = [];
	      d3.geo.stream(feature, bound);
	      var n = ranges.length;
	      if (n) {
	        ranges.sort(compareRanges);
	        for (var i = 1, a = ranges[0], b, merged = [ a ]; i < n; ++i) {
	          b = ranges[i];
	          if (withinRange(b[0], a) || withinRange(b[1], a)) {
	            if (angle(a[0], b[1]) > angle(a[0], a[1])) a[1] = b[1];
	            if (angle(b[0], a[1]) > angle(a[0], a[1])) a[0] = b[0];
	          } else {
	            merged.push(a = b);
	          }
	        }
	        var best = -Infinity, dλ;
	        for (var n = merged.length - 1, i = 0, a = merged[n], b; i <= n; a = b, ++i) {
	          b = merged[i];
	          if ((dλ = angle(a[1], b[0])) > best) best = dλ, λ0 = b[0], λ1 = a[1];
	        }
	      }
	      ranges = range = null;
	      return λ0 === Infinity || φ0 === Infinity ? [ [ NaN, NaN ], [ NaN, NaN ] ] : [ [ λ0, φ0 ], [ λ1, φ1 ] ];
	    };
	  }();
	  d3.geo.centroid = function(object) {
	    d3_geo_centroidW0 = d3_geo_centroidW1 = d3_geo_centroidX0 = d3_geo_centroidY0 = d3_geo_centroidZ0 = d3_geo_centroidX1 = d3_geo_centroidY1 = d3_geo_centroidZ1 = d3_geo_centroidX2 = d3_geo_centroidY2 = d3_geo_centroidZ2 = 0;
	    d3.geo.stream(object, d3_geo_centroid);
	    var x = d3_geo_centroidX2, y = d3_geo_centroidY2, z = d3_geo_centroidZ2, m = x * x + y * y + z * z;
	    if (m < ε2) {
	      x = d3_geo_centroidX1, y = d3_geo_centroidY1, z = d3_geo_centroidZ1;
	      if (d3_geo_centroidW1 < ε) x = d3_geo_centroidX0, y = d3_geo_centroidY0, z = d3_geo_centroidZ0;
	      m = x * x + y * y + z * z;
	      if (m < ε2) return [ NaN, NaN ];
	    }
	    return [ Math.atan2(y, x) * d3_degrees, d3_asin(z / Math.sqrt(m)) * d3_degrees ];
	  };
	  var d3_geo_centroidW0, d3_geo_centroidW1, d3_geo_centroidX0, d3_geo_centroidY0, d3_geo_centroidZ0, d3_geo_centroidX1, d3_geo_centroidY1, d3_geo_centroidZ1, d3_geo_centroidX2, d3_geo_centroidY2, d3_geo_centroidZ2;
	  var d3_geo_centroid = {
	    sphere: d3_noop,
	    point: d3_geo_centroidPoint,
	    lineStart: d3_geo_centroidLineStart,
	    lineEnd: d3_geo_centroidLineEnd,
	    polygonStart: function() {
	      d3_geo_centroid.lineStart = d3_geo_centroidRingStart;
	    },
	    polygonEnd: function() {
	      d3_geo_centroid.lineStart = d3_geo_centroidLineStart;
	    }
	  };
	  function d3_geo_centroidPoint(λ, φ) {
	    λ *= d3_radians;
	    var cosφ = Math.cos(φ *= d3_radians);
	    d3_geo_centroidPointXYZ(cosφ * Math.cos(λ), cosφ * Math.sin(λ), Math.sin(φ));
	  }
	  function d3_geo_centroidPointXYZ(x, y, z) {
	    ++d3_geo_centroidW0;
	    d3_geo_centroidX0 += (x - d3_geo_centroidX0) / d3_geo_centroidW0;
	    d3_geo_centroidY0 += (y - d3_geo_centroidY0) / d3_geo_centroidW0;
	    d3_geo_centroidZ0 += (z - d3_geo_centroidZ0) / d3_geo_centroidW0;
	  }
	  function d3_geo_centroidLineStart() {
	    var x0, y0, z0;
	    d3_geo_centroid.point = function(λ, φ) {
	      λ *= d3_radians;
	      var cosφ = Math.cos(φ *= d3_radians);
	      x0 = cosφ * Math.cos(λ);
	      y0 = cosφ * Math.sin(λ);
	      z0 = Math.sin(φ);
	      d3_geo_centroid.point = nextPoint;
	      d3_geo_centroidPointXYZ(x0, y0, z0);
	    };
	    function nextPoint(λ, φ) {
	      λ *= d3_radians;
	      var cosφ = Math.cos(φ *= d3_radians), x = cosφ * Math.cos(λ), y = cosφ * Math.sin(λ), z = Math.sin(φ), w = Math.atan2(Math.sqrt((w = y0 * z - z0 * y) * w + (w = z0 * x - x0 * z) * w + (w = x0 * y - y0 * x) * w), x0 * x + y0 * y + z0 * z);
	      d3_geo_centroidW1 += w;
	      d3_geo_centroidX1 += w * (x0 + (x0 = x));
	      d3_geo_centroidY1 += w * (y0 + (y0 = y));
	      d3_geo_centroidZ1 += w * (z0 + (z0 = z));
	      d3_geo_centroidPointXYZ(x0, y0, z0);
	    }
	  }
	  function d3_geo_centroidLineEnd() {
	    d3_geo_centroid.point = d3_geo_centroidPoint;
	  }
	  function d3_geo_centroidRingStart() {
	    var λ00, φ00, x0, y0, z0;
	    d3_geo_centroid.point = function(λ, φ) {
	      λ00 = λ, φ00 = φ;
	      d3_geo_centroid.point = nextPoint;
	      λ *= d3_radians;
	      var cosφ = Math.cos(φ *= d3_radians);
	      x0 = cosφ * Math.cos(λ);
	      y0 = cosφ * Math.sin(λ);
	      z0 = Math.sin(φ);
	      d3_geo_centroidPointXYZ(x0, y0, z0);
	    };
	    d3_geo_centroid.lineEnd = function() {
	      nextPoint(λ00, φ00);
	      d3_geo_centroid.lineEnd = d3_geo_centroidLineEnd;
	      d3_geo_centroid.point = d3_geo_centroidPoint;
	    };
	    function nextPoint(λ, φ) {
	      λ *= d3_radians;
	      var cosφ = Math.cos(φ *= d3_radians), x = cosφ * Math.cos(λ), y = cosφ * Math.sin(λ), z = Math.sin(φ), cx = y0 * z - z0 * y, cy = z0 * x - x0 * z, cz = x0 * y - y0 * x, m = Math.sqrt(cx * cx + cy * cy + cz * cz), u = x0 * x + y0 * y + z0 * z, v = m && -d3_acos(u) / m, w = Math.atan2(m, u);
	      d3_geo_centroidX2 += v * cx;
	      d3_geo_centroidY2 += v * cy;
	      d3_geo_centroidZ2 += v * cz;
	      d3_geo_centroidW1 += w;
	      d3_geo_centroidX1 += w * (x0 + (x0 = x));
	      d3_geo_centroidY1 += w * (y0 + (y0 = y));
	      d3_geo_centroidZ1 += w * (z0 + (z0 = z));
	      d3_geo_centroidPointXYZ(x0, y0, z0);
	    }
	  }
	  function d3_geo_compose(a, b) {
	    function compose(x, y) {
	      return x = a(x, y), b(x[0], x[1]);
	    }
	    if (a.invert && b.invert) compose.invert = function(x, y) {
	      return x = b.invert(x, y), x && a.invert(x[0], x[1]);
	    };
	    return compose;
	  }
	  function d3_true() {
	    return true;
	  }
	  function d3_geo_clipPolygon(segments, compare, clipStartInside, interpolate, listener) {
	    var subject = [], clip = [];
	    segments.forEach(function(segment) {
	      if ((n = segment.length - 1) <= 0) return;
	      var n, p0 = segment[0], p1 = segment[n];
	      if (d3_geo_sphericalEqual(p0, p1)) {
	        listener.lineStart();
	        for (var i = 0; i < n; ++i) listener.point((p0 = segment[i])[0], p0[1]);
	        listener.lineEnd();
	        return;
	      }
	      var a = new d3_geo_clipPolygonIntersection(p0, segment, null, true), b = new d3_geo_clipPolygonIntersection(p0, null, a, false);
	      a.o = b;
	      subject.push(a);
	      clip.push(b);
	      a = new d3_geo_clipPolygonIntersection(p1, segment, null, false);
	      b = new d3_geo_clipPolygonIntersection(p1, null, a, true);
	      a.o = b;
	      subject.push(a);
	      clip.push(b);
	    });
	    clip.sort(compare);
	    d3_geo_clipPolygonLinkCircular(subject);
	    d3_geo_clipPolygonLinkCircular(clip);
	    if (!subject.length) return;
	    for (var i = 0, entry = clipStartInside, n = clip.length; i < n; ++i) {
	      clip[i].e = entry = !entry;
	    }
	    var start = subject[0], points, point;
	    while (1) {
	      var current = start, isSubject = true;
	      while (current.v) if ((current = current.n) === start) return;
	      points = current.z;
	      listener.lineStart();
	      do {
	        current.v = current.o.v = true;
	        if (current.e) {
	          if (isSubject) {
	            for (var i = 0, n = points.length; i < n; ++i) listener.point((point = points[i])[0], point[1]);
	          } else {
	            interpolate(current.x, current.n.x, 1, listener);
	          }
	          current = current.n;
	        } else {
	          if (isSubject) {
	            points = current.p.z;
	            for (var i = points.length - 1; i >= 0; --i) listener.point((point = points[i])[0], point[1]);
	          } else {
	            interpolate(current.x, current.p.x, -1, listener);
	          }
	          current = current.p;
	        }
	        current = current.o;
	        points = current.z;
	        isSubject = !isSubject;
	      } while (!current.v);
	      listener.lineEnd();
	    }
	  }
	  function d3_geo_clipPolygonLinkCircular(array) {
	    if (!(n = array.length)) return;
	    var n, i = 0, a = array[0], b;
	    while (++i < n) {
	      a.n = b = array[i];
	      b.p = a;
	      a = b;
	    }
	    a.n = b = array[0];
	    b.p = a;
	  }
	  function d3_geo_clipPolygonIntersection(point, points, other, entry) {
	    this.x = point;
	    this.z = points;
	    this.o = other;
	    this.e = entry;
	    this.v = false;
	    this.n = this.p = null;
	  }
	  function d3_geo_clip(pointVisible, clipLine, interpolate, clipStart) {
	    return function(rotate, listener) {
	      var line = clipLine(listener), rotatedClipStart = rotate.invert(clipStart[0], clipStart[1]);
	      var clip = {
	        point: point,
	        lineStart: lineStart,
	        lineEnd: lineEnd,
	        polygonStart: function() {
	          clip.point = pointRing;
	          clip.lineStart = ringStart;
	          clip.lineEnd = ringEnd;
	          segments = [];
	          polygon = [];
	        },
	        polygonEnd: function() {
	          clip.point = point;
	          clip.lineStart = lineStart;
	          clip.lineEnd = lineEnd;
	          segments = d3.merge(segments);
	          var clipStartInside = d3_geo_pointInPolygon(rotatedClipStart, polygon);
	          if (segments.length) {
	            if (!polygonStarted) listener.polygonStart(), polygonStarted = true;
	            d3_geo_clipPolygon(segments, d3_geo_clipSort, clipStartInside, interpolate, listener);
	          } else if (clipStartInside) {
	            if (!polygonStarted) listener.polygonStart(), polygonStarted = true;
	            listener.lineStart();
	            interpolate(null, null, 1, listener);
	            listener.lineEnd();
	          }
	          if (polygonStarted) listener.polygonEnd(), polygonStarted = false;
	          segments = polygon = null;
	        },
	        sphere: function() {
	          listener.polygonStart();
	          listener.lineStart();
	          interpolate(null, null, 1, listener);
	          listener.lineEnd();
	          listener.polygonEnd();
	        }
	      };
	      function point(λ, φ) {
	        var point = rotate(λ, φ);
	        if (pointVisible(λ = point[0], φ = point[1])) listener.point(λ, φ);
	      }
	      function pointLine(λ, φ) {
	        var point = rotate(λ, φ);
	        line.point(point[0], point[1]);
	      }
	      function lineStart() {
	        clip.point = pointLine;
	        line.lineStart();
	      }
	      function lineEnd() {
	        clip.point = point;
	        line.lineEnd();
	      }
	      var segments;
	      var buffer = d3_geo_clipBufferListener(), ringListener = clipLine(buffer), polygonStarted = false, polygon, ring;
	      function pointRing(λ, φ) {
	        ring.push([ λ, φ ]);
	        var point = rotate(λ, φ);
	        ringListener.point(point[0], point[1]);
	      }
	      function ringStart() {
	        ringListener.lineStart();
	        ring = [];
	      }
	      function ringEnd() {
	        pointRing(ring[0][0], ring[0][1]);
	        ringListener.lineEnd();
	        var clean = ringListener.clean(), ringSegments = buffer.buffer(), segment, n = ringSegments.length;
	        ring.pop();
	        polygon.push(ring);
	        ring = null;
	        if (!n) return;
	        if (clean & 1) {
	          segment = ringSegments[0];
	          var n = segment.length - 1, i = -1, point;
	          if (n > 0) {
	            if (!polygonStarted) listener.polygonStart(), polygonStarted = true;
	            listener.lineStart();
	            while (++i < n) listener.point((point = segment[i])[0], point[1]);
	            listener.lineEnd();
	          }
	          return;
	        }
	        if (n > 1 && clean & 2) ringSegments.push(ringSegments.pop().concat(ringSegments.shift()));
	        segments.push(ringSegments.filter(d3_geo_clipSegmentLength1));
	      }
	      return clip;
	    };
	  }
	  function d3_geo_clipSegmentLength1(segment) {
	    return segment.length > 1;
	  }
	  function d3_geo_clipBufferListener() {
	    var lines = [], line;
	    return {
	      lineStart: function() {
	        lines.push(line = []);
	      },
	      point: function(λ, φ) {
	        line.push([ λ, φ ]);
	      },
	      lineEnd: d3_noop,
	      buffer: function() {
	        var buffer = lines;
	        lines = [];
	        line = null;
	        return buffer;
	      },
	      rejoin: function() {
	        if (lines.length > 1) lines.push(lines.pop().concat(lines.shift()));
	      }
	    };
	  }
	  function d3_geo_clipSort(a, b) {
	    return ((a = a.x)[0] < 0 ? a[1] - halfπ - ε : halfπ - a[1]) - ((b = b.x)[0] < 0 ? b[1] - halfπ - ε : halfπ - b[1]);
	  }
	  var d3_geo_clipAntimeridian = d3_geo_clip(d3_true, d3_geo_clipAntimeridianLine, d3_geo_clipAntimeridianInterpolate, [ -π, -π / 2 ]);
	  function d3_geo_clipAntimeridianLine(listener) {
	    var λ0 = NaN, φ0 = NaN, sλ0 = NaN, clean;
	    return {
	      lineStart: function() {
	        listener.lineStart();
	        clean = 1;
	      },
	      point: function(λ1, φ1) {
	        var sλ1 = λ1 > 0 ? π : -π, dλ = abs(λ1 - λ0);
	        if (abs(dλ - π) < ε) {
	          listener.point(λ0, φ0 = (φ0 + φ1) / 2 > 0 ? halfπ : -halfπ);
	          listener.point(sλ0, φ0);
	          listener.lineEnd();
	          listener.lineStart();
	          listener.point(sλ1, φ0);
	          listener.point(λ1, φ0);
	          clean = 0;
	        } else if (sλ0 !== sλ1 && dλ >= π) {
	          if (abs(λ0 - sλ0) < ε) λ0 -= sλ0 * ε;
	          if (abs(λ1 - sλ1) < ε) λ1 -= sλ1 * ε;
	          φ0 = d3_geo_clipAntimeridianIntersect(λ0, φ0, λ1, φ1);
	          listener.point(sλ0, φ0);
	          listener.lineEnd();
	          listener.lineStart();
	          listener.point(sλ1, φ0);
	          clean = 0;
	        }
	        listener.point(λ0 = λ1, φ0 = φ1);
	        sλ0 = sλ1;
	      },
	      lineEnd: function() {
	        listener.lineEnd();
	        λ0 = φ0 = NaN;
	      },
	      clean: function() {
	        return 2 - clean;
	      }
	    };
	  }
	  function d3_geo_clipAntimeridianIntersect(λ0, φ0, λ1, φ1) {
	    var cosφ0, cosφ1, sinλ0_λ1 = Math.sin(λ0 - λ1);
	    return abs(sinλ0_λ1) > ε ? Math.atan((Math.sin(φ0) * (cosφ1 = Math.cos(φ1)) * Math.sin(λ1) - Math.sin(φ1) * (cosφ0 = Math.cos(φ0)) * Math.sin(λ0)) / (cosφ0 * cosφ1 * sinλ0_λ1)) : (φ0 + φ1) / 2;
	  }
	  function d3_geo_clipAntimeridianInterpolate(from, to, direction, listener) {
	    var φ;
	    if (from == null) {
	      φ = direction * halfπ;
	      listener.point(-π, φ);
	      listener.point(0, φ);
	      listener.point(π, φ);
	      listener.point(π, 0);
	      listener.point(π, -φ);
	      listener.point(0, -φ);
	      listener.point(-π, -φ);
	      listener.point(-π, 0);
	      listener.point(-π, φ);
	    } else if (abs(from[0] - to[0]) > ε) {
	      var s = from[0] < to[0] ? π : -π;
	      φ = direction * s / 2;
	      listener.point(-s, φ);
	      listener.point(0, φ);
	      listener.point(s, φ);
	    } else {
	      listener.point(to[0], to[1]);
	    }
	  }
	  function d3_geo_pointInPolygon(point, polygon) {
	    var meridian = point[0], parallel = point[1], meridianNormal = [ Math.sin(meridian), -Math.cos(meridian), 0 ], polarAngle = 0, winding = 0;
	    d3_geo_areaRingSum.reset();
	    for (var i = 0, n = polygon.length; i < n; ++i) {
	      var ring = polygon[i], m = ring.length;
	      if (!m) continue;
	      var point0 = ring[0], λ0 = point0[0], φ0 = point0[1] / 2 + π / 4, sinφ0 = Math.sin(φ0), cosφ0 = Math.cos(φ0), j = 1;
	      while (true) {
	        if (j === m) j = 0;
	        point = ring[j];
	        var λ = point[0], φ = point[1] / 2 + π / 4, sinφ = Math.sin(φ), cosφ = Math.cos(φ), dλ = λ - λ0, sdλ = dλ >= 0 ? 1 : -1, adλ = sdλ * dλ, antimeridian = adλ > π, k = sinφ0 * sinφ;
	        d3_geo_areaRingSum.add(Math.atan2(k * sdλ * Math.sin(adλ), cosφ0 * cosφ + k * Math.cos(adλ)));
	        polarAngle += antimeridian ? dλ + sdλ * τ : dλ;
	        if (antimeridian ^ λ0 >= meridian ^ λ >= meridian) {
	          var arc = d3_geo_cartesianCross(d3_geo_cartesian(point0), d3_geo_cartesian(point));
	          d3_geo_cartesianNormalize(arc);
	          var intersection = d3_geo_cartesianCross(meridianNormal, arc);
	          d3_geo_cartesianNormalize(intersection);
	          var φarc = (antimeridian ^ dλ >= 0 ? -1 : 1) * d3_asin(intersection[2]);
	          if (parallel > φarc || parallel === φarc && (arc[0] || arc[1])) {
	            winding += antimeridian ^ dλ >= 0 ? 1 : -1;
	          }
	        }
	        if (!j++) break;
	        λ0 = λ, sinφ0 = sinφ, cosφ0 = cosφ, point0 = point;
	      }
	    }
	    return (polarAngle < -ε || polarAngle < ε && d3_geo_areaRingSum < -ε) ^ winding & 1;
	  }
	  function d3_geo_clipCircle(radius) {
	    var cr = Math.cos(radius), smallRadius = cr > 0, notHemisphere = abs(cr) > ε, interpolate = d3_geo_circleInterpolate(radius, 6 * d3_radians);
	    return d3_geo_clip(visible, clipLine, interpolate, smallRadius ? [ 0, -radius ] : [ -π, radius - π ]);
	    function visible(λ, φ) {
	      return Math.cos(λ) * Math.cos(φ) > cr;
	    }
	    function clipLine(listener) {
	      var point0, c0, v0, v00, clean;
	      return {
	        lineStart: function() {
	          v00 = v0 = false;
	          clean = 1;
	        },
	        point: function(λ, φ) {
	          var point1 = [ λ, φ ], point2, v = visible(λ, φ), c = smallRadius ? v ? 0 : code(λ, φ) : v ? code(λ + (λ < 0 ? π : -π), φ) : 0;
	          if (!point0 && (v00 = v0 = v)) listener.lineStart();
	          if (v !== v0) {
	            point2 = intersect(point0, point1);
	            if (d3_geo_sphericalEqual(point0, point2) || d3_geo_sphericalEqual(point1, point2)) {
	              point1[0] += ε;
	              point1[1] += ε;
	              v = visible(point1[0], point1[1]);
	            }
	          }
	          if (v !== v0) {
	            clean = 0;
	            if (v) {
	              listener.lineStart();
	              point2 = intersect(point1, point0);
	              listener.point(point2[0], point2[1]);
	            } else {
	              point2 = intersect(point0, point1);
	              listener.point(point2[0], point2[1]);
	              listener.lineEnd();
	            }
	            point0 = point2;
	          } else if (notHemisphere && point0 && smallRadius ^ v) {
	            var t;
	            if (!(c & c0) && (t = intersect(point1, point0, true))) {
	              clean = 0;
	              if (smallRadius) {
	                listener.lineStart();
	                listener.point(t[0][0], t[0][1]);
	                listener.point(t[1][0], t[1][1]);
	                listener.lineEnd();
	              } else {
	                listener.point(t[1][0], t[1][1]);
	                listener.lineEnd();
	                listener.lineStart();
	                listener.point(t[0][0], t[0][1]);
	              }
	            }
	          }
	          if (v && (!point0 || !d3_geo_sphericalEqual(point0, point1))) {
	            listener.point(point1[0], point1[1]);
	          }
	          point0 = point1, v0 = v, c0 = c;
	        },
	        lineEnd: function() {
	          if (v0) listener.lineEnd();
	          point0 = null;
	        },
	        clean: function() {
	          return clean | (v00 && v0) << 1;
	        }
	      };
	    }
	    function intersect(a, b, two) {
	      var pa = d3_geo_cartesian(a), pb = d3_geo_cartesian(b);
	      var n1 = [ 1, 0, 0 ], n2 = d3_geo_cartesianCross(pa, pb), n2n2 = d3_geo_cartesianDot(n2, n2), n1n2 = n2[0], determinant = n2n2 - n1n2 * n1n2;
	      if (!determinant) return !two && a;
	      var c1 = cr * n2n2 / determinant, c2 = -cr * n1n2 / determinant, n1xn2 = d3_geo_cartesianCross(n1, n2), A = d3_geo_cartesianScale(n1, c1), B = d3_geo_cartesianScale(n2, c2);
	      d3_geo_cartesianAdd(A, B);
	      var u = n1xn2, w = d3_geo_cartesianDot(A, u), uu = d3_geo_cartesianDot(u, u), t2 = w * w - uu * (d3_geo_cartesianDot(A, A) - 1);
	      if (t2 < 0) return;
	      var t = Math.sqrt(t2), q = d3_geo_cartesianScale(u, (-w - t) / uu);
	      d3_geo_cartesianAdd(q, A);
	      q = d3_geo_spherical(q);
	      if (!two) return q;
	      var λ0 = a[0], λ1 = b[0], φ0 = a[1], φ1 = b[1], z;
	      if (λ1 < λ0) z = λ0, λ0 = λ1, λ1 = z;
	      var δλ = λ1 - λ0, polar = abs(δλ - π) < ε, meridian = polar || δλ < ε;
	      if (!polar && φ1 < φ0) z = φ0, φ0 = φ1, φ1 = z;
	      if (meridian ? polar ? φ0 + φ1 > 0 ^ q[1] < (abs(q[0] - λ0) < ε ? φ0 : φ1) : φ0 <= q[1] && q[1] <= φ1 : δλ > π ^ (λ0 <= q[0] && q[0] <= λ1)) {
	        var q1 = d3_geo_cartesianScale(u, (-w + t) / uu);
	        d3_geo_cartesianAdd(q1, A);
	        return [ q, d3_geo_spherical(q1) ];
	      }
	    }
	    function code(λ, φ) {
	      var r = smallRadius ? radius : π - radius, code = 0;
	      if (λ < -r) code |= 1; else if (λ > r) code |= 2;
	      if (φ < -r) code |= 4; else if (φ > r) code |= 8;
	      return code;
	    }
	  }
	  function d3_geom_clipLine(x0, y0, x1, y1) {
	    return function(line) {
	      var a = line.a, b = line.b, ax = a.x, ay = a.y, bx = b.x, by = b.y, t0 = 0, t1 = 1, dx = bx - ax, dy = by - ay, r;
	      r = x0 - ax;
	      if (!dx && r > 0) return;
	      r /= dx;
	      if (dx < 0) {
	        if (r < t0) return;
	        if (r < t1) t1 = r;
	      } else if (dx > 0) {
	        if (r > t1) return;
	        if (r > t0) t0 = r;
	      }
	      r = x1 - ax;
	      if (!dx && r < 0) return;
	      r /= dx;
	      if (dx < 0) {
	        if (r > t1) return;
	        if (r > t0) t0 = r;
	      } else if (dx > 0) {
	        if (r < t0) return;
	        if (r < t1) t1 = r;
	      }
	      r = y0 - ay;
	      if (!dy && r > 0) return;
	      r /= dy;
	      if (dy < 0) {
	        if (r < t0) return;
	        if (r < t1) t1 = r;
	      } else if (dy > 0) {
	        if (r > t1) return;
	        if (r > t0) t0 = r;
	      }
	      r = y1 - ay;
	      if (!dy && r < 0) return;
	      r /= dy;
	      if (dy < 0) {
	        if (r > t1) return;
	        if (r > t0) t0 = r;
	      } else if (dy > 0) {
	        if (r < t0) return;
	        if (r < t1) t1 = r;
	      }
	      if (t0 > 0) line.a = {
	        x: ax + t0 * dx,
	        y: ay + t0 * dy
	      };
	      if (t1 < 1) line.b = {
	        x: ax + t1 * dx,
	        y: ay + t1 * dy
	      };
	      return line;
	    };
	  }
	  var d3_geo_clipExtentMAX = 1e9;
	  d3.geo.clipExtent = function() {
	    var x0, y0, x1, y1, stream, clip, clipExtent = {
	      stream: function(output) {
	        if (stream) stream.valid = false;
	        stream = clip(output);
	        stream.valid = true;
	        return stream;
	      },
	      extent: function(_) {
	        if (!arguments.length) return [ [ x0, y0 ], [ x1, y1 ] ];
	        clip = d3_geo_clipExtent(x0 = +_[0][0], y0 = +_[0][1], x1 = +_[1][0], y1 = +_[1][1]);
	        if (stream) stream.valid = false, stream = null;
	        return clipExtent;
	      }
	    };
	    return clipExtent.extent([ [ 0, 0 ], [ 960, 500 ] ]);
	  };
	  function d3_geo_clipExtent(x0, y0, x1, y1) {
	    return function(listener) {
	      var listener_ = listener, bufferListener = d3_geo_clipBufferListener(), clipLine = d3_geom_clipLine(x0, y0, x1, y1), segments, polygon, ring;
	      var clip = {
	        point: point,
	        lineStart: lineStart,
	        lineEnd: lineEnd,
	        polygonStart: function() {
	          listener = bufferListener;
	          segments = [];
	          polygon = [];
	          clean = true;
	        },
	        polygonEnd: function() {
	          listener = listener_;
	          segments = d3.merge(segments);
	          var clipStartInside = insidePolygon([ x0, y1 ]), inside = clean && clipStartInside, visible = segments.length;
	          if (inside || visible) {
	            listener.polygonStart();
	            if (inside) {
	              listener.lineStart();
	              interpolate(null, null, 1, listener);
	              listener.lineEnd();
	            }
	            if (visible) {
	              d3_geo_clipPolygon(segments, compare, clipStartInside, interpolate, listener);
	            }
	            listener.polygonEnd();
	          }
	          segments = polygon = ring = null;
	        }
	      };
	      function insidePolygon(p) {
	        var wn = 0, n = polygon.length, y = p[1];
	        for (var i = 0; i < n; ++i) {
	          for (var j = 1, v = polygon[i], m = v.length, a = v[0], b; j < m; ++j) {
	            b = v[j];
	            if (a[1] <= y) {
	              if (b[1] > y && d3_cross2d(a, b, p) > 0) ++wn;
	            } else {
	              if (b[1] <= y && d3_cross2d(a, b, p) < 0) --wn;
	            }
	            a = b;
	          }
	        }
	        return wn !== 0;
	      }
	      function interpolate(from, to, direction, listener) {
	        var a = 0, a1 = 0;
	        if (from == null || (a = corner(from, direction)) !== (a1 = corner(to, direction)) || comparePoints(from, to) < 0 ^ direction > 0) {
	          do {
	            listener.point(a === 0 || a === 3 ? x0 : x1, a > 1 ? y1 : y0);
	          } while ((a = (a + direction + 4) % 4) !== a1);
	        } else {
	          listener.point(to[0], to[1]);
	        }
	      }
	      function pointVisible(x, y) {
	        return x0 <= x && x <= x1 && y0 <= y && y <= y1;
	      }
	      function point(x, y) {
	        if (pointVisible(x, y)) listener.point(x, y);
	      }
	      var x__, y__, v__, x_, y_, v_, first, clean;
	      function lineStart() {
	        clip.point = linePoint;
	        if (polygon) polygon.push(ring = []);
	        first = true;
	        v_ = false;
	        x_ = y_ = NaN;
	      }
	      function lineEnd() {
	        if (segments) {
	          linePoint(x__, y__);
	          if (v__ && v_) bufferListener.rejoin();
	          segments.push(bufferListener.buffer());
	        }
	        clip.point = point;
	        if (v_) listener.lineEnd();
	      }
	      function linePoint(x, y) {
	        x = Math.max(-d3_geo_clipExtentMAX, Math.min(d3_geo_clipExtentMAX, x));
	        y = Math.max(-d3_geo_clipExtentMAX, Math.min(d3_geo_clipExtentMAX, y));
	        var v = pointVisible(x, y);
	        if (polygon) ring.push([ x, y ]);
	        if (first) {
	          x__ = x, y__ = y, v__ = v;
	          first = false;
	          if (v) {
	            listener.lineStart();
	            listener.point(x, y);
	          }
	        } else {
	          if (v && v_) listener.point(x, y); else {
	            var l = {
	              a: {
	                x: x_,
	                y: y_
	              },
	              b: {
	                x: x,
	                y: y
	              }
	            };
	            if (clipLine(l)) {
	              if (!v_) {
	                listener.lineStart();
	                listener.point(l.a.x, l.a.y);
	              }
	              listener.point(l.b.x, l.b.y);
	              if (!v) listener.lineEnd();
	              clean = false;
	            } else if (v) {
	              listener.lineStart();
	              listener.point(x, y);
	              clean = false;
	            }
	          }
	        }
	        x_ = x, y_ = y, v_ = v;
	      }
	      return clip;
	    };
	    function corner(p, direction) {
	      return abs(p[0] - x0) < ε ? direction > 0 ? 0 : 3 : abs(p[0] - x1) < ε ? direction > 0 ? 2 : 1 : abs(p[1] - y0) < ε ? direction > 0 ? 1 : 0 : direction > 0 ? 3 : 2;
	    }
	    function compare(a, b) {
	      return comparePoints(a.x, b.x);
	    }
	    function comparePoints(a, b) {
	      var ca = corner(a, 1), cb = corner(b, 1);
	      return ca !== cb ? ca - cb : ca === 0 ? b[1] - a[1] : ca === 1 ? a[0] - b[0] : ca === 2 ? a[1] - b[1] : b[0] - a[0];
	    }
	  }
	  function d3_geo_conic(projectAt) {
	    var φ0 = 0, φ1 = π / 3, m = d3_geo_projectionMutator(projectAt), p = m(φ0, φ1);
	    p.parallels = function(_) {
	      if (!arguments.length) return [ φ0 / π * 180, φ1 / π * 180 ];
	      return m(φ0 = _[0] * π / 180, φ1 = _[1] * π / 180);
	    };
	    return p;
	  }
	  function d3_geo_conicEqualArea(φ0, φ1) {
	    var sinφ0 = Math.sin(φ0), n = (sinφ0 + Math.sin(φ1)) / 2, C = 1 + sinφ0 * (2 * n - sinφ0), ρ0 = Math.sqrt(C) / n;
	    function forward(λ, φ) {
	      var ρ = Math.sqrt(C - 2 * n * Math.sin(φ)) / n;
	      return [ ρ * Math.sin(λ *= n), ρ0 - ρ * Math.cos(λ) ];
	    }
	    forward.invert = function(x, y) {
	      var ρ0_y = ρ0 - y;
	      return [ Math.atan2(x, ρ0_y) / n, d3_asin((C - (x * x + ρ0_y * ρ0_y) * n * n) / (2 * n)) ];
	    };
	    return forward;
	  }
	  (d3.geo.conicEqualArea = function() {
	    return d3_geo_conic(d3_geo_conicEqualArea);
	  }).raw = d3_geo_conicEqualArea;
	  d3.geo.albers = function() {
	    return d3.geo.conicEqualArea().rotate([ 96, 0 ]).center([ -.6, 38.7 ]).parallels([ 29.5, 45.5 ]).scale(1070);
	  };
	  d3.geo.albersUsa = function() {
	    var lower48 = d3.geo.albers();
	    var alaska = d3.geo.conicEqualArea().rotate([ 154, 0 ]).center([ -2, 58.5 ]).parallels([ 55, 65 ]);
	    var hawaii = d3.geo.conicEqualArea().rotate([ 157, 0 ]).center([ -3, 19.9 ]).parallels([ 8, 18 ]);
	    var point, pointStream = {
	      point: function(x, y) {
	        point = [ x, y ];
	      }
	    }, lower48Point, alaskaPoint, hawaiiPoint;
	    function albersUsa(coordinates) {
	      var x = coordinates[0], y = coordinates[1];
	      point = null;
	      (lower48Point(x, y), point) || (alaskaPoint(x, y), point) || hawaiiPoint(x, y);
	      return point;
	    }
	    albersUsa.invert = function(coordinates) {
	      var k = lower48.scale(), t = lower48.translate(), x = (coordinates[0] - t[0]) / k, y = (coordinates[1] - t[1]) / k;
	      return (y >= .12 && y < .234 && x >= -.425 && x < -.214 ? alaska : y >= .166 && y < .234 && x >= -.214 && x < -.115 ? hawaii : lower48).invert(coordinates);
	    };
	    albersUsa.stream = function(stream) {
	      var lower48Stream = lower48.stream(stream), alaskaStream = alaska.stream(stream), hawaiiStream = hawaii.stream(stream);
	      return {
	        point: function(x, y) {
	          lower48Stream.point(x, y);
	          alaskaStream.point(x, y);
	          hawaiiStream.point(x, y);
	        },
	        sphere: function() {
	          lower48Stream.sphere();
	          alaskaStream.sphere();
	          hawaiiStream.sphere();
	        },
	        lineStart: function() {
	          lower48Stream.lineStart();
	          alaskaStream.lineStart();
	          hawaiiStream.lineStart();
	        },
	        lineEnd: function() {
	          lower48Stream.lineEnd();
	          alaskaStream.lineEnd();
	          hawaiiStream.lineEnd();
	        },
	        polygonStart: function() {
	          lower48Stream.polygonStart();
	          alaskaStream.polygonStart();
	          hawaiiStream.polygonStart();
	        },
	        polygonEnd: function() {
	          lower48Stream.polygonEnd();
	          alaskaStream.polygonEnd();
	          hawaiiStream.polygonEnd();
	        }
	      };
	    };
	    albersUsa.precision = function(_) {
	      if (!arguments.length) return lower48.precision();
	      lower48.precision(_);
	      alaska.precision(_);
	      hawaii.precision(_);
	      return albersUsa;
	    };
	    albersUsa.scale = function(_) {
	      if (!arguments.length) return lower48.scale();
	      lower48.scale(_);
	      alaska.scale(_ * .35);
	      hawaii.scale(_);
	      return albersUsa.translate(lower48.translate());
	    };
	    albersUsa.translate = function(_) {
	      if (!arguments.length) return lower48.translate();
	      var k = lower48.scale(), x = +_[0], y = +_[1];
	      lower48Point = lower48.translate(_).clipExtent([ [ x - .455 * k, y - .238 * k ], [ x + .455 * k, y + .238 * k ] ]).stream(pointStream).point;
	      alaskaPoint = alaska.translate([ x - .307 * k, y + .201 * k ]).clipExtent([ [ x - .425 * k + ε, y + .12 * k + ε ], [ x - .214 * k - ε, y + .234 * k - ε ] ]).stream(pointStream).point;
	      hawaiiPoint = hawaii.translate([ x - .205 * k, y + .212 * k ]).clipExtent([ [ x - .214 * k + ε, y + .166 * k + ε ], [ x - .115 * k - ε, y + .234 * k - ε ] ]).stream(pointStream).point;
	      return albersUsa;
	    };
	    return albersUsa.scale(1070);
	  };
	  var d3_geo_pathAreaSum, d3_geo_pathAreaPolygon, d3_geo_pathArea = {
	    point: d3_noop,
	    lineStart: d3_noop,
	    lineEnd: d3_noop,
	    polygonStart: function() {
	      d3_geo_pathAreaPolygon = 0;
	      d3_geo_pathArea.lineStart = d3_geo_pathAreaRingStart;
	    },
	    polygonEnd: function() {
	      d3_geo_pathArea.lineStart = d3_geo_pathArea.lineEnd = d3_geo_pathArea.point = d3_noop;
	      d3_geo_pathAreaSum += abs(d3_geo_pathAreaPolygon / 2);
	    }
	  };
	  function d3_geo_pathAreaRingStart() {
	    var x00, y00, x0, y0;
	    d3_geo_pathArea.point = function(x, y) {
	      d3_geo_pathArea.point = nextPoint;
	      x00 = x0 = x, y00 = y0 = y;
	    };
	    function nextPoint(x, y) {
	      d3_geo_pathAreaPolygon += y0 * x - x0 * y;
	      x0 = x, y0 = y;
	    }
	    d3_geo_pathArea.lineEnd = function() {
	      nextPoint(x00, y00);
	    };
	  }
	  var d3_geo_pathBoundsX0, d3_geo_pathBoundsY0, d3_geo_pathBoundsX1, d3_geo_pathBoundsY1;
	  var d3_geo_pathBounds = {
	    point: d3_geo_pathBoundsPoint,
	    lineStart: d3_noop,
	    lineEnd: d3_noop,
	    polygonStart: d3_noop,
	    polygonEnd: d3_noop
	  };
	  function d3_geo_pathBoundsPoint(x, y) {
	    if (x < d3_geo_pathBoundsX0) d3_geo_pathBoundsX0 = x;
	    if (x > d3_geo_pathBoundsX1) d3_geo_pathBoundsX1 = x;
	    if (y < d3_geo_pathBoundsY0) d3_geo_pathBoundsY0 = y;
	    if (y > d3_geo_pathBoundsY1) d3_geo_pathBoundsY1 = y;
	  }
	  function d3_geo_pathBuffer() {
	    var pointCircle = d3_geo_pathBufferCircle(4.5), buffer = [];
	    var stream = {
	      point: point,
	      lineStart: function() {
	        stream.point = pointLineStart;
	      },
	      lineEnd: lineEnd,
	      polygonStart: function() {
	        stream.lineEnd = lineEndPolygon;
	      },
	      polygonEnd: function() {
	        stream.lineEnd = lineEnd;
	        stream.point = point;
	      },
	      pointRadius: function(_) {
	        pointCircle = d3_geo_pathBufferCircle(_);
	        return stream;
	      },
	      result: function() {
	        if (buffer.length) {
	          var result = buffer.join("");
	          buffer = [];
	          return result;
	        }
	      }
	    };
	    function point(x, y) {
	      buffer.push("M", x, ",", y, pointCircle);
	    }
	    function pointLineStart(x, y) {
	      buffer.push("M", x, ",", y);
	      stream.point = pointLine;
	    }
	    function pointLine(x, y) {
	      buffer.push("L", x, ",", y);
	    }
	    function lineEnd() {
	      stream.point = point;
	    }
	    function lineEndPolygon() {
	      buffer.push("Z");
	    }
	    return stream;
	  }
	  function d3_geo_pathBufferCircle(radius) {
	    return "m0," + radius + "a" + radius + "," + radius + " 0 1,1 0," + -2 * radius + "a" + radius + "," + radius + " 0 1,1 0," + 2 * radius + "z";
	  }
	  var d3_geo_pathCentroid = {
	    point: d3_geo_pathCentroidPoint,
	    lineStart: d3_geo_pathCentroidLineStart,
	    lineEnd: d3_geo_pathCentroidLineEnd,
	    polygonStart: function() {
	      d3_geo_pathCentroid.lineStart = d3_geo_pathCentroidRingStart;
	    },
	    polygonEnd: function() {
	      d3_geo_pathCentroid.point = d3_geo_pathCentroidPoint;
	      d3_geo_pathCentroid.lineStart = d3_geo_pathCentroidLineStart;
	      d3_geo_pathCentroid.lineEnd = d3_geo_pathCentroidLineEnd;
	    }
	  };
	  function d3_geo_pathCentroidPoint(x, y) {
	    d3_geo_centroidX0 += x;
	    d3_geo_centroidY0 += y;
	    ++d3_geo_centroidZ0;
	  }
	  function d3_geo_pathCentroidLineStart() {
	    var x0, y0;
	    d3_geo_pathCentroid.point = function(x, y) {
	      d3_geo_pathCentroid.point = nextPoint;
	      d3_geo_pathCentroidPoint(x0 = x, y0 = y);
	    };
	    function nextPoint(x, y) {
	      var dx = x - x0, dy = y - y0, z = Math.sqrt(dx * dx + dy * dy);
	      d3_geo_centroidX1 += z * (x0 + x) / 2;
	      d3_geo_centroidY1 += z * (y0 + y) / 2;
	      d3_geo_centroidZ1 += z;
	      d3_geo_pathCentroidPoint(x0 = x, y0 = y);
	    }
	  }
	  function d3_geo_pathCentroidLineEnd() {
	    d3_geo_pathCentroid.point = d3_geo_pathCentroidPoint;
	  }
	  function d3_geo_pathCentroidRingStart() {
	    var x00, y00, x0, y0;
	    d3_geo_pathCentroid.point = function(x, y) {
	      d3_geo_pathCentroid.point = nextPoint;
	      d3_geo_pathCentroidPoint(x00 = x0 = x, y00 = y0 = y);
	    };
	    function nextPoint(x, y) {
	      var dx = x - x0, dy = y - y0, z = Math.sqrt(dx * dx + dy * dy);
	      d3_geo_centroidX1 += z * (x0 + x) / 2;
	      d3_geo_centroidY1 += z * (y0 + y) / 2;
	      d3_geo_centroidZ1 += z;
	      z = y0 * x - x0 * y;
	      d3_geo_centroidX2 += z * (x0 + x);
	      d3_geo_centroidY2 += z * (y0 + y);
	      d3_geo_centroidZ2 += z * 3;
	      d3_geo_pathCentroidPoint(x0 = x, y0 = y);
	    }
	    d3_geo_pathCentroid.lineEnd = function() {
	      nextPoint(x00, y00);
	    };
	  }
	  function d3_geo_pathContext(context) {
	    var pointRadius = 4.5;
	    var stream = {
	      point: point,
	      lineStart: function() {
	        stream.point = pointLineStart;
	      },
	      lineEnd: lineEnd,
	      polygonStart: function() {
	        stream.lineEnd = lineEndPolygon;
	      },
	      polygonEnd: function() {
	        stream.lineEnd = lineEnd;
	        stream.point = point;
	      },
	      pointRadius: function(_) {
	        pointRadius = _;
	        return stream;
	      },
	      result: d3_noop
	    };
	    function point(x, y) {
	      context.moveTo(x + pointRadius, y);
	      context.arc(x, y, pointRadius, 0, τ);
	    }
	    function pointLineStart(x, y) {
	      context.moveTo(x, y);
	      stream.point = pointLine;
	    }
	    function pointLine(x, y) {
	      context.lineTo(x, y);
	    }
	    function lineEnd() {
	      stream.point = point;
	    }
	    function lineEndPolygon() {
	      context.closePath();
	    }
	    return stream;
	  }
	  function d3_geo_resample(project) {
	    var δ2 = .5, cosMinDistance = Math.cos(30 * d3_radians), maxDepth = 16;
	    function resample(stream) {
	      return (maxDepth ? resampleRecursive : resampleNone)(stream);
	    }
	    function resampleNone(stream) {
	      return d3_geo_transformPoint(stream, function(x, y) {
	        x = project(x, y);
	        stream.point(x[0], x[1]);
	      });
	    }
	    function resampleRecursive(stream) {
	      var λ00, φ00, x00, y00, a00, b00, c00, λ0, x0, y0, a0, b0, c0;
	      var resample = {
	        point: point,
	        lineStart: lineStart,
	        lineEnd: lineEnd,
	        polygonStart: function() {
	          stream.polygonStart();
	          resample.lineStart = ringStart;
	        },
	        polygonEnd: function() {
	          stream.polygonEnd();
	          resample.lineStart = lineStart;
	        }
	      };
	      function point(x, y) {
	        x = project(x, y);
	        stream.point(x[0], x[1]);
	      }
	      function lineStart() {
	        x0 = NaN;
	        resample.point = linePoint;
	        stream.lineStart();
	      }
	      function linePoint(λ, φ) {
	        var c = d3_geo_cartesian([ λ, φ ]), p = project(λ, φ);
	        resampleLineTo(x0, y0, λ0, a0, b0, c0, x0 = p[0], y0 = p[1], λ0 = λ, a0 = c[0], b0 = c[1], c0 = c[2], maxDepth, stream);
	        stream.point(x0, y0);
	      }
	      function lineEnd() {
	        resample.point = point;
	        stream.lineEnd();
	      }
	      function ringStart() {
	        lineStart();
	        resample.point = ringPoint;
	        resample.lineEnd = ringEnd;
	      }
	      function ringPoint(λ, φ) {
	        linePoint(λ00 = λ, φ00 = φ), x00 = x0, y00 = y0, a00 = a0, b00 = b0, c00 = c0;
	        resample.point = linePoint;
	      }
	      function ringEnd() {
	        resampleLineTo(x0, y0, λ0, a0, b0, c0, x00, y00, λ00, a00, b00, c00, maxDepth, stream);
	        resample.lineEnd = lineEnd;
	        lineEnd();
	      }
	      return resample;
	    }
	    function resampleLineTo(x0, y0, λ0, a0, b0, c0, x1, y1, λ1, a1, b1, c1, depth, stream) {
	      var dx = x1 - x0, dy = y1 - y0, d2 = dx * dx + dy * dy;
	      if (d2 > 4 * δ2 && depth--) {
	        var a = a0 + a1, b = b0 + b1, c = c0 + c1, m = Math.sqrt(a * a + b * b + c * c), φ2 = Math.asin(c /= m), λ2 = abs(abs(c) - 1) < ε || abs(λ0 - λ1) < ε ? (λ0 + λ1) / 2 : Math.atan2(b, a), p = project(λ2, φ2), x2 = p[0], y2 = p[1], dx2 = x2 - x0, dy2 = y2 - y0, dz = dy * dx2 - dx * dy2;
	        if (dz * dz / d2 > δ2 || abs((dx * dx2 + dy * dy2) / d2 - .5) > .3 || a0 * a1 + b0 * b1 + c0 * c1 < cosMinDistance) {
	          resampleLineTo(x0, y0, λ0, a0, b0, c0, x2, y2, λ2, a /= m, b /= m, c, depth, stream);
	          stream.point(x2, y2);
	          resampleLineTo(x2, y2, λ2, a, b, c, x1, y1, λ1, a1, b1, c1, depth, stream);
	        }
	      }
	    }
	    resample.precision = function(_) {
	      if (!arguments.length) return Math.sqrt(δ2);
	      maxDepth = (δ2 = _ * _) > 0 && 16;
	      return resample;
	    };
	    return resample;
	  }
	  d3.geo.path = function() {
	    var pointRadius = 4.5, projection, context, projectStream, contextStream, cacheStream;
	    function path(object) {
	      if (object) {
	        if (typeof pointRadius === "function") contextStream.pointRadius(+pointRadius.apply(this, arguments));
	        if (!cacheStream || !cacheStream.valid) cacheStream = projectStream(contextStream);
	        d3.geo.stream(object, cacheStream);
	      }
	      return contextStream.result();
	    }
	    path.area = function(object) {
	      d3_geo_pathAreaSum = 0;
	      d3.geo.stream(object, projectStream(d3_geo_pathArea));
	      return d3_geo_pathAreaSum;
	    };
	    path.centroid = function(object) {
	      d3_geo_centroidX0 = d3_geo_centroidY0 = d3_geo_centroidZ0 = d3_geo_centroidX1 = d3_geo_centroidY1 = d3_geo_centroidZ1 = d3_geo_centroidX2 = d3_geo_centroidY2 = d3_geo_centroidZ2 = 0;
	      d3.geo.stream(object, projectStream(d3_geo_pathCentroid));
	      return d3_geo_centroidZ2 ? [ d3_geo_centroidX2 / d3_geo_centroidZ2, d3_geo_centroidY2 / d3_geo_centroidZ2 ] : d3_geo_centroidZ1 ? [ d3_geo_centroidX1 / d3_geo_centroidZ1, d3_geo_centroidY1 / d3_geo_centroidZ1 ] : d3_geo_centroidZ0 ? [ d3_geo_centroidX0 / d3_geo_centroidZ0, d3_geo_centroidY0 / d3_geo_centroidZ0 ] : [ NaN, NaN ];
	    };
	    path.bounds = function(object) {
	      d3_geo_pathBoundsX1 = d3_geo_pathBoundsY1 = -(d3_geo_pathBoundsX0 = d3_geo_pathBoundsY0 = Infinity);
	      d3.geo.stream(object, projectStream(d3_geo_pathBounds));
	      return [ [ d3_geo_pathBoundsX0, d3_geo_pathBoundsY0 ], [ d3_geo_pathBoundsX1, d3_geo_pathBoundsY1 ] ];
	    };
	    path.projection = function(_) {
	      if (!arguments.length) return projection;
	      projectStream = (projection = _) ? _.stream || d3_geo_pathProjectStream(_) : d3_identity;
	      return reset();
	    };
	    path.context = function(_) {
	      if (!arguments.length) return context;
	      contextStream = (context = _) == null ? new d3_geo_pathBuffer() : new d3_geo_pathContext(_);
	      if (typeof pointRadius !== "function") contextStream.pointRadius(pointRadius);
	      return reset();
	    };
	    path.pointRadius = function(_) {
	      if (!arguments.length) return pointRadius;
	      pointRadius = typeof _ === "function" ? _ : (contextStream.pointRadius(+_), +_);
	      return path;
	    };
	    function reset() {
	      cacheStream = null;
	      return path;
	    }
	    return path.projection(d3.geo.albersUsa()).context(null);
	  };
	  function d3_geo_pathProjectStream(project) {
	    var resample = d3_geo_resample(function(x, y) {
	      return project([ x * d3_degrees, y * d3_degrees ]);
	    });
	    return function(stream) {
	      return d3_geo_projectionRadians(resample(stream));
	    };
	  }
	  d3.geo.transform = function(methods) {
	    return {
	      stream: function(stream) {
	        var transform = new d3_geo_transform(stream);
	        for (var k in methods) transform[k] = methods[k];
	        return transform;
	      }
	    };
	  };
	  function d3_geo_transform(stream) {
	    this.stream = stream;
	  }
	  d3_geo_transform.prototype = {
	    point: function(x, y) {
	      this.stream.point(x, y);
	    },
	    sphere: function() {
	      this.stream.sphere();
	    },
	    lineStart: function() {
	      this.stream.lineStart();
	    },
	    lineEnd: function() {
	      this.stream.lineEnd();
	    },
	    polygonStart: function() {
	      this.stream.polygonStart();
	    },
	    polygonEnd: function() {
	      this.stream.polygonEnd();
	    }
	  };
	  function d3_geo_transformPoint(stream, point) {
	    return {
	      point: point,
	      sphere: function() {
	        stream.sphere();
	      },
	      lineStart: function() {
	        stream.lineStart();
	      },
	      lineEnd: function() {
	        stream.lineEnd();
	      },
	      polygonStart: function() {
	        stream.polygonStart();
	      },
	      polygonEnd: function() {
	        stream.polygonEnd();
	      }
	    };
	  }
	  d3.geo.projection = d3_geo_projection;
	  d3.geo.projectionMutator = d3_geo_projectionMutator;
	  function d3_geo_projection(project) {
	    return d3_geo_projectionMutator(function() {
	      return project;
	    })();
	  }
	  function d3_geo_projectionMutator(projectAt) {
	    var project, rotate, projectRotate, projectResample = d3_geo_resample(function(x, y) {
	      x = project(x, y);
	      return [ x[0] * k + δx, δy - x[1] * k ];
	    }), k = 150, x = 480, y = 250, λ = 0, φ = 0, δλ = 0, δφ = 0, δγ = 0, δx, δy, preclip = d3_geo_clipAntimeridian, postclip = d3_identity, clipAngle = null, clipExtent = null, stream;
	    function projection(point) {
	      point = projectRotate(point[0] * d3_radians, point[1] * d3_radians);
	      return [ point[0] * k + δx, δy - point[1] * k ];
	    }
	    function invert(point) {
	      point = projectRotate.invert((point[0] - δx) / k, (δy - point[1]) / k);
	      return point && [ point[0] * d3_degrees, point[1] * d3_degrees ];
	    }
	    projection.stream = function(output) {
	      if (stream) stream.valid = false;
	      stream = d3_geo_projectionRadians(preclip(rotate, projectResample(postclip(output))));
	      stream.valid = true;
	      return stream;
	    };
	    projection.clipAngle = function(_) {
	      if (!arguments.length) return clipAngle;
	      preclip = _ == null ? (clipAngle = _, d3_geo_clipAntimeridian) : d3_geo_clipCircle((clipAngle = +_) * d3_radians);
	      return invalidate();
	    };
	    projection.clipExtent = function(_) {
	      if (!arguments.length) return clipExtent;
	      clipExtent = _;
	      postclip = _ ? d3_geo_clipExtent(_[0][0], _[0][1], _[1][0], _[1][1]) : d3_identity;
	      return invalidate();
	    };
	    projection.scale = function(_) {
	      if (!arguments.length) return k;
	      k = +_;
	      return reset();
	    };
	    projection.translate = function(_) {
	      if (!arguments.length) return [ x, y ];
	      x = +_[0];
	      y = +_[1];
	      return reset();
	    };
	    projection.center = function(_) {
	      if (!arguments.length) return [ λ * d3_degrees, φ * d3_degrees ];
	      λ = _[0] % 360 * d3_radians;
	      φ = _[1] % 360 * d3_radians;
	      return reset();
	    };
	    projection.rotate = function(_) {
	      if (!arguments.length) return [ δλ * d3_degrees, δφ * d3_degrees, δγ * d3_degrees ];
	      δλ = _[0] % 360 * d3_radians;
	      δφ = _[1] % 360 * d3_radians;
	      δγ = _.length > 2 ? _[2] % 360 * d3_radians : 0;
	      return reset();
	    };
	    d3.rebind(projection, projectResample, "precision");
	    function reset() {
	      projectRotate = d3_geo_compose(rotate = d3_geo_rotation(δλ, δφ, δγ), project);
	      var center = project(λ, φ);
	      δx = x - center[0] * k;
	      δy = y + center[1] * k;
	      return invalidate();
	    }
	    function invalidate() {
	      if (stream) stream.valid = false, stream = null;
	      return projection;
	    }
	    return function() {
	      project = projectAt.apply(this, arguments);
	      projection.invert = project.invert && invert;
	      return reset();
	    };
	  }
	  function d3_geo_projectionRadians(stream) {
	    return d3_geo_transformPoint(stream, function(x, y) {
	      stream.point(x * d3_radians, y * d3_radians);
	    });
	  }
	  function d3_geo_equirectangular(λ, φ) {
	    return [ λ, φ ];
	  }
	  (d3.geo.equirectangular = function() {
	    return d3_geo_projection(d3_geo_equirectangular);
	  }).raw = d3_geo_equirectangular.invert = d3_geo_equirectangular;
	  d3.geo.rotation = function(rotate) {
	    rotate = d3_geo_rotation(rotate[0] % 360 * d3_radians, rotate[1] * d3_radians, rotate.length > 2 ? rotate[2] * d3_radians : 0);
	    function forward(coordinates) {
	      coordinates = rotate(coordinates[0] * d3_radians, coordinates[1] * d3_radians);
	      return coordinates[0] *= d3_degrees, coordinates[1] *= d3_degrees, coordinates;
	    }
	    forward.invert = function(coordinates) {
	      coordinates = rotate.invert(coordinates[0] * d3_radians, coordinates[1] * d3_radians);
	      return coordinates[0] *= d3_degrees, coordinates[1] *= d3_degrees, coordinates;
	    };
	    return forward;
	  };
	  function d3_geo_identityRotation(λ, φ) {
	    return [ λ > π ? λ - τ : λ < -π ? λ + τ : λ, φ ];
	  }
	  d3_geo_identityRotation.invert = d3_geo_equirectangular;
	  function d3_geo_rotation(δλ, δφ, δγ) {
	    return δλ ? δφ || δγ ? d3_geo_compose(d3_geo_rotationλ(δλ), d3_geo_rotationφγ(δφ, δγ)) : d3_geo_rotationλ(δλ) : δφ || δγ ? d3_geo_rotationφγ(δφ, δγ) : d3_geo_identityRotation;
	  }
	  function d3_geo_forwardRotationλ(δλ) {
	    return function(λ, φ) {
	      return λ += δλ, [ λ > π ? λ - τ : λ < -π ? λ + τ : λ, φ ];
	    };
	  }
	  function d3_geo_rotationλ(δλ) {
	    var rotation = d3_geo_forwardRotationλ(δλ);
	    rotation.invert = d3_geo_forwardRotationλ(-δλ);
	    return rotation;
	  }
	  function d3_geo_rotationφγ(δφ, δγ) {
	    var cosδφ = Math.cos(δφ), sinδφ = Math.sin(δφ), cosδγ = Math.cos(δγ), sinδγ = Math.sin(δγ);
	    function rotation(λ, φ) {
	      var cosφ = Math.cos(φ), x = Math.cos(λ) * cosφ, y = Math.sin(λ) * cosφ, z = Math.sin(φ), k = z * cosδφ + x * sinδφ;
	      return [ Math.atan2(y * cosδγ - k * sinδγ, x * cosδφ - z * sinδφ), d3_asin(k * cosδγ + y * sinδγ) ];
	    }
	    rotation.invert = function(λ, φ) {
	      var cosφ = Math.cos(φ), x = Math.cos(λ) * cosφ, y = Math.sin(λ) * cosφ, z = Math.sin(φ), k = z * cosδγ - y * sinδγ;
	      return [ Math.atan2(y * cosδγ + z * sinδγ, x * cosδφ + k * sinδφ), d3_asin(k * cosδφ - x * sinδφ) ];
	    };
	    return rotation;
	  }
	  d3.geo.circle = function() {
	    var origin = [ 0, 0 ], angle, precision = 6, interpolate;
	    function circle() {
	      var center = typeof origin === "function" ? origin.apply(this, arguments) : origin, rotate = d3_geo_rotation(-center[0] * d3_radians, -center[1] * d3_radians, 0).invert, ring = [];
	      interpolate(null, null, 1, {
	        point: function(x, y) {
	          ring.push(x = rotate(x, y));
	          x[0] *= d3_degrees, x[1] *= d3_degrees;
	        }
	      });
	      return {
	        type: "Polygon",
	        coordinates: [ ring ]
	      };
	    }
	    circle.origin = function(x) {
	      if (!arguments.length) return origin;
	      origin = x;
	      return circle;
	    };
	    circle.angle = function(x) {
	      if (!arguments.length) return angle;
	      interpolate = d3_geo_circleInterpolate((angle = +x) * d3_radians, precision * d3_radians);
	      return circle;
	    };
	    circle.precision = function(_) {
	      if (!arguments.length) return precision;
	      interpolate = d3_geo_circleInterpolate(angle * d3_radians, (precision = +_) * d3_radians);
	      return circle;
	    };
	    return circle.angle(90);
	  };
	  function d3_geo_circleInterpolate(radius, precision) {
	    var cr = Math.cos(radius), sr = Math.sin(radius);
	    return function(from, to, direction, listener) {
	      var step = direction * precision;
	      if (from != null) {
	        from = d3_geo_circleAngle(cr, from);
	        to = d3_geo_circleAngle(cr, to);
	        if (direction > 0 ? from < to : from > to) from += direction * τ;
	      } else {
	        from = radius + direction * τ;
	        to = radius - .5 * step;
	      }
	      for (var point, t = from; direction > 0 ? t > to : t < to; t -= step) {
	        listener.point((point = d3_geo_spherical([ cr, -sr * Math.cos(t), -sr * Math.sin(t) ]))[0], point[1]);
	      }
	    };
	  }
	  function d3_geo_circleAngle(cr, point) {
	    var a = d3_geo_cartesian(point);
	    a[0] -= cr;
	    d3_geo_cartesianNormalize(a);
	    var angle = d3_acos(-a[1]);
	    return ((-a[2] < 0 ? -angle : angle) + 2 * Math.PI - ε) % (2 * Math.PI);
	  }
	  d3.geo.distance = function(a, b) {
	    var Δλ = (b[0] - a[0]) * d3_radians, φ0 = a[1] * d3_radians, φ1 = b[1] * d3_radians, sinΔλ = Math.sin(Δλ), cosΔλ = Math.cos(Δλ), sinφ0 = Math.sin(φ0), cosφ0 = Math.cos(φ0), sinφ1 = Math.sin(φ1), cosφ1 = Math.cos(φ1), t;
	    return Math.atan2(Math.sqrt((t = cosφ1 * sinΔλ) * t + (t = cosφ0 * sinφ1 - sinφ0 * cosφ1 * cosΔλ) * t), sinφ0 * sinφ1 + cosφ0 * cosφ1 * cosΔλ);
	  };
	  d3.geo.graticule = function() {
	    var x1, x0, X1, X0, y1, y0, Y1, Y0, dx = 10, dy = dx, DX = 90, DY = 360, x, y, X, Y, precision = 2.5;
	    function graticule() {
	      return {
	        type: "MultiLineString",
	        coordinates: lines()
	      };
	    }
	    function lines() {
	      return d3.range(Math.ceil(X0 / DX) * DX, X1, DX).map(X).concat(d3.range(Math.ceil(Y0 / DY) * DY, Y1, DY).map(Y)).concat(d3.range(Math.ceil(x0 / dx) * dx, x1, dx).filter(function(x) {
	        return abs(x % DX) > ε;
	      }).map(x)).concat(d3.range(Math.ceil(y0 / dy) * dy, y1, dy).filter(function(y) {
	        return abs(y % DY) > ε;
	      }).map(y));
	    }
	    graticule.lines = function() {
	      return lines().map(function(coordinates) {
	        return {
	          type: "LineString",
	          coordinates: coordinates
	        };
	      });
	    };
	    graticule.outline = function() {
	      return {
	        type: "Polygon",
	        coordinates: [ X(X0).concat(Y(Y1).slice(1), X(X1).reverse().slice(1), Y(Y0).reverse().slice(1)) ]
	      };
	    };
	    graticule.extent = function(_) {
	      if (!arguments.length) return graticule.minorExtent();
	      return graticule.majorExtent(_).minorExtent(_);
	    };
	    graticule.majorExtent = function(_) {
	      if (!arguments.length) return [ [ X0, Y0 ], [ X1, Y1 ] ];
	      X0 = +_[0][0], X1 = +_[1][0];
	      Y0 = +_[0][1], Y1 = +_[1][1];
	      if (X0 > X1) _ = X0, X0 = X1, X1 = _;
	      if (Y0 > Y1) _ = Y0, Y0 = Y1, Y1 = _;
	      return graticule.precision(precision);
	    };
	    graticule.minorExtent = function(_) {
	      if (!arguments.length) return [ [ x0, y0 ], [ x1, y1 ] ];
	      x0 = +_[0][0], x1 = +_[1][0];
	      y0 = +_[0][1], y1 = +_[1][1];
	      if (x0 > x1) _ = x0, x0 = x1, x1 = _;
	      if (y0 > y1) _ = y0, y0 = y1, y1 = _;
	      return graticule.precision(precision);
	    };
	    graticule.step = function(_) {
	      if (!arguments.length) return graticule.minorStep();
	      return graticule.majorStep(_).minorStep(_);
	    };
	    graticule.majorStep = function(_) {
	      if (!arguments.length) return [ DX, DY ];
	      DX = +_[0], DY = +_[1];
	      return graticule;
	    };
	    graticule.minorStep = function(_) {
	      if (!arguments.length) return [ dx, dy ];
	      dx = +_[0], dy = +_[1];
	      return graticule;
	    };
	    graticule.precision = function(_) {
	      if (!arguments.length) return precision;
	      precision = +_;
	      x = d3_geo_graticuleX(y0, y1, 90);
	      y = d3_geo_graticuleY(x0, x1, precision);
	      X = d3_geo_graticuleX(Y0, Y1, 90);
	      Y = d3_geo_graticuleY(X0, X1, precision);
	      return graticule;
	    };
	    return graticule.majorExtent([ [ -180, -90 + ε ], [ 180, 90 - ε ] ]).minorExtent([ [ -180, -80 - ε ], [ 180, 80 + ε ] ]);
	  };
	  function d3_geo_graticuleX(y0, y1, dy) {
	    var y = d3.range(y0, y1 - ε, dy).concat(y1);
	    return function(x) {
	      return y.map(function(y) {
	        return [ x, y ];
	      });
	    };
	  }
	  function d3_geo_graticuleY(x0, x1, dx) {
	    var x = d3.range(x0, x1 - ε, dx).concat(x1);
	    return function(y) {
	      return x.map(function(x) {
	        return [ x, y ];
	      });
	    };
	  }
	  function d3_source(d) {
	    return d.source;
	  }
	  function d3_target(d) {
	    return d.target;
	  }
	  d3.geo.greatArc = function() {
	    var source = d3_source, source_, target = d3_target, target_;
	    function greatArc() {
	      return {
	        type: "LineString",
	        coordinates: [ source_ || source.apply(this, arguments), target_ || target.apply(this, arguments) ]
	      };
	    }
	    greatArc.distance = function() {
	      return d3.geo.distance(source_ || source.apply(this, arguments), target_ || target.apply(this, arguments));
	    };
	    greatArc.source = function(_) {
	      if (!arguments.length) return source;
	      source = _, source_ = typeof _ === "function" ? null : _;
	      return greatArc;
	    };
	    greatArc.target = function(_) {
	      if (!arguments.length) return target;
	      target = _, target_ = typeof _ === "function" ? null : _;
	      return greatArc;
	    };
	    greatArc.precision = function() {
	      return arguments.length ? greatArc : 0;
	    };
	    return greatArc;
	  };
	  d3.geo.interpolate = function(source, target) {
	    return d3_geo_interpolate(source[0] * d3_radians, source[1] * d3_radians, target[0] * d3_radians, target[1] * d3_radians);
	  };
	  function d3_geo_interpolate(x0, y0, x1, y1) {
	    var cy0 = Math.cos(y0), sy0 = Math.sin(y0), cy1 = Math.cos(y1), sy1 = Math.sin(y1), kx0 = cy0 * Math.cos(x0), ky0 = cy0 * Math.sin(x0), kx1 = cy1 * Math.cos(x1), ky1 = cy1 * Math.sin(x1), d = 2 * Math.asin(Math.sqrt(d3_haversin(y1 - y0) + cy0 * cy1 * d3_haversin(x1 - x0))), k = 1 / Math.sin(d);
	    var interpolate = d ? function(t) {
	      var B = Math.sin(t *= d) * k, A = Math.sin(d - t) * k, x = A * kx0 + B * kx1, y = A * ky0 + B * ky1, z = A * sy0 + B * sy1;
	      return [ Math.atan2(y, x) * d3_degrees, Math.atan2(z, Math.sqrt(x * x + y * y)) * d3_degrees ];
	    } : function() {
	      return [ x0 * d3_degrees, y0 * d3_degrees ];
	    };
	    interpolate.distance = d;
	    return interpolate;
	  }
	  d3.geo.length = function(object) {
	    d3_geo_lengthSum = 0;
	    d3.geo.stream(object, d3_geo_length);
	    return d3_geo_lengthSum;
	  };
	  var d3_geo_lengthSum;
	  var d3_geo_length = {
	    sphere: d3_noop,
	    point: d3_noop,
	    lineStart: d3_geo_lengthLineStart,
	    lineEnd: d3_noop,
	    polygonStart: d3_noop,
	    polygonEnd: d3_noop
	  };
	  function d3_geo_lengthLineStart() {
	    var λ0, sinφ0, cosφ0;
	    d3_geo_length.point = function(λ, φ) {
	      λ0 = λ * d3_radians, sinφ0 = Math.sin(φ *= d3_radians), cosφ0 = Math.cos(φ);
	      d3_geo_length.point = nextPoint;
	    };
	    d3_geo_length.lineEnd = function() {
	      d3_geo_length.point = d3_geo_length.lineEnd = d3_noop;
	    };
	    function nextPoint(λ, φ) {
	      var sinφ = Math.sin(φ *= d3_radians), cosφ = Math.cos(φ), t = abs((λ *= d3_radians) - λ0), cosΔλ = Math.cos(t);
	      d3_geo_lengthSum += Math.atan2(Math.sqrt((t = cosφ * Math.sin(t)) * t + (t = cosφ0 * sinφ - sinφ0 * cosφ * cosΔλ) * t), sinφ0 * sinφ + cosφ0 * cosφ * cosΔλ);
	      λ0 = λ, sinφ0 = sinφ, cosφ0 = cosφ;
	    }
	  }
	  function d3_geo_azimuthal(scale, angle) {
	    function azimuthal(λ, φ) {
	      var cosλ = Math.cos(λ), cosφ = Math.cos(φ), k = scale(cosλ * cosφ);
	      return [ k * cosφ * Math.sin(λ), k * Math.sin(φ) ];
	    }
	    azimuthal.invert = function(x, y) {
	      var ρ = Math.sqrt(x * x + y * y), c = angle(ρ), sinc = Math.sin(c), cosc = Math.cos(c);
	      return [ Math.atan2(x * sinc, ρ * cosc), Math.asin(ρ && y * sinc / ρ) ];
	    };
	    return azimuthal;
	  }
	  var d3_geo_azimuthalEqualArea = d3_geo_azimuthal(function(cosλcosφ) {
	    return Math.sqrt(2 / (1 + cosλcosφ));
	  }, function(ρ) {
	    return 2 * Math.asin(ρ / 2);
	  });
	  (d3.geo.azimuthalEqualArea = function() {
	    return d3_geo_projection(d3_geo_azimuthalEqualArea);
	  }).raw = d3_geo_azimuthalEqualArea;
	  var d3_geo_azimuthalEquidistant = d3_geo_azimuthal(function(cosλcosφ) {
	    var c = Math.acos(cosλcosφ);
	    return c && c / Math.sin(c);
	  }, d3_identity);
	  (d3.geo.azimuthalEquidistant = function() {
	    return d3_geo_projection(d3_geo_azimuthalEquidistant);
	  }).raw = d3_geo_azimuthalEquidistant;
	  function d3_geo_conicConformal(φ0, φ1) {
	    var cosφ0 = Math.cos(φ0), t = function(φ) {
	      return Math.tan(π / 4 + φ / 2);
	    }, n = φ0 === φ1 ? Math.sin(φ0) : Math.log(cosφ0 / Math.cos(φ1)) / Math.log(t(φ1) / t(φ0)), F = cosφ0 * Math.pow(t(φ0), n) / n;
	    if (!n) return d3_geo_mercator;
	    function forward(λ, φ) {
	      if (F > 0) {
	        if (φ < -halfπ + ε) φ = -halfπ + ε;
	      } else {
	        if (φ > halfπ - ε) φ = halfπ - ε;
	      }
	      var ρ = F / Math.pow(t(φ), n);
	      return [ ρ * Math.sin(n * λ), F - ρ * Math.cos(n * λ) ];
	    }
	    forward.invert = function(x, y) {
	      var ρ0_y = F - y, ρ = d3_sgn(n) * Math.sqrt(x * x + ρ0_y * ρ0_y);
	      return [ Math.atan2(x, ρ0_y) / n, 2 * Math.atan(Math.pow(F / ρ, 1 / n)) - halfπ ];
	    };
	    return forward;
	  }
	  (d3.geo.conicConformal = function() {
	    return d3_geo_conic(d3_geo_conicConformal);
	  }).raw = d3_geo_conicConformal;
	  function d3_geo_conicEquidistant(φ0, φ1) {
	    var cosφ0 = Math.cos(φ0), n = φ0 === φ1 ? Math.sin(φ0) : (cosφ0 - Math.cos(φ1)) / (φ1 - φ0), G = cosφ0 / n + φ0;
	    if (abs(n) < ε) return d3_geo_equirectangular;
	    function forward(λ, φ) {
	      var ρ = G - φ;
	      return [ ρ * Math.sin(n * λ), G - ρ * Math.cos(n * λ) ];
	    }
	    forward.invert = function(x, y) {
	      var ρ0_y = G - y;
	      return [ Math.atan2(x, ρ0_y) / n, G - d3_sgn(n) * Math.sqrt(x * x + ρ0_y * ρ0_y) ];
	    };
	    return forward;
	  }
	  (d3.geo.conicEquidistant = function() {
	    return d3_geo_conic(d3_geo_conicEquidistant);
	  }).raw = d3_geo_conicEquidistant;
	  var d3_geo_gnomonic = d3_geo_azimuthal(function(cosλcosφ) {
	    return 1 / cosλcosφ;
	  }, Math.atan);
	  (d3.geo.gnomonic = function() {
	    return d3_geo_projection(d3_geo_gnomonic);
	  }).raw = d3_geo_gnomonic;
	  function d3_geo_mercator(λ, φ) {
	    return [ λ, Math.log(Math.tan(π / 4 + φ / 2)) ];
	  }
	  d3_geo_mercator.invert = function(x, y) {
	    return [ x, 2 * Math.atan(Math.exp(y)) - halfπ ];
	  };
	  function d3_geo_mercatorProjection(project) {
	    var m = d3_geo_projection(project), scale = m.scale, translate = m.translate, clipExtent = m.clipExtent, clipAuto;
	    m.scale = function() {
	      var v = scale.apply(m, arguments);
	      return v === m ? clipAuto ? m.clipExtent(null) : m : v;
	    };
	    m.translate = function() {
	      var v = translate.apply(m, arguments);
	      return v === m ? clipAuto ? m.clipExtent(null) : m : v;
	    };
	    m.clipExtent = function(_) {
	      var v = clipExtent.apply(m, arguments);
	      if (v === m) {
	        if (clipAuto = _ == null) {
	          var k = π * scale(), t = translate();
	          clipExtent([ [ t[0] - k, t[1] - k ], [ t[0] + k, t[1] + k ] ]);
	        }
	      } else if (clipAuto) {
	        v = null;
	      }
	      return v;
	    };
	    return m.clipExtent(null);
	  }
	  (d3.geo.mercator = function() {
	    return d3_geo_mercatorProjection(d3_geo_mercator);
	  }).raw = d3_geo_mercator;
	  var d3_geo_orthographic = d3_geo_azimuthal(function() {
	    return 1;
	  }, Math.asin);
	  (d3.geo.orthographic = function() {
	    return d3_geo_projection(d3_geo_orthographic);
	  }).raw = d3_geo_orthographic;
	  var d3_geo_stereographic = d3_geo_azimuthal(function(cosλcosφ) {
	    return 1 / (1 + cosλcosφ);
	  }, function(ρ) {
	    return 2 * Math.atan(ρ);
	  });
	  (d3.geo.stereographic = function() {
	    return d3_geo_projection(d3_geo_stereographic);
	  }).raw = d3_geo_stereographic;
	  function d3_geo_transverseMercator(λ, φ) {
	    return [ Math.log(Math.tan(π / 4 + φ / 2)), -λ ];
	  }
	  d3_geo_transverseMercator.invert = function(x, y) {
	    return [ -y, 2 * Math.atan(Math.exp(x)) - halfπ ];
	  };
	  (d3.geo.transverseMercator = function() {
	    var projection = d3_geo_mercatorProjection(d3_geo_transverseMercator), center = projection.center, rotate = projection.rotate;
	    projection.center = function(_) {
	      return _ ? center([ -_[1], _[0] ]) : (_ = center(), [ _[1], -_[0] ]);
	    };
	    projection.rotate = function(_) {
	      return _ ? rotate([ _[0], _[1], _.length > 2 ? _[2] + 90 : 90 ]) : (_ = rotate(), 
	      [ _[0], _[1], _[2] - 90 ]);
	    };
	    return rotate([ 0, 0, 90 ]);
	  }).raw = d3_geo_transverseMercator;
	  d3.geom = {};
	  function d3_geom_pointX(d) {
	    return d[0];
	  }
	  function d3_geom_pointY(d) {
	    return d[1];
	  }
	  d3.geom.hull = function(vertices) {
	    var x = d3_geom_pointX, y = d3_geom_pointY;
	    if (arguments.length) return hull(vertices);
	    function hull(data) {
	      if (data.length < 3) return [];
	      var fx = d3_functor(x), fy = d3_functor(y), i, n = data.length, points = [], flippedPoints = [];
	      for (i = 0; i < n; i++) {
	        points.push([ +fx.call(this, data[i], i), +fy.call(this, data[i], i), i ]);
	      }
	      points.sort(d3_geom_hullOrder);
	      for (i = 0; i < n; i++) flippedPoints.push([ points[i][0], -points[i][1] ]);
	      var upper = d3_geom_hullUpper(points), lower = d3_geom_hullUpper(flippedPoints);
	      var skipLeft = lower[0] === upper[0], skipRight = lower[lower.length - 1] === upper[upper.length - 1], polygon = [];
	      for (i = upper.length - 1; i >= 0; --i) polygon.push(data[points[upper[i]][2]]);
	      for (i = +skipLeft; i < lower.length - skipRight; ++i) polygon.push(data[points[lower[i]][2]]);
	      return polygon;
	    }
	    hull.x = function(_) {
	      return arguments.length ? (x = _, hull) : x;
	    };
	    hull.y = function(_) {
	      return arguments.length ? (y = _, hull) : y;
	    };
	    return hull;
	  };
	  function d3_geom_hullUpper(points) {
	    var n = points.length, hull = [ 0, 1 ], hs = 2;
	    for (var i = 2; i < n; i++) {
	      while (hs > 1 && d3_cross2d(points[hull[hs - 2]], points[hull[hs - 1]], points[i]) <= 0) --hs;
	      hull[hs++] = i;
	    }
	    return hull.slice(0, hs);
	  }
	  function d3_geom_hullOrder(a, b) {
	    return a[0] - b[0] || a[1] - b[1];
	  }
	  d3.geom.polygon = function(coordinates) {
	    d3_subclass(coordinates, d3_geom_polygonPrototype);
	    return coordinates;
	  };
	  var d3_geom_polygonPrototype = d3.geom.polygon.prototype = [];
	  d3_geom_polygonPrototype.area = function() {
	    var i = -1, n = this.length, a, b = this[n - 1], area = 0;
	    while (++i < n) {
	      a = b;
	      b = this[i];
	      area += a[1] * b[0] - a[0] * b[1];
	    }
	    return area * .5;
	  };
	  d3_geom_polygonPrototype.centroid = function(k) {
	    var i = -1, n = this.length, x = 0, y = 0, a, b = this[n - 1], c;
	    if (!arguments.length) k = -1 / (6 * this.area());
	    while (++i < n) {
	      a = b;
	      b = this[i];
	      c = a[0] * b[1] - b[0] * a[1];
	      x += (a[0] + b[0]) * c;
	      y += (a[1] + b[1]) * c;
	    }
	    return [ x * k, y * k ];
	  };
	  d3_geom_polygonPrototype.clip = function(subject) {
	    var input, closed = d3_geom_polygonClosed(subject), i = -1, n = this.length - d3_geom_polygonClosed(this), j, m, a = this[n - 1], b, c, d;
	    while (++i < n) {
	      input = subject.slice();
	      subject.length = 0;
	      b = this[i];
	      c = input[(m = input.length - closed) - 1];
	      j = -1;
	      while (++j < m) {
	        d = input[j];
	        if (d3_geom_polygonInside(d, a, b)) {
	          if (!d3_geom_polygonInside(c, a, b)) {
	            subject.push(d3_geom_polygonIntersect(c, d, a, b));
	          }
	          subject.push(d);
	        } else if (d3_geom_polygonInside(c, a, b)) {
	          subject.push(d3_geom_polygonIntersect(c, d, a, b));
	        }
	        c = d;
	      }
	      if (closed) subject.push(subject[0]);
	      a = b;
	    }
	    return subject;
	  };
	  function d3_geom_polygonInside(p, a, b) {
	    return (b[0] - a[0]) * (p[1] - a[1]) < (b[1] - a[1]) * (p[0] - a[0]);
	  }
	  function d3_geom_polygonIntersect(c, d, a, b) {
	    var x1 = c[0], x3 = a[0], x21 = d[0] - x1, x43 = b[0] - x3, y1 = c[1], y3 = a[1], y21 = d[1] - y1, y43 = b[1] - y3, ua = (x43 * (y1 - y3) - y43 * (x1 - x3)) / (y43 * x21 - x43 * y21);
	    return [ x1 + ua * x21, y1 + ua * y21 ];
	  }
	  function d3_geom_polygonClosed(coordinates) {
	    var a = coordinates[0], b = coordinates[coordinates.length - 1];
	    return !(a[0] - b[0] || a[1] - b[1]);
	  }
	  var d3_geom_voronoiEdges, d3_geom_voronoiCells, d3_geom_voronoiBeaches, d3_geom_voronoiBeachPool = [], d3_geom_voronoiFirstCircle, d3_geom_voronoiCircles, d3_geom_voronoiCirclePool = [];
	  function d3_geom_voronoiBeach() {
	    d3_geom_voronoiRedBlackNode(this);
	    this.edge = this.site = this.circle = null;
	  }
	  function d3_geom_voronoiCreateBeach(site) {
	    var beach = d3_geom_voronoiBeachPool.pop() || new d3_geom_voronoiBeach();
	    beach.site = site;
	    return beach;
	  }
	  function d3_geom_voronoiDetachBeach(beach) {
	    d3_geom_voronoiDetachCircle(beach);
	    d3_geom_voronoiBeaches.remove(beach);
	    d3_geom_voronoiBeachPool.push(beach);
	    d3_geom_voronoiRedBlackNode(beach);
	  }
	  function d3_geom_voronoiRemoveBeach(beach) {
	    var circle = beach.circle, x = circle.x, y = circle.cy, vertex = {
	      x: x,
	      y: y
	    }, previous = beach.P, next = beach.N, disappearing = [ beach ];
	    d3_geom_voronoiDetachBeach(beach);
	    var lArc = previous;
	    while (lArc.circle && abs(x - lArc.circle.x) < ε && abs(y - lArc.circle.cy) < ε) {
	      previous = lArc.P;
	      disappearing.unshift(lArc);
	      d3_geom_voronoiDetachBeach(lArc);
	      lArc = previous;
	    }
	    disappearing.unshift(lArc);
	    d3_geom_voronoiDetachCircle(lArc);
	    var rArc = next;
	    while (rArc.circle && abs(x - rArc.circle.x) < ε && abs(y - rArc.circle.cy) < ε) {
	      next = rArc.N;
	      disappearing.push(rArc);
	      d3_geom_voronoiDetachBeach(rArc);
	      rArc = next;
	    }
	    disappearing.push(rArc);
	    d3_geom_voronoiDetachCircle(rArc);
	    var nArcs = disappearing.length, iArc;
	    for (iArc = 1; iArc < nArcs; ++iArc) {
	      rArc = disappearing[iArc];
	      lArc = disappearing[iArc - 1];
	      d3_geom_voronoiSetEdgeEnd(rArc.edge, lArc.site, rArc.site, vertex);
	    }
	    lArc = disappearing[0];
	    rArc = disappearing[nArcs - 1];
	    rArc.edge = d3_geom_voronoiCreateEdge(lArc.site, rArc.site, null, vertex);
	    d3_geom_voronoiAttachCircle(lArc);
	    d3_geom_voronoiAttachCircle(rArc);
	  }
	  function d3_geom_voronoiAddBeach(site) {
	    var x = site.x, directrix = site.y, lArc, rArc, dxl, dxr, node = d3_geom_voronoiBeaches._;
	    while (node) {
	      dxl = d3_geom_voronoiLeftBreakPoint(node, directrix) - x;
	      if (dxl > ε) node = node.L; else {
	        dxr = x - d3_geom_voronoiRightBreakPoint(node, directrix);
	        if (dxr > ε) {
	          if (!node.R) {
	            lArc = node;
	            break;
	          }
	          node = node.R;
	        } else {
	          if (dxl > -ε) {
	            lArc = node.P;
	            rArc = node;
	          } else if (dxr > -ε) {
	            lArc = node;
	            rArc = node.N;
	          } else {
	            lArc = rArc = node;
	          }
	          break;
	        }
	      }
	    }
	    var newArc = d3_geom_voronoiCreateBeach(site);
	    d3_geom_voronoiBeaches.insert(lArc, newArc);
	    if (!lArc && !rArc) return;
	    if (lArc === rArc) {
	      d3_geom_voronoiDetachCircle(lArc);
	      rArc = d3_geom_voronoiCreateBeach(lArc.site);
	      d3_geom_voronoiBeaches.insert(newArc, rArc);
	      newArc.edge = rArc.edge = d3_geom_voronoiCreateEdge(lArc.site, newArc.site);
	      d3_geom_voronoiAttachCircle(lArc);
	      d3_geom_voronoiAttachCircle(rArc);
	      return;
	    }
	    if (!rArc) {
	      newArc.edge = d3_geom_voronoiCreateEdge(lArc.site, newArc.site);
	      return;
	    }
	    d3_geom_voronoiDetachCircle(lArc);
	    d3_geom_voronoiDetachCircle(rArc);
	    var lSite = lArc.site, ax = lSite.x, ay = lSite.y, bx = site.x - ax, by = site.y - ay, rSite = rArc.site, cx = rSite.x - ax, cy = rSite.y - ay, d = 2 * (bx * cy - by * cx), hb = bx * bx + by * by, hc = cx * cx + cy * cy, vertex = {
	      x: (cy * hb - by * hc) / d + ax,
	      y: (bx * hc - cx * hb) / d + ay
	    };
	    d3_geom_voronoiSetEdgeEnd(rArc.edge, lSite, rSite, vertex);
	    newArc.edge = d3_geom_voronoiCreateEdge(lSite, site, null, vertex);
	    rArc.edge = d3_geom_voronoiCreateEdge(site, rSite, null, vertex);
	    d3_geom_voronoiAttachCircle(lArc);
	    d3_geom_voronoiAttachCircle(rArc);
	  }
	  function d3_geom_voronoiLeftBreakPoint(arc, directrix) {
	    var site = arc.site, rfocx = site.x, rfocy = site.y, pby2 = rfocy - directrix;
	    if (!pby2) return rfocx;
	    var lArc = arc.P;
	    if (!lArc) return -Infinity;
	    site = lArc.site;
	    var lfocx = site.x, lfocy = site.y, plby2 = lfocy - directrix;
	    if (!plby2) return lfocx;
	    var hl = lfocx - rfocx, aby2 = 1 / pby2 - 1 / plby2, b = hl / plby2;
	    if (aby2) return (-b + Math.sqrt(b * b - 2 * aby2 * (hl * hl / (-2 * plby2) - lfocy + plby2 / 2 + rfocy - pby2 / 2))) / aby2 + rfocx;
	    return (rfocx + lfocx) / 2;
	  }
	  function d3_geom_voronoiRightBreakPoint(arc, directrix) {
	    var rArc = arc.N;
	    if (rArc) return d3_geom_voronoiLeftBreakPoint(rArc, directrix);
	    var site = arc.site;
	    return site.y === directrix ? site.x : Infinity;
	  }
	  function d3_geom_voronoiCell(site) {
	    this.site = site;
	    this.edges = [];
	  }
	  d3_geom_voronoiCell.prototype.prepare = function() {
	    var halfEdges = this.edges, iHalfEdge = halfEdges.length, edge;
	    while (iHalfEdge--) {
	      edge = halfEdges[iHalfEdge].edge;
	      if (!edge.b || !edge.a) halfEdges.splice(iHalfEdge, 1);
	    }
	    halfEdges.sort(d3_geom_voronoiHalfEdgeOrder);
	    return halfEdges.length;
	  };
	  function d3_geom_voronoiCloseCells(extent) {
	    var x0 = extent[0][0], x1 = extent[1][0], y0 = extent[0][1], y1 = extent[1][1], x2, y2, x3, y3, cells = d3_geom_voronoiCells, iCell = cells.length, cell, iHalfEdge, halfEdges, nHalfEdges, start, end;
	    while (iCell--) {
	      cell = cells[iCell];
	      if (!cell || !cell.prepare()) continue;
	      halfEdges = cell.edges;
	      nHalfEdges = halfEdges.length;
	      iHalfEdge = 0;
	      while (iHalfEdge < nHalfEdges) {
	        end = halfEdges[iHalfEdge].end(), x3 = end.x, y3 = end.y;
	        start = halfEdges[++iHalfEdge % nHalfEdges].start(), x2 = start.x, y2 = start.y;
	        if (abs(x3 - x2) > ε || abs(y3 - y2) > ε) {
	          halfEdges.splice(iHalfEdge, 0, new d3_geom_voronoiHalfEdge(d3_geom_voronoiCreateBorderEdge(cell.site, end, abs(x3 - x0) < ε && y1 - y3 > ε ? {
	            x: x0,
	            y: abs(x2 - x0) < ε ? y2 : y1
	          } : abs(y3 - y1) < ε && x1 - x3 > ε ? {
	            x: abs(y2 - y1) < ε ? x2 : x1,
	            y: y1
	          } : abs(x3 - x1) < ε && y3 - y0 > ε ? {
	            x: x1,
	            y: abs(x2 - x1) < ε ? y2 : y0
	          } : abs(y3 - y0) < ε && x3 - x0 > ε ? {
	            x: abs(y2 - y0) < ε ? x2 : x0,
	            y: y0
	          } : null), cell.site, null));
	          ++nHalfEdges;
	        }
	      }
	    }
	  }
	  function d3_geom_voronoiHalfEdgeOrder(a, b) {
	    return b.angle - a.angle;
	  }
	  function d3_geom_voronoiCircle() {
	    d3_geom_voronoiRedBlackNode(this);
	    this.x = this.y = this.arc = this.site = this.cy = null;
	  }
	  function d3_geom_voronoiAttachCircle(arc) {
	    var lArc = arc.P, rArc = arc.N;
	    if (!lArc || !rArc) return;
	    var lSite = lArc.site, cSite = arc.site, rSite = rArc.site;
	    if (lSite === rSite) return;
	    var bx = cSite.x, by = cSite.y, ax = lSite.x - bx, ay = lSite.y - by, cx = rSite.x - bx, cy = rSite.y - by;
	    var d = 2 * (ax * cy - ay * cx);
	    if (d >= -ε2) return;
	    var ha = ax * ax + ay * ay, hc = cx * cx + cy * cy, x = (cy * ha - ay * hc) / d, y = (ax * hc - cx * ha) / d, cy = y + by;
	    var circle = d3_geom_voronoiCirclePool.pop() || new d3_geom_voronoiCircle();
	    circle.arc = arc;
	    circle.site = cSite;
	    circle.x = x + bx;
	    circle.y = cy + Math.sqrt(x * x + y * y);
	    circle.cy = cy;
	    arc.circle = circle;
	    var before = null, node = d3_geom_voronoiCircles._;
	    while (node) {
	      if (circle.y < node.y || circle.y === node.y && circle.x <= node.x) {
	        if (node.L) node = node.L; else {
	          before = node.P;
	          break;
	        }
	      } else {
	        if (node.R) node = node.R; else {
	          before = node;
	          break;
	        }
	      }
	    }
	    d3_geom_voronoiCircles.insert(before, circle);
	    if (!before) d3_geom_voronoiFirstCircle = circle;
	  }
	  function d3_geom_voronoiDetachCircle(arc) {
	    var circle = arc.circle;
	    if (circle) {
	      if (!circle.P) d3_geom_voronoiFirstCircle = circle.N;
	      d3_geom_voronoiCircles.remove(circle);
	      d3_geom_voronoiCirclePool.push(circle);
	      d3_geom_voronoiRedBlackNode(circle);
	      arc.circle = null;
	    }
	  }
	  function d3_geom_voronoiClipEdges(extent) {
	    var edges = d3_geom_voronoiEdges, clip = d3_geom_clipLine(extent[0][0], extent[0][1], extent[1][0], extent[1][1]), i = edges.length, e;
	    while (i--) {
	      e = edges[i];
	      if (!d3_geom_voronoiConnectEdge(e, extent) || !clip(e) || abs(e.a.x - e.b.x) < ε && abs(e.a.y - e.b.y) < ε) {
	        e.a = e.b = null;
	        edges.splice(i, 1);
	      }
	    }
	  }
	  function d3_geom_voronoiConnectEdge(edge, extent) {
	    var vb = edge.b;
	    if (vb) return true;
	    var va = edge.a, x0 = extent[0][0], x1 = extent[1][0], y0 = extent[0][1], y1 = extent[1][1], lSite = edge.l, rSite = edge.r, lx = lSite.x, ly = lSite.y, rx = rSite.x, ry = rSite.y, fx = (lx + rx) / 2, fy = (ly + ry) / 2, fm, fb;
	    if (ry === ly) {
	      if (fx < x0 || fx >= x1) return;
	      if (lx > rx) {
	        if (!va) va = {
	          x: fx,
	          y: y0
	        }; else if (va.y >= y1) return;
	        vb = {
	          x: fx,
	          y: y1
	        };
	      } else {
	        if (!va) va = {
	          x: fx,
	          y: y1
	        }; else if (va.y < y0) return;
	        vb = {
	          x: fx,
	          y: y0
	        };
	      }
	    } else {
	      fm = (lx - rx) / (ry - ly);
	      fb = fy - fm * fx;
	      if (fm < -1 || fm > 1) {
	        if (lx > rx) {
	          if (!va) va = {
	            x: (y0 - fb) / fm,
	            y: y0
	          }; else if (va.y >= y1) return;
	          vb = {
	            x: (y1 - fb) / fm,
	            y: y1
	          };
	        } else {
	          if (!va) va = {
	            x: (y1 - fb) / fm,
	            y: y1
	          }; else if (va.y < y0) return;
	          vb = {
	            x: (y0 - fb) / fm,
	            y: y0
	          };
	        }
	      } else {
	        if (ly < ry) {
	          if (!va) va = {
	            x: x0,
	            y: fm * x0 + fb
	          }; else if (va.x >= x1) return;
	          vb = {
	            x: x1,
	            y: fm * x1 + fb
	          };
	        } else {
	          if (!va) va = {
	            x: x1,
	            y: fm * x1 + fb
	          }; else if (va.x < x0) return;
	          vb = {
	            x: x0,
	            y: fm * x0 + fb
	          };
	        }
	      }
	    }
	    edge.a = va;
	    edge.b = vb;
	    return true;
	  }
	  function d3_geom_voronoiEdge(lSite, rSite) {
	    this.l = lSite;
	    this.r = rSite;
	    this.a = this.b = null;
	  }
	  function d3_geom_voronoiCreateEdge(lSite, rSite, va, vb) {
	    var edge = new d3_geom_voronoiEdge(lSite, rSite);
	    d3_geom_voronoiEdges.push(edge);
	    if (va) d3_geom_voronoiSetEdgeEnd(edge, lSite, rSite, va);
	    if (vb) d3_geom_voronoiSetEdgeEnd(edge, rSite, lSite, vb);
	    d3_geom_voronoiCells[lSite.i].edges.push(new d3_geom_voronoiHalfEdge(edge, lSite, rSite));
	    d3_geom_voronoiCells[rSite.i].edges.push(new d3_geom_voronoiHalfEdge(edge, rSite, lSite));
	    return edge;
	  }
	  function d3_geom_voronoiCreateBorderEdge(lSite, va, vb) {
	    var edge = new d3_geom_voronoiEdge(lSite, null);
	    edge.a = va;
	    edge.b = vb;
	    d3_geom_voronoiEdges.push(edge);
	    return edge;
	  }
	  function d3_geom_voronoiSetEdgeEnd(edge, lSite, rSite, vertex) {
	    if (!edge.a && !edge.b) {
	      edge.a = vertex;
	      edge.l = lSite;
	      edge.r = rSite;
	    } else if (edge.l === rSite) {
	      edge.b = vertex;
	    } else {
	      edge.a = vertex;
	    }
	  }
	  function d3_geom_voronoiHalfEdge(edge, lSite, rSite) {
	    var va = edge.a, vb = edge.b;
	    this.edge = edge;
	    this.site = lSite;
	    this.angle = rSite ? Math.atan2(rSite.y - lSite.y, rSite.x - lSite.x) : edge.l === lSite ? Math.atan2(vb.x - va.x, va.y - vb.y) : Math.atan2(va.x - vb.x, vb.y - va.y);
	  }
	  d3_geom_voronoiHalfEdge.prototype = {
	    start: function() {
	      return this.edge.l === this.site ? this.edge.a : this.edge.b;
	    },
	    end: function() {
	      return this.edge.l === this.site ? this.edge.b : this.edge.a;
	    }
	  };
	  function d3_geom_voronoiRedBlackTree() {
	    this._ = null;
	  }
	  function d3_geom_voronoiRedBlackNode(node) {
	    node.U = node.C = node.L = node.R = node.P = node.N = null;
	  }
	  d3_geom_voronoiRedBlackTree.prototype = {
	    insert: function(after, node) {
	      var parent, grandpa, uncle;
	      if (after) {
	        node.P = after;
	        node.N = after.N;
	        if (after.N) after.N.P = node;
	        after.N = node;
	        if (after.R) {
	          after = after.R;
	          while (after.L) after = after.L;
	          after.L = node;
	        } else {
	          after.R = node;
	        }
	        parent = after;
	      } else if (this._) {
	        after = d3_geom_voronoiRedBlackFirst(this._);
	        node.P = null;
	        node.N = after;
	        after.P = after.L = node;
	        parent = after;
	      } else {
	        node.P = node.N = null;
	        this._ = node;
	        parent = null;
	      }
	      node.L = node.R = null;
	      node.U = parent;
	      node.C = true;
	      after = node;
	      while (parent && parent.C) {
	        grandpa = parent.U;
	        if (parent === grandpa.L) {
	          uncle = grandpa.R;
	          if (uncle && uncle.C) {
	            parent.C = uncle.C = false;
	            grandpa.C = true;
	            after = grandpa;
	          } else {
	            if (after === parent.R) {
	              d3_geom_voronoiRedBlackRotateLeft(this, parent);
	              after = parent;
	              parent = after.U;
	            }
	            parent.C = false;
	            grandpa.C = true;
	            d3_geom_voronoiRedBlackRotateRight(this, grandpa);
	          }
	        } else {
	          uncle = grandpa.L;
	          if (uncle && uncle.C) {
	            parent.C = uncle.C = false;
	            grandpa.C = true;
	            after = grandpa;
	          } else {
	            if (after === parent.L) {
	              d3_geom_voronoiRedBlackRotateRight(this, parent);
	              after = parent;
	              parent = after.U;
	            }
	            parent.C = false;
	            grandpa.C = true;
	            d3_geom_voronoiRedBlackRotateLeft(this, grandpa);
	          }
	        }
	        parent = after.U;
	      }
	      this._.C = false;
	    },
	    remove: function(node) {
	      if (node.N) node.N.P = node.P;
	      if (node.P) node.P.N = node.N;
	      node.N = node.P = null;
	      var parent = node.U, sibling, left = node.L, right = node.R, next, red;
	      if (!left) next = right; else if (!right) next = left; else next = d3_geom_voronoiRedBlackFirst(right);
	      if (parent) {
	        if (parent.L === node) parent.L = next; else parent.R = next;
	      } else {
	        this._ = next;
	      }
	      if (left && right) {
	        red = next.C;
	        next.C = node.C;
	        next.L = left;
	        left.U = next;
	        if (next !== right) {
	          parent = next.U;
	          next.U = node.U;
	          node = next.R;
	          parent.L = node;
	          next.R = right;
	          right.U = next;
	        } else {
	          next.U = parent;
	          parent = next;
	          node = next.R;
	        }
	      } else {
	        red = node.C;
	        node = next;
	      }
	      if (node) node.U = parent;
	      if (red) return;
	      if (node && node.C) {
	        node.C = false;
	        return;
	      }
	      do {
	        if (node === this._) break;
	        if (node === parent.L) {
	          sibling = parent.R;
	          if (sibling.C) {
	            sibling.C = false;
	            parent.C = true;
	            d3_geom_voronoiRedBlackRotateLeft(this, parent);
	            sibling = parent.R;
	          }
	          if (sibling.L && sibling.L.C || sibling.R && sibling.R.C) {
	            if (!sibling.R || !sibling.R.C) {
	              sibling.L.C = false;
	              sibling.C = true;
	              d3_geom_voronoiRedBlackRotateRight(this, sibling);
	              sibling = parent.R;
	            }
	            sibling.C = parent.C;
	            parent.C = sibling.R.C = false;
	            d3_geom_voronoiRedBlackRotateLeft(this, parent);
	            node = this._;
	            break;
	          }
	        } else {
	          sibling = parent.L;
	          if (sibling.C) {
	            sibling.C = false;
	            parent.C = true;
	            d3_geom_voronoiRedBlackRotateRight(this, parent);
	            sibling = parent.L;
	          }
	          if (sibling.L && sibling.L.C || sibling.R && sibling.R.C) {
	            if (!sibling.L || !sibling.L.C) {
	              sibling.R.C = false;
	              sibling.C = true;
	              d3_geom_voronoiRedBlackRotateLeft(this, sibling);
	              sibling = parent.L;
	            }
	            sibling.C = parent.C;
	            parent.C = sibling.L.C = false;
	            d3_geom_voronoiRedBlackRotateRight(this, parent);
	            node = this._;
	            break;
	          }
	        }
	        sibling.C = true;
	        node = parent;
	        parent = parent.U;
	      } while (!node.C);
	      if (node) node.C = false;
	    }
	  };
	  function d3_geom_voronoiRedBlackRotateLeft(tree, node) {
	    var p = node, q = node.R, parent = p.U;
	    if (parent) {
	      if (parent.L === p) parent.L = q; else parent.R = q;
	    } else {
	      tree._ = q;
	    }
	    q.U = parent;
	    p.U = q;
	    p.R = q.L;
	    if (p.R) p.R.U = p;
	    q.L = p;
	  }
	  function d3_geom_voronoiRedBlackRotateRight(tree, node) {
	    var p = node, q = node.L, parent = p.U;
	    if (parent) {
	      if (parent.L === p) parent.L = q; else parent.R = q;
	    } else {
	      tree._ = q;
	    }
	    q.U = parent;
	    p.U = q;
	    p.L = q.R;
	    if (p.L) p.L.U = p;
	    q.R = p;
	  }
	  function d3_geom_voronoiRedBlackFirst(node) {
	    while (node.L) node = node.L;
	    return node;
	  }
	  function d3_geom_voronoi(sites, bbox) {
	    var site = sites.sort(d3_geom_voronoiVertexOrder).pop(), x0, y0, circle;
	    d3_geom_voronoiEdges = [];
	    d3_geom_voronoiCells = new Array(sites.length);
	    d3_geom_voronoiBeaches = new d3_geom_voronoiRedBlackTree();
	    d3_geom_voronoiCircles = new d3_geom_voronoiRedBlackTree();
	    while (true) {
	      circle = d3_geom_voronoiFirstCircle;
	      if (site && (!circle || site.y < circle.y || site.y === circle.y && site.x < circle.x)) {
	        if (site.x !== x0 || site.y !== y0) {
	          d3_geom_voronoiCells[site.i] = new d3_geom_voronoiCell(site);
	          d3_geom_voronoiAddBeach(site);
	          x0 = site.x, y0 = site.y;
	        }
	        site = sites.pop();
	      } else if (circle) {
	        d3_geom_voronoiRemoveBeach(circle.arc);
	      } else {
	        break;
	      }
	    }
	    if (bbox) d3_geom_voronoiClipEdges(bbox), d3_geom_voronoiCloseCells(bbox);
	    var diagram = {
	      cells: d3_geom_voronoiCells,
	      edges: d3_geom_voronoiEdges
	    };
	    d3_geom_voronoiBeaches = d3_geom_voronoiCircles = d3_geom_voronoiEdges = d3_geom_voronoiCells = null;
	    return diagram;
	  }
	  function d3_geom_voronoiVertexOrder(a, b) {
	    return b.y - a.y || b.x - a.x;
	  }
	  d3.geom.voronoi = function(points) {
	    var x = d3_geom_pointX, y = d3_geom_pointY, fx = x, fy = y, clipExtent = d3_geom_voronoiClipExtent;
	    if (points) return voronoi(points);
	    function voronoi(data) {
	      var polygons = new Array(data.length), x0 = clipExtent[0][0], y0 = clipExtent[0][1], x1 = clipExtent[1][0], y1 = clipExtent[1][1];
	      d3_geom_voronoi(sites(data), clipExtent).cells.forEach(function(cell, i) {
	        var edges = cell.edges, site = cell.site, polygon = polygons[i] = edges.length ? edges.map(function(e) {
	          var s = e.start();
	          return [ s.x, s.y ];
	        }) : site.x >= x0 && site.x <= x1 && site.y >= y0 && site.y <= y1 ? [ [ x0, y1 ], [ x1, y1 ], [ x1, y0 ], [ x0, y0 ] ] : [];
	        polygon.point = data[i];
	      });
	      return polygons;
	    }
	    function sites(data) {
	      return data.map(function(d, i) {
	        return {
	          x: Math.round(fx(d, i) / ε) * ε,
	          y: Math.round(fy(d, i) / ε) * ε,
	          i: i
	        };
	      });
	    }
	    voronoi.links = function(data) {
	      return d3_geom_voronoi(sites(data)).edges.filter(function(edge) {
	        return edge.l && edge.r;
	      }).map(function(edge) {
	        return {
	          source: data[edge.l.i],
	          target: data[edge.r.i]
	        };
	      });
	    };
	    voronoi.triangles = function(data) {
	      var triangles = [];
	      d3_geom_voronoi(sites(data)).cells.forEach(function(cell, i) {
	        var site = cell.site, edges = cell.edges.sort(d3_geom_voronoiHalfEdgeOrder), j = -1, m = edges.length, e0, s0, e1 = edges[m - 1].edge, s1 = e1.l === site ? e1.r : e1.l;
	        while (++j < m) {
	          e0 = e1;
	          s0 = s1;
	          e1 = edges[j].edge;
	          s1 = e1.l === site ? e1.r : e1.l;
	          if (i < s0.i && i < s1.i && d3_geom_voronoiTriangleArea(site, s0, s1) < 0) {
	            triangles.push([ data[i], data[s0.i], data[s1.i] ]);
	          }
	        }
	      });
	      return triangles;
	    };
	    voronoi.x = function(_) {
	      return arguments.length ? (fx = d3_functor(x = _), voronoi) : x;
	    };
	    voronoi.y = function(_) {
	      return arguments.length ? (fy = d3_functor(y = _), voronoi) : y;
	    };
	    voronoi.clipExtent = function(_) {
	      if (!arguments.length) return clipExtent === d3_geom_voronoiClipExtent ? null : clipExtent;
	      clipExtent = _ == null ? d3_geom_voronoiClipExtent : _;
	      return voronoi;
	    };
	    voronoi.size = function(_) {
	      if (!arguments.length) return clipExtent === d3_geom_voronoiClipExtent ? null : clipExtent && clipExtent[1];
	      return voronoi.clipExtent(_ && [ [ 0, 0 ], _ ]);
	    };
	    return voronoi;
	  };
	  var d3_geom_voronoiClipExtent = [ [ -1e6, -1e6 ], [ 1e6, 1e6 ] ];
	  function d3_geom_voronoiTriangleArea(a, b, c) {
	    return (a.x - c.x) * (b.y - a.y) - (a.x - b.x) * (c.y - a.y);
	  }
	  d3.geom.delaunay = function(vertices) {
	    return d3.geom.voronoi().triangles(vertices);
	  };
	  d3.geom.quadtree = function(points, x1, y1, x2, y2) {
	    var x = d3_geom_pointX, y = d3_geom_pointY, compat;
	    if (compat = arguments.length) {
	      x = d3_geom_quadtreeCompatX;
	      y = d3_geom_quadtreeCompatY;
	      if (compat === 3) {
	        y2 = y1;
	        x2 = x1;
	        y1 = x1 = 0;
	      }
	      return quadtree(points);
	    }
	    function quadtree(data) {
	      var d, fx = d3_functor(x), fy = d3_functor(y), xs, ys, i, n, x1_, y1_, x2_, y2_;
	      if (x1 != null) {
	        x1_ = x1, y1_ = y1, x2_ = x2, y2_ = y2;
	      } else {
	        x2_ = y2_ = -(x1_ = y1_ = Infinity);
	        xs = [], ys = [];
	        n = data.length;
	        if (compat) for (i = 0; i < n; ++i) {
	          d = data[i];
	          if (d.x < x1_) x1_ = d.x;
	          if (d.y < y1_) y1_ = d.y;
	          if (d.x > x2_) x2_ = d.x;
	          if (d.y > y2_) y2_ = d.y;
	          xs.push(d.x);
	          ys.push(d.y);
	        } else for (i = 0; i < n; ++i) {
	          var x_ = +fx(d = data[i], i), y_ = +fy(d, i);
	          if (x_ < x1_) x1_ = x_;
	          if (y_ < y1_) y1_ = y_;
	          if (x_ > x2_) x2_ = x_;
	          if (y_ > y2_) y2_ = y_;
	          xs.push(x_);
	          ys.push(y_);
	        }
	      }
	      var dx = x2_ - x1_, dy = y2_ - y1_;
	      if (dx > dy) y2_ = y1_ + dx; else x2_ = x1_ + dy;
	      function insert(n, d, x, y, x1, y1, x2, y2) {
	        if (isNaN(x) || isNaN(y)) return;
	        if (n.leaf) {
	          var nx = n.x, ny = n.y;
	          if (nx != null) {
	            if (abs(nx - x) + abs(ny - y) < .01) {
	              insertChild(n, d, x, y, x1, y1, x2, y2);
	            } else {
	              var nPoint = n.point;
	              n.x = n.y = n.point = null;
	              insertChild(n, nPoint, nx, ny, x1, y1, x2, y2);
	              insertChild(n, d, x, y, x1, y1, x2, y2);
	            }
	          } else {
	            n.x = x, n.y = y, n.point = d;
	          }
	        } else {
	          insertChild(n, d, x, y, x1, y1, x2, y2);
	        }
	      }
	      function insertChild(n, d, x, y, x1, y1, x2, y2) {
	        var xm = (x1 + x2) * .5, ym = (y1 + y2) * .5, right = x >= xm, below = y >= ym, i = below << 1 | right;
	        n.leaf = false;
	        n = n.nodes[i] || (n.nodes[i] = d3_geom_quadtreeNode());
	        if (right) x1 = xm; else x2 = xm;
	        if (below) y1 = ym; else y2 = ym;
	        insert(n, d, x, y, x1, y1, x2, y2);
	      }
	      var root = d3_geom_quadtreeNode();
	      root.add = function(d) {
	        insert(root, d, +fx(d, ++i), +fy(d, i), x1_, y1_, x2_, y2_);
	      };
	      root.visit = function(f) {
	        d3_geom_quadtreeVisit(f, root, x1_, y1_, x2_, y2_);
	      };
	      root.find = function(point) {
	        return d3_geom_quadtreeFind(root, point[0], point[1], x1_, y1_, x2_, y2_);
	      };
	      i = -1;
	      if (x1 == null) {
	        while (++i < n) {
	          insert(root, data[i], xs[i], ys[i], x1_, y1_, x2_, y2_);
	        }
	        --i;
	      } else data.forEach(root.add);
	      xs = ys = data = d = null;
	      return root;
	    }
	    quadtree.x = function(_) {
	      return arguments.length ? (x = _, quadtree) : x;
	    };
	    quadtree.y = function(_) {
	      return arguments.length ? (y = _, quadtree) : y;
	    };
	    quadtree.extent = function(_) {
	      if (!arguments.length) return x1 == null ? null : [ [ x1, y1 ], [ x2, y2 ] ];
	      if (_ == null) x1 = y1 = x2 = y2 = null; else x1 = +_[0][0], y1 = +_[0][1], x2 = +_[1][0], 
	      y2 = +_[1][1];
	      return quadtree;
	    };
	    quadtree.size = function(_) {
	      if (!arguments.length) return x1 == null ? null : [ x2 - x1, y2 - y1 ];
	      if (_ == null) x1 = y1 = x2 = y2 = null; else x1 = y1 = 0, x2 = +_[0], y2 = +_[1];
	      return quadtree;
	    };
	    return quadtree;
	  };
	  function d3_geom_quadtreeCompatX(d) {
	    return d.x;
	  }
	  function d3_geom_quadtreeCompatY(d) {
	    return d.y;
	  }
	  function d3_geom_quadtreeNode() {
	    return {
	      leaf: true,
	      nodes: [],
	      point: null,
	      x: null,
	      y: null
	    };
	  }
	  function d3_geom_quadtreeVisit(f, node, x1, y1, x2, y2) {
	    if (!f(node, x1, y1, x2, y2)) {
	      var sx = (x1 + x2) * .5, sy = (y1 + y2) * .5, children = node.nodes;
	      if (children[0]) d3_geom_quadtreeVisit(f, children[0], x1, y1, sx, sy);
	      if (children[1]) d3_geom_quadtreeVisit(f, children[1], sx, y1, x2, sy);
	      if (children[2]) d3_geom_quadtreeVisit(f, children[2], x1, sy, sx, y2);
	      if (children[3]) d3_geom_quadtreeVisit(f, children[3], sx, sy, x2, y2);
	    }
	  }
	  function d3_geom_quadtreeFind(root, x, y, x0, y0, x3, y3) {
	    var minDistance2 = Infinity, closestPoint;
	    (function find(node, x1, y1, x2, y2) {
	      if (x1 > x3 || y1 > y3 || x2 < x0 || y2 < y0) return;
	      if (point = node.point) {
	        var point, dx = x - node.x, dy = y - node.y, distance2 = dx * dx + dy * dy;
	        if (distance2 < minDistance2) {
	          var distance = Math.sqrt(minDistance2 = distance2);
	          x0 = x - distance, y0 = y - distance;
	          x3 = x + distance, y3 = y + distance;
	          closestPoint = point;
	        }
	      }
	      var children = node.nodes, xm = (x1 + x2) * .5, ym = (y1 + y2) * .5, right = x >= xm, below = y >= ym;
	      for (var i = below << 1 | right, j = i + 4; i < j; ++i) {
	        if (node = children[i & 3]) switch (i & 3) {
	         case 0:
	          find(node, x1, y1, xm, ym);
	          break;

	         case 1:
	          find(node, xm, y1, x2, ym);
	          break;

	         case 2:
	          find(node, x1, ym, xm, y2);
	          break;

	         case 3:
	          find(node, xm, ym, x2, y2);
	          break;
	        }
	      }
	    })(root, x0, y0, x3, y3);
	    return closestPoint;
	  }
	  d3.interpolateRgb = d3_interpolateRgb;
	  function d3_interpolateRgb(a, b) {
	    a = d3.rgb(a);
	    b = d3.rgb(b);
	    var ar = a.r, ag = a.g, ab = a.b, br = b.r - ar, bg = b.g - ag, bb = b.b - ab;
	    return function(t) {
	      return "#" + d3_rgb_hex(Math.round(ar + br * t)) + d3_rgb_hex(Math.round(ag + bg * t)) + d3_rgb_hex(Math.round(ab + bb * t));
	    };
	  }
	  d3.interpolateObject = d3_interpolateObject;
	  function d3_interpolateObject(a, b) {
	    var i = {}, c = {}, k;
	    for (k in a) {
	      if (k in b) {
	        i[k] = d3_interpolate(a[k], b[k]);
	      } else {
	        c[k] = a[k];
	      }
	    }
	    for (k in b) {
	      if (!(k in a)) {
	        c[k] = b[k];
	      }
	    }
	    return function(t) {
	      for (k in i) c[k] = i[k](t);
	      return c;
	    };
	  }
	  d3.interpolateNumber = d3_interpolateNumber;
	  function d3_interpolateNumber(a, b) {
	    a = +a, b = +b;
	    return function(t) {
	      return a * (1 - t) + b * t;
	    };
	  }
	  d3.interpolateString = d3_interpolateString;
	  function d3_interpolateString(a, b) {
	    var bi = d3_interpolate_numberA.lastIndex = d3_interpolate_numberB.lastIndex = 0, am, bm, bs, i = -1, s = [], q = [];
	    a = a + "", b = b + "";
	    while ((am = d3_interpolate_numberA.exec(a)) && (bm = d3_interpolate_numberB.exec(b))) {
	      if ((bs = bm.index) > bi) {
	        bs = b.slice(bi, bs);
	        if (s[i]) s[i] += bs; else s[++i] = bs;
	      }
	      if ((am = am[0]) === (bm = bm[0])) {
	        if (s[i]) s[i] += bm; else s[++i] = bm;
	      } else {
	        s[++i] = null;
	        q.push({
	          i: i,
	          x: d3_interpolateNumber(am, bm)
	        });
	      }
	      bi = d3_interpolate_numberB.lastIndex;
	    }
	    if (bi < b.length) {
	      bs = b.slice(bi);
	      if (s[i]) s[i] += bs; else s[++i] = bs;
	    }
	    return s.length < 2 ? q[0] ? (b = q[0].x, function(t) {
	      return b(t) + "";
	    }) : function() {
	      return b;
	    } : (b = q.length, function(t) {
	      for (var i = 0, o; i < b; ++i) s[(o = q[i]).i] = o.x(t);
	      return s.join("");
	    });
	  }
	  var d3_interpolate_numberA = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g, d3_interpolate_numberB = new RegExp(d3_interpolate_numberA.source, "g");
	  d3.interpolate = d3_interpolate;
	  function d3_interpolate(a, b) {
	    var i = d3.interpolators.length, f;
	    while (--i >= 0 && !(f = d3.interpolators[i](a, b))) ;
	    return f;
	  }
	  d3.interpolators = [ function(a, b) {
	    var t = typeof b;
	    return (t === "string" ? d3_rgb_names.has(b.toLowerCase()) || /^(#|rgb\(|hsl\()/i.test(b) ? d3_interpolateRgb : d3_interpolateString : b instanceof d3_color ? d3_interpolateRgb : Array.isArray(b) ? d3_interpolateArray : t === "object" && isNaN(b) ? d3_interpolateObject : d3_interpolateNumber)(a, b);
	  } ];
	  d3.interpolateArray = d3_interpolateArray;
	  function d3_interpolateArray(a, b) {
	    var x = [], c = [], na = a.length, nb = b.length, n0 = Math.min(a.length, b.length), i;
	    for (i = 0; i < n0; ++i) x.push(d3_interpolate(a[i], b[i]));
	    for (;i < na; ++i) c[i] = a[i];
	    for (;i < nb; ++i) c[i] = b[i];
	    return function(t) {
	      for (i = 0; i < n0; ++i) c[i] = x[i](t);
	      return c;
	    };
	  }
	  var d3_ease_default = function() {
	    return d3_identity;
	  };
	  var d3_ease = d3.map({
	    linear: d3_ease_default,
	    poly: d3_ease_poly,
	    quad: function() {
	      return d3_ease_quad;
	    },
	    cubic: function() {
	      return d3_ease_cubic;
	    },
	    sin: function() {
	      return d3_ease_sin;
	    },
	    exp: function() {
	      return d3_ease_exp;
	    },
	    circle: function() {
	      return d3_ease_circle;
	    },
	    elastic: d3_ease_elastic,
	    back: d3_ease_back,
	    bounce: function() {
	      return d3_ease_bounce;
	    }
	  });
	  var d3_ease_mode = d3.map({
	    "in": d3_identity,
	    out: d3_ease_reverse,
	    "in-out": d3_ease_reflect,
	    "out-in": function(f) {
	      return d3_ease_reflect(d3_ease_reverse(f));
	    }
	  });
	  d3.ease = function(name) {
	    var i = name.indexOf("-"), t = i >= 0 ? name.slice(0, i) : name, m = i >= 0 ? name.slice(i + 1) : "in";
	    t = d3_ease.get(t) || d3_ease_default;
	    m = d3_ease_mode.get(m) || d3_identity;
	    return d3_ease_clamp(m(t.apply(null, d3_arraySlice.call(arguments, 1))));
	  };
	  function d3_ease_clamp(f) {
	    return function(t) {
	      return t <= 0 ? 0 : t >= 1 ? 1 : f(t);
	    };
	  }
	  function d3_ease_reverse(f) {
	    return function(t) {
	      return 1 - f(1 - t);
	    };
	  }
	  function d3_ease_reflect(f) {
	    return function(t) {
	      return .5 * (t < .5 ? f(2 * t) : 2 - f(2 - 2 * t));
	    };
	  }
	  function d3_ease_quad(t) {
	    return t * t;
	  }
	  function d3_ease_cubic(t) {
	    return t * t * t;
	  }
	  function d3_ease_cubicInOut(t) {
	    if (t <= 0) return 0;
	    if (t >= 1) return 1;
	    var t2 = t * t, t3 = t2 * t;
	    return 4 * (t < .5 ? t3 : 3 * (t - t2) + t3 - .75);
	  }
	  function d3_ease_poly(e) {
	    return function(t) {
	      return Math.pow(t, e);
	    };
	  }
	  function d3_ease_sin(t) {
	    return 1 - Math.cos(t * halfπ);
	  }
	  function d3_ease_exp(t) {
	    return Math.pow(2, 10 * (t - 1));
	  }
	  function d3_ease_circle(t) {
	    return 1 - Math.sqrt(1 - t * t);
	  }
	  function d3_ease_elastic(a, p) {
	    var s;
	    if (arguments.length < 2) p = .45;
	    if (arguments.length) s = p / τ * Math.asin(1 / a); else a = 1, s = p / 4;
	    return function(t) {
	      return 1 + a * Math.pow(2, -10 * t) * Math.sin((t - s) * τ / p);
	    };
	  }
	  function d3_ease_back(s) {
	    if (!s) s = 1.70158;
	    return function(t) {
	      return t * t * ((s + 1) * t - s);
	    };
	  }
	  function d3_ease_bounce(t) {
	    return t < 1 / 2.75 ? 7.5625 * t * t : t < 2 / 2.75 ? 7.5625 * (t -= 1.5 / 2.75) * t + .75 : t < 2.5 / 2.75 ? 7.5625 * (t -= 2.25 / 2.75) * t + .9375 : 7.5625 * (t -= 2.625 / 2.75) * t + .984375;
	  }
	  d3.interpolateHcl = d3_interpolateHcl;
	  function d3_interpolateHcl(a, b) {
	    a = d3.hcl(a);
	    b = d3.hcl(b);
	    var ah = a.h, ac = a.c, al = a.l, bh = b.h - ah, bc = b.c - ac, bl = b.l - al;
	    if (isNaN(bc)) bc = 0, ac = isNaN(ac) ? b.c : ac;
	    if (isNaN(bh)) bh = 0, ah = isNaN(ah) ? b.h : ah; else if (bh > 180) bh -= 360; else if (bh < -180) bh += 360;
	    return function(t) {
	      return d3_hcl_lab(ah + bh * t, ac + bc * t, al + bl * t) + "";
	    };
	  }
	  d3.interpolateHsl = d3_interpolateHsl;
	  function d3_interpolateHsl(a, b) {
	    a = d3.hsl(a);
	    b = d3.hsl(b);
	    var ah = a.h, as = a.s, al = a.l, bh = b.h - ah, bs = b.s - as, bl = b.l - al;
	    if (isNaN(bs)) bs = 0, as = isNaN(as) ? b.s : as;
	    if (isNaN(bh)) bh = 0, ah = isNaN(ah) ? b.h : ah; else if (bh > 180) bh -= 360; else if (bh < -180) bh += 360;
	    return function(t) {
	      return d3_hsl_rgb(ah + bh * t, as + bs * t, al + bl * t) + "";
	    };
	  }
	  d3.interpolateLab = d3_interpolateLab;
	  function d3_interpolateLab(a, b) {
	    a = d3.lab(a);
	    b = d3.lab(b);
	    var al = a.l, aa = a.a, ab = a.b, bl = b.l - al, ba = b.a - aa, bb = b.b - ab;
	    return function(t) {
	      return d3_lab_rgb(al + bl * t, aa + ba * t, ab + bb * t) + "";
	    };
	  }
	  d3.interpolateRound = d3_interpolateRound;
	  function d3_interpolateRound(a, b) {
	    b -= a;
	    return function(t) {
	      return Math.round(a + b * t);
	    };
	  }
	  d3.transform = function(string) {
	    var g = d3_document.createElementNS(d3.ns.prefix.svg, "g");
	    return (d3.transform = function(string) {
	      if (string != null) {
	        g.setAttribute("transform", string);
	        var t = g.transform.baseVal.consolidate();
	      }
	      return new d3_transform(t ? t.matrix : d3_transformIdentity);
	    })(string);
	  };
	  function d3_transform(m) {
	    var r0 = [ m.a, m.b ], r1 = [ m.c, m.d ], kx = d3_transformNormalize(r0), kz = d3_transformDot(r0, r1), ky = d3_transformNormalize(d3_transformCombine(r1, r0, -kz)) || 0;
	    if (r0[0] * r1[1] < r1[0] * r0[1]) {
	      r0[0] *= -1;
	      r0[1] *= -1;
	      kx *= -1;
	      kz *= -1;
	    }
	    this.rotate = (kx ? Math.atan2(r0[1], r0[0]) : Math.atan2(-r1[0], r1[1])) * d3_degrees;
	    this.translate = [ m.e, m.f ];
	    this.scale = [ kx, ky ];
	    this.skew = ky ? Math.atan2(kz, ky) * d3_degrees : 0;
	  }
	  d3_transform.prototype.toString = function() {
	    return "translate(" + this.translate + ")rotate(" + this.rotate + ")skewX(" + this.skew + ")scale(" + this.scale + ")";
	  };
	  function d3_transformDot(a, b) {
	    return a[0] * b[0] + a[1] * b[1];
	  }
	  function d3_transformNormalize(a) {
	    var k = Math.sqrt(d3_transformDot(a, a));
	    if (k) {
	      a[0] /= k;
	      a[1] /= k;
	    }
	    return k;
	  }
	  function d3_transformCombine(a, b, k) {
	    a[0] += k * b[0];
	    a[1] += k * b[1];
	    return a;
	  }
	  var d3_transformIdentity = {
	    a: 1,
	    b: 0,
	    c: 0,
	    d: 1,
	    e: 0,
	    f: 0
	  };
	  d3.interpolateTransform = d3_interpolateTransform;
	  function d3_interpolateTransformPop(s) {
	    return s.length ? s.pop() + "," : "";
	  }
	  function d3_interpolateTranslate(ta, tb, s, q) {
	    if (ta[0] !== tb[0] || ta[1] !== tb[1]) {
	      var i = s.push("translate(", null, ",", null, ")");
	      q.push({
	        i: i - 4,
	        x: d3_interpolateNumber(ta[0], tb[0])
	      }, {
	        i: i - 2,
	        x: d3_interpolateNumber(ta[1], tb[1])
	      });
	    } else if (tb[0] || tb[1]) {
	      s.push("translate(" + tb + ")");
	    }
	  }
	  function d3_interpolateRotate(ra, rb, s, q) {
	    if (ra !== rb) {
	      if (ra - rb > 180) rb += 360; else if (rb - ra > 180) ra += 360;
	      q.push({
	        i: s.push(d3_interpolateTransformPop(s) + "rotate(", null, ")") - 2,
	        x: d3_interpolateNumber(ra, rb)
	      });
	    } else if (rb) {
	      s.push(d3_interpolateTransformPop(s) + "rotate(" + rb + ")");
	    }
	  }
	  function d3_interpolateSkew(wa, wb, s, q) {
	    if (wa !== wb) {
	      q.push({
	        i: s.push(d3_interpolateTransformPop(s) + "skewX(", null, ")") - 2,
	        x: d3_interpolateNumber(wa, wb)
	      });
	    } else if (wb) {
	      s.push(d3_interpolateTransformPop(s) + "skewX(" + wb + ")");
	    }
	  }
	  function d3_interpolateScale(ka, kb, s, q) {
	    if (ka[0] !== kb[0] || ka[1] !== kb[1]) {
	      var i = s.push(d3_interpolateTransformPop(s) + "scale(", null, ",", null, ")");
	      q.push({
	        i: i - 4,
	        x: d3_interpolateNumber(ka[0], kb[0])
	      }, {
	        i: i - 2,
	        x: d3_interpolateNumber(ka[1], kb[1])
	      });
	    } else if (kb[0] !== 1 || kb[1] !== 1) {
	      s.push(d3_interpolateTransformPop(s) + "scale(" + kb + ")");
	    }
	  }
	  function d3_interpolateTransform(a, b) {
	    var s = [], q = [];
	    a = d3.transform(a), b = d3.transform(b);
	    d3_interpolateTranslate(a.translate, b.translate, s, q);
	    d3_interpolateRotate(a.rotate, b.rotate, s, q);
	    d3_interpolateSkew(a.skew, b.skew, s, q);
	    d3_interpolateScale(a.scale, b.scale, s, q);
	    a = b = null;
	    return function(t) {
	      var i = -1, n = q.length, o;
	      while (++i < n) s[(o = q[i]).i] = o.x(t);
	      return s.join("");
	    };
	  }
	  function d3_uninterpolateNumber(a, b) {
	    b = (b -= a = +a) || 1 / b;
	    return function(x) {
	      return (x - a) / b;
	    };
	  }
	  function d3_uninterpolateClamp(a, b) {
	    b = (b -= a = +a) || 1 / b;
	    return function(x) {
	      return Math.max(0, Math.min(1, (x - a) / b));
	    };
	  }
	  d3.layout = {};
	  d3.layout.bundle = function() {
	    return function(links) {
	      var paths = [], i = -1, n = links.length;
	      while (++i < n) paths.push(d3_layout_bundlePath(links[i]));
	      return paths;
	    };
	  };
	  function d3_layout_bundlePath(link) {
	    var start = link.source, end = link.target, lca = d3_layout_bundleLeastCommonAncestor(start, end), points = [ start ];
	    while (start !== lca) {
	      start = start.parent;
	      points.push(start);
	    }
	    var k = points.length;
	    while (end !== lca) {
	      points.splice(k, 0, end);
	      end = end.parent;
	    }
	    return points;
	  }
	  function d3_layout_bundleAncestors(node) {
	    var ancestors = [], parent = node.parent;
	    while (parent != null) {
	      ancestors.push(node);
	      node = parent;
	      parent = parent.parent;
	    }
	    ancestors.push(node);
	    return ancestors;
	  }
	  function d3_layout_bundleLeastCommonAncestor(a, b) {
	    if (a === b) return a;
	    var aNodes = d3_layout_bundleAncestors(a), bNodes = d3_layout_bundleAncestors(b), aNode = aNodes.pop(), bNode = bNodes.pop(), sharedNode = null;
	    while (aNode === bNode) {
	      sharedNode = aNode;
	      aNode = aNodes.pop();
	      bNode = bNodes.pop();
	    }
	    return sharedNode;
	  }
	  d3.layout.chord = function() {
	    var chord = {}, chords, groups, matrix, n, padding = 0, sortGroups, sortSubgroups, sortChords;
	    function relayout() {
	      var subgroups = {}, groupSums = [], groupIndex = d3.range(n), subgroupIndex = [], k, x, x0, i, j;
	      chords = [];
	      groups = [];
	      k = 0, i = -1;
	      while (++i < n) {
	        x = 0, j = -1;
	        while (++j < n) {
	          x += matrix[i][j];
	        }
	        groupSums.push(x);
	        subgroupIndex.push(d3.range(n));
	        k += x;
	      }
	      if (sortGroups) {
	        groupIndex.sort(function(a, b) {
	          return sortGroups(groupSums[a], groupSums[b]);
	        });
	      }
	      if (sortSubgroups) {
	        subgroupIndex.forEach(function(d, i) {
	          d.sort(function(a, b) {
	            return sortSubgroups(matrix[i][a], matrix[i][b]);
	          });
	        });
	      }
	      k = (τ - padding * n) / k;
	      x = 0, i = -1;
	      while (++i < n) {
	        x0 = x, j = -1;
	        while (++j < n) {
	          var di = groupIndex[i], dj = subgroupIndex[di][j], v = matrix[di][dj], a0 = x, a1 = x += v * k;
	          subgroups[di + "-" + dj] = {
	            index: di,
	            subindex: dj,
	            startAngle: a0,
	            endAngle: a1,
	            value: v
	          };
	        }
	        groups[di] = {
	          index: di,
	          startAngle: x0,
	          endAngle: x,
	          value: groupSums[di]
	        };
	        x += padding;
	      }
	      i = -1;
	      while (++i < n) {
	        j = i - 1;
	        while (++j < n) {
	          var source = subgroups[i + "-" + j], target = subgroups[j + "-" + i];
	          if (source.value || target.value) {
	            chords.push(source.value < target.value ? {
	              source: target,
	              target: source
	            } : {
	              source: source,
	              target: target
	            });
	          }
	        }
	      }
	      if (sortChords) resort();
	    }
	    function resort() {
	      chords.sort(function(a, b) {
	        return sortChords((a.source.value + a.target.value) / 2, (b.source.value + b.target.value) / 2);
	      });
	    }
	    chord.matrix = function(x) {
	      if (!arguments.length) return matrix;
	      n = (matrix = x) && matrix.length;
	      chords = groups = null;
	      return chord;
	    };
	    chord.padding = function(x) {
	      if (!arguments.length) return padding;
	      padding = x;
	      chords = groups = null;
	      return chord;
	    };
	    chord.sortGroups = function(x) {
	      if (!arguments.length) return sortGroups;
	      sortGroups = x;
	      chords = groups = null;
	      return chord;
	    };
	    chord.sortSubgroups = function(x) {
	      if (!arguments.length) return sortSubgroups;
	      sortSubgroups = x;
	      chords = null;
	      return chord;
	    };
	    chord.sortChords = function(x) {
	      if (!arguments.length) return sortChords;
	      sortChords = x;
	      if (chords) resort();
	      return chord;
	    };
	    chord.chords = function() {
	      if (!chords) relayout();
	      return chords;
	    };
	    chord.groups = function() {
	      if (!groups) relayout();
	      return groups;
	    };
	    return chord;
	  };
	  d3.layout.force = function() {
	    var force = {}, event = d3.dispatch("start", "tick", "end"), timer, size = [ 1, 1 ], drag, alpha, friction = .9, linkDistance = d3_layout_forceLinkDistance, linkStrength = d3_layout_forceLinkStrength, charge = -30, chargeDistance2 = d3_layout_forceChargeDistance2, gravity = .1, theta2 = .64, nodes = [], links = [], distances, strengths, charges;
	    function repulse(node) {
	      return function(quad, x1, _, x2) {
	        if (quad.point !== node) {
	          var dx = quad.cx - node.x, dy = quad.cy - node.y, dw = x2 - x1, dn = dx * dx + dy * dy;
	          if (dw * dw / theta2 < dn) {
	            if (dn < chargeDistance2) {
	              var k = quad.charge / dn;
	              node.px -= dx * k;
	              node.py -= dy * k;
	            }
	            return true;
	          }
	          if (quad.point && dn && dn < chargeDistance2) {
	            var k = quad.pointCharge / dn;
	            node.px -= dx * k;
	            node.py -= dy * k;
	          }
	        }
	        return !quad.charge;
	      };
	    }
	    force.tick = function() {
	      if ((alpha *= .99) < .005) {
	        timer = null;
	        event.end({
	          type: "end",
	          alpha: alpha = 0
	        });
	        return true;
	      }
	      var n = nodes.length, m = links.length, q, i, o, s, t, l, k, x, y;
	      for (i = 0; i < m; ++i) {
	        o = links[i];
	        s = o.source;
	        t = o.target;
	        x = t.x - s.x;
	        y = t.y - s.y;
	        if (l = x * x + y * y) {
	          l = alpha * strengths[i] * ((l = Math.sqrt(l)) - distances[i]) / l;
	          x *= l;
	          y *= l;
	          t.x -= x * (k = s.weight + t.weight ? s.weight / (s.weight + t.weight) : .5);
	          t.y -= y * k;
	          s.x += x * (k = 1 - k);
	          s.y += y * k;
	        }
	      }
	      if (k = alpha * gravity) {
	        x = size[0] / 2;
	        y = size[1] / 2;
	        i = -1;
	        if (k) while (++i < n) {
	          o = nodes[i];
	          o.x += (x - o.x) * k;
	          o.y += (y - o.y) * k;
	        }
	      }
	      if (charge) {
	        d3_layout_forceAccumulate(q = d3.geom.quadtree(nodes), alpha, charges);
	        i = -1;
	        while (++i < n) {
	          if (!(o = nodes[i]).fixed) {
	            q.visit(repulse(o));
	          }
	        }
	      }
	      i = -1;
	      while (++i < n) {
	        o = nodes[i];
	        if (o.fixed) {
	          o.x = o.px;
	          o.y = o.py;
	        } else {
	          o.x -= (o.px - (o.px = o.x)) * friction;
	          o.y -= (o.py - (o.py = o.y)) * friction;
	        }
	      }
	      event.tick({
	        type: "tick",
	        alpha: alpha
	      });
	    };
	    force.nodes = function(x) {
	      if (!arguments.length) return nodes;
	      nodes = x;
	      return force;
	    };
	    force.links = function(x) {
	      if (!arguments.length) return links;
	      links = x;
	      return force;
	    };
	    force.size = function(x) {
	      if (!arguments.length) return size;
	      size = x;
	      return force;
	    };
	    force.linkDistance = function(x) {
	      if (!arguments.length) return linkDistance;
	      linkDistance = typeof x === "function" ? x : +x;
	      return force;
	    };
	    force.distance = force.linkDistance;
	    force.linkStrength = function(x) {
	      if (!arguments.length) return linkStrength;
	      linkStrength = typeof x === "function" ? x : +x;
	      return force;
	    };
	    force.friction = function(x) {
	      if (!arguments.length) return friction;
	      friction = +x;
	      return force;
	    };
	    force.charge = function(x) {
	      if (!arguments.length) return charge;
	      charge = typeof x === "function" ? x : +x;
	      return force;
	    };
	    force.chargeDistance = function(x) {
	      if (!arguments.length) return Math.sqrt(chargeDistance2);
	      chargeDistance2 = x * x;
	      return force;
	    };
	    force.gravity = function(x) {
	      if (!arguments.length) return gravity;
	      gravity = +x;
	      return force;
	    };
	    force.theta = function(x) {
	      if (!arguments.length) return Math.sqrt(theta2);
	      theta2 = x * x;
	      return force;
	    };
	    force.alpha = function(x) {
	      if (!arguments.length) return alpha;
	      x = +x;
	      if (alpha) {
	        if (x > 0) {
	          alpha = x;
	        } else {
	          timer.c = null, timer.t = NaN, timer = null;
	          event.end({
	            type: "end",
	            alpha: alpha = 0
	          });
	        }
	      } else if (x > 0) {
	        event.start({
	          type: "start",
	          alpha: alpha = x
	        });
	        timer = d3_timer(force.tick);
	      }
	      return force;
	    };
	    force.start = function() {
	      var i, n = nodes.length, m = links.length, w = size[0], h = size[1], neighbors, o;
	      for (i = 0; i < n; ++i) {
	        (o = nodes[i]).index = i;
	        o.weight = 0;
	      }
	      for (i = 0; i < m; ++i) {
	        o = links[i];
	        if (typeof o.source == "number") o.source = nodes[o.source];
	        if (typeof o.target == "number") o.target = nodes[o.target];
	        ++o.source.weight;
	        ++o.target.weight;
	      }
	      for (i = 0; i < n; ++i) {
	        o = nodes[i];
	        if (isNaN(o.x)) o.x = position("x", w);
	        if (isNaN(o.y)) o.y = position("y", h);
	        if (isNaN(o.px)) o.px = o.x;
	        if (isNaN(o.py)) o.py = o.y;
	      }
	      distances = [];
	      if (typeof linkDistance === "function") for (i = 0; i < m; ++i) distances[i] = +linkDistance.call(this, links[i], i); else for (i = 0; i < m; ++i) distances[i] = linkDistance;
	      strengths = [];
	      if (typeof linkStrength === "function") for (i = 0; i < m; ++i) strengths[i] = +linkStrength.call(this, links[i], i); else for (i = 0; i < m; ++i) strengths[i] = linkStrength;
	      charges = [];
	      if (typeof charge === "function") for (i = 0; i < n; ++i) charges[i] = +charge.call(this, nodes[i], i); else for (i = 0; i < n; ++i) charges[i] = charge;
	      function position(dimension, size) {
	        if (!neighbors) {
	          neighbors = new Array(n);
	          for (j = 0; j < n; ++j) {
	            neighbors[j] = [];
	          }
	          for (j = 0; j < m; ++j) {
	            var o = links[j];
	            neighbors[o.source.index].push(o.target);
	            neighbors[o.target.index].push(o.source);
	          }
	        }
	        var candidates = neighbors[i], j = -1, l = candidates.length, x;
	        while (++j < l) if (!isNaN(x = candidates[j][dimension])) return x;
	        return Math.random() * size;
	      }
	      return force.resume();
	    };
	    force.resume = function() {
	      return force.alpha(.1);
	    };
	    force.stop = function() {
	      return force.alpha(0);
	    };
	    force.drag = function() {
	      if (!drag) drag = d3.behavior.drag().origin(d3_identity).on("dragstart.force", d3_layout_forceDragstart).on("drag.force", dragmove).on("dragend.force", d3_layout_forceDragend);
	      if (!arguments.length) return drag;
	      this.on("mouseover.force", d3_layout_forceMouseover).on("mouseout.force", d3_layout_forceMouseout).call(drag);
	    };
	    function dragmove(d) {
	      d.px = d3.event.x, d.py = d3.event.y;
	      force.resume();
	    }
	    return d3.rebind(force, event, "on");
	  };
	  function d3_layout_forceDragstart(d) {
	    d.fixed |= 2;
	  }
	  function d3_layout_forceDragend(d) {
	    d.fixed &= ~6;
	  }
	  function d3_layout_forceMouseover(d) {
	    d.fixed |= 4;
	    d.px = d.x, d.py = d.y;
	  }
	  function d3_layout_forceMouseout(d) {
	    d.fixed &= ~4;
	  }
	  function d3_layout_forceAccumulate(quad, alpha, charges) {
	    var cx = 0, cy = 0;
	    quad.charge = 0;
	    if (!quad.leaf) {
	      var nodes = quad.nodes, n = nodes.length, i = -1, c;
	      while (++i < n) {
	        c = nodes[i];
	        if (c == null) continue;
	        d3_layout_forceAccumulate(c, alpha, charges);
	        quad.charge += c.charge;
	        cx += c.charge * c.cx;
	        cy += c.charge * c.cy;
	      }
	    }
	    if (quad.point) {
	      if (!quad.leaf) {
	        quad.point.x += Math.random() - .5;
	        quad.point.y += Math.random() - .5;
	      }
	      var k = alpha * charges[quad.point.index];
	      quad.charge += quad.pointCharge = k;
	      cx += k * quad.point.x;
	      cy += k * quad.point.y;
	    }
	    quad.cx = cx / quad.charge;
	    quad.cy = cy / quad.charge;
	  }
	  var d3_layout_forceLinkDistance = 20, d3_layout_forceLinkStrength = 1, d3_layout_forceChargeDistance2 = Infinity;
	  d3.layout.hierarchy = function() {
	    var sort = d3_layout_hierarchySort, children = d3_layout_hierarchyChildren, value = d3_layout_hierarchyValue;
	    function hierarchy(root) {
	      var stack = [ root ], nodes = [], node;
	      root.depth = 0;
	      while ((node = stack.pop()) != null) {
	        nodes.push(node);
	        if ((childs = children.call(hierarchy, node, node.depth)) && (n = childs.length)) {
	          var n, childs, child;
	          while (--n >= 0) {
	            stack.push(child = childs[n]);
	            child.parent = node;
	            child.depth = node.depth + 1;
	          }
	          if (value) node.value = 0;
	          node.children = childs;
	        } else {
	          if (value) node.value = +value.call(hierarchy, node, node.depth) || 0;
	          delete node.children;
	        }
	      }
	      d3_layout_hierarchyVisitAfter(root, function(node) {
	        var childs, parent;
	        if (sort && (childs = node.children)) childs.sort(sort);
	        if (value && (parent = node.parent)) parent.value += node.value;
	      });
	      return nodes;
	    }
	    hierarchy.sort = function(x) {
	      if (!arguments.length) return sort;
	      sort = x;
	      return hierarchy;
	    };
	    hierarchy.children = function(x) {
	      if (!arguments.length) return children;
	      children = x;
	      return hierarchy;
	    };
	    hierarchy.value = function(x) {
	      if (!arguments.length) return value;
	      value = x;
	      return hierarchy;
	    };
	    hierarchy.revalue = function(root) {
	      if (value) {
	        d3_layout_hierarchyVisitBefore(root, function(node) {
	          if (node.children) node.value = 0;
	        });
	        d3_layout_hierarchyVisitAfter(root, function(node) {
	          var parent;
	          if (!node.children) node.value = +value.call(hierarchy, node, node.depth) || 0;
	          if (parent = node.parent) parent.value += node.value;
	        });
	      }
	      return root;
	    };
	    return hierarchy;
	  };
	  function d3_layout_hierarchyRebind(object, hierarchy) {
	    d3.rebind(object, hierarchy, "sort", "children", "value");
	    object.nodes = object;
	    object.links = d3_layout_hierarchyLinks;
	    return object;
	  }
	  function d3_layout_hierarchyVisitBefore(node, callback) {
	    var nodes = [ node ];
	    while ((node = nodes.pop()) != null) {
	      callback(node);
	      if ((children = node.children) && (n = children.length)) {
	        var n, children;
	        while (--n >= 0) nodes.push(children[n]);
	      }
	    }
	  }
	  function d3_layout_hierarchyVisitAfter(node, callback) {
	    var nodes = [ node ], nodes2 = [];
	    while ((node = nodes.pop()) != null) {
	      nodes2.push(node);
	      if ((children = node.children) && (n = children.length)) {
	        var i = -1, n, children;
	        while (++i < n) nodes.push(children[i]);
	      }
	    }
	    while ((node = nodes2.pop()) != null) {
	      callback(node);
	    }
	  }
	  function d3_layout_hierarchyChildren(d) {
	    return d.children;
	  }
	  function d3_layout_hierarchyValue(d) {
	    return d.value;
	  }
	  function d3_layout_hierarchySort(a, b) {
	    return b.value - a.value;
	  }
	  function d3_layout_hierarchyLinks(nodes) {
	    return d3.merge(nodes.map(function(parent) {
	      return (parent.children || []).map(function(child) {
	        return {
	          source: parent,
	          target: child
	        };
	      });
	    }));
	  }
	  d3.layout.partition = function() {
	    var hierarchy = d3.layout.hierarchy(), size = [ 1, 1 ];
	    function position(node, x, dx, dy) {
	      var children = node.children;
	      node.x = x;
	      node.y = node.depth * dy;
	      node.dx = dx;
	      node.dy = dy;
	      if (children && (n = children.length)) {
	        var i = -1, n, c, d;
	        dx = node.value ? dx / node.value : 0;
	        while (++i < n) {
	          position(c = children[i], x, d = c.value * dx, dy);
	          x += d;
	        }
	      }
	    }
	    function depth(node) {
	      var children = node.children, d = 0;
	      if (children && (n = children.length)) {
	        var i = -1, n;
	        while (++i < n) d = Math.max(d, depth(children[i]));
	      }
	      return 1 + d;
	    }
	    function partition(d, i) {
	      var nodes = hierarchy.call(this, d, i);
	      position(nodes[0], 0, size[0], size[1] / depth(nodes[0]));
	      return nodes;
	    }
	    partition.size = function(x) {
	      if (!arguments.length) return size;
	      size = x;
	      return partition;
	    };
	    return d3_layout_hierarchyRebind(partition, hierarchy);
	  };
	  d3.layout.pie = function() {
	    var value = Number, sort = d3_layout_pieSortByValue, startAngle = 0, endAngle = τ, padAngle = 0;
	    function pie(data) {
	      var n = data.length, values = data.map(function(d, i) {
	        return +value.call(pie, d, i);
	      }), a = +(typeof startAngle === "function" ? startAngle.apply(this, arguments) : startAngle), da = (typeof endAngle === "function" ? endAngle.apply(this, arguments) : endAngle) - a, p = Math.min(Math.abs(da) / n, +(typeof padAngle === "function" ? padAngle.apply(this, arguments) : padAngle)), pa = p * (da < 0 ? -1 : 1), sum = d3.sum(values), k = sum ? (da - n * pa) / sum : 0, index = d3.range(n), arcs = [], v;
	      if (sort != null) index.sort(sort === d3_layout_pieSortByValue ? function(i, j) {
	        return values[j] - values[i];
	      } : function(i, j) {
	        return sort(data[i], data[j]);
	      });
	      index.forEach(function(i) {
	        arcs[i] = {
	          data: data[i],
	          value: v = values[i],
	          startAngle: a,
	          endAngle: a += v * k + pa,
	          padAngle: p
	        };
	      });
	      return arcs;
	    }
	    pie.value = function(_) {
	      if (!arguments.length) return value;
	      value = _;
	      return pie;
	    };
	    pie.sort = function(_) {
	      if (!arguments.length) return sort;
	      sort = _;
	      return pie;
	    };
	    pie.startAngle = function(_) {
	      if (!arguments.length) return startAngle;
	      startAngle = _;
	      return pie;
	    };
	    pie.endAngle = function(_) {
	      if (!arguments.length) return endAngle;
	      endAngle = _;
	      return pie;
	    };
	    pie.padAngle = function(_) {
	      if (!arguments.length) return padAngle;
	      padAngle = _;
	      return pie;
	    };
	    return pie;
	  };
	  var d3_layout_pieSortByValue = {};
	  d3.layout.stack = function() {
	    var values = d3_identity, order = d3_layout_stackOrderDefault, offset = d3_layout_stackOffsetZero, out = d3_layout_stackOut, x = d3_layout_stackX, y = d3_layout_stackY;
	    function stack(data, index) {
	      if (!(n = data.length)) return data;
	      var series = data.map(function(d, i) {
	        return values.call(stack, d, i);
	      });
	      var points = series.map(function(d) {
	        return d.map(function(v, i) {
	          return [ x.call(stack, v, i), y.call(stack, v, i) ];
	        });
	      });
	      var orders = order.call(stack, points, index);
	      series = d3.permute(series, orders);
	      points = d3.permute(points, orders);
	      var offsets = offset.call(stack, points, index);
	      var m = series[0].length, n, i, j, o;
	      for (j = 0; j < m; ++j) {
	        out.call(stack, series[0][j], o = offsets[j], points[0][j][1]);
	        for (i = 1; i < n; ++i) {
	          out.call(stack, series[i][j], o += points[i - 1][j][1], points[i][j][1]);
	        }
	      }
	      return data;
	    }
	    stack.values = function(x) {
	      if (!arguments.length) return values;
	      values = x;
	      return stack;
	    };
	    stack.order = function(x) {
	      if (!arguments.length) return order;
	      order = typeof x === "function" ? x : d3_layout_stackOrders.get(x) || d3_layout_stackOrderDefault;
	      return stack;
	    };
	    stack.offset = function(x) {
	      if (!arguments.length) return offset;
	      offset = typeof x === "function" ? x : d3_layout_stackOffsets.get(x) || d3_layout_stackOffsetZero;
	      return stack;
	    };
	    stack.x = function(z) {
	      if (!arguments.length) return x;
	      x = z;
	      return stack;
	    };
	    stack.y = function(z) {
	      if (!arguments.length) return y;
	      y = z;
	      return stack;
	    };
	    stack.out = function(z) {
	      if (!arguments.length) return out;
	      out = z;
	      return stack;
	    };
	    return stack;
	  };
	  function d3_layout_stackX(d) {
	    return d.x;
	  }
	  function d3_layout_stackY(d) {
	    return d.y;
	  }
	  function d3_layout_stackOut(d, y0, y) {
	    d.y0 = y0;
	    d.y = y;
	  }
	  var d3_layout_stackOrders = d3.map({
	    "inside-out": function(data) {
	      var n = data.length, i, j, max = data.map(d3_layout_stackMaxIndex), sums = data.map(d3_layout_stackReduceSum), index = d3.range(n).sort(function(a, b) {
	        return max[a] - max[b];
	      }), top = 0, bottom = 0, tops = [], bottoms = [];
	      for (i = 0; i < n; ++i) {
	        j = index[i];
	        if (top < bottom) {
	          top += sums[j];
	          tops.push(j);
	        } else {
	          bottom += sums[j];
	          bottoms.push(j);
	        }
	      }
	      return bottoms.reverse().concat(tops);
	    },
	    reverse: function(data) {
	      return d3.range(data.length).reverse();
	    },
	    "default": d3_layout_stackOrderDefault
	  });
	  var d3_layout_stackOffsets = d3.map({
	    silhouette: function(data) {
	      var n = data.length, m = data[0].length, sums = [], max = 0, i, j, o, y0 = [];
	      for (j = 0; j < m; ++j) {
	        for (i = 0, o = 0; i < n; i++) o += data[i][j][1];
	        if (o > max) max = o;
	        sums.push(o);
	      }
	      for (j = 0; j < m; ++j) {
	        y0[j] = (max - sums[j]) / 2;
	      }
	      return y0;
	    },
	    wiggle: function(data) {
	      var n = data.length, x = data[0], m = x.length, i, j, k, s1, s2, s3, dx, o, o0, y0 = [];
	      y0[0] = o = o0 = 0;
	      for (j = 1; j < m; ++j) {
	        for (i = 0, s1 = 0; i < n; ++i) s1 += data[i][j][1];
	        for (i = 0, s2 = 0, dx = x[j][0] - x[j - 1][0]; i < n; ++i) {
	          for (k = 0, s3 = (data[i][j][1] - data[i][j - 1][1]) / (2 * dx); k < i; ++k) {
	            s3 += (data[k][j][1] - data[k][j - 1][1]) / dx;
	          }
	          s2 += s3 * data[i][j][1];
	        }
	        y0[j] = o -= s1 ? s2 / s1 * dx : 0;
	        if (o < o0) o0 = o;
	      }
	      for (j = 0; j < m; ++j) y0[j] -= o0;
	      return y0;
	    },
	    expand: function(data) {
	      var n = data.length, m = data[0].length, k = 1 / n, i, j, o, y0 = [];
	      for (j = 0; j < m; ++j) {
	        for (i = 0, o = 0; i < n; i++) o += data[i][j][1];
	        if (o) for (i = 0; i < n; i++) data[i][j][1] /= o; else for (i = 0; i < n; i++) data[i][j][1] = k;
	      }
	      for (j = 0; j < m; ++j) y0[j] = 0;
	      return y0;
	    },
	    zero: d3_layout_stackOffsetZero
	  });
	  function d3_layout_stackOrderDefault(data) {
	    return d3.range(data.length);
	  }
	  function d3_layout_stackOffsetZero(data) {
	    var j = -1, m = data[0].length, y0 = [];
	    while (++j < m) y0[j] = 0;
	    return y0;
	  }
	  function d3_layout_stackMaxIndex(array) {
	    var i = 1, j = 0, v = array[0][1], k, n = array.length;
	    for (;i < n; ++i) {
	      if ((k = array[i][1]) > v) {
	        j = i;
	        v = k;
	      }
	    }
	    return j;
	  }
	  function d3_layout_stackReduceSum(d) {
	    return d.reduce(d3_layout_stackSum, 0);
	  }
	  function d3_layout_stackSum(p, d) {
	    return p + d[1];
	  }
	  d3.layout.histogram = function() {
	    var frequency = true, valuer = Number, ranger = d3_layout_histogramRange, binner = d3_layout_histogramBinSturges;
	    function histogram(data, i) {
	      var bins = [], values = data.map(valuer, this), range = ranger.call(this, values, i), thresholds = binner.call(this, range, values, i), bin, i = -1, n = values.length, m = thresholds.length - 1, k = frequency ? 1 : 1 / n, x;
	      while (++i < m) {
	        bin = bins[i] = [];
	        bin.dx = thresholds[i + 1] - (bin.x = thresholds[i]);
	        bin.y = 0;
	      }
	      if (m > 0) {
	        i = -1;
	        while (++i < n) {
	          x = values[i];
	          if (x >= range[0] && x <= range[1]) {
	            bin = bins[d3.bisect(thresholds, x, 1, m) - 1];
	            bin.y += k;
	            bin.push(data[i]);
	          }
	        }
	      }
	      return bins;
	    }
	    histogram.value = function(x) {
	      if (!arguments.length) return valuer;
	      valuer = x;
	      return histogram;
	    };
	    histogram.range = function(x) {
	      if (!arguments.length) return ranger;
	      ranger = d3_functor(x);
	      return histogram;
	    };
	    histogram.bins = function(x) {
	      if (!arguments.length) return binner;
	      binner = typeof x === "number" ? function(range) {
	        return d3_layout_histogramBinFixed(range, x);
	      } : d3_functor(x);
	      return histogram;
	    };
	    histogram.frequency = function(x) {
	      if (!arguments.length) return frequency;
	      frequency = !!x;
	      return histogram;
	    };
	    return histogram;
	  };
	  function d3_layout_histogramBinSturges(range, values) {
	    return d3_layout_histogramBinFixed(range, Math.ceil(Math.log(values.length) / Math.LN2 + 1));
	  }
	  function d3_layout_histogramBinFixed(range, n) {
	    var x = -1, b = +range[0], m = (range[1] - b) / n, f = [];
	    while (++x <= n) f[x] = m * x + b;
	    return f;
	  }
	  function d3_layout_histogramRange(values) {
	    return [ d3.min(values), d3.max(values) ];
	  }
	  d3.layout.pack = function() {
	    var hierarchy = d3.layout.hierarchy().sort(d3_layout_packSort), padding = 0, size = [ 1, 1 ], radius;
	    function pack(d, i) {
	      var nodes = hierarchy.call(this, d, i), root = nodes[0], w = size[0], h = size[1], r = radius == null ? Math.sqrt : typeof radius === "function" ? radius : function() {
	        return radius;
	      };
	      root.x = root.y = 0;
	      d3_layout_hierarchyVisitAfter(root, function(d) {
	        d.r = +r(d.value);
	      });
	      d3_layout_hierarchyVisitAfter(root, d3_layout_packSiblings);
	      if (padding) {
	        var dr = padding * (radius ? 1 : Math.max(2 * root.r / w, 2 * root.r / h)) / 2;
	        d3_layout_hierarchyVisitAfter(root, function(d) {
	          d.r += dr;
	        });
	        d3_layout_hierarchyVisitAfter(root, d3_layout_packSiblings);
	        d3_layout_hierarchyVisitAfter(root, function(d) {
	          d.r -= dr;
	        });
	      }
	      d3_layout_packTransform(root, w / 2, h / 2, radius ? 1 : 1 / Math.max(2 * root.r / w, 2 * root.r / h));
	      return nodes;
	    }
	    pack.size = function(_) {
	      if (!arguments.length) return size;
	      size = _;
	      return pack;
	    };
	    pack.radius = function(_) {
	      if (!arguments.length) return radius;
	      radius = _ == null || typeof _ === "function" ? _ : +_;
	      return pack;
	    };
	    pack.padding = function(_) {
	      if (!arguments.length) return padding;
	      padding = +_;
	      return pack;
	    };
	    return d3_layout_hierarchyRebind(pack, hierarchy);
	  };
	  function d3_layout_packSort(a, b) {
	    return a.value - b.value;
	  }
	  function d3_layout_packInsert(a, b) {
	    var c = a._pack_next;
	    a._pack_next = b;
	    b._pack_prev = a;
	    b._pack_next = c;
	    c._pack_prev = b;
	  }
	  function d3_layout_packSplice(a, b) {
	    a._pack_next = b;
	    b._pack_prev = a;
	  }
	  function d3_layout_packIntersects(a, b) {
	    var dx = b.x - a.x, dy = b.y - a.y, dr = a.r + b.r;
	    return .999 * dr * dr > dx * dx + dy * dy;
	  }
	  function d3_layout_packSiblings(node) {
	    if (!(nodes = node.children) || !(n = nodes.length)) return;
	    var nodes, xMin = Infinity, xMax = -Infinity, yMin = Infinity, yMax = -Infinity, a, b, c, i, j, k, n;
	    function bound(node) {
	      xMin = Math.min(node.x - node.r, xMin);
	      xMax = Math.max(node.x + node.r, xMax);
	      yMin = Math.min(node.y - node.r, yMin);
	      yMax = Math.max(node.y + node.r, yMax);
	    }
	    nodes.forEach(d3_layout_packLink);
	    a = nodes[0];
	    a.x = -a.r;
	    a.y = 0;
	    bound(a);
	    if (n > 1) {
	      b = nodes[1];
	      b.x = b.r;
	      b.y = 0;
	      bound(b);
	      if (n > 2) {
	        c = nodes[2];
	        d3_layout_packPlace(a, b, c);
	        bound(c);
	        d3_layout_packInsert(a, c);
	        a._pack_prev = c;
	        d3_layout_packInsert(c, b);
	        b = a._pack_next;
	        for (i = 3; i < n; i++) {
	          d3_layout_packPlace(a, b, c = nodes[i]);
	          var isect = 0, s1 = 1, s2 = 1;
	          for (j = b._pack_next; j !== b; j = j._pack_next, s1++) {
	            if (d3_layout_packIntersects(j, c)) {
	              isect = 1;
	              break;
	            }
	          }
	          if (isect == 1) {
	            for (k = a._pack_prev; k !== j._pack_prev; k = k._pack_prev, s2++) {
	              if (d3_layout_packIntersects(k, c)) {
	                break;
	              }
	            }
	          }
	          if (isect) {
	            if (s1 < s2 || s1 == s2 && b.r < a.r) d3_layout_packSplice(a, b = j); else d3_layout_packSplice(a = k, b);
	            i--;
	          } else {
	            d3_layout_packInsert(a, c);
	            b = c;
	            bound(c);
	          }
	        }
	      }
	    }
	    var cx = (xMin + xMax) / 2, cy = (yMin + yMax) / 2, cr = 0;
	    for (i = 0; i < n; i++) {
	      c = nodes[i];
	      c.x -= cx;
	      c.y -= cy;
	      cr = Math.max(cr, c.r + Math.sqrt(c.x * c.x + c.y * c.y));
	    }
	    node.r = cr;
	    nodes.forEach(d3_layout_packUnlink);
	  }
	  function d3_layout_packLink(node) {
	    node._pack_next = node._pack_prev = node;
	  }
	  function d3_layout_packUnlink(node) {
	    delete node._pack_next;
	    delete node._pack_prev;
	  }
	  function d3_layout_packTransform(node, x, y, k) {
	    var children = node.children;
	    node.x = x += k * node.x;
	    node.y = y += k * node.y;
	    node.r *= k;
	    if (children) {
	      var i = -1, n = children.length;
	      while (++i < n) d3_layout_packTransform(children[i], x, y, k);
	    }
	  }
	  function d3_layout_packPlace(a, b, c) {
	    var db = a.r + c.r, dx = b.x - a.x, dy = b.y - a.y;
	    if (db && (dx || dy)) {
	      var da = b.r + c.r, dc = dx * dx + dy * dy;
	      da *= da;
	      db *= db;
	      var x = .5 + (db - da) / (2 * dc), y = Math.sqrt(Math.max(0, 2 * da * (db + dc) - (db -= dc) * db - da * da)) / (2 * dc);
	      c.x = a.x + x * dx + y * dy;
	      c.y = a.y + x * dy - y * dx;
	    } else {
	      c.x = a.x + db;
	      c.y = a.y;
	    }
	  }
	  d3.layout.tree = function() {
	    var hierarchy = d3.layout.hierarchy().sort(null).value(null), separation = d3_layout_treeSeparation, size = [ 1, 1 ], nodeSize = null;
	    function tree(d, i) {
	      var nodes = hierarchy.call(this, d, i), root0 = nodes[0], root1 = wrapTree(root0);
	      d3_layout_hierarchyVisitAfter(root1, firstWalk), root1.parent.m = -root1.z;
	      d3_layout_hierarchyVisitBefore(root1, secondWalk);
	      if (nodeSize) d3_layout_hierarchyVisitBefore(root0, sizeNode); else {
	        var left = root0, right = root0, bottom = root0;
	        d3_layout_hierarchyVisitBefore(root0, function(node) {
	          if (node.x < left.x) left = node;
	          if (node.x > right.x) right = node;
	          if (node.depth > bottom.depth) bottom = node;
	        });
	        var tx = separation(left, right) / 2 - left.x, kx = size[0] / (right.x + separation(right, left) / 2 + tx), ky = size[1] / (bottom.depth || 1);
	        d3_layout_hierarchyVisitBefore(root0, function(node) {
	          node.x = (node.x + tx) * kx;
	          node.y = node.depth * ky;
	        });
	      }
	      return nodes;
	    }
	    function wrapTree(root0) {
	      var root1 = {
	        A: null,
	        children: [ root0 ]
	      }, queue = [ root1 ], node1;
	      while ((node1 = queue.pop()) != null) {
	        for (var children = node1.children, child, i = 0, n = children.length; i < n; ++i) {
	          queue.push((children[i] = child = {
	            _: children[i],
	            parent: node1,
	            children: (child = children[i].children) && child.slice() || [],
	            A: null,
	            a: null,
	            z: 0,
	            m: 0,
	            c: 0,
	            s: 0,
	            t: null,
	            i: i
	          }).a = child);
	        }
	      }
	      return root1.children[0];
	    }
	    function firstWalk(v) {
	      var children = v.children, siblings = v.parent.children, w = v.i ? siblings[v.i - 1] : null;
	      if (children.length) {
	        d3_layout_treeShift(v);
	        var midpoint = (children[0].z + children[children.length - 1].z) / 2;
	        if (w) {
	          v.z = w.z + separation(v._, w._);
	          v.m = v.z - midpoint;
	        } else {
	          v.z = midpoint;
	        }
	      } else if (w) {
	        v.z = w.z + separation(v._, w._);
	      }
	      v.parent.A = apportion(v, w, v.parent.A || siblings[0]);
	    }
	    function secondWalk(v) {
	      v._.x = v.z + v.parent.m;
	      v.m += v.parent.m;
	    }
	    function apportion(v, w, ancestor) {
	      if (w) {
	        var vip = v, vop = v, vim = w, vom = vip.parent.children[0], sip = vip.m, sop = vop.m, sim = vim.m, som = vom.m, shift;
	        while (vim = d3_layout_treeRight(vim), vip = d3_layout_treeLeft(vip), vim && vip) {
	          vom = d3_layout_treeLeft(vom);
	          vop = d3_layout_treeRight(vop);
	          vop.a = v;
	          shift = vim.z + sim - vip.z - sip + separation(vim._, vip._);
	          if (shift > 0) {
	            d3_layout_treeMove(d3_layout_treeAncestor(vim, v, ancestor), v, shift);
	            sip += shift;
	            sop += shift;
	          }
	          sim += vim.m;
	          sip += vip.m;
	          som += vom.m;
	          sop += vop.m;
	        }
	        if (vim && !d3_layout_treeRight(vop)) {
	          vop.t = vim;
	          vop.m += sim - sop;
	        }
	        if (vip && !d3_layout_treeLeft(vom)) {
	          vom.t = vip;
	          vom.m += sip - som;
	          ancestor = v;
	        }
	      }
	      return ancestor;
	    }
	    function sizeNode(node) {
	      node.x *= size[0];
	      node.y = node.depth * size[1];
	    }
	    tree.separation = function(x) {
	      if (!arguments.length) return separation;
	      separation = x;
	      return tree;
	    };
	    tree.size = function(x) {
	      if (!arguments.length) return nodeSize ? null : size;
	      nodeSize = (size = x) == null ? sizeNode : null;
	      return tree;
	    };
	    tree.nodeSize = function(x) {
	      if (!arguments.length) return nodeSize ? size : null;
	      nodeSize = (size = x) == null ? null : sizeNode;
	      return tree;
	    };
	    return d3_layout_hierarchyRebind(tree, hierarchy);
	  };
	  function d3_layout_treeSeparation(a, b) {
	    return a.parent == b.parent ? 1 : 2;
	  }
	  function d3_layout_treeLeft(v) {
	    var children = v.children;
	    return children.length ? children[0] : v.t;
	  }
	  function d3_layout_treeRight(v) {
	    var children = v.children, n;
	    return (n = children.length) ? children[n - 1] : v.t;
	  }
	  function d3_layout_treeMove(wm, wp, shift) {
	    var change = shift / (wp.i - wm.i);
	    wp.c -= change;
	    wp.s += shift;
	    wm.c += change;
	    wp.z += shift;
	    wp.m += shift;
	  }
	  function d3_layout_treeShift(v) {
	    var shift = 0, change = 0, children = v.children, i = children.length, w;
	    while (--i >= 0) {
	      w = children[i];
	      w.z += shift;
	      w.m += shift;
	      shift += w.s + (change += w.c);
	    }
	  }
	  function d3_layout_treeAncestor(vim, v, ancestor) {
	    return vim.a.parent === v.parent ? vim.a : ancestor;
	  }
	  d3.layout.cluster = function() {
	    var hierarchy = d3.layout.hierarchy().sort(null).value(null), separation = d3_layout_treeSeparation, size = [ 1, 1 ], nodeSize = false;
	    function cluster(d, i) {
	      var nodes = hierarchy.call(this, d, i), root = nodes[0], previousNode, x = 0;
	      d3_layout_hierarchyVisitAfter(root, function(node) {
	        var children = node.children;
	        if (children && children.length) {
	          node.x = d3_layout_clusterX(children);
	          node.y = d3_layout_clusterY(children);
	        } else {
	          node.x = previousNode ? x += separation(node, previousNode) : 0;
	          node.y = 0;
	          previousNode = node;
	        }
	      });
	      var left = d3_layout_clusterLeft(root), right = d3_layout_clusterRight(root), x0 = left.x - separation(left, right) / 2, x1 = right.x + separation(right, left) / 2;
	      d3_layout_hierarchyVisitAfter(root, nodeSize ? function(node) {
	        node.x = (node.x - root.x) * size[0];
	        node.y = (root.y - node.y) * size[1];
	      } : function(node) {
	        node.x = (node.x - x0) / (x1 - x0) * size[0];
	        node.y = (1 - (root.y ? node.y / root.y : 1)) * size[1];
	      });
	      return nodes;
	    }
	    cluster.separation = function(x) {
	      if (!arguments.length) return separation;
	      separation = x;
	      return cluster;
	    };
	    cluster.size = function(x) {
	      if (!arguments.length) return nodeSize ? null : size;
	      nodeSize = (size = x) == null;
	      return cluster;
	    };
	    cluster.nodeSize = function(x) {
	      if (!arguments.length) return nodeSize ? size : null;
	      nodeSize = (size = x) != null;
	      return cluster;
	    };
	    return d3_layout_hierarchyRebind(cluster, hierarchy);
	  };
	  function d3_layout_clusterY(children) {
	    return 1 + d3.max(children, function(child) {
	      return child.y;
	    });
	  }
	  function d3_layout_clusterX(children) {
	    return children.reduce(function(x, child) {
	      return x + child.x;
	    }, 0) / children.length;
	  }
	  function d3_layout_clusterLeft(node) {
	    var children = node.children;
	    return children && children.length ? d3_layout_clusterLeft(children[0]) : node;
	  }
	  function d3_layout_clusterRight(node) {
	    var children = node.children, n;
	    return children && (n = children.length) ? d3_layout_clusterRight(children[n - 1]) : node;
	  }
	  d3.layout.treemap = function() {
	    var hierarchy = d3.layout.hierarchy(), round = Math.round, size = [ 1, 1 ], padding = null, pad = d3_layout_treemapPadNull, sticky = false, stickies, mode = "squarify", ratio = .5 * (1 + Math.sqrt(5));
	    function scale(children, k) {
	      var i = -1, n = children.length, child, area;
	      while (++i < n) {
	        area = (child = children[i]).value * (k < 0 ? 0 : k);
	        child.area = isNaN(area) || area <= 0 ? 0 : area;
	      }
	    }
	    function squarify(node) {
	      var children = node.children;
	      if (children && children.length) {
	        var rect = pad(node), row = [], remaining = children.slice(), child, best = Infinity, score, u = mode === "slice" ? rect.dx : mode === "dice" ? rect.dy : mode === "slice-dice" ? node.depth & 1 ? rect.dy : rect.dx : Math.min(rect.dx, rect.dy), n;
	        scale(remaining, rect.dx * rect.dy / node.value);
	        row.area = 0;
	        while ((n = remaining.length) > 0) {
	          row.push(child = remaining[n - 1]);
	          row.area += child.area;
	          if (mode !== "squarify" || (score = worst(row, u)) <= best) {
	            remaining.pop();
	            best = score;
	          } else {
	            row.area -= row.pop().area;
	            position(row, u, rect, false);
	            u = Math.min(rect.dx, rect.dy);
	            row.length = row.area = 0;
	            best = Infinity;
	          }
	        }
	        if (row.length) {
	          position(row, u, rect, true);
	          row.length = row.area = 0;
	        }
	        children.forEach(squarify);
	      }
	    }
	    function stickify(node) {
	      var children = node.children;
	      if (children && children.length) {
	        var rect = pad(node), remaining = children.slice(), child, row = [];
	        scale(remaining, rect.dx * rect.dy / node.value);
	        row.area = 0;
	        while (child = remaining.pop()) {
	          row.push(child);
	          row.area += child.area;
	          if (child.z != null) {
	            position(row, child.z ? rect.dx : rect.dy, rect, !remaining.length);
	            row.length = row.area = 0;
	          }
	        }
	        children.forEach(stickify);
	      }
	    }
	    function worst(row, u) {
	      var s = row.area, r, rmax = 0, rmin = Infinity, i = -1, n = row.length;
	      while (++i < n) {
	        if (!(r = row[i].area)) continue;
	        if (r < rmin) rmin = r;
	        if (r > rmax) rmax = r;
	      }
	      s *= s;
	      u *= u;
	      return s ? Math.max(u * rmax * ratio / s, s / (u * rmin * ratio)) : Infinity;
	    }
	    function position(row, u, rect, flush) {
	      var i = -1, n = row.length, x = rect.x, y = rect.y, v = u ? round(row.area / u) : 0, o;
	      if (u == rect.dx) {
	        if (flush || v > rect.dy) v = rect.dy;
	        while (++i < n) {
	          o = row[i];
	          o.x = x;
	          o.y = y;
	          o.dy = v;
	          x += o.dx = Math.min(rect.x + rect.dx - x, v ? round(o.area / v) : 0);
	        }
	        o.z = true;
	        o.dx += rect.x + rect.dx - x;
	        rect.y += v;
	        rect.dy -= v;
	      } else {
	        if (flush || v > rect.dx) v = rect.dx;
	        while (++i < n) {
	          o = row[i];
	          o.x = x;
	          o.y = y;
	          o.dx = v;
	          y += o.dy = Math.min(rect.y + rect.dy - y, v ? round(o.area / v) : 0);
	        }
	        o.z = false;
	        o.dy += rect.y + rect.dy - y;
	        rect.x += v;
	        rect.dx -= v;
	      }
	    }
	    function treemap(d) {
	      var nodes = stickies || hierarchy(d), root = nodes[0];
	      root.x = root.y = 0;
	      if (root.value) root.dx = size[0], root.dy = size[1]; else root.dx = root.dy = 0;
	      if (stickies) hierarchy.revalue(root);
	      scale([ root ], root.dx * root.dy / root.value);
	      (stickies ? stickify : squarify)(root);
	      if (sticky) stickies = nodes;
	      return nodes;
	    }
	    treemap.size = function(x) {
	      if (!arguments.length) return size;
	      size = x;
	      return treemap;
	    };
	    treemap.padding = function(x) {
	      if (!arguments.length) return padding;
	      function padFunction(node) {
	        var p = x.call(treemap, node, node.depth);
	        return p == null ? d3_layout_treemapPadNull(node) : d3_layout_treemapPad(node, typeof p === "number" ? [ p, p, p, p ] : p);
	      }
	      function padConstant(node) {
	        return d3_layout_treemapPad(node, x);
	      }
	      var type;
	      pad = (padding = x) == null ? d3_layout_treemapPadNull : (type = typeof x) === "function" ? padFunction : type === "number" ? (x = [ x, x, x, x ], 
	      padConstant) : padConstant;
	      return treemap;
	    };
	    treemap.round = function(x) {
	      if (!arguments.length) return round != Number;
	      round = x ? Math.round : Number;
	      return treemap;
	    };
	    treemap.sticky = function(x) {
	      if (!arguments.length) return sticky;
	      sticky = x;
	      stickies = null;
	      return treemap;
	    };
	    treemap.ratio = function(x) {
	      if (!arguments.length) return ratio;
	      ratio = x;
	      return treemap;
	    };
	    treemap.mode = function(x) {
	      if (!arguments.length) return mode;
	      mode = x + "";
	      return treemap;
	    };
	    return d3_layout_hierarchyRebind(treemap, hierarchy);
	  };
	  function d3_layout_treemapPadNull(node) {
	    return {
	      x: node.x,
	      y: node.y,
	      dx: node.dx,
	      dy: node.dy
	    };
	  }
	  function d3_layout_treemapPad(node, padding) {
	    var x = node.x + padding[3], y = node.y + padding[0], dx = node.dx - padding[1] - padding[3], dy = node.dy - padding[0] - padding[2];
	    if (dx < 0) {
	      x += dx / 2;
	      dx = 0;
	    }
	    if (dy < 0) {
	      y += dy / 2;
	      dy = 0;
	    }
	    return {
	      x: x,
	      y: y,
	      dx: dx,
	      dy: dy
	    };
	  }
	  d3.random = {
	    normal: function(µ, σ) {
	      var n = arguments.length;
	      if (n < 2) σ = 1;
	      if (n < 1) µ = 0;
	      return function() {
	        var x, y, r;
	        do {
	          x = Math.random() * 2 - 1;
	          y = Math.random() * 2 - 1;
	          r = x * x + y * y;
	        } while (!r || r > 1);
	        return µ + σ * x * Math.sqrt(-2 * Math.log(r) / r);
	      };
	    },
	    logNormal: function() {
	      var random = d3.random.normal.apply(d3, arguments);
	      return function() {
	        return Math.exp(random());
	      };
	    },
	    bates: function(m) {
	      var random = d3.random.irwinHall(m);
	      return function() {
	        return random() / m;
	      };
	    },
	    irwinHall: function(m) {
	      return function() {
	        for (var s = 0, j = 0; j < m; j++) s += Math.random();
	        return s;
	      };
	    }
	  };
	  d3.scale = {};
	  function d3_scaleExtent(domain) {
	    var start = domain[0], stop = domain[domain.length - 1];
	    return start < stop ? [ start, stop ] : [ stop, start ];
	  }
	  function d3_scaleRange(scale) {
	    return scale.rangeExtent ? scale.rangeExtent() : d3_scaleExtent(scale.range());
	  }
	  function d3_scale_bilinear(domain, range, uninterpolate, interpolate) {
	    var u = uninterpolate(domain[0], domain[1]), i = interpolate(range[0], range[1]);
	    return function(x) {
	      return i(u(x));
	    };
	  }
	  function d3_scale_nice(domain, nice) {
	    var i0 = 0, i1 = domain.length - 1, x0 = domain[i0], x1 = domain[i1], dx;
	    if (x1 < x0) {
	      dx = i0, i0 = i1, i1 = dx;
	      dx = x0, x0 = x1, x1 = dx;
	    }
	    domain[i0] = nice.floor(x0);
	    domain[i1] = nice.ceil(x1);
	    return domain;
	  }
	  function d3_scale_niceStep(step) {
	    return step ? {
	      floor: function(x) {
	        return Math.floor(x / step) * step;
	      },
	      ceil: function(x) {
	        return Math.ceil(x / step) * step;
	      }
	    } : d3_scale_niceIdentity;
	  }
	  var d3_scale_niceIdentity = {
	    floor: d3_identity,
	    ceil: d3_identity
	  };
	  function d3_scale_polylinear(domain, range, uninterpolate, interpolate) {
	    var u = [], i = [], j = 0, k = Math.min(domain.length, range.length) - 1;
	    if (domain[k] < domain[0]) {
	      domain = domain.slice().reverse();
	      range = range.slice().reverse();
	    }
	    while (++j <= k) {
	      u.push(uninterpolate(domain[j - 1], domain[j]));
	      i.push(interpolate(range[j - 1], range[j]));
	    }
	    return function(x) {
	      var j = d3.bisect(domain, x, 1, k) - 1;
	      return i[j](u[j](x));
	    };
	  }
	  d3.scale.linear = function() {
	    return d3_scale_linear([ 0, 1 ], [ 0, 1 ], d3_interpolate, false);
	  };
	  function d3_scale_linear(domain, range, interpolate, clamp) {
	    var output, input;
	    function rescale() {
	      var linear = Math.min(domain.length, range.length) > 2 ? d3_scale_polylinear : d3_scale_bilinear, uninterpolate = clamp ? d3_uninterpolateClamp : d3_uninterpolateNumber;
	      output = linear(domain, range, uninterpolate, interpolate);
	      input = linear(range, domain, uninterpolate, d3_interpolate);
	      return scale;
	    }
	    function scale(x) {
	      return output(x);
	    }
	    scale.invert = function(y) {
	      return input(y);
	    };
	    scale.domain = function(x) {
	      if (!arguments.length) return domain;
	      domain = x.map(Number);
	      return rescale();
	    };
	    scale.range = function(x) {
	      if (!arguments.length) return range;
	      range = x;
	      return rescale();
	    };
	    scale.rangeRound = function(x) {
	      return scale.range(x).interpolate(d3_interpolateRound);
	    };
	    scale.clamp = function(x) {
	      if (!arguments.length) return clamp;
	      clamp = x;
	      return rescale();
	    };
	    scale.interpolate = function(x) {
	      if (!arguments.length) return interpolate;
	      interpolate = x;
	      return rescale();
	    };
	    scale.ticks = function(m) {
	      return d3_scale_linearTicks(domain, m);
	    };
	    scale.tickFormat = function(m, format) {
	      return d3_scale_linearTickFormat(domain, m, format);
	    };
	    scale.nice = function(m) {
	      d3_scale_linearNice(domain, m);
	      return rescale();
	    };
	    scale.copy = function() {
	      return d3_scale_linear(domain, range, interpolate, clamp);
	    };
	    return rescale();
	  }
	  function d3_scale_linearRebind(scale, linear) {
	    return d3.rebind(scale, linear, "range", "rangeRound", "interpolate", "clamp");
	  }
	  function d3_scale_linearNice(domain, m) {
	    d3_scale_nice(domain, d3_scale_niceStep(d3_scale_linearTickRange(domain, m)[2]));
	    d3_scale_nice(domain, d3_scale_niceStep(d3_scale_linearTickRange(domain, m)[2]));
	    return domain;
	  }
	  function d3_scale_linearTickRange(domain, m) {
	    if (m == null) m = 10;
	    var extent = d3_scaleExtent(domain), span = extent[1] - extent[0], step = Math.pow(10, Math.floor(Math.log(span / m) / Math.LN10)), err = m / span * step;
	    if (err <= .15) step *= 10; else if (err <= .35) step *= 5; else if (err <= .75) step *= 2;
	    extent[0] = Math.ceil(extent[0] / step) * step;
	    extent[1] = Math.floor(extent[1] / step) * step + step * .5;
	    extent[2] = step;
	    return extent;
	  }
	  function d3_scale_linearTicks(domain, m) {
	    return d3.range.apply(d3, d3_scale_linearTickRange(domain, m));
	  }
	  function d3_scale_linearTickFormat(domain, m, format) {
	    var range = d3_scale_linearTickRange(domain, m);
	    if (format) {
	      var match = d3_format_re.exec(format);
	      match.shift();
	      if (match[8] === "s") {
	        var prefix = d3.formatPrefix(Math.max(abs(range[0]), abs(range[1])));
	        if (!match[7]) match[7] = "." + d3_scale_linearPrecision(prefix.scale(range[2]));
	        match[8] = "f";
	        format = d3.format(match.join(""));
	        return function(d) {
	          return format(prefix.scale(d)) + prefix.symbol;
	        };
	      }
	      if (!match[7]) match[7] = "." + d3_scale_linearFormatPrecision(match[8], range);
	      format = match.join("");
	    } else {
	      format = ",." + d3_scale_linearPrecision(range[2]) + "f";
	    }
	    return d3.format(format);
	  }
	  var d3_scale_linearFormatSignificant = {
	    s: 1,
	    g: 1,
	    p: 1,
	    r: 1,
	    e: 1
	  };
	  function d3_scale_linearPrecision(value) {
	    return -Math.floor(Math.log(value) / Math.LN10 + .01);
	  }
	  function d3_scale_linearFormatPrecision(type, range) {
	    var p = d3_scale_linearPrecision(range[2]);
	    return type in d3_scale_linearFormatSignificant ? Math.abs(p - d3_scale_linearPrecision(Math.max(abs(range[0]), abs(range[1])))) + +(type !== "e") : p - (type === "%") * 2;
	  }
	  d3.scale.log = function() {
	    return d3_scale_log(d3.scale.linear().domain([ 0, 1 ]), 10, true, [ 1, 10 ]);
	  };
	  function d3_scale_log(linear, base, positive, domain) {
	    function log(x) {
	      return (positive ? Math.log(x < 0 ? 0 : x) : -Math.log(x > 0 ? 0 : -x)) / Math.log(base);
	    }
	    function pow(x) {
	      return positive ? Math.pow(base, x) : -Math.pow(base, -x);
	    }
	    function scale(x) {
	      return linear(log(x));
	    }
	    scale.invert = function(x) {
	      return pow(linear.invert(x));
	    };
	    scale.domain = function(x) {
	      if (!arguments.length) return domain;
	      positive = x[0] >= 0;
	      linear.domain((domain = x.map(Number)).map(log));
	      return scale;
	    };
	    scale.base = function(_) {
	      if (!arguments.length) return base;
	      base = +_;
	      linear.domain(domain.map(log));
	      return scale;
	    };
	    scale.nice = function() {
	      var niced = d3_scale_nice(domain.map(log), positive ? Math : d3_scale_logNiceNegative);
	      linear.domain(niced);
	      domain = niced.map(pow);
	      return scale;
	    };
	    scale.ticks = function() {
	      var extent = d3_scaleExtent(domain), ticks = [], u = extent[0], v = extent[1], i = Math.floor(log(u)), j = Math.ceil(log(v)), n = base % 1 ? 2 : base;
	      if (isFinite(j - i)) {
	        if (positive) {
	          for (;i < j; i++) for (var k = 1; k < n; k++) ticks.push(pow(i) * k);
	          ticks.push(pow(i));
	        } else {
	          ticks.push(pow(i));
	          for (;i++ < j; ) for (var k = n - 1; k > 0; k--) ticks.push(pow(i) * k);
	        }
	        for (i = 0; ticks[i] < u; i++) {}
	        for (j = ticks.length; ticks[j - 1] > v; j--) {}
	        ticks = ticks.slice(i, j);
	      }
	      return ticks;
	    };
	    scale.tickFormat = function(n, format) {
	      if (!arguments.length) return d3_scale_logFormat;
	      if (arguments.length < 2) format = d3_scale_logFormat; else if (typeof format !== "function") format = d3.format(format);
	      var k = Math.max(1, base * n / scale.ticks().length);
	      return function(d) {
	        var i = d / pow(Math.round(log(d)));
	        if (i * base < base - .5) i *= base;
	        return i <= k ? format(d) : "";
	      };
	    };
	    scale.copy = function() {
	      return d3_scale_log(linear.copy(), base, positive, domain);
	    };
	    return d3_scale_linearRebind(scale, linear);
	  }
	  var d3_scale_logFormat = d3.format(".0e"), d3_scale_logNiceNegative = {
	    floor: function(x) {
	      return -Math.ceil(-x);
	    },
	    ceil: function(x) {
	      return -Math.floor(-x);
	    }
	  };
	  d3.scale.pow = function() {
	    return d3_scale_pow(d3.scale.linear(), 1, [ 0, 1 ]);
	  };
	  function d3_scale_pow(linear, exponent, domain) {
	    var powp = d3_scale_powPow(exponent), powb = d3_scale_powPow(1 / exponent);
	    function scale(x) {
	      return linear(powp(x));
	    }
	    scale.invert = function(x) {
	      return powb(linear.invert(x));
	    };
	    scale.domain = function(x) {
	      if (!arguments.length) return domain;
	      linear.domain((domain = x.map(Number)).map(powp));
	      return scale;
	    };
	    scale.ticks = function(m) {
	      return d3_scale_linearTicks(domain, m);
	    };
	    scale.tickFormat = function(m, format) {
	      return d3_scale_linearTickFormat(domain, m, format);
	    };
	    scale.nice = function(m) {
	      return scale.domain(d3_scale_linearNice(domain, m));
	    };
	    scale.exponent = function(x) {
	      if (!arguments.length) return exponent;
	      powp = d3_scale_powPow(exponent = x);
	      powb = d3_scale_powPow(1 / exponent);
	      linear.domain(domain.map(powp));
	      return scale;
	    };
	    scale.copy = function() {
	      return d3_scale_pow(linear.copy(), exponent, domain);
	    };
	    return d3_scale_linearRebind(scale, linear);
	  }
	  function d3_scale_powPow(e) {
	    return function(x) {
	      return x < 0 ? -Math.pow(-x, e) : Math.pow(x, e);
	    };
	  }
	  d3.scale.sqrt = function() {
	    return d3.scale.pow().exponent(.5);
	  };
	  d3.scale.ordinal = function() {
	    return d3_scale_ordinal([], {
	      t: "range",
	      a: [ [] ]
	    });
	  };
	  function d3_scale_ordinal(domain, ranger) {
	    var index, range, rangeBand;
	    function scale(x) {
	      return range[((index.get(x) || (ranger.t === "range" ? index.set(x, domain.push(x)) : NaN)) - 1) % range.length];
	    }
	    function steps(start, step) {
	      return d3.range(domain.length).map(function(i) {
	        return start + step * i;
	      });
	    }
	    scale.domain = function(x) {
	      if (!arguments.length) return domain;
	      domain = [];
	      index = new d3_Map();
	      var i = -1, n = x.length, xi;
	      while (++i < n) if (!index.has(xi = x[i])) index.set(xi, domain.push(xi));
	      return scale[ranger.t].apply(scale, ranger.a);
	    };
	    scale.range = function(x) {
	      if (!arguments.length) return range;
	      range = x;
	      rangeBand = 0;
	      ranger = {
	        t: "range",
	        a: arguments
	      };
	      return scale;
	    };
	    scale.rangePoints = function(x, padding) {
	      if (arguments.length < 2) padding = 0;
	      var start = x[0], stop = x[1], step = domain.length < 2 ? (start = (start + stop) / 2, 
	      0) : (stop - start) / (domain.length - 1 + padding);
	      range = steps(start + step * padding / 2, step);
	      rangeBand = 0;
	      ranger = {
	        t: "rangePoints",
	        a: arguments
	      };
	      return scale;
	    };
	    scale.rangeRoundPoints = function(x, padding) {
	      if (arguments.length < 2) padding = 0;
	      var start = x[0], stop = x[1], step = domain.length < 2 ? (start = stop = Math.round((start + stop) / 2), 
	      0) : (stop - start) / (domain.length - 1 + padding) | 0;
	      range = steps(start + Math.round(step * padding / 2 + (stop - start - (domain.length - 1 + padding) * step) / 2), step);
	      rangeBand = 0;
	      ranger = {
	        t: "rangeRoundPoints",
	        a: arguments
	      };
	      return scale;
	    };
	    scale.rangeBands = function(x, padding, outerPadding) {
	      if (arguments.length < 2) padding = 0;
	      if (arguments.length < 3) outerPadding = padding;
	      var reverse = x[1] < x[0], start = x[reverse - 0], stop = x[1 - reverse], step = (stop - start) / (domain.length - padding + 2 * outerPadding);
	      range = steps(start + step * outerPadding, step);
	      if (reverse) range.reverse();
	      rangeBand = step * (1 - padding);
	      ranger = {
	        t: "rangeBands",
	        a: arguments
	      };
	      return scale;
	    };
	    scale.rangeRoundBands = function(x, padding, outerPadding) {
	      if (arguments.length < 2) padding = 0;
	      if (arguments.length < 3) outerPadding = padding;
	      var reverse = x[1] < x[0], start = x[reverse - 0], stop = x[1 - reverse], step = Math.floor((stop - start) / (domain.length - padding + 2 * outerPadding));
	      range = steps(start + Math.round((stop - start - (domain.length - padding) * step) / 2), step);
	      if (reverse) range.reverse();
	      rangeBand = Math.round(step * (1 - padding));
	      ranger = {
	        t: "rangeRoundBands",
	        a: arguments
	      };
	      return scale;
	    };
	    scale.rangeBand = function() {
	      return rangeBand;
	    };
	    scale.rangeExtent = function() {
	      return d3_scaleExtent(ranger.a[0]);
	    };
	    scale.copy = function() {
	      return d3_scale_ordinal(domain, ranger);
	    };
	    return scale.domain(domain);
	  }
	  d3.scale.category10 = function() {
	    return d3.scale.ordinal().range(d3_category10);
	  };
	  d3.scale.category20 = function() {
	    return d3.scale.ordinal().range(d3_category20);
	  };
	  d3.scale.category20b = function() {
	    return d3.scale.ordinal().range(d3_category20b);
	  };
	  d3.scale.category20c = function() {
	    return d3.scale.ordinal().range(d3_category20c);
	  };
	  var d3_category10 = [ 2062260, 16744206, 2924588, 14034728, 9725885, 9197131, 14907330, 8355711, 12369186, 1556175 ].map(d3_rgbString);
	  var d3_category20 = [ 2062260, 11454440, 16744206, 16759672, 2924588, 10018698, 14034728, 16750742, 9725885, 12955861, 9197131, 12885140, 14907330, 16234194, 8355711, 13092807, 12369186, 14408589, 1556175, 10410725 ].map(d3_rgbString);
	  var d3_category20b = [ 3750777, 5395619, 7040719, 10264286, 6519097, 9216594, 11915115, 13556636, 9202993, 12426809, 15186514, 15190932, 8666169, 11356490, 14049643, 15177372, 8077683, 10834324, 13528509, 14589654 ].map(d3_rgbString);
	  var d3_category20c = [ 3244733, 7057110, 10406625, 13032431, 15095053, 16616764, 16625259, 16634018, 3253076, 7652470, 10607003, 13101504, 7695281, 10394312, 12369372, 14342891, 6513507, 9868950, 12434877, 14277081 ].map(d3_rgbString);
	  d3.scale.quantile = function() {
	    return d3_scale_quantile([], []);
	  };
	  function d3_scale_quantile(domain, range) {
	    var thresholds;
	    function rescale() {
	      var k = 0, q = range.length;
	      thresholds = [];
	      while (++k < q) thresholds[k - 1] = d3.quantile(domain, k / q);
	      return scale;
	    }
	    function scale(x) {
	      if (!isNaN(x = +x)) return range[d3.bisect(thresholds, x)];
	    }
	    scale.domain = function(x) {
	      if (!arguments.length) return domain;
	      domain = x.map(d3_number).filter(d3_numeric).sort(d3_ascending);
	      return rescale();
	    };
	    scale.range = function(x) {
	      if (!arguments.length) return range;
	      range = x;
	      return rescale();
	    };
	    scale.quantiles = function() {
	      return thresholds;
	    };
	    scale.invertExtent = function(y) {
	      y = range.indexOf(y);
	      return y < 0 ? [ NaN, NaN ] : [ y > 0 ? thresholds[y - 1] : domain[0], y < thresholds.length ? thresholds[y] : domain[domain.length - 1] ];
	    };
	    scale.copy = function() {
	      return d3_scale_quantile(domain, range);
	    };
	    return rescale();
	  }
	  d3.scale.quantize = function() {
	    return d3_scale_quantize(0, 1, [ 0, 1 ]);
	  };
	  function d3_scale_quantize(x0, x1, range) {
	    var kx, i;
	    function scale(x) {
	      return range[Math.max(0, Math.min(i, Math.floor(kx * (x - x0))))];
	    }
	    function rescale() {
	      kx = range.length / (x1 - x0);
	      i = range.length - 1;
	      return scale;
	    }
	    scale.domain = function(x) {
	      if (!arguments.length) return [ x0, x1 ];
	      x0 = +x[0];
	      x1 = +x[x.length - 1];
	      return rescale();
	    };
	    scale.range = function(x) {
	      if (!arguments.length) return range;
	      range = x;
	      return rescale();
	    };
	    scale.invertExtent = function(y) {
	      y = range.indexOf(y);
	      y = y < 0 ? NaN : y / kx + x0;
	      return [ y, y + 1 / kx ];
	    };
	    scale.copy = function() {
	      return d3_scale_quantize(x0, x1, range);
	    };
	    return rescale();
	  }
	  d3.scale.threshold = function() {
	    return d3_scale_threshold([ .5 ], [ 0, 1 ]);
	  };
	  function d3_scale_threshold(domain, range) {
	    function scale(x) {
	      if (x <= x) return range[d3.bisect(domain, x)];
	    }
	    scale.domain = function(_) {
	      if (!arguments.length) return domain;
	      domain = _;
	      return scale;
	    };
	    scale.range = function(_) {
	      if (!arguments.length) return range;
	      range = _;
	      return scale;
	    };
	    scale.invertExtent = function(y) {
	      y = range.indexOf(y);
	      return [ domain[y - 1], domain[y] ];
	    };
	    scale.copy = function() {
	      return d3_scale_threshold(domain, range);
	    };
	    return scale;
	  }
	  d3.scale.identity = function() {
	    return d3_scale_identity([ 0, 1 ]);
	  };
	  function d3_scale_identity(domain) {
	    function identity(x) {
	      return +x;
	    }
	    identity.invert = identity;
	    identity.domain = identity.range = function(x) {
	      if (!arguments.length) return domain;
	      domain = x.map(identity);
	      return identity;
	    };
	    identity.ticks = function(m) {
	      return d3_scale_linearTicks(domain, m);
	    };
	    identity.tickFormat = function(m, format) {
	      return d3_scale_linearTickFormat(domain, m, format);
	    };
	    identity.copy = function() {
	      return d3_scale_identity(domain);
	    };
	    return identity;
	  }
	  d3.svg = {};
	  function d3_zero() {
	    return 0;
	  }
	  d3.svg.arc = function() {
	    var innerRadius = d3_svg_arcInnerRadius, outerRadius = d3_svg_arcOuterRadius, cornerRadius = d3_zero, padRadius = d3_svg_arcAuto, startAngle = d3_svg_arcStartAngle, endAngle = d3_svg_arcEndAngle, padAngle = d3_svg_arcPadAngle;
	    function arc() {
	      var r0 = Math.max(0, +innerRadius.apply(this, arguments)), r1 = Math.max(0, +outerRadius.apply(this, arguments)), a0 = startAngle.apply(this, arguments) - halfπ, a1 = endAngle.apply(this, arguments) - halfπ, da = Math.abs(a1 - a0), cw = a0 > a1 ? 0 : 1;
	      if (r1 < r0) rc = r1, r1 = r0, r0 = rc;
	      if (da >= τε) return circleSegment(r1, cw) + (r0 ? circleSegment(r0, 1 - cw) : "") + "Z";
	      var rc, cr, rp, ap, p0 = 0, p1 = 0, x0, y0, x1, y1, x2, y2, x3, y3, path = [];
	      if (ap = (+padAngle.apply(this, arguments) || 0) / 2) {
	        rp = padRadius === d3_svg_arcAuto ? Math.sqrt(r0 * r0 + r1 * r1) : +padRadius.apply(this, arguments);
	        if (!cw) p1 *= -1;
	        if (r1) p1 = d3_asin(rp / r1 * Math.sin(ap));
	        if (r0) p0 = d3_asin(rp / r0 * Math.sin(ap));
	      }
	      if (r1) {
	        x0 = r1 * Math.cos(a0 + p1);
	        y0 = r1 * Math.sin(a0 + p1);
	        x1 = r1 * Math.cos(a1 - p1);
	        y1 = r1 * Math.sin(a1 - p1);
	        var l1 = Math.abs(a1 - a0 - 2 * p1) <= π ? 0 : 1;
	        if (p1 && d3_svg_arcSweep(x0, y0, x1, y1) === cw ^ l1) {
	          var h1 = (a0 + a1) / 2;
	          x0 = r1 * Math.cos(h1);
	          y0 = r1 * Math.sin(h1);
	          x1 = y1 = null;
	        }
	      } else {
	        x0 = y0 = 0;
	      }
	      if (r0) {
	        x2 = r0 * Math.cos(a1 - p0);
	        y2 = r0 * Math.sin(a1 - p0);
	        x3 = r0 * Math.cos(a0 + p0);
	        y3 = r0 * Math.sin(a0 + p0);
	        var l0 = Math.abs(a0 - a1 + 2 * p0) <= π ? 0 : 1;
	        if (p0 && d3_svg_arcSweep(x2, y2, x3, y3) === 1 - cw ^ l0) {
	          var h0 = (a0 + a1) / 2;
	          x2 = r0 * Math.cos(h0);
	          y2 = r0 * Math.sin(h0);
	          x3 = y3 = null;
	        }
	      } else {
	        x2 = y2 = 0;
	      }
	      if (da > ε && (rc = Math.min(Math.abs(r1 - r0) / 2, +cornerRadius.apply(this, arguments))) > .001) {
	        cr = r0 < r1 ^ cw ? 0 : 1;
	        var rc1 = rc, rc0 = rc;
	        if (da < π) {
	          var oc = x3 == null ? [ x2, y2 ] : x1 == null ? [ x0, y0 ] : d3_geom_polygonIntersect([ x0, y0 ], [ x3, y3 ], [ x1, y1 ], [ x2, y2 ]), ax = x0 - oc[0], ay = y0 - oc[1], bx = x1 - oc[0], by = y1 - oc[1], kc = 1 / Math.sin(Math.acos((ax * bx + ay * by) / (Math.sqrt(ax * ax + ay * ay) * Math.sqrt(bx * bx + by * by))) / 2), lc = Math.sqrt(oc[0] * oc[0] + oc[1] * oc[1]);
	          rc0 = Math.min(rc, (r0 - lc) / (kc - 1));
	          rc1 = Math.min(rc, (r1 - lc) / (kc + 1));
	        }
	        if (x1 != null) {
	          var t30 = d3_svg_arcCornerTangents(x3 == null ? [ x2, y2 ] : [ x3, y3 ], [ x0, y0 ], r1, rc1, cw), t12 = d3_svg_arcCornerTangents([ x1, y1 ], [ x2, y2 ], r1, rc1, cw);
	          if (rc === rc1) {
	            path.push("M", t30[0], "A", rc1, ",", rc1, " 0 0,", cr, " ", t30[1], "A", r1, ",", r1, " 0 ", 1 - cw ^ d3_svg_arcSweep(t30[1][0], t30[1][1], t12[1][0], t12[1][1]), ",", cw, " ", t12[1], "A", rc1, ",", rc1, " 0 0,", cr, " ", t12[0]);
	          } else {
	            path.push("M", t30[0], "A", rc1, ",", rc1, " 0 1,", cr, " ", t12[0]);
	          }
	        } else {
	          path.push("M", x0, ",", y0);
	        }
	        if (x3 != null) {
	          var t03 = d3_svg_arcCornerTangents([ x0, y0 ], [ x3, y3 ], r0, -rc0, cw), t21 = d3_svg_arcCornerTangents([ x2, y2 ], x1 == null ? [ x0, y0 ] : [ x1, y1 ], r0, -rc0, cw);
	          if (rc === rc0) {
	            path.push("L", t21[0], "A", rc0, ",", rc0, " 0 0,", cr, " ", t21[1], "A", r0, ",", r0, " 0 ", cw ^ d3_svg_arcSweep(t21[1][0], t21[1][1], t03[1][0], t03[1][1]), ",", 1 - cw, " ", t03[1], "A", rc0, ",", rc0, " 0 0,", cr, " ", t03[0]);
	          } else {
	            path.push("L", t21[0], "A", rc0, ",", rc0, " 0 0,", cr, " ", t03[0]);
	          }
	        } else {
	          path.push("L", x2, ",", y2);
	        }
	      } else {
	        path.push("M", x0, ",", y0);
	        if (x1 != null) path.push("A", r1, ",", r1, " 0 ", l1, ",", cw, " ", x1, ",", y1);
	        path.push("L", x2, ",", y2);
	        if (x3 != null) path.push("A", r0, ",", r0, " 0 ", l0, ",", 1 - cw, " ", x3, ",", y3);
	      }
	      path.push("Z");
	      return path.join("");
	    }
	    function circleSegment(r1, cw) {
	      return "M0," + r1 + "A" + r1 + "," + r1 + " 0 1," + cw + " 0," + -r1 + "A" + r1 + "," + r1 + " 0 1," + cw + " 0," + r1;
	    }
	    arc.innerRadius = function(v) {
	      if (!arguments.length) return innerRadius;
	      innerRadius = d3_functor(v);
	      return arc;
	    };
	    arc.outerRadius = function(v) {
	      if (!arguments.length) return outerRadius;
	      outerRadius = d3_functor(v);
	      return arc;
	    };
	    arc.cornerRadius = function(v) {
	      if (!arguments.length) return cornerRadius;
	      cornerRadius = d3_functor(v);
	      return arc;
	    };
	    arc.padRadius = function(v) {
	      if (!arguments.length) return padRadius;
	      padRadius = v == d3_svg_arcAuto ? d3_svg_arcAuto : d3_functor(v);
	      return arc;
	    };
	    arc.startAngle = function(v) {
	      if (!arguments.length) return startAngle;
	      startAngle = d3_functor(v);
	      return arc;
	    };
	    arc.endAngle = function(v) {
	      if (!arguments.length) return endAngle;
	      endAngle = d3_functor(v);
	      return arc;
	    };
	    arc.padAngle = function(v) {
	      if (!arguments.length) return padAngle;
	      padAngle = d3_functor(v);
	      return arc;
	    };
	    arc.centroid = function() {
	      var r = (+innerRadius.apply(this, arguments) + +outerRadius.apply(this, arguments)) / 2, a = (+startAngle.apply(this, arguments) + +endAngle.apply(this, arguments)) / 2 - halfπ;
	      return [ Math.cos(a) * r, Math.sin(a) * r ];
	    };
	    return arc;
	  };
	  var d3_svg_arcAuto = "auto";
	  function d3_svg_arcInnerRadius(d) {
	    return d.innerRadius;
	  }
	  function d3_svg_arcOuterRadius(d) {
	    return d.outerRadius;
	  }
	  function d3_svg_arcStartAngle(d) {
	    return d.startAngle;
	  }
	  function d3_svg_arcEndAngle(d) {
	    return d.endAngle;
	  }
	  function d3_svg_arcPadAngle(d) {
	    return d && d.padAngle;
	  }
	  function d3_svg_arcSweep(x0, y0, x1, y1) {
	    return (x0 - x1) * y0 - (y0 - y1) * x0 > 0 ? 0 : 1;
	  }
	  function d3_svg_arcCornerTangents(p0, p1, r1, rc, cw) {
	    var x01 = p0[0] - p1[0], y01 = p0[1] - p1[1], lo = (cw ? rc : -rc) / Math.sqrt(x01 * x01 + y01 * y01), ox = lo * y01, oy = -lo * x01, x1 = p0[0] + ox, y1 = p0[1] + oy, x2 = p1[0] + ox, y2 = p1[1] + oy, x3 = (x1 + x2) / 2, y3 = (y1 + y2) / 2, dx = x2 - x1, dy = y2 - y1, d2 = dx * dx + dy * dy, r = r1 - rc, D = x1 * y2 - x2 * y1, d = (dy < 0 ? -1 : 1) * Math.sqrt(Math.max(0, r * r * d2 - D * D)), cx0 = (D * dy - dx * d) / d2, cy0 = (-D * dx - dy * d) / d2, cx1 = (D * dy + dx * d) / d2, cy1 = (-D * dx + dy * d) / d2, dx0 = cx0 - x3, dy0 = cy0 - y3, dx1 = cx1 - x3, dy1 = cy1 - y3;
	    if (dx0 * dx0 + dy0 * dy0 > dx1 * dx1 + dy1 * dy1) cx0 = cx1, cy0 = cy1;
	    return [ [ cx0 - ox, cy0 - oy ], [ cx0 * r1 / r, cy0 * r1 / r ] ];
	  }
	  function d3_svg_line(projection) {
	    var x = d3_geom_pointX, y = d3_geom_pointY, defined = d3_true, interpolate = d3_svg_lineLinear, interpolateKey = interpolate.key, tension = .7;
	    function line(data) {
	      var segments = [], points = [], i = -1, n = data.length, d, fx = d3_functor(x), fy = d3_functor(y);
	      function segment() {
	        segments.push("M", interpolate(projection(points), tension));
	      }
	      while (++i < n) {
	        if (defined.call(this, d = data[i], i)) {
	          points.push([ +fx.call(this, d, i), +fy.call(this, d, i) ]);
	        } else if (points.length) {
	          segment();
	          points = [];
	        }
	      }
	      if (points.length) segment();
	      return segments.length ? segments.join("") : null;
	    }
	    line.x = function(_) {
	      if (!arguments.length) return x;
	      x = _;
	      return line;
	    };
	    line.y = function(_) {
	      if (!arguments.length) return y;
	      y = _;
	      return line;
	    };
	    line.defined = function(_) {
	      if (!arguments.length) return defined;
	      defined = _;
	      return line;
	    };
	    line.interpolate = function(_) {
	      if (!arguments.length) return interpolateKey;
	      if (typeof _ === "function") interpolateKey = interpolate = _; else interpolateKey = (interpolate = d3_svg_lineInterpolators.get(_) || d3_svg_lineLinear).key;
	      return line;
	    };
	    line.tension = function(_) {
	      if (!arguments.length) return tension;
	      tension = _;
	      return line;
	    };
	    return line;
	  }
	  d3.svg.line = function() {
	    return d3_svg_line(d3_identity);
	  };
	  var d3_svg_lineInterpolators = d3.map({
	    linear: d3_svg_lineLinear,
	    "linear-closed": d3_svg_lineLinearClosed,
	    step: d3_svg_lineStep,
	    "step-before": d3_svg_lineStepBefore,
	    "step-after": d3_svg_lineStepAfter,
	    basis: d3_svg_lineBasis,
	    "basis-open": d3_svg_lineBasisOpen,
	    "basis-closed": d3_svg_lineBasisClosed,
	    bundle: d3_svg_lineBundle,
	    cardinal: d3_svg_lineCardinal,
	    "cardinal-open": d3_svg_lineCardinalOpen,
	    "cardinal-closed": d3_svg_lineCardinalClosed,
	    monotone: d3_svg_lineMonotone
	  });
	  d3_svg_lineInterpolators.forEach(function(key, value) {
	    value.key = key;
	    value.closed = /-closed$/.test(key);
	  });
	  function d3_svg_lineLinear(points) {
	    return points.length > 1 ? points.join("L") : points + "Z";
	  }
	  function d3_svg_lineLinearClosed(points) {
	    return points.join("L") + "Z";
	  }
	  function d3_svg_lineStep(points) {
	    var i = 0, n = points.length, p = points[0], path = [ p[0], ",", p[1] ];
	    while (++i < n) path.push("H", (p[0] + (p = points[i])[0]) / 2, "V", p[1]);
	    if (n > 1) path.push("H", p[0]);
	    return path.join("");
	  }
	  function d3_svg_lineStepBefore(points) {
	    var i = 0, n = points.length, p = points[0], path = [ p[0], ",", p[1] ];
	    while (++i < n) path.push("V", (p = points[i])[1], "H", p[0]);
	    return path.join("");
	  }
	  function d3_svg_lineStepAfter(points) {
	    var i = 0, n = points.length, p = points[0], path = [ p[0], ",", p[1] ];
	    while (++i < n) path.push("H", (p = points[i])[0], "V", p[1]);
	    return path.join("");
	  }
	  function d3_svg_lineCardinalOpen(points, tension) {
	    return points.length < 4 ? d3_svg_lineLinear(points) : points[1] + d3_svg_lineHermite(points.slice(1, -1), d3_svg_lineCardinalTangents(points, tension));
	  }
	  function d3_svg_lineCardinalClosed(points, tension) {
	    return points.length < 3 ? d3_svg_lineLinearClosed(points) : points[0] + d3_svg_lineHermite((points.push(points[0]), 
	    points), d3_svg_lineCardinalTangents([ points[points.length - 2] ].concat(points, [ points[1] ]), tension));
	  }
	  function d3_svg_lineCardinal(points, tension) {
	    return points.length < 3 ? d3_svg_lineLinear(points) : points[0] + d3_svg_lineHermite(points, d3_svg_lineCardinalTangents(points, tension));
	  }
	  function d3_svg_lineHermite(points, tangents) {
	    if (tangents.length < 1 || points.length != tangents.length && points.length != tangents.length + 2) {
	      return d3_svg_lineLinear(points);
	    }
	    var quad = points.length != tangents.length, path = "", p0 = points[0], p = points[1], t0 = tangents[0], t = t0, pi = 1;
	    if (quad) {
	      path += "Q" + (p[0] - t0[0] * 2 / 3) + "," + (p[1] - t0[1] * 2 / 3) + "," + p[0] + "," + p[1];
	      p0 = points[1];
	      pi = 2;
	    }
	    if (tangents.length > 1) {
	      t = tangents[1];
	      p = points[pi];
	      pi++;
	      path += "C" + (p0[0] + t0[0]) + "," + (p0[1] + t0[1]) + "," + (p[0] - t[0]) + "," + (p[1] - t[1]) + "," + p[0] + "," + p[1];
	      for (var i = 2; i < tangents.length; i++, pi++) {
	        p = points[pi];
	        t = tangents[i];
	        path += "S" + (p[0] - t[0]) + "," + (p[1] - t[1]) + "," + p[0] + "," + p[1];
	      }
	    }
	    if (quad) {
	      var lp = points[pi];
	      path += "Q" + (p[0] + t[0] * 2 / 3) + "," + (p[1] + t[1] * 2 / 3) + "," + lp[0] + "," + lp[1];
	    }
	    return path;
	  }
	  function d3_svg_lineCardinalTangents(points, tension) {
	    var tangents = [], a = (1 - tension) / 2, p0, p1 = points[0], p2 = points[1], i = 1, n = points.length;
	    while (++i < n) {
	      p0 = p1;
	      p1 = p2;
	      p2 = points[i];
	      tangents.push([ a * (p2[0] - p0[0]), a * (p2[1] - p0[1]) ]);
	    }
	    return tangents;
	  }
	  function d3_svg_lineBasis(points) {
	    if (points.length < 3) return d3_svg_lineLinear(points);
	    var i = 1, n = points.length, pi = points[0], x0 = pi[0], y0 = pi[1], px = [ x0, x0, x0, (pi = points[1])[0] ], py = [ y0, y0, y0, pi[1] ], path = [ x0, ",", y0, "L", d3_svg_lineDot4(d3_svg_lineBasisBezier3, px), ",", d3_svg_lineDot4(d3_svg_lineBasisBezier3, py) ];
	    points.push(points[n - 1]);
	    while (++i <= n) {
	      pi = points[i];
	      px.shift();
	      px.push(pi[0]);
	      py.shift();
	      py.push(pi[1]);
	      d3_svg_lineBasisBezier(path, px, py);
	    }
	    points.pop();
	    path.push("L", pi);
	    return path.join("");
	  }
	  function d3_svg_lineBasisOpen(points) {
	    if (points.length < 4) return d3_svg_lineLinear(points);
	    var path = [], i = -1, n = points.length, pi, px = [ 0 ], py = [ 0 ];
	    while (++i < 3) {
	      pi = points[i];
	      px.push(pi[0]);
	      py.push(pi[1]);
	    }
	    path.push(d3_svg_lineDot4(d3_svg_lineBasisBezier3, px) + "," + d3_svg_lineDot4(d3_svg_lineBasisBezier3, py));
	    --i;
	    while (++i < n) {
	      pi = points[i];
	      px.shift();
	      px.push(pi[0]);
	      py.shift();
	      py.push(pi[1]);
	      d3_svg_lineBasisBezier(path, px, py);
	    }
	    return path.join("");
	  }
	  function d3_svg_lineBasisClosed(points) {
	    var path, i = -1, n = points.length, m = n + 4, pi, px = [], py = [];
	    while (++i < 4) {
	      pi = points[i % n];
	      px.push(pi[0]);
	      py.push(pi[1]);
	    }
	    path = [ d3_svg_lineDot4(d3_svg_lineBasisBezier3, px), ",", d3_svg_lineDot4(d3_svg_lineBasisBezier3, py) ];
	    --i;
	    while (++i < m) {
	      pi = points[i % n];
	      px.shift();
	      px.push(pi[0]);
	      py.shift();
	      py.push(pi[1]);
	      d3_svg_lineBasisBezier(path, px, py);
	    }
	    return path.join("");
	  }
	  function d3_svg_lineBundle(points, tension) {
	    var n = points.length - 1;
	    if (n) {
	      var x0 = points[0][0], y0 = points[0][1], dx = points[n][0] - x0, dy = points[n][1] - y0, i = -1, p, t;
	      while (++i <= n) {
	        p = points[i];
	        t = i / n;
	        p[0] = tension * p[0] + (1 - tension) * (x0 + t * dx);
	        p[1] = tension * p[1] + (1 - tension) * (y0 + t * dy);
	      }
	    }
	    return d3_svg_lineBasis(points);
	  }
	  function d3_svg_lineDot4(a, b) {
	    return a[0] * b[0] + a[1] * b[1] + a[2] * b[2] + a[3] * b[3];
	  }
	  var d3_svg_lineBasisBezier1 = [ 0, 2 / 3, 1 / 3, 0 ], d3_svg_lineBasisBezier2 = [ 0, 1 / 3, 2 / 3, 0 ], d3_svg_lineBasisBezier3 = [ 0, 1 / 6, 2 / 3, 1 / 6 ];
	  function d3_svg_lineBasisBezier(path, x, y) {
	    path.push("C", d3_svg_lineDot4(d3_svg_lineBasisBezier1, x), ",", d3_svg_lineDot4(d3_svg_lineBasisBezier1, y), ",", d3_svg_lineDot4(d3_svg_lineBasisBezier2, x), ",", d3_svg_lineDot4(d3_svg_lineBasisBezier2, y), ",", d3_svg_lineDot4(d3_svg_lineBasisBezier3, x), ",", d3_svg_lineDot4(d3_svg_lineBasisBezier3, y));
	  }
	  function d3_svg_lineSlope(p0, p1) {
	    return (p1[1] - p0[1]) / (p1[0] - p0[0]);
	  }
	  function d3_svg_lineFiniteDifferences(points) {
	    var i = 0, j = points.length - 1, m = [], p0 = points[0], p1 = points[1], d = m[0] = d3_svg_lineSlope(p0, p1);
	    while (++i < j) {
	      m[i] = (d + (d = d3_svg_lineSlope(p0 = p1, p1 = points[i + 1]))) / 2;
	    }
	    m[i] = d;
	    return m;
	  }
	  function d3_svg_lineMonotoneTangents(points) {
	    var tangents = [], d, a, b, s, m = d3_svg_lineFiniteDifferences(points), i = -1, j = points.length - 1;
	    while (++i < j) {
	      d = d3_svg_lineSlope(points[i], points[i + 1]);
	      if (abs(d) < ε) {
	        m[i] = m[i + 1] = 0;
	      } else {
	        a = m[i] / d;
	        b = m[i + 1] / d;
	        s = a * a + b * b;
	        if (s > 9) {
	          s = d * 3 / Math.sqrt(s);
	          m[i] = s * a;
	          m[i + 1] = s * b;
	        }
	      }
	    }
	    i = -1;
	    while (++i <= j) {
	      s = (points[Math.min(j, i + 1)][0] - points[Math.max(0, i - 1)][0]) / (6 * (1 + m[i] * m[i]));
	      tangents.push([ s || 0, m[i] * s || 0 ]);
	    }
	    return tangents;
	  }
	  function d3_svg_lineMonotone(points) {
	    return points.length < 3 ? d3_svg_lineLinear(points) : points[0] + d3_svg_lineHermite(points, d3_svg_lineMonotoneTangents(points));
	  }
	  d3.svg.line.radial = function() {
	    var line = d3_svg_line(d3_svg_lineRadial);
	    line.radius = line.x, delete line.x;
	    line.angle = line.y, delete line.y;
	    return line;
	  };
	  function d3_svg_lineRadial(points) {
	    var point, i = -1, n = points.length, r, a;
	    while (++i < n) {
	      point = points[i];
	      r = point[0];
	      a = point[1] - halfπ;
	      point[0] = r * Math.cos(a);
	      point[1] = r * Math.sin(a);
	    }
	    return points;
	  }
	  function d3_svg_area(projection) {
	    var x0 = d3_geom_pointX, x1 = d3_geom_pointX, y0 = 0, y1 = d3_geom_pointY, defined = d3_true, interpolate = d3_svg_lineLinear, interpolateKey = interpolate.key, interpolateReverse = interpolate, L = "L", tension = .7;
	    function area(data) {
	      var segments = [], points0 = [], points1 = [], i = -1, n = data.length, d, fx0 = d3_functor(x0), fy0 = d3_functor(y0), fx1 = x0 === x1 ? function() {
	        return x;
	      } : d3_functor(x1), fy1 = y0 === y1 ? function() {
	        return y;
	      } : d3_functor(y1), x, y;
	      function segment() {
	        segments.push("M", interpolate(projection(points1), tension), L, interpolateReverse(projection(points0.reverse()), tension), "Z");
	      }
	      while (++i < n) {
	        if (defined.call(this, d = data[i], i)) {
	          points0.push([ x = +fx0.call(this, d, i), y = +fy0.call(this, d, i) ]);
	          points1.push([ +fx1.call(this, d, i), +fy1.call(this, d, i) ]);
	        } else if (points0.length) {
	          segment();
	          points0 = [];
	          points1 = [];
	        }
	      }
	      if (points0.length) segment();
	      return segments.length ? segments.join("") : null;
	    }
	    area.x = function(_) {
	      if (!arguments.length) return x1;
	      x0 = x1 = _;
	      return area;
	    };
	    area.x0 = function(_) {
	      if (!arguments.length) return x0;
	      x0 = _;
	      return area;
	    };
	    area.x1 = function(_) {
	      if (!arguments.length) return x1;
	      x1 = _;
	      return area;
	    };
	    area.y = function(_) {
	      if (!arguments.length) return y1;
	      y0 = y1 = _;
	      return area;
	    };
	    area.y0 = function(_) {
	      if (!arguments.length) return y0;
	      y0 = _;
	      return area;
	    };
	    area.y1 = function(_) {
	      if (!arguments.length) return y1;
	      y1 = _;
	      return area;
	    };
	    area.defined = function(_) {
	      if (!arguments.length) return defined;
	      defined = _;
	      return area;
	    };
	    area.interpolate = function(_) {
	      if (!arguments.length) return interpolateKey;
	      if (typeof _ === "function") interpolateKey = interpolate = _; else interpolateKey = (interpolate = d3_svg_lineInterpolators.get(_) || d3_svg_lineLinear).key;
	      interpolateReverse = interpolate.reverse || interpolate;
	      L = interpolate.closed ? "M" : "L";
	      return area;
	    };
	    area.tension = function(_) {
	      if (!arguments.length) return tension;
	      tension = _;
	      return area;
	    };
	    return area;
	  }
	  d3_svg_lineStepBefore.reverse = d3_svg_lineStepAfter;
	  d3_svg_lineStepAfter.reverse = d3_svg_lineStepBefore;
	  d3.svg.area = function() {
	    return d3_svg_area(d3_identity);
	  };
	  d3.svg.area.radial = function() {
	    var area = d3_svg_area(d3_svg_lineRadial);
	    area.radius = area.x, delete area.x;
	    area.innerRadius = area.x0, delete area.x0;
	    area.outerRadius = area.x1, delete area.x1;
	    area.angle = area.y, delete area.y;
	    area.startAngle = area.y0, delete area.y0;
	    area.endAngle = area.y1, delete area.y1;
	    return area;
	  };
	  d3.svg.chord = function() {
	    var source = d3_source, target = d3_target, radius = d3_svg_chordRadius, startAngle = d3_svg_arcStartAngle, endAngle = d3_svg_arcEndAngle;
	    function chord(d, i) {
	      var s = subgroup(this, source, d, i), t = subgroup(this, target, d, i);
	      return "M" + s.p0 + arc(s.r, s.p1, s.a1 - s.a0) + (equals(s, t) ? curve(s.r, s.p1, s.r, s.p0) : curve(s.r, s.p1, t.r, t.p0) + arc(t.r, t.p1, t.a1 - t.a0) + curve(t.r, t.p1, s.r, s.p0)) + "Z";
	    }
	    function subgroup(self, f, d, i) {
	      var subgroup = f.call(self, d, i), r = radius.call(self, subgroup, i), a0 = startAngle.call(self, subgroup, i) - halfπ, a1 = endAngle.call(self, subgroup, i) - halfπ;
	      return {
	        r: r,
	        a0: a0,
	        a1: a1,
	        p0: [ r * Math.cos(a0), r * Math.sin(a0) ],
	        p1: [ r * Math.cos(a1), r * Math.sin(a1) ]
	      };
	    }
	    function equals(a, b) {
	      return a.a0 == b.a0 && a.a1 == b.a1;
	    }
	    function arc(r, p, a) {
	      return "A" + r + "," + r + " 0 " + +(a > π) + ",1 " + p;
	    }
	    function curve(r0, p0, r1, p1) {
	      return "Q 0,0 " + p1;
	    }
	    chord.radius = function(v) {
	      if (!arguments.length) return radius;
	      radius = d3_functor(v);
	      return chord;
	    };
	    chord.source = function(v) {
	      if (!arguments.length) return source;
	      source = d3_functor(v);
	      return chord;
	    };
	    chord.target = function(v) {
	      if (!arguments.length) return target;
	      target = d3_functor(v);
	      return chord;
	    };
	    chord.startAngle = function(v) {
	      if (!arguments.length) return startAngle;
	      startAngle = d3_functor(v);
	      return chord;
	    };
	    chord.endAngle = function(v) {
	      if (!arguments.length) return endAngle;
	      endAngle = d3_functor(v);
	      return chord;
	    };
	    return chord;
	  };
	  function d3_svg_chordRadius(d) {
	    return d.radius;
	  }
	  d3.svg.diagonal = function() {
	    var source = d3_source, target = d3_target, projection = d3_svg_diagonalProjection;
	    function diagonal(d, i) {
	      var p0 = source.call(this, d, i), p3 = target.call(this, d, i), m = (p0.y + p3.y) / 2, p = [ p0, {
	        x: p0.x,
	        y: m
	      }, {
	        x: p3.x,
	        y: m
	      }, p3 ];
	      p = p.map(projection);
	      return "M" + p[0] + "C" + p[1] + " " + p[2] + " " + p[3];
	    }
	    diagonal.source = function(x) {
	      if (!arguments.length) return source;
	      source = d3_functor(x);
	      return diagonal;
	    };
	    diagonal.target = function(x) {
	      if (!arguments.length) return target;
	      target = d3_functor(x);
	      return diagonal;
	    };
	    diagonal.projection = function(x) {
	      if (!arguments.length) return projection;
	      projection = x;
	      return diagonal;
	    };
	    return diagonal;
	  };
	  function d3_svg_diagonalProjection(d) {
	    return [ d.x, d.y ];
	  }
	  d3.svg.diagonal.radial = function() {
	    var diagonal = d3.svg.diagonal(), projection = d3_svg_diagonalProjection, projection_ = diagonal.projection;
	    diagonal.projection = function(x) {
	      return arguments.length ? projection_(d3_svg_diagonalRadialProjection(projection = x)) : projection;
	    };
	    return diagonal;
	  };
	  function d3_svg_diagonalRadialProjection(projection) {
	    return function() {
	      var d = projection.apply(this, arguments), r = d[0], a = d[1] - halfπ;
	      return [ r * Math.cos(a), r * Math.sin(a) ];
	    };
	  }
	  d3.svg.symbol = function() {
	    var type = d3_svg_symbolType, size = d3_svg_symbolSize;
	    function symbol(d, i) {
	      return (d3_svg_symbols.get(type.call(this, d, i)) || d3_svg_symbolCircle)(size.call(this, d, i));
	    }
	    symbol.type = function(x) {
	      if (!arguments.length) return type;
	      type = d3_functor(x);
	      return symbol;
	    };
	    symbol.size = function(x) {
	      if (!arguments.length) return size;
	      size = d3_functor(x);
	      return symbol;
	    };
	    return symbol;
	  };
	  function d3_svg_symbolSize() {
	    return 64;
	  }
	  function d3_svg_symbolType() {
	    return "circle";
	  }
	  function d3_svg_symbolCircle(size) {
	    var r = Math.sqrt(size / π);
	    return "M0," + r + "A" + r + "," + r + " 0 1,1 0," + -r + "A" + r + "," + r + " 0 1,1 0," + r + "Z";
	  }
	  var d3_svg_symbols = d3.map({
	    circle: d3_svg_symbolCircle,
	    cross: function(size) {
	      var r = Math.sqrt(size / 5) / 2;
	      return "M" + -3 * r + "," + -r + "H" + -r + "V" + -3 * r + "H" + r + "V" + -r + "H" + 3 * r + "V" + r + "H" + r + "V" + 3 * r + "H" + -r + "V" + r + "H" + -3 * r + "Z";
	    },
	    diamond: function(size) {
	      var ry = Math.sqrt(size / (2 * d3_svg_symbolTan30)), rx = ry * d3_svg_symbolTan30;
	      return "M0," + -ry + "L" + rx + ",0" + " 0," + ry + " " + -rx + ",0" + "Z";
	    },
	    square: function(size) {
	      var r = Math.sqrt(size) / 2;
	      return "M" + -r + "," + -r + "L" + r + "," + -r + " " + r + "," + r + " " + -r + "," + r + "Z";
	    },
	    "triangle-down": function(size) {
	      var rx = Math.sqrt(size / d3_svg_symbolSqrt3), ry = rx * d3_svg_symbolSqrt3 / 2;
	      return "M0," + ry + "L" + rx + "," + -ry + " " + -rx + "," + -ry + "Z";
	    },
	    "triangle-up": function(size) {
	      var rx = Math.sqrt(size / d3_svg_symbolSqrt3), ry = rx * d3_svg_symbolSqrt3 / 2;
	      return "M0," + -ry + "L" + rx + "," + ry + " " + -rx + "," + ry + "Z";
	    }
	  });
	  d3.svg.symbolTypes = d3_svg_symbols.keys();
	  var d3_svg_symbolSqrt3 = Math.sqrt(3), d3_svg_symbolTan30 = Math.tan(30 * d3_radians);
	  d3_selectionPrototype.transition = function(name) {
	    var id = d3_transitionInheritId || ++d3_transitionId, ns = d3_transitionNamespace(name), subgroups = [], subgroup, node, transition = d3_transitionInherit || {
	      time: Date.now(),
	      ease: d3_ease_cubicInOut,
	      delay: 0,
	      duration: 250
	    };
	    for (var j = -1, m = this.length; ++j < m; ) {
	      subgroups.push(subgroup = []);
	      for (var group = this[j], i = -1, n = group.length; ++i < n; ) {
	        if (node = group[i]) d3_transitionNode(node, i, ns, id, transition);
	        subgroup.push(node);
	      }
	    }
	    return d3_transition(subgroups, ns, id);
	  };
	  d3_selectionPrototype.interrupt = function(name) {
	    return this.each(name == null ? d3_selection_interrupt : d3_selection_interruptNS(d3_transitionNamespace(name)));
	  };
	  var d3_selection_interrupt = d3_selection_interruptNS(d3_transitionNamespace());
	  function d3_selection_interruptNS(ns) {
	    return function() {
	      var lock, activeId, active;
	      if ((lock = this[ns]) && (active = lock[activeId = lock.active])) {
	        active.timer.c = null;
	        active.timer.t = NaN;
	        if (--lock.count) delete lock[activeId]; else delete this[ns];
	        lock.active += .5;
	        active.event && active.event.interrupt.call(this, this.__data__, active.index);
	      }
	    };
	  }
	  function d3_transition(groups, ns, id) {
	    d3_subclass(groups, d3_transitionPrototype);
	    groups.namespace = ns;
	    groups.id = id;
	    return groups;
	  }
	  var d3_transitionPrototype = [], d3_transitionId = 0, d3_transitionInheritId, d3_transitionInherit;
	  d3_transitionPrototype.call = d3_selectionPrototype.call;
	  d3_transitionPrototype.empty = d3_selectionPrototype.empty;
	  d3_transitionPrototype.node = d3_selectionPrototype.node;
	  d3_transitionPrototype.size = d3_selectionPrototype.size;
	  d3.transition = function(selection, name) {
	    return selection && selection.transition ? d3_transitionInheritId ? selection.transition(name) : selection : d3.selection().transition(selection);
	  };
	  d3.transition.prototype = d3_transitionPrototype;
	  d3_transitionPrototype.select = function(selector) {
	    var id = this.id, ns = this.namespace, subgroups = [], subgroup, subnode, node;
	    selector = d3_selection_selector(selector);
	    for (var j = -1, m = this.length; ++j < m; ) {
	      subgroups.push(subgroup = []);
	      for (var group = this[j], i = -1, n = group.length; ++i < n; ) {
	        if ((node = group[i]) && (subnode = selector.call(node, node.__data__, i, j))) {
	          if ("__data__" in node) subnode.__data__ = node.__data__;
	          d3_transitionNode(subnode, i, ns, id, node[ns][id]);
	          subgroup.push(subnode);
	        } else {
	          subgroup.push(null);
	        }
	      }
	    }
	    return d3_transition(subgroups, ns, id);
	  };
	  d3_transitionPrototype.selectAll = function(selector) {
	    var id = this.id, ns = this.namespace, subgroups = [], subgroup, subnodes, node, subnode, transition;
	    selector = d3_selection_selectorAll(selector);
	    for (var j = -1, m = this.length; ++j < m; ) {
	      for (var group = this[j], i = -1, n = group.length; ++i < n; ) {
	        if (node = group[i]) {
	          transition = node[ns][id];
	          subnodes = selector.call(node, node.__data__, i, j);
	          subgroups.push(subgroup = []);
	          for (var k = -1, o = subnodes.length; ++k < o; ) {
	            if (subnode = subnodes[k]) d3_transitionNode(subnode, k, ns, id, transition);
	            subgroup.push(subnode);
	          }
	        }
	      }
	    }
	    return d3_transition(subgroups, ns, id);
	  };
	  d3_transitionPrototype.filter = function(filter) {
	    var subgroups = [], subgroup, group, node;
	    if (typeof filter !== "function") filter = d3_selection_filter(filter);
	    for (var j = 0, m = this.length; j < m; j++) {
	      subgroups.push(subgroup = []);
	      for (var group = this[j], i = 0, n = group.length; i < n; i++) {
	        if ((node = group[i]) && filter.call(node, node.__data__, i, j)) {
	          subgroup.push(node);
	        }
	      }
	    }
	    return d3_transition(subgroups, this.namespace, this.id);
	  };
	  d3_transitionPrototype.tween = function(name, tween) {
	    var id = this.id, ns = this.namespace;
	    if (arguments.length < 2) return this.node()[ns][id].tween.get(name);
	    return d3_selection_each(this, tween == null ? function(node) {
	      node[ns][id].tween.remove(name);
	    } : function(node) {
	      node[ns][id].tween.set(name, tween);
	    });
	  };
	  function d3_transition_tween(groups, name, value, tween) {
	    var id = groups.id, ns = groups.namespace;
	    return d3_selection_each(groups, typeof value === "function" ? function(node, i, j) {
	      node[ns][id].tween.set(name, tween(value.call(node, node.__data__, i, j)));
	    } : (value = tween(value), function(node) {
	      node[ns][id].tween.set(name, value);
	    }));
	  }
	  d3_transitionPrototype.attr = function(nameNS, value) {
	    if (arguments.length < 2) {
	      for (value in nameNS) this.attr(value, nameNS[value]);
	      return this;
	    }
	    var interpolate = nameNS == "transform" ? d3_interpolateTransform : d3_interpolate, name = d3.ns.qualify(nameNS);
	    function attrNull() {
	      this.removeAttribute(name);
	    }
	    function attrNullNS() {
	      this.removeAttributeNS(name.space, name.local);
	    }
	    function attrTween(b) {
	      return b == null ? attrNull : (b += "", function() {
	        var a = this.getAttribute(name), i;
	        return a !== b && (i = interpolate(a, b), function(t) {
	          this.setAttribute(name, i(t));
	        });
	      });
	    }
	    function attrTweenNS(b) {
	      return b == null ? attrNullNS : (b += "", function() {
	        var a = this.getAttributeNS(name.space, name.local), i;
	        return a !== b && (i = interpolate(a, b), function(t) {
	          this.setAttributeNS(name.space, name.local, i(t));
	        });
	      });
	    }
	    return d3_transition_tween(this, "attr." + nameNS, value, name.local ? attrTweenNS : attrTween);
	  };
	  d3_transitionPrototype.attrTween = function(nameNS, tween) {
	    var name = d3.ns.qualify(nameNS);
	    function attrTween(d, i) {
	      var f = tween.call(this, d, i, this.getAttribute(name));
	      return f && function(t) {
	        this.setAttribute(name, f(t));
	      };
	    }
	    function attrTweenNS(d, i) {
	      var f = tween.call(this, d, i, this.getAttributeNS(name.space, name.local));
	      return f && function(t) {
	        this.setAttributeNS(name.space, name.local, f(t));
	      };
	    }
	    return this.tween("attr." + nameNS, name.local ? attrTweenNS : attrTween);
	  };
	  d3_transitionPrototype.style = function(name, value, priority) {
	    var n = arguments.length;
	    if (n < 3) {
	      if (typeof name !== "string") {
	        if (n < 2) value = "";
	        for (priority in name) this.style(priority, name[priority], value);
	        return this;
	      }
	      priority = "";
	    }
	    function styleNull() {
	      this.style.removeProperty(name);
	    }
	    function styleString(b) {
	      return b == null ? styleNull : (b += "", function() {
	        var a = d3_window(this).getComputedStyle(this, null).getPropertyValue(name), i;
	        return a !== b && (i = d3_interpolate(a, b), function(t) {
	          this.style.setProperty(name, i(t), priority);
	        });
	      });
	    }
	    return d3_transition_tween(this, "style." + name, value, styleString);
	  };
	  d3_transitionPrototype.styleTween = function(name, tween, priority) {
	    if (arguments.length < 3) priority = "";
	    function styleTween(d, i) {
	      var f = tween.call(this, d, i, d3_window(this).getComputedStyle(this, null).getPropertyValue(name));
	      return f && function(t) {
	        this.style.setProperty(name, f(t), priority);
	      };
	    }
	    return this.tween("style." + name, styleTween);
	  };
	  d3_transitionPrototype.text = function(value) {
	    return d3_transition_tween(this, "text", value, d3_transition_text);
	  };
	  function d3_transition_text(b) {
	    if (b == null) b = "";
	    return function() {
	      this.textContent = b;
	    };
	  }
	  d3_transitionPrototype.remove = function() {
	    var ns = this.namespace;
	    return this.each("end.transition", function() {
	      var p;
	      if (this[ns].count < 2 && (p = this.parentNode)) p.removeChild(this);
	    });
	  };
	  d3_transitionPrototype.ease = function(value) {
	    var id = this.id, ns = this.namespace;
	    if (arguments.length < 1) return this.node()[ns][id].ease;
	    if (typeof value !== "function") value = d3.ease.apply(d3, arguments);
	    return d3_selection_each(this, function(node) {
	      node[ns][id].ease = value;
	    });
	  };
	  d3_transitionPrototype.delay = function(value) {
	    var id = this.id, ns = this.namespace;
	    if (arguments.length < 1) return this.node()[ns][id].delay;
	    return d3_selection_each(this, typeof value === "function" ? function(node, i, j) {
	      node[ns][id].delay = +value.call(node, node.__data__, i, j);
	    } : (value = +value, function(node) {
	      node[ns][id].delay = value;
	    }));
	  };
	  d3_transitionPrototype.duration = function(value) {
	    var id = this.id, ns = this.namespace;
	    if (arguments.length < 1) return this.node()[ns][id].duration;
	    return d3_selection_each(this, typeof value === "function" ? function(node, i, j) {
	      node[ns][id].duration = Math.max(1, value.call(node, node.__data__, i, j));
	    } : (value = Math.max(1, value), function(node) {
	      node[ns][id].duration = value;
	    }));
	  };
	  d3_transitionPrototype.each = function(type, listener) {
	    var id = this.id, ns = this.namespace;
	    if (arguments.length < 2) {
	      var inherit = d3_transitionInherit, inheritId = d3_transitionInheritId;
	      try {
	        d3_transitionInheritId = id;
	        d3_selection_each(this, function(node, i, j) {
	          d3_transitionInherit = node[ns][id];
	          type.call(node, node.__data__, i, j);
	        });
	      } finally {
	        d3_transitionInherit = inherit;
	        d3_transitionInheritId = inheritId;
	      }
	    } else {
	      d3_selection_each(this, function(node) {
	        var transition = node[ns][id];
	        (transition.event || (transition.event = d3.dispatch("start", "end", "interrupt"))).on(type, listener);
	      });
	    }
	    return this;
	  };
	  d3_transitionPrototype.transition = function() {
	    var id0 = this.id, id1 = ++d3_transitionId, ns = this.namespace, subgroups = [], subgroup, group, node, transition;
	    for (var j = 0, m = this.length; j < m; j++) {
	      subgroups.push(subgroup = []);
	      for (var group = this[j], i = 0, n = group.length; i < n; i++) {
	        if (node = group[i]) {
	          transition = node[ns][id0];
	          d3_transitionNode(node, i, ns, id1, {
	            time: transition.time,
	            ease: transition.ease,
	            delay: transition.delay + transition.duration,
	            duration: transition.duration
	          });
	        }
	        subgroup.push(node);
	      }
	    }
	    return d3_transition(subgroups, ns, id1);
	  };
	  function d3_transitionNamespace(name) {
	    return name == null ? "__transition__" : "__transition_" + name + "__";
	  }
	  function d3_transitionNode(node, i, ns, id, inherit) {
	    var lock = node[ns] || (node[ns] = {
	      active: 0,
	      count: 0
	    }), transition = lock[id], time, timer, duration, ease, tweens;
	    function schedule(elapsed) {
	      var delay = transition.delay;
	      timer.t = delay + time;
	      if (delay <= elapsed) return start(elapsed - delay);
	      timer.c = start;
	    }
	    function start(elapsed) {
	      var activeId = lock.active, active = lock[activeId];
	      if (active) {
	        active.timer.c = null;
	        active.timer.t = NaN;
	        --lock.count;
	        delete lock[activeId];
	        active.event && active.event.interrupt.call(node, node.__data__, active.index);
	      }
	      for (var cancelId in lock) {
	        if (+cancelId < id) {
	          var cancel = lock[cancelId];
	          cancel.timer.c = null;
	          cancel.timer.t = NaN;
	          --lock.count;
	          delete lock[cancelId];
	        }
	      }
	      timer.c = tick;
	      d3_timer(function() {
	        if (timer.c && tick(elapsed || 1)) {
	          timer.c = null;
	          timer.t = NaN;
	        }
	        return 1;
	      }, 0, time);
	      lock.active = id;
	      transition.event && transition.event.start.call(node, node.__data__, i);
	      tweens = [];
	      transition.tween.forEach(function(key, value) {
	        if (value = value.call(node, node.__data__, i)) {
	          tweens.push(value);
	        }
	      });
	      ease = transition.ease;
	      duration = transition.duration;
	    }
	    function tick(elapsed) {
	      var t = elapsed / duration, e = ease(t), n = tweens.length;
	      while (n > 0) {
	        tweens[--n].call(node, e);
	      }
	      if (t >= 1) {
	        transition.event && transition.event.end.call(node, node.__data__, i);
	        if (--lock.count) delete lock[id]; else delete node[ns];
	        return 1;
	      }
	    }
	    if (!transition) {
	      time = inherit.time;
	      timer = d3_timer(schedule, 0, time);
	      transition = lock[id] = {
	        tween: new d3_Map(),
	        time: time,
	        timer: timer,
	        delay: inherit.delay,
	        duration: inherit.duration,
	        ease: inherit.ease,
	        index: i
	      };
	      inherit = null;
	      ++lock.count;
	    }
	  }
	  d3.svg.axis = function() {
	    var scale = d3.scale.linear(), orient = d3_svg_axisDefaultOrient, innerTickSize = 6, outerTickSize = 6, tickPadding = 3, tickArguments_ = [ 10 ], tickValues = null, tickFormat_;
	    function axis(g) {
	      g.each(function() {
	        var g = d3.select(this);
	        var scale0 = this.__chart__ || scale, scale1 = this.__chart__ = scale.copy();
	        var ticks = tickValues == null ? scale1.ticks ? scale1.ticks.apply(scale1, tickArguments_) : scale1.domain() : tickValues, tickFormat = tickFormat_ == null ? scale1.tickFormat ? scale1.tickFormat.apply(scale1, tickArguments_) : d3_identity : tickFormat_, tick = g.selectAll(".tick").data(ticks, scale1), tickEnter = tick.enter().insert("g", ".domain").attr("class", "tick").style("opacity", ε), tickExit = d3.transition(tick.exit()).style("opacity", ε).remove(), tickUpdate = d3.transition(tick.order()).style("opacity", 1), tickSpacing = Math.max(innerTickSize, 0) + tickPadding, tickTransform;
	        var range = d3_scaleRange(scale1), path = g.selectAll(".domain").data([ 0 ]), pathUpdate = (path.enter().append("path").attr("class", "domain"), 
	        d3.transition(path));
	        tickEnter.append("line");
	        tickEnter.append("text");
	        var lineEnter = tickEnter.select("line"), lineUpdate = tickUpdate.select("line"), text = tick.select("text").text(tickFormat), textEnter = tickEnter.select("text"), textUpdate = tickUpdate.select("text"), sign = orient === "top" || orient === "left" ? -1 : 1, x1, x2, y1, y2;
	        if (orient === "bottom" || orient === "top") {
	          tickTransform = d3_svg_axisX, x1 = "x", y1 = "y", x2 = "x2", y2 = "y2";
	          text.attr("dy", sign < 0 ? "0em" : ".71em").style("text-anchor", "middle");
	          pathUpdate.attr("d", "M" + range[0] + "," + sign * outerTickSize + "V0H" + range[1] + "V" + sign * outerTickSize);
	        } else {
	          tickTransform = d3_svg_axisY, x1 = "y", y1 = "x", x2 = "y2", y2 = "x2";
	          text.attr("dy", ".32em").style("text-anchor", sign < 0 ? "end" : "start");
	          pathUpdate.attr("d", "M" + sign * outerTickSize + "," + range[0] + "H0V" + range[1] + "H" + sign * outerTickSize);
	        }
	        lineEnter.attr(y2, sign * innerTickSize);
	        textEnter.attr(y1, sign * tickSpacing);
	        lineUpdate.attr(x2, 0).attr(y2, sign * innerTickSize);
	        textUpdate.attr(x1, 0).attr(y1, sign * tickSpacing);
	        if (scale1.rangeBand) {
	          var x = scale1, dx = x.rangeBand() / 2;
	          scale0 = scale1 = function(d) {
	            return x(d) + dx;
	          };
	        } else if (scale0.rangeBand) {
	          scale0 = scale1;
	        } else {
	          tickExit.call(tickTransform, scale1, scale0);
	        }
	        tickEnter.call(tickTransform, scale0, scale1);
	        tickUpdate.call(tickTransform, scale1, scale1);
	      });
	    }
	    axis.scale = function(x) {
	      if (!arguments.length) return scale;
	      scale = x;
	      return axis;
	    };
	    axis.orient = function(x) {
	      if (!arguments.length) return orient;
	      orient = x in d3_svg_axisOrients ? x + "" : d3_svg_axisDefaultOrient;
	      return axis;
	    };
	    axis.ticks = function() {
	      if (!arguments.length) return tickArguments_;
	      tickArguments_ = d3_array(arguments);
	      return axis;
	    };
	    axis.tickValues = function(x) {
	      if (!arguments.length) return tickValues;
	      tickValues = x;
	      return axis;
	    };
	    axis.tickFormat = function(x) {
	      if (!arguments.length) return tickFormat_;
	      tickFormat_ = x;
	      return axis;
	    };
	    axis.tickSize = function(x) {
	      var n = arguments.length;
	      if (!n) return innerTickSize;
	      innerTickSize = +x;
	      outerTickSize = +arguments[n - 1];
	      return axis;
	    };
	    axis.innerTickSize = function(x) {
	      if (!arguments.length) return innerTickSize;
	      innerTickSize = +x;
	      return axis;
	    };
	    axis.outerTickSize = function(x) {
	      if (!arguments.length) return outerTickSize;
	      outerTickSize = +x;
	      return axis;
	    };
	    axis.tickPadding = function(x) {
	      if (!arguments.length) return tickPadding;
	      tickPadding = +x;
	      return axis;
	    };
	    axis.tickSubdivide = function() {
	      return arguments.length && axis;
	    };
	    return axis;
	  };
	  var d3_svg_axisDefaultOrient = "bottom", d3_svg_axisOrients = {
	    top: 1,
	    right: 1,
	    bottom: 1,
	    left: 1
	  };
	  function d3_svg_axisX(selection, x0, x1) {
	    selection.attr("transform", function(d) {
	      var v0 = x0(d);
	      return "translate(" + (isFinite(v0) ? v0 : x1(d)) + ",0)";
	    });
	  }
	  function d3_svg_axisY(selection, y0, y1) {
	    selection.attr("transform", function(d) {
	      var v0 = y0(d);
	      return "translate(0," + (isFinite(v0) ? v0 : y1(d)) + ")";
	    });
	  }
	  d3.svg.brush = function() {
	    var event = d3_eventDispatch(brush, "brushstart", "brush", "brushend"), x = null, y = null, xExtent = [ 0, 0 ], yExtent = [ 0, 0 ], xExtentDomain, yExtentDomain, xClamp = true, yClamp = true, resizes = d3_svg_brushResizes[0];
	    function brush(g) {
	      g.each(function() {
	        var g = d3.select(this).style("pointer-events", "all").style("-webkit-tap-highlight-color", "rgba(0,0,0,0)").on("mousedown.brush", brushstart).on("touchstart.brush", brushstart);
	        var background = g.selectAll(".background").data([ 0 ]);
	        background.enter().append("rect").attr("class", "background").style("visibility", "hidden").style("cursor", "crosshair");
	        g.selectAll(".extent").data([ 0 ]).enter().append("rect").attr("class", "extent").style("cursor", "move");
	        var resize = g.selectAll(".resize").data(resizes, d3_identity);
	        resize.exit().remove();
	        resize.enter().append("g").attr("class", function(d) {
	          return "resize " + d;
	        }).style("cursor", function(d) {
	          return d3_svg_brushCursor[d];
	        }).append("rect").attr("x", function(d) {
	          return /[ew]$/.test(d) ? -3 : null;
	        }).attr("y", function(d) {
	          return /^[ns]/.test(d) ? -3 : null;
	        }).attr("width", 6).attr("height", 6).style("visibility", "hidden");
	        resize.style("display", brush.empty() ? "none" : null);
	        var gUpdate = d3.transition(g), backgroundUpdate = d3.transition(background), range;
	        if (x) {
	          range = d3_scaleRange(x);
	          backgroundUpdate.attr("x", range[0]).attr("width", range[1] - range[0]);
	          redrawX(gUpdate);
	        }
	        if (y) {
	          range = d3_scaleRange(y);
	          backgroundUpdate.attr("y", range[0]).attr("height", range[1] - range[0]);
	          redrawY(gUpdate);
	        }
	        redraw(gUpdate);
	      });
	    }
	    brush.event = function(g) {
	      g.each(function() {
	        var event_ = event.of(this, arguments), extent1 = {
	          x: xExtent,
	          y: yExtent,
	          i: xExtentDomain,
	          j: yExtentDomain
	        }, extent0 = this.__chart__ || extent1;
	        this.__chart__ = extent1;
	        if (d3_transitionInheritId) {
	          d3.select(this).transition().each("start.brush", function() {
	            xExtentDomain = extent0.i;
	            yExtentDomain = extent0.j;
	            xExtent = extent0.x;
	            yExtent = extent0.y;
	            event_({
	              type: "brushstart"
	            });
	          }).tween("brush:brush", function() {
	            var xi = d3_interpolateArray(xExtent, extent1.x), yi = d3_interpolateArray(yExtent, extent1.y);
	            xExtentDomain = yExtentDomain = null;
	            return function(t) {
	              xExtent = extent1.x = xi(t);
	              yExtent = extent1.y = yi(t);
	              event_({
	                type: "brush",
	                mode: "resize"
	              });
	            };
	          }).each("end.brush", function() {
	            xExtentDomain = extent1.i;
	            yExtentDomain = extent1.j;
	            event_({
	              type: "brush",
	              mode: "resize"
	            });
	            event_({
	              type: "brushend"
	            });
	          });
	        } else {
	          event_({
	            type: "brushstart"
	          });
	          event_({
	            type: "brush",
	            mode: "resize"
	          });
	          event_({
	            type: "brushend"
	          });
	        }
	      });
	    };
	    function redraw(g) {
	      g.selectAll(".resize").attr("transform", function(d) {
	        return "translate(" + xExtent[+/e$/.test(d)] + "," + yExtent[+/^s/.test(d)] + ")";
	      });
	    }
	    function redrawX(g) {
	      g.select(".extent").attr("x", xExtent[0]);
	      g.selectAll(".extent,.n>rect,.s>rect").attr("width", xExtent[1] - xExtent[0]);
	    }
	    function redrawY(g) {
	      g.select(".extent").attr("y", yExtent[0]);
	      g.selectAll(".extent,.e>rect,.w>rect").attr("height", yExtent[1] - yExtent[0]);
	    }
	    function brushstart() {
	      var target = this, eventTarget = d3.select(d3.event.target), event_ = event.of(target, arguments), g = d3.select(target), resizing = eventTarget.datum(), resizingX = !/^(n|s)$/.test(resizing) && x, resizingY = !/^(e|w)$/.test(resizing) && y, dragging = eventTarget.classed("extent"), dragRestore = d3_event_dragSuppress(target), center, origin = d3.mouse(target), offset;
	      var w = d3.select(d3_window(target)).on("keydown.brush", keydown).on("keyup.brush", keyup);
	      if (d3.event.changedTouches) {
	        w.on("touchmove.brush", brushmove).on("touchend.brush", brushend);
	      } else {
	        w.on("mousemove.brush", brushmove).on("mouseup.brush", brushend);
	      }
	      g.interrupt().selectAll("*").interrupt();
	      if (dragging) {
	        origin[0] = xExtent[0] - origin[0];
	        origin[1] = yExtent[0] - origin[1];
	      } else if (resizing) {
	        var ex = +/w$/.test(resizing), ey = +/^n/.test(resizing);
	        offset = [ xExtent[1 - ex] - origin[0], yExtent[1 - ey] - origin[1] ];
	        origin[0] = xExtent[ex];
	        origin[1] = yExtent[ey];
	      } else if (d3.event.altKey) center = origin.slice();
	      g.style("pointer-events", "none").selectAll(".resize").style("display", null);
	      d3.select("body").style("cursor", eventTarget.style("cursor"));
	      event_({
	        type: "brushstart"
	      });
	      brushmove();
	      function keydown() {
	        if (d3.event.keyCode == 32) {
	          if (!dragging) {
	            center = null;
	            origin[0] -= xExtent[1];
	            origin[1] -= yExtent[1];
	            dragging = 2;
	          }
	          d3_eventPreventDefault();
	        }
	      }
	      function keyup() {
	        if (d3.event.keyCode == 32 && dragging == 2) {
	          origin[0] += xExtent[1];
	          origin[1] += yExtent[1];
	          dragging = 0;
	          d3_eventPreventDefault();
	        }
	      }
	      function brushmove() {
	        var point = d3.mouse(target), moved = false;
	        if (offset) {
	          point[0] += offset[0];
	          point[1] += offset[1];
	        }
	        if (!dragging) {
	          if (d3.event.altKey) {
	            if (!center) center = [ (xExtent[0] + xExtent[1]) / 2, (yExtent[0] + yExtent[1]) / 2 ];
	            origin[0] = xExtent[+(point[0] < center[0])];
	            origin[1] = yExtent[+(point[1] < center[1])];
	          } else center = null;
	        }
	        if (resizingX && move1(point, x, 0)) {
	          redrawX(g);
	          moved = true;
	        }
	        if (resizingY && move1(point, y, 1)) {
	          redrawY(g);
	          moved = true;
	        }
	        if (moved) {
	          redraw(g);
	          event_({
	            type: "brush",
	            mode: dragging ? "move" : "resize"
	          });
	        }
	      }
	      function move1(point, scale, i) {
	        var range = d3_scaleRange(scale), r0 = range[0], r1 = range[1], position = origin[i], extent = i ? yExtent : xExtent, size = extent[1] - extent[0], min, max;
	        if (dragging) {
	          r0 -= position;
	          r1 -= size + position;
	        }
	        min = (i ? yClamp : xClamp) ? Math.max(r0, Math.min(r1, point[i])) : point[i];
	        if (dragging) {
	          max = (min += position) + size;
	        } else {
	          if (center) position = Math.max(r0, Math.min(r1, 2 * center[i] - min));
	          if (position < min) {
	            max = min;
	            min = position;
	          } else {
	            max = position;
	          }
	        }
	        if (extent[0] != min || extent[1] != max) {
	          if (i) yExtentDomain = null; else xExtentDomain = null;
	          extent[0] = min;
	          extent[1] = max;
	          return true;
	        }
	      }
	      function brushend() {
	        brushmove();
	        g.style("pointer-events", "all").selectAll(".resize").style("display", brush.empty() ? "none" : null);
	        d3.select("body").style("cursor", null);
	        w.on("mousemove.brush", null).on("mouseup.brush", null).on("touchmove.brush", null).on("touchend.brush", null).on("keydown.brush", null).on("keyup.brush", null);
	        dragRestore();
	        event_({
	          type: "brushend"
	        });
	      }
	    }
	    brush.x = function(z) {
	      if (!arguments.length) return x;
	      x = z;
	      resizes = d3_svg_brushResizes[!x << 1 | !y];
	      return brush;
	    };
	    brush.y = function(z) {
	      if (!arguments.length) return y;
	      y = z;
	      resizes = d3_svg_brushResizes[!x << 1 | !y];
	      return brush;
	    };
	    brush.clamp = function(z) {
	      if (!arguments.length) return x && y ? [ xClamp, yClamp ] : x ? xClamp : y ? yClamp : null;
	      if (x && y) xClamp = !!z[0], yClamp = !!z[1]; else if (x) xClamp = !!z; else if (y) yClamp = !!z;
	      return brush;
	    };
	    brush.extent = function(z) {
	      var x0, x1, y0, y1, t;
	      if (!arguments.length) {
	        if (x) {
	          if (xExtentDomain) {
	            x0 = xExtentDomain[0], x1 = xExtentDomain[1];
	          } else {
	            x0 = xExtent[0], x1 = xExtent[1];
	            if (x.invert) x0 = x.invert(x0), x1 = x.invert(x1);
	            if (x1 < x0) t = x0, x0 = x1, x1 = t;
	          }
	        }
	        if (y) {
	          if (yExtentDomain) {
	            y0 = yExtentDomain[0], y1 = yExtentDomain[1];
	          } else {
	            y0 = yExtent[0], y1 = yExtent[1];
	            if (y.invert) y0 = y.invert(y0), y1 = y.invert(y1);
	            if (y1 < y0) t = y0, y0 = y1, y1 = t;
	          }
	        }
	        return x && y ? [ [ x0, y0 ], [ x1, y1 ] ] : x ? [ x0, x1 ] : y && [ y0, y1 ];
	      }
	      if (x) {
	        x0 = z[0], x1 = z[1];
	        if (y) x0 = x0[0], x1 = x1[0];
	        xExtentDomain = [ x0, x1 ];
	        if (x.invert) x0 = x(x0), x1 = x(x1);
	        if (x1 < x0) t = x0, x0 = x1, x1 = t;
	        if (x0 != xExtent[0] || x1 != xExtent[1]) xExtent = [ x0, x1 ];
	      }
	      if (y) {
	        y0 = z[0], y1 = z[1];
	        if (x) y0 = y0[1], y1 = y1[1];
	        yExtentDomain = [ y0, y1 ];
	        if (y.invert) y0 = y(y0), y1 = y(y1);
	        if (y1 < y0) t = y0, y0 = y1, y1 = t;
	        if (y0 != yExtent[0] || y1 != yExtent[1]) yExtent = [ y0, y1 ];
	      }
	      return brush;
	    };
	    brush.clear = function() {
	      if (!brush.empty()) {
	        xExtent = [ 0, 0 ], yExtent = [ 0, 0 ];
	        xExtentDomain = yExtentDomain = null;
	      }
	      return brush;
	    };
	    brush.empty = function() {
	      return !!x && xExtent[0] == xExtent[1] || !!y && yExtent[0] == yExtent[1];
	    };
	    return d3.rebind(brush, event, "on");
	  };
	  var d3_svg_brushCursor = {
	    n: "ns-resize",
	    e: "ew-resize",
	    s: "ns-resize",
	    w: "ew-resize",
	    nw: "nwse-resize",
	    ne: "nesw-resize",
	    se: "nwse-resize",
	    sw: "nesw-resize"
	  };
	  var d3_svg_brushResizes = [ [ "n", "e", "s", "w", "nw", "ne", "se", "sw" ], [ "e", "w" ], [ "n", "s" ], [] ];
	  var d3_time_format = d3_time.format = d3_locale_enUS.timeFormat;
	  var d3_time_formatUtc = d3_time_format.utc;
	  var d3_time_formatIso = d3_time_formatUtc("%Y-%m-%dT%H:%M:%S.%LZ");
	  d3_time_format.iso = Date.prototype.toISOString && +new Date("2000-01-01T00:00:00.000Z") ? d3_time_formatIsoNative : d3_time_formatIso;
	  function d3_time_formatIsoNative(date) {
	    return date.toISOString();
	  }
	  d3_time_formatIsoNative.parse = function(string) {
	    var date = new Date(string);
	    return isNaN(date) ? null : date;
	  };
	  d3_time_formatIsoNative.toString = d3_time_formatIso.toString;
	  d3_time.second = d3_time_interval(function(date) {
	    return new d3_date(Math.floor(date / 1e3) * 1e3);
	  }, function(date, offset) {
	    date.setTime(date.getTime() + Math.floor(offset) * 1e3);
	  }, function(date) {
	    return date.getSeconds();
	  });
	  d3_time.seconds = d3_time.second.range;
	  d3_time.seconds.utc = d3_time.second.utc.range;
	  d3_time.minute = d3_time_interval(function(date) {
	    return new d3_date(Math.floor(date / 6e4) * 6e4);
	  }, function(date, offset) {
	    date.setTime(date.getTime() + Math.floor(offset) * 6e4);
	  }, function(date) {
	    return date.getMinutes();
	  });
	  d3_time.minutes = d3_time.minute.range;
	  d3_time.minutes.utc = d3_time.minute.utc.range;
	  d3_time.hour = d3_time_interval(function(date) {
	    var timezone = date.getTimezoneOffset() / 60;
	    return new d3_date((Math.floor(date / 36e5 - timezone) + timezone) * 36e5);
	  }, function(date, offset) {
	    date.setTime(date.getTime() + Math.floor(offset) * 36e5);
	  }, function(date) {
	    return date.getHours();
	  });
	  d3_time.hours = d3_time.hour.range;
	  d3_time.hours.utc = d3_time.hour.utc.range;
	  d3_time.month = d3_time_interval(function(date) {
	    date = d3_time.day(date);
	    date.setDate(1);
	    return date;
	  }, function(date, offset) {
	    date.setMonth(date.getMonth() + offset);
	  }, function(date) {
	    return date.getMonth();
	  });
	  d3_time.months = d3_time.month.range;
	  d3_time.months.utc = d3_time.month.utc.range;
	  function d3_time_scale(linear, methods, format) {
	    function scale(x) {
	      return linear(x);
	    }
	    scale.invert = function(x) {
	      return d3_time_scaleDate(linear.invert(x));
	    };
	    scale.domain = function(x) {
	      if (!arguments.length) return linear.domain().map(d3_time_scaleDate);
	      linear.domain(x);
	      return scale;
	    };
	    function tickMethod(extent, count) {
	      var span = extent[1] - extent[0], target = span / count, i = d3.bisect(d3_time_scaleSteps, target);
	      return i == d3_time_scaleSteps.length ? [ methods.year, d3_scale_linearTickRange(extent.map(function(d) {
	        return d / 31536e6;
	      }), count)[2] ] : !i ? [ d3_time_scaleMilliseconds, d3_scale_linearTickRange(extent, count)[2] ] : methods[target / d3_time_scaleSteps[i - 1] < d3_time_scaleSteps[i] / target ? i - 1 : i];
	    }
	    scale.nice = function(interval, skip) {
	      var domain = scale.domain(), extent = d3_scaleExtent(domain), method = interval == null ? tickMethod(extent, 10) : typeof interval === "number" && tickMethod(extent, interval);
	      if (method) interval = method[0], skip = method[1];
	      function skipped(date) {
	        return !isNaN(date) && !interval.range(date, d3_time_scaleDate(+date + 1), skip).length;
	      }
	      return scale.domain(d3_scale_nice(domain, skip > 1 ? {
	        floor: function(date) {
	          while (skipped(date = interval.floor(date))) date = d3_time_scaleDate(date - 1);
	          return date;
	        },
	        ceil: function(date) {
	          while (skipped(date = interval.ceil(date))) date = d3_time_scaleDate(+date + 1);
	          return date;
	        }
	      } : interval));
	    };
	    scale.ticks = function(interval, skip) {
	      var extent = d3_scaleExtent(scale.domain()), method = interval == null ? tickMethod(extent, 10) : typeof interval === "number" ? tickMethod(extent, interval) : !interval.range && [ {
	        range: interval
	      }, skip ];
	      if (method) interval = method[0], skip = method[1];
	      return interval.range(extent[0], d3_time_scaleDate(+extent[1] + 1), skip < 1 ? 1 : skip);
	    };
	    scale.tickFormat = function() {
	      return format;
	    };
	    scale.copy = function() {
	      return d3_time_scale(linear.copy(), methods, format);
	    };
	    return d3_scale_linearRebind(scale, linear);
	  }
	  function d3_time_scaleDate(t) {
	    return new Date(t);
	  }
	  var d3_time_scaleSteps = [ 1e3, 5e3, 15e3, 3e4, 6e4, 3e5, 9e5, 18e5, 36e5, 108e5, 216e5, 432e5, 864e5, 1728e5, 6048e5, 2592e6, 7776e6, 31536e6 ];
	  var d3_time_scaleLocalMethods = [ [ d3_time.second, 1 ], [ d3_time.second, 5 ], [ d3_time.second, 15 ], [ d3_time.second, 30 ], [ d3_time.minute, 1 ], [ d3_time.minute, 5 ], [ d3_time.minute, 15 ], [ d3_time.minute, 30 ], [ d3_time.hour, 1 ], [ d3_time.hour, 3 ], [ d3_time.hour, 6 ], [ d3_time.hour, 12 ], [ d3_time.day, 1 ], [ d3_time.day, 2 ], [ d3_time.week, 1 ], [ d3_time.month, 1 ], [ d3_time.month, 3 ], [ d3_time.year, 1 ] ];
	  var d3_time_scaleLocalFormat = d3_time_format.multi([ [ ".%L", function(d) {
	    return d.getMilliseconds();
	  } ], [ ":%S", function(d) {
	    return d.getSeconds();
	  } ], [ "%I:%M", function(d) {
	    return d.getMinutes();
	  } ], [ "%I %p", function(d) {
	    return d.getHours();
	  } ], [ "%a %d", function(d) {
	    return d.getDay() && d.getDate() != 1;
	  } ], [ "%b %d", function(d) {
	    return d.getDate() != 1;
	  } ], [ "%B", function(d) {
	    return d.getMonth();
	  } ], [ "%Y", d3_true ] ]);
	  var d3_time_scaleMilliseconds = {
	    range: function(start, stop, step) {
	      return d3.range(Math.ceil(start / step) * step, +stop, step).map(d3_time_scaleDate);
	    },
	    floor: d3_identity,
	    ceil: d3_identity
	  };
	  d3_time_scaleLocalMethods.year = d3_time.year;
	  d3_time.scale = function() {
	    return d3_time_scale(d3.scale.linear(), d3_time_scaleLocalMethods, d3_time_scaleLocalFormat);
	  };
	  var d3_time_scaleUtcMethods = d3_time_scaleLocalMethods.map(function(m) {
	    return [ m[0].utc, m[1] ];
	  });
	  var d3_time_scaleUtcFormat = d3_time_formatUtc.multi([ [ ".%L", function(d) {
	    return d.getUTCMilliseconds();
	  } ], [ ":%S", function(d) {
	    return d.getUTCSeconds();
	  } ], [ "%I:%M", function(d) {
	    return d.getUTCMinutes();
	  } ], [ "%I %p", function(d) {
	    return d.getUTCHours();
	  } ], [ "%a %d", function(d) {
	    return d.getUTCDay() && d.getUTCDate() != 1;
	  } ], [ "%b %d", function(d) {
	    return d.getUTCDate() != 1;
	  } ], [ "%B", function(d) {
	    return d.getUTCMonth();
	  } ], [ "%Y", d3_true ] ]);
	  d3_time_scaleUtcMethods.year = d3_time.year.utc;
	  d3_time.scale.utc = function() {
	    return d3_time_scale(d3.scale.linear(), d3_time_scaleUtcMethods, d3_time_scaleUtcFormat);
	  };
	  d3.text = d3_xhrType(function(request) {
	    return request.responseText;
	  });
	  d3.json = function(url, callback) {
	    return d3_xhr(url, "application/json", d3_json, callback);
	  };
	  function d3_json(request) {
	    return JSON.parse(request.responseText);
	  }
	  d3.html = function(url, callback) {
	    return d3_xhr(url, "text/html", d3_html, callback);
	  };
	  function d3_html(request) {
	    var range = d3_document.createRange();
	    range.selectNode(d3_document.body);
	    return range.createContextualFragment(request.responseText);
	  }
	  d3.xml = d3_xhrType(function(request) {
	    return request.responseXML;
	  });
	  if (true) this.d3 = d3, !(__WEBPACK_AMD_DEFINE_FACTORY__ = (d3), __WEBPACK_AMD_DEFINE_RESULT__ = (typeof __WEBPACK_AMD_DEFINE_FACTORY__ === 'function' ? (__WEBPACK_AMD_DEFINE_FACTORY__.call(exports, __webpack_require__, exports, module)) : __WEBPACK_AMD_DEFINE_FACTORY__), __WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__)); else if (typeof module === "object" && module.exports) module.exports = d3; else this.d3 = d3;
	}();

/***/ },
/* 228 */
/***/ function(module, exports, __webpack_require__) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	/**
	 * A component for each individual Bar to render an SVG rect element.
	 */

	var _class = function (_Component) {
	  _inherits(_class, _Component);

	  function _class() {
	    _classCallCheck(this, _class);

	    return _possibleConstructorReturn(this, Object.getPrototypeOf(_class).apply(this, arguments));
	  }

	  _createClass(_class, [{
	    key: "render",
	    value: function render() {
	      var xScale = this.props.xScale;
	      var yScale = this.props.yScale;
	      var height = this.props.height;
	      var i = this.props.i;
	      var d = this.props.data;

	      //var tip = d3.tip()
	      //    .attr('class', 'd3-tip')
	      //    .offset([-10, 0])
	      //    .html(function (d) {
	      //        return "";
	      //    });

	      return _react2.default.createElement(
	        "rect",
	        {
	          className: "bar series-" + (i + 1),
	          x: xScale(d.x),
	          y: yScale(d.y + d.y0),
	          fill: this.props.fill,
	          width: xScale.rangeBand(),
	          height: height,
	          "data-x": "" + d.x.toString(),
	          "data-y": "" + d.y.toString()
	        },
	        _react2.default.createElement(
	          "title",
	          null,
	          d.title + ", " + d.x.toString() + " (" + d.y.toString() + ")"
	        )
	      );
	    }
	  }], [{
	    key: "propTypes",
	    value: function propTypes() {
	      return {
	        xScale: _react2.default.PropTypes.func.isRequired,
	        yScale: _react2.default.PropTypes.func.isRequired,
	        height: _react2.default.PropTypes.number.isRequired,
	        i: _react2.default.PropTypes.number.isRequired,
	        data: _react2.default.PropTypes.shape({
	          x: _react2.default.PropTypes.isRequired,
	          y: _react2.default.PropTypes.isRequired
	        }).isRequired
	      };
	    }
	  }]);

	  return _class;
	}(_react.Component);

	exports.default = _class;

/***/ },
/* 229 */
/***/ function(module, exports, __webpack_require__) {

	var Element = __webpack_require__(230)
	var Window = __webpack_require__(242)

	var ReactFauxDOM = {
	  Element: Element,
	  defaultView: Window,
	  createElement: function (nodeName) {
	    return new Element(nodeName)
	  },
	  createElementNS: function (namespace, nodeName) {
	    return this.createElement(nodeName)
	  },
	  compareDocumentPosition: function () {
	    // The selector engine tries to validate with this, but we don't care.
	    // 8 = DOCUMENT_POSITION_CONTAINS, so we say all nodes are in this document.
	    return 8
	  }
	}

	Element.prototype.ownerDocument = ReactFauxDOM

	module.exports = ReactFauxDOM


/***/ },
/* 230 */
/***/ function(module, exports, __webpack_require__) {

	var React = __webpack_require__(4)
	var styleAttr = __webpack_require__(231)
	var querySelectorAll = __webpack_require__(232)
	var camelCase = __webpack_require__(236)
	var isString = __webpack_require__(237)
	var isUndefined = __webpack_require__(238)
	var assign = __webpack_require__(239)
	var mapValues = __webpack_require__(240)
	var styleCamelCase = __webpack_require__(241)

	function Element (nodeName, parentNode) {
	  this.nodeName = nodeName
	  this.parentNode = parentNode
	  this.childNodes = []
	  this.eventListeners = {}
	  this.text = ''
	  var props = this.props = {
	    style: {
	      setProperty: function (name, value) {
	        props.style[styleCamelCase(name)] = value
	      },
	      getProperty: function (name) {
	        return props.style[styleCamelCase(name)]
	      },
	      removeProperty: function (name) {
	        delete props.style[styleCamelCase(name)]
	      }
	    }
	  }

	  this.style = props.style
	}

	Element.prototype.nodeType = 1

	// This was easy to do with Vim.
	// Just saying.
	Element.prototype.eventNameMappings = {
	  'blur': 'onBlur',
	  'change': 'onChange',
	  'click': 'onClick',
	  'contextmenu': 'onContextMenu',
	  'copy': 'onCopy',
	  'cut': 'onCut',
	  'doubleclick': 'onDoubleClick',
	  'drag': 'onDrag',
	  'dragend': 'onDragEnd',
	  'dragenter': 'onDragEnter',
	  'dragexit': 'onDragExit',
	  'dragleave': 'onDragLeave',
	  'dragover': 'onDragOver',
	  'dragstart': 'onDragStart',
	  'drop': 'onDrop',
	  'error': 'onError',
	  'focus': 'onFocus',
	  'input': 'onInput',
	  'keydown': 'onKeyDown',
	  'keypress': 'onKeyPress',
	  'keyup': 'onKeyUp',
	  'load': 'onLoad',
	  'mousedown': 'onMouseDown',
	  'mouseenter': 'onMouseEnter',
	  'mouseleave': 'onMouseLeave',
	  'mousemove': 'onMouseMove',
	  'mouseout': 'onMouseOut',
	  'mouseover': 'onMouseOver',
	  'mouseup': 'onMouseUp',
	  'paste': 'onPaste',
	  'scroll': 'onScroll',
	  'submit': 'onSubmit',
	  'touchcancel': 'onTouchCancel',
	  'touchend': 'onTouchEnd',
	  'touchmove': 'onTouchMove',
	  'touchstart': 'onTouchStart',
	  'wheel': 'onWheel'
	}

	Element.prototype.skipNameTransformationExpressions = [
	  /^data-/,
	  /^aria-/
	]

	Element.prototype.attributeNameMappings = {
	  'class': 'className'
	}

	Element.prototype.attributeToPropName = function (name) {
	  var skipTransformMatches = this.skipNameTransformationExpressions.map(function (expr) {
	    return expr.test(name)
	  })

	  if (skipTransformMatches.some(Boolean)) {
	    return name
	  } else {
	    return this.attributeNameMappings[name] || camelCase(name)
	  }
	}

	Element.prototype.setAttribute = function (name, value) {
	  if (name === 'style' && isString(value)) {
	    var styles = styleAttr.parse(value)

	    for (var key in styles) {
	      this.style.setProperty(key, styles[key])
	    }
	  } else {
	    this.props[this.attributeToPropName(name)] = value
	  }
	}

	Element.prototype.getAttribute = function (name) {
	  return this.props[this.attributeToPropName(name)]
	}

	Element.prototype.getAttributeNode = function (name) {
	  var value = this.getAttribute(name)

	  if (!isUndefined(value)) {
	    return {
	      value: value,
	      specified: true
	    }
	  }
	}

	Element.prototype.removeAttribute = function (name) {
	  delete this.props[this.attributeToPropName(name)]
	}

	Element.prototype.eventToPropName = function (name) {
	  return this.eventNameMappings[name] || name
	}

	Element.prototype.addEventListener = function (name, fn) {
	  var prop = this.eventToPropName(name)
	  this.eventListeners[prop] = this.eventListeners[prop] || []
	  this.eventListeners[prop].push(fn)
	}

	Element.prototype.removeEventListener = function (name, fn) {
	  var listeners = this.eventListeners[this.eventToPropName(name)]

	  if (listeners) {
	    var match = listeners.indexOf(fn)

	    if (match !== -1) {
	      listeners.splice(match, 1)
	    }
	  }
	}

	Element.prototype.appendChild = function (el) {
	  el.parentNode = this
	  this.childNodes.push(el)
	  return el
	}

	Element.prototype.insertBefore = function (el, before) {
	  var index = this.childNodes.indexOf(before)
	  el.parentNode = this

	  if (index !== -1) {
	    this.childNodes.splice(index, 0, el)
	  } else {
	    this.childNodes.push(el)
	  }

	  return el
	}

	Element.prototype.removeChild = function (child) {
	  var target = this.childNodes.indexOf(child)
	  this.childNodes.splice(target, 1)
	}

	Element.prototype.querySelector = function () {
	  return this.querySelectorAll.apply(this, arguments)[0] || null
	}

	Element.prototype.querySelectorAll = function (selector) {
	  if (!selector) {
	    throw new Error('Not enough arguments')
	  }

	  return querySelectorAll(selector, this)
	}

	Element.prototype.getElementsByTagName = function (nodeName) {
	  var children = this.children

	  if (children.length === 0) {
	    return []
	  } else {
	    var matches

	    if (nodeName !== '*') {
	      matches = children.filter(function (el) {
	        return el.nodeName === nodeName
	      })
	    } else {
	      matches = children
	    }

	    var childMatches = children.map(function (el) {
	      return el.getElementsByTagName(nodeName)
	    })

	    return matches.concat.apply(matches, childMatches)
	  }
	}

	Element.prototype.getElementById = function (id) {
	  var children = this.children

	  if (children.length === 0) {
	    return null
	  } else {
	    var match = children.filter(function (el) {
	      return el.getAttribute('id') === id
	    })[0]

	    if (match) {
	      return match
	    } else {
	      var childMatches = children.map(function (el) {
	        return el.getElementById(id)
	      })

	      return childMatches.filter(function (match) {
	        return match !== null
	      })[0] || null
	    }
	  }
	}

	Element.prototype.toReact = function (index) {
	  index = index || 0
	  var props = assign({}, this.props)
	  props.style = assign({}, props.style)

	  var originalElement = this

	  function uniqueKey () {
	    return 'faux-dom-' + index
	  }

	  if (isUndefined(props.key)) {
	    props.key = uniqueKey()
	  }

	  delete props.style.setProperty
	  delete props.style.getProperty
	  delete props.style.removeProperty

	  assign(props, mapValues(this.eventListeners, function (listeners) {
	    return function (syntheticEvent) {
	      var event

	      if (syntheticEvent) {
	        event = syntheticEvent.nativeEvent
	        event.syntheticEvent = syntheticEvent
	      }

	      mapValues(listeners, function (listener) {
	        listener.call(originalElement, event)
	      })
	    }
	  }))

	  return React.createElement(this.nodeName, props, this.text || this.children.map(function (el, i) {
	    if (el instanceof Element) {
	      return el.toReact(i)
	    } else {
	      return el
	    }
	  }))
	}

	Object.defineProperties(Element.prototype, {
	  nextSibling: {
	    get: function () {
	      var siblings = this.parentNode.children
	      var me = siblings.indexOf(this)
	      return siblings[me + 1]
	    }
	  },
	  previousSibling: {
	    get: function () {
	      var siblings = this.parentNode.children
	      var me = siblings.indexOf(this)
	      return siblings[me - 1]
	    }
	  },
	  innerHTML: {
	    get: function () {
	      return this.text
	    },
	    set: function (text) {
	      this.text = text
	    }
	  },
	  textContent: {
	    get: function () {
	      return this.text
	    },
	    set: function (text) {
	      this.text = text
	    }
	  },
	  children: {
	    get: function () {
	      // So far nodes created by this library are all of nodeType 1 (elements),
	      // but this could change in the future.
	      return this.childNodes.filter(function (el) {
	        if (!el.nodeType) {
	          // It's a React element, we always add it
	          return true
	        }

	        // It's a HTML node. We want to filter to have only nodes with type 1
	        return el.nodeType === 1
	      })
	    }
	  }
	})

	// These NS methods are called by things like D3 if it spots a namespace.
	// Like xlink:href. I don't care about namespaces, so these functions have NS aliases created.
	var namespaceMethods = [
	  'setAttribute',
	  'getAttribute',
	  'getAttributeNode',
	  'removeAttribute',
	  'getElementsByTagName',
	  'getElementById'
	]

	namespaceMethods.forEach(function (name) {
	  var fn = Element.prototype[name]
	  Element.prototype[name + 'NS'] = function () {
	    return fn.apply(this, Array.prototype.slice.call(arguments, 1))
	  }
	})

	module.exports = Element


/***/ },
/* 231 */
/***/ function(module, exports) {

	

	/*

	style-attr
	====

	Very simple parsing and stringifying of style attributes.

	`parse`
	----

	Convert a style attribute string to an object.

	*/
	function parse(raw) {
	  var trim = function (s) {
	    return s.trim();
	  };
	  var obj = {};

	  getKeyValueChunks(raw).map(trim).filter(Boolean).forEach(function (item) {
	    // split with `.indexOf` rather than `.split` because the value may also contain colons.
	    var pos = item.indexOf(':');
	    var key = item.substr(0, pos).trim();
	    var val = item.substr(pos + 1).trim();

	    obj[key] = val;
	  });

	  return obj;
	}

	/*

	`getKeyValueChunks`
	----

	Split a string into chunks matching `<key>: <value>`

	*/
	function getKeyValueChunks(raw) {
	  var chunks = [];
	  var offset = 0;
	  var sep = ';';
	  var hasUnclosedUrl = /url\([^\)]+$/;
	  var chunk = '';
	  var nextSplit;
	  while (offset < raw.length) {
	    nextSplit = raw.indexOf(sep, offset);
	    if (nextSplit === -1) {
	      nextSplit = raw.length;
	    }

	    chunk += raw.substring(offset, nextSplit);

	    // data URIs can contain semicolons, so make sure we get the whole thing
	    if (hasUnclosedUrl.test(chunk)) {
	      chunk += ';';
	      offset = nextSplit + 1;
	      continue;
	    }

	    chunks.push(chunk);
	    chunk = '';
	    offset = nextSplit + 1;
	  }

	  return chunks;
	}

	/*

	`stringify`
	----

	Convert an object into an attribute string

	*/
	function stringify(obj) {
	  return Object.keys(obj).map(function (key) {
	    return key + ':' + obj[key];
	  }).join(';');
	}

	/*

	`normalize`
	----

	Normalize an attribute string (eg. collapse duplicates)

	*/
	function normalize(str) {
	  return stringify(parse(str));
	}

	module.exports.parse = parse;
	module.exports.stringify = stringify;
	module.exports.normalize = normalize;

/***/ },
/* 232 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__(233);

/***/ },
/* 233 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * @ignore
	 * css3 selector engine for ie6-8
	 * @author yiminghe@gmail.com
	 */

	var util = __webpack_require__(234);
	var parser = __webpack_require__(235);

	var EXPANDO_SELECTOR_KEY = '_ks_data_selector_id_',
	  caches = {},
	  isContextXML,
	  uuid = 0,
	  subMatchesCache = {},
	  getAttr = function (el, name) {
	    if (isContextXML) {
	      return util.getSimpleAttr(el, name);
	    } else {
	      return util.attr(el, name);
	    }
	  },
	  hasSingleClass = util.hasSingleClass,
	  isTag = util.isTag,
	  aNPlusB = /^(([+-]?(?:\d+)?)?n)?([+-]?\d+)?$/;

	// CSS escapes http://www.w3.org/TR/CSS21/syndata.html#escaped-characters
	var unescape = /\\([\da-fA-F]{1,6}[\x20\t\r\n\f]?|.)/g,
	  unescapeFn = function (_, escaped) {
	    var high = '0x' + escaped - 0x10000;
	    // NaN means non-codepoint
	    return isNaN(high) ?
	      escaped :
	      // BMP codepoint
	      high < 0 ?
	        String.fromCharCode(high + 0x10000) :
	        // Supplemental Plane codepoint (surrogate pair)
	        String.fromCharCode(high >> 10 | 0xD800, high & 0x3FF | 0xDC00);
	  };

	var matchExpr;

	var pseudoFnExpr = {
	  'nth-child': function (el, param) {
	    var ab = getAb(param),
	      a = ab.a,
	      b = ab.b;
	    if (a === 0 && b === 0) {
	      return 0;
	    }
	    var index = 0,
	      parent = el.parentNode;
	    if (parent) {
	      var childNodes = parent.childNodes,
	        count = 0,
	        child,
	        ret,
	        len = childNodes.length;
	      for (; count < len; count++) {
	        child = childNodes[count];
	        if (child.nodeType === 1) {
	          index++;
	          ret = matchIndexByAb(index, a, b, child === el);
	          if (ret !== undefined) {
	            return ret;
	          }
	        }
	      }
	    }
	    return 0;
	  },
	  'nth-last-child': function (el, param) {
	    var ab = getAb(param),
	      a = ab.a,
	      b = ab.b;
	    if (a === 0 && b === 0) {
	      return 0;
	    }
	    var index = 0,
	      parent = el.parentNode;
	    if (parent) {
	      var childNodes = parent.childNodes,
	        len = childNodes.length,
	        count = len - 1,
	        child,
	        ret;
	      for (; count >= 0; count--) {
	        child = childNodes[count];
	        if (child.nodeType === 1) {
	          index++;
	          ret = matchIndexByAb(index, a, b, child === el);
	          if (ret !== undefined) {
	            return ret;
	          }
	        }
	      }
	    }
	    return 0;
	  },
	  'nth-of-type': function (el, param) {
	    var ab = getAb(param),
	      a = ab.a,
	      b = ab.b;
	    if (a === 0 && b === 0) {
	      return 0;
	    }
	    var index = 0,
	      parent = el.parentNode;
	    if (parent) {
	      var childNodes = parent.childNodes,
	        elType = el.tagName,
	        count = 0,
	        child,
	        ret,
	        len = childNodes.length;
	      for (; count < len; count++) {
	        child = childNodes[count];
	        if (child.tagName === elType) {
	          index++;
	          ret = matchIndexByAb(index, a, b, child === el);
	          if (ret !== undefined) {
	            return ret;
	          }
	        }
	      }
	    }
	    return 0;
	  },
	  'nth-last-of-type': function (el, param) {
	    var ab = getAb(param),
	      a = ab.a,
	      b = ab.b;
	    if (a === 0 && b === 0) {
	      return 0;
	    }
	    var index = 0,
	      parent = el.parentNode;
	    if (parent) {
	      var childNodes = parent.childNodes,
	        len = childNodes.length,
	        elType = el.tagName,
	        count = len - 1,
	        child,
	        ret;
	      for (; count >= 0; count--) {
	        child = childNodes[count];
	        if (child.tagName === elType) {
	          index++;
	          ret = matchIndexByAb(index, a, b, child === el);
	          if (ret !== undefined) {
	            return ret;
	          }
	        }
	      }
	    }
	    return 0;
	  },
	  lang: function (el, lang) {
	    var elLang;
	    lang = unEscape(lang.toLowerCase());
	    do {
	      if ((elLang = (isContextXML ?
	        el.getAttribute('xml:lang') || el.getAttribute('lang') :
	          el.lang))) {
	        elLang = elLang.toLowerCase();
	        return elLang === lang || elLang.indexOf(lang + '-') === 0;
	      }
	    } while ((el = el.parentNode) && el.nodeType === 1);
	    return false;
	  },
	  not: function (el, negationArg) {
	    return !matchExpr[negationArg.t](el, negationArg.value);
	  }
	};

	var pseudoIdentExpr = {
	  empty: function (el) {
	    var childNodes = el.childNodes,
	      index = 0,
	      len = childNodes.length - 1,
	      child,
	      nodeType;
	    for (; index < len; index++) {
	      child = childNodes[index];
	      nodeType = child.nodeType;
	      // only element nodes and content nodes
	      // (such as Dom [Dom-LEVEL-3-CORE] text nodes,
	      // CDATA nodes, and entity references
	      if (nodeType === 1 || nodeType === 3 || nodeType === 4 || nodeType === 5) {
	        return 0;
	      }
	    }
	    return 1;
	  },
	  root: function (el) {
	    if (el.nodeType === 9) {
	      return true;
	    }
	    return el.ownerDocument &&
	      el === el.ownerDocument.documentElement;
	  },
	  'first-child': function (el) {
	    return pseudoFnExpr['nth-child'](el, 1);
	  },
	  'last-child': function (el) {
	    return pseudoFnExpr['nth-last-child'](el, 1);
	  },
	  'first-of-type': function (el) {
	    return pseudoFnExpr['nth-of-type'](el, 1);
	  },
	  'last-of-type': function (el) {
	    return pseudoFnExpr['nth-last-of-type'](el, 1);
	  },
	  'only-child': function (el) {
	    return pseudoIdentExpr['first-child'](el) &&
	      pseudoIdentExpr['last-child'](el);
	  },
	  'only-of-type': function (el) {
	    return pseudoIdentExpr['first-of-type'](el) &&
	      pseudoIdentExpr['last-of-type'](el);
	  },
	  focus: function (el) {
	    var doc = el.ownerDocument;
	    return doc && el === doc.activeElement &&
	      (!doc.hasFocus || doc.hasFocus()) && !!(el.type || el.href || el.tabIndex >= 0);
	  },
	  target: function (el) {
	    var hash = location.hash;
	    return hash && hash.slice(1) === getAttr(el, 'id');
	  },
	  enabled: function (el) {
	    return !el.disabled;
	  },
	  disabled: function (el) {
	    return el.disabled;
	  },
	  checked: function (el) {
	    var nodeName = el.nodeName.toLowerCase();
	    return (nodeName === 'input' && el.checked) ||
	      (nodeName === 'option' && el.selected);
	  }
	};

	var attributeExpr = {
	  '~=': function (elValue, value) {
	    if (!value || value.indexOf(' ') > -1) {
	      return 0;
	    }
	    return (' ' + elValue + ' ').indexOf(' ' + value + ' ') !== -1;
	  },
	  '|=': function (elValue, value) {
	    return (' ' + elValue).indexOf(' ' + value + '-') !== -1;
	  },
	  '^=': function (elValue, value) {
	    return value && util.startsWith(elValue, value);
	  },
	  '$=': function (elValue, value) {
	    return value && util.endsWith(elValue, value);
	  },
	  '*=': function (elValue, value) {
	    return value && elValue.indexOf(value) !== -1;
	  },
	  '=': function (elValue, value) {
	    return elValue === value;
	  }
	};

	var relativeExpr = {
	  '>': {
	    dir: 'parentNode',
	    immediate: 1
	  },
	  ' ': {
	    dir: 'parentNode'
	  },
	  '+': {
	    dir: 'previousSibling',
	    immediate: 1
	  },
	  '~': {
	    dir: 'previousSibling'
	  }
	};

	matchExpr = {
	  tag: isTag,
	  cls: hasSingleClass,
	  id: function (el, value) {
	    return getAttr(el, 'id') === value;
	  },
	  attrib: function (el, value) {
	    var name = value.ident;
	    if (!isContextXML) {
	      name = name.toLowerCase();
	    }
	    var elValue = getAttr(el, name);
	    var match = value.match;
	    if (!match && elValue !== undefined) {
	      return 1;
	    } else if (match) {
	      if (elValue === undefined) {
	        return 0;
	      }
	      var matchFn = attributeExpr[match];
	      if (matchFn) {
	        return matchFn(elValue + '', value.value + '');
	      }
	    }
	    return 0;
	  },
	  pseudo: function (el, value) {
	    var fn, fnStr, ident;
	    if ((fnStr = value.fn)) {
	      if (!(fn = pseudoFnExpr[fnStr])) {
	        throw new SyntaxError('Syntax error: not support pseudo: ' + fnStr);
	      }
	      return fn(el, value.param);
	    }
	    if ((ident = value.ident)) {
	      if (!pseudoIdentExpr[ident]) {
	        throw new SyntaxError('Syntax error: not support pseudo: ' + ident);
	      }
	      return pseudoIdentExpr[ident](el);
	    }
	    return 0;
	  }
	};

	function unEscape(str) {
	  return str.replace(unescape, unescapeFn);
	}

	parser.lexer.yy = {
	  trim: util.trim,
	  unEscape: unEscape,
	  unEscapeStr: function (str) {
	    return this.unEscape(str.slice(1, -1));
	  }
	};

	function resetStatus() {
	  subMatchesCache = {};
	}

	function dir(el, direction) {
	  do {
	    el = el[direction];
	  } while (el && el.nodeType !== 1);
	  return el;
	}

	function getAb(param) {
	  var a = 0,
	    match,
	    b = 0;
	  if (typeof param === 'number') {
	    b = param;
	  } else if (param === 'odd') {
	    a = 2;
	    b = 1;
	  } else if (param === 'even') {
	    a = 2;
	    b = 0;
	  } else if ((match = param.replace(/\s/g, '').match(aNPlusB))) {
	    if (match[1]) {
	      a = parseInt(match[2], 10);
	      if (isNaN(a)) {
	        if (match[2] === '-') {
	          a = -1;
	        } else {
	          a = 1;
	        }
	      }
	    } else {
	      a = 0;
	    }
	    b = parseInt(match[3], 10) || 0;
	  }
	  return {
	    a: a,
	    b: b
	  };
	}

	function matchIndexByAb(index, a, b, eq) {
	  if (a === 0) {
	    if (index === b) {
	      return eq;
	    }
	  } else {
	    if ((index - b) / a >= 0 && (index - b) % a === 0 && eq) {
	      return 1;
	    }
	  }
	  return undefined;
	}

	function isXML(elem) {
	  var documentElement = elem && (elem.ownerDocument || elem).documentElement;
	  return documentElement ? documentElement.nodeName.toLowerCase() !== 'html' : false;
	}

	function matches(str, seeds) {
	  return select(str, null, seeds);
	}

	function singleMatch(el, match) {
	  if (!match) {
	    return true;
	  }
	  if (!el) {
	    return false;
	  }

	  if (el.nodeType === 9) {
	    return false;
	  }

	  var matched = 1,
	    matchSuffix = match.suffix,
	    matchSuffixLen,
	    matchSuffixIndex;

	  if (match.t === 'tag') {
	    matched &= matchExpr.tag(el, match.value);
	  }

	  if (matched && matchSuffix) {
	    matchSuffixLen = matchSuffix.length;
	    matchSuffixIndex = 0;
	    for (; matched && matchSuffixIndex < matchSuffixLen; matchSuffixIndex++) {
	      var singleMatchSuffix = matchSuffix[matchSuffixIndex],
	        singleMatchSuffixType = singleMatchSuffix.t;
	      if (matchExpr[singleMatchSuffixType]) {
	        matched &= matchExpr[singleMatchSuffixType](el, singleMatchSuffix.value);
	      }
	    }
	  }

	  return matched;
	}

	// match by adjacent immediate single selector match
	function matchImmediate(el, match) {
	  var matched = 1,
	    startEl = el,
	    relativeOp,
	    startMatch = match;

	  do {
	    matched &= singleMatch(el, match);
	    if (matched) {
	      // advance
	      match = match && match.prev;
	      if (!match) {
	        return true;
	      }
	      relativeOp = relativeExpr[match.nextCombinator];
	      el = dir(el, relativeOp.dir);
	      if (!relativeOp.immediate) {
	        return {
	          // advance for non-immediate
	          el: el,
	          match: match
	        };
	      }
	    } else {
	      relativeOp = relativeExpr[match.nextCombinator];
	      if (relativeOp.immediate) {
	        // retreat but advance startEl
	        return {
	          el: dir(startEl, relativeExpr[startMatch.nextCombinator].dir),
	          match: startMatch
	        };
	      } else {
	        // advance (before immediate match + jump unmatched)
	        return {
	          el: el && dir(el, relativeOp.dir),
	          match: match
	        };
	      }
	    }
	  } while (el);

	  // only occur when match immediate
	  return {
	    el: dir(startEl, relativeExpr[startMatch.nextCombinator].dir),
	    match: startMatch
	  };
	}

	// find fixed part, fixed with seeds
	function findFixedMatchFromHead(el, head) {
	  var relativeOp,
	    cur = head;

	  do {
	    if (!singleMatch(el, cur)) {
	      return null;
	    }
	    cur = cur.prev;
	    if (!cur) {
	      return true;
	    }
	    relativeOp = relativeExpr[cur.nextCombinator];
	    el = dir(el, relativeOp.dir);
	  } while (el && relativeOp.immediate);
	  if (!el) {
	    return null;
	  }
	  return {
	    el: el,
	    match: cur
	  };
	}

	function genId(el) {
	  var selectorId;

	  if (isContextXML) {
	    if (!(selectorId = el.getAttribute(EXPANDO_SELECTOR_KEY))) {
	      el.setAttribute(EXPANDO_SELECTOR_KEY, selectorId = (+new Date() + '_' + (++uuid)));
	    }
	  } else {
	    if (!(selectorId = el[EXPANDO_SELECTOR_KEY])) {
	      selectorId = el[EXPANDO_SELECTOR_KEY] = (+new Date()) + '_' + (++uuid);
	    }
	  }

	  return selectorId;
	}

	function matchSub(el, match) {
	  var selectorId = genId(el),
	    matchKey;
	  matchKey = selectorId + '_' + (match.order || 0);
	  if (matchKey in subMatchesCache) {
	    return subMatchesCache[matchKey];
	  }
	  subMatchesCache[matchKey] = matchSubInternal(el, match);
	  return subMatchesCache[matchKey];
	}

	// recursive match by sub selector string from right to left
	// grouped by immediate selectors
	function matchSubInternal(el, match) {
	  var matchImmediateRet = matchImmediate(el, match);
	  if (matchImmediateRet === true) {
	    return true;
	  } else {
	    el = matchImmediateRet.el;
	    match = matchImmediateRet.match;
	    while (el) {
	      if (matchSub(el, match)) {
	        return true;
	      }
	      el = dir(el, relativeExpr[match.nextCombinator].dir);
	    }
	    return false;
	  }
	}

	function select(str, context, seeds) {
	  if (!caches[str]) {
	    caches[str] = parser.parse(str);
	  }

	  var selector = caches[str],
	    groupIndex = 0,
	    groupLen = selector.length,
	    contextDocument,
	    group,
	    ret = [];

	  if (seeds) {
	    context = context || seeds[0].ownerDocument;
	  }

	  contextDocument = context && context.ownerDocument || typeof document !== 'undefined' && document;

	  if (context && context.nodeType === 9 && !contextDocument) {
	    contextDocument = context;
	  }

	  context = context || contextDocument;

	  isContextXML = isXML(context);

	  for (; groupIndex < groupLen; groupIndex++) {
	    resetStatus();

	    group = selector[groupIndex];

	    var suffix = group.suffix,
	      suffixIndex,
	      suffixLen,
	      seedsIndex,
	      mySeeds = seeds,
	      seedsLen,
	      id = null;

	    if (!mySeeds) {
	      if (suffix && !isContextXML) {
	        suffixIndex = 0;
	        suffixLen = suffix.length;
	        for (; suffixIndex < suffixLen; suffixIndex++) {
	          var singleSuffix = suffix[suffixIndex];
	          if (singleSuffix.t === 'id') {
	            id = singleSuffix.value;
	            break;
	          }
	        }
	      }

	      if (id) {
	        // http://yiminghe.github.io/lab/playground/fragment-selector/selector.html
	        var doesNotHasById = !context.getElementById,
	          contextInDom = util.contains(contextDocument, context),
	          tmp = doesNotHasById ? (
	            contextInDom ?
	              contextDocument.getElementById(id) :
	              null
	          ) : context.getElementById(id);
	        // id bug
	        // https://github.com/kissyteam/kissy/issues/67
	        if (!tmp && doesNotHasById || tmp && getAttr(tmp, 'id') !== id) {
	          var tmps = util.getElementsByTagName('*', context),
	            tmpLen = tmps.length,
	            tmpI = 0;
	          for (; tmpI < tmpLen; tmpI++) {
	            tmp = tmps[tmpI];
	            if (getAttr(tmp, 'id') === id) {
	              mySeeds = [tmp];
	              break;
	            }
	          }
	          if (tmpI === tmpLen) {
	            mySeeds = [];
	          }
	        } else {
	          if (contextInDom && tmp && context !== contextDocument) {
	            tmp = util.contains(context, tmp) ? tmp : null;
	          }
	          mySeeds = tmp ? [tmp] : [];
	        }
	      } else {
	        mySeeds = util.getElementsByTagName(group.value || '*', context);
	      }
	    }

	    seedsIndex = 0;
	    seedsLen = mySeeds.length;

	    if (!seedsLen) {
	      continue;
	    }

	    for (; seedsIndex < seedsLen; seedsIndex++) {
	      var seed = mySeeds[seedsIndex];
	      var matchHead = findFixedMatchFromHead(seed, group);
	      if (matchHead === true) {
	        ret.push(seed);
	      } else if (matchHead) {
	        if (matchSub(matchHead.el, matchHead.match)) {
	          ret.push(seed);
	        }
	      }
	    }
	  }

	  if (groupLen > 1) {
	    ret = util.unique(ret);
	  }

	  return ret;
	}

	module.exports = select;

	select.parse = function (str) {
	  return parser.parse(str);
	};

	select.matches = matches;

	select.util = util;

	select.version = '@VERSION@';
	/**
	 * @ignore
	 * note 2013-03-28
	 *  - use recursive call to replace backtracking algorithm
	 *
	 * refer
	 *  - http://www.w3.org/TR/selectors/
	 *  - http://www.impressivewebs.com/browser-support-css3-selectors/
	 *  - http://blogs.msdn.com/ie/archive/2010/05/13/the-css-corner-css3-selectors.aspx
	 *  - http://sizzlejs.com/
	 */

/***/ },
/* 234 */
/***/ function(module, exports) {

	/**
	 * attr fix for old ie
	 * @author yiminghe@gmail.com
	 */
	var R_BOOLEAN = /^(?:autofocus|autoplay|async|checked|controls|defer|disabled|hidden|loop|multiple|open|readonly|required|scoped|selected)$/i,
	  R_FOCUSABLE = /^(?:button|input|object|select|textarea)$/i,
	  R_CLICKABLE = /^a(?:rea)?$/i,
	  R_INVALID_CHAR = /:|^on/;

	var attrFix = {},
	  propFix,
	  attrHooks = {
	    // http://fluidproject.org/blog/2008/01/09/getting-setting-and-removing-tabindex-values-with-javascript/
	    tabindex: {
	      get: function (el) {
	        // elem.tabIndex doesn't always return the correct value when it hasn't been explicitly set
	        var attributeNode = el.getAttributeNode('tabindex');
	        return attributeNode && attributeNode.specified ?
	          parseInt(attributeNode.value, 10) :
	          R_FOCUSABLE.test(el.nodeName) ||
	          R_CLICKABLE.test(el.nodeName) && el.href ?
	            0 :
	            undefined;
	      }
	    }
	  },
	  boolHook = {
	    get: function (elem, name) {
	      // 转发到 prop 方法
	      return elem[propFix[name] || name] ?
	        // 根据 w3c attribute , true 时返回属性名字符串
	        name.toLowerCase() :
	        undefined;
	    }
	  },
	  attrNodeHook = {};

	attrHooks.style = {
	  get: function (el) {
	    return el.style.cssText;
	  }
	};

	propFix = {
	  hidefocus: 'hideFocus',
	  tabindex: 'tabIndex',
	  readonly: 'readOnly',
	  'for': 'htmlFor',
	  'class': 'className',
	  maxlength: 'maxLength',
	  cellspacing: 'cellSpacing',
	  cellpadding: 'cellPadding',
	  rowspan: 'rowSpan',
	  colspan: 'colSpan',
	  usemap: 'useMap',
	  frameborder: 'frameBorder',
	  contenteditable: 'contentEditable'
	};

	var ua = typeof navigator !== 'undefined' ? navigator.userAgent : '';
	var doc = typeof document !== 'undefined' ? document : {};

	function numberify(s) {
	  var c = 0;
	  // convert '1.2.3.4' to 1.234
	  return parseFloat(s.replace(/\./g, function () {
	    return (c++ === 0) ? '.' : '';
	  }));
	}

	function ieVersion() {
	  var m, v;
	  if ((m = ua.match(/MSIE ([^;]*)|Trident.*; rv(?:\s|:)?([0-9.]+)/)) &&
	    (v = (m[1] || m[2]))) {
	    return doc.documentMode || numberify(v);
	  }
	}

	function mix(s, t) {
	  for (var p in t) {
	    s[p] = t[p];
	  }
	}

	function each(arr, fn) {
	  var i = 0, l = arr.length;
	  for (; i < l; i++) {
	    if (fn(arr[i], i) === false) {
	      break;
	    }
	  }
	}
	var ie = ieVersion();

	if (ie && ie < 8) {
	  attrHooks.style.set = function (el, val) {
	    el.style.cssText = val;
	  };

	  // get attribute value from attribute node for ie
	  mix(attrNodeHook, {
	    get: function (elem, name) {
	      var ret = elem.getAttributeNode(name);
	      // Return undefined if attribute node specified by user
	      return ret && (
	        // fix #100
	      ret.specified || ret.nodeValue) ?
	        ret.nodeValue :
	        undefined;
	    }
	  });

	  // ie6,7 不区分 attribute 与 property
	  mix(attrFix, propFix);

	  // http://fluidproject.org/blog/2008/01/09/getting-setting-and-removing-tabindex-values-with-javascript/
	  attrHooks.tabIndex = attrHooks.tabindex;

	  // 不光是 href, src, 还有 rowspan 等非 mapping 属性，也需要用第 2 个参数来获取原始值
	  // 注意 colSpan rowSpan 已经由 propFix 转为大写
	  each(['href', 'src', 'width', 'height', 'colSpan', 'rowSpan'], function (name) {
	    attrHooks[name] = {
	      get: function (elem) {
	        var ret = elem.getAttribute(name, 2);
	        return ret === null ? undefined : ret;
	      }
	    };
	  });

	  attrHooks.placeholder = {
	    get: function (elem, name) {
	      return elem[name] || attrNodeHook.get(elem, name);
	    }
	  };
	}

	if (ie) {
	  var hrefFix = attrHooks.href = attrHooks.href || {};
	  hrefFix.set = function (el, val, name) {
	    var childNodes = el.childNodes,
	      b,
	      len = childNodes.length,
	      allText = len > 0;
	    for (len = len - 1; len >= 0; len--) {
	      if (childNodes[len].nodeType !== 3) {
	        allText = 0;
	      }
	    }
	    if (allText) {
	      b = el.ownerDocument.createElement('b');
	      b.style.display = 'none';
	      el.appendChild(b);
	    }
	    el.setAttribute(name, '' + val);
	    if (b) {
	      el.removeChild(b);
	    }
	  };
	}

	var RE_TRIM = /^[\s\xa0]+|[\s\xa0]+$/g,
	  trim = String.prototype.trim;
	var SPACE = ' ';

	var getElementsByTagName;
	getElementsByTagName = function (name, context) {
	  return context.getElementsByTagName(name);
	};

	if (doc.createElement) {
	  var div = doc.createElement('div');
	  div.appendChild(document.createComment(''));
	  if (div.getElementsByTagName('*').length) {
	    getElementsByTagName = function (name, context) {
	      var nodes = context.getElementsByTagName(name),
	        needsFilter = name === '*';
	      // <input id='length'>
	      if (needsFilter || typeof nodes.length !== 'number') {
	        var ret = [],
	          i = 0,
	          el;
	        while ((el = nodes[i++])) {
	          if (!needsFilter || el.nodeType === 1) {
	            ret.push(el);
	          }
	        }
	        return ret;
	      } else {
	        return nodes;
	      }
	    };
	  }
	}

	var compareNodeOrder = ('sourceIndex' in (doc && doc.documentElement || {})) ? function (a, b) {
	  return a.sourceIndex - b.sourceIndex;
	} : function (a, b) {
	  if (!a.compareDocumentPosition || !b.compareDocumentPosition) {
	    return a.compareDocumentPosition ? -1 : 1;
	  }
	  var bit = a.compareDocumentPosition(b) & 4;
	  return bit ? -1 : 1;
	};

	var util = module.exports = {
	  ie: ie,

	  unique: (function () {
	    var hasDuplicate,
	      baseHasDuplicate = true;

	    // Here we check if the JavaScript engine is using some sort of
	    // optimization where it does not always call our comparison
	    // function. If that is the case, discard the hasDuplicate value.
	    // Thus far that includes Google Chrome.
	    [0, 0].sort(function () {
	      baseHasDuplicate = false;
	      return 0;
	    });

	    function sortOrder(a, b) {
	      if (a === b) {
	        hasDuplicate = true;
	        return 0;
	      }

	      return compareNodeOrder(a, b);
	    }

	    // 排序去重
	    return function (elements) {
	      hasDuplicate = baseHasDuplicate;
	      elements.sort(sortOrder);

	      if (hasDuplicate) {
	        var i = 1, len = elements.length;
	        while (i < len) {
	          if (elements[i] === elements[i - 1]) {
	            elements.splice(i, 1);
	            --len;
	          } else {
	            i++;
	          }
	        }
	      }
	      return elements;
	    };
	  })(),

	  getElementsByTagName: getElementsByTagName,

	  getSimpleAttr: function (el, name) {
	    var ret = el && el.getAttributeNode(name);
	    if (ret && ret.specified) {
	      return 'value' in ret ? ret.value : ret.nodeValue;
	    }
	    return undefined;
	  },

	  contains: ie ? function (a, b) {
	    if (a.nodeType === 9) {
	      a = a.documentElement;
	    }
	    // !a.contains => a===document || text
	    // 注意原生 contains 判断时 a===b 也返回 true
	    b = b.parentNode;

	    if (a === b) {
	      return true;
	    }

	    // when b is document, a.contains(b) 不支持的接口 in ie
	    if (b && b.nodeType === 1) {
	      return a.contains && a.contains(b);
	    } else {
	      return false;
	    }
	  } : function (a, b) {
	    return !!(a.compareDocumentPosition(b) & 16);
	  },

	  isTag: function (el, value) {
	    return value === '*' || el.nodeName.toLowerCase() === value.toLowerCase();
	  },

	  hasSingleClass: function (el, cls) {
	    // consider xml
	    // https://github.com/kissyteam/kissy/issues/532
	    var className = el && util.getSimpleAttr(el, 'class');
	    return className && (className = className.replace(/[\r\t\n]/g, SPACE)) &&
	      (SPACE + className + SPACE).indexOf(SPACE + cls + SPACE) > -1;
	  },

	  startsWith: function (str, prefix) {
	    return str.lastIndexOf(prefix, 0) === 0;
	  },

	  endsWith: function (str, suffix) {
	    var ind = str.length - suffix.length;
	    return ind >= 0 && str.indexOf(suffix, ind) === ind;
	  },

	  trim: trim ?
	    function (str) {
	      return str == null ? '' : trim.call(str);
	    } :
	    function (str) {
	      return str == null ? '' : (str + '').replace(RE_TRIM, '');
	    },

	  attr: function (el, name) {
	    var attrNormalizer, ret;
	    // scrollLeft
	    name = name.toLowerCase();
	    // custom attrs
	    name = attrFix[name] || name;
	    if (R_BOOLEAN.test(name)) {
	      attrNormalizer = boolHook;
	    } else if (R_INVALID_CHAR.test(name)) {
	      // only old ie?
	      attrNormalizer = attrNodeHook;
	    } else {
	      attrNormalizer = attrHooks[name];
	    }
	    if (el && el.nodeType === 1) {
	      // browsers index elements by id/name on forms, give priority to attributes.
	      if (el.nodeName.toLowerCase() === 'form') {
	        attrNormalizer = attrNodeHook;
	      }
	      if (attrNormalizer && attrNormalizer.get) {
	        return attrNormalizer.get(el, name);
	      }
	      ret = el.getAttribute(name);
	      if (ret === '') {
	        var attrNode = el.getAttributeNode(name);
	        if (!attrNode || !attrNode.specified) {
	          return undefined;
	        }
	      }
	      // standard browser non-existing attribute return null
	      // ie<8 will return undefined , because it return property
	      // so norm to undefined
	      return ret === null ? undefined : ret;
	    }
	  }
	};

/***/ },
/* 235 */
/***/ function(module, exports, __webpack_require__) {

	/*
	  Generated by kison.*/
	var parser = (function (undefined) {
	    /*jshint quotmark:false, loopfunc:true, indent:false, unused:false, asi:true, boss:true*/
	    /* Generated by kison */
	    var parser = {},
	        GrammarConst = {
	            'SHIFT_TYPE': 1,
	            'REDUCE_TYPE': 2,
	            'ACCEPT_TYPE': 0,
	            'TYPE_INDEX': 0,
	            'PRODUCTION_INDEX': 1,
	            'TO_INDEX': 2
	        };
	    /*jslint quotmark: false*/
	    function mix(to, from) {
	        for (var f in from) {
	            to[f] = from[f];
	        }
	    }

	    function isArray(obj) {
	        return '[object Array]' === Object.prototype.toString.call(obj);
	    }

	    function each(object, fn, context) {
	        if (object) {
	            var key,
	                val,
	                length,
	                i = 0;

	            context = context || null;

	            if (!isArray(object)) {
	                for (key in object) {
	                    // can not use hasOwnProperty
	                    if (fn.call(context, object[key], key, object) === false) {
	                        break;
	                    }
	                }
	            } else {
	                length = object.length;
	                for (val = object[0]; i < length; val = object[++i]) {
	                    if (fn.call(context, val, i, object) === false) {
	                        break;
	                    }
	                }
	            }
	        }
	    }

	    function inArray(item, arr) {
	        for (var i = 0, l = arr.length; i < l; i++) {
	            if (arr[i] === item) {
	                return true;
	            }
	        }
	        return false;
	    }
	    var Lexer = function Lexer(cfg) {

	        var self = this;

	        /*
	     lex rules.
	     @type {Object[]}
	     @example
	     [
	     {
	     regexp:'\\w+',
	     state:['xx'],
	     token:'c',
	     // this => lex
	     action:function(){}
	     }
	     ]
	     */
	        self.rules = [];

	        mix(self, cfg);

	        /*
	     Input languages
	     @type {String}
	     */

	        self.resetInput(self.input);
	    };
	    Lexer.prototype = {
	        'resetInput': function (input) {
	            mix(this, {
	                input: input,
	                matched: '',
	                stateStack: [Lexer.STATIC.INITIAL],
	                match: '',
	                text: '',
	                firstLine: 1,
	                lineNumber: 1,
	                lastLine: 1,
	                firstColumn: 1,
	                lastColumn: 1
	            });
	        },
	        'getCurrentRules': function () {
	            var self = this,
	                currentState = self.stateStack[self.stateStack.length - 1],
	                rules = [];
	            //#JSCOVERAGE_IF
	            if (self.mapState) {
	                currentState = self.mapState(currentState);
	            }
	            each(self.rules, function (r) {
	                var state = r.state || r[3];
	                if (!state) {
	                    if (currentState === Lexer.STATIC.INITIAL) {
	                        rules.push(r);
	                    }
	                } else if (inArray(currentState, state)) {
	                    rules.push(r);
	                }
	            });
	            return rules;
	        },
	        'pushState': function (state) {
	            this.stateStack.push(state);
	        },
	        'popState': function (num) {
	            num = num || 1;
	            var ret;
	            while (num--) {
	                ret = this.stateStack.pop();
	            }
	            return ret;
	        },
	        'showDebugInfo': function () {
	            var self = this,
	                DEBUG_CONTEXT_LIMIT = Lexer.STATIC.DEBUG_CONTEXT_LIMIT,
	                matched = self.matched,
	                match = self.match,
	                input = self.input;
	            matched = matched.slice(0, matched.length - match.length);
	            //#JSCOVERAGE_IF 0
	            var past = (matched.length > DEBUG_CONTEXT_LIMIT ? '...' : '') +
	                matched.slice(0 - DEBUG_CONTEXT_LIMIT).replace(/\n/, ' '),
	                next = match + input;
	            //#JSCOVERAGE_ENDIF
	            next = next.slice(0, DEBUG_CONTEXT_LIMIT) +
	                (next.length > DEBUG_CONTEXT_LIMIT ? '...' : '');
	            return past + next + '\n' + new Array(past.length + 1).join('-') + '^';
	        },
	        'mapSymbol': function mapSymbolForCodeGen(t) {
	            return this.symbolMap[t];
	        },
	        'mapReverseSymbol': function (rs) {
	            var self = this,
	                symbolMap = self.symbolMap,
	                i,
	                reverseSymbolMap = self.reverseSymbolMap;
	            if (!reverseSymbolMap && symbolMap) {
	                reverseSymbolMap = self.reverseSymbolMap = {};
	                for (i in symbolMap) {
	                    reverseSymbolMap[symbolMap[i]] = i;
	                }
	            }
	            //#JSCOVERAGE_IF
	            if (reverseSymbolMap) {
	                return reverseSymbolMap[rs];
	            } else {
	                return rs;
	            }
	        },
	        'lex': function () {
	            var self = this,
	                input = self.input,
	                i,
	                rule,
	                m,
	                ret,
	                lines,
	                rules = self.getCurrentRules();

	            self.match = self.text = '';

	            if (!input) {
	                return self.mapSymbol(Lexer.STATIC.END_TAG);
	            }

	            for (i = 0; i < rules.length; i++) {
	                rule = rules[i];
	                //#JSCOVERAGE_IF 0
	                var regexp = rule.regexp || rule[1],
	                    token = rule.token || rule[0],
	                    action = rule.action || rule[2] || undefined;
	                //#JSCOVERAGE_ENDIF
	                if ((m = input.match(regexp))) {
	                    lines = m[0].match(/\n.*/g);
	                    if (lines) {
	                        self.lineNumber += lines.length;
	                    }
	                    mix(self, {
	                        firstLine: self.lastLine,
	                        lastLine: self.lineNumber + 1,
	                        firstColumn: self.lastColumn,
	                        lastColumn: lines ?
	                            lines[lines.length - 1].length - 1 : self.lastColumn + m[0].length
	                    });
	                    var match;
	                    // for error report
	                    match = self.match = m[0];

	                    // all matches
	                    self.matches = m;
	                    // may change by user
	                    self.text = match;
	                    // matched content utils now
	                    self.matched += match;
	                    ret = action && action.call(self);
	                    if (ret === undefined) {
	                        ret = token;
	                    } else {
	                        ret = self.mapSymbol(ret);
	                    }
	                    input = input.slice(match.length);
	                    self.input = input;

	                    if (ret) {
	                        return ret;
	                    } else {
	                        // ignore
	                        return self.lex();
	                    }
	                }
	            }
	        }
	    };
	    Lexer.STATIC = {
	        'INITIAL': 'I',
	        'DEBUG_CONTEXT_LIMIT': 20,
	        'END_TAG': '$EOF'
	    };
	    var lexer = new Lexer({
	        'rules': [
	            ['b', /^\[(?:[\t\r\n\f\x20]*)/,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['c', /^(?:[\t\r\n\f\x20]*)\]/,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['d', /^(?:[\t\r\n\f\x20]*)~=(?:[\t\r\n\f\x20]*)/,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['e', /^(?:[\t\r\n\f\x20]*)\|=(?:[\t\r\n\f\x20]*)/,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['f', /^(?:[\t\r\n\f\x20]*)\^=(?:[\t\r\n\f\x20]*)/,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['g', /^(?:[\t\r\n\f\x20]*)\$=(?:[\t\r\n\f\x20]*)/,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['h', /^(?:[\t\r\n\f\x20]*)\*=(?:[\t\r\n\f\x20]*)/,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['i', /^(?:[\t\r\n\f\x20]*)\=(?:[\t\r\n\f\x20]*)/,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['j', /^(?:(?:[\w]|[^\x00-\xa0]|(?:\\[^\n\r\f0-9a-f]))(?:[\w\d-]|[^\x00-\xa0]|(?:\\[^\n\r\f0-9a-f]))*)\(/,
	                function () {
	                    this.text = this.yy.trim(this.text).slice(0, -1);
	                    this.pushState('fn');
	                }
	            ],
	            ['k', /^[^\)]*/,
	                function () {
	                    this.popState();
	                },
	                ['fn']
	            ],
	            ['l', /^(?:[\t\r\n\f\x20]*)\)/,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['m', /^:not\((?:[\t\r\n\f\x20]*)/i,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['n', /^(?:(?:[\w]|[^\x00-\xa0]|(?:\\[^\n\r\f0-9a-f]))(?:[\w\d-]|[^\x00-\xa0]|(?:\\[^\n\r\f0-9a-f]))*)/,
	                function () {
	                    this.text = this.yy.unEscape(this.text);
	                }
	            ],
	            ['o', /^"(\\"|[^"])*"/,
	                function () {
	                    this.text = this.yy.unEscapeStr(this.text);
	                }
	            ],
	            ['o', /^'(\\'|[^'])*'/,
	                function () {
	                    this.text = this.yy.unEscapeStr(this.text);
	                }
	            ],
	            ['p', /^#(?:(?:[\w\d-]|[^\x00-\xa0]|(?:\\[^\n\r\f0-9a-f]))+)/,
	                function () {
	                    this.text = this.yy.unEscape(this.text.slice(1));
	                }
	            ],
	            ['q', /^\.(?:(?:[\w]|[^\x00-\xa0]|(?:\\[^\n\r\f0-9a-f]))(?:[\w\d-]|[^\x00-\xa0]|(?:\\[^\n\r\f0-9a-f]))*)/,
	                function () {
	                    this.text = this.yy.unEscape(this.text.slice(1));
	                }
	            ],
	            ['r', /^(?:[\t\r\n\f\x20]*),(?:[\t\r\n\f\x20]*)/,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['s', /^::?/, 0],
	            ['t', /^(?:[\t\r\n\f\x20]*)\+(?:[\t\r\n\f\x20]*)/,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['u', /^(?:[\t\r\n\f\x20]*)>(?:[\t\r\n\f\x20]*)/,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['v', /^(?:[\t\r\n\f\x20]*)~(?:[\t\r\n\f\x20]*)/,
	                function () {
	                    this.text = this.yy.trim(this.text);
	                }
	            ],
	            ['w', /^\*/, 0],
	            ['x', /^(?:[\t\r\n\f\x20]+)/, 0],
	            ['y', /^./, 0]
	        ]
	    });
	    parser.lexer = lexer;
	    lexer.symbolMap = {
	        '$EOF': 'a',
	        'LEFT_BRACKET': 'b',
	        'RIGHT_BRACKET': 'c',
	        'INCLUDES': 'd',
	        'DASH_MATCH': 'e',
	        'PREFIX_MATCH': 'f',
	        'SUFFIX_MATCH': 'g',
	        'SUBSTRING_MATCH': 'h',
	        'ALL_MATCH': 'i',
	        'FUNCTION': 'j',
	        'PARAMETER': 'k',
	        'RIGHT_PARENTHESES': 'l',
	        'NOT': 'm',
	        'IDENT': 'n',
	        'STRING': 'o',
	        'HASH': 'p',
	        'CLASS': 'q',
	        'COMMA': 'r',
	        'COLON': 's',
	        'PLUS': 't',
	        'GREATER': 'u',
	        'TILDE': 'v',
	        'UNIVERSAL': 'w',
	        'S': 'x',
	        'INVALID': 'y',
	        '$START': 'z',
	        'selectors_group': 'aa',
	        'selector': 'ab',
	        'simple_selector_sequence': 'ac',
	        'combinator': 'ad',
	        'type_selector': 'ae',
	        'id_selector': 'af',
	        'class_selector': 'ag',
	        'attrib_match': 'ah',
	        'attrib': 'ai',
	        'attrib_val': 'aj',
	        'pseudo': 'ak',
	        'negation': 'al',
	        'negation_arg': 'am',
	        'suffix_selector': 'an',
	        'suffix_selectors': 'ao'
	    };
	    parser.productions = [
	        ['z', ['aa']],
	        ['aa', ['ab'],
	            function () {
	                return [this.$1];
	            }
	        ],
	        ['aa', ['aa', 'r', 'ab'],
	            function () {
	                this.$1.push(this.$3);
	            }
	        ],
	        ['ab', ['ac']],
	        ['ab', ['ab', 'ad', 'ac'],
	            function () {
	                // LinkedList

	                this.$1.nextCombinator = this.$3.prevCombinator = this.$2;
	                var order;
	                order = this.$1.order = this.$1.order || 0;
	                this.$3.order = order + 1;
	                this.$3.prev = this.$1;
	                this.$1.next = this.$3;
	                return this.$3;
	            }
	        ],
	        ['ad', ['t']],
	        ['ad', ['u']],
	        ['ad', ['v']],
	        ['ad', ['x'],
	            function () {
	                return ' ';
	            }
	        ],
	        ['ae', ['n'],
	            function () {
	                return {
	                    t: 'tag',
	                    value: this.$1
	                };
	            }
	        ],
	        ['ae', ['w'],
	            function () {
	                return {
	                    t: 'tag',
	                    value: this.$1
	                };
	            }
	        ],
	        ['af', ['p'],
	            function () {
	                return {
	                    t: 'id',
	                    value: this.$1
	                };
	            }
	        ],
	        ['ag', ['q'],
	            function () {
	                return {
	                    t: 'cls',
	                    value: this.$1
	                };
	            }
	        ],
	        ['ah', ['f']],
	        ['ah', ['g']],
	        ['ah', ['h']],
	        ['ah', ['i']],
	        ['ah', ['d']],
	        ['ah', ['e']],
	        ['ai', ['b', 'n', 'c'],
	            function () {
	                return {
	                    t: 'attrib',
	                    value: {
	                        ident: this.$2
	                    }
	                };
	            }
	        ],
	        ['aj', ['n']],
	        ['aj', ['o']],
	        ['ai', ['b', 'n', 'ah', 'aj', 'c'],
	            function () {
	                return {
	                    t: 'attrib',
	                    value: {
	                        ident: this.$2,
	                        match: this.$3,
	                        value: this.$4
	                    }
	                };
	            }
	        ],
	        ['ak', ['s', 'j', 'k', 'l'],
	            function () {
	                return {
	                    t: 'pseudo',
	                    value: {
	                        fn: this.$2.toLowerCase(),
	                        param: this.$3
	                    }
	                };
	            }
	        ],
	        ['ak', ['s', 'n'],
	            function () {
	                return {
	                    t: 'pseudo',
	                    value: {
	                        ident: this.$2.toLowerCase()
	                    }
	                };
	            }
	        ],
	        ['al', ['m', 'am', 'l'],
	            function () {
	                return {
	                    t: 'pseudo',
	                    value: {
	                        fn: 'not',
	                        param: this.$2
	                    }
	                };
	            }
	        ],
	        ['am', ['ae']],
	        ['am', ['af']],
	        ['am', ['ag']],
	        ['am', ['ai']],
	        ['am', ['ak']],
	        ['an', ['af']],
	        ['an', ['ag']],
	        ['an', ['ai']],
	        ['an', ['ak']],
	        ['an', ['al']],
	        ['ao', ['an'],
	            function () {
	                return [this.$1];
	            }
	        ],
	        ['ao', ['ao', 'an'],
	            function () {
	                this.$1.push(this.$2);
	            }
	        ],
	        ['ac', ['ae']],
	        ['ac', ['ao'],
	            function () {
	                return {
	                    suffix: this.$1
	                };
	            }
	        ],
	        ['ac', ['ae', 'ao'],
	            function () {
	                return {
	                    t: 'tag',
	                    value: this.$1.value,
	                    suffix: this.$2
	                };
	            }
	        ]
	    ];
	    parser.table = {
	        'gotos': {
	            '0': {
	                'aa': 8,
	                'ab': 9,
	                'ae': 10,
	                'af': 11,
	                'ag': 12,
	                'ai': 13,
	                'ak': 14,
	                'al': 15,
	                'an': 16,
	                'ao': 17,
	                'ac': 18
	            },
	            '2': {
	                'ae': 20,
	                'af': 21,
	                'ag': 22,
	                'ai': 23,
	                'ak': 24,
	                'am': 25
	            },
	            '9': {
	                'ad': 33
	            },
	            '10': {
	                'af': 11,
	                'ag': 12,
	                'ai': 13,
	                'ak': 14,
	                'al': 15,
	                'an': 16,
	                'ao': 34
	            },
	            '17': {
	                'af': 11,
	                'ag': 12,
	                'ai': 13,
	                'ak': 14,
	                'al': 15,
	                'an': 35
	            },
	            '19': {
	                'ah': 43
	            },
	            '28': {
	                'ab': 46,
	                'ae': 10,
	                'af': 11,
	                'ag': 12,
	                'ai': 13,
	                'ak': 14,
	                'al': 15,
	                'an': 16,
	                'ao': 17,
	                'ac': 18
	            },
	            '33': {
	                'ae': 10,
	                'af': 11,
	                'ag': 12,
	                'ai': 13,
	                'ak': 14,
	                'al': 15,
	                'an': 16,
	                'ao': 17,
	                'ac': 47
	            },
	            '34': {
	                'af': 11,
	                'ag': 12,
	                'ai': 13,
	                'ak': 14,
	                'al': 15,
	                'an': 35
	            },
	            '43': {
	                'aj': 50
	            },
	            '46': {
	                'ad': 33
	            }
	        },
	        'action': {
	            '0': {
	                'b': [1, undefined, 1],
	                'm': [1, undefined, 2],
	                'n': [1, undefined, 3],
	                'p': [1, undefined, 4],
	                'q': [1, undefined, 5],
	                's': [1, undefined, 6],
	                'w': [1, undefined, 7]
	            },
	            '1': {
	                'n': [1, undefined, 19]
	            },
	            '2': {
	                'b': [1, undefined, 1],
	                'n': [1, undefined, 3],
	                'p': [1, undefined, 4],
	                'q': [1, undefined, 5],
	                's': [1, undefined, 6],
	                'w': [1, undefined, 7]
	            },
	            '3': {
	                'a': [2, 9],
	                'r': [2, 9],
	                't': [2, 9],
	                'u': [2, 9],
	                'v': [2, 9],
	                'x': [2, 9],
	                'p': [2, 9],
	                'q': [2, 9],
	                'b': [2, 9],
	                's': [2, 9],
	                'm': [2, 9],
	                'l': [2, 9]
	            },
	            '4': {
	                'a': [2, 11],
	                'r': [2, 11],
	                't': [2, 11],
	                'u': [2, 11],
	                'v': [2, 11],
	                'x': [2, 11],
	                'p': [2, 11],
	                'q': [2, 11],
	                'b': [2, 11],
	                's': [2, 11],
	                'm': [2, 11],
	                'l': [2, 11]
	            },
	            '5': {
	                'a': [2, 12],
	                'r': [2, 12],
	                't': [2, 12],
	                'u': [2, 12],
	                'v': [2, 12],
	                'x': [2, 12],
	                'p': [2, 12],
	                'q': [2, 12],
	                'b': [2, 12],
	                's': [2, 12],
	                'm': [2, 12],
	                'l': [2, 12]
	            },
	            '6': {
	                'j': [1, undefined, 26],
	                'n': [1, undefined, 27]
	            },
	            '7': {
	                'a': [2, 10],
	                'r': [2, 10],
	                't': [2, 10],
	                'u': [2, 10],
	                'v': [2, 10],
	                'x': [2, 10],
	                'p': [2, 10],
	                'q': [2, 10],
	                'b': [2, 10],
	                's': [2, 10],
	                'm': [2, 10],
	                'l': [2, 10]
	            },
	            '8': {
	                'a': [0],
	                'r': [1, undefined, 28]
	            },
	            '9': {
	                'a': [2, 1],
	                'r': [2, 1],
	                't': [1, undefined, 29],
	                'u': [1, undefined, 30],
	                'v': [1, undefined, 31],
	                'x': [1, undefined, 32]
	            },
	            '10': {
	                'a': [2, 38],
	                'r': [2, 38],
	                't': [2, 38],
	                'u': [2, 38],
	                'v': [2, 38],
	                'x': [2, 38],
	                'b': [1, undefined, 1],
	                'm': [1, undefined, 2],
	                'p': [1, undefined, 4],
	                'q': [1, undefined, 5],
	                's': [1, undefined, 6]
	            },
	            '11': {
	                'a': [2, 31],
	                'r': [2, 31],
	                't': [2, 31],
	                'u': [2, 31],
	                'v': [2, 31],
	                'x': [2, 31],
	                'p': [2, 31],
	                'q': [2, 31],
	                'b': [2, 31],
	                's': [2, 31],
	                'm': [2, 31]
	            },
	            '12': {
	                'a': [2, 32],
	                'r': [2, 32],
	                't': [2, 32],
	                'u': [2, 32],
	                'v': [2, 32],
	                'x': [2, 32],
	                'p': [2, 32],
	                'q': [2, 32],
	                'b': [2, 32],
	                's': [2, 32],
	                'm': [2, 32]
	            },
	            '13': {
	                'a': [2, 33],
	                'r': [2, 33],
	                't': [2, 33],
	                'u': [2, 33],
	                'v': [2, 33],
	                'x': [2, 33],
	                'p': [2, 33],
	                'q': [2, 33],
	                'b': [2, 33],
	                's': [2, 33],
	                'm': [2, 33]
	            },
	            '14': {
	                'a': [2, 34],
	                'r': [2, 34],
	                't': [2, 34],
	                'u': [2, 34],
	                'v': [2, 34],
	                'x': [2, 34],
	                'p': [2, 34],
	                'q': [2, 34],
	                'b': [2, 34],
	                's': [2, 34],
	                'm': [2, 34]
	            },
	            '15': {
	                'a': [2, 35],
	                'r': [2, 35],
	                't': [2, 35],
	                'u': [2, 35],
	                'v': [2, 35],
	                'x': [2, 35],
	                'p': [2, 35],
	                'q': [2, 35],
	                'b': [2, 35],
	                's': [2, 35],
	                'm': [2, 35]
	            },
	            '16': {
	                'a': [2, 36],
	                'r': [2, 36],
	                't': [2, 36],
	                'u': [2, 36],
	                'v': [2, 36],
	                'x': [2, 36],
	                'p': [2, 36],
	                'q': [2, 36],
	                'b': [2, 36],
	                's': [2, 36],
	                'm': [2, 36]
	            },
	            '17': {
	                'a': [2, 39],
	                'r': [2, 39],
	                't': [2, 39],
	                'u': [2, 39],
	                'v': [2, 39],
	                'x': [2, 39],
	                'b': [1, undefined, 1],
	                'm': [1, undefined, 2],
	                'p': [1, undefined, 4],
	                'q': [1, undefined, 5],
	                's': [1, undefined, 6]
	            },
	            '18': {
	                'a': [2, 3],
	                'r': [2, 3],
	                't': [2, 3],
	                'u': [2, 3],
	                'v': [2, 3],
	                'x': [2, 3]
	            },
	            '19': {
	                'c': [1, undefined, 36],
	                'd': [1, undefined, 37],
	                'e': [1, undefined, 38],
	                'f': [1, undefined, 39],
	                'g': [1, undefined, 40],
	                'h': [1, undefined, 41],
	                'i': [1, undefined, 42]
	            },
	            '20': {
	                'l': [2, 26]
	            },
	            '21': {
	                'l': [2, 27]
	            },
	            '22': {
	                'l': [2, 28]
	            },
	            '23': {
	                'l': [2, 29]
	            },
	            '24': {
	                'l': [2, 30]
	            },
	            '25': {
	                'l': [1, undefined, 44]
	            },
	            '26': {
	                'k': [1, undefined, 45]
	            },
	            '27': {
	                'a': [2, 24],
	                'r': [2, 24],
	                't': [2, 24],
	                'u': [2, 24],
	                'v': [2, 24],
	                'x': [2, 24],
	                'p': [2, 24],
	                'q': [2, 24],
	                'b': [2, 24],
	                's': [2, 24],
	                'm': [2, 24],
	                'l': [2, 24]
	            },
	            '28': {
	                'b': [1, undefined, 1],
	                'm': [1, undefined, 2],
	                'n': [1, undefined, 3],
	                'p': [1, undefined, 4],
	                'q': [1, undefined, 5],
	                's': [1, undefined, 6],
	                'w': [1, undefined, 7]
	            },
	            '29': {
	                'n': [2, 5],
	                'w': [2, 5],
	                'p': [2, 5],
	                'q': [2, 5],
	                'b': [2, 5],
	                's': [2, 5],
	                'm': [2, 5]
	            },
	            '30': {
	                'n': [2, 6],
	                'w': [2, 6],
	                'p': [2, 6],
	                'q': [2, 6],
	                'b': [2, 6],
	                's': [2, 6],
	                'm': [2, 6]
	            },
	            '31': {
	                'n': [2, 7],
	                'w': [2, 7],
	                'p': [2, 7],
	                'q': [2, 7],
	                'b': [2, 7],
	                's': [2, 7],
	                'm': [2, 7]
	            },
	            '32': {
	                'n': [2, 8],
	                'w': [2, 8],
	                'p': [2, 8],
	                'q': [2, 8],
	                'b': [2, 8],
	                's': [2, 8],
	                'm': [2, 8]
	            },
	            '33': {
	                'b': [1, undefined, 1],
	                'm': [1, undefined, 2],
	                'n': [1, undefined, 3],
	                'p': [1, undefined, 4],
	                'q': [1, undefined, 5],
	                's': [1, undefined, 6],
	                'w': [1, undefined, 7]
	            },
	            '34': {
	                'a': [2, 40],
	                'r': [2, 40],
	                't': [2, 40],
	                'u': [2, 40],
	                'v': [2, 40],
	                'x': [2, 40],
	                'b': [1, undefined, 1],
	                'm': [1, undefined, 2],
	                'p': [1, undefined, 4],
	                'q': [1, undefined, 5],
	                's': [1, undefined, 6]
	            },
	            '35': {
	                'a': [2, 37],
	                'r': [2, 37],
	                't': [2, 37],
	                'u': [2, 37],
	                'v': [2, 37],
	                'x': [2, 37],
	                'p': [2, 37],
	                'q': [2, 37],
	                'b': [2, 37],
	                's': [2, 37],
	                'm': [2, 37]
	            },
	            '36': {
	                'a': [2, 19],
	                'r': [2, 19],
	                't': [2, 19],
	                'u': [2, 19],
	                'v': [2, 19],
	                'x': [2, 19],
	                'p': [2, 19],
	                'q': [2, 19],
	                'b': [2, 19],
	                's': [2, 19],
	                'm': [2, 19],
	                'l': [2, 19]
	            },
	            '37': {
	                'n': [2, 17],
	                'o': [2, 17]
	            },
	            '38': {
	                'n': [2, 18],
	                'o': [2, 18]
	            },
	            '39': {
	                'n': [2, 13],
	                'o': [2, 13]
	            },
	            '40': {
	                'n': [2, 14],
	                'o': [2, 14]
	            },
	            '41': {
	                'n': [2, 15],
	                'o': [2, 15]
	            },
	            '42': {
	                'n': [2, 16],
	                'o': [2, 16]
	            },
	            '43': {
	                'n': [1, undefined, 48],
	                'o': [1, undefined, 49]
	            },
	            '44': {
	                'a': [2, 25],
	                'r': [2, 25],
	                't': [2, 25],
	                'u': [2, 25],
	                'v': [2, 25],
	                'x': [2, 25],
	                'p': [2, 25],
	                'q': [2, 25],
	                'b': [2, 25],
	                's': [2, 25],
	                'm': [2, 25]
	            },
	            '45': {
	                'l': [1, undefined, 51]
	            },
	            '46': {
	                'a': [2, 2],
	                'r': [2, 2],
	                't': [1, undefined, 29],
	                'u': [1, undefined, 30],
	                'v': [1, undefined, 31],
	                'x': [1, undefined, 32]
	            },
	            '47': {
	                'a': [2, 4],
	                'r': [2, 4],
	                't': [2, 4],
	                'u': [2, 4],
	                'v': [2, 4],
	                'x': [2, 4]
	            },
	            '48': {
	                'c': [2, 20]
	            },
	            '49': {
	                'c': [2, 21]
	            },
	            '50': {
	                'c': [1, undefined, 52]
	            },
	            '51': {
	                'a': [2, 23],
	                'r': [2, 23],
	                't': [2, 23],
	                'u': [2, 23],
	                'v': [2, 23],
	                'x': [2, 23],
	                'p': [2, 23],
	                'q': [2, 23],
	                'b': [2, 23],
	                's': [2, 23],
	                'm': [2, 23],
	                'l': [2, 23]
	            },
	            '52': {
	                'a': [2, 22],
	                'r': [2, 22],
	                't': [2, 22],
	                'u': [2, 22],
	                'v': [2, 22],
	                'x': [2, 22],
	                'p': [2, 22],
	                'q': [2, 22],
	                'b': [2, 22],
	                's': [2, 22],
	                'm': [2, 22],
	                'l': [2, 22]
	            }
	        }
	    };
	    parser.parse = function parse(input, filename) {
	        var self = this,
	            lexer = self.lexer,
	            state,
	            symbol,
	            action,
	            table = self.table,
	            gotos = table.gotos,
	            tableAction = table.action,
	            productions = self.productions,
	            valueStack = [null],
	            // for debug info
	            prefix = filename ? ('in file: ' + filename + ' ') : '',
	            stack = [0];

	        lexer.resetInput(input);

	        while (1) {
	            // retrieve state number from top of stack
	            state = stack[stack.length - 1];

	            if (!symbol) {
	                symbol = lexer.lex();
	            }

	            if (symbol) {
	                // read action for current state and first input
	                action = tableAction[state] && tableAction[state][symbol];
	            } else {
	                action = null;
	            }

	            if (!action) {
	                var expected = [],
	                    error;
	                //#JSCOVERAGE_IF
	                if (tableAction[state]) {
	                    for (var symbolForState in tableAction[state]) {
	                        expected.push(self.lexer.mapReverseSymbol(symbolForState));
	                    }
	                }
	                error = prefix + 'syntax error at line ' + lexer.lineNumber +
	                    ':\n' + lexer.showDebugInfo() +
	                    '\n' + 'expect ' + expected.join(', ');
	                throw new Error(error);
	            }

	            switch (action[GrammarConst.TYPE_INDEX]) {
	            case GrammarConst.SHIFT_TYPE:
	                stack.push(symbol);

	                valueStack.push(lexer.text);

	                // push state
	                stack.push(action[GrammarConst.TO_INDEX]);

	                // allow to read more
	                symbol = null;

	                break;

	            case GrammarConst.REDUCE_TYPE:
	                var production = productions[action[GrammarConst.PRODUCTION_INDEX]],
	                    reducedSymbol = production.symbol || production[0],
	                    reducedAction = production.action || production[2],
	                    reducedRhs = production.rhs || production[1],
	                    len = reducedRhs.length,
	                    i = 0,
	                    ret,
	                    $$ = valueStack[valueStack.length - len]; // default to $$ = $1

	                ret = undefined;

	                self.$$ = $$;

	                for (; i < len; i++) {
	                    self['$' + (len - i)] = valueStack[valueStack.length - 1 - i];
	                }

	                if (reducedAction) {
	                    ret = reducedAction.call(self);
	                }

	                if (ret !== undefined) {
	                    $$ = ret;
	                } else {
	                    $$ = self.$$;
	                }

	                stack = stack.slice(0, -1 * len * 2);
	                valueStack = valueStack.slice(0, -1 * len);

	                stack.push(reducedSymbol);

	                valueStack.push($$);

	                var newState = gotos[stack[stack.length - 2]][stack[stack.length - 1]];

	                stack.push(newState);

	                break;

	            case GrammarConst.ACCEPT_TYPE:
	                return $$;
	            }
	        }
	    };
	    return parser;
	})();
	if (true) {
	    module.exports = parser;
	}

/***/ },
/* 236 */
/***/ function(module, exports) {

	var hyphenExpression = /\-+([a-z])/gi

	function upperCaseFirstMatch (match, c, offset) {
	  if (offset !== 0) {
	    return c.toUpperCase()
	  } else {
	    return c
	  }
	}

	function camelCase (str) {
	  var camelCased = str.replace(hyphenExpression, upperCaseFirstMatch)
	  hyphenExpression.lastIndex = 0
	  return camelCased
	}

	module.exports = camelCase


/***/ },
/* 237 */
/***/ function(module, exports) {

	function isString (value) {
	  return typeof value === 'string'
	}

	module.exports = isString


/***/ },
/* 238 */
/***/ function(module, exports) {

	function isUndefined (value) {
	  return typeof value === 'undefined'
	}

	module.exports = isUndefined


/***/ },
/* 239 */
/***/ function(module, exports) {

	function assign (dest) {
	  var args = arguments
	  var source

	  for (var i = 1; i < args.length; i++) {
	    source = args[i]

	    for (var key in source) {
	      dest[key] = source[key]
	    }
	  }

	  return dest
	}

	module.exports = assign


/***/ },
/* 240 */
/***/ function(module, exports) {

	function mapValues (source, fn) {
	  var destination = {}

	  for (var key in source) {
	    if (source.hasOwnProperty(key)) {
	      destination[key] = fn(source[key])
	    }
	  }

	  return destination
	}

	module.exports = mapValues


/***/ },
/* 241 */
/***/ function(module, exports, __webpack_require__) {

	var camelCase = __webpack_require__(236)

	function styleCamelCase (name) {
	  var camel = camelCase(name)

	  // Detect if the style property is already camelCased
	  // To not convert Webkit*, Moz* and O* to lowercase
	  if (camel.charAt(0).toUpperCase() === name.charAt(0)) {
	    return name.charAt(0) + camel.slice(1)
	  }

	  if (name.charAt(0) === '-') {
	    return camel.indexOf('ms') === 0 ? camel
	      : camel.charAt(0).toUpperCase() + camel.slice(1)
	  } else {
	    return camel
	  }
	}

	module.exports = styleCamelCase


/***/ },
/* 242 */
/***/ function(module, exports) {

	var Window = {
	  getComputedStyle: function (node) {
	    return {
	      getPropertyValue: node.style.getProperty
	    }
	  }
	}

	module.exports = Window


/***/ },
/* 243 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _underscore = __webpack_require__(210);

	var _underscore2 = _interopRequireDefault(_underscore);

	var _immutable = __webpack_require__(244);

	var _immutable2 = _interopRequireDefault(_immutable);

	var _data = __webpack_require__(225);

	var _data2 = _interopRequireDefault(_data);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	var getMarkupStatsBarData = function getMarkupStatsBarData(rows) {
	  // NOTE: this is an old comment
	  //
	  // getMarkupStats guarantees that it has a bijection for the set of marked up and
	  // not marked up, where the mapping function is the identity (i.e., for each year
	  // in one set, that year exists in the other set), and also that they are in order.
	  //
	  //

	  /**
	   * an ordered array from objects to titles and years to counts
	   * @type {{}}
	   */
	  var data = [];
	  _underscore2.default.each(rows, function (el) {
	    var index = el.key[0][0];
	    var title = el.key[0][1];
	    var year = el.key[1];
	    var count = el.value;

	    data[index] = data[index] || {
	      title: title,
	      data: {}
	    };
	    data[index].data[year] = (data[index].data[year] || 0) + count; //Add to this year's data
	  });

	  // make sure each title contains every x value
	  var xVals = _immutable2.default.Set();
	  _underscore2.default.each(data, function (bucket) {
	    xVals = xVals.union(_immutable2.default.Set(_underscore2.default.keys(bucket.data)));
	  });
	  _underscore2.default.each(data, function (bucket) {
	    xVals.forEach(function (xVal) {
	      if (!bucket.data[xVal]) bucket.data[xVal] = 0;
	    });
	  });

	  // Convert all hashes to {x, y} objects, sort
	  var getObj = function getObj(title) {
	    return function (count, year) {
	      return { "x": year, "y": count, "title": title };
	    };
	  };
	  var sort = function sort(o) {
	    return o.x;
	  };
	  _underscore2.default.each(data, function (bucket) {
	    bucket.data = _underscore2.default.sortBy(_underscore2.default.map(bucket.data, getObj(bucket.title)), sort);
	  });

	  //console.log(data);

	  return data;
	};

	var markupStats = getMarkupStatsBarData(_data2.default.rows);

	exports.default = markupStats;

/***/ },
/* 244 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 *  Copyright (c) 2014-2015, Facebook, Inc.
	 *  All rights reserved.
	 *
	 *  This source code is licensed under the BSD-style license found in the
	 *  LICENSE file in the root directory of this source tree. An additional grant
	 *  of patent rights can be found in the PATENTS file in the same directory.
	 */

	(function (global, factory) {
	   true ? module.exports = factory() :
	  typeof define === 'function' && define.amd ? define(factory) :
	  (global.Immutable = factory());
	}(this, function () { 'use strict';var SLICE$0 = Array.prototype.slice;

	  function createClass(ctor, superClass) {
	    if (superClass) {
	      ctor.prototype = Object.create(superClass.prototype);
	    }
	    ctor.prototype.constructor = ctor;
	  }

	  function Iterable(value) {
	      return isIterable(value) ? value : Seq(value);
	    }


	  createClass(KeyedIterable, Iterable);
	    function KeyedIterable(value) {
	      return isKeyed(value) ? value : KeyedSeq(value);
	    }


	  createClass(IndexedIterable, Iterable);
	    function IndexedIterable(value) {
	      return isIndexed(value) ? value : IndexedSeq(value);
	    }


	  createClass(SetIterable, Iterable);
	    function SetIterable(value) {
	      return isIterable(value) && !isAssociative(value) ? value : SetSeq(value);
	    }



	  function isIterable(maybeIterable) {
	    return !!(maybeIterable && maybeIterable[IS_ITERABLE_SENTINEL]);
	  }

	  function isKeyed(maybeKeyed) {
	    return !!(maybeKeyed && maybeKeyed[IS_KEYED_SENTINEL]);
	  }

	  function isIndexed(maybeIndexed) {
	    return !!(maybeIndexed && maybeIndexed[IS_INDEXED_SENTINEL]);
	  }

	  function isAssociative(maybeAssociative) {
	    return isKeyed(maybeAssociative) || isIndexed(maybeAssociative);
	  }

	  function isOrdered(maybeOrdered) {
	    return !!(maybeOrdered && maybeOrdered[IS_ORDERED_SENTINEL]);
	  }

	  Iterable.isIterable = isIterable;
	  Iterable.isKeyed = isKeyed;
	  Iterable.isIndexed = isIndexed;
	  Iterable.isAssociative = isAssociative;
	  Iterable.isOrdered = isOrdered;

	  Iterable.Keyed = KeyedIterable;
	  Iterable.Indexed = IndexedIterable;
	  Iterable.Set = SetIterable;


	  var IS_ITERABLE_SENTINEL = '@@__IMMUTABLE_ITERABLE__@@';
	  var IS_KEYED_SENTINEL = '@@__IMMUTABLE_KEYED__@@';
	  var IS_INDEXED_SENTINEL = '@@__IMMUTABLE_INDEXED__@@';
	  var IS_ORDERED_SENTINEL = '@@__IMMUTABLE_ORDERED__@@';

	  // Used for setting prototype methods that IE8 chokes on.
	  var DELETE = 'delete';

	  // Constants describing the size of trie nodes.
	  var SHIFT = 5; // Resulted in best performance after ______?
	  var SIZE = 1 << SHIFT;
	  var MASK = SIZE - 1;

	  // A consistent shared value representing "not set" which equals nothing other
	  // than itself, and nothing that could be provided externally.
	  var NOT_SET = {};

	  // Boolean references, Rough equivalent of `bool &`.
	  var CHANGE_LENGTH = { value: false };
	  var DID_ALTER = { value: false };

	  function MakeRef(ref) {
	    ref.value = false;
	    return ref;
	  }

	  function SetRef(ref) {
	    ref && (ref.value = true);
	  }

	  // A function which returns a value representing an "owner" for transient writes
	  // to tries. The return value will only ever equal itself, and will not equal
	  // the return of any subsequent call of this function.
	  function OwnerID() {}

	  // http://jsperf.com/copy-array-inline
	  function arrCopy(arr, offset) {
	    offset = offset || 0;
	    var len = Math.max(0, arr.length - offset);
	    var newArr = new Array(len);
	    for (var ii = 0; ii < len; ii++) {
	      newArr[ii] = arr[ii + offset];
	    }
	    return newArr;
	  }

	  function ensureSize(iter) {
	    if (iter.size === undefined) {
	      iter.size = iter.__iterate(returnTrue);
	    }
	    return iter.size;
	  }

	  function wrapIndex(iter, index) {
	    // This implements "is array index" which the ECMAString spec defines as:
	    //
	    //     A String property name P is an array index if and only if
	    //     ToString(ToUint32(P)) is equal to P and ToUint32(P) is not equal
	    //     to 2^32−1.
	    //
	    // http://www.ecma-international.org/ecma-262/6.0/#sec-array-exotic-objects
	    if (typeof index !== 'number') {
	      var uint32Index = index >>> 0; // N >>> 0 is shorthand for ToUint32
	      if ('' + uint32Index !== index || uint32Index === 4294967295) {
	        return NaN;
	      }
	      index = uint32Index;
	    }
	    return index < 0 ? ensureSize(iter) + index : index;
	  }

	  function returnTrue() {
	    return true;
	  }

	  function wholeSlice(begin, end, size) {
	    return (begin === 0 || (size !== undefined && begin <= -size)) &&
	      (end === undefined || (size !== undefined && end >= size));
	  }

	  function resolveBegin(begin, size) {
	    return resolveIndex(begin, size, 0);
	  }

	  function resolveEnd(end, size) {
	    return resolveIndex(end, size, size);
	  }

	  function resolveIndex(index, size, defaultIndex) {
	    return index === undefined ?
	      defaultIndex :
	      index < 0 ?
	        Math.max(0, size + index) :
	        size === undefined ?
	          index :
	          Math.min(size, index);
	  }

	  /* global Symbol */

	  var ITERATE_KEYS = 0;
	  var ITERATE_VALUES = 1;
	  var ITERATE_ENTRIES = 2;

	  var REAL_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
	  var FAUX_ITERATOR_SYMBOL = '@@iterator';

	  var ITERATOR_SYMBOL = REAL_ITERATOR_SYMBOL || FAUX_ITERATOR_SYMBOL;


	  function Iterator(next) {
	      this.next = next;
	    }

	    Iterator.prototype.toString = function() {
	      return '[Iterator]';
	    };


	  Iterator.KEYS = ITERATE_KEYS;
	  Iterator.VALUES = ITERATE_VALUES;
	  Iterator.ENTRIES = ITERATE_ENTRIES;

	  Iterator.prototype.inspect =
	  Iterator.prototype.toSource = function () { return this.toString(); }
	  Iterator.prototype[ITERATOR_SYMBOL] = function () {
	    return this;
	  };


	  function iteratorValue(type, k, v, iteratorResult) {
	    var value = type === 0 ? k : type === 1 ? v : [k, v];
	    iteratorResult ? (iteratorResult.value = value) : (iteratorResult = {
	      value: value, done: false
	    });
	    return iteratorResult;
	  }

	  function iteratorDone() {
	    return { value: undefined, done: true };
	  }

	  function hasIterator(maybeIterable) {
	    return !!getIteratorFn(maybeIterable);
	  }

	  function isIterator(maybeIterator) {
	    return maybeIterator && typeof maybeIterator.next === 'function';
	  }

	  function getIterator(iterable) {
	    var iteratorFn = getIteratorFn(iterable);
	    return iteratorFn && iteratorFn.call(iterable);
	  }

	  function getIteratorFn(iterable) {
	    var iteratorFn = iterable && (
	      (REAL_ITERATOR_SYMBOL && iterable[REAL_ITERATOR_SYMBOL]) ||
	      iterable[FAUX_ITERATOR_SYMBOL]
	    );
	    if (typeof iteratorFn === 'function') {
	      return iteratorFn;
	    }
	  }

	  function isArrayLike(value) {
	    return value && typeof value.length === 'number';
	  }

	  createClass(Seq, Iterable);
	    function Seq(value) {
	      return value === null || value === undefined ? emptySequence() :
	        isIterable(value) ? value.toSeq() : seqFromValue(value);
	    }

	    Seq.of = function(/*...values*/) {
	      return Seq(arguments);
	    };

	    Seq.prototype.toSeq = function() {
	      return this;
	    };

	    Seq.prototype.toString = function() {
	      return this.__toString('Seq {', '}');
	    };

	    Seq.prototype.cacheResult = function() {
	      if (!this._cache && this.__iterateUncached) {
	        this._cache = this.entrySeq().toArray();
	        this.size = this._cache.length;
	      }
	      return this;
	    };

	    // abstract __iterateUncached(fn, reverse)

	    Seq.prototype.__iterate = function(fn, reverse) {
	      return seqIterate(this, fn, reverse, true);
	    };

	    // abstract __iteratorUncached(type, reverse)

	    Seq.prototype.__iterator = function(type, reverse) {
	      return seqIterator(this, type, reverse, true);
	    };



	  createClass(KeyedSeq, Seq);
	    function KeyedSeq(value) {
	      return value === null || value === undefined ?
	        emptySequence().toKeyedSeq() :
	        isIterable(value) ?
	          (isKeyed(value) ? value.toSeq() : value.fromEntrySeq()) :
	          keyedSeqFromValue(value);
	    }

	    KeyedSeq.prototype.toKeyedSeq = function() {
	      return this;
	    };



	  createClass(IndexedSeq, Seq);
	    function IndexedSeq(value) {
	      return value === null || value === undefined ? emptySequence() :
	        !isIterable(value) ? indexedSeqFromValue(value) :
	        isKeyed(value) ? value.entrySeq() : value.toIndexedSeq();
	    }

	    IndexedSeq.of = function(/*...values*/) {
	      return IndexedSeq(arguments);
	    };

	    IndexedSeq.prototype.toIndexedSeq = function() {
	      return this;
	    };

	    IndexedSeq.prototype.toString = function() {
	      return this.__toString('Seq [', ']');
	    };

	    IndexedSeq.prototype.__iterate = function(fn, reverse) {
	      return seqIterate(this, fn, reverse, false);
	    };

	    IndexedSeq.prototype.__iterator = function(type, reverse) {
	      return seqIterator(this, type, reverse, false);
	    };



	  createClass(SetSeq, Seq);
	    function SetSeq(value) {
	      return (
	        value === null || value === undefined ? emptySequence() :
	        !isIterable(value) ? indexedSeqFromValue(value) :
	        isKeyed(value) ? value.entrySeq() : value
	      ).toSetSeq();
	    }

	    SetSeq.of = function(/*...values*/) {
	      return SetSeq(arguments);
	    };

	    SetSeq.prototype.toSetSeq = function() {
	      return this;
	    };



	  Seq.isSeq = isSeq;
	  Seq.Keyed = KeyedSeq;
	  Seq.Set = SetSeq;
	  Seq.Indexed = IndexedSeq;

	  var IS_SEQ_SENTINEL = '@@__IMMUTABLE_SEQ__@@';

	  Seq.prototype[IS_SEQ_SENTINEL] = true;



	  createClass(ArraySeq, IndexedSeq);
	    function ArraySeq(array) {
	      this._array = array;
	      this.size = array.length;
	    }

	    ArraySeq.prototype.get = function(index, notSetValue) {
	      return this.has(index) ? this._array[wrapIndex(this, index)] : notSetValue;
	    };

	    ArraySeq.prototype.__iterate = function(fn, reverse) {
	      var array = this._array;
	      var maxIndex = array.length - 1;
	      for (var ii = 0; ii <= maxIndex; ii++) {
	        if (fn(array[reverse ? maxIndex - ii : ii], ii, this) === false) {
	          return ii + 1;
	        }
	      }
	      return ii;
	    };

	    ArraySeq.prototype.__iterator = function(type, reverse) {
	      var array = this._array;
	      var maxIndex = array.length - 1;
	      var ii = 0;
	      return new Iterator(function() 
	        {return ii > maxIndex ?
	          iteratorDone() :
	          iteratorValue(type, ii, array[reverse ? maxIndex - ii++ : ii++])}
	      );
	    };



	  createClass(ObjectSeq, KeyedSeq);
	    function ObjectSeq(object) {
	      var keys = Object.keys(object);
	      this._object = object;
	      this._keys = keys;
	      this.size = keys.length;
	    }

	    ObjectSeq.prototype.get = function(key, notSetValue) {
	      if (notSetValue !== undefined && !this.has(key)) {
	        return notSetValue;
	      }
	      return this._object[key];
	    };

	    ObjectSeq.prototype.has = function(key) {
	      return this._object.hasOwnProperty(key);
	    };

	    ObjectSeq.prototype.__iterate = function(fn, reverse) {
	      var object = this._object;
	      var keys = this._keys;
	      var maxIndex = keys.length - 1;
	      for (var ii = 0; ii <= maxIndex; ii++) {
	        var key = keys[reverse ? maxIndex - ii : ii];
	        if (fn(object[key], key, this) === false) {
	          return ii + 1;
	        }
	      }
	      return ii;
	    };

	    ObjectSeq.prototype.__iterator = function(type, reverse) {
	      var object = this._object;
	      var keys = this._keys;
	      var maxIndex = keys.length - 1;
	      var ii = 0;
	      return new Iterator(function()  {
	        var key = keys[reverse ? maxIndex - ii : ii];
	        return ii++ > maxIndex ?
	          iteratorDone() :
	          iteratorValue(type, key, object[key]);
	      });
	    };

	  ObjectSeq.prototype[IS_ORDERED_SENTINEL] = true;


	  createClass(IterableSeq, IndexedSeq);
	    function IterableSeq(iterable) {
	      this._iterable = iterable;
	      this.size = iterable.length || iterable.size;
	    }

	    IterableSeq.prototype.__iterateUncached = function(fn, reverse) {
	      if (reverse) {
	        return this.cacheResult().__iterate(fn, reverse);
	      }
	      var iterable = this._iterable;
	      var iterator = getIterator(iterable);
	      var iterations = 0;
	      if (isIterator(iterator)) {
	        var step;
	        while (!(step = iterator.next()).done) {
	          if (fn(step.value, iterations++, this) === false) {
	            break;
	          }
	        }
	      }
	      return iterations;
	    };

	    IterableSeq.prototype.__iteratorUncached = function(type, reverse) {
	      if (reverse) {
	        return this.cacheResult().__iterator(type, reverse);
	      }
	      var iterable = this._iterable;
	      var iterator = getIterator(iterable);
	      if (!isIterator(iterator)) {
	        return new Iterator(iteratorDone);
	      }
	      var iterations = 0;
	      return new Iterator(function()  {
	        var step = iterator.next();
	        return step.done ? step : iteratorValue(type, iterations++, step.value);
	      });
	    };



	  createClass(IteratorSeq, IndexedSeq);
	    function IteratorSeq(iterator) {
	      this._iterator = iterator;
	      this._iteratorCache = [];
	    }

	    IteratorSeq.prototype.__iterateUncached = function(fn, reverse) {
	      if (reverse) {
	        return this.cacheResult().__iterate(fn, reverse);
	      }
	      var iterator = this._iterator;
	      var cache = this._iteratorCache;
	      var iterations = 0;
	      while (iterations < cache.length) {
	        if (fn(cache[iterations], iterations++, this) === false) {
	          return iterations;
	        }
	      }
	      var step;
	      while (!(step = iterator.next()).done) {
	        var val = step.value;
	        cache[iterations] = val;
	        if (fn(val, iterations++, this) === false) {
	          break;
	        }
	      }
	      return iterations;
	    };

	    IteratorSeq.prototype.__iteratorUncached = function(type, reverse) {
	      if (reverse) {
	        return this.cacheResult().__iterator(type, reverse);
	      }
	      var iterator = this._iterator;
	      var cache = this._iteratorCache;
	      var iterations = 0;
	      return new Iterator(function()  {
	        if (iterations >= cache.length) {
	          var step = iterator.next();
	          if (step.done) {
	            return step;
	          }
	          cache[iterations] = step.value;
	        }
	        return iteratorValue(type, iterations, cache[iterations++]);
	      });
	    };




	  // # pragma Helper functions

	  function isSeq(maybeSeq) {
	    return !!(maybeSeq && maybeSeq[IS_SEQ_SENTINEL]);
	  }

	  var EMPTY_SEQ;

	  function emptySequence() {
	    return EMPTY_SEQ || (EMPTY_SEQ = new ArraySeq([]));
	  }

	  function keyedSeqFromValue(value) {
	    var seq =
	      Array.isArray(value) ? new ArraySeq(value).fromEntrySeq() :
	      isIterator(value) ? new IteratorSeq(value).fromEntrySeq() :
	      hasIterator(value) ? new IterableSeq(value).fromEntrySeq() :
	      typeof value === 'object' ? new ObjectSeq(value) :
	      undefined;
	    if (!seq) {
	      throw new TypeError(
	        'Expected Array or iterable object of [k, v] entries, '+
	        'or keyed object: ' + value
	      );
	    }
	    return seq;
	  }

	  function indexedSeqFromValue(value) {
	    var seq = maybeIndexedSeqFromValue(value);
	    if (!seq) {
	      throw new TypeError(
	        'Expected Array or iterable object of values: ' + value
	      );
	    }
	    return seq;
	  }

	  function seqFromValue(value) {
	    var seq = maybeIndexedSeqFromValue(value) ||
	      (typeof value === 'object' && new ObjectSeq(value));
	    if (!seq) {
	      throw new TypeError(
	        'Expected Array or iterable object of values, or keyed object: ' + value
	      );
	    }
	    return seq;
	  }

	  function maybeIndexedSeqFromValue(value) {
	    return (
	      isArrayLike(value) ? new ArraySeq(value) :
	      isIterator(value) ? new IteratorSeq(value) :
	      hasIterator(value) ? new IterableSeq(value) :
	      undefined
	    );
	  }

	  function seqIterate(seq, fn, reverse, useKeys) {
	    var cache = seq._cache;
	    if (cache) {
	      var maxIndex = cache.length - 1;
	      for (var ii = 0; ii <= maxIndex; ii++) {
	        var entry = cache[reverse ? maxIndex - ii : ii];
	        if (fn(entry[1], useKeys ? entry[0] : ii, seq) === false) {
	          return ii + 1;
	        }
	      }
	      return ii;
	    }
	    return seq.__iterateUncached(fn, reverse);
	  }

	  function seqIterator(seq, type, reverse, useKeys) {
	    var cache = seq._cache;
	    if (cache) {
	      var maxIndex = cache.length - 1;
	      var ii = 0;
	      return new Iterator(function()  {
	        var entry = cache[reverse ? maxIndex - ii : ii];
	        return ii++ > maxIndex ?
	          iteratorDone() :
	          iteratorValue(type, useKeys ? entry[0] : ii - 1, entry[1]);
	      });
	    }
	    return seq.__iteratorUncached(type, reverse);
	  }

	  function fromJS(json, converter) {
	    return converter ?
	      fromJSWith(converter, json, '', {'': json}) :
	      fromJSDefault(json);
	  }

	  function fromJSWith(converter, json, key, parentJSON) {
	    if (Array.isArray(json)) {
	      return converter.call(parentJSON, key, IndexedSeq(json).map(function(v, k)  {return fromJSWith(converter, v, k, json)}));
	    }
	    if (isPlainObj(json)) {
	      return converter.call(parentJSON, key, KeyedSeq(json).map(function(v, k)  {return fromJSWith(converter, v, k, json)}));
	    }
	    return json;
	  }

	  function fromJSDefault(json) {
	    if (Array.isArray(json)) {
	      return IndexedSeq(json).map(fromJSDefault).toList();
	    }
	    if (isPlainObj(json)) {
	      return KeyedSeq(json).map(fromJSDefault).toMap();
	    }
	    return json;
	  }

	  function isPlainObj(value) {
	    return value && (value.constructor === Object || value.constructor === undefined);
	  }

	  /**
	   * An extension of the "same-value" algorithm as [described for use by ES6 Map
	   * and Set](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map#Key_equality)
	   *
	   * NaN is considered the same as NaN, however -0 and 0 are considered the same
	   * value, which is different from the algorithm described by
	   * [`Object.is`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is).
	   *
	   * This is extended further to allow Objects to describe the values they
	   * represent, by way of `valueOf` or `equals` (and `hashCode`).
	   *
	   * Note: because of this extension, the key equality of Immutable.Map and the
	   * value equality of Immutable.Set will differ from ES6 Map and Set.
	   *
	   * ### Defining custom values
	   *
	   * The easiest way to describe the value an object represents is by implementing
	   * `valueOf`. For example, `Date` represents a value by returning a unix
	   * timestamp for `valueOf`:
	   *
	   *     var date1 = new Date(1234567890000); // Fri Feb 13 2009 ...
	   *     var date2 = new Date(1234567890000);
	   *     date1.valueOf(); // 1234567890000
	   *     assert( date1 !== date2 );
	   *     assert( Immutable.is( date1, date2 ) );
	   *
	   * Note: overriding `valueOf` may have other implications if you use this object
	   * where JavaScript expects a primitive, such as implicit string coercion.
	   *
	   * For more complex types, especially collections, implementing `valueOf` may
	   * not be performant. An alternative is to implement `equals` and `hashCode`.
	   *
	   * `equals` takes another object, presumably of similar type, and returns true
	   * if the it is equal. Equality is symmetrical, so the same result should be
	   * returned if this and the argument are flipped.
	   *
	   *     assert( a.equals(b) === b.equals(a) );
	   *
	   * `hashCode` returns a 32bit integer number representing the object which will
	   * be used to determine how to store the value object in a Map or Set. You must
	   * provide both or neither methods, one must not exist without the other.
	   *
	   * Also, an important relationship between these methods must be upheld: if two
	   * values are equal, they *must* return the same hashCode. If the values are not
	   * equal, they might have the same hashCode; this is called a hash collision,
	   * and while undesirable for performance reasons, it is acceptable.
	   *
	   *     if (a.equals(b)) {
	   *       assert( a.hashCode() === b.hashCode() );
	   *     }
	   *
	   * All Immutable collections implement `equals` and `hashCode`.
	   *
	   */
	  function is(valueA, valueB) {
	    if (valueA === valueB || (valueA !== valueA && valueB !== valueB)) {
	      return true;
	    }
	    if (!valueA || !valueB) {
	      return false;
	    }
	    if (typeof valueA.valueOf === 'function' &&
	        typeof valueB.valueOf === 'function') {
	      valueA = valueA.valueOf();
	      valueB = valueB.valueOf();
	      if (valueA === valueB || (valueA !== valueA && valueB !== valueB)) {
	        return true;
	      }
	      if (!valueA || !valueB) {
	        return false;
	      }
	    }
	    if (typeof valueA.equals === 'function' &&
	        typeof valueB.equals === 'function' &&
	        valueA.equals(valueB)) {
	      return true;
	    }
	    return false;
	  }

	  function deepEqual(a, b) {
	    if (a === b) {
	      return true;
	    }

	    if (
	      !isIterable(b) ||
	      a.size !== undefined && b.size !== undefined && a.size !== b.size ||
	      a.__hash !== undefined && b.__hash !== undefined && a.__hash !== b.__hash ||
	      isKeyed(a) !== isKeyed(b) ||
	      isIndexed(a) !== isIndexed(b) ||
	      isOrdered(a) !== isOrdered(b)
	    ) {
	      return false;
	    }

	    if (a.size === 0 && b.size === 0) {
	      return true;
	    }

	    var notAssociative = !isAssociative(a);

	    if (isOrdered(a)) {
	      var entries = a.entries();
	      return b.every(function(v, k)  {
	        var entry = entries.next().value;
	        return entry && is(entry[1], v) && (notAssociative || is(entry[0], k));
	      }) && entries.next().done;
	    }

	    var flipped = false;

	    if (a.size === undefined) {
	      if (b.size === undefined) {
	        if (typeof a.cacheResult === 'function') {
	          a.cacheResult();
	        }
	      } else {
	        flipped = true;
	        var _ = a;
	        a = b;
	        b = _;
	      }
	    }

	    var allEqual = true;
	    var bSize = b.__iterate(function(v, k)  {
	      if (notAssociative ? !a.has(v) :
	          flipped ? !is(v, a.get(k, NOT_SET)) : !is(a.get(k, NOT_SET), v)) {
	        allEqual = false;
	        return false;
	      }
	    });

	    return allEqual && a.size === bSize;
	  }

	  createClass(Repeat, IndexedSeq);

	    function Repeat(value, times) {
	      if (!(this instanceof Repeat)) {
	        return new Repeat(value, times);
	      }
	      this._value = value;
	      this.size = times === undefined ? Infinity : Math.max(0, times);
	      if (this.size === 0) {
	        if (EMPTY_REPEAT) {
	          return EMPTY_REPEAT;
	        }
	        EMPTY_REPEAT = this;
	      }
	    }

	    Repeat.prototype.toString = function() {
	      if (this.size === 0) {
	        return 'Repeat []';
	      }
	      return 'Repeat [ ' + this._value + ' ' + this.size + ' times ]';
	    };

	    Repeat.prototype.get = function(index, notSetValue) {
	      return this.has(index) ? this._value : notSetValue;
	    };

	    Repeat.prototype.includes = function(searchValue) {
	      return is(this._value, searchValue);
	    };

	    Repeat.prototype.slice = function(begin, end) {
	      var size = this.size;
	      return wholeSlice(begin, end, size) ? this :
	        new Repeat(this._value, resolveEnd(end, size) - resolveBegin(begin, size));
	    };

	    Repeat.prototype.reverse = function() {
	      return this;
	    };

	    Repeat.prototype.indexOf = function(searchValue) {
	      if (is(this._value, searchValue)) {
	        return 0;
	      }
	      return -1;
	    };

	    Repeat.prototype.lastIndexOf = function(searchValue) {
	      if (is(this._value, searchValue)) {
	        return this.size;
	      }
	      return -1;
	    };

	    Repeat.prototype.__iterate = function(fn, reverse) {
	      for (var ii = 0; ii < this.size; ii++) {
	        if (fn(this._value, ii, this) === false) {
	          return ii + 1;
	        }
	      }
	      return ii;
	    };

	    Repeat.prototype.__iterator = function(type, reverse) {var this$0 = this;
	      var ii = 0;
	      return new Iterator(function() 
	        {return ii < this$0.size ? iteratorValue(type, ii++, this$0._value) : iteratorDone()}
	      );
	    };

	    Repeat.prototype.equals = function(other) {
	      return other instanceof Repeat ?
	        is(this._value, other._value) :
	        deepEqual(other);
	    };


	  var EMPTY_REPEAT;

	  function invariant(condition, error) {
	    if (!condition) throw new Error(error);
	  }

	  createClass(Range, IndexedSeq);

	    function Range(start, end, step) {
	      if (!(this instanceof Range)) {
	        return new Range(start, end, step);
	      }
	      invariant(step !== 0, 'Cannot step a Range by 0');
	      start = start || 0;
	      if (end === undefined) {
	        end = Infinity;
	      }
	      step = step === undefined ? 1 : Math.abs(step);
	      if (end < start) {
	        step = -step;
	      }
	      this._start = start;
	      this._end = end;
	      this._step = step;
	      this.size = Math.max(0, Math.ceil((end - start) / step - 1) + 1);
	      if (this.size === 0) {
	        if (EMPTY_RANGE) {
	          return EMPTY_RANGE;
	        }
	        EMPTY_RANGE = this;
	      }
	    }

	    Range.prototype.toString = function() {
	      if (this.size === 0) {
	        return 'Range []';
	      }
	      return 'Range [ ' +
	        this._start + '...' + this._end +
	        (this._step !== 1 ? ' by ' + this._step : '') +
	      ' ]';
	    };

	    Range.prototype.get = function(index, notSetValue) {
	      return this.has(index) ?
	        this._start + wrapIndex(this, index) * this._step :
	        notSetValue;
	    };

	    Range.prototype.includes = function(searchValue) {
	      var possibleIndex = (searchValue - this._start) / this._step;
	      return possibleIndex >= 0 &&
	        possibleIndex < this.size &&
	        possibleIndex === Math.floor(possibleIndex);
	    };

	    Range.prototype.slice = function(begin, end) {
	      if (wholeSlice(begin, end, this.size)) {
	        return this;
	      }
	      begin = resolveBegin(begin, this.size);
	      end = resolveEnd(end, this.size);
	      if (end <= begin) {
	        return new Range(0, 0);
	      }
	      return new Range(this.get(begin, this._end), this.get(end, this._end), this._step);
	    };

	    Range.prototype.indexOf = function(searchValue) {
	      var offsetValue = searchValue - this._start;
	      if (offsetValue % this._step === 0) {
	        var index = offsetValue / this._step;
	        if (index >= 0 && index < this.size) {
	          return index
	        }
	      }
	      return -1;
	    };

	    Range.prototype.lastIndexOf = function(searchValue) {
	      return this.indexOf(searchValue);
	    };

	    Range.prototype.__iterate = function(fn, reverse) {
	      var maxIndex = this.size - 1;
	      var step = this._step;
	      var value = reverse ? this._start + maxIndex * step : this._start;
	      for (var ii = 0; ii <= maxIndex; ii++) {
	        if (fn(value, ii, this) === false) {
	          return ii + 1;
	        }
	        value += reverse ? -step : step;
	      }
	      return ii;
	    };

	    Range.prototype.__iterator = function(type, reverse) {
	      var maxIndex = this.size - 1;
	      var step = this._step;
	      var value = reverse ? this._start + maxIndex * step : this._start;
	      var ii = 0;
	      return new Iterator(function()  {
	        var v = value;
	        value += reverse ? -step : step;
	        return ii > maxIndex ? iteratorDone() : iteratorValue(type, ii++, v);
	      });
	    };

	    Range.prototype.equals = function(other) {
	      return other instanceof Range ?
	        this._start === other._start &&
	        this._end === other._end &&
	        this._step === other._step :
	        deepEqual(this, other);
	    };


	  var EMPTY_RANGE;

	  createClass(Collection, Iterable);
	    function Collection() {
	      throw TypeError('Abstract');
	    }


	  createClass(KeyedCollection, Collection);function KeyedCollection() {}

	  createClass(IndexedCollection, Collection);function IndexedCollection() {}

	  createClass(SetCollection, Collection);function SetCollection() {}


	  Collection.Keyed = KeyedCollection;
	  Collection.Indexed = IndexedCollection;
	  Collection.Set = SetCollection;

	  var imul =
	    typeof Math.imul === 'function' && Math.imul(0xffffffff, 2) === -2 ?
	    Math.imul :
	    function imul(a, b) {
	      a = a | 0; // int
	      b = b | 0; // int
	      var c = a & 0xffff;
	      var d = b & 0xffff;
	      // Shift by 0 fixes the sign on the high part.
	      return (c * d) + ((((a >>> 16) * d + c * (b >>> 16)) << 16) >>> 0) | 0; // int
	    };

	  // v8 has an optimization for storing 31-bit signed numbers.
	  // Values which have either 00 or 11 as the high order bits qualify.
	  // This function drops the highest order bit in a signed number, maintaining
	  // the sign bit.
	  function smi(i32) {
	    return ((i32 >>> 1) & 0x40000000) | (i32 & 0xBFFFFFFF);
	  }

	  function hash(o) {
	    if (o === false || o === null || o === undefined) {
	      return 0;
	    }
	    if (typeof o.valueOf === 'function') {
	      o = o.valueOf();
	      if (o === false || o === null || o === undefined) {
	        return 0;
	      }
	    }
	    if (o === true) {
	      return 1;
	    }
	    var type = typeof o;
	    if (type === 'number') {
	      if (o !== o || o === Infinity) {
	        return 0;
	      }
	      var h = o | 0;
	      if (h !== o) {
	        h ^= o * 0xFFFFFFFF;
	      }
	      while (o > 0xFFFFFFFF) {
	        o /= 0xFFFFFFFF;
	        h ^= o;
	      }
	      return smi(h);
	    }
	    if (type === 'string') {
	      return o.length > STRING_HASH_CACHE_MIN_STRLEN ? cachedHashString(o) : hashString(o);
	    }
	    if (typeof o.hashCode === 'function') {
	      return o.hashCode();
	    }
	    if (type === 'object') {
	      return hashJSObj(o);
	    }
	    if (typeof o.toString === 'function') {
	      return hashString(o.toString());
	    }
	    throw new Error('Value type ' + type + ' cannot be hashed.');
	  }

	  function cachedHashString(string) {
	    var hash = stringHashCache[string];
	    if (hash === undefined) {
	      hash = hashString(string);
	      if (STRING_HASH_CACHE_SIZE === STRING_HASH_CACHE_MAX_SIZE) {
	        STRING_HASH_CACHE_SIZE = 0;
	        stringHashCache = {};
	      }
	      STRING_HASH_CACHE_SIZE++;
	      stringHashCache[string] = hash;
	    }
	    return hash;
	  }

	  // http://jsperf.com/hashing-strings
	  function hashString(string) {
	    // This is the hash from JVM
	    // The hash code for a string is computed as
	    // s[0] * 31 ^ (n - 1) + s[1] * 31 ^ (n - 2) + ... + s[n - 1],
	    // where s[i] is the ith character of the string and n is the length of
	    // the string. We "mod" the result to make it between 0 (inclusive) and 2^31
	    // (exclusive) by dropping high bits.
	    var hash = 0;
	    for (var ii = 0; ii < string.length; ii++) {
	      hash = 31 * hash + string.charCodeAt(ii) | 0;
	    }
	    return smi(hash);
	  }

	  function hashJSObj(obj) {
	    var hash;
	    if (usingWeakMap) {
	      hash = weakMap.get(obj);
	      if (hash !== undefined) {
	        return hash;
	      }
	    }

	    hash = obj[UID_HASH_KEY];
	    if (hash !== undefined) {
	      return hash;
	    }

	    if (!canDefineProperty) {
	      hash = obj.propertyIsEnumerable && obj.propertyIsEnumerable[UID_HASH_KEY];
	      if (hash !== undefined) {
	        return hash;
	      }

	      hash = getIENodeHash(obj);
	      if (hash !== undefined) {
	        return hash;
	      }
	    }

	    hash = ++objHashUID;
	    if (objHashUID & 0x40000000) {
	      objHashUID = 0;
	    }

	    if (usingWeakMap) {
	      weakMap.set(obj, hash);
	    } else if (isExtensible !== undefined && isExtensible(obj) === false) {
	      throw new Error('Non-extensible objects are not allowed as keys.');
	    } else if (canDefineProperty) {
	      Object.defineProperty(obj, UID_HASH_KEY, {
	        'enumerable': false,
	        'configurable': false,
	        'writable': false,
	        'value': hash
	      });
	    } else if (obj.propertyIsEnumerable !== undefined &&
	               obj.propertyIsEnumerable === obj.constructor.prototype.propertyIsEnumerable) {
	      // Since we can't define a non-enumerable property on the object
	      // we'll hijack one of the less-used non-enumerable properties to
	      // save our hash on it. Since this is a function it will not show up in
	      // `JSON.stringify` which is what we want.
	      obj.propertyIsEnumerable = function() {
	        return this.constructor.prototype.propertyIsEnumerable.apply(this, arguments);
	      };
	      obj.propertyIsEnumerable[UID_HASH_KEY] = hash;
	    } else if (obj.nodeType !== undefined) {
	      // At this point we couldn't get the IE `uniqueID` to use as a hash
	      // and we couldn't use a non-enumerable property to exploit the
	      // dontEnum bug so we simply add the `UID_HASH_KEY` on the node
	      // itself.
	      obj[UID_HASH_KEY] = hash;
	    } else {
	      throw new Error('Unable to set a non-enumerable property on object.');
	    }

	    return hash;
	  }

	  // Get references to ES5 object methods.
	  var isExtensible = Object.isExtensible;

	  // True if Object.defineProperty works as expected. IE8 fails this test.
	  var canDefineProperty = (function() {
	    try {
	      Object.defineProperty({}, '@', {});
	      return true;
	    } catch (e) {
	      return false;
	    }
	  }());

	  // IE has a `uniqueID` property on DOM nodes. We can construct the hash from it
	  // and avoid memory leaks from the IE cloneNode bug.
	  function getIENodeHash(node) {
	    if (node && node.nodeType > 0) {
	      switch (node.nodeType) {
	        case 1: // Element
	          return node.uniqueID;
	        case 9: // Document
	          return node.documentElement && node.documentElement.uniqueID;
	      }
	    }
	  }

	  // If possible, use a WeakMap.
	  var usingWeakMap = typeof WeakMap === 'function';
	  var weakMap;
	  if (usingWeakMap) {
	    weakMap = new WeakMap();
	  }

	  var objHashUID = 0;

	  var UID_HASH_KEY = '__immutablehash__';
	  if (typeof Symbol === 'function') {
	    UID_HASH_KEY = Symbol(UID_HASH_KEY);
	  }

	  var STRING_HASH_CACHE_MIN_STRLEN = 16;
	  var STRING_HASH_CACHE_MAX_SIZE = 255;
	  var STRING_HASH_CACHE_SIZE = 0;
	  var stringHashCache = {};

	  function assertNotInfinite(size) {
	    invariant(
	      size !== Infinity,
	      'Cannot perform this action with an infinite size.'
	    );
	  }

	  createClass(Map, KeyedCollection);

	    // @pragma Construction

	    function Map(value) {
	      return value === null || value === undefined ? emptyMap() :
	        isMap(value) && !isOrdered(value) ? value :
	        emptyMap().withMutations(function(map ) {
	          var iter = KeyedIterable(value);
	          assertNotInfinite(iter.size);
	          iter.forEach(function(v, k)  {return map.set(k, v)});
	        });
	    }

	    Map.of = function() {var keyValues = SLICE$0.call(arguments, 0);
	      return emptyMap().withMutations(function(map ) {
	        for (var i = 0; i < keyValues.length; i += 2) {
	          if (i + 1 >= keyValues.length) {
	            throw new Error('Missing value for key: ' + keyValues[i]);
	          }
	          map.set(keyValues[i], keyValues[i + 1]);
	        }
	      });
	    };

	    Map.prototype.toString = function() {
	      return this.__toString('Map {', '}');
	    };

	    // @pragma Access

	    Map.prototype.get = function(k, notSetValue) {
	      return this._root ?
	        this._root.get(0, undefined, k, notSetValue) :
	        notSetValue;
	    };

	    // @pragma Modification

	    Map.prototype.set = function(k, v) {
	      return updateMap(this, k, v);
	    };

	    Map.prototype.setIn = function(keyPath, v) {
	      return this.updateIn(keyPath, NOT_SET, function()  {return v});
	    };

	    Map.prototype.remove = function(k) {
	      return updateMap(this, k, NOT_SET);
	    };

	    Map.prototype.deleteIn = function(keyPath) {
	      return this.updateIn(keyPath, function()  {return NOT_SET});
	    };

	    Map.prototype.update = function(k, notSetValue, updater) {
	      return arguments.length === 1 ?
	        k(this) :
	        this.updateIn([k], notSetValue, updater);
	    };

	    Map.prototype.updateIn = function(keyPath, notSetValue, updater) {
	      if (!updater) {
	        updater = notSetValue;
	        notSetValue = undefined;
	      }
	      var updatedValue = updateInDeepMap(
	        this,
	        forceIterator(keyPath),
	        notSetValue,
	        updater
	      );
	      return updatedValue === NOT_SET ? undefined : updatedValue;
	    };

	    Map.prototype.clear = function() {
	      if (this.size === 0) {
	        return this;
	      }
	      if (this.__ownerID) {
	        this.size = 0;
	        this._root = null;
	        this.__hash = undefined;
	        this.__altered = true;
	        return this;
	      }
	      return emptyMap();
	    };

	    // @pragma Composition

	    Map.prototype.merge = function(/*...iters*/) {
	      return mergeIntoMapWith(this, undefined, arguments);
	    };

	    Map.prototype.mergeWith = function(merger) {var iters = SLICE$0.call(arguments, 1);
	      return mergeIntoMapWith(this, merger, iters);
	    };

	    Map.prototype.mergeIn = function(keyPath) {var iters = SLICE$0.call(arguments, 1);
	      return this.updateIn(
	        keyPath,
	        emptyMap(),
	        function(m ) {return typeof m.merge === 'function' ?
	          m.merge.apply(m, iters) :
	          iters[iters.length - 1]}
	      );
	    };

	    Map.prototype.mergeDeep = function(/*...iters*/) {
	      return mergeIntoMapWith(this, deepMerger, arguments);
	    };

	    Map.prototype.mergeDeepWith = function(merger) {var iters = SLICE$0.call(arguments, 1);
	      return mergeIntoMapWith(this, deepMergerWith(merger), iters);
	    };

	    Map.prototype.mergeDeepIn = function(keyPath) {var iters = SLICE$0.call(arguments, 1);
	      return this.updateIn(
	        keyPath,
	        emptyMap(),
	        function(m ) {return typeof m.mergeDeep === 'function' ?
	          m.mergeDeep.apply(m, iters) :
	          iters[iters.length - 1]}
	      );
	    };

	    Map.prototype.sort = function(comparator) {
	      // Late binding
	      return OrderedMap(sortFactory(this, comparator));
	    };

	    Map.prototype.sortBy = function(mapper, comparator) {
	      // Late binding
	      return OrderedMap(sortFactory(this, comparator, mapper));
	    };

	    // @pragma Mutability

	    Map.prototype.withMutations = function(fn) {
	      var mutable = this.asMutable();
	      fn(mutable);
	      return mutable.wasAltered() ? mutable.__ensureOwner(this.__ownerID) : this;
	    };

	    Map.prototype.asMutable = function() {
	      return this.__ownerID ? this : this.__ensureOwner(new OwnerID());
	    };

	    Map.prototype.asImmutable = function() {
	      return this.__ensureOwner();
	    };

	    Map.prototype.wasAltered = function() {
	      return this.__altered;
	    };

	    Map.prototype.__iterator = function(type, reverse) {
	      return new MapIterator(this, type, reverse);
	    };

	    Map.prototype.__iterate = function(fn, reverse) {var this$0 = this;
	      var iterations = 0;
	      this._root && this._root.iterate(function(entry ) {
	        iterations++;
	        return fn(entry[1], entry[0], this$0);
	      }, reverse);
	      return iterations;
	    };

	    Map.prototype.__ensureOwner = function(ownerID) {
	      if (ownerID === this.__ownerID) {
	        return this;
	      }
	      if (!ownerID) {
	        this.__ownerID = ownerID;
	        this.__altered = false;
	        return this;
	      }
	      return makeMap(this.size, this._root, ownerID, this.__hash);
	    };


	  function isMap(maybeMap) {
	    return !!(maybeMap && maybeMap[IS_MAP_SENTINEL]);
	  }

	  Map.isMap = isMap;

	  var IS_MAP_SENTINEL = '@@__IMMUTABLE_MAP__@@';

	  var MapPrototype = Map.prototype;
	  MapPrototype[IS_MAP_SENTINEL] = true;
	  MapPrototype[DELETE] = MapPrototype.remove;
	  MapPrototype.removeIn = MapPrototype.deleteIn;


	  // #pragma Trie Nodes



	    function ArrayMapNode(ownerID, entries) {
	      this.ownerID = ownerID;
	      this.entries = entries;
	    }

	    ArrayMapNode.prototype.get = function(shift, keyHash, key, notSetValue) {
	      var entries = this.entries;
	      for (var ii = 0, len = entries.length; ii < len; ii++) {
	        if (is(key, entries[ii][0])) {
	          return entries[ii][1];
	        }
	      }
	      return notSetValue;
	    };

	    ArrayMapNode.prototype.update = function(ownerID, shift, keyHash, key, value, didChangeSize, didAlter) {
	      var removed = value === NOT_SET;

	      var entries = this.entries;
	      var idx = 0;
	      for (var len = entries.length; idx < len; idx++) {
	        if (is(key, entries[idx][0])) {
	          break;
	        }
	      }
	      var exists = idx < len;

	      if (exists ? entries[idx][1] === value : removed) {
	        return this;
	      }

	      SetRef(didAlter);
	      (removed || !exists) && SetRef(didChangeSize);

	      if (removed && entries.length === 1) {
	        return; // undefined
	      }

	      if (!exists && !removed && entries.length >= MAX_ARRAY_MAP_SIZE) {
	        return createNodes(ownerID, entries, key, value);
	      }

	      var isEditable = ownerID && ownerID === this.ownerID;
	      var newEntries = isEditable ? entries : arrCopy(entries);

	      if (exists) {
	        if (removed) {
	          idx === len - 1 ? newEntries.pop() : (newEntries[idx] = newEntries.pop());
	        } else {
	          newEntries[idx] = [key, value];
	        }
	      } else {
	        newEntries.push([key, value]);
	      }

	      if (isEditable) {
	        this.entries = newEntries;
	        return this;
	      }

	      return new ArrayMapNode(ownerID, newEntries);
	    };




	    function BitmapIndexedNode(ownerID, bitmap, nodes) {
	      this.ownerID = ownerID;
	      this.bitmap = bitmap;
	      this.nodes = nodes;
	    }

	    BitmapIndexedNode.prototype.get = function(shift, keyHash, key, notSetValue) {
	      if (keyHash === undefined) {
	        keyHash = hash(key);
	      }
	      var bit = (1 << ((shift === 0 ? keyHash : keyHash >>> shift) & MASK));
	      var bitmap = this.bitmap;
	      return (bitmap & bit) === 0 ? notSetValue :
	        this.nodes[popCount(bitmap & (bit - 1))].get(shift + SHIFT, keyHash, key, notSetValue);
	    };

	    BitmapIndexedNode.prototype.update = function(ownerID, shift, keyHash, key, value, didChangeSize, didAlter) {
	      if (keyHash === undefined) {
	        keyHash = hash(key);
	      }
	      var keyHashFrag = (shift === 0 ? keyHash : keyHash >>> shift) & MASK;
	      var bit = 1 << keyHashFrag;
	      var bitmap = this.bitmap;
	      var exists = (bitmap & bit) !== 0;

	      if (!exists && value === NOT_SET) {
	        return this;
	      }

	      var idx = popCount(bitmap & (bit - 1));
	      var nodes = this.nodes;
	      var node = exists ? nodes[idx] : undefined;
	      var newNode = updateNode(node, ownerID, shift + SHIFT, keyHash, key, value, didChangeSize, didAlter);

	      if (newNode === node) {
	        return this;
	      }

	      if (!exists && newNode && nodes.length >= MAX_BITMAP_INDEXED_SIZE) {
	        return expandNodes(ownerID, nodes, bitmap, keyHashFrag, newNode);
	      }

	      if (exists && !newNode && nodes.length === 2 && isLeafNode(nodes[idx ^ 1])) {
	        return nodes[idx ^ 1];
	      }

	      if (exists && newNode && nodes.length === 1 && isLeafNode(newNode)) {
	        return newNode;
	      }

	      var isEditable = ownerID && ownerID === this.ownerID;
	      var newBitmap = exists ? newNode ? bitmap : bitmap ^ bit : bitmap | bit;
	      var newNodes = exists ? newNode ?
	        setIn(nodes, idx, newNode, isEditable) :
	        spliceOut(nodes, idx, isEditable) :
	        spliceIn(nodes, idx, newNode, isEditable);

	      if (isEditable) {
	        this.bitmap = newBitmap;
	        this.nodes = newNodes;
	        return this;
	      }

	      return new BitmapIndexedNode(ownerID, newBitmap, newNodes);
	    };




	    function HashArrayMapNode(ownerID, count, nodes) {
	      this.ownerID = ownerID;
	      this.count = count;
	      this.nodes = nodes;
	    }

	    HashArrayMapNode.prototype.get = function(shift, keyHash, key, notSetValue) {
	      if (keyHash === undefined) {
	        keyHash = hash(key);
	      }
	      var idx = (shift === 0 ? keyHash : keyHash >>> shift) & MASK;
	      var node = this.nodes[idx];
	      return node ? node.get(shift + SHIFT, keyHash, key, notSetValue) : notSetValue;
	    };

	    HashArrayMapNode.prototype.update = function(ownerID, shift, keyHash, key, value, didChangeSize, didAlter) {
	      if (keyHash === undefined) {
	        keyHash = hash(key);
	      }
	      var idx = (shift === 0 ? keyHash : keyHash >>> shift) & MASK;
	      var removed = value === NOT_SET;
	      var nodes = this.nodes;
	      var node = nodes[idx];

	      if (removed && !node) {
	        return this;
	      }

	      var newNode = updateNode(node, ownerID, shift + SHIFT, keyHash, key, value, didChangeSize, didAlter);
	      if (newNode === node) {
	        return this;
	      }

	      var newCount = this.count;
	      if (!node) {
	        newCount++;
	      } else if (!newNode) {
	        newCount--;
	        if (newCount < MIN_HASH_ARRAY_MAP_SIZE) {
	          return packNodes(ownerID, nodes, newCount, idx);
	        }
	      }

	      var isEditable = ownerID && ownerID === this.ownerID;
	      var newNodes = setIn(nodes, idx, newNode, isEditable);

	      if (isEditable) {
	        this.count = newCount;
	        this.nodes = newNodes;
	        return this;
	      }

	      return new HashArrayMapNode(ownerID, newCount, newNodes);
	    };




	    function HashCollisionNode(ownerID, keyHash, entries) {
	      this.ownerID = ownerID;
	      this.keyHash = keyHash;
	      this.entries = entries;
	    }

	    HashCollisionNode.prototype.get = function(shift, keyHash, key, notSetValue) {
	      var entries = this.entries;
	      for (var ii = 0, len = entries.length; ii < len; ii++) {
	        if (is(key, entries[ii][0])) {
	          return entries[ii][1];
	        }
	      }
	      return notSetValue;
	    };

	    HashCollisionNode.prototype.update = function(ownerID, shift, keyHash, key, value, didChangeSize, didAlter) {
	      if (keyHash === undefined) {
	        keyHash = hash(key);
	      }

	      var removed = value === NOT_SET;

	      if (keyHash !== this.keyHash) {
	        if (removed) {
	          return this;
	        }
	        SetRef(didAlter);
	        SetRef(didChangeSize);
	        return mergeIntoNode(this, ownerID, shift, keyHash, [key, value]);
	      }

	      var entries = this.entries;
	      var idx = 0;
	      for (var len = entries.length; idx < len; idx++) {
	        if (is(key, entries[idx][0])) {
	          break;
	        }
	      }
	      var exists = idx < len;

	      if (exists ? entries[idx][1] === value : removed) {
	        return this;
	      }

	      SetRef(didAlter);
	      (removed || !exists) && SetRef(didChangeSize);

	      if (removed && len === 2) {
	        return new ValueNode(ownerID, this.keyHash, entries[idx ^ 1]);
	      }

	      var isEditable = ownerID && ownerID === this.ownerID;
	      var newEntries = isEditable ? entries : arrCopy(entries);

	      if (exists) {
	        if (removed) {
	          idx === len - 1 ? newEntries.pop() : (newEntries[idx] = newEntries.pop());
	        } else {
	          newEntries[idx] = [key, value];
	        }
	      } else {
	        newEntries.push([key, value]);
	      }

	      if (isEditable) {
	        this.entries = newEntries;
	        return this;
	      }

	      return new HashCollisionNode(ownerID, this.keyHash, newEntries);
	    };




	    function ValueNode(ownerID, keyHash, entry) {
	      this.ownerID = ownerID;
	      this.keyHash = keyHash;
	      this.entry = entry;
	    }

	    ValueNode.prototype.get = function(shift, keyHash, key, notSetValue) {
	      return is(key, this.entry[0]) ? this.entry[1] : notSetValue;
	    };

	    ValueNode.prototype.update = function(ownerID, shift, keyHash, key, value, didChangeSize, didAlter) {
	      var removed = value === NOT_SET;
	      var keyMatch = is(key, this.entry[0]);
	      if (keyMatch ? value === this.entry[1] : removed) {
	        return this;
	      }

	      SetRef(didAlter);

	      if (removed) {
	        SetRef(didChangeSize);
	        return; // undefined
	      }

	      if (keyMatch) {
	        if (ownerID && ownerID === this.ownerID) {
	          this.entry[1] = value;
	          return this;
	        }
	        return new ValueNode(ownerID, this.keyHash, [key, value]);
	      }

	      SetRef(didChangeSize);
	      return mergeIntoNode(this, ownerID, shift, hash(key), [key, value]);
	    };



	  // #pragma Iterators

	  ArrayMapNode.prototype.iterate =
	  HashCollisionNode.prototype.iterate = function (fn, reverse) {
	    var entries = this.entries;
	    for (var ii = 0, maxIndex = entries.length - 1; ii <= maxIndex; ii++) {
	      if (fn(entries[reverse ? maxIndex - ii : ii]) === false) {
	        return false;
	      }
	    }
	  }

	  BitmapIndexedNode.prototype.iterate =
	  HashArrayMapNode.prototype.iterate = function (fn, reverse) {
	    var nodes = this.nodes;
	    for (var ii = 0, maxIndex = nodes.length - 1; ii <= maxIndex; ii++) {
	      var node = nodes[reverse ? maxIndex - ii : ii];
	      if (node && node.iterate(fn, reverse) === false) {
	        return false;
	      }
	    }
	  }

	  ValueNode.prototype.iterate = function (fn, reverse) {
	    return fn(this.entry);
	  }

	  createClass(MapIterator, Iterator);

	    function MapIterator(map, type, reverse) {
	      this._type = type;
	      this._reverse = reverse;
	      this._stack = map._root && mapIteratorFrame(map._root);
	    }

	    MapIterator.prototype.next = function() {
	      var type = this._type;
	      var stack = this._stack;
	      while (stack) {
	        var node = stack.node;
	        var index = stack.index++;
	        var maxIndex;
	        if (node.entry) {
	          if (index === 0) {
	            return mapIteratorValue(type, node.entry);
	          }
	        } else if (node.entries) {
	          maxIndex = node.entries.length - 1;
	          if (index <= maxIndex) {
	            return mapIteratorValue(type, node.entries[this._reverse ? maxIndex - index : index]);
	          }
	        } else {
	          maxIndex = node.nodes.length - 1;
	          if (index <= maxIndex) {
	            var subNode = node.nodes[this._reverse ? maxIndex - index : index];
	            if (subNode) {
	              if (subNode.entry) {
	                return mapIteratorValue(type, subNode.entry);
	              }
	              stack = this._stack = mapIteratorFrame(subNode, stack);
	            }
	            continue;
	          }
	        }
	        stack = this._stack = this._stack.__prev;
	      }
	      return iteratorDone();
	    };


	  function mapIteratorValue(type, entry) {
	    return iteratorValue(type, entry[0], entry[1]);
	  }

	  function mapIteratorFrame(node, prev) {
	    return {
	      node: node,
	      index: 0,
	      __prev: prev
	    };
	  }

	  function makeMap(size, root, ownerID, hash) {
	    var map = Object.create(MapPrototype);
	    map.size = size;
	    map._root = root;
	    map.__ownerID = ownerID;
	    map.__hash = hash;
	    map.__altered = false;
	    return map;
	  }

	  var EMPTY_MAP;
	  function emptyMap() {
	    return EMPTY_MAP || (EMPTY_MAP = makeMap(0));
	  }

	  function updateMap(map, k, v) {
	    var newRoot;
	    var newSize;
	    if (!map._root) {
	      if (v === NOT_SET) {
	        return map;
	      }
	      newSize = 1;
	      newRoot = new ArrayMapNode(map.__ownerID, [[k, v]]);
	    } else {
	      var didChangeSize = MakeRef(CHANGE_LENGTH);
	      var didAlter = MakeRef(DID_ALTER);
	      newRoot = updateNode(map._root, map.__ownerID, 0, undefined, k, v, didChangeSize, didAlter);
	      if (!didAlter.value) {
	        return map;
	      }
	      newSize = map.size + (didChangeSize.value ? v === NOT_SET ? -1 : 1 : 0);
	    }
	    if (map.__ownerID) {
	      map.size = newSize;
	      map._root = newRoot;
	      map.__hash = undefined;
	      map.__altered = true;
	      return map;
	    }
	    return newRoot ? makeMap(newSize, newRoot) : emptyMap();
	  }

	  function updateNode(node, ownerID, shift, keyHash, key, value, didChangeSize, didAlter) {
	    if (!node) {
	      if (value === NOT_SET) {
	        return node;
	      }
	      SetRef(didAlter);
	      SetRef(didChangeSize);
	      return new ValueNode(ownerID, keyHash, [key, value]);
	    }
	    return node.update(ownerID, shift, keyHash, key, value, didChangeSize, didAlter);
	  }

	  function isLeafNode(node) {
	    return node.constructor === ValueNode || node.constructor === HashCollisionNode;
	  }

	  function mergeIntoNode(node, ownerID, shift, keyHash, entry) {
	    if (node.keyHash === keyHash) {
	      return new HashCollisionNode(ownerID, keyHash, [node.entry, entry]);
	    }

	    var idx1 = (shift === 0 ? node.keyHash : node.keyHash >>> shift) & MASK;
	    var idx2 = (shift === 0 ? keyHash : keyHash >>> shift) & MASK;

	    var newNode;
	    var nodes = idx1 === idx2 ?
	      [mergeIntoNode(node, ownerID, shift + SHIFT, keyHash, entry)] :
	      ((newNode = new ValueNode(ownerID, keyHash, entry)), idx1 < idx2 ? [node, newNode] : [newNode, node]);

	    return new BitmapIndexedNode(ownerID, (1 << idx1) | (1 << idx2), nodes);
	  }

	  function createNodes(ownerID, entries, key, value) {
	    if (!ownerID) {
	      ownerID = new OwnerID();
	    }
	    var node = new ValueNode(ownerID, hash(key), [key, value]);
	    for (var ii = 0; ii < entries.length; ii++) {
	      var entry = entries[ii];
	      node = node.update(ownerID, 0, undefined, entry[0], entry[1]);
	    }
	    return node;
	  }

	  function packNodes(ownerID, nodes, count, excluding) {
	    var bitmap = 0;
	    var packedII = 0;
	    var packedNodes = new Array(count);
	    for (var ii = 0, bit = 1, len = nodes.length; ii < len; ii++, bit <<= 1) {
	      var node = nodes[ii];
	      if (node !== undefined && ii !== excluding) {
	        bitmap |= bit;
	        packedNodes[packedII++] = node;
	      }
	    }
	    return new BitmapIndexedNode(ownerID, bitmap, packedNodes);
	  }

	  function expandNodes(ownerID, nodes, bitmap, including, node) {
	    var count = 0;
	    var expandedNodes = new Array(SIZE);
	    for (var ii = 0; bitmap !== 0; ii++, bitmap >>>= 1) {
	      expandedNodes[ii] = bitmap & 1 ? nodes[count++] : undefined;
	    }
	    expandedNodes[including] = node;
	    return new HashArrayMapNode(ownerID, count + 1, expandedNodes);
	  }

	  function mergeIntoMapWith(map, merger, iterables) {
	    var iters = [];
	    for (var ii = 0; ii < iterables.length; ii++) {
	      var value = iterables[ii];
	      var iter = KeyedIterable(value);
	      if (!isIterable(value)) {
	        iter = iter.map(function(v ) {return fromJS(v)});
	      }
	      iters.push(iter);
	    }
	    return mergeIntoCollectionWith(map, merger, iters);
	  }

	  function deepMerger(existing, value, key) {
	    return existing && existing.mergeDeep && isIterable(value) ?
	      existing.mergeDeep(value) :
	      is(existing, value) ? existing : value;
	  }

	  function deepMergerWith(merger) {
	    return function(existing, value, key)  {
	      if (existing && existing.mergeDeepWith && isIterable(value)) {
	        return existing.mergeDeepWith(merger, value);
	      }
	      var nextValue = merger(existing, value, key);
	      return is(existing, nextValue) ? existing : nextValue;
	    };
	  }

	  function mergeIntoCollectionWith(collection, merger, iters) {
	    iters = iters.filter(function(x ) {return x.size !== 0});
	    if (iters.length === 0) {
	      return collection;
	    }
	    if (collection.size === 0 && !collection.__ownerID && iters.length === 1) {
	      return collection.constructor(iters[0]);
	    }
	    return collection.withMutations(function(collection ) {
	      var mergeIntoMap = merger ?
	        function(value, key)  {
	          collection.update(key, NOT_SET, function(existing )
	            {return existing === NOT_SET ? value : merger(existing, value, key)}
	          );
	        } :
	        function(value, key)  {
	          collection.set(key, value);
	        }
	      for (var ii = 0; ii < iters.length; ii++) {
	        iters[ii].forEach(mergeIntoMap);
	      }
	    });
	  }

	  function updateInDeepMap(existing, keyPathIter, notSetValue, updater) {
	    var isNotSet = existing === NOT_SET;
	    var step = keyPathIter.next();
	    if (step.done) {
	      var existingValue = isNotSet ? notSetValue : existing;
	      var newValue = updater(existingValue);
	      return newValue === existingValue ? existing : newValue;
	    }
	    invariant(
	      isNotSet || (existing && existing.set),
	      'invalid keyPath'
	    );
	    var key = step.value;
	    var nextExisting = isNotSet ? NOT_SET : existing.get(key, NOT_SET);
	    var nextUpdated = updateInDeepMap(
	      nextExisting,
	      keyPathIter,
	      notSetValue,
	      updater
	    );
	    return nextUpdated === nextExisting ? existing :
	      nextUpdated === NOT_SET ? existing.remove(key) :
	      (isNotSet ? emptyMap() : existing).set(key, nextUpdated);
	  }

	  function popCount(x) {
	    x = x - ((x >> 1) & 0x55555555);
	    x = (x & 0x33333333) + ((x >> 2) & 0x33333333);
	    x = (x + (x >> 4)) & 0x0f0f0f0f;
	    x = x + (x >> 8);
	    x = x + (x >> 16);
	    return x & 0x7f;
	  }

	  function setIn(array, idx, val, canEdit) {
	    var newArray = canEdit ? array : arrCopy(array);
	    newArray[idx] = val;
	    return newArray;
	  }

	  function spliceIn(array, idx, val, canEdit) {
	    var newLen = array.length + 1;
	    if (canEdit && idx + 1 === newLen) {
	      array[idx] = val;
	      return array;
	    }
	    var newArray = new Array(newLen);
	    var after = 0;
	    for (var ii = 0; ii < newLen; ii++) {
	      if (ii === idx) {
	        newArray[ii] = val;
	        after = -1;
	      } else {
	        newArray[ii] = array[ii + after];
	      }
	    }
	    return newArray;
	  }

	  function spliceOut(array, idx, canEdit) {
	    var newLen = array.length - 1;
	    if (canEdit && idx === newLen) {
	      array.pop();
	      return array;
	    }
	    var newArray = new Array(newLen);
	    var after = 0;
	    for (var ii = 0; ii < newLen; ii++) {
	      if (ii === idx) {
	        after = 1;
	      }
	      newArray[ii] = array[ii + after];
	    }
	    return newArray;
	  }

	  var MAX_ARRAY_MAP_SIZE = SIZE / 4;
	  var MAX_BITMAP_INDEXED_SIZE = SIZE / 2;
	  var MIN_HASH_ARRAY_MAP_SIZE = SIZE / 4;

	  createClass(List, IndexedCollection);

	    // @pragma Construction

	    function List(value) {
	      var empty = emptyList();
	      if (value === null || value === undefined) {
	        return empty;
	      }
	      if (isList(value)) {
	        return value;
	      }
	      var iter = IndexedIterable(value);
	      var size = iter.size;
	      if (size === 0) {
	        return empty;
	      }
	      assertNotInfinite(size);
	      if (size > 0 && size < SIZE) {
	        return makeList(0, size, SHIFT, null, new VNode(iter.toArray()));
	      }
	      return empty.withMutations(function(list ) {
	        list.setSize(size);
	        iter.forEach(function(v, i)  {return list.set(i, v)});
	      });
	    }

	    List.of = function(/*...values*/) {
	      return this(arguments);
	    };

	    List.prototype.toString = function() {
	      return this.__toString('List [', ']');
	    };

	    // @pragma Access

	    List.prototype.get = function(index, notSetValue) {
	      index = wrapIndex(this, index);
	      if (index >= 0 && index < this.size) {
	        index += this._origin;
	        var node = listNodeFor(this, index);
	        return node && node.array[index & MASK];
	      }
	      return notSetValue;
	    };

	    // @pragma Modification

	    List.prototype.set = function(index, value) {
	      return updateList(this, index, value);
	    };

	    List.prototype.remove = function(index) {
	      return !this.has(index) ? this :
	        index === 0 ? this.shift() :
	        index === this.size - 1 ? this.pop() :
	        this.splice(index, 1);
	    };

	    List.prototype.insert = function(index, value) {
	      return this.splice(index, 0, value);
	    };

	    List.prototype.clear = function() {
	      if (this.size === 0) {
	        return this;
	      }
	      if (this.__ownerID) {
	        this.size = this._origin = this._capacity = 0;
	        this._level = SHIFT;
	        this._root = this._tail = null;
	        this.__hash = undefined;
	        this.__altered = true;
	        return this;
	      }
	      return emptyList();
	    };

	    List.prototype.push = function(/*...values*/) {
	      var values = arguments;
	      var oldSize = this.size;
	      return this.withMutations(function(list ) {
	        setListBounds(list, 0, oldSize + values.length);
	        for (var ii = 0; ii < values.length; ii++) {
	          list.set(oldSize + ii, values[ii]);
	        }
	      });
	    };

	    List.prototype.pop = function() {
	      return setListBounds(this, 0, -1);
	    };

	    List.prototype.unshift = function(/*...values*/) {
	      var values = arguments;
	      return this.withMutations(function(list ) {
	        setListBounds(list, -values.length);
	        for (var ii = 0; ii < values.length; ii++) {
	          list.set(ii, values[ii]);
	        }
	      });
	    };

	    List.prototype.shift = function() {
	      return setListBounds(this, 1);
	    };

	    // @pragma Composition

	    List.prototype.merge = function(/*...iters*/) {
	      return mergeIntoListWith(this, undefined, arguments);
	    };

	    List.prototype.mergeWith = function(merger) {var iters = SLICE$0.call(arguments, 1);
	      return mergeIntoListWith(this, merger, iters);
	    };

	    List.prototype.mergeDeep = function(/*...iters*/) {
	      return mergeIntoListWith(this, deepMerger, arguments);
	    };

	    List.prototype.mergeDeepWith = function(merger) {var iters = SLICE$0.call(arguments, 1);
	      return mergeIntoListWith(this, deepMergerWith(merger), iters);
	    };

	    List.prototype.setSize = function(size) {
	      return setListBounds(this, 0, size);
	    };

	    // @pragma Iteration

	    List.prototype.slice = function(begin, end) {
	      var size = this.size;
	      if (wholeSlice(begin, end, size)) {
	        return this;
	      }
	      return setListBounds(
	        this,
	        resolveBegin(begin, size),
	        resolveEnd(end, size)
	      );
	    };

	    List.prototype.__iterator = function(type, reverse) {
	      var index = 0;
	      var values = iterateList(this, reverse);
	      return new Iterator(function()  {
	        var value = values();
	        return value === DONE ?
	          iteratorDone() :
	          iteratorValue(type, index++, value);
	      });
	    };

	    List.prototype.__iterate = function(fn, reverse) {
	      var index = 0;
	      var values = iterateList(this, reverse);
	      var value;
	      while ((value = values()) !== DONE) {
	        if (fn(value, index++, this) === false) {
	          break;
	        }
	      }
	      return index;
	    };

	    List.prototype.__ensureOwner = function(ownerID) {
	      if (ownerID === this.__ownerID) {
	        return this;
	      }
	      if (!ownerID) {
	        this.__ownerID = ownerID;
	        return this;
	      }
	      return makeList(this._origin, this._capacity, this._level, this._root, this._tail, ownerID, this.__hash);
	    };


	  function isList(maybeList) {
	    return !!(maybeList && maybeList[IS_LIST_SENTINEL]);
	  }

	  List.isList = isList;

	  var IS_LIST_SENTINEL = '@@__IMMUTABLE_LIST__@@';

	  var ListPrototype = List.prototype;
	  ListPrototype[IS_LIST_SENTINEL] = true;
	  ListPrototype[DELETE] = ListPrototype.remove;
	  ListPrototype.setIn = MapPrototype.setIn;
	  ListPrototype.deleteIn =
	  ListPrototype.removeIn = MapPrototype.removeIn;
	  ListPrototype.update = MapPrototype.update;
	  ListPrototype.updateIn = MapPrototype.updateIn;
	  ListPrototype.mergeIn = MapPrototype.mergeIn;
	  ListPrototype.mergeDeepIn = MapPrototype.mergeDeepIn;
	  ListPrototype.withMutations = MapPrototype.withMutations;
	  ListPrototype.asMutable = MapPrototype.asMutable;
	  ListPrototype.asImmutable = MapPrototype.asImmutable;
	  ListPrototype.wasAltered = MapPrototype.wasAltered;



	    function VNode(array, ownerID) {
	      this.array = array;
	      this.ownerID = ownerID;
	    }

	    // TODO: seems like these methods are very similar

	    VNode.prototype.removeBefore = function(ownerID, level, index) {
	      if (index === level ? 1 << level : 0 || this.array.length === 0) {
	        return this;
	      }
	      var originIndex = (index >>> level) & MASK;
	      if (originIndex >= this.array.length) {
	        return new VNode([], ownerID);
	      }
	      var removingFirst = originIndex === 0;
	      var newChild;
	      if (level > 0) {
	        var oldChild = this.array[originIndex];
	        newChild = oldChild && oldChild.removeBefore(ownerID, level - SHIFT, index);
	        if (newChild === oldChild && removingFirst) {
	          return this;
	        }
	      }
	      if (removingFirst && !newChild) {
	        return this;
	      }
	      var editable = editableVNode(this, ownerID);
	      if (!removingFirst) {
	        for (var ii = 0; ii < originIndex; ii++) {
	          editable.array[ii] = undefined;
	        }
	      }
	      if (newChild) {
	        editable.array[originIndex] = newChild;
	      }
	      return editable;
	    };

	    VNode.prototype.removeAfter = function(ownerID, level, index) {
	      if (index === (level ? 1 << level : 0) || this.array.length === 0) {
	        return this;
	      }
	      var sizeIndex = ((index - 1) >>> level) & MASK;
	      if (sizeIndex >= this.array.length) {
	        return this;
	      }

	      var newChild;
	      if (level > 0) {
	        var oldChild = this.array[sizeIndex];
	        newChild = oldChild && oldChild.removeAfter(ownerID, level - SHIFT, index);
	        if (newChild === oldChild && sizeIndex === this.array.length - 1) {
	          return this;
	        }
	      }

	      var editable = editableVNode(this, ownerID);
	      editable.array.splice(sizeIndex + 1);
	      if (newChild) {
	        editable.array[sizeIndex] = newChild;
	      }
	      return editable;
	    };



	  var DONE = {};

	  function iterateList(list, reverse) {
	    var left = list._origin;
	    var right = list._capacity;
	    var tailPos = getTailOffset(right);
	    var tail = list._tail;

	    return iterateNodeOrLeaf(list._root, list._level, 0);

	    function iterateNodeOrLeaf(node, level, offset) {
	      return level === 0 ?
	        iterateLeaf(node, offset) :
	        iterateNode(node, level, offset);
	    }

	    function iterateLeaf(node, offset) {
	      var array = offset === tailPos ? tail && tail.array : node && node.array;
	      var from = offset > left ? 0 : left - offset;
	      var to = right - offset;
	      if (to > SIZE) {
	        to = SIZE;
	      }
	      return function()  {
	        if (from === to) {
	          return DONE;
	        }
	        var idx = reverse ? --to : from++;
	        return array && array[idx];
	      };
	    }

	    function iterateNode(node, level, offset) {
	      var values;
	      var array = node && node.array;
	      var from = offset > left ? 0 : (left - offset) >> level;
	      var to = ((right - offset) >> level) + 1;
	      if (to > SIZE) {
	        to = SIZE;
	      }
	      return function()  {
	        do {
	          if (values) {
	            var value = values();
	            if (value !== DONE) {
	              return value;
	            }
	            values = null;
	          }
	          if (from === to) {
	            return DONE;
	          }
	          var idx = reverse ? --to : from++;
	          values = iterateNodeOrLeaf(
	            array && array[idx], level - SHIFT, offset + (idx << level)
	          );
	        } while (true);
	      };
	    }
	  }

	  function makeList(origin, capacity, level, root, tail, ownerID, hash) {
	    var list = Object.create(ListPrototype);
	    list.size = capacity - origin;
	    list._origin = origin;
	    list._capacity = capacity;
	    list._level = level;
	    list._root = root;
	    list._tail = tail;
	    list.__ownerID = ownerID;
	    list.__hash = hash;
	    list.__altered = false;
	    return list;
	  }

	  var EMPTY_LIST;
	  function emptyList() {
	    return EMPTY_LIST || (EMPTY_LIST = makeList(0, 0, SHIFT));
	  }

	  function updateList(list, index, value) {
	    index = wrapIndex(list, index);

	    if (index !== index) {
	      return list;
	    }

	    if (index >= list.size || index < 0) {
	      return list.withMutations(function(list ) {
	        index < 0 ?
	          setListBounds(list, index).set(0, value) :
	          setListBounds(list, 0, index + 1).set(index, value)
	      });
	    }

	    index += list._origin;

	    var newTail = list._tail;
	    var newRoot = list._root;
	    var didAlter = MakeRef(DID_ALTER);
	    if (index >= getTailOffset(list._capacity)) {
	      newTail = updateVNode(newTail, list.__ownerID, 0, index, value, didAlter);
	    } else {
	      newRoot = updateVNode(newRoot, list.__ownerID, list._level, index, value, didAlter);
	    }

	    if (!didAlter.value) {
	      return list;
	    }

	    if (list.__ownerID) {
	      list._root = newRoot;
	      list._tail = newTail;
	      list.__hash = undefined;
	      list.__altered = true;
	      return list;
	    }
	    return makeList(list._origin, list._capacity, list._level, newRoot, newTail);
	  }

	  function updateVNode(node, ownerID, level, index, value, didAlter) {
	    var idx = (index >>> level) & MASK;
	    var nodeHas = node && idx < node.array.length;
	    if (!nodeHas && value === undefined) {
	      return node;
	    }

	    var newNode;

	    if (level > 0) {
	      var lowerNode = node && node.array[idx];
	      var newLowerNode = updateVNode(lowerNode, ownerID, level - SHIFT, index, value, didAlter);
	      if (newLowerNode === lowerNode) {
	        return node;
	      }
	      newNode = editableVNode(node, ownerID);
	      newNode.array[idx] = newLowerNode;
	      return newNode;
	    }

	    if (nodeHas && node.array[idx] === value) {
	      return node;
	    }

	    SetRef(didAlter);

	    newNode = editableVNode(node, ownerID);
	    if (value === undefined && idx === newNode.array.length - 1) {
	      newNode.array.pop();
	    } else {
	      newNode.array[idx] = value;
	    }
	    return newNode;
	  }

	  function editableVNode(node, ownerID) {
	    if (ownerID && node && ownerID === node.ownerID) {
	      return node;
	    }
	    return new VNode(node ? node.array.slice() : [], ownerID);
	  }

	  function listNodeFor(list, rawIndex) {
	    if (rawIndex >= getTailOffset(list._capacity)) {
	      return list._tail;
	    }
	    if (rawIndex < 1 << (list._level + SHIFT)) {
	      var node = list._root;
	      var level = list._level;
	      while (node && level > 0) {
	        node = node.array[(rawIndex >>> level) & MASK];
	        level -= SHIFT;
	      }
	      return node;
	    }
	  }

	  function setListBounds(list, begin, end) {
	    // Sanitize begin & end using this shorthand for ToInt32(argument)
	    // http://www.ecma-international.org/ecma-262/6.0/#sec-toint32
	    if (begin !== undefined) {
	      begin = begin | 0;
	    }
	    if (end !== undefined) {
	      end = end | 0;
	    }
	    var owner = list.__ownerID || new OwnerID();
	    var oldOrigin = list._origin;
	    var oldCapacity = list._capacity;
	    var newOrigin = oldOrigin + begin;
	    var newCapacity = end === undefined ? oldCapacity : end < 0 ? oldCapacity + end : oldOrigin + end;
	    if (newOrigin === oldOrigin && newCapacity === oldCapacity) {
	      return list;
	    }

	    // If it's going to end after it starts, it's empty.
	    if (newOrigin >= newCapacity) {
	      return list.clear();
	    }

	    var newLevel = list._level;
	    var newRoot = list._root;

	    // New origin might need creating a higher root.
	    var offsetShift = 0;
	    while (newOrigin + offsetShift < 0) {
	      newRoot = new VNode(newRoot && newRoot.array.length ? [undefined, newRoot] : [], owner);
	      newLevel += SHIFT;
	      offsetShift += 1 << newLevel;
	    }
	    if (offsetShift) {
	      newOrigin += offsetShift;
	      oldOrigin += offsetShift;
	      newCapacity += offsetShift;
	      oldCapacity += offsetShift;
	    }

	    var oldTailOffset = getTailOffset(oldCapacity);
	    var newTailOffset = getTailOffset(newCapacity);

	    // New size might need creating a higher root.
	    while (newTailOffset >= 1 << (newLevel + SHIFT)) {
	      newRoot = new VNode(newRoot && newRoot.array.length ? [newRoot] : [], owner);
	      newLevel += SHIFT;
	    }

	    // Locate or create the new tail.
	    var oldTail = list._tail;
	    var newTail = newTailOffset < oldTailOffset ?
	      listNodeFor(list, newCapacity - 1) :
	      newTailOffset > oldTailOffset ? new VNode([], owner) : oldTail;

	    // Merge Tail into tree.
	    if (oldTail && newTailOffset > oldTailOffset && newOrigin < oldCapacity && oldTail.array.length) {
	      newRoot = editableVNode(newRoot, owner);
	      var node = newRoot;
	      for (var level = newLevel; level > SHIFT; level -= SHIFT) {
	        var idx = (oldTailOffset >>> level) & MASK;
	        node = node.array[idx] = editableVNode(node.array[idx], owner);
	      }
	      node.array[(oldTailOffset >>> SHIFT) & MASK] = oldTail;
	    }

	    // If the size has been reduced, there's a chance the tail needs to be trimmed.
	    if (newCapacity < oldCapacity) {
	      newTail = newTail && newTail.removeAfter(owner, 0, newCapacity);
	    }

	    // If the new origin is within the tail, then we do not need a root.
	    if (newOrigin >= newTailOffset) {
	      newOrigin -= newTailOffset;
	      newCapacity -= newTailOffset;
	      newLevel = SHIFT;
	      newRoot = null;
	      newTail = newTail && newTail.removeBefore(owner, 0, newOrigin);

	    // Otherwise, if the root has been trimmed, garbage collect.
	    } else if (newOrigin > oldOrigin || newTailOffset < oldTailOffset) {
	      offsetShift = 0;

	      // Identify the new top root node of the subtree of the old root.
	      while (newRoot) {
	        var beginIndex = (newOrigin >>> newLevel) & MASK;
	        if (beginIndex !== (newTailOffset >>> newLevel) & MASK) {
	          break;
	        }
	        if (beginIndex) {
	          offsetShift += (1 << newLevel) * beginIndex;
	        }
	        newLevel -= SHIFT;
	        newRoot = newRoot.array[beginIndex];
	      }

	      // Trim the new sides of the new root.
	      if (newRoot && newOrigin > oldOrigin) {
	        newRoot = newRoot.removeBefore(owner, newLevel, newOrigin - offsetShift);
	      }
	      if (newRoot && newTailOffset < oldTailOffset) {
	        newRoot = newRoot.removeAfter(owner, newLevel, newTailOffset - offsetShift);
	      }
	      if (offsetShift) {
	        newOrigin -= offsetShift;
	        newCapacity -= offsetShift;
	      }
	    }

	    if (list.__ownerID) {
	      list.size = newCapacity - newOrigin;
	      list._origin = newOrigin;
	      list._capacity = newCapacity;
	      list._level = newLevel;
	      list._root = newRoot;
	      list._tail = newTail;
	      list.__hash = undefined;
	      list.__altered = true;
	      return list;
	    }
	    return makeList(newOrigin, newCapacity, newLevel, newRoot, newTail);
	  }

	  function mergeIntoListWith(list, merger, iterables) {
	    var iters = [];
	    var maxSize = 0;
	    for (var ii = 0; ii < iterables.length; ii++) {
	      var value = iterables[ii];
	      var iter = IndexedIterable(value);
	      if (iter.size > maxSize) {
	        maxSize = iter.size;
	      }
	      if (!isIterable(value)) {
	        iter = iter.map(function(v ) {return fromJS(v)});
	      }
	      iters.push(iter);
	    }
	    if (maxSize > list.size) {
	      list = list.setSize(maxSize);
	    }
	    return mergeIntoCollectionWith(list, merger, iters);
	  }

	  function getTailOffset(size) {
	    return size < SIZE ? 0 : (((size - 1) >>> SHIFT) << SHIFT);
	  }

	  createClass(OrderedMap, Map);

	    // @pragma Construction

	    function OrderedMap(value) {
	      return value === null || value === undefined ? emptyOrderedMap() :
	        isOrderedMap(value) ? value :
	        emptyOrderedMap().withMutations(function(map ) {
	          var iter = KeyedIterable(value);
	          assertNotInfinite(iter.size);
	          iter.forEach(function(v, k)  {return map.set(k, v)});
	        });
	    }

	    OrderedMap.of = function(/*...values*/) {
	      return this(arguments);
	    };

	    OrderedMap.prototype.toString = function() {
	      return this.__toString('OrderedMap {', '}');
	    };

	    // @pragma Access

	    OrderedMap.prototype.get = function(k, notSetValue) {
	      var index = this._map.get(k);
	      return index !== undefined ? this._list.get(index)[1] : notSetValue;
	    };

	    // @pragma Modification

	    OrderedMap.prototype.clear = function() {
	      if (this.size === 0) {
	        return this;
	      }
	      if (this.__ownerID) {
	        this.size = 0;
	        this._map.clear();
	        this._list.clear();
	        return this;
	      }
	      return emptyOrderedMap();
	    };

	    OrderedMap.prototype.set = function(k, v) {
	      return updateOrderedMap(this, k, v);
	    };

	    OrderedMap.prototype.remove = function(k) {
	      return updateOrderedMap(this, k, NOT_SET);
	    };

	    OrderedMap.prototype.wasAltered = function() {
	      return this._map.wasAltered() || this._list.wasAltered();
	    };

	    OrderedMap.prototype.__iterate = function(fn, reverse) {var this$0 = this;
	      return this._list.__iterate(
	        function(entry ) {return entry && fn(entry[1], entry[0], this$0)},
	        reverse
	      );
	    };

	    OrderedMap.prototype.__iterator = function(type, reverse) {
	      return this._list.fromEntrySeq().__iterator(type, reverse);
	    };

	    OrderedMap.prototype.__ensureOwner = function(ownerID) {
	      if (ownerID === this.__ownerID) {
	        return this;
	      }
	      var newMap = this._map.__ensureOwner(ownerID);
	      var newList = this._list.__ensureOwner(ownerID);
	      if (!ownerID) {
	        this.__ownerID = ownerID;
	        this._map = newMap;
	        this._list = newList;
	        return this;
	      }
	      return makeOrderedMap(newMap, newList, ownerID, this.__hash);
	    };


	  function isOrderedMap(maybeOrderedMap) {
	    return isMap(maybeOrderedMap) && isOrdered(maybeOrderedMap);
	  }

	  OrderedMap.isOrderedMap = isOrderedMap;

	  OrderedMap.prototype[IS_ORDERED_SENTINEL] = true;
	  OrderedMap.prototype[DELETE] = OrderedMap.prototype.remove;



	  function makeOrderedMap(map, list, ownerID, hash) {
	    var omap = Object.create(OrderedMap.prototype);
	    omap.size = map ? map.size : 0;
	    omap._map = map;
	    omap._list = list;
	    omap.__ownerID = ownerID;
	    omap.__hash = hash;
	    return omap;
	  }

	  var EMPTY_ORDERED_MAP;
	  function emptyOrderedMap() {
	    return EMPTY_ORDERED_MAP || (EMPTY_ORDERED_MAP = makeOrderedMap(emptyMap(), emptyList()));
	  }

	  function updateOrderedMap(omap, k, v) {
	    var map = omap._map;
	    var list = omap._list;
	    var i = map.get(k);
	    var has = i !== undefined;
	    var newMap;
	    var newList;
	    if (v === NOT_SET) { // removed
	      if (!has) {
	        return omap;
	      }
	      if (list.size >= SIZE && list.size >= map.size * 2) {
	        newList = list.filter(function(entry, idx)  {return entry !== undefined && i !== idx});
	        newMap = newList.toKeyedSeq().map(function(entry ) {return entry[0]}).flip().toMap();
	        if (omap.__ownerID) {
	          newMap.__ownerID = newList.__ownerID = omap.__ownerID;
	        }
	      } else {
	        newMap = map.remove(k);
	        newList = i === list.size - 1 ? list.pop() : list.set(i, undefined);
	      }
	    } else {
	      if (has) {
	        if (v === list.get(i)[1]) {
	          return omap;
	        }
	        newMap = map;
	        newList = list.set(i, [k, v]);
	      } else {
	        newMap = map.set(k, list.size);
	        newList = list.set(list.size, [k, v]);
	      }
	    }
	    if (omap.__ownerID) {
	      omap.size = newMap.size;
	      omap._map = newMap;
	      omap._list = newList;
	      omap.__hash = undefined;
	      return omap;
	    }
	    return makeOrderedMap(newMap, newList);
	  }

	  createClass(ToKeyedSequence, KeyedSeq);
	    function ToKeyedSequence(indexed, useKeys) {
	      this._iter = indexed;
	      this._useKeys = useKeys;
	      this.size = indexed.size;
	    }

	    ToKeyedSequence.prototype.get = function(key, notSetValue) {
	      return this._iter.get(key, notSetValue);
	    };

	    ToKeyedSequence.prototype.has = function(key) {
	      return this._iter.has(key);
	    };

	    ToKeyedSequence.prototype.valueSeq = function() {
	      return this._iter.valueSeq();
	    };

	    ToKeyedSequence.prototype.reverse = function() {var this$0 = this;
	      var reversedSequence = reverseFactory(this, true);
	      if (!this._useKeys) {
	        reversedSequence.valueSeq = function()  {return this$0._iter.toSeq().reverse()};
	      }
	      return reversedSequence;
	    };

	    ToKeyedSequence.prototype.map = function(mapper, context) {var this$0 = this;
	      var mappedSequence = mapFactory(this, mapper, context);
	      if (!this._useKeys) {
	        mappedSequence.valueSeq = function()  {return this$0._iter.toSeq().map(mapper, context)};
	      }
	      return mappedSequence;
	    };

	    ToKeyedSequence.prototype.__iterate = function(fn, reverse) {var this$0 = this;
	      var ii;
	      return this._iter.__iterate(
	        this._useKeys ?
	          function(v, k)  {return fn(v, k, this$0)} :
	          ((ii = reverse ? resolveSize(this) : 0),
	            function(v ) {return fn(v, reverse ? --ii : ii++, this$0)}),
	        reverse
	      );
	    };

	    ToKeyedSequence.prototype.__iterator = function(type, reverse) {
	      if (this._useKeys) {
	        return this._iter.__iterator(type, reverse);
	      }
	      var iterator = this._iter.__iterator(ITERATE_VALUES, reverse);
	      var ii = reverse ? resolveSize(this) : 0;
	      return new Iterator(function()  {
	        var step = iterator.next();
	        return step.done ? step :
	          iteratorValue(type, reverse ? --ii : ii++, step.value, step);
	      });
	    };

	  ToKeyedSequence.prototype[IS_ORDERED_SENTINEL] = true;


	  createClass(ToIndexedSequence, IndexedSeq);
	    function ToIndexedSequence(iter) {
	      this._iter = iter;
	      this.size = iter.size;
	    }

	    ToIndexedSequence.prototype.includes = function(value) {
	      return this._iter.includes(value);
	    };

	    ToIndexedSequence.prototype.__iterate = function(fn, reverse) {var this$0 = this;
	      var iterations = 0;
	      return this._iter.__iterate(function(v ) {return fn(v, iterations++, this$0)}, reverse);
	    };

	    ToIndexedSequence.prototype.__iterator = function(type, reverse) {
	      var iterator = this._iter.__iterator(ITERATE_VALUES, reverse);
	      var iterations = 0;
	      return new Iterator(function()  {
	        var step = iterator.next();
	        return step.done ? step :
	          iteratorValue(type, iterations++, step.value, step)
	      });
	    };



	  createClass(ToSetSequence, SetSeq);
	    function ToSetSequence(iter) {
	      this._iter = iter;
	      this.size = iter.size;
	    }

	    ToSetSequence.prototype.has = function(key) {
	      return this._iter.includes(key);
	    };

	    ToSetSequence.prototype.__iterate = function(fn, reverse) {var this$0 = this;
	      return this._iter.__iterate(function(v ) {return fn(v, v, this$0)}, reverse);
	    };

	    ToSetSequence.prototype.__iterator = function(type, reverse) {
	      var iterator = this._iter.__iterator(ITERATE_VALUES, reverse);
	      return new Iterator(function()  {
	        var step = iterator.next();
	        return step.done ? step :
	          iteratorValue(type, step.value, step.value, step);
	      });
	    };



	  createClass(FromEntriesSequence, KeyedSeq);
	    function FromEntriesSequence(entries) {
	      this._iter = entries;
	      this.size = entries.size;
	    }

	    FromEntriesSequence.prototype.entrySeq = function() {
	      return this._iter.toSeq();
	    };

	    FromEntriesSequence.prototype.__iterate = function(fn, reverse) {var this$0 = this;
	      return this._iter.__iterate(function(entry ) {
	        // Check if entry exists first so array access doesn't throw for holes
	        // in the parent iteration.
	        if (entry) {
	          validateEntry(entry);
	          var indexedIterable = isIterable(entry);
	          return fn(
	            indexedIterable ? entry.get(1) : entry[1],
	            indexedIterable ? entry.get(0) : entry[0],
	            this$0
	          );
	        }
	      }, reverse);
	    };

	    FromEntriesSequence.prototype.__iterator = function(type, reverse) {
	      var iterator = this._iter.__iterator(ITERATE_VALUES, reverse);
	      return new Iterator(function()  {
	        while (true) {
	          var step = iterator.next();
	          if (step.done) {
	            return step;
	          }
	          var entry = step.value;
	          // Check if entry exists first so array access doesn't throw for holes
	          // in the parent iteration.
	          if (entry) {
	            validateEntry(entry);
	            var indexedIterable = isIterable(entry);
	            return iteratorValue(
	              type,
	              indexedIterable ? entry.get(0) : entry[0],
	              indexedIterable ? entry.get(1) : entry[1],
	              step
	            );
	          }
	        }
	      });
	    };


	  ToIndexedSequence.prototype.cacheResult =
	  ToKeyedSequence.prototype.cacheResult =
	  ToSetSequence.prototype.cacheResult =
	  FromEntriesSequence.prototype.cacheResult =
	    cacheResultThrough;


	  function flipFactory(iterable) {
	    var flipSequence = makeSequence(iterable);
	    flipSequence._iter = iterable;
	    flipSequence.size = iterable.size;
	    flipSequence.flip = function()  {return iterable};
	    flipSequence.reverse = function () {
	      var reversedSequence = iterable.reverse.apply(this); // super.reverse()
	      reversedSequence.flip = function()  {return iterable.reverse()};
	      return reversedSequence;
	    };
	    flipSequence.has = function(key ) {return iterable.includes(key)};
	    flipSequence.includes = function(key ) {return iterable.has(key)};
	    flipSequence.cacheResult = cacheResultThrough;
	    flipSequence.__iterateUncached = function (fn, reverse) {var this$0 = this;
	      return iterable.__iterate(function(v, k)  {return fn(k, v, this$0) !== false}, reverse);
	    }
	    flipSequence.__iteratorUncached = function(type, reverse) {
	      if (type === ITERATE_ENTRIES) {
	        var iterator = iterable.__iterator(type, reverse);
	        return new Iterator(function()  {
	          var step = iterator.next();
	          if (!step.done) {
	            var k = step.value[0];
	            step.value[0] = step.value[1];
	            step.value[1] = k;
	          }
	          return step;
	        });
	      }
	      return iterable.__iterator(
	        type === ITERATE_VALUES ? ITERATE_KEYS : ITERATE_VALUES,
	        reverse
	      );
	    }
	    return flipSequence;
	  }


	  function mapFactory(iterable, mapper, context) {
	    var mappedSequence = makeSequence(iterable);
	    mappedSequence.size = iterable.size;
	    mappedSequence.has = function(key ) {return iterable.has(key)};
	    mappedSequence.get = function(key, notSetValue)  {
	      var v = iterable.get(key, NOT_SET);
	      return v === NOT_SET ?
	        notSetValue :
	        mapper.call(context, v, key, iterable);
	    };
	    mappedSequence.__iterateUncached = function (fn, reverse) {var this$0 = this;
	      return iterable.__iterate(
	        function(v, k, c)  {return fn(mapper.call(context, v, k, c), k, this$0) !== false},
	        reverse
	      );
	    }
	    mappedSequence.__iteratorUncached = function (type, reverse) {
	      var iterator = iterable.__iterator(ITERATE_ENTRIES, reverse);
	      return new Iterator(function()  {
	        var step = iterator.next();
	        if (step.done) {
	          return step;
	        }
	        var entry = step.value;
	        var key = entry[0];
	        return iteratorValue(
	          type,
	          key,
	          mapper.call(context, entry[1], key, iterable),
	          step
	        );
	      });
	    }
	    return mappedSequence;
	  }


	  function reverseFactory(iterable, useKeys) {
	    var reversedSequence = makeSequence(iterable);
	    reversedSequence._iter = iterable;
	    reversedSequence.size = iterable.size;
	    reversedSequence.reverse = function()  {return iterable};
	    if (iterable.flip) {
	      reversedSequence.flip = function () {
	        var flipSequence = flipFactory(iterable);
	        flipSequence.reverse = function()  {return iterable.flip()};
	        return flipSequence;
	      };
	    }
	    reversedSequence.get = function(key, notSetValue) 
	      {return iterable.get(useKeys ? key : -1 - key, notSetValue)};
	    reversedSequence.has = function(key )
	      {return iterable.has(useKeys ? key : -1 - key)};
	    reversedSequence.includes = function(value ) {return iterable.includes(value)};
	    reversedSequence.cacheResult = cacheResultThrough;
	    reversedSequence.__iterate = function (fn, reverse) {var this$0 = this;
	      return iterable.__iterate(function(v, k)  {return fn(v, k, this$0)}, !reverse);
	    };
	    reversedSequence.__iterator =
	      function(type, reverse)  {return iterable.__iterator(type, !reverse)};
	    return reversedSequence;
	  }


	  function filterFactory(iterable, predicate, context, useKeys) {
	    var filterSequence = makeSequence(iterable);
	    if (useKeys) {
	      filterSequence.has = function(key ) {
	        var v = iterable.get(key, NOT_SET);
	        return v !== NOT_SET && !!predicate.call(context, v, key, iterable);
	      };
	      filterSequence.get = function(key, notSetValue)  {
	        var v = iterable.get(key, NOT_SET);
	        return v !== NOT_SET && predicate.call(context, v, key, iterable) ?
	          v : notSetValue;
	      };
	    }
	    filterSequence.__iterateUncached = function (fn, reverse) {var this$0 = this;
	      var iterations = 0;
	      iterable.__iterate(function(v, k, c)  {
	        if (predicate.call(context, v, k, c)) {
	          iterations++;
	          return fn(v, useKeys ? k : iterations - 1, this$0);
	        }
	      }, reverse);
	      return iterations;
	    };
	    filterSequence.__iteratorUncached = function (type, reverse) {
	      var iterator = iterable.__iterator(ITERATE_ENTRIES, reverse);
	      var iterations = 0;
	      return new Iterator(function()  {
	        while (true) {
	          var step = iterator.next();
	          if (step.done) {
	            return step;
	          }
	          var entry = step.value;
	          var key = entry[0];
	          var value = entry[1];
	          if (predicate.call(context, value, key, iterable)) {
	            return iteratorValue(type, useKeys ? key : iterations++, value, step);
	          }
	        }
	      });
	    }
	    return filterSequence;
	  }


	  function countByFactory(iterable, grouper, context) {
	    var groups = Map().asMutable();
	    iterable.__iterate(function(v, k)  {
	      groups.update(
	        grouper.call(context, v, k, iterable),
	        0,
	        function(a ) {return a + 1}
	      );
	    });
	    return groups.asImmutable();
	  }


	  function groupByFactory(iterable, grouper, context) {
	    var isKeyedIter = isKeyed(iterable);
	    var groups = (isOrdered(iterable) ? OrderedMap() : Map()).asMutable();
	    iterable.__iterate(function(v, k)  {
	      groups.update(
	        grouper.call(context, v, k, iterable),
	        function(a ) {return (a = a || [], a.push(isKeyedIter ? [k, v] : v), a)}
	      );
	    });
	    var coerce = iterableClass(iterable);
	    return groups.map(function(arr ) {return reify(iterable, coerce(arr))});
	  }


	  function sliceFactory(iterable, begin, end, useKeys) {
	    var originalSize = iterable.size;

	    // Sanitize begin & end using this shorthand for ToInt32(argument)
	    // http://www.ecma-international.org/ecma-262/6.0/#sec-toint32
	    if (begin !== undefined) {
	      begin = begin | 0;
	    }
	    if (end !== undefined) {
	      if (end === Infinity) {
	        end = originalSize;
	      } else {
	        end = end | 0;
	      }
	    }

	    if (wholeSlice(begin, end, originalSize)) {
	      return iterable;
	    }

	    var resolvedBegin = resolveBegin(begin, originalSize);
	    var resolvedEnd = resolveEnd(end, originalSize);

	    // begin or end will be NaN if they were provided as negative numbers and
	    // this iterable's size is unknown. In that case, cache first so there is
	    // a known size and these do not resolve to NaN.
	    if (resolvedBegin !== resolvedBegin || resolvedEnd !== resolvedEnd) {
	      return sliceFactory(iterable.toSeq().cacheResult(), begin, end, useKeys);
	    }

	    // Note: resolvedEnd is undefined when the original sequence's length is
	    // unknown and this slice did not supply an end and should contain all
	    // elements after resolvedBegin.
	    // In that case, resolvedSize will be NaN and sliceSize will remain undefined.
	    var resolvedSize = resolvedEnd - resolvedBegin;
	    var sliceSize;
	    if (resolvedSize === resolvedSize) {
	      sliceSize = resolvedSize < 0 ? 0 : resolvedSize;
	    }

	    var sliceSeq = makeSequence(iterable);

	    // If iterable.size is undefined, the size of the realized sliceSeq is
	    // unknown at this point unless the number of items to slice is 0
	    sliceSeq.size = sliceSize === 0 ? sliceSize : iterable.size && sliceSize || undefined;

	    if (!useKeys && isSeq(iterable) && sliceSize >= 0) {
	      sliceSeq.get = function (index, notSetValue) {
	        index = wrapIndex(this, index);
	        return index >= 0 && index < sliceSize ?
	          iterable.get(index + resolvedBegin, notSetValue) :
	          notSetValue;
	      }
	    }

	    sliceSeq.__iterateUncached = function(fn, reverse) {var this$0 = this;
	      if (sliceSize === 0) {
	        return 0;
	      }
	      if (reverse) {
	        return this.cacheResult().__iterate(fn, reverse);
	      }
	      var skipped = 0;
	      var isSkipping = true;
	      var iterations = 0;
	      iterable.__iterate(function(v, k)  {
	        if (!(isSkipping && (isSkipping = skipped++ < resolvedBegin))) {
	          iterations++;
	          return fn(v, useKeys ? k : iterations - 1, this$0) !== false &&
	                 iterations !== sliceSize;
	        }
	      });
	      return iterations;
	    };

	    sliceSeq.__iteratorUncached = function(type, reverse) {
	      if (sliceSize !== 0 && reverse) {
	        return this.cacheResult().__iterator(type, reverse);
	      }
	      // Don't bother instantiating parent iterator if taking 0.
	      var iterator = sliceSize !== 0 && iterable.__iterator(type, reverse);
	      var skipped = 0;
	      var iterations = 0;
	      return new Iterator(function()  {
	        while (skipped++ < resolvedBegin) {
	          iterator.next();
	        }
	        if (++iterations > sliceSize) {
	          return iteratorDone();
	        }
	        var step = iterator.next();
	        if (useKeys || type === ITERATE_VALUES) {
	          return step;
	        } else if (type === ITERATE_KEYS) {
	          return iteratorValue(type, iterations - 1, undefined, step);
	        } else {
	          return iteratorValue(type, iterations - 1, step.value[1], step);
	        }
	      });
	    }

	    return sliceSeq;
	  }


	  function takeWhileFactory(iterable, predicate, context) {
	    var takeSequence = makeSequence(iterable);
	    takeSequence.__iterateUncached = function(fn, reverse) {var this$0 = this;
	      if (reverse) {
	        return this.cacheResult().__iterate(fn, reverse);
	      }
	      var iterations = 0;
	      iterable.__iterate(function(v, k, c) 
	        {return predicate.call(context, v, k, c) && ++iterations && fn(v, k, this$0)}
	      );
	      return iterations;
	    };
	    takeSequence.__iteratorUncached = function(type, reverse) {var this$0 = this;
	      if (reverse) {
	        return this.cacheResult().__iterator(type, reverse);
	      }
	      var iterator = iterable.__iterator(ITERATE_ENTRIES, reverse);
	      var iterating = true;
	      return new Iterator(function()  {
	        if (!iterating) {
	          return iteratorDone();
	        }
	        var step = iterator.next();
	        if (step.done) {
	          return step;
	        }
	        var entry = step.value;
	        var k = entry[0];
	        var v = entry[1];
	        if (!predicate.call(context, v, k, this$0)) {
	          iterating = false;
	          return iteratorDone();
	        }
	        return type === ITERATE_ENTRIES ? step :
	          iteratorValue(type, k, v, step);
	      });
	    };
	    return takeSequence;
	  }


	  function skipWhileFactory(iterable, predicate, context, useKeys) {
	    var skipSequence = makeSequence(iterable);
	    skipSequence.__iterateUncached = function (fn, reverse) {var this$0 = this;
	      if (reverse) {
	        return this.cacheResult().__iterate(fn, reverse);
	      }
	      var isSkipping = true;
	      var iterations = 0;
	      iterable.__iterate(function(v, k, c)  {
	        if (!(isSkipping && (isSkipping = predicate.call(context, v, k, c)))) {
	          iterations++;
	          return fn(v, useKeys ? k : iterations - 1, this$0);
	        }
	      });
	      return iterations;
	    };
	    skipSequence.__iteratorUncached = function(type, reverse) {var this$0 = this;
	      if (reverse) {
	        return this.cacheResult().__iterator(type, reverse);
	      }
	      var iterator = iterable.__iterator(ITERATE_ENTRIES, reverse);
	      var skipping = true;
	      var iterations = 0;
	      return new Iterator(function()  {
	        var step, k, v;
	        do {
	          step = iterator.next();
	          if (step.done) {
	            if (useKeys || type === ITERATE_VALUES) {
	              return step;
	            } else if (type === ITERATE_KEYS) {
	              return iteratorValue(type, iterations++, undefined, step);
	            } else {
	              return iteratorValue(type, iterations++, step.value[1], step);
	            }
	          }
	          var entry = step.value;
	          k = entry[0];
	          v = entry[1];
	          skipping && (skipping = predicate.call(context, v, k, this$0));
	        } while (skipping);
	        return type === ITERATE_ENTRIES ? step :
	          iteratorValue(type, k, v, step);
	      });
	    };
	    return skipSequence;
	  }


	  function concatFactory(iterable, values) {
	    var isKeyedIterable = isKeyed(iterable);
	    var iters = [iterable].concat(values).map(function(v ) {
	      if (!isIterable(v)) {
	        v = isKeyedIterable ?
	          keyedSeqFromValue(v) :
	          indexedSeqFromValue(Array.isArray(v) ? v : [v]);
	      } else if (isKeyedIterable) {
	        v = KeyedIterable(v);
	      }
	      return v;
	    }).filter(function(v ) {return v.size !== 0});

	    if (iters.length === 0) {
	      return iterable;
	    }

	    if (iters.length === 1) {
	      var singleton = iters[0];
	      if (singleton === iterable ||
	          isKeyedIterable && isKeyed(singleton) ||
	          isIndexed(iterable) && isIndexed(singleton)) {
	        return singleton;
	      }
	    }

	    var concatSeq = new ArraySeq(iters);
	    if (isKeyedIterable) {
	      concatSeq = concatSeq.toKeyedSeq();
	    } else if (!isIndexed(iterable)) {
	      concatSeq = concatSeq.toSetSeq();
	    }
	    concatSeq = concatSeq.flatten(true);
	    concatSeq.size = iters.reduce(
	      function(sum, seq)  {
	        if (sum !== undefined) {
	          var size = seq.size;
	          if (size !== undefined) {
	            return sum + size;
	          }
	        }
	      },
	      0
	    );
	    return concatSeq;
	  }


	  function flattenFactory(iterable, depth, useKeys) {
	    var flatSequence = makeSequence(iterable);
	    flatSequence.__iterateUncached = function(fn, reverse) {
	      var iterations = 0;
	      var stopped = false;
	      function flatDeep(iter, currentDepth) {var this$0 = this;
	        iter.__iterate(function(v, k)  {
	          if ((!depth || currentDepth < depth) && isIterable(v)) {
	            flatDeep(v, currentDepth + 1);
	          } else if (fn(v, useKeys ? k : iterations++, this$0) === false) {
	            stopped = true;
	          }
	          return !stopped;
	        }, reverse);
	      }
	      flatDeep(iterable, 0);
	      return iterations;
	    }
	    flatSequence.__iteratorUncached = function(type, reverse) {
	      var iterator = iterable.__iterator(type, reverse);
	      var stack = [];
	      var iterations = 0;
	      return new Iterator(function()  {
	        while (iterator) {
	          var step = iterator.next();
	          if (step.done !== false) {
	            iterator = stack.pop();
	            continue;
	          }
	          var v = step.value;
	          if (type === ITERATE_ENTRIES) {
	            v = v[1];
	          }
	          if ((!depth || stack.length < depth) && isIterable(v)) {
	            stack.push(iterator);
	            iterator = v.__iterator(type, reverse);
	          } else {
	            return useKeys ? step : iteratorValue(type, iterations++, v, step);
	          }
	        }
	        return iteratorDone();
	      });
	    }
	    return flatSequence;
	  }


	  function flatMapFactory(iterable, mapper, context) {
	    var coerce = iterableClass(iterable);
	    return iterable.toSeq().map(
	      function(v, k)  {return coerce(mapper.call(context, v, k, iterable))}
	    ).flatten(true);
	  }


	  function interposeFactory(iterable, separator) {
	    var interposedSequence = makeSequence(iterable);
	    interposedSequence.size = iterable.size && iterable.size * 2 -1;
	    interposedSequence.__iterateUncached = function(fn, reverse) {var this$0 = this;
	      var iterations = 0;
	      iterable.__iterate(function(v, k) 
	        {return (!iterations || fn(separator, iterations++, this$0) !== false) &&
	        fn(v, iterations++, this$0) !== false},
	        reverse
	      );
	      return iterations;
	    };
	    interposedSequence.__iteratorUncached = function(type, reverse) {
	      var iterator = iterable.__iterator(ITERATE_VALUES, reverse);
	      var iterations = 0;
	      var step;
	      return new Iterator(function()  {
	        if (!step || iterations % 2) {
	          step = iterator.next();
	          if (step.done) {
	            return step;
	          }
	        }
	        return iterations % 2 ?
	          iteratorValue(type, iterations++, separator) :
	          iteratorValue(type, iterations++, step.value, step);
	      });
	    };
	    return interposedSequence;
	  }


	  function sortFactory(iterable, comparator, mapper) {
	    if (!comparator) {
	      comparator = defaultComparator;
	    }
	    var isKeyedIterable = isKeyed(iterable);
	    var index = 0;
	    var entries = iterable.toSeq().map(
	      function(v, k)  {return [k, v, index++, mapper ? mapper(v, k, iterable) : v]}
	    ).toArray();
	    entries.sort(function(a, b)  {return comparator(a[3], b[3]) || a[2] - b[2]}).forEach(
	      isKeyedIterable ?
	      function(v, i)  { entries[i].length = 2; } :
	      function(v, i)  { entries[i] = v[1]; }
	    );
	    return isKeyedIterable ? KeyedSeq(entries) :
	      isIndexed(iterable) ? IndexedSeq(entries) :
	      SetSeq(entries);
	  }


	  function maxFactory(iterable, comparator, mapper) {
	    if (!comparator) {
	      comparator = defaultComparator;
	    }
	    if (mapper) {
	      var entry = iterable.toSeq()
	        .map(function(v, k)  {return [v, mapper(v, k, iterable)]})
	        .reduce(function(a, b)  {return maxCompare(comparator, a[1], b[1]) ? b : a});
	      return entry && entry[0];
	    } else {
	      return iterable.reduce(function(a, b)  {return maxCompare(comparator, a, b) ? b : a});
	    }
	  }

	  function maxCompare(comparator, a, b) {
	    var comp = comparator(b, a);
	    // b is considered the new max if the comparator declares them equal, but
	    // they are not equal and b is in fact a nullish value.
	    return (comp === 0 && b !== a && (b === undefined || b === null || b !== b)) || comp > 0;
	  }


	  function zipWithFactory(keyIter, zipper, iters) {
	    var zipSequence = makeSequence(keyIter);
	    zipSequence.size = new ArraySeq(iters).map(function(i ) {return i.size}).min();
	    // Note: this a generic base implementation of __iterate in terms of
	    // __iterator which may be more generically useful in the future.
	    zipSequence.__iterate = function(fn, reverse) {
	      /* generic:
	      var iterator = this.__iterator(ITERATE_ENTRIES, reverse);
	      var step;
	      var iterations = 0;
	      while (!(step = iterator.next()).done) {
	        iterations++;
	        if (fn(step.value[1], step.value[0], this) === false) {
	          break;
	        }
	      }
	      return iterations;
	      */
	      // indexed:
	      var iterator = this.__iterator(ITERATE_VALUES, reverse);
	      var step;
	      var iterations = 0;
	      while (!(step = iterator.next()).done) {
	        if (fn(step.value, iterations++, this) === false) {
	          break;
	        }
	      }
	      return iterations;
	    };
	    zipSequence.__iteratorUncached = function(type, reverse) {
	      var iterators = iters.map(function(i )
	        {return (i = Iterable(i), getIterator(reverse ? i.reverse() : i))}
	      );
	      var iterations = 0;
	      var isDone = false;
	      return new Iterator(function()  {
	        var steps;
	        if (!isDone) {
	          steps = iterators.map(function(i ) {return i.next()});
	          isDone = steps.some(function(s ) {return s.done});
	        }
	        if (isDone) {
	          return iteratorDone();
	        }
	        return iteratorValue(
	          type,
	          iterations++,
	          zipper.apply(null, steps.map(function(s ) {return s.value}))
	        );
	      });
	    };
	    return zipSequence
	  }


	  // #pragma Helper Functions

	  function reify(iter, seq) {
	    return isSeq(iter) ? seq : iter.constructor(seq);
	  }

	  function validateEntry(entry) {
	    if (entry !== Object(entry)) {
	      throw new TypeError('Expected [K, V] tuple: ' + entry);
	    }
	  }

	  function resolveSize(iter) {
	    assertNotInfinite(iter.size);
	    return ensureSize(iter);
	  }

	  function iterableClass(iterable) {
	    return isKeyed(iterable) ? KeyedIterable :
	      isIndexed(iterable) ? IndexedIterable :
	      SetIterable;
	  }

	  function makeSequence(iterable) {
	    return Object.create(
	      (
	        isKeyed(iterable) ? KeyedSeq :
	        isIndexed(iterable) ? IndexedSeq :
	        SetSeq
	      ).prototype
	    );
	  }

	  function cacheResultThrough() {
	    if (this._iter.cacheResult) {
	      this._iter.cacheResult();
	      this.size = this._iter.size;
	      return this;
	    } else {
	      return Seq.prototype.cacheResult.call(this);
	    }
	  }

	  function defaultComparator(a, b) {
	    return a > b ? 1 : a < b ? -1 : 0;
	  }

	  function forceIterator(keyPath) {
	    var iter = getIterator(keyPath);
	    if (!iter) {
	      // Array might not be iterable in this environment, so we need a fallback
	      // to our wrapped type.
	      if (!isArrayLike(keyPath)) {
	        throw new TypeError('Expected iterable or array-like: ' + keyPath);
	      }
	      iter = getIterator(Iterable(keyPath));
	    }
	    return iter;
	  }

	  createClass(Record, KeyedCollection);

	    function Record(defaultValues, name) {
	      var hasInitialized;

	      var RecordType = function Record(values) {
	        if (values instanceof RecordType) {
	          return values;
	        }
	        if (!(this instanceof RecordType)) {
	          return new RecordType(values);
	        }
	        if (!hasInitialized) {
	          hasInitialized = true;
	          var keys = Object.keys(defaultValues);
	          setProps(RecordTypePrototype, keys);
	          RecordTypePrototype.size = keys.length;
	          RecordTypePrototype._name = name;
	          RecordTypePrototype._keys = keys;
	          RecordTypePrototype._defaultValues = defaultValues;
	        }
	        this._map = Map(values);
	      };

	      var RecordTypePrototype = RecordType.prototype = Object.create(RecordPrototype);
	      RecordTypePrototype.constructor = RecordType;

	      return RecordType;
	    }

	    Record.prototype.toString = function() {
	      return this.__toString(recordName(this) + ' {', '}');
	    };

	    // @pragma Access

	    Record.prototype.has = function(k) {
	      return this._defaultValues.hasOwnProperty(k);
	    };

	    Record.prototype.get = function(k, notSetValue) {
	      if (!this.has(k)) {
	        return notSetValue;
	      }
	      var defaultVal = this._defaultValues[k];
	      return this._map ? this._map.get(k, defaultVal) : defaultVal;
	    };

	    // @pragma Modification

	    Record.prototype.clear = function() {
	      if (this.__ownerID) {
	        this._map && this._map.clear();
	        return this;
	      }
	      var RecordType = this.constructor;
	      return RecordType._empty || (RecordType._empty = makeRecord(this, emptyMap()));
	    };

	    Record.prototype.set = function(k, v) {
	      if (!this.has(k)) {
	        throw new Error('Cannot set unknown key "' + k + '" on ' + recordName(this));
	      }
	      if (this._map && !this._map.has(k)) {
	        var defaultVal = this._defaultValues[k];
	        if (v === defaultVal) {
	          return this;
	        }
	      }
	      var newMap = this._map && this._map.set(k, v);
	      if (this.__ownerID || newMap === this._map) {
	        return this;
	      }
	      return makeRecord(this, newMap);
	    };

	    Record.prototype.remove = function(k) {
	      if (!this.has(k)) {
	        return this;
	      }
	      var newMap = this._map && this._map.remove(k);
	      if (this.__ownerID || newMap === this._map) {
	        return this;
	      }
	      return makeRecord(this, newMap);
	    };

	    Record.prototype.wasAltered = function() {
	      return this._map.wasAltered();
	    };

	    Record.prototype.__iterator = function(type, reverse) {var this$0 = this;
	      return KeyedIterable(this._defaultValues).map(function(_, k)  {return this$0.get(k)}).__iterator(type, reverse);
	    };

	    Record.prototype.__iterate = function(fn, reverse) {var this$0 = this;
	      return KeyedIterable(this._defaultValues).map(function(_, k)  {return this$0.get(k)}).__iterate(fn, reverse);
	    };

	    Record.prototype.__ensureOwner = function(ownerID) {
	      if (ownerID === this.__ownerID) {
	        return this;
	      }
	      var newMap = this._map && this._map.__ensureOwner(ownerID);
	      if (!ownerID) {
	        this.__ownerID = ownerID;
	        this._map = newMap;
	        return this;
	      }
	      return makeRecord(this, newMap, ownerID);
	    };


	  var RecordPrototype = Record.prototype;
	  RecordPrototype[DELETE] = RecordPrototype.remove;
	  RecordPrototype.deleteIn =
	  RecordPrototype.removeIn = MapPrototype.removeIn;
	  RecordPrototype.merge = MapPrototype.merge;
	  RecordPrototype.mergeWith = MapPrototype.mergeWith;
	  RecordPrototype.mergeIn = MapPrototype.mergeIn;
	  RecordPrototype.mergeDeep = MapPrototype.mergeDeep;
	  RecordPrototype.mergeDeepWith = MapPrototype.mergeDeepWith;
	  RecordPrototype.mergeDeepIn = MapPrototype.mergeDeepIn;
	  RecordPrototype.setIn = MapPrototype.setIn;
	  RecordPrototype.update = MapPrototype.update;
	  RecordPrototype.updateIn = MapPrototype.updateIn;
	  RecordPrototype.withMutations = MapPrototype.withMutations;
	  RecordPrototype.asMutable = MapPrototype.asMutable;
	  RecordPrototype.asImmutable = MapPrototype.asImmutable;


	  function makeRecord(likeRecord, map, ownerID) {
	    var record = Object.create(Object.getPrototypeOf(likeRecord));
	    record._map = map;
	    record.__ownerID = ownerID;
	    return record;
	  }

	  function recordName(record) {
	    return record._name || record.constructor.name || 'Record';
	  }

	  function setProps(prototype, names) {
	    try {
	      names.forEach(setProp.bind(undefined, prototype));
	    } catch (error) {
	      // Object.defineProperty failed. Probably IE8.
	    }
	  }

	  function setProp(prototype, name) {
	    Object.defineProperty(prototype, name, {
	      get: function() {
	        return this.get(name);
	      },
	      set: function(value) {
	        invariant(this.__ownerID, 'Cannot set on an immutable record.');
	        this.set(name, value);
	      }
	    });
	  }

	  createClass(Set, SetCollection);

	    // @pragma Construction

	    function Set(value) {
	      return value === null || value === undefined ? emptySet() :
	        isSet(value) && !isOrdered(value) ? value :
	        emptySet().withMutations(function(set ) {
	          var iter = SetIterable(value);
	          assertNotInfinite(iter.size);
	          iter.forEach(function(v ) {return set.add(v)});
	        });
	    }

	    Set.of = function(/*...values*/) {
	      return this(arguments);
	    };

	    Set.fromKeys = function(value) {
	      return this(KeyedIterable(value).keySeq());
	    };

	    Set.prototype.toString = function() {
	      return this.__toString('Set {', '}');
	    };

	    // @pragma Access

	    Set.prototype.has = function(value) {
	      return this._map.has(value);
	    };

	    // @pragma Modification

	    Set.prototype.add = function(value) {
	      return updateSet(this, this._map.set(value, true));
	    };

	    Set.prototype.remove = function(value) {
	      return updateSet(this, this._map.remove(value));
	    };

	    Set.prototype.clear = function() {
	      return updateSet(this, this._map.clear());
	    };

	    // @pragma Composition

	    Set.prototype.union = function() {var iters = SLICE$0.call(arguments, 0);
	      iters = iters.filter(function(x ) {return x.size !== 0});
	      if (iters.length === 0) {
	        return this;
	      }
	      if (this.size === 0 && !this.__ownerID && iters.length === 1) {
	        return this.constructor(iters[0]);
	      }
	      return this.withMutations(function(set ) {
	        for (var ii = 0; ii < iters.length; ii++) {
	          SetIterable(iters[ii]).forEach(function(value ) {return set.add(value)});
	        }
	      });
	    };

	    Set.prototype.intersect = function() {var iters = SLICE$0.call(arguments, 0);
	      if (iters.length === 0) {
	        return this;
	      }
	      iters = iters.map(function(iter ) {return SetIterable(iter)});
	      var originalSet = this;
	      return this.withMutations(function(set ) {
	        originalSet.forEach(function(value ) {
	          if (!iters.every(function(iter ) {return iter.includes(value)})) {
	            set.remove(value);
	          }
	        });
	      });
	    };

	    Set.prototype.subtract = function() {var iters = SLICE$0.call(arguments, 0);
	      if (iters.length === 0) {
	        return this;
	      }
	      iters = iters.map(function(iter ) {return SetIterable(iter)});
	      var originalSet = this;
	      return this.withMutations(function(set ) {
	        originalSet.forEach(function(value ) {
	          if (iters.some(function(iter ) {return iter.includes(value)})) {
	            set.remove(value);
	          }
	        });
	      });
	    };

	    Set.prototype.merge = function() {
	      return this.union.apply(this, arguments);
	    };

	    Set.prototype.mergeWith = function(merger) {var iters = SLICE$0.call(arguments, 1);
	      return this.union.apply(this, iters);
	    };

	    Set.prototype.sort = function(comparator) {
	      // Late binding
	      return OrderedSet(sortFactory(this, comparator));
	    };

	    Set.prototype.sortBy = function(mapper, comparator) {
	      // Late binding
	      return OrderedSet(sortFactory(this, comparator, mapper));
	    };

	    Set.prototype.wasAltered = function() {
	      return this._map.wasAltered();
	    };

	    Set.prototype.__iterate = function(fn, reverse) {var this$0 = this;
	      return this._map.__iterate(function(_, k)  {return fn(k, k, this$0)}, reverse);
	    };

	    Set.prototype.__iterator = function(type, reverse) {
	      return this._map.map(function(_, k)  {return k}).__iterator(type, reverse);
	    };

	    Set.prototype.__ensureOwner = function(ownerID) {
	      if (ownerID === this.__ownerID) {
	        return this;
	      }
	      var newMap = this._map.__ensureOwner(ownerID);
	      if (!ownerID) {
	        this.__ownerID = ownerID;
	        this._map = newMap;
	        return this;
	      }
	      return this.__make(newMap, ownerID);
	    };


	  function isSet(maybeSet) {
	    return !!(maybeSet && maybeSet[IS_SET_SENTINEL]);
	  }

	  Set.isSet = isSet;

	  var IS_SET_SENTINEL = '@@__IMMUTABLE_SET__@@';

	  var SetPrototype = Set.prototype;
	  SetPrototype[IS_SET_SENTINEL] = true;
	  SetPrototype[DELETE] = SetPrototype.remove;
	  SetPrototype.mergeDeep = SetPrototype.merge;
	  SetPrototype.mergeDeepWith = SetPrototype.mergeWith;
	  SetPrototype.withMutations = MapPrototype.withMutations;
	  SetPrototype.asMutable = MapPrototype.asMutable;
	  SetPrototype.asImmutable = MapPrototype.asImmutable;

	  SetPrototype.__empty = emptySet;
	  SetPrototype.__make = makeSet;

	  function updateSet(set, newMap) {
	    if (set.__ownerID) {
	      set.size = newMap.size;
	      set._map = newMap;
	      return set;
	    }
	    return newMap === set._map ? set :
	      newMap.size === 0 ? set.__empty() :
	      set.__make(newMap);
	  }

	  function makeSet(map, ownerID) {
	    var set = Object.create(SetPrototype);
	    set.size = map ? map.size : 0;
	    set._map = map;
	    set.__ownerID = ownerID;
	    return set;
	  }

	  var EMPTY_SET;
	  function emptySet() {
	    return EMPTY_SET || (EMPTY_SET = makeSet(emptyMap()));
	  }

	  createClass(OrderedSet, Set);

	    // @pragma Construction

	    function OrderedSet(value) {
	      return value === null || value === undefined ? emptyOrderedSet() :
	        isOrderedSet(value) ? value :
	        emptyOrderedSet().withMutations(function(set ) {
	          var iter = SetIterable(value);
	          assertNotInfinite(iter.size);
	          iter.forEach(function(v ) {return set.add(v)});
	        });
	    }

	    OrderedSet.of = function(/*...values*/) {
	      return this(arguments);
	    };

	    OrderedSet.fromKeys = function(value) {
	      return this(KeyedIterable(value).keySeq());
	    };

	    OrderedSet.prototype.toString = function() {
	      return this.__toString('OrderedSet {', '}');
	    };


	  function isOrderedSet(maybeOrderedSet) {
	    return isSet(maybeOrderedSet) && isOrdered(maybeOrderedSet);
	  }

	  OrderedSet.isOrderedSet = isOrderedSet;

	  var OrderedSetPrototype = OrderedSet.prototype;
	  OrderedSetPrototype[IS_ORDERED_SENTINEL] = true;

	  OrderedSetPrototype.__empty = emptyOrderedSet;
	  OrderedSetPrototype.__make = makeOrderedSet;

	  function makeOrderedSet(map, ownerID) {
	    var set = Object.create(OrderedSetPrototype);
	    set.size = map ? map.size : 0;
	    set._map = map;
	    set.__ownerID = ownerID;
	    return set;
	  }

	  var EMPTY_ORDERED_SET;
	  function emptyOrderedSet() {
	    return EMPTY_ORDERED_SET || (EMPTY_ORDERED_SET = makeOrderedSet(emptyOrderedMap()));
	  }

	  createClass(Stack, IndexedCollection);

	    // @pragma Construction

	    function Stack(value) {
	      return value === null || value === undefined ? emptyStack() :
	        isStack(value) ? value :
	        emptyStack().unshiftAll(value);
	    }

	    Stack.of = function(/*...values*/) {
	      return this(arguments);
	    };

	    Stack.prototype.toString = function() {
	      return this.__toString('Stack [', ']');
	    };

	    // @pragma Access

	    Stack.prototype.get = function(index, notSetValue) {
	      var head = this._head;
	      index = wrapIndex(this, index);
	      while (head && index--) {
	        head = head.next;
	      }
	      return head ? head.value : notSetValue;
	    };

	    Stack.prototype.peek = function() {
	      return this._head && this._head.value;
	    };

	    // @pragma Modification

	    Stack.prototype.push = function(/*...values*/) {
	      if (arguments.length === 0) {
	        return this;
	      }
	      var newSize = this.size + arguments.length;
	      var head = this._head;
	      for (var ii = arguments.length - 1; ii >= 0; ii--) {
	        head = {
	          value: arguments[ii],
	          next: head
	        };
	      }
	      if (this.__ownerID) {
	        this.size = newSize;
	        this._head = head;
	        this.__hash = undefined;
	        this.__altered = true;
	        return this;
	      }
	      return makeStack(newSize, head);
	    };

	    Stack.prototype.pushAll = function(iter) {
	      iter = IndexedIterable(iter);
	      if (iter.size === 0) {
	        return this;
	      }
	      assertNotInfinite(iter.size);
	      var newSize = this.size;
	      var head = this._head;
	      iter.reverse().forEach(function(value ) {
	        newSize++;
	        head = {
	          value: value,
	          next: head
	        };
	      });
	      if (this.__ownerID) {
	        this.size = newSize;
	        this._head = head;
	        this.__hash = undefined;
	        this.__altered = true;
	        return this;
	      }
	      return makeStack(newSize, head);
	    };

	    Stack.prototype.pop = function() {
	      return this.slice(1);
	    };

	    Stack.prototype.unshift = function(/*...values*/) {
	      return this.push.apply(this, arguments);
	    };

	    Stack.prototype.unshiftAll = function(iter) {
	      return this.pushAll(iter);
	    };

	    Stack.prototype.shift = function() {
	      return this.pop.apply(this, arguments);
	    };

	    Stack.prototype.clear = function() {
	      if (this.size === 0) {
	        return this;
	      }
	      if (this.__ownerID) {
	        this.size = 0;
	        this._head = undefined;
	        this.__hash = undefined;
	        this.__altered = true;
	        return this;
	      }
	      return emptyStack();
	    };

	    Stack.prototype.slice = function(begin, end) {
	      if (wholeSlice(begin, end, this.size)) {
	        return this;
	      }
	      var resolvedBegin = resolveBegin(begin, this.size);
	      var resolvedEnd = resolveEnd(end, this.size);
	      if (resolvedEnd !== this.size) {
	        // super.slice(begin, end);
	        return IndexedCollection.prototype.slice.call(this, begin, end);
	      }
	      var newSize = this.size - resolvedBegin;
	      var head = this._head;
	      while (resolvedBegin--) {
	        head = head.next;
	      }
	      if (this.__ownerID) {
	        this.size = newSize;
	        this._head = head;
	        this.__hash = undefined;
	        this.__altered = true;
	        return this;
	      }
	      return makeStack(newSize, head);
	    };

	    // @pragma Mutability

	    Stack.prototype.__ensureOwner = function(ownerID) {
	      if (ownerID === this.__ownerID) {
	        return this;
	      }
	      if (!ownerID) {
	        this.__ownerID = ownerID;
	        this.__altered = false;
	        return this;
	      }
	      return makeStack(this.size, this._head, ownerID, this.__hash);
	    };

	    // @pragma Iteration

	    Stack.prototype.__iterate = function(fn, reverse) {
	      if (reverse) {
	        return this.reverse().__iterate(fn);
	      }
	      var iterations = 0;
	      var node = this._head;
	      while (node) {
	        if (fn(node.value, iterations++, this) === false) {
	          break;
	        }
	        node = node.next;
	      }
	      return iterations;
	    };

	    Stack.prototype.__iterator = function(type, reverse) {
	      if (reverse) {
	        return this.reverse().__iterator(type);
	      }
	      var iterations = 0;
	      var node = this._head;
	      return new Iterator(function()  {
	        if (node) {
	          var value = node.value;
	          node = node.next;
	          return iteratorValue(type, iterations++, value);
	        }
	        return iteratorDone();
	      });
	    };


	  function isStack(maybeStack) {
	    return !!(maybeStack && maybeStack[IS_STACK_SENTINEL]);
	  }

	  Stack.isStack = isStack;

	  var IS_STACK_SENTINEL = '@@__IMMUTABLE_STACK__@@';

	  var StackPrototype = Stack.prototype;
	  StackPrototype[IS_STACK_SENTINEL] = true;
	  StackPrototype.withMutations = MapPrototype.withMutations;
	  StackPrototype.asMutable = MapPrototype.asMutable;
	  StackPrototype.asImmutable = MapPrototype.asImmutable;
	  StackPrototype.wasAltered = MapPrototype.wasAltered;


	  function makeStack(size, head, ownerID, hash) {
	    var map = Object.create(StackPrototype);
	    map.size = size;
	    map._head = head;
	    map.__ownerID = ownerID;
	    map.__hash = hash;
	    map.__altered = false;
	    return map;
	  }

	  var EMPTY_STACK;
	  function emptyStack() {
	    return EMPTY_STACK || (EMPTY_STACK = makeStack(0));
	  }

	  /**
	   * Contributes additional methods to a constructor
	   */
	  function mixin(ctor, methods) {
	    var keyCopier = function(key ) { ctor.prototype[key] = methods[key]; };
	    Object.keys(methods).forEach(keyCopier);
	    Object.getOwnPropertySymbols &&
	      Object.getOwnPropertySymbols(methods).forEach(keyCopier);
	    return ctor;
	  }

	  Iterable.Iterator = Iterator;

	  mixin(Iterable, {

	    // ### Conversion to other types

	    toArray: function() {
	      assertNotInfinite(this.size);
	      var array = new Array(this.size || 0);
	      this.valueSeq().__iterate(function(v, i)  { array[i] = v; });
	      return array;
	    },

	    toIndexedSeq: function() {
	      return new ToIndexedSequence(this);
	    },

	    toJS: function() {
	      return this.toSeq().map(
	        function(value ) {return value && typeof value.toJS === 'function' ? value.toJS() : value}
	      ).__toJS();
	    },

	    toJSON: function() {
	      return this.toSeq().map(
	        function(value ) {return value && typeof value.toJSON === 'function' ? value.toJSON() : value}
	      ).__toJS();
	    },

	    toKeyedSeq: function() {
	      return new ToKeyedSequence(this, true);
	    },

	    toMap: function() {
	      // Use Late Binding here to solve the circular dependency.
	      return Map(this.toKeyedSeq());
	    },

	    toObject: function() {
	      assertNotInfinite(this.size);
	      var object = {};
	      this.__iterate(function(v, k)  { object[k] = v; });
	      return object;
	    },

	    toOrderedMap: function() {
	      // Use Late Binding here to solve the circular dependency.
	      return OrderedMap(this.toKeyedSeq());
	    },

	    toOrderedSet: function() {
	      // Use Late Binding here to solve the circular dependency.
	      return OrderedSet(isKeyed(this) ? this.valueSeq() : this);
	    },

	    toSet: function() {
	      // Use Late Binding here to solve the circular dependency.
	      return Set(isKeyed(this) ? this.valueSeq() : this);
	    },

	    toSetSeq: function() {
	      return new ToSetSequence(this);
	    },

	    toSeq: function() {
	      return isIndexed(this) ? this.toIndexedSeq() :
	        isKeyed(this) ? this.toKeyedSeq() :
	        this.toSetSeq();
	    },

	    toStack: function() {
	      // Use Late Binding here to solve the circular dependency.
	      return Stack(isKeyed(this) ? this.valueSeq() : this);
	    },

	    toList: function() {
	      // Use Late Binding here to solve the circular dependency.
	      return List(isKeyed(this) ? this.valueSeq() : this);
	    },


	    // ### Common JavaScript methods and properties

	    toString: function() {
	      return '[Iterable]';
	    },

	    __toString: function(head, tail) {
	      if (this.size === 0) {
	        return head + tail;
	      }
	      return head + ' ' + this.toSeq().map(this.__toStringMapper).join(', ') + ' ' + tail;
	    },


	    // ### ES6 Collection methods (ES6 Array and Map)

	    concat: function() {var values = SLICE$0.call(arguments, 0);
	      return reify(this, concatFactory(this, values));
	    },

	    includes: function(searchValue) {
	      return this.some(function(value ) {return is(value, searchValue)});
	    },

	    entries: function() {
	      return this.__iterator(ITERATE_ENTRIES);
	    },

	    every: function(predicate, context) {
	      assertNotInfinite(this.size);
	      var returnValue = true;
	      this.__iterate(function(v, k, c)  {
	        if (!predicate.call(context, v, k, c)) {
	          returnValue = false;
	          return false;
	        }
	      });
	      return returnValue;
	    },

	    filter: function(predicate, context) {
	      return reify(this, filterFactory(this, predicate, context, true));
	    },

	    find: function(predicate, context, notSetValue) {
	      var entry = this.findEntry(predicate, context);
	      return entry ? entry[1] : notSetValue;
	    },

	    forEach: function(sideEffect, context) {
	      assertNotInfinite(this.size);
	      return this.__iterate(context ? sideEffect.bind(context) : sideEffect);
	    },

	    join: function(separator) {
	      assertNotInfinite(this.size);
	      separator = separator !== undefined ? '' + separator : ',';
	      var joined = '';
	      var isFirst = true;
	      this.__iterate(function(v ) {
	        isFirst ? (isFirst = false) : (joined += separator);
	        joined += v !== null && v !== undefined ? v.toString() : '';
	      });
	      return joined;
	    },

	    keys: function() {
	      return this.__iterator(ITERATE_KEYS);
	    },

	    map: function(mapper, context) {
	      return reify(this, mapFactory(this, mapper, context));
	    },

	    reduce: function(reducer, initialReduction, context) {
	      assertNotInfinite(this.size);
	      var reduction;
	      var useFirst;
	      if (arguments.length < 2) {
	        useFirst = true;
	      } else {
	        reduction = initialReduction;
	      }
	      this.__iterate(function(v, k, c)  {
	        if (useFirst) {
	          useFirst = false;
	          reduction = v;
	        } else {
	          reduction = reducer.call(context, reduction, v, k, c);
	        }
	      });
	      return reduction;
	    },

	    reduceRight: function(reducer, initialReduction, context) {
	      var reversed = this.toKeyedSeq().reverse();
	      return reversed.reduce.apply(reversed, arguments);
	    },

	    reverse: function() {
	      return reify(this, reverseFactory(this, true));
	    },

	    slice: function(begin, end) {
	      return reify(this, sliceFactory(this, begin, end, true));
	    },

	    some: function(predicate, context) {
	      return !this.every(not(predicate), context);
	    },

	    sort: function(comparator) {
	      return reify(this, sortFactory(this, comparator));
	    },

	    values: function() {
	      return this.__iterator(ITERATE_VALUES);
	    },


	    // ### More sequential methods

	    butLast: function() {
	      return this.slice(0, -1);
	    },

	    isEmpty: function() {
	      return this.size !== undefined ? this.size === 0 : !this.some(function()  {return true});
	    },

	    count: function(predicate, context) {
	      return ensureSize(
	        predicate ? this.toSeq().filter(predicate, context) : this
	      );
	    },

	    countBy: function(grouper, context) {
	      return countByFactory(this, grouper, context);
	    },

	    equals: function(other) {
	      return deepEqual(this, other);
	    },

	    entrySeq: function() {
	      var iterable = this;
	      if (iterable._cache) {
	        // We cache as an entries array, so we can just return the cache!
	        return new ArraySeq(iterable._cache);
	      }
	      var entriesSequence = iterable.toSeq().map(entryMapper).toIndexedSeq();
	      entriesSequence.fromEntrySeq = function()  {return iterable.toSeq()};
	      return entriesSequence;
	    },

	    filterNot: function(predicate, context) {
	      return this.filter(not(predicate), context);
	    },

	    findEntry: function(predicate, context, notSetValue) {
	      var found = notSetValue;
	      this.__iterate(function(v, k, c)  {
	        if (predicate.call(context, v, k, c)) {
	          found = [k, v];
	          return false;
	        }
	      });
	      return found;
	    },

	    findKey: function(predicate, context) {
	      var entry = this.findEntry(predicate, context);
	      return entry && entry[0];
	    },

	    findLast: function(predicate, context, notSetValue) {
	      return this.toKeyedSeq().reverse().find(predicate, context, notSetValue);
	    },

	    findLastEntry: function(predicate, context, notSetValue) {
	      return this.toKeyedSeq().reverse().findEntry(predicate, context, notSetValue);
	    },

	    findLastKey: function(predicate, context) {
	      return this.toKeyedSeq().reverse().findKey(predicate, context);
	    },

	    first: function() {
	      return this.find(returnTrue);
	    },

	    flatMap: function(mapper, context) {
	      return reify(this, flatMapFactory(this, mapper, context));
	    },

	    flatten: function(depth) {
	      return reify(this, flattenFactory(this, depth, true));
	    },

	    fromEntrySeq: function() {
	      return new FromEntriesSequence(this);
	    },

	    get: function(searchKey, notSetValue) {
	      return this.find(function(_, key)  {return is(key, searchKey)}, undefined, notSetValue);
	    },

	    getIn: function(searchKeyPath, notSetValue) {
	      var nested = this;
	      // Note: in an ES6 environment, we would prefer:
	      // for (var key of searchKeyPath) {
	      var iter = forceIterator(searchKeyPath);
	      var step;
	      while (!(step = iter.next()).done) {
	        var key = step.value;
	        nested = nested && nested.get ? nested.get(key, NOT_SET) : NOT_SET;
	        if (nested === NOT_SET) {
	          return notSetValue;
	        }
	      }
	      return nested;
	    },

	    groupBy: function(grouper, context) {
	      return groupByFactory(this, grouper, context);
	    },

	    has: function(searchKey) {
	      return this.get(searchKey, NOT_SET) !== NOT_SET;
	    },

	    hasIn: function(searchKeyPath) {
	      return this.getIn(searchKeyPath, NOT_SET) !== NOT_SET;
	    },

	    isSubset: function(iter) {
	      iter = typeof iter.includes === 'function' ? iter : Iterable(iter);
	      return this.every(function(value ) {return iter.includes(value)});
	    },

	    isSuperset: function(iter) {
	      iter = typeof iter.isSubset === 'function' ? iter : Iterable(iter);
	      return iter.isSubset(this);
	    },

	    keyOf: function(searchValue) {
	      return this.findKey(function(value ) {return is(value, searchValue)});
	    },

	    keySeq: function() {
	      return this.toSeq().map(keyMapper).toIndexedSeq();
	    },

	    last: function() {
	      return this.toSeq().reverse().first();
	    },

	    lastKeyOf: function(searchValue) {
	      return this.toKeyedSeq().reverse().keyOf(searchValue);
	    },

	    max: function(comparator) {
	      return maxFactory(this, comparator);
	    },

	    maxBy: function(mapper, comparator) {
	      return maxFactory(this, comparator, mapper);
	    },

	    min: function(comparator) {
	      return maxFactory(this, comparator ? neg(comparator) : defaultNegComparator);
	    },

	    minBy: function(mapper, comparator) {
	      return maxFactory(this, comparator ? neg(comparator) : defaultNegComparator, mapper);
	    },

	    rest: function() {
	      return this.slice(1);
	    },

	    skip: function(amount) {
	      return this.slice(Math.max(0, amount));
	    },

	    skipLast: function(amount) {
	      return reify(this, this.toSeq().reverse().skip(amount).reverse());
	    },

	    skipWhile: function(predicate, context) {
	      return reify(this, skipWhileFactory(this, predicate, context, true));
	    },

	    skipUntil: function(predicate, context) {
	      return this.skipWhile(not(predicate), context);
	    },

	    sortBy: function(mapper, comparator) {
	      return reify(this, sortFactory(this, comparator, mapper));
	    },

	    take: function(amount) {
	      return this.slice(0, Math.max(0, amount));
	    },

	    takeLast: function(amount) {
	      return reify(this, this.toSeq().reverse().take(amount).reverse());
	    },

	    takeWhile: function(predicate, context) {
	      return reify(this, takeWhileFactory(this, predicate, context));
	    },

	    takeUntil: function(predicate, context) {
	      return this.takeWhile(not(predicate), context);
	    },

	    valueSeq: function() {
	      return this.toIndexedSeq();
	    },


	    // ### Hashable Object

	    hashCode: function() {
	      return this.__hash || (this.__hash = hashIterable(this));
	    }


	    // ### Internal

	    // abstract __iterate(fn, reverse)

	    // abstract __iterator(type, reverse)
	  });

	  // var IS_ITERABLE_SENTINEL = '@@__IMMUTABLE_ITERABLE__@@';
	  // var IS_KEYED_SENTINEL = '@@__IMMUTABLE_KEYED__@@';
	  // var IS_INDEXED_SENTINEL = '@@__IMMUTABLE_INDEXED__@@';
	  // var IS_ORDERED_SENTINEL = '@@__IMMUTABLE_ORDERED__@@';

	  var IterablePrototype = Iterable.prototype;
	  IterablePrototype[IS_ITERABLE_SENTINEL] = true;
	  IterablePrototype[ITERATOR_SYMBOL] = IterablePrototype.values;
	  IterablePrototype.__toJS = IterablePrototype.toArray;
	  IterablePrototype.__toStringMapper = quoteString;
	  IterablePrototype.inspect =
	  IterablePrototype.toSource = function() { return this.toString(); };
	  IterablePrototype.chain = IterablePrototype.flatMap;
	  IterablePrototype.contains = IterablePrototype.includes;

	  mixin(KeyedIterable, {

	    // ### More sequential methods

	    flip: function() {
	      return reify(this, flipFactory(this));
	    },

	    mapEntries: function(mapper, context) {var this$0 = this;
	      var iterations = 0;
	      return reify(this,
	        this.toSeq().map(
	          function(v, k)  {return mapper.call(context, [k, v], iterations++, this$0)}
	        ).fromEntrySeq()
	      );
	    },

	    mapKeys: function(mapper, context) {var this$0 = this;
	      return reify(this,
	        this.toSeq().flip().map(
	          function(k, v)  {return mapper.call(context, k, v, this$0)}
	        ).flip()
	      );
	    }

	  });

	  var KeyedIterablePrototype = KeyedIterable.prototype;
	  KeyedIterablePrototype[IS_KEYED_SENTINEL] = true;
	  KeyedIterablePrototype[ITERATOR_SYMBOL] = IterablePrototype.entries;
	  KeyedIterablePrototype.__toJS = IterablePrototype.toObject;
	  KeyedIterablePrototype.__toStringMapper = function(v, k)  {return JSON.stringify(k) + ': ' + quoteString(v)};



	  mixin(IndexedIterable, {

	    // ### Conversion to other types

	    toKeyedSeq: function() {
	      return new ToKeyedSequence(this, false);
	    },


	    // ### ES6 Collection methods (ES6 Array and Map)

	    filter: function(predicate, context) {
	      return reify(this, filterFactory(this, predicate, context, false));
	    },

	    findIndex: function(predicate, context) {
	      var entry = this.findEntry(predicate, context);
	      return entry ? entry[0] : -1;
	    },

	    indexOf: function(searchValue) {
	      var key = this.keyOf(searchValue);
	      return key === undefined ? -1 : key;
	    },

	    lastIndexOf: function(searchValue) {
	      var key = this.lastKeyOf(searchValue);
	      return key === undefined ? -1 : key;
	    },

	    reverse: function() {
	      return reify(this, reverseFactory(this, false));
	    },

	    slice: function(begin, end) {
	      return reify(this, sliceFactory(this, begin, end, false));
	    },

	    splice: function(index, removeNum /*, ...values*/) {
	      var numArgs = arguments.length;
	      removeNum = Math.max(removeNum | 0, 0);
	      if (numArgs === 0 || (numArgs === 2 && !removeNum)) {
	        return this;
	      }
	      // If index is negative, it should resolve relative to the size of the
	      // collection. However size may be expensive to compute if not cached, so
	      // only call count() if the number is in fact negative.
	      index = resolveBegin(index, index < 0 ? this.count() : this.size);
	      var spliced = this.slice(0, index);
	      return reify(
	        this,
	        numArgs === 1 ?
	          spliced :
	          spliced.concat(arrCopy(arguments, 2), this.slice(index + removeNum))
	      );
	    },


	    // ### More collection methods

	    findLastIndex: function(predicate, context) {
	      var entry = this.findLastEntry(predicate, context);
	      return entry ? entry[0] : -1;
	    },

	    first: function() {
	      return this.get(0);
	    },

	    flatten: function(depth) {
	      return reify(this, flattenFactory(this, depth, false));
	    },

	    get: function(index, notSetValue) {
	      index = wrapIndex(this, index);
	      return (index < 0 || (this.size === Infinity ||
	          (this.size !== undefined && index > this.size))) ?
	        notSetValue :
	        this.find(function(_, key)  {return key === index}, undefined, notSetValue);
	    },

	    has: function(index) {
	      index = wrapIndex(this, index);
	      return index >= 0 && (this.size !== undefined ?
	        this.size === Infinity || index < this.size :
	        this.indexOf(index) !== -1
	      );
	    },

	    interpose: function(separator) {
	      return reify(this, interposeFactory(this, separator));
	    },

	    interleave: function(/*...iterables*/) {
	      var iterables = [this].concat(arrCopy(arguments));
	      var zipped = zipWithFactory(this.toSeq(), IndexedSeq.of, iterables);
	      var interleaved = zipped.flatten(true);
	      if (zipped.size) {
	        interleaved.size = zipped.size * iterables.length;
	      }
	      return reify(this, interleaved);
	    },

	    keySeq: function() {
	      return Range(0, this.size);
	    },

	    last: function() {
	      return this.get(-1);
	    },

	    skipWhile: function(predicate, context) {
	      return reify(this, skipWhileFactory(this, predicate, context, false));
	    },

	    zip: function(/*, ...iterables */) {
	      var iterables = [this].concat(arrCopy(arguments));
	      return reify(this, zipWithFactory(this, defaultZipper, iterables));
	    },

	    zipWith: function(zipper/*, ...iterables */) {
	      var iterables = arrCopy(arguments);
	      iterables[0] = this;
	      return reify(this, zipWithFactory(this, zipper, iterables));
	    }

	  });

	  IndexedIterable.prototype[IS_INDEXED_SENTINEL] = true;
	  IndexedIterable.prototype[IS_ORDERED_SENTINEL] = true;



	  mixin(SetIterable, {

	    // ### ES6 Collection methods (ES6 Array and Map)

	    get: function(value, notSetValue) {
	      return this.has(value) ? value : notSetValue;
	    },

	    includes: function(value) {
	      return this.has(value);
	    },


	    // ### More sequential methods

	    keySeq: function() {
	      return this.valueSeq();
	    }

	  });

	  SetIterable.prototype.has = IterablePrototype.includes;
	  SetIterable.prototype.contains = SetIterable.prototype.includes;


	  // Mixin subclasses

	  mixin(KeyedSeq, KeyedIterable.prototype);
	  mixin(IndexedSeq, IndexedIterable.prototype);
	  mixin(SetSeq, SetIterable.prototype);

	  mixin(KeyedCollection, KeyedIterable.prototype);
	  mixin(IndexedCollection, IndexedIterable.prototype);
	  mixin(SetCollection, SetIterable.prototype);


	  // #pragma Helper functions

	  function keyMapper(v, k) {
	    return k;
	  }

	  function entryMapper(v, k) {
	    return [k, v];
	  }

	  function not(predicate) {
	    return function() {
	      return !predicate.apply(this, arguments);
	    }
	  }

	  function neg(predicate) {
	    return function() {
	      return -predicate.apply(this, arguments);
	    }
	  }

	  function quoteString(value) {
	    return typeof value === 'string' ? JSON.stringify(value) : String(value);
	  }

	  function defaultZipper() {
	    return arrCopy(arguments);
	  }

	  function defaultNegComparator(a, b) {
	    return a < b ? 1 : a > b ? -1 : 0;
	  }

	  function hashIterable(iterable) {
	    if (iterable.size === Infinity) {
	      return 0;
	    }
	    var ordered = isOrdered(iterable);
	    var keyed = isKeyed(iterable);
	    var h = ordered ? 1 : 0;
	    var size = iterable.__iterate(
	      keyed ?
	        ordered ?
	          function(v, k)  { h = 31 * h + hashMerge(hash(v), hash(k)) | 0; } :
	          function(v, k)  { h = h + hashMerge(hash(v), hash(k)) | 0; } :
	        ordered ?
	          function(v ) { h = 31 * h + hash(v) | 0; } :
	          function(v ) { h = h + hash(v) | 0; }
	    );
	    return murmurHashOfSize(size, h);
	  }

	  function murmurHashOfSize(size, h) {
	    h = imul(h, 0xCC9E2D51);
	    h = imul(h << 15 | h >>> -15, 0x1B873593);
	    h = imul(h << 13 | h >>> -13, 5);
	    h = (h + 0xE6546B64 | 0) ^ size;
	    h = imul(h ^ h >>> 16, 0x85EBCA6B);
	    h = imul(h ^ h >>> 13, 0xC2B2AE35);
	    h = smi(h ^ h >>> 16);
	    return h;
	  }

	  function hashMerge(a, b) {
	    return a ^ b + 0x9E3779B9 + (a << 6) + (a >> 2) | 0; // int
	  }

	  var Immutable = {

	    Iterable: Iterable,

	    Seq: Seq,
	    Collection: Collection,
	    Map: Map,
	    OrderedMap: OrderedMap,
	    List: List,
	    Stack: Stack,
	    Set: Set,
	    OrderedSet: OrderedSet,

	    Record: Record,
	    Range: Range,
	    Repeat: Repeat,

	    is: is,
	    fromJS: fromJS

	  };

	  return Immutable;

	}));

/***/ },
/* 245 */
/***/ function(module, exports, __webpack_require__) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var _class = function (_Component) {
	  _inherits(_class, _Component);

	  function _class() {
	    _classCallCheck(this, _class);

	    return _possibleConstructorReturn(this, Object.getPrototypeOf(_class).apply(this, arguments));
	  }

	  _createClass(_class, [{
	    key: "render",
	    value: function render() {
	      var fig = this.props.fig;
	      var href = "#" + fig.id;

	      return _react2.default.createElement(
	        "a",
	        { href: href },
	        "Figure ",
	        fig.num
	      );
	    }
	  }], [{
	    key: "propTypes",
	    value: function propTypes() {
	      return {
	        fig: _react2.default.PropTypes.shape({
	          id: _react2.default.PropTypes.string.isRequired,
	          num: _react2.default.PropTypes.isRequired
	        }).isRequired
	      };
	    }
	  }]);

	  return _class;
	}(_react.Component);

	exports.default = _class;
	;

/***/ },
/* 246 */
/***/ function(module, exports) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	var introIntroSections = {
	    rechtspraakNl: {
	        id: 'rechtspraak-nl',
	        title: "Rechtspraak.nl"
	    },
	    rechtspraakNlMarkup: {
	        id: 'rechtspraak-nl-markup',
	        title: "Rechtspraak.nl markup"
	    },
	    importing: {
	        id: 'importing',
	        title: 'Importing & Tokenizing Data'
	    }
	};

	introIntroSections.inOrder = [introIntroSections.rechtspraakNl, introIntroSections.rechtspraakNlMarkup, introIntroSections.importing];

	exports.default = introIntroSections;

/***/ },
/* 247 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _chapters = __webpack_require__(217);

	var _chapters2 = _interopRequireDefault(_chapters);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var Introduction = function (_Component) {
	    _inherits(Introduction, _Component);

	    function Introduction() {
	        _classCallCheck(this, Introduction);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Introduction).apply(this, arguments));
	    }

	    _createClass(Introduction, [{
	        key: 'render',
	        value: function render() {
	            var nonValidatingXml = "https://validator.w3.org/nu/?useragent=Validator.nu%2FLV+http%3A%2F%2Fvalidator.w3.org%2Fservices&amp;doc=http%3A%2F%2Fuitspraken.rechtspraak.nl%2Finziendocument%3Fid%3DECLI%3ANL%3ACBB%3A2010%3ABN1294";

	            var htmlManifestation = "http://uitspraken.rechtspraak.nl/inziendocument?id=ECLI:NL:CBB:2010:BN1294";
	            var xmlManifestation = "http://data.rechtspraak.nl/uitspraken/content?id=ECLI:NL:CBB:2010:BN1294";

	            var urlSchemeText = "http://data.rechtspraak.nl/uitspraken/content?id={ecli}";
	            var urlSchemeUrl = "http://deeplink.rechtspraak.nl/uitspraak?id=ECLI:NL:CBB:2010:BN1294";

	            var relativeToRoot = this.props.path.match(/\//g).slice(1).map(function (_) {
	                return "../";
	            }).join("");

	            return _react2.default.createElement(
	                'div',
	                null,
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'If we are to automatically annotate documents in the corpus with some semantic mark up, it is helpful to see what is already done in this regard by Rechtspraak.nl. As we have seen, in recent years documents are more richly marked up than older documents. Indeed: most older documents consist exclusively of ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'para'
	                    ),
	                    ' and ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'paragroup'
	                    ),
	                    ' tags, denoting paragraphs and groups of paragraphs respectively. We observe that important structural tags are ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'uitspraak.info'
	                    ),
	                    ', ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'conclusie.info'
	                    ),
	                    'and ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'section'
	                    ),
	                    '. The first two denote a header section containing general metadata about a case, and we will refer to these tags as ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        '*.info'
	                    ),
	                    '. ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'section'
	                    ),
	                    ' tags obviously denote a sections. ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'section'
	                    ),
	                    ' tags optionally contain a ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'role'
	                    ),
	                    ' attribute, which represents the role of the section within the proceedings (e.g., \'considerations\', \'judgment\').'
	                ),
	                _react2.default.createElement(
	                    'section',
	                    { id: 'xml-schema' },
	                    _react2.default.createElement(
	                        'h4',
	                        null,
	                        'XML Schema'
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        'Sadly, Rechtspraak.nl does not offer an XML schema. This makes it a little more difficult to create programs that work with the XML data, such as a converter to HTML. This is because we don\'t know exactly which elements we can expect in the XML documents. In the absence of an official schema, we have created a makeshift XML schema, that was automatically generated from a random sample of 500 documents, and then manually corrected.'
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        'Using this schema, we can utilize a technology called ',
	                        _react2.default.createElement(
	                            'a',
	                            { href: 'https://en.wikipedia.org/wiki/Java_Architecture_for_XML_Binding' },
	                            'JAXB'
	                        ),
	                        ' to automatically marshall and demarshall Rechtspraak.nl XML documents to and from Java objects. (Source code and schema available ',
	                        _react2.default.createElement(
	                            'a',
	                            { href: 'https://github.com/digitalheir/java-rechtspraak-library' },
	                            'on Github.'
	                        ),
	                        ')'
	                    )
	                ),
	                _react2.default.createElement(
	                    'section',
	                    { id: 'html' },
	                    _react2.default.createElement(
	                        'h4',
	                        null,
	                        'HTML'
	                    ),
	                    _react2.default.createElement(
	                        'strong',
	                        null,
	                        'TODO: delete section?'
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        'Rechtspraak.nl offers HTML manifestations on its website through the URL scheme ',
	                        _react2.default.createElement(
	                            'a',
	                            { href: urlSchemeUrl },
	                            _react2.default.createElement(
	                                'code',
	                                null,
	                                urlSchemeText
	                            )
	                        ),
	                        '.'
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        'Although the generated HTML snippet for document content is generally valid HTML (the ',
	                        _react2.default.createElement(
	                            'a',
	                            { href: nonValidatingXml },
	                            'full page is not'
	                        ),
	                        '), we still convert XML to HTML. The reason for this is that the semantic richness of some XML is abated by the transformation process of Rechtspraak.nl.',
	                        _react2.default.createElement(
	                            'strike',
	                            null,
	                            'For example, consider the HTML manifestation for ',
	                            _react2.default.createElement(
	                                'a',
	                                {
	                                    href: htmlManifestation },
	                                'ECLI:NL:CBB:2010:BN1294'
	                            )
	                        ),
	                        '.'
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        'In the ',
	                        _react2.default.createElement(
	                            'a',
	                            { href: xmlManifestation },
	                            'XML manifestation'
	                        ),
	                        ', sections are described as such (e.g, ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            '<section role="beslissing">'
	                        ),
	                        '). In the HTML manifestation, however, these sections are homogenized with most other block elements to ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            '<div>'
	                        ),
	                        'tags (e.g.,',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            '<div class="section beslissing">'
	                        ),
	                        ').'
	                    )
	                )
	            );
	        }
	    }]);

	    return Introduction;
	}(_react.Component);

	exports.default = Introduction;

/***/ },
/* 248 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _Source = __webpack_require__(220);

	var _Source2 = _interopRequireDefault(_Source);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var Importing = function (_Component) {
	    _inherits(Importing, _Component);

	    function Importing() {
	        _classCallCheck(this, Importing);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Importing).apply(this, arguments));
	    }

	    _createClass(Importing, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                'div',
	                null,
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Importing and tokenizing documents on Rechtspraak.nl is theoretically straightforward, so it will suffice to make only a couple of remarks on the subject.'
	                ),
	                _react2.default.createElement(
	                    'strike',
	                    null,
	                    'We have developed two separate Java libraries: one for importing (and enriching) documents from Rechtspraak.nl (',
	                    _react2.default.createElement(
	                        'a',
	                        { href: 'https://github.com/digitalheir/java-rechtspraak-library' },
	                        'source code on GitHub'
	                    ),
	                    '), and one for'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'As described above, we have a separate library for importing mirroring the Rechtspraak.nl corpus as a CouchDB database. This was done in order to facilitate the collection of statistics of case law documents used in this document. The project includes a module to convert XML to JSON and a number of MapReduce functions to generate statistics. The source code for this projects is available ',
	                    _react2.default.createElement(
	                        'a',
	                        {
	                            href: 'https://github.com/digitalheir/dutch-case-law-to-couchdb' },
	                        'on GitHub'
	                    ),
	                    '.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Regardings tokenization, we need to so some forward thinking to determine how to split Rechtspraak.nl XML texts. We will use four terminal nodes in our section hierarchy:'
	                ),
	                _react2.default.createElement(
	                    'ol',
	                    null,
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'numbering'
	                        ),
	                        ', for numbering in a section heading'
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'title text'
	                        ),
	                        ', for text in a section heading'
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'text block'
	                        ),
	                        ', for running text outside of a section heading'
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'newline'
	                        ),
	                        ', for newlines'
	                    )
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'So we tokenize a document for portions of the document that are could be labeled any of the above. In this regard, newlines are trivial to detect, and we assume that Rechtspraak.nl has already done a job of splitting text blocks in ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'para'
	                    ),
	                    ' tags, but still there are a number of arbitrary decisions we need to make about how granular a token must be. These detailed can be consulted in the ',
	                    _react2.default.createElement(
	                        'a',
	                        {
	                            href: 'https://github.com/digitalheir/java-rechtspraak-library/blob/master/src/main/java/org/leibnizcenter/rechtspraak/tokens/tokentree/TokenTree.java' },
	                        'tokenization source code'
	                    ),
	                    '.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'One complication is that Rechtspraak.nl delivers an XML tree, which is more rich than a linear array of words. Indeed, it often happens that multiple ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'para'
	                    ),
	                    ' tags are wrapped in a ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'paragroup'
	                    ),
	                    ', which sometimes represents a coherent set of paragraphs. On the other hand, sometimes the paragraph grouping makes no sense. Although it is possible to efficiently tag tree structures instead of a linear list of tokens (',
	                    _references2.default.cite(_bib2.default.bradley2010learning),
	                    '), this requires more somewhat more complicated machinery. So the tagging algorithm that we use in the following supposes a linear sequence of tokens. This means that it is possible that we lose some information in the process, because we coerce a tree structure into a linear sequence, and then build a tree structure which may clash with the original tree structure. Merging the two resulting tree structures is a possibility, but we do not currently consider this.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Another noteworthy issue is that numberings are often not annotated. In our parsing algorithm, we assume that a potential numbering occurs as the beginning of a text block and represents Arabic or Roman numerals, alphabetic numbering or a list marking (i.e. some symbol such as \'-\' or \'*\').'
	                )
	            );
	        }
	    }]);

	    return Importing;
	}(_react.Component);

	exports.default = Importing;

/***/ },
/* 249 */
/***/ function(module, exports) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	var taggingSections = {
	    taggingIntroduction: {
	        id: 'tagging-introduction',
	        title: 'Introduction'
	    },
	    taggingMethods: {
	        id: 'tagging-methods',
	        title: 'Methods'
	    },
	    taggingResults: {
	        id: 'tagging-results',
	        title: 'Results'
	    },
	    taggingDiscussion: {
	        id: 'tagging-discussion',
	        title: 'Evaluation'
	    }
	};

	taggingSections.inOrder = [taggingSections.taggingIntroduction, taggingSections.taggingMethods, taggingSections.taggingResults, taggingSections.taggingDiscussion];
	exports.default = taggingSections;

/***/ },
/* 250 */
/***/ function(module, exports) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	var taggingSections = {
	    // intro: {
	    //     id: 'introduction',
	    //     title: "Introduction",
	    //     component: Introduction
	    // },
	    // methods: {
	    //     id: 'methods',
	    //     title: "Methods",
	    //     component: Methods
	    // },
	    crf: {
	        id: 'crf',
	        title: "Conditional Random Fields"
	    },
	    featureSelection: {
	        id: 'feature-selection',
	        title: 'Feature Selection'
	    },
	    deterministic: {
	        id: 'deterministic-tagger',
	        title: 'Deterministic Tagger'
	    },
	    manual: {
	        id: 'manual-tagger',
	        title: 'Manual Tagger'
	    }
	};

	taggingSections.inOrder = [taggingSections.featureSelection, taggingSections.crf, taggingSections.deterministic, taggingSections.manual];
	exports.default = taggingSections;

/***/ },
/* 251 */
/***/ function(module, exports) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	var inferringSections = {
	    intro: {
	        id: 'parse-tree-introduction',
	        title: "Introduction"
	    },
	    methods: {
	        id: 'parse-tree-methods',
	        title: "Methods"
	    },
	    evaluation: {
	        id: 'parse-tree-evaluation',
	        title: 'Evaluation'
	    }
	};

	inferringSections.inOrder = [inferringSections.intro, inferringSections.methods, inferringSections.evaluation];
	exports.default = inferringSections;

/***/ },
/* 252 */
/***/ function(module, exports) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	var methodsSections = {
	    scfg: {
	        id: 'scfg',
	        title: 'Stochastic Context Free Grammars'
	    },
	    cyk: {
	        id: 'cyk',
	        title: 'CYK Algorithm'
	    }
	};

	methodsSections.inOrder = [methodsSections.scfg, methodsSections.cyk];
	exports.default = methodsSections;

/***/ },
/* 253 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable

	// import FigRef from './../../Figures/FigRef'
	// import FigImg from './../../Figures/Image/Image'
	// import figs from './../../Figures/figs'


	// import F from  '../../Math/Math';

	var Results = function (_Component) {
	    _inherits(Results, _Component);

	    function Results() {
	        _classCallCheck(this, Results);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Results).apply(this, arguments));
	    }

	    _createClass(Results, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement('div', null);
	        }
	    }]);

	    return Results;
	}(_react.Component);

	exports.default = Results;

/***/ },
/* 254 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _chapters = __webpack_require__(217);

	var _chapters2 = _interopRequireDefault(_chapters);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var methodsSections = {};

	methodsSections.inOrder = [];

	var Tagging = function (_Component) {
	    _inherits(Tagging, _Component);

	    function Tagging() {
	        _classCallCheck(this, Tagging);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Tagging).apply(this, arguments));
	    }

	    _createClass(Tagging, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                'div',
	                null,
	                _react2.default.createElement(
	                    'h4',
	                    null,
	                    'Discussion'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'CRFs tend to have state-of-the-art performance on NLP tasks such as part-of-speech tagging, but that appears to depend on extensive feature engineering. As a result, it is likely that a given model is fitted to a particular, and suffers in portability with respect to other copora. Consider ',
	                    _references2.default.cite(_bib2.default.finkel2004exploiting),
	                    ':'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Tend to have many features, ',
	                    _references2.default.cite(_bib2.default.krishnan2006effective),
	                    ':'
	                ),
	                _react2.default.createElement(
	                    'blockquote',
	                    null,
	                    'Our baseline CRF is a sequence model in which labels for tokens directly depend only on the labels corresponding to the previous and next tokens. We use features that have been shown to be effective in NER, namely the current, previous and next words, character n-grams of the current word, Part of Speech tag of the current word and surrounding words, the shallow parse chunk of the current word, shape of the current word, the surrounding word shape sequence, the presence of a word in a left window of size 5 around the current word and the presence of a word in a left window of size 5 around the current word. This gives us a competitive baseline CRF using local information alone, whose performance is close to the best published local CRF models, for Named Entity Recognition'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    '[[',
	                    _references2.default.cite(_bib2.default.klinger2009feature),
	                    ' even reports millions of features.'
	                ),
	                _react2.default.createElement(
	                    'blockquote',
	                    null,
	                    'Using the set of features designed for that task in CoNLL 2003 [24], our system achieves an f-score of 0.76 on the BioCreative development data, a dramatic ten points lower than its f-score of 0.86 on the CoNLL newswire data. Despite the massive size of the final feature set (almost twice as many features as used for CoNLL), its final performance of 0.83 is still below its performance on the CoNLL data'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'In our case, this is ideal, because we aspire only to deal with the corpus of Dutch case law.'
	                )
	            );
	        }
	    }], [{
	        key: 'title',
	        value: function title() {
	            return _chapters2.default.tagging.title;
	        }
	    }]);

	    return Tagging;
	}(_react.Component);

	exports.default = Tagging;

/***/ },
/* 255 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _chapters = __webpack_require__(217);

	var _chapters2 = _interopRequireDefault(_chapters);

	var _ChapterSectionContent = __webpack_require__(256);

	var _ChapterSectionContent2 = _interopRequireDefault(_ChapterSectionContent);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var methodsSections = {
	    featureSelection: {
	        id: 'feature-selection',
	        title: "Feature Selection"
	    },
	    crf: {
	        id: 'crf',
	        title: "Conditional Random Fields"
	    },
	    deterministic: {
	        id: 'deterministic-tagger',
	        title: "Deterministic tagger"
	    }
	};

	methodsSections.inOrder = [methodsSections.featureSelection, methodsSections.crf, methodsSections.deterministic];

	var Tagging = function (_Component) {
	    _inherits(Tagging, _Component);

	    function Tagging() {
	        _classCallCheck(this, Tagging);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Tagging).apply(this, arguments));
	    }

	    _createClass(Tagging, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(_ChapterSectionContent2.default, _extends({}, this.props, { sections: methodsSections.inOrder }));
	        }
	    }], [{
	        key: 'title',
	        value: function title() {
	            return _chapters2.default.tagging.title;
	        }

	        //noinspection JSUnusedGlobalSymbols

	    }, {
	        key: 'getSections',
	        value: function getSections() {
	            return methodsSections;
	        }
	    }]);

	    return Tagging;
	}(_react.Component);

	exports.default = Tagging;

/***/ },
/* 256 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _getSectionComponent = __webpack_require__(218);

	var _getSectionComponent2 = _interopRequireDefault(_getSectionComponent);

	var _chapters = __webpack_require__(217);

	var _chapters2 = _interopRequireDefault(_chapters);

	var _ToC = __webpack_require__(216);

	var _ToC2 = _interopRequireDefault(_ToC);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	/**
	 * A component for a chapter page on our website.
	 */

	var Chapter = function (_React$Component) {
	    _inherits(Chapter, _React$Component);

	    function Chapter() {
	        _classCallCheck(this, Chapter);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Chapter).apply(this, arguments));
	    }

	    _createClass(Chapter, [{
	        key: 'render',
	        value: function render() {
	            var _this2 = this;

	            var subsections = this.props.sections.map(function (section) {
	                var SectionContent = (0, _getSectionComponent2.default)(section.id);
	                return _react2.default.createElement(
	                    'section',
	                    { key: section.id,
	                        id: section.id },
	                    _react2.default.createElement(
	                        'h4',
	                        { className: 'title' },
	                        _react2.default.createElement('a', { className: 'link-up', href: '#' }),
	                        section.title
	                    ),
	                    _react2.default.createElement(SectionContent, _this2.props)
	                );
	            });

	            return _react2.default.createElement(
	                'div',
	                null,
	                this.props.children,
	                subsections
	            );
	        }
	    }]);

	    return Chapter;
	}(_react2.default.Component);

	exports.default = Chapter;

	Chapter.propTypes = {
	    title: _react2.default.PropTypes.string.isRequired,
	    sections: _react2.default.PropTypes.arrayOf(_react2.default.PropTypes.shape({
	        component: _react2.default.PropTypes.func.isRequired
	    })).isRequired
	};

	module.exports = Chapter;

/***/ },
/* 257 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _chapters = __webpack_require__(217);

	var _chapters2 = _interopRequireDefault(_chapters);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var TaggingIntroduction = function (_Component) {
	    _inherits(TaggingIntroduction, _Component);

	    function TaggingIntroduction() {
	        _classCallCheck(this, TaggingIntroduction);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(TaggingIntroduction).apply(this, arguments));
	    }

	    _createClass(TaggingIntroduction, [{
	        key: 'render',
	        value: function render() {
	            var relativeToRoot = this.props.path.match(/\//g).slice(1).map(function (_) {
	                return "../";
	            }).join("");
	            var urlToIntrochapter = relativeToRoot + _chapters2.default.introduction.route.replace('/', '');
	            return _react2.default.createElement(
	                'div',
	                null,
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'In the ',
	                    _react2.default.createElement(
	                        'a',
	                        { href: urlToIntrochapter },
	                        'previous chapter'
	                    ),
	                    ' we developed a way to import Rechtspraak.nl XML documents and distill them into a list of text elements, or tokens. In this chapter, we consider how to label these tokens with any of four labels:'
	                ),
	                _react2.default.createElement(
	                    'ol',
	                    null,
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'numbering'
	                        ),
	                        ', for numbering in a section heading'
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'title text'
	                        ),
	                        ', for text in a section heading'
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'text block'
	                        ),
	                        ', for running text outside of a section heading'
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'newline'
	                        ),
	                        ', for newlines'
	                    )
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Even as a human reader, it can be hard to distinguish what should properly be called a section, and so what is a section heading. So there is some subjectivity involved in tagging. Consider, for example, a numbered enumeration of facts which might be considered a list or a section sequence. For our purposes, we take the radical stance to call a \'section\' any semantic grouping of text that is headed by a title or a number, inspired by the HTML5 definition of ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'section'
	                    ),
	                    ':'
	                ),
	                _react2.default.createElement(
	                    'blockquote',
	                    { cite: 'https://www.w3.org/TR/html5/sections.html#the-section-element' },
	                    'A section is a thematic grouping of content. The theme of each section should be identified, typically by including a heading (h1-h6 element) as a child of the section element.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Labeling a string of tokens is a task that has been widely covered in literature, mostly in the application of part-of-speech tagging in natural language. Popular methods include graphical models, which model the probability distributions of labels and observations occurring together. These include Hidden Markov Models (HMMs) and the closely related Conditional Random Fields (CRFs). In this chapter, we experiment with CRFs for labeling our tokens, and we compare the results to a hand-written deterministic tagger that utilizes similar features.'
	                ),
	                'TODO a quick summary of results?'
	            );
	        }
	    }]);

	    return TaggingIntroduction;
	}(_react.Component);

	exports.default = TaggingIntroduction;

/***/ },
/* 258 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _FigRef = __webpack_require__(245);

	var _FigRef2 = _interopRequireDefault(_FigRef);

	var _Image = __webpack_require__(259);

	var _Image2 = _interopRequireDefault(_Image);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	var _Math = __webpack_require__(260);

	var _Math2 = _interopRequireDefault(_Math);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var DeterministicTagger = function (_Component) {
	    _inherits(DeterministicTagger, _Component);

	    function DeterministicTagger() {
	        _classCallCheck(this, DeterministicTagger);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(DeterministicTagger).apply(this, arguments));
	    }

	    _createClass(DeterministicTagger, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                'div',
	                null,
	                'Along with automatic taggers, we have created GUI which makes it possible to tag tokens by hand. This is useful for creating a gold standard data set or manually validating an algorithmic tagging algorithm.'
	            );
	        }
	    }]);

	    return DeterministicTagger;
	}(_react.Component);

	exports.default = DeterministicTagger;

/***/ },
/* 259 */
/***/ function(module, exports, __webpack_require__) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var _class = function (_Component) {
	    _inherits(_class, _Component);

	    function _class() {
	        _classCallCheck(this, _class);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(_class).apply(this, arguments));
	    }

	    _createClass(_class, [{
	        key: "render",
	        value: function render() {
	            var data = this.props.fig ? this.props.fig : this.props;

	            var caption = this.props.children && _react2.default.Children.count(this.props.children) > 0 ? this.props.children : data.alt;

	            return _react2.default.createElement(
	                "figure",
	                { id: data.id },
	                _react2.default.createElement(
	                    "div",
	                    { className: "figure-img-container" },
	                    _react2.default.createElement("img", {
	                        style: {
	                            width: this.props.width,
	                            display: "inline-block"
	                        },
	                        src: this.props.relativeToRoot + data.src,
	                        alt: data.alt })
	                ),
	                _react2.default.createElement(
	                    "figcaption",
	                    null,
	                    _react2.default.createElement(
	                        "span",
	                        { className: "figure-number" },
	                        "Fig ",
	                        data.num,
	                        "."
	                    ),
	                    " ",
	                    caption
	                )
	            );
	        }
	    }], [{
	        key: "defaultProps",
	        value: function defaultProps() {
	            return {
	                fig: undefined,
	                width: "100%"
	            };
	        }
	    }]);

	    return _class;
	}(_react.Component);

	exports.default = _class;

/***/ },
/* 260 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _katex = __webpack_require__(261);

	var _katex2 = _interopRequireDefault(_katex);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var Math = function (_Component) {
	    _inherits(Math, _Component);

	    function Math() {
	        _classCallCheck(this, Math);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Math).apply(this, arguments));
	    }

	    _createClass(Math, [{
	        key: 'render',
	        value: function render() {
	            //console.log(katex.renderToString(this.props.latex,
	            //    {displayMode: this.props.displayMode}));
	            var latex = this.props.latex || this.props.l;
	            var displayMode = this.props.displayMode || this.props.display;

	            return _react2.default.createElement('span', { dangerouslySetInnerHTML: {
	                    __html: _katex2.default.renderToString(latex, { displayMode: displayMode })
	                } });
	        }
	    }]);

	    return Math;
	}(_react.Component);

	Math.defaultProps = {
	    latex: undefined,
	    l: undefined,
	    displayMode: false
	};

	exports.default = Math;

/***/ },
/* 261 */
/***/ function(module, exports, __webpack_require__) {

	/* eslint no-console:0 */
	/**
	 * This is the main entry point for KaTeX. Here, we expose functions for
	 * rendering expressions either to DOM nodes or to markup strings.
	 *
	 * We also expose the ParseError class to check if errors thrown from KaTeX are
	 * errors in the expression, or errors in javascript handling.
	 */

	var ParseError = __webpack_require__(262);
	var Settings = __webpack_require__(263);

	var buildTree = __webpack_require__(264);
	var parseTree = __webpack_require__(277);
	var utils = __webpack_require__(269);

	/**
	 * Parse and build an expression, and place that expression in the DOM node
	 * given.
	 */
	var render = function(expression, baseNode, options) {
	    utils.clearNode(baseNode);

	    var settings = new Settings(options);

	    var tree = parseTree(expression, settings);
	    var node = buildTree(tree, expression, settings).toNode();

	    baseNode.appendChild(node);
	};

	// KaTeX's styles don't work properly in quirks mode. Print out an error, and
	// disable rendering.
	if (typeof document !== "undefined") {
	    if (document.compatMode !== "CSS1Compat") {
	        typeof console !== "undefined" && console.warn(
	            "Warning: KaTeX doesn't work in quirks mode. Make sure your " +
	                "website has a suitable doctype.");

	        render = function() {
	            throw new ParseError("KaTeX doesn't work in quirks mode.");
	        };
	    }
	}

	/**
	 * Parse and build an expression, and return the markup for that.
	 */
	var renderToString = function(expression, options) {
	    var settings = new Settings(options);

	    var tree = parseTree(expression, settings);
	    return buildTree(tree, expression, settings).toMarkup();
	};

	/**
	 * Parse an expression and return the parse tree.
	 */
	var generateParseTree = function(expression, options) {
	    var settings = new Settings(options);
	    return parseTree(expression, settings);
	};

	module.exports = {
	    render: render,
	    renderToString: renderToString,
	    /**
	     * NOTE: This method is not currently recommended for public use.
	     * The internal tree representation is unstable and is very likely
	     * to change. Use at your own risk.
	     */
	    __parse: generateParseTree,
	    ParseError: ParseError,
	};


/***/ },
/* 262 */
/***/ function(module, exports) {

	/**
	 * This is the ParseError class, which is the main error thrown by KaTeX
	 * functions when something has gone wrong. This is used to distinguish internal
	 * errors from errors in the expression that the user provided.
	 */
	function ParseError(message, lexer, position) {
	    var error = "KaTeX parse error: " + message;

	    if (lexer !== undefined && position !== undefined) {
	        // If we have the input and a position, make the error a bit fancier

	        // Prepend some information
	        error += " at position " + position + ": ";

	        // Get the input
	        var input = lexer._input;
	        // Insert a combining underscore at the correct position
	        input = input.slice(0, position) + "\u0332" +
	            input.slice(position);

	        // Extract some context from the input and add it to the error
	        var begin = Math.max(0, position - 15);
	        var end = position + 15;
	        error += input.slice(begin, end);
	    }

	    // Some hackery to make ParseError a prototype of Error
	    // See http://stackoverflow.com/a/8460753
	    var self = new Error(error);
	    self.name = "ParseError";
	    self.__proto__ = ParseError.prototype;

	    self.position = position;
	    return self;
	}

	// More hackery
	ParseError.prototype.__proto__ = Error.prototype;

	module.exports = ParseError;


/***/ },
/* 263 */
/***/ function(module, exports) {

	/**
	 * This is a module for storing settings passed into KaTeX. It correctly handles
	 * default settings.
	 */

	/**
	 * Helper function for getting a default value if the value is undefined
	 */
	function get(option, defaultValue) {
	    return option === undefined ? defaultValue : option;
	}

	/**
	 * The main Settings object
	 *
	 * The current options stored are:
	 *  - displayMode: Whether the expression should be typeset by default in
	 *                 textstyle or displaystyle (default false)
	 */
	function Settings(options) {
	    // allow null options
	    options = options || {};
	    this.displayMode = get(options.displayMode, false);
	    this.throwOnError = get(options.throwOnError, true);
	    this.errorColor = get(options.errorColor, "#cc0000");
	}

	module.exports = Settings;


/***/ },
/* 264 */
/***/ function(module, exports, __webpack_require__) {

	var buildHTML = __webpack_require__(265);
	var buildMathML = __webpack_require__(274);
	var buildCommon = __webpack_require__(267);
	var Options = __webpack_require__(276);
	var Settings = __webpack_require__(263);
	var Style = __webpack_require__(266);

	var makeSpan = buildCommon.makeSpan;

	var buildTree = function(tree, expression, settings) {
	    settings = settings || new Settings({});

	    var startStyle = Style.TEXT;
	    if (settings.displayMode) {
	        startStyle = Style.DISPLAY;
	    }

	    // Setup the default options
	    var options = new Options({
	        style: startStyle,
	        size: "size5",
	    });

	    // `buildHTML` sometimes messes with the parse tree (like turning bins ->
	    // ords), so we build the MathML version first.
	    var mathMLNode = buildMathML(tree, expression, options);
	    var htmlNode = buildHTML(tree, options);

	    var katexNode = makeSpan(["katex"], [
	        mathMLNode, htmlNode,
	    ]);

	    if (settings.displayMode) {
	        return makeSpan(["katex-display"], [katexNode]);
	    } else {
	        return katexNode;
	    }
	};

	module.exports = buildTree;


/***/ },
/* 265 */
/***/ function(module, exports, __webpack_require__) {

	/* eslint no-console:0 */
	/**
	 * This file does the main work of building a domTree structure from a parse
	 * tree. The entry point is the `buildHTML` function, which takes a parse tree.
	 * Then, the buildExpression, buildGroup, and various groupTypes functions are
	 * called, to produce a final HTML tree.
	 */

	var ParseError = __webpack_require__(262);
	var Style = __webpack_require__(266);

	var buildCommon = __webpack_require__(267);
	var delimiter = __webpack_require__(273);
	var domTree = __webpack_require__(268);
	var fontMetrics = __webpack_require__(270);
	var utils = __webpack_require__(269);

	var makeSpan = buildCommon.makeSpan;

	/**
	 * Take a list of nodes, build them in order, and return a list of the built
	 * nodes. This function handles the `prev` node correctly, and passes the
	 * previous element from the list as the prev of the next element.
	 */
	var buildExpression = function(expression, options, prev) {
	    var groups = [];
	    for (var i = 0; i < expression.length; i++) {
	        var group = expression[i];
	        groups.push(buildGroup(group, options, prev));
	        prev = group;
	    }
	    return groups;
	};

	// List of types used by getTypeOfGroup,
	// see https://github.com/Khan/KaTeX/wiki/Examining-TeX#group-types
	var groupToType = {
	    mathord: "mord",
	    textord: "mord",
	    bin: "mbin",
	    rel: "mrel",
	    text: "mord",
	    open: "mopen",
	    close: "mclose",
	    inner: "minner",
	    genfrac: "mord",
	    array: "mord",
	    spacing: "mord",
	    punct: "mpunct",
	    ordgroup: "mord",
	    op: "mop",
	    katex: "mord",
	    overline: "mord",
	    underline: "mord",
	    rule: "mord",
	    leftright: "minner",
	    sqrt: "mord",
	    accent: "mord",
	};

	/**
	 * Gets the final math type of an expression, given its group type. This type is
	 * used to determine spacing between elements, and affects bin elements by
	 * causing them to change depending on what types are around them. This type
	 * must be attached to the outermost node of an element as a CSS class so that
	 * spacing with its surrounding elements works correctly.
	 *
	 * Some elements can be mapped one-to-one from group type to math type, and
	 * those are listed in the `groupToType` table.
	 *
	 * Others (usually elements that wrap around other elements) often have
	 * recursive definitions, and thus call `getTypeOfGroup` on their inner
	 * elements.
	 */
	var getTypeOfGroup = function(group) {
	    if (group == null) {
	        // Like when typesetting $^3$
	        return groupToType.mathord;
	    } else if (group.type === "supsub") {
	        return getTypeOfGroup(group.value.base);
	    } else if (group.type === "llap" || group.type === "rlap") {
	        return getTypeOfGroup(group.value);
	    } else if (group.type === "color") {
	        return getTypeOfGroup(group.value.value);
	    } else if (group.type === "sizing") {
	        return getTypeOfGroup(group.value.value);
	    } else if (group.type === "styling") {
	        return getTypeOfGroup(group.value.value);
	    } else if (group.type === "delimsizing") {
	        return groupToType[group.value.delimType];
	    } else {
	        return groupToType[group.type];
	    }
	};

	/**
	 * Sometimes, groups perform special rules when they have superscripts or
	 * subscripts attached to them. This function lets the `supsub` group know that
	 * its inner element should handle the superscripts and subscripts instead of
	 * handling them itself.
	 */
	var shouldHandleSupSub = function(group, options) {
	    if (!group) {
	        return false;
	    } else if (group.type === "op") {
	        // Operators handle supsubs differently when they have limits
	        // (e.g. `\displaystyle\sum_2^3`)
	        return group.value.limits &&
	            (options.style.size === Style.DISPLAY.size ||
	            group.value.alwaysHandleSupSub);
	    } else if (group.type === "accent") {
	        return isCharacterBox(group.value.base);
	    } else {
	        return null;
	    }
	};

	/**
	 * Sometimes we want to pull out the innermost element of a group. In most
	 * cases, this will just be the group itself, but when ordgroups and colors have
	 * a single element, we want to pull that out.
	 */
	var getBaseElem = function(group) {
	    if (!group) {
	        return false;
	    } else if (group.type === "ordgroup") {
	        if (group.value.length === 1) {
	            return getBaseElem(group.value[0]);
	        } else {
	            return group;
	        }
	    } else if (group.type === "color") {
	        if (group.value.value.length === 1) {
	            return getBaseElem(group.value.value[0]);
	        } else {
	            return group;
	        }
	    } else {
	        return group;
	    }
	};

	/**
	 * TeXbook algorithms often reference "character boxes", which are simply groups
	 * with a single character in them. To decide if something is a character box,
	 * we find its innermost group, and see if it is a single character.
	 */
	var isCharacterBox = function(group) {
	    var baseElem = getBaseElem(group);

	    // These are all they types of groups which hold single characters
	    return baseElem.type === "mathord" ||
	        baseElem.type === "textord" ||
	        baseElem.type === "bin" ||
	        baseElem.type === "rel" ||
	        baseElem.type === "inner" ||
	        baseElem.type === "open" ||
	        baseElem.type === "close" ||
	        baseElem.type === "punct";
	};

	var makeNullDelimiter = function(options) {
	    return makeSpan([
	        "sizing", "reset-" + options.size, "size5",
	        options.style.reset(), Style.TEXT.cls(),
	        "nulldelimiter",
	    ]);
	};

	/**
	 * This is a map of group types to the function used to handle that type.
	 * Simpler types come at the beginning, while complicated types come afterwards.
	 */
	var groupTypes = {};

	groupTypes.mathord = function(group, options, prev) {
	    return buildCommon.makeOrd(group, options, "mathord");
	};

	groupTypes.textord = function(group, options, prev) {
	    return buildCommon.makeOrd(group, options, "textord");
	};

	groupTypes.bin = function(group, options, prev) {
	    var className = "mbin";
	    // Pull out the most recent element. Do some special handling to find
	    // things at the end of a \color group. Note that we don't use the same
	    // logic for ordgroups (which count as ords).
	    var prevAtom = prev;
	    while (prevAtom && prevAtom.type === "color") {
	        var atoms = prevAtom.value.value;
	        prevAtom = atoms[atoms.length - 1];
	    }
	    // See TeXbook pg. 442-446, Rules 5 and 6, and the text before Rule 19.
	    // Here, we determine whether the bin should turn into an ord. We
	    // currently only apply Rule 5.
	    if (!prev || utils.contains(["mbin", "mopen", "mrel", "mop", "mpunct"],
	            getTypeOfGroup(prevAtom))) {
	        group.type = "textord";
	        className = "mord";
	    }

	    return buildCommon.mathsym(
	        group.value, group.mode, options.getColor(), [className]);
	};

	groupTypes.rel = function(group, options, prev) {
	    return buildCommon.mathsym(
	        group.value, group.mode, options.getColor(), ["mrel"]);
	};

	groupTypes.open = function(group, options, prev) {
	    return buildCommon.mathsym(
	        group.value, group.mode, options.getColor(), ["mopen"]);
	};

	groupTypes.close = function(group, options, prev) {
	    return buildCommon.mathsym(
	        group.value, group.mode, options.getColor(), ["mclose"]);
	};

	groupTypes.inner = function(group, options, prev) {
	    return buildCommon.mathsym(
	        group.value, group.mode, options.getColor(), ["minner"]);
	};

	groupTypes.punct = function(group, options, prev) {
	    return buildCommon.mathsym(
	        group.value, group.mode, options.getColor(), ["mpunct"]);
	};

	groupTypes.ordgroup = function(group, options, prev) {
	    return makeSpan(
	        ["mord", options.style.cls()],
	        buildExpression(group.value, options.reset())
	    );
	};

	groupTypes.text = function(group, options, prev) {
	    return makeSpan(["text", "mord", options.style.cls()],
	        buildExpression(group.value.body, options.reset()));
	};

	groupTypes.color = function(group, options, prev) {
	    var elements = buildExpression(
	        group.value.value,
	        options.withColor(group.value.color),
	        prev
	    );

	    // \color isn't supposed to affect the type of the elements it contains.
	    // To accomplish this, we wrap the results in a fragment, so the inner
	    // elements will be able to directly interact with their neighbors. For
	    // example, `\color{red}{2 +} 3` has the same spacing as `2 + 3`
	    return new buildCommon.makeFragment(elements);
	};

	groupTypes.supsub = function(group, options, prev) {
	    // Superscript and subscripts are handled in the TeXbook on page
	    // 445-446, rules 18(a-f).

	    // Here is where we defer to the inner group if it should handle
	    // superscripts and subscripts itself.
	    if (shouldHandleSupSub(group.value.base, options)) {
	        return groupTypes[group.value.base.type](group, options, prev);
	    }

	    var base = buildGroup(group.value.base, options.reset());
	    var supmid;
	    var submid;
	    var sup;
	    var sub;

	    if (group.value.sup) {
	        sup = buildGroup(group.value.sup,
	                options.withStyle(options.style.sup()));
	        supmid = makeSpan(
	                [options.style.reset(), options.style.sup().cls()], [sup]);
	    }

	    if (group.value.sub) {
	        sub = buildGroup(group.value.sub,
	                options.withStyle(options.style.sub()));
	        submid = makeSpan(
	                [options.style.reset(), options.style.sub().cls()], [sub]);
	    }

	    // Rule 18a
	    var supShift;
	    var subShift;
	    if (isCharacterBox(group.value.base)) {
	        supShift = 0;
	        subShift = 0;
	    } else {
	        supShift = base.height - fontMetrics.metrics.supDrop;
	        subShift = base.depth + fontMetrics.metrics.subDrop;
	    }

	    // Rule 18c
	    var minSupShift;
	    if (options.style === Style.DISPLAY) {
	        minSupShift = fontMetrics.metrics.sup1;
	    } else if (options.style.cramped) {
	        minSupShift = fontMetrics.metrics.sup3;
	    } else {
	        minSupShift = fontMetrics.metrics.sup2;
	    }

	    // scriptspace is a font-size-independent size, so scale it
	    // appropriately
	    var multiplier = Style.TEXT.sizeMultiplier *
	            options.style.sizeMultiplier;
	    var scriptspace =
	        (0.5 / fontMetrics.metrics.ptPerEm) / multiplier + "em";

	    var supsub;
	    if (!group.value.sup) {
	        // Rule 18b
	        subShift = Math.max(
	            subShift, fontMetrics.metrics.sub1,
	            sub.height - 0.8 * fontMetrics.metrics.xHeight);

	        supsub = buildCommon.makeVList([
	            {type: "elem", elem: submid},
	        ], "shift", subShift, options);

	        supsub.children[0].style.marginRight = scriptspace;

	        // Subscripts shouldn't be shifted by the base's italic correction.
	        // Account for that by shifting the subscript back the appropriate
	        // amount. Note we only do this when the base is a single symbol.
	        if (base instanceof domTree.symbolNode) {
	            supsub.children[0].style.marginLeft = -base.italic + "em";
	        }
	    } else if (!group.value.sub) {
	        // Rule 18c, d
	        supShift = Math.max(supShift, minSupShift,
	            sup.depth + 0.25 * fontMetrics.metrics.xHeight);

	        supsub = buildCommon.makeVList([
	            {type: "elem", elem: supmid},
	        ], "shift", -supShift, options);

	        supsub.children[0].style.marginRight = scriptspace;
	    } else {
	        supShift = Math.max(
	            supShift, minSupShift,
	            sup.depth + 0.25 * fontMetrics.metrics.xHeight);
	        subShift = Math.max(subShift, fontMetrics.metrics.sub2);

	        var ruleWidth = fontMetrics.metrics.defaultRuleThickness;

	        // Rule 18e
	        if ((supShift - sup.depth) - (sub.height - subShift) <
	                4 * ruleWidth) {
	            subShift = 4 * ruleWidth - (supShift - sup.depth) + sub.height;
	            var psi = 0.8 * fontMetrics.metrics.xHeight -
	                (supShift - sup.depth);
	            if (psi > 0) {
	                supShift += psi;
	                subShift -= psi;
	            }
	        }

	        supsub = buildCommon.makeVList([
	            {type: "elem", elem: submid, shift: subShift},
	            {type: "elem", elem: supmid, shift: -supShift},
	        ], "individualShift", null, options);

	        // See comment above about subscripts not being shifted
	        if (base instanceof domTree.symbolNode) {
	            supsub.children[0].style.marginLeft = -base.italic + "em";
	        }

	        supsub.children[0].style.marginRight = scriptspace;
	        supsub.children[1].style.marginRight = scriptspace;
	    }

	    return makeSpan([getTypeOfGroup(group.value.base)],
	        [base, supsub]);
	};

	groupTypes.genfrac = function(group, options, prev) {
	    // Fractions are handled in the TeXbook on pages 444-445, rules 15(a-e).
	    // Figure out what style this fraction should be in based on the
	    // function used
	    var fstyle = options.style;
	    if (group.value.size === "display") {
	        fstyle = Style.DISPLAY;
	    } else if (group.value.size === "text") {
	        fstyle = Style.TEXT;
	    }

	    var nstyle = fstyle.fracNum();
	    var dstyle = fstyle.fracDen();

	    var numer = buildGroup(group.value.numer, options.withStyle(nstyle));
	    var numerreset = makeSpan([fstyle.reset(), nstyle.cls()], [numer]);

	    var denom = buildGroup(group.value.denom, options.withStyle(dstyle));
	    var denomreset = makeSpan([fstyle.reset(), dstyle.cls()], [denom]);

	    var ruleWidth;
	    if (group.value.hasBarLine) {
	        ruleWidth = fontMetrics.metrics.defaultRuleThickness /
	            options.style.sizeMultiplier;
	    } else {
	        ruleWidth = 0;
	    }

	    // Rule 15b
	    var numShift;
	    var clearance;
	    var denomShift;
	    if (fstyle.size === Style.DISPLAY.size) {
	        numShift = fontMetrics.metrics.num1;
	        if (ruleWidth > 0) {
	            clearance = 3 * ruleWidth;
	        } else {
	            clearance = 7 * fontMetrics.metrics.defaultRuleThickness;
	        }
	        denomShift = fontMetrics.metrics.denom1;
	    } else {
	        if (ruleWidth > 0) {
	            numShift = fontMetrics.metrics.num2;
	            clearance = ruleWidth;
	        } else {
	            numShift = fontMetrics.metrics.num3;
	            clearance = 3 * fontMetrics.metrics.defaultRuleThickness;
	        }
	        denomShift = fontMetrics.metrics.denom2;
	    }

	    var frac;
	    if (ruleWidth === 0) {
	        // Rule 15c
	        var candiateClearance =
	            (numShift - numer.depth) - (denom.height - denomShift);
	        if (candiateClearance < clearance) {
	            numShift += 0.5 * (clearance - candiateClearance);
	            denomShift += 0.5 * (clearance - candiateClearance);
	        }

	        frac = buildCommon.makeVList([
	            {type: "elem", elem: denomreset, shift: denomShift},
	            {type: "elem", elem: numerreset, shift: -numShift},
	        ], "individualShift", null, options);
	    } else {
	        // Rule 15d
	        var axisHeight = fontMetrics.metrics.axisHeight;

	        if ((numShift - numer.depth) - (axisHeight + 0.5 * ruleWidth) <
	                clearance) {
	            numShift +=
	                clearance - ((numShift - numer.depth) -
	                             (axisHeight + 0.5 * ruleWidth));
	        }

	        if ((axisHeight - 0.5 * ruleWidth) - (denom.height - denomShift) <
	                clearance) {
	            denomShift +=
	                clearance - ((axisHeight - 0.5 * ruleWidth) -
	                             (denom.height - denomShift));
	        }

	        var mid = makeSpan(
	            [options.style.reset(), Style.TEXT.cls(), "frac-line"]);
	        // Manually set the height of the line because its height is
	        // created in CSS
	        mid.height = ruleWidth;

	        var midShift = -(axisHeight - 0.5 * ruleWidth);

	        frac = buildCommon.makeVList([
	            {type: "elem", elem: denomreset, shift: denomShift},
	            {type: "elem", elem: mid,        shift: midShift},
	            {type: "elem", elem: numerreset, shift: -numShift},
	        ], "individualShift", null, options);
	    }

	    // Since we manually change the style sometimes (with \dfrac or \tfrac),
	    // account for the possible size change here.
	    frac.height *= fstyle.sizeMultiplier / options.style.sizeMultiplier;
	    frac.depth *= fstyle.sizeMultiplier / options.style.sizeMultiplier;

	    // Rule 15e
	    var delimSize;
	    if (fstyle.size === Style.DISPLAY.size) {
	        delimSize = fontMetrics.metrics.delim1;
	    } else {
	        delimSize = fontMetrics.metrics.getDelim2(fstyle);
	    }

	    var leftDelim;
	    var rightDelim;
	    if (group.value.leftDelim == null) {
	        leftDelim = makeNullDelimiter(options);
	    } else {
	        leftDelim = delimiter.customSizedDelim(
	            group.value.leftDelim, delimSize, true,
	            options.withStyle(fstyle), group.mode);
	    }
	    if (group.value.rightDelim == null) {
	        rightDelim = makeNullDelimiter(options);
	    } else {
	        rightDelim = delimiter.customSizedDelim(
	            group.value.rightDelim, delimSize, true,
	            options.withStyle(fstyle), group.mode);
	    }

	    return makeSpan(
	        ["mord", options.style.reset(), fstyle.cls()],
	        [leftDelim, makeSpan(["mfrac"], [frac]), rightDelim],
	        options.getColor());
	};

	groupTypes.array = function(group, options, prev) {
	    var r;
	    var c;
	    var nr = group.value.body.length;
	    var nc = 0;
	    var body = new Array(nr);

	    // Horizontal spacing
	    var pt = 1 / fontMetrics.metrics.ptPerEm;
	    var arraycolsep = 5 * pt; // \arraycolsep in article.cls

	    // Vertical spacing
	    var baselineskip = 12 * pt; // see size10.clo
	    // Default \arraystretch from lttab.dtx
	    // TODO(gagern): may get redefined once we have user-defined macros
	    var arraystretch = utils.deflt(group.value.arraystretch, 1);
	    var arrayskip = arraystretch * baselineskip;
	    var arstrutHeight = 0.7 * arrayskip; // \strutbox in ltfsstrc.dtx and
	    var arstrutDepth = 0.3 * arrayskip;  // \@arstrutbox in lttab.dtx

	    var totalHeight = 0;
	    for (r = 0; r < group.value.body.length; ++r) {
	        var inrow = group.value.body[r];
	        var height = arstrutHeight; // \@array adds an \@arstrut
	        var depth = arstrutDepth;   // to each tow (via the template)

	        if (nc < inrow.length) {
	            nc = inrow.length;
	        }

	        var outrow = new Array(inrow.length);
	        for (c = 0; c < inrow.length; ++c) {
	            var elt = buildGroup(inrow[c], options);
	            if (depth < elt.depth) {
	                depth = elt.depth;
	            }
	            if (height < elt.height) {
	                height = elt.height;
	            }
	            outrow[c] = elt;
	        }

	        var gap = 0;
	        if (group.value.rowGaps[r]) {
	            gap = group.value.rowGaps[r].value;
	            switch (gap.unit) {
	                case "em":
	                    gap = gap.number;
	                    break;
	                case "ex":
	                    gap = gap.number * fontMetrics.metrics.emPerEx;
	                    break;
	                default:
	                    console.error("Can't handle unit " + gap.unit);
	                    gap = 0;
	            }
	            if (gap > 0) { // \@argarraycr
	                gap += arstrutDepth;
	                if (depth < gap) {
	                    depth = gap; // \@xargarraycr
	                }
	                gap = 0;
	            }
	        }

	        outrow.height = height;
	        outrow.depth = depth;
	        totalHeight += height;
	        outrow.pos = totalHeight;
	        totalHeight += depth + gap; // \@yargarraycr
	        body[r] = outrow;
	    }

	    var offset = totalHeight / 2 + fontMetrics.metrics.axisHeight;
	    var colDescriptions = group.value.cols || [];
	    var cols = [];
	    var colSep;
	    var colDescrNum;
	    for (c = 0, colDescrNum = 0;
	         // Continue while either there are more columns or more column
	         // descriptions, so trailing separators don't get lost.
	         c < nc || colDescrNum < colDescriptions.length;
	         ++c, ++colDescrNum) {

	        var colDescr = colDescriptions[colDescrNum] || {};

	        var firstSeparator = true;
	        while (colDescr.type === "separator") {
	            // If there is more than one separator in a row, add a space
	            // between them.
	            if (!firstSeparator) {
	                colSep = makeSpan(["arraycolsep"], []);
	                colSep.style.width =
	                    fontMetrics.metrics.doubleRuleSep + "em";
	                cols.push(colSep);
	            }

	            if (colDescr.separator === "|") {
	                var separator = makeSpan(
	                    ["vertical-separator"],
	                    []);
	                separator.style.height = totalHeight + "em";
	                separator.style.verticalAlign =
	                    -(totalHeight - offset) + "em";

	                cols.push(separator);
	            } else {
	                throw new ParseError(
	                    "Invalid separator type: " + colDescr.separator);
	            }

	            colDescrNum++;
	            colDescr = colDescriptions[colDescrNum] || {};
	            firstSeparator = false;
	        }

	        if (c >= nc) {
	            continue;
	        }

	        var sepwidth;
	        if (c > 0 || group.value.hskipBeforeAndAfter) {
	            sepwidth = utils.deflt(colDescr.pregap, arraycolsep);
	            if (sepwidth !== 0) {
	                colSep = makeSpan(["arraycolsep"], []);
	                colSep.style.width = sepwidth + "em";
	                cols.push(colSep);
	            }
	        }

	        var col = [];
	        for (r = 0; r < nr; ++r) {
	            var row = body[r];
	            var elem = row[c];
	            if (!elem) {
	                continue;
	            }
	            var shift = row.pos - offset;
	            elem.depth = row.depth;
	            elem.height = row.height;
	            col.push({type: "elem", elem: elem, shift: shift});
	        }

	        col = buildCommon.makeVList(col, "individualShift", null, options);
	        col = makeSpan(
	            ["col-align-" + (colDescr.align || "c")],
	            [col]);
	        cols.push(col);

	        if (c < nc - 1 || group.value.hskipBeforeAndAfter) {
	            sepwidth = utils.deflt(colDescr.postgap, arraycolsep);
	            if (sepwidth !== 0) {
	                colSep = makeSpan(["arraycolsep"], []);
	                colSep.style.width = sepwidth + "em";
	                cols.push(colSep);
	            }
	        }
	    }
	    body = makeSpan(["mtable"], cols);
	    return makeSpan(["mord"], [body], options.getColor());
	};

	groupTypes.spacing = function(group, options, prev) {
	    if (group.value === "\\ " || group.value === "\\space" ||
	        group.value === " " || group.value === "~") {
	        // Spaces are generated by adding an actual space. Each of these
	        // things has an entry in the symbols table, so these will be turned
	        // into appropriate outputs.
	        return makeSpan(
	            ["mord", "mspace"],
	            [buildCommon.mathsym(group.value, group.mode)]
	        );
	    } else {
	        // Other kinds of spaces are of arbitrary width. We use CSS to
	        // generate these.
	        return makeSpan(
	            ["mord", "mspace",
	             buildCommon.spacingFunctions[group.value].className]);
	    }
	};

	groupTypes.llap = function(group, options, prev) {
	    var inner = makeSpan(
	        ["inner"], [buildGroup(group.value.body, options.reset())]);
	    var fix = makeSpan(["fix"], []);
	    return makeSpan(
	        ["llap", options.style.cls()], [inner, fix]);
	};

	groupTypes.rlap = function(group, options, prev) {
	    var inner = makeSpan(
	        ["inner"], [buildGroup(group.value.body, options.reset())]);
	    var fix = makeSpan(["fix"], []);
	    return makeSpan(
	        ["rlap", options.style.cls()], [inner, fix]);
	};

	groupTypes.op = function(group, options, prev) {
	    // Operators are handled in the TeXbook pg. 443-444, rule 13(a).
	    var supGroup;
	    var subGroup;
	    var hasLimits = false;
	    if (group.type === "supsub" ) {
	        // If we have limits, supsub will pass us its group to handle. Pull
	        // out the superscript and subscript and set the group to the op in
	        // its base.
	        supGroup = group.value.sup;
	        subGroup = group.value.sub;
	        group = group.value.base;
	        hasLimits = true;
	    }

	    // Most operators have a large successor symbol, but these don't.
	    var noSuccessor = [
	        "\\smallint",
	    ];

	    var large = false;
	    if (options.style.size === Style.DISPLAY.size &&
	        group.value.symbol &&
	        !utils.contains(noSuccessor, group.value.body)) {

	        // Most symbol operators get larger in displaystyle (rule 13)
	        large = true;
	    }

	    var base;
	    var baseShift = 0;
	    var slant = 0;
	    if (group.value.symbol) {
	        // If this is a symbol, create the symbol.
	        var style = large ? "Size2-Regular" : "Size1-Regular";
	        base = buildCommon.makeSymbol(
	            group.value.body, style, "math", options.getColor(),
	            ["op-symbol", large ? "large-op" : "small-op", "mop"]);

	        // Shift the symbol so its center lies on the axis (rule 13). It
	        // appears that our fonts have the centers of the symbols already
	        // almost on the axis, so these numbers are very small. Note we
	        // don't actually apply this here, but instead it is used either in
	        // the vlist creation or separately when there are no limits.
	        baseShift = (base.height - base.depth) / 2 -
	            fontMetrics.metrics.axisHeight *
	            options.style.sizeMultiplier;

	        // The slant of the symbol is just its italic correction.
	        slant = base.italic;
	    } else {
	        // Otherwise, this is a text operator. Build the text from the
	        // operator's name.
	        // TODO(emily): Add a space in the middle of some of these
	        // operators, like \limsup
	        var output = [];
	        for (var i = 1; i < group.value.body.length; i++) {
	            output.push(buildCommon.mathsym(group.value.body[i], group.mode));
	        }
	        base = makeSpan(["mop"], output, options.getColor());
	    }

	    if (hasLimits) {
	        // IE 8 clips \int if it is in a display: inline-block. We wrap it
	        // in a new span so it is an inline, and works.
	        base = makeSpan([], [base]);

	        var supmid;
	        var supKern;
	        var submid;
	        var subKern;
	        // We manually have to handle the superscripts and subscripts. This,
	        // aside from the kern calculations, is copied from supsub.
	        if (supGroup) {
	            var sup = buildGroup(
	                supGroup, options.withStyle(options.style.sup()));
	            supmid = makeSpan(
	                [options.style.reset(), options.style.sup().cls()], [sup]);

	            supKern = Math.max(
	                fontMetrics.metrics.bigOpSpacing1,
	                fontMetrics.metrics.bigOpSpacing3 - sup.depth);
	        }

	        if (subGroup) {
	            var sub = buildGroup(
	                subGroup, options.withStyle(options.style.sub()));
	            submid = makeSpan(
	                [options.style.reset(), options.style.sub().cls()],
	                [sub]);

	            subKern = Math.max(
	                fontMetrics.metrics.bigOpSpacing2,
	                fontMetrics.metrics.bigOpSpacing4 - sub.height);
	        }

	        // Build the final group as a vlist of the possible subscript, base,
	        // and possible superscript.
	        var finalGroup;
	        var top;
	        var bottom;
	        if (!supGroup) {
	            top = base.height - baseShift;

	            finalGroup = buildCommon.makeVList([
	                {type: "kern", size: fontMetrics.metrics.bigOpSpacing5},
	                {type: "elem", elem: submid},
	                {type: "kern", size: subKern},
	                {type: "elem", elem: base},
	            ], "top", top, options);

	            // Here, we shift the limits by the slant of the symbol. Note
	            // that we are supposed to shift the limits by 1/2 of the slant,
	            // but since we are centering the limits adding a full slant of
	            // margin will shift by 1/2 that.
	            finalGroup.children[0].style.marginLeft = -slant + "em";
	        } else if (!subGroup) {
	            bottom = base.depth + baseShift;

	            finalGroup = buildCommon.makeVList([
	                {type: "elem", elem: base},
	                {type: "kern", size: supKern},
	                {type: "elem", elem: supmid},
	                {type: "kern", size: fontMetrics.metrics.bigOpSpacing5},
	            ], "bottom", bottom, options);

	            // See comment above about slants
	            finalGroup.children[1].style.marginLeft = slant + "em";
	        } else if (!supGroup && !subGroup) {
	            // This case probably shouldn't occur (this would mean the
	            // supsub was sending us a group with no superscript or
	            // subscript) but be safe.
	            return base;
	        } else {
	            bottom = fontMetrics.metrics.bigOpSpacing5 +
	                submid.height + submid.depth +
	                subKern +
	                base.depth + baseShift;

	            finalGroup = buildCommon.makeVList([
	                {type: "kern", size: fontMetrics.metrics.bigOpSpacing5},
	                {type: "elem", elem: submid},
	                {type: "kern", size: subKern},
	                {type: "elem", elem: base},
	                {type: "kern", size: supKern},
	                {type: "elem", elem: supmid},
	                {type: "kern", size: fontMetrics.metrics.bigOpSpacing5},
	            ], "bottom", bottom, options);

	            // See comment above about slants
	            finalGroup.children[0].style.marginLeft = -slant + "em";
	            finalGroup.children[2].style.marginLeft = slant + "em";
	        }

	        return makeSpan(["mop", "op-limits"], [finalGroup]);
	    } else {
	        if (group.value.symbol) {
	            base.style.top = baseShift + "em";
	        }

	        return base;
	    }
	};

	groupTypes.katex = function(group, options, prev) {
	    // The KaTeX logo. The offsets for the K and a were chosen to look
	    // good, but the offsets for the T, E, and X were taken from the
	    // definition of \TeX in TeX (see TeXbook pg. 356)
	    var k = makeSpan(
	        ["k"], [buildCommon.mathsym("K", group.mode)]);
	    var a = makeSpan(
	        ["a"], [buildCommon.mathsym("A", group.mode)]);

	    a.height = (a.height + 0.2) * 0.75;
	    a.depth = (a.height - 0.2) * 0.75;

	    var t = makeSpan(
	        ["t"], [buildCommon.mathsym("T", group.mode)]);
	    var e = makeSpan(
	        ["e"], [buildCommon.mathsym("E", group.mode)]);

	    e.height = (e.height - 0.2155);
	    e.depth = (e.depth + 0.2155);

	    var x = makeSpan(
	        ["x"], [buildCommon.mathsym("X", group.mode)]);

	    return makeSpan(
	        ["katex-logo", "mord"], [k, a, t, e, x], options.getColor());
	};

	groupTypes.overline = function(group, options, prev) {
	    // Overlines are handled in the TeXbook pg 443, Rule 9.

	    // Build the inner group in the cramped style.
	    var innerGroup = buildGroup(group.value.body,
	            options.withStyle(options.style.cramp()));

	    var ruleWidth = fontMetrics.metrics.defaultRuleThickness /
	        options.style.sizeMultiplier;

	    // Create the line above the body
	    var line = makeSpan(
	        [options.style.reset(), Style.TEXT.cls(), "overline-line"]);
	    line.height = ruleWidth;
	    line.maxFontSize = 1.0;

	    // Generate the vlist, with the appropriate kerns
	    var vlist = buildCommon.makeVList([
	        {type: "elem", elem: innerGroup},
	        {type: "kern", size: 3 * ruleWidth},
	        {type: "elem", elem: line},
	        {type: "kern", size: ruleWidth},
	    ], "firstBaseline", null, options);

	    return makeSpan(["overline", "mord"], [vlist], options.getColor());
	};

	groupTypes.underline = function(group, options, prev) {
	    // Underlines are handled in the TeXbook pg 443, Rule 10.

	    // Build the inner group.
	    var innerGroup = buildGroup(group.value.body, options);

	    var ruleWidth = fontMetrics.metrics.defaultRuleThickness /
	        options.style.sizeMultiplier;

	    // Create the line above the body
	    var line = makeSpan(
	        [options.style.reset(), Style.TEXT.cls(), "underline-line"]);
	    line.height = ruleWidth;
	    line.maxFontSize = 1.0;

	    // Generate the vlist, with the appropriate kerns
	    var vlist = buildCommon.makeVList([
	        {type: "kern", size: ruleWidth},
	        {type: "elem", elem: line},
	        {type: "kern", size: 3 * ruleWidth},
	        {type: "elem", elem: innerGroup},
	    ], "top", innerGroup.height, options);

	    return makeSpan(["underline", "mord"], [vlist], options.getColor());
	};

	groupTypes.sqrt = function(group, options, prev) {
	    // Square roots are handled in the TeXbook pg. 443, Rule 11.

	    // First, we do the same steps as in overline to build the inner group
	    // and line
	    var inner = buildGroup(group.value.body,
	            options.withStyle(options.style.cramp()));

	    var ruleWidth = fontMetrics.metrics.defaultRuleThickness /
	        options.style.sizeMultiplier;

	    var line = makeSpan(
	        [options.style.reset(), Style.TEXT.cls(), "sqrt-line"], [],
	        options.getColor());
	    line.height = ruleWidth;
	    line.maxFontSize = 1.0;

	    var phi = ruleWidth;
	    if (options.style.id < Style.TEXT.id) {
	        phi = fontMetrics.metrics.xHeight;
	    }

	    // Calculate the clearance between the body and line
	    var lineClearance = ruleWidth + phi / 4;

	    var innerHeight =
	        (inner.height + inner.depth) * options.style.sizeMultiplier;
	    var minDelimiterHeight = innerHeight + lineClearance + ruleWidth;

	    // Create a \surd delimiter of the required minimum size
	    var delim = makeSpan(["sqrt-sign"], [
	        delimiter.customSizedDelim("\\surd", minDelimiterHeight,
	                                   false, options, group.mode)],
	                         options.getColor());

	    var delimDepth = (delim.height + delim.depth) - ruleWidth;

	    // Adjust the clearance based on the delimiter size
	    if (delimDepth > inner.height + inner.depth + lineClearance) {
	        lineClearance =
	            (lineClearance + delimDepth - inner.height - inner.depth) / 2;
	    }

	    // Shift the delimiter so that its top lines up with the top of the line
	    var delimShift = -(inner.height + lineClearance + ruleWidth) + delim.height;
	    delim.style.top = delimShift + "em";
	    delim.height -= delimShift;
	    delim.depth += delimShift;

	    // We add a special case here, because even when `inner` is empty, we
	    // still get a line. So, we use a simple heuristic to decide if we
	    // should omit the body entirely. (note this doesn't work for something
	    // like `\sqrt{\rlap{x}}`, but if someone is doing that they deserve for
	    // it not to work.
	    var body;
	    if (inner.height === 0 && inner.depth === 0) {
	        body = makeSpan();
	    } else {
	        body = buildCommon.makeVList([
	            {type: "elem", elem: inner},
	            {type: "kern", size: lineClearance},
	            {type: "elem", elem: line},
	            {type: "kern", size: ruleWidth},
	        ], "firstBaseline", null, options);
	    }

	    if (!group.value.index) {
	        return makeSpan(["sqrt", "mord"], [delim, body]);
	    } else {
	        // Handle the optional root index

	        // The index is always in scriptscript style
	        var root = buildGroup(
	            group.value.index,
	            options.withStyle(Style.SCRIPTSCRIPT));
	        var rootWrap = makeSpan(
	            [options.style.reset(), Style.SCRIPTSCRIPT.cls()],
	            [root]);

	        // Figure out the height and depth of the inner part
	        var innerRootHeight = Math.max(delim.height, body.height);
	        var innerRootDepth = Math.max(delim.depth, body.depth);

	        // The amount the index is shifted by. This is taken from the TeX
	        // source, in the definition of `\r@@t`.
	        var toShift = 0.6 * (innerRootHeight - innerRootDepth);

	        // Build a VList with the superscript shifted up correctly
	        var rootVList = buildCommon.makeVList(
	            [{type: "elem", elem: rootWrap}],
	            "shift", -toShift, options);
	        // Add a class surrounding it so we can add on the appropriate
	        // kerning
	        var rootVListWrap = makeSpan(["root"], [rootVList]);

	        return makeSpan(["sqrt", "mord"], [rootVListWrap, delim, body]);
	    }
	};

	groupTypes.sizing = function(group, options, prev) {
	    // Handle sizing operators like \Huge. Real TeX doesn't actually allow
	    // these functions inside of math expressions, so we do some special
	    // handling.
	    var inner = buildExpression(group.value.value,
	            options.withSize(group.value.size), prev);

	    var span = makeSpan(["mord"],
	        [makeSpan(["sizing", "reset-" + options.size, group.value.size,
	                   options.style.cls()],
	                  inner)]);

	    // Calculate the correct maxFontSize manually
	    var fontSize = buildCommon.sizingMultiplier[group.value.size];
	    span.maxFontSize = fontSize * options.style.sizeMultiplier;

	    return span;
	};

	groupTypes.styling = function(group, options, prev) {
	    // Style changes are handled in the TeXbook on pg. 442, Rule 3.

	    // Figure out what style we're changing to.
	    var style = {
	        "display": Style.DISPLAY,
	        "text": Style.TEXT,
	        "script": Style.SCRIPT,
	        "scriptscript": Style.SCRIPTSCRIPT,
	    };

	    var newStyle = style[group.value.style];

	    // Build the inner expression in the new style.
	    var inner = buildExpression(
	        group.value.value, options.withStyle(newStyle), prev);

	    return makeSpan([options.style.reset(), newStyle.cls()], inner);
	};

	groupTypes.font = function(group, options, prev) {
	    var font = group.value.font;
	    return buildGroup(group.value.body, options.withFont(font), prev);
	};

	groupTypes.delimsizing = function(group, options, prev) {
	    var delim = group.value.value;

	    if (delim === ".") {
	        // Empty delimiters still count as elements, even though they don't
	        // show anything.
	        return makeSpan([groupToType[group.value.delimType]]);
	    }

	    // Use delimiter.sizedDelim to generate the delimiter.
	    return makeSpan(
	        [groupToType[group.value.delimType]],
	        [delimiter.sizedDelim(
	            delim, group.value.size, options, group.mode)]);
	};

	groupTypes.leftright = function(group, options, prev) {
	    // Build the inner expression
	    var inner = buildExpression(group.value.body, options.reset());

	    var innerHeight = 0;
	    var innerDepth = 0;

	    // Calculate its height and depth
	    for (var i = 0; i < inner.length; i++) {
	        innerHeight = Math.max(inner[i].height, innerHeight);
	        innerDepth = Math.max(inner[i].depth, innerDepth);
	    }

	    // The size of delimiters is the same, regardless of what style we are
	    // in. Thus, to correctly calculate the size of delimiter we need around
	    // a group, we scale down the inner size based on the size.
	    innerHeight *= options.style.sizeMultiplier;
	    innerDepth *= options.style.sizeMultiplier;

	    var leftDelim;
	    if (group.value.left === ".") {
	        // Empty delimiters in \left and \right make null delimiter spaces.
	        leftDelim = makeNullDelimiter(options);
	    } else {
	        // Otherwise, use leftRightDelim to generate the correct sized
	        // delimiter.
	        leftDelim = delimiter.leftRightDelim(
	            group.value.left, innerHeight, innerDepth, options,
	            group.mode);
	    }
	    // Add it to the beginning of the expression
	    inner.unshift(leftDelim);

	    var rightDelim;
	    // Same for the right delimiter
	    if (group.value.right === ".") {
	        rightDelim = makeNullDelimiter(options);
	    } else {
	        rightDelim = delimiter.leftRightDelim(
	            group.value.right, innerHeight, innerDepth, options,
	            group.mode);
	    }
	    // Add it to the end of the expression.
	    inner.push(rightDelim);

	    return makeSpan(
	        ["minner", options.style.cls()], inner, options.getColor());
	};

	groupTypes.rule = function(group, options, prev) {
	    // Make an empty span for the rule
	    var rule = makeSpan(["mord", "rule"], [], options.getColor());

	    // Calculate the shift, width, and height of the rule, and account for units
	    var shift = 0;
	    if (group.value.shift) {
	        shift = group.value.shift.number;
	        if (group.value.shift.unit === "ex") {
	            shift *= fontMetrics.metrics.xHeight;
	        }
	    }

	    var width = group.value.width.number;
	    if (group.value.width.unit === "ex") {
	        width *= fontMetrics.metrics.xHeight;
	    }

	    var height = group.value.height.number;
	    if (group.value.height.unit === "ex") {
	        height *= fontMetrics.metrics.xHeight;
	    }

	    // The sizes of rules are absolute, so make it larger if we are in a
	    // smaller style.
	    shift /= options.style.sizeMultiplier;
	    width /= options.style.sizeMultiplier;
	    height /= options.style.sizeMultiplier;

	    // Style the rule to the right size
	    rule.style.borderRightWidth = width + "em";
	    rule.style.borderTopWidth = height + "em";
	    rule.style.bottom = shift + "em";

	    // Record the height and width
	    rule.width = width;
	    rule.height = height + shift;
	    rule.depth = -shift;

	    return rule;
	};

	groupTypes.accent = function(group, options, prev) {
	    // Accents are handled in the TeXbook pg. 443, rule 12.
	    var base = group.value.base;

	    var supsubGroup;
	    if (group.type === "supsub") {
	        // If our base is a character box, and we have superscripts and
	        // subscripts, the supsub will defer to us. In particular, we want
	        // to attach the superscripts and subscripts to the inner body (so
	        // that the position of the superscripts and subscripts won't be
	        // affected by the height of the accent). We accomplish this by
	        // sticking the base of the accent into the base of the supsub, and
	        // rendering that, while keeping track of where the accent is.

	        // The supsub group is the group that was passed in
	        var supsub = group;
	        // The real accent group is the base of the supsub group
	        group = supsub.value.base;
	        // The character box is the base of the accent group
	        base = group.value.base;
	        // Stick the character box into the base of the supsub group
	        supsub.value.base = base;

	        // Rerender the supsub group with its new base, and store that
	        // result.
	        supsubGroup = buildGroup(
	            supsub, options.reset(), prev);
	    }

	    // Build the base group
	    var body = buildGroup(
	        base, options.withStyle(options.style.cramp()));

	    // Calculate the skew of the accent. This is based on the line "If the
	    // nucleus is not a single character, let s = 0; otherwise set s to the
	    // kern amount for the nucleus followed by the \skewchar of its font."
	    // Note that our skew metrics are just the kern between each character
	    // and the skewchar.
	    var skew;
	    if (isCharacterBox(base)) {
	        // If the base is a character box, then we want the skew of the
	        // innermost character. To do that, we find the innermost character:
	        var baseChar = getBaseElem(base);
	        // Then, we render its group to get the symbol inside it
	        var baseGroup = buildGroup(
	            baseChar, options.withStyle(options.style.cramp()));
	        // Finally, we pull the skew off of the symbol.
	        skew = baseGroup.skew;
	        // Note that we now throw away baseGroup, because the layers we
	        // removed with getBaseElem might contain things like \color which
	        // we can't get rid of.
	        // TODO(emily): Find a better way to get the skew
	    } else {
	        skew = 0;
	    }

	    // calculate the amount of space between the body and the accent
	    var clearance = Math.min(body.height, fontMetrics.metrics.xHeight);

	    // Build the accent
	    var accent = buildCommon.makeSymbol(
	        group.value.accent, "Main-Regular", "math", options.getColor());
	    // Remove the italic correction of the accent, because it only serves to
	    // shift the accent over to a place we don't want.
	    accent.italic = 0;

	    // The \vec character that the fonts use is a combining character, and
	    // thus shows up much too far to the left. To account for this, we add a
	    // specific class which shifts the accent over to where we want it.
	    // TODO(emily): Fix this in a better way, like by changing the font
	    var vecClass = group.value.accent === "\\vec" ? "accent-vec" : null;

	    var accentBody = makeSpan(["accent-body", vecClass], [
	        makeSpan([], [accent])]);

	    accentBody = buildCommon.makeVList([
	        {type: "elem", elem: body},
	        {type: "kern", size: -clearance},
	        {type: "elem", elem: accentBody},
	    ], "firstBaseline", null, options);

	    // Shift the accent over by the skew. Note we shift by twice the skew
	    // because we are centering the accent, so by adding 2*skew to the left,
	    // we shift it to the right by 1*skew.
	    accentBody.children[1].style.marginLeft = 2 * skew + "em";

	    var accentWrap = makeSpan(["mord", "accent"], [accentBody]);

	    if (supsubGroup) {
	        // Here, we replace the "base" child of the supsub with our newly
	        // generated accent.
	        supsubGroup.children[0] = accentWrap;

	        // Since we don't rerun the height calculation after replacing the
	        // accent, we manually recalculate height.
	        supsubGroup.height = Math.max(accentWrap.height, supsubGroup.height);

	        // Accents should always be ords, even when their innards are not.
	        supsubGroup.classes[0] = "mord";

	        return supsubGroup;
	    } else {
	        return accentWrap;
	    }
	};

	groupTypes.phantom = function(group, options, prev) {
	    var elements = buildExpression(
	        group.value.value,
	        options.withPhantom(),
	        prev
	    );

	    // \phantom isn't supposed to affect the elements it contains.
	    // See "color" for more details.
	    return new buildCommon.makeFragment(elements);
	};

	/**
	 * buildGroup is the function that takes a group and calls the correct groupType
	 * function for it. It also handles the interaction of size and style changes
	 * between parents and children.
	 */
	var buildGroup = function(group, options, prev) {
	    if (!group) {
	        return makeSpan();
	    }

	    if (groupTypes[group.type]) {
	        // Call the groupTypes function
	        var groupNode = groupTypes[group.type](group, options, prev);
	        var multiplier;

	        // If the style changed between the parent and the current group,
	        // account for the size difference
	        if (options.style !== options.parentStyle) {
	            multiplier = options.style.sizeMultiplier /
	                    options.parentStyle.sizeMultiplier;

	            groupNode.height *= multiplier;
	            groupNode.depth *= multiplier;
	        }

	        // If the size changed between the parent and the current group, account
	        // for that size difference.
	        if (options.size !== options.parentSize) {
	            multiplier = buildCommon.sizingMultiplier[options.size] /
	                    buildCommon.sizingMultiplier[options.parentSize];

	            groupNode.height *= multiplier;
	            groupNode.depth *= multiplier;
	        }

	        return groupNode;
	    } else {
	        throw new ParseError(
	            "Got group of unknown type: '" + group.type + "'");
	    }
	};

	/**
	 * Take an entire parse tree, and build it into an appropriate set of HTML
	 * nodes.
	 */
	var buildHTML = function(tree, options) {
	    // buildExpression is destructive, so we need to make a clone
	    // of the incoming tree so that it isn't accidentally changed
	    tree = JSON.parse(JSON.stringify(tree));

	    // Build the expression contained in the tree
	    var expression = buildExpression(tree, options);
	    var body = makeSpan(["base", options.style.cls()], expression);

	    // Add struts, which ensure that the top of the HTML element falls at the
	    // height of the expression, and the bottom of the HTML element falls at the
	    // depth of the expression.
	    var topStrut = makeSpan(["strut"]);
	    var bottomStrut = makeSpan(["strut", "bottom"]);

	    topStrut.style.height = body.height + "em";
	    bottomStrut.style.height = (body.height + body.depth) + "em";
	    // We'd like to use `vertical-align: top` but in IE 9 this lowers the
	    // baseline of the box to the bottom of this strut (instead staying in the
	    // normal place) so we use an absolute value for vertical-align instead
	    bottomStrut.style.verticalAlign = -body.depth + "em";

	    // Wrap the struts and body together
	    var htmlNode = makeSpan(["katex-html"], [topStrut, bottomStrut, body]);

	    htmlNode.setAttribute("aria-hidden", "true");

	    return htmlNode;
	};

	module.exports = buildHTML;


/***/ },
/* 266 */
/***/ function(module, exports) {

	/**
	 * This file contains information and classes for the various kinds of styles
	 * used in TeX. It provides a generic `Style` class, which holds information
	 * about a specific style. It then provides instances of all the different kinds
	 * of styles possible, and provides functions to move between them and get
	 * information about them.
	 */

	/**
	 * The main style class. Contains a unique id for the style, a size (which is
	 * the same for cramped and uncramped version of a style), a cramped flag, and a
	 * size multiplier, which gives the size difference between a style and
	 * textstyle.
	 */
	function Style(id, size, multiplier, cramped) {
	    this.id = id;
	    this.size = size;
	    this.cramped = cramped;
	    this.sizeMultiplier = multiplier;
	}

	/**
	 * Get the style of a superscript given a base in the current style.
	 */
	Style.prototype.sup = function() {
	    return styles[sup[this.id]];
	};

	/**
	 * Get the style of a subscript given a base in the current style.
	 */
	Style.prototype.sub = function() {
	    return styles[sub[this.id]];
	};

	/**
	 * Get the style of a fraction numerator given the fraction in the current
	 * style.
	 */
	Style.prototype.fracNum = function() {
	    return styles[fracNum[this.id]];
	};

	/**
	 * Get the style of a fraction denominator given the fraction in the current
	 * style.
	 */
	Style.prototype.fracDen = function() {
	    return styles[fracDen[this.id]];
	};

	/**
	 * Get the cramped version of a style (in particular, cramping a cramped style
	 * doesn't change the style).
	 */
	Style.prototype.cramp = function() {
	    return styles[cramp[this.id]];
	};

	/**
	 * HTML class name, like "displaystyle cramped"
	 */
	Style.prototype.cls = function() {
	    return sizeNames[this.size] + (this.cramped ? " cramped" : " uncramped");
	};

	/**
	 * HTML Reset class name, like "reset-textstyle"
	 */
	Style.prototype.reset = function() {
	    return resetNames[this.size];
	};

	// IDs of the different styles
	var D = 0;
	var Dc = 1;
	var T = 2;
	var Tc = 3;
	var S = 4;
	var Sc = 5;
	var SS = 6;
	var SSc = 7;

	// String names for the different sizes
	var sizeNames = [
	    "displaystyle textstyle",
	    "textstyle",
	    "scriptstyle",
	    "scriptscriptstyle",
	];

	// Reset names for the different sizes
	var resetNames = [
	    "reset-textstyle",
	    "reset-textstyle",
	    "reset-scriptstyle",
	    "reset-scriptscriptstyle",
	];

	// Instances of the different styles
	var styles = [
	    new Style(D, 0, 1.0, false),
	    new Style(Dc, 0, 1.0, true),
	    new Style(T, 1, 1.0, false),
	    new Style(Tc, 1, 1.0, true),
	    new Style(S, 2, 0.7, false),
	    new Style(Sc, 2, 0.7, true),
	    new Style(SS, 3, 0.5, false),
	    new Style(SSc, 3, 0.5, true),
	];

	// Lookup tables for switching from one style to another
	var sup = [S, Sc, S, Sc, SS, SSc, SS, SSc];
	var sub = [Sc, Sc, Sc, Sc, SSc, SSc, SSc, SSc];
	var fracNum = [T, Tc, S, Sc, SS, SSc, SS, SSc];
	var fracDen = [Tc, Tc, Sc, Sc, SSc, SSc, SSc, SSc];
	var cramp = [Dc, Dc, Tc, Tc, Sc, Sc, SSc, SSc];

	// We only export some of the styles. Also, we don't export the `Style` class so
	// no more styles can be generated.
	module.exports = {
	    DISPLAY: styles[D],
	    TEXT: styles[T],
	    SCRIPT: styles[S],
	    SCRIPTSCRIPT: styles[SS],
	};


/***/ },
/* 267 */
/***/ function(module, exports, __webpack_require__) {

	/* eslint no-console:0 */
	/**
	 * This module contains general functions that can be used for building
	 * different kinds of domTree nodes in a consistent manner.
	 */

	var domTree = __webpack_require__(268);
	var fontMetrics = __webpack_require__(270);
	var symbols = __webpack_require__(272);
	var utils = __webpack_require__(269);

	var greekCapitals = [
	    "\\Gamma",
	    "\\Delta",
	    "\\Theta",
	    "\\Lambda",
	    "\\Xi",
	    "\\Pi",
	    "\\Sigma",
	    "\\Upsilon",
	    "\\Phi",
	    "\\Psi",
	    "\\Omega",
	];

	var dotlessLetters = [
	    "\u0131",   // dotless i, \imath
	    "\u0237",   // dotless j, \jmath
	];

	/**
	 * Makes a symbolNode after translation via the list of symbols in symbols.js.
	 * Correctly pulls out metrics for the character, and optionally takes a list of
	 * classes to be attached to the node.
	 */
	var makeSymbol = function(value, style, mode, color, classes) {
	    // Replace the value with its replaced value from symbol.js
	    if (symbols[mode][value] && symbols[mode][value].replace) {
	        value = symbols[mode][value].replace;
	    }

	    var metrics = fontMetrics.getCharacterMetrics(value, style);

	    var symbolNode;
	    if (metrics) {
	        symbolNode = new domTree.symbolNode(
	            value, metrics.height, metrics.depth, metrics.italic, metrics.skew,
	            classes);
	    } else {
	        // TODO(emily): Figure out a good way to only print this in development
	        typeof console !== "undefined" && console.warn(
	            "No character metrics for '" + value + "' in style '" +
	                style + "'");
	        symbolNode = new domTree.symbolNode(value, 0, 0, 0, 0, classes);
	    }

	    if (color) {
	        symbolNode.style.color = color;
	    }

	    return symbolNode;
	};

	/**
	 * Makes a symbol in Main-Regular or AMS-Regular.
	 * Used for rel, bin, open, close, inner, and punct.
	 */
	var mathsym = function(value, mode, color, classes) {
	    // Decide what font to render the symbol in by its entry in the symbols
	    // table.
	    // Have a special case for when the value = \ because the \ is used as a
	    // textord in unsupported command errors but cannot be parsed as a regular
	    // text ordinal and is therefore not present as a symbol in the symbols
	    // table for text
	    if (value === "\\" || symbols[mode][value].font === "main") {
	        return makeSymbol(value, "Main-Regular", mode, color, classes);
	    } else {
	        return makeSymbol(
	            value, "AMS-Regular", mode, color, classes.concat(["amsrm"]));
	    }
	};

	/**
	 * Makes a symbol in the default font for mathords and textords.
	 */
	var mathDefault = function(value, mode, color, classes, type) {
	    if (type === "mathord") {
	        return mathit(value, mode, color, classes);
	    } else if (type === "textord") {
	        return makeSymbol(
	            value, "Main-Regular", mode, color, classes.concat(["mathrm"]));
	    } else {
	        throw new Error("unexpected type: " + type + " in mathDefault");
	    }
	};

	/**
	 * Makes a symbol in the italic math font.
	 */
	var mathit = function(value, mode, color, classes) {
	    if (/[0-9]/.test(value.charAt(0)) ||
	            // glyphs for \imath and \jmath do not exist in Math-Italic so we
	            // need to use Main-Italic instead
	            utils.contains(dotlessLetters, value) ||
	            utils.contains(greekCapitals, value)) {
	        return makeSymbol(
	            value, "Main-Italic", mode, color, classes.concat(["mainit"]));
	    } else {
	        return makeSymbol(
	            value, "Math-Italic", mode, color, classes.concat(["mathit"]));
	    }
	};

	/**
	 * Makes either a mathord or textord in the correct font and color.
	 */
	var makeOrd = function(group, options, type) {
	    var mode = group.mode;
	    var value = group.value;
	    if (symbols[mode][value] && symbols[mode][value].replace) {
	        value = symbols[mode][value].replace;
	    }

	    var classes = ["mord"];
	    var color = options.getColor();

	    var font = options.font;
	    if (font) {
	        if (font === "mathit" || utils.contains(dotlessLetters, value)) {
	            return mathit(value, mode, color, classes);
	        } else {
	            var fontName = fontMap[font].fontName;
	            if (fontMetrics.getCharacterMetrics(value, fontName)) {
	                return makeSymbol(
	                    value, fontName, mode, color, classes.concat([font]));
	            } else {
	                return mathDefault(value, mode, color, classes, type);
	            }
	        }
	    } else {
	        return mathDefault(value, mode, color, classes, type);
	    }
	};

	/**
	 * Calculate the height, depth, and maxFontSize of an element based on its
	 * children.
	 */
	var sizeElementFromChildren = function(elem) {
	    var height = 0;
	    var depth = 0;
	    var maxFontSize = 0;

	    if (elem.children) {
	        for (var i = 0; i < elem.children.length; i++) {
	            if (elem.children[i].height > height) {
	                height = elem.children[i].height;
	            }
	            if (elem.children[i].depth > depth) {
	                depth = elem.children[i].depth;
	            }
	            if (elem.children[i].maxFontSize > maxFontSize) {
	                maxFontSize = elem.children[i].maxFontSize;
	            }
	        }
	    }

	    elem.height = height;
	    elem.depth = depth;
	    elem.maxFontSize = maxFontSize;
	};

	/**
	 * Makes a span with the given list of classes, list of children, and color.
	 */
	var makeSpan = function(classes, children, color) {
	    var span = new domTree.span(classes, children);

	    sizeElementFromChildren(span);

	    if (color) {
	        span.style.color = color;
	    }

	    return span;
	};

	/**
	 * Makes a document fragment with the given list of children.
	 */
	var makeFragment = function(children) {
	    var fragment = new domTree.documentFragment(children);

	    sizeElementFromChildren(fragment);

	    return fragment;
	};

	/**
	 * Makes an element placed in each of the vlist elements to ensure that each
	 * element has the same max font size. To do this, we create a zero-width space
	 * with the correct font size.
	 */
	var makeFontSizer = function(options, fontSize) {
	    var fontSizeInner = makeSpan([], [new domTree.symbolNode("\u200b")]);
	    fontSizeInner.style.fontSize =
	        (fontSize / options.style.sizeMultiplier) + "em";

	    var fontSizer = makeSpan(
	        ["fontsize-ensurer", "reset-" + options.size, "size5"],
	        [fontSizeInner]);

	    return fontSizer;
	};

	/**
	 * Makes a vertical list by stacking elements and kerns on top of each other.
	 * Allows for many different ways of specifying the positioning method.
	 *
	 * Arguments:
	 *  - children: A list of child or kern nodes to be stacked on top of each other
	 *              (i.e. the first element will be at the bottom, and the last at
	 *              the top). Element nodes are specified as
	 *                {type: "elem", elem: node}
	 *              while kern nodes are specified as
	 *                {type: "kern", size: size}
	 *  - positionType: The method by which the vlist should be positioned. Valid
	 *                  values are:
	 *                   - "individualShift": The children list only contains elem
	 *                                        nodes, and each node contains an extra
	 *                                        "shift" value of how much it should be
	 *                                        shifted (note that shifting is always
	 *                                        moving downwards). positionData is
	 *                                        ignored.
	 *                   - "top": The positionData specifies the topmost point of
	 *                            the vlist (note this is expected to be a height,
	 *                            so positive values move up)
	 *                   - "bottom": The positionData specifies the bottommost point
	 *                               of the vlist (note this is expected to be a
	 *                               depth, so positive values move down
	 *                   - "shift": The vlist will be positioned such that its
	 *                              baseline is positionData away from the baseline
	 *                              of the first child. Positive values move
	 *                              downwards.
	 *                   - "firstBaseline": The vlist will be positioned such that
	 *                                      its baseline is aligned with the
	 *                                      baseline of the first child.
	 *                                      positionData is ignored. (this is
	 *                                      equivalent to "shift" with
	 *                                      positionData=0)
	 *  - positionData: Data used in different ways depending on positionType
	 *  - options: An Options object
	 *
	 */
	var makeVList = function(children, positionType, positionData, options) {
	    var depth;
	    var currPos;
	    var i;
	    if (positionType === "individualShift") {
	        var oldChildren = children;
	        children = [oldChildren[0]];

	        // Add in kerns to the list of children to get each element to be
	        // shifted to the correct specified shift
	        depth = -oldChildren[0].shift - oldChildren[0].elem.depth;
	        currPos = depth;
	        for (i = 1; i < oldChildren.length; i++) {
	            var diff = -oldChildren[i].shift - currPos -
	                oldChildren[i].elem.depth;
	            var size = diff -
	                (oldChildren[i - 1].elem.height +
	                 oldChildren[i - 1].elem.depth);

	            currPos = currPos + diff;

	            children.push({type: "kern", size: size});
	            children.push(oldChildren[i]);
	        }
	    } else if (positionType === "top") {
	        // We always start at the bottom, so calculate the bottom by adding up
	        // all the sizes
	        var bottom = positionData;
	        for (i = 0; i < children.length; i++) {
	            if (children[i].type === "kern") {
	                bottom -= children[i].size;
	            } else {
	                bottom -= children[i].elem.height + children[i].elem.depth;
	            }
	        }
	        depth = bottom;
	    } else if (positionType === "bottom") {
	        depth = -positionData;
	    } else if (positionType === "shift") {
	        depth = -children[0].elem.depth - positionData;
	    } else if (positionType === "firstBaseline") {
	        depth = -children[0].elem.depth;
	    } else {
	        depth = 0;
	    }

	    // Make the fontSizer
	    var maxFontSize = 0;
	    for (i = 0; i < children.length; i++) {
	        if (children[i].type === "elem") {
	            maxFontSize = Math.max(maxFontSize, children[i].elem.maxFontSize);
	        }
	    }
	    var fontSizer = makeFontSizer(options, maxFontSize);

	    // Create a new list of actual children at the correct offsets
	    var realChildren = [];
	    currPos = depth;
	    for (i = 0; i < children.length; i++) {
	        if (children[i].type === "kern") {
	            currPos += children[i].size;
	        } else {
	            var child = children[i].elem;

	            var shift = -child.depth - currPos;
	            currPos += child.height + child.depth;

	            var childWrap = makeSpan([], [fontSizer, child]);
	            childWrap.height -= shift;
	            childWrap.depth += shift;
	            childWrap.style.top = shift + "em";

	            realChildren.push(childWrap);
	        }
	    }

	    // Add in an element at the end with no offset to fix the calculation of
	    // baselines in some browsers (namely IE, sometimes safari)
	    var baselineFix = makeSpan(
	        ["baseline-fix"], [fontSizer, new domTree.symbolNode("\u200b")]);
	    realChildren.push(baselineFix);

	    var vlist = makeSpan(["vlist"], realChildren);
	    // Fix the final height and depth, in case there were kerns at the ends
	    // since the makeSpan calculation won't take that in to account.
	    vlist.height = Math.max(currPos, vlist.height);
	    vlist.depth = Math.max(-depth, vlist.depth);
	    return vlist;
	};

	// A table of size -> font size for the different sizing functions
	var sizingMultiplier = {
	    size1: 0.5,
	    size2: 0.7,
	    size3: 0.8,
	    size4: 0.9,
	    size5: 1.0,
	    size6: 1.2,
	    size7: 1.44,
	    size8: 1.73,
	    size9: 2.07,
	    size10: 2.49,
	};

	// A map of spacing functions to their attributes, like size and corresponding
	// CSS class
	var spacingFunctions = {
	    "\\qquad": {
	        size: "2em",
	        className: "qquad",
	    },
	    "\\quad": {
	        size: "1em",
	        className: "quad",
	    },
	    "\\enspace": {
	        size: "0.5em",
	        className: "enspace",
	    },
	    "\\;": {
	        size: "0.277778em",
	        className: "thickspace",
	    },
	    "\\:": {
	        size: "0.22222em",
	        className: "mediumspace",
	    },
	    "\\,": {
	        size: "0.16667em",
	        className: "thinspace",
	    },
	    "\\!": {
	        size: "-0.16667em",
	        className: "negativethinspace",
	    },
	};

	/**
	 * Maps TeX font commands to objects containing:
	 * - variant: string used for "mathvariant" attribute in buildMathML.js
	 * - fontName: the "style" parameter to fontMetrics.getCharacterMetrics
	 */
	// A map between tex font commands an MathML mathvariant attribute values
	var fontMap = {
	    // styles
	    "mathbf": {
	        variant: "bold",
	        fontName: "Main-Bold",
	    },
	    "mathrm": {
	        variant: "normal",
	        fontName: "Main-Regular",
	    },

	    // "mathit" is missing because it requires the use of two fonts: Main-Italic
	    // and Math-Italic.  This is handled by a special case in makeOrd which ends
	    // up calling mathit.

	    // families
	    "mathbb": {
	        variant: "double-struck",
	        fontName: "AMS-Regular",
	    },
	    "mathcal": {
	        variant: "script",
	        fontName: "Caligraphic-Regular",
	    },
	    "mathfrak": {
	        variant: "fraktur",
	        fontName: "Fraktur-Regular",
	    },
	    "mathscr": {
	        variant: "script",
	        fontName: "Script-Regular",
	    },
	    "mathsf": {
	        variant: "sans-serif",
	        fontName: "SansSerif-Regular",
	    },
	    "mathtt": {
	        variant: "monospace",
	        fontName: "Typewriter-Regular",
	    },
	};

	module.exports = {
	    fontMap: fontMap,
	    makeSymbol: makeSymbol,
	    mathsym: mathsym,
	    makeSpan: makeSpan,
	    makeFragment: makeFragment,
	    makeVList: makeVList,
	    makeOrd: makeOrd,
	    sizingMultiplier: sizingMultiplier,
	    spacingFunctions: spacingFunctions,
	};


/***/ },
/* 268 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * These objects store the data about the DOM nodes we create, as well as some
	 * extra data. They can then be transformed into real DOM nodes with the
	 * `toNode` function or HTML markup using `toMarkup`. They are useful for both
	 * storing extra properties on the nodes, as well as providing a way to easily
	 * work with the DOM.
	 *
	 * Similar functions for working with MathML nodes exist in mathMLTree.js.
	 */

	var utils = __webpack_require__(269);

	/**
	 * Create an HTML className based on a list of classes. In addition to joining
	 * with spaces, we also remove null or empty classes.
	 */
	var createClass = function(classes) {
	    classes = classes.slice();
	    for (var i = classes.length - 1; i >= 0; i--) {
	        if (!classes[i]) {
	            classes.splice(i, 1);
	        }
	    }

	    return classes.join(" ");
	};

	/**
	 * This node represents a span node, with a className, a list of children, and
	 * an inline style. It also contains information about its height, depth, and
	 * maxFontSize.
	 */
	function span(classes, children, height, depth, maxFontSize, style) {
	    this.classes = classes || [];
	    this.children = children || [];
	    this.height = height || 0;
	    this.depth = depth || 0;
	    this.maxFontSize = maxFontSize || 0;
	    this.style = style || {};
	    this.attributes = {};
	}

	/**
	 * Sets an arbitrary attribute on the span. Warning: use this wisely. Not all
	 * browsers support attributes the same, and having too many custom attributes
	 * is probably bad.
	 */
	span.prototype.setAttribute = function(attribute, value) {
	    this.attributes[attribute] = value;
	};

	/**
	 * Convert the span into an HTML node
	 */
	span.prototype.toNode = function() {
	    var span = document.createElement("span");

	    // Apply the class
	    span.className = createClass(this.classes);

	    // Apply inline styles
	    for (var style in this.style) {
	        if (Object.prototype.hasOwnProperty.call(this.style, style)) {
	            span.style[style] = this.style[style];
	        }
	    }

	    // Apply attributes
	    for (var attr in this.attributes) {
	        if (Object.prototype.hasOwnProperty.call(this.attributes, attr)) {
	            span.setAttribute(attr, this.attributes[attr]);
	        }
	    }

	    // Append the children, also as HTML nodes
	    for (var i = 0; i < this.children.length; i++) {
	        span.appendChild(this.children[i].toNode());
	    }

	    return span;
	};

	/**
	 * Convert the span into an HTML markup string
	 */
	span.prototype.toMarkup = function() {
	    var markup = "<span";

	    // Add the class
	    if (this.classes.length) {
	        markup += " class=\"";
	        markup += utils.escape(createClass(this.classes));
	        markup += "\"";
	    }

	    var styles = "";

	    // Add the styles, after hyphenation
	    for (var style in this.style) {
	        if (this.style.hasOwnProperty(style)) {
	            styles += utils.hyphenate(style) + ":" + this.style[style] + ";";
	        }
	    }

	    if (styles) {
	        markup += " style=\"" + utils.escape(styles) + "\"";
	    }

	    // Add the attributes
	    for (var attr in this.attributes) {
	        if (Object.prototype.hasOwnProperty.call(this.attributes, attr)) {
	            markup += " " + attr + "=\"";
	            markup += utils.escape(this.attributes[attr]);
	            markup += "\"";
	        }
	    }

	    markup += ">";

	    // Add the markup of the children, also as markup
	    for (var i = 0; i < this.children.length; i++) {
	        markup += this.children[i].toMarkup();
	    }

	    markup += "</span>";

	    return markup;
	};

	/**
	 * This node represents a document fragment, which contains elements, but when
	 * placed into the DOM doesn't have any representation itself. Thus, it only
	 * contains children and doesn't have any HTML properties. It also keeps track
	 * of a height, depth, and maxFontSize.
	 */
	function documentFragment(children, height, depth, maxFontSize) {
	    this.children = children || [];
	    this.height = height || 0;
	    this.depth = depth || 0;
	    this.maxFontSize = maxFontSize || 0;
	}

	/**
	 * Convert the fragment into a node
	 */
	documentFragment.prototype.toNode = function() {
	    // Create a fragment
	    var frag = document.createDocumentFragment();

	    // Append the children
	    for (var i = 0; i < this.children.length; i++) {
	        frag.appendChild(this.children[i].toNode());
	    }

	    return frag;
	};

	/**
	 * Convert the fragment into HTML markup
	 */
	documentFragment.prototype.toMarkup = function() {
	    var markup = "";

	    // Simply concatenate the markup for the children together
	    for (var i = 0; i < this.children.length; i++) {
	        markup += this.children[i].toMarkup();
	    }

	    return markup;
	};

	/**
	 * A symbol node contains information about a single symbol. It either renders
	 * to a single text node, or a span with a single text node in it, depending on
	 * whether it has CSS classes, styles, or needs italic correction.
	 */
	function symbolNode(value, height, depth, italic, skew, classes, style) {
	    this.value = value || "";
	    this.height = height || 0;
	    this.depth = depth || 0;
	    this.italic = italic || 0;
	    this.skew = skew || 0;
	    this.classes = classes || [];
	    this.style = style || {};
	    this.maxFontSize = 0;
	}

	/**
	 * Creates a text node or span from a symbol node. Note that a span is only
	 * created if it is needed.
	 */
	symbolNode.prototype.toNode = function() {
	    var node = document.createTextNode(this.value);
	    var span = null;

	    if (this.italic > 0) {
	        span = document.createElement("span");
	        span.style.marginRight = this.italic + "em";
	    }

	    if (this.classes.length > 0) {
	        span = span || document.createElement("span");
	        span.className = createClass(this.classes);
	    }

	    for (var style in this.style) {
	        if (this.style.hasOwnProperty(style)) {
	            span = span || document.createElement("span");
	            span.style[style] = this.style[style];
	        }
	    }

	    if (span) {
	        span.appendChild(node);
	        return span;
	    } else {
	        return node;
	    }
	};

	/**
	 * Creates markup for a symbol node.
	 */
	symbolNode.prototype.toMarkup = function() {
	    // TODO(alpert): More duplication than I'd like from
	    // span.prototype.toMarkup and symbolNode.prototype.toNode...
	    var needsSpan = false;

	    var markup = "<span";

	    if (this.classes.length) {
	        needsSpan = true;
	        markup += " class=\"";
	        markup += utils.escape(createClass(this.classes));
	        markup += "\"";
	    }

	    var styles = "";

	    if (this.italic > 0) {
	        styles += "margin-right:" + this.italic + "em;";
	    }
	    for (var style in this.style) {
	        if (this.style.hasOwnProperty(style)) {
	            styles += utils.hyphenate(style) + ":" + this.style[style] + ";";
	        }
	    }

	    if (styles) {
	        needsSpan = true;
	        markup += " style=\"" + utils.escape(styles) + "\"";
	    }

	    var escaped = utils.escape(this.value);
	    if (needsSpan) {
	        markup += ">";
	        markup += escaped;
	        markup += "</span>";
	        return markup;
	    } else {
	        return escaped;
	    }
	};

	module.exports = {
	    span: span,
	    documentFragment: documentFragment,
	    symbolNode: symbolNode,
	};


/***/ },
/* 269 */
/***/ function(module, exports) {

	/**
	 * This file contains a list of utility functions which are useful in other
	 * files.
	 */

	/**
	 * Provide an `indexOf` function which works in IE8, but defers to native if
	 * possible.
	 */
	var nativeIndexOf = Array.prototype.indexOf;
	var indexOf = function(list, elem) {
	    if (list == null) {
	        return -1;
	    }
	    if (nativeIndexOf && list.indexOf === nativeIndexOf) {
	        return list.indexOf(elem);
	    }
	    var i = 0;
	    var l = list.length;
	    for (; i < l; i++) {
	        if (list[i] === elem) {
	            return i;
	        }
	    }
	    return -1;
	};

	/**
	 * Return whether an element is contained in a list
	 */
	var contains = function(list, elem) {
	    return indexOf(list, elem) !== -1;
	};

	/**
	 * Provide a default value if a setting is undefined
	 */
	var deflt = function(setting, defaultIfUndefined) {
	    return setting === undefined ? defaultIfUndefined : setting;
	};

	// hyphenate and escape adapted from Facebook's React under Apache 2 license

	var uppercase = /([A-Z])/g;
	var hyphenate = function(str) {
	    return str.replace(uppercase, "-$1").toLowerCase();
	};

	var ESCAPE_LOOKUP = {
	    "&": "&amp;",
	    ">": "&gt;",
	    "<": "&lt;",
	    "\"": "&quot;",
	    "'": "&#x27;",
	};

	var ESCAPE_REGEX = /[&><"']/g;

	function escaper(match) {
	    return ESCAPE_LOOKUP[match];
	}

	/**
	 * Escapes text to prevent scripting attacks.
	 *
	 * @param {*} text Text value to escape.
	 * @return {string} An escaped string.
	 */
	function escape(text) {
	    return ("" + text).replace(ESCAPE_REGEX, escaper);
	}

	/**
	 * A function to set the text content of a DOM element in all supported
	 * browsers. Note that we don't define this if there is no document.
	 */
	var setTextContent;
	if (typeof document !== "undefined") {
	    var testNode = document.createElement("span");
	    if ("textContent" in testNode) {
	        setTextContent = function(node, text) {
	            node.textContent = text;
	        };
	    } else {
	        setTextContent = function(node, text) {
	            node.innerText = text;
	        };
	    }
	}

	/**
	 * A function to clear a node.
	 */
	function clearNode(node) {
	    setTextContent(node, "");
	}

	module.exports = {
	    contains: contains,
	    deflt: deflt,
	    escape: escape,
	    hyphenate: hyphenate,
	    indexOf: indexOf,
	    setTextContent: setTextContent,
	    clearNode: clearNode,
	};


/***/ },
/* 270 */
/***/ function(module, exports, __webpack_require__) {

	/* eslint no-unused-vars:0 */

	var Style = __webpack_require__(266);

	/**
	 * This file contains metrics regarding fonts and individual symbols. The sigma
	 * and xi variables, as well as the metricMap map contain data extracted from
	 * TeX, TeX font metrics, and the TTF files. These data are then exposed via the
	 * `metrics` variable and the getCharacterMetrics function.
	 */

	// These font metrics are extracted from TeX by using
	// \font\a=cmmi10
	// \showthe\fontdimenX\a
	// where X is the corresponding variable number. These correspond to the font
	// parameters of the symbol fonts. In TeX, there are actually three sets of
	// dimensions, one for each of textstyle, scriptstyle, and scriptscriptstyle,
	// but we only use the textstyle ones, and scale certain dimensions accordingly.
	// See the TeXbook, page 441.
	var sigma1 = 0.025;
	var sigma2 = 0;
	var sigma3 = 0;
	var sigma4 = 0;
	var sigma5 = 0.431;
	var sigma6 = 1;
	var sigma7 = 0;
	var sigma8 = 0.677;
	var sigma9 = 0.394;
	var sigma10 = 0.444;
	var sigma11 = 0.686;
	var sigma12 = 0.345;
	var sigma13 = 0.413;
	var sigma14 = 0.363;
	var sigma15 = 0.289;
	var sigma16 = 0.150;
	var sigma17 = 0.247;
	var sigma18 = 0.386;
	var sigma19 = 0.050;
	var sigma20 = 2.390;
	var sigma21 = 1.01;
	var sigma21Script = 0.81;
	var sigma21ScriptScript = 0.71;
	var sigma22 = 0.250;

	// These font metrics are extracted from TeX by using
	// \font\a=cmex10
	// \showthe\fontdimenX\a
	// where X is the corresponding variable number. These correspond to the font
	// parameters of the extension fonts (family 3). See the TeXbook, page 441.
	var xi1 = 0;
	var xi2 = 0;
	var xi3 = 0;
	var xi4 = 0;
	var xi5 = 0.431;
	var xi6 = 1;
	var xi7 = 0;
	var xi8 = 0.04;
	var xi9 = 0.111;
	var xi10 = 0.166;
	var xi11 = 0.2;
	var xi12 = 0.6;
	var xi13 = 0.1;

	// This value determines how large a pt is, for metrics which are defined in
	// terms of pts.
	// This value is also used in katex.less; if you change it make sure the values
	// match.
	var ptPerEm = 10.0;

	// The space between adjacent `|` columns in an array definition. From
	// `\showthe\doublerulesep` in LaTeX.
	var doubleRuleSep = 2.0 / ptPerEm;

	/**
	 * This is just a mapping from common names to real metrics
	 */
	var metrics = {
	    xHeight: sigma5,
	    quad: sigma6,
	    num1: sigma8,
	    num2: sigma9,
	    num3: sigma10,
	    denom1: sigma11,
	    denom2: sigma12,
	    sup1: sigma13,
	    sup2: sigma14,
	    sup3: sigma15,
	    sub1: sigma16,
	    sub2: sigma17,
	    supDrop: sigma18,
	    subDrop: sigma19,
	    axisHeight: sigma22,
	    defaultRuleThickness: xi8,
	    bigOpSpacing1: xi9,
	    bigOpSpacing2: xi10,
	    bigOpSpacing3: xi11,
	    bigOpSpacing4: xi12,
	    bigOpSpacing5: xi13,
	    ptPerEm: ptPerEm,
	    emPerEx: sigma5 / sigma6,
	    doubleRuleSep: doubleRuleSep,

	    // TODO(alpert): Missing parallel structure here. We should probably add
	    // style-specific metrics for all of these.
	    delim1: sigma20,
	    getDelim2: function(style) {
	        if (style.size === Style.TEXT.size) {
	            return sigma21;
	        } else if (style.size === Style.SCRIPT.size) {
	            return sigma21Script;
	        } else if (style.size === Style.SCRIPTSCRIPT.size) {
	            return sigma21ScriptScript;
	        }
	        throw new Error("Unexpected style size: " + style.size);
	    },
	};

	// This map contains a mapping from font name and character code to character
	// metrics, including height, depth, italic correction, and skew (kern from the
	// character to the corresponding \skewchar)
	// This map is generated via `make metrics`. It should not be changed manually.
	var metricMap = __webpack_require__(271);

	/**
	 * This function is a convenience function for looking up information in the
	 * metricMap table. It takes a character as a string, and a style.
	 *
	 * Note: the `width` property may be undefined if fontMetricsData.js wasn't
	 * built using `Make extended_metrics`.
	 */
	var getCharacterMetrics = function(character, style) {
	    var metrics = metricMap[style][character.charCodeAt(0)];
	    if (metrics) {
	        return {
	            depth: metrics[0],
	            height: metrics[1],
	            italic: metrics[2],
	            skew: metrics[3],
	            width: metrics[4],
	        };
	    }
	};

	module.exports = {
	    metrics: metrics,
	    getCharacterMetrics: getCharacterMetrics,
	};


/***/ },
/* 271 */
/***/ function(module, exports) {

	module.exports = {
	    "AMS-Regular": {
	        "65": [0, 0.68889, 0, 0],
	        "66": [0, 0.68889, 0, 0],
	        "67": [0, 0.68889, 0, 0],
	        "68": [0, 0.68889, 0, 0],
	        "69": [0, 0.68889, 0, 0],
	        "70": [0, 0.68889, 0, 0],
	        "71": [0, 0.68889, 0, 0],
	        "72": [0, 0.68889, 0, 0],
	        "73": [0, 0.68889, 0, 0],
	        "74": [0.16667, 0.68889, 0, 0],
	        "75": [0, 0.68889, 0, 0],
	        "76": [0, 0.68889, 0, 0],
	        "77": [0, 0.68889, 0, 0],
	        "78": [0, 0.68889, 0, 0],
	        "79": [0.16667, 0.68889, 0, 0],
	        "80": [0, 0.68889, 0, 0],
	        "81": [0.16667, 0.68889, 0, 0],
	        "82": [0, 0.68889, 0, 0],
	        "83": [0, 0.68889, 0, 0],
	        "84": [0, 0.68889, 0, 0],
	        "85": [0, 0.68889, 0, 0],
	        "86": [0, 0.68889, 0, 0],
	        "87": [0, 0.68889, 0, 0],
	        "88": [0, 0.68889, 0, 0],
	        "89": [0, 0.68889, 0, 0],
	        "90": [0, 0.68889, 0, 0],
	        "107": [0, 0.68889, 0, 0],
	        "165": [0, 0.675, 0.025, 0],
	        "174": [0.15559, 0.69224, 0, 0],
	        "240": [0, 0.68889, 0, 0],
	        "295": [0, 0.68889, 0, 0],
	        "710": [0, 0.825, 0, 0],
	        "732": [0, 0.9, 0, 0],
	        "770": [0, 0.825, 0, 0],
	        "771": [0, 0.9, 0, 0],
	        "989": [0.08167, 0.58167, 0, 0],
	        "1008": [0, 0.43056, 0.04028, 0],
	        "8245": [0, 0.54986, 0, 0],
	        "8463": [0, 0.68889, 0, 0],
	        "8487": [0, 0.68889, 0, 0],
	        "8498": [0, 0.68889, 0, 0],
	        "8502": [0, 0.68889, 0, 0],
	        "8503": [0, 0.68889, 0, 0],
	        "8504": [0, 0.68889, 0, 0],
	        "8513": [0, 0.68889, 0, 0],
	        "8592": [-0.03598, 0.46402, 0, 0],
	        "8594": [-0.03598, 0.46402, 0, 0],
	        "8602": [-0.13313, 0.36687, 0, 0],
	        "8603": [-0.13313, 0.36687, 0, 0],
	        "8606": [0.01354, 0.52239, 0, 0],
	        "8608": [0.01354, 0.52239, 0, 0],
	        "8610": [0.01354, 0.52239, 0, 0],
	        "8611": [0.01354, 0.52239, 0, 0],
	        "8619": [0, 0.54986, 0, 0],
	        "8620": [0, 0.54986, 0, 0],
	        "8621": [-0.13313, 0.37788, 0, 0],
	        "8622": [-0.13313, 0.36687, 0, 0],
	        "8624": [0, 0.69224, 0, 0],
	        "8625": [0, 0.69224, 0, 0],
	        "8630": [0, 0.43056, 0, 0],
	        "8631": [0, 0.43056, 0, 0],
	        "8634": [0.08198, 0.58198, 0, 0],
	        "8635": [0.08198, 0.58198, 0, 0],
	        "8638": [0.19444, 0.69224, 0, 0],
	        "8639": [0.19444, 0.69224, 0, 0],
	        "8642": [0.19444, 0.69224, 0, 0],
	        "8643": [0.19444, 0.69224, 0, 0],
	        "8644": [0.1808, 0.675, 0, 0],
	        "8646": [0.1808, 0.675, 0, 0],
	        "8647": [0.1808, 0.675, 0, 0],
	        "8648": [0.19444, 0.69224, 0, 0],
	        "8649": [0.1808, 0.675, 0, 0],
	        "8650": [0.19444, 0.69224, 0, 0],
	        "8651": [0.01354, 0.52239, 0, 0],
	        "8652": [0.01354, 0.52239, 0, 0],
	        "8653": [-0.13313, 0.36687, 0, 0],
	        "8654": [-0.13313, 0.36687, 0, 0],
	        "8655": [-0.13313, 0.36687, 0, 0],
	        "8666": [0.13667, 0.63667, 0, 0],
	        "8667": [0.13667, 0.63667, 0, 0],
	        "8669": [-0.13313, 0.37788, 0, 0],
	        "8672": [-0.064, 0.437, 0, 0],
	        "8674": [-0.064, 0.437, 0, 0],
	        "8705": [0, 0.825, 0, 0],
	        "8708": [0, 0.68889, 0, 0],
	        "8709": [0.08167, 0.58167, 0, 0],
	        "8717": [0, 0.43056, 0, 0],
	        "8722": [-0.03598, 0.46402, 0, 0],
	        "8724": [0.08198, 0.69224, 0, 0],
	        "8726": [0.08167, 0.58167, 0, 0],
	        "8733": [0, 0.69224, 0, 0],
	        "8736": [0, 0.69224, 0, 0],
	        "8737": [0, 0.69224, 0, 0],
	        "8738": [0.03517, 0.52239, 0, 0],
	        "8739": [0.08167, 0.58167, 0, 0],
	        "8740": [0.25142, 0.74111, 0, 0],
	        "8741": [0.08167, 0.58167, 0, 0],
	        "8742": [0.25142, 0.74111, 0, 0],
	        "8756": [0, 0.69224, 0, 0],
	        "8757": [0, 0.69224, 0, 0],
	        "8764": [-0.13313, 0.36687, 0, 0],
	        "8765": [-0.13313, 0.37788, 0, 0],
	        "8769": [-0.13313, 0.36687, 0, 0],
	        "8770": [-0.03625, 0.46375, 0, 0],
	        "8774": [0.30274, 0.79383, 0, 0],
	        "8776": [-0.01688, 0.48312, 0, 0],
	        "8778": [0.08167, 0.58167, 0, 0],
	        "8782": [0.06062, 0.54986, 0, 0],
	        "8783": [0.06062, 0.54986, 0, 0],
	        "8785": [0.08198, 0.58198, 0, 0],
	        "8786": [0.08198, 0.58198, 0, 0],
	        "8787": [0.08198, 0.58198, 0, 0],
	        "8790": [0, 0.69224, 0, 0],
	        "8791": [0.22958, 0.72958, 0, 0],
	        "8796": [0.08198, 0.91667, 0, 0],
	        "8806": [0.25583, 0.75583, 0, 0],
	        "8807": [0.25583, 0.75583, 0, 0],
	        "8808": [0.25142, 0.75726, 0, 0],
	        "8809": [0.25142, 0.75726, 0, 0],
	        "8812": [0.25583, 0.75583, 0, 0],
	        "8814": [0.20576, 0.70576, 0, 0],
	        "8815": [0.20576, 0.70576, 0, 0],
	        "8816": [0.30274, 0.79383, 0, 0],
	        "8817": [0.30274, 0.79383, 0, 0],
	        "8818": [0.22958, 0.72958, 0, 0],
	        "8819": [0.22958, 0.72958, 0, 0],
	        "8822": [0.1808, 0.675, 0, 0],
	        "8823": [0.1808, 0.675, 0, 0],
	        "8828": [0.13667, 0.63667, 0, 0],
	        "8829": [0.13667, 0.63667, 0, 0],
	        "8830": [0.22958, 0.72958, 0, 0],
	        "8831": [0.22958, 0.72958, 0, 0],
	        "8832": [0.20576, 0.70576, 0, 0],
	        "8833": [0.20576, 0.70576, 0, 0],
	        "8840": [0.30274, 0.79383, 0, 0],
	        "8841": [0.30274, 0.79383, 0, 0],
	        "8842": [0.13597, 0.63597, 0, 0],
	        "8843": [0.13597, 0.63597, 0, 0],
	        "8847": [0.03517, 0.54986, 0, 0],
	        "8848": [0.03517, 0.54986, 0, 0],
	        "8858": [0.08198, 0.58198, 0, 0],
	        "8859": [0.08198, 0.58198, 0, 0],
	        "8861": [0.08198, 0.58198, 0, 0],
	        "8862": [0, 0.675, 0, 0],
	        "8863": [0, 0.675, 0, 0],
	        "8864": [0, 0.675, 0, 0],
	        "8865": [0, 0.675, 0, 0],
	        "8872": [0, 0.69224, 0, 0],
	        "8873": [0, 0.69224, 0, 0],
	        "8874": [0, 0.69224, 0, 0],
	        "8876": [0, 0.68889, 0, 0],
	        "8877": [0, 0.68889, 0, 0],
	        "8878": [0, 0.68889, 0, 0],
	        "8879": [0, 0.68889, 0, 0],
	        "8882": [0.03517, 0.54986, 0, 0],
	        "8883": [0.03517, 0.54986, 0, 0],
	        "8884": [0.13667, 0.63667, 0, 0],
	        "8885": [0.13667, 0.63667, 0, 0],
	        "8888": [0, 0.54986, 0, 0],
	        "8890": [0.19444, 0.43056, 0, 0],
	        "8891": [0.19444, 0.69224, 0, 0],
	        "8892": [0.19444, 0.69224, 0, 0],
	        "8901": [0, 0.54986, 0, 0],
	        "8903": [0.08167, 0.58167, 0, 0],
	        "8905": [0.08167, 0.58167, 0, 0],
	        "8906": [0.08167, 0.58167, 0, 0],
	        "8907": [0, 0.69224, 0, 0],
	        "8908": [0, 0.69224, 0, 0],
	        "8909": [-0.03598, 0.46402, 0, 0],
	        "8910": [0, 0.54986, 0, 0],
	        "8911": [0, 0.54986, 0, 0],
	        "8912": [0.03517, 0.54986, 0, 0],
	        "8913": [0.03517, 0.54986, 0, 0],
	        "8914": [0, 0.54986, 0, 0],
	        "8915": [0, 0.54986, 0, 0],
	        "8916": [0, 0.69224, 0, 0],
	        "8918": [0.0391, 0.5391, 0, 0],
	        "8919": [0.0391, 0.5391, 0, 0],
	        "8920": [0.03517, 0.54986, 0, 0],
	        "8921": [0.03517, 0.54986, 0, 0],
	        "8922": [0.38569, 0.88569, 0, 0],
	        "8923": [0.38569, 0.88569, 0, 0],
	        "8926": [0.13667, 0.63667, 0, 0],
	        "8927": [0.13667, 0.63667, 0, 0],
	        "8928": [0.30274, 0.79383, 0, 0],
	        "8929": [0.30274, 0.79383, 0, 0],
	        "8934": [0.23222, 0.74111, 0, 0],
	        "8935": [0.23222, 0.74111, 0, 0],
	        "8936": [0.23222, 0.74111, 0, 0],
	        "8937": [0.23222, 0.74111, 0, 0],
	        "8938": [0.20576, 0.70576, 0, 0],
	        "8939": [0.20576, 0.70576, 0, 0],
	        "8940": [0.30274, 0.79383, 0, 0],
	        "8941": [0.30274, 0.79383, 0, 0],
	        "8994": [0.19444, 0.69224, 0, 0],
	        "8995": [0.19444, 0.69224, 0, 0],
	        "9416": [0.15559, 0.69224, 0, 0],
	        "9484": [0, 0.69224, 0, 0],
	        "9488": [0, 0.69224, 0, 0],
	        "9492": [0, 0.37788, 0, 0],
	        "9496": [0, 0.37788, 0, 0],
	        "9585": [0.19444, 0.68889, 0, 0],
	        "9586": [0.19444, 0.74111, 0, 0],
	        "9632": [0, 0.675, 0, 0],
	        "9633": [0, 0.675, 0, 0],
	        "9650": [0, 0.54986, 0, 0],
	        "9651": [0, 0.54986, 0, 0],
	        "9654": [0.03517, 0.54986, 0, 0],
	        "9660": [0, 0.54986, 0, 0],
	        "9661": [0, 0.54986, 0, 0],
	        "9664": [0.03517, 0.54986, 0, 0],
	        "9674": [0.11111, 0.69224, 0, 0],
	        "9733": [0.19444, 0.69224, 0, 0],
	        "10003": [0, 0.69224, 0, 0],
	        "10016": [0, 0.69224, 0, 0],
	        "10731": [0.11111, 0.69224, 0, 0],
	        "10846": [0.19444, 0.75583, 0, 0],
	        "10877": [0.13667, 0.63667, 0, 0],
	        "10878": [0.13667, 0.63667, 0, 0],
	        "10885": [0.25583, 0.75583, 0, 0],
	        "10886": [0.25583, 0.75583, 0, 0],
	        "10887": [0.13597, 0.63597, 0, 0],
	        "10888": [0.13597, 0.63597, 0, 0],
	        "10889": [0.26167, 0.75726, 0, 0],
	        "10890": [0.26167, 0.75726, 0, 0],
	        "10891": [0.48256, 0.98256, 0, 0],
	        "10892": [0.48256, 0.98256, 0, 0],
	        "10901": [0.13667, 0.63667, 0, 0],
	        "10902": [0.13667, 0.63667, 0, 0],
	        "10933": [0.25142, 0.75726, 0, 0],
	        "10934": [0.25142, 0.75726, 0, 0],
	        "10935": [0.26167, 0.75726, 0, 0],
	        "10936": [0.26167, 0.75726, 0, 0],
	        "10937": [0.26167, 0.75726, 0, 0],
	        "10938": [0.26167, 0.75726, 0, 0],
	        "10949": [0.25583, 0.75583, 0, 0],
	        "10950": [0.25583, 0.75583, 0, 0],
	        "10955": [0.28481, 0.79383, 0, 0],
	        "10956": [0.28481, 0.79383, 0, 0],
	        "57350": [0.08167, 0.58167, 0, 0],
	        "57351": [0.08167, 0.58167, 0, 0],
	        "57352": [0.08167, 0.58167, 0, 0],
	        "57353": [0, 0.43056, 0.04028, 0],
	        "57356": [0.25142, 0.75726, 0, 0],
	        "57357": [0.25142, 0.75726, 0, 0],
	        "57358": [0.41951, 0.91951, 0, 0],
	        "57359": [0.30274, 0.79383, 0, 0],
	        "57360": [0.30274, 0.79383, 0, 0],
	        "57361": [0.41951, 0.91951, 0, 0],
	        "57366": [0.25142, 0.75726, 0, 0],
	        "57367": [0.25142, 0.75726, 0, 0],
	        "57368": [0.25142, 0.75726, 0, 0],
	        "57369": [0.25142, 0.75726, 0, 0],
	        "57370": [0.13597, 0.63597, 0, 0],
	        "57371": [0.13597, 0.63597, 0, 0],
	    },
	    "Caligraphic-Regular": {
	        "48": [0, 0.43056, 0, 0],
	        "49": [0, 0.43056, 0, 0],
	        "50": [0, 0.43056, 0, 0],
	        "51": [0.19444, 0.43056, 0, 0],
	        "52": [0.19444, 0.43056, 0, 0],
	        "53": [0.19444, 0.43056, 0, 0],
	        "54": [0, 0.64444, 0, 0],
	        "55": [0.19444, 0.43056, 0, 0],
	        "56": [0, 0.64444, 0, 0],
	        "57": [0.19444, 0.43056, 0, 0],
	        "65": [0, 0.68333, 0, 0.19445],
	        "66": [0, 0.68333, 0.03041, 0.13889],
	        "67": [0, 0.68333, 0.05834, 0.13889],
	        "68": [0, 0.68333, 0.02778, 0.08334],
	        "69": [0, 0.68333, 0.08944, 0.11111],
	        "70": [0, 0.68333, 0.09931, 0.11111],
	        "71": [0.09722, 0.68333, 0.0593, 0.11111],
	        "72": [0, 0.68333, 0.00965, 0.11111],
	        "73": [0, 0.68333, 0.07382, 0],
	        "74": [0.09722, 0.68333, 0.18472, 0.16667],
	        "75": [0, 0.68333, 0.01445, 0.05556],
	        "76": [0, 0.68333, 0, 0.13889],
	        "77": [0, 0.68333, 0, 0.13889],
	        "78": [0, 0.68333, 0.14736, 0.08334],
	        "79": [0, 0.68333, 0.02778, 0.11111],
	        "80": [0, 0.68333, 0.08222, 0.08334],
	        "81": [0.09722, 0.68333, 0, 0.11111],
	        "82": [0, 0.68333, 0, 0.08334],
	        "83": [0, 0.68333, 0.075, 0.13889],
	        "84": [0, 0.68333, 0.25417, 0],
	        "85": [0, 0.68333, 0.09931, 0.08334],
	        "86": [0, 0.68333, 0.08222, 0],
	        "87": [0, 0.68333, 0.08222, 0.08334],
	        "88": [0, 0.68333, 0.14643, 0.13889],
	        "89": [0.09722, 0.68333, 0.08222, 0.08334],
	        "90": [0, 0.68333, 0.07944, 0.13889],
	    },
	    "Fraktur-Regular": {
	        "33": [0, 0.69141, 0, 0],
	        "34": [0, 0.69141, 0, 0],
	        "38": [0, 0.69141, 0, 0],
	        "39": [0, 0.69141, 0, 0],
	        "40": [0.24982, 0.74947, 0, 0],
	        "41": [0.24982, 0.74947, 0, 0],
	        "42": [0, 0.62119, 0, 0],
	        "43": [0.08319, 0.58283, 0, 0],
	        "44": [0, 0.10803, 0, 0],
	        "45": [0.08319, 0.58283, 0, 0],
	        "46": [0, 0.10803, 0, 0],
	        "47": [0.24982, 0.74947, 0, 0],
	        "48": [0, 0.47534, 0, 0],
	        "49": [0, 0.47534, 0, 0],
	        "50": [0, 0.47534, 0, 0],
	        "51": [0.18906, 0.47534, 0, 0],
	        "52": [0.18906, 0.47534, 0, 0],
	        "53": [0.18906, 0.47534, 0, 0],
	        "54": [0, 0.69141, 0, 0],
	        "55": [0.18906, 0.47534, 0, 0],
	        "56": [0, 0.69141, 0, 0],
	        "57": [0.18906, 0.47534, 0, 0],
	        "58": [0, 0.47534, 0, 0],
	        "59": [0.12604, 0.47534, 0, 0],
	        "61": [-0.13099, 0.36866, 0, 0],
	        "63": [0, 0.69141, 0, 0],
	        "65": [0, 0.69141, 0, 0],
	        "66": [0, 0.69141, 0, 0],
	        "67": [0, 0.69141, 0, 0],
	        "68": [0, 0.69141, 0, 0],
	        "69": [0, 0.69141, 0, 0],
	        "70": [0.12604, 0.69141, 0, 0],
	        "71": [0, 0.69141, 0, 0],
	        "72": [0.06302, 0.69141, 0, 0],
	        "73": [0, 0.69141, 0, 0],
	        "74": [0.12604, 0.69141, 0, 0],
	        "75": [0, 0.69141, 0, 0],
	        "76": [0, 0.69141, 0, 0],
	        "77": [0, 0.69141, 0, 0],
	        "78": [0, 0.69141, 0, 0],
	        "79": [0, 0.69141, 0, 0],
	        "80": [0.18906, 0.69141, 0, 0],
	        "81": [0.03781, 0.69141, 0, 0],
	        "82": [0, 0.69141, 0, 0],
	        "83": [0, 0.69141, 0, 0],
	        "84": [0, 0.69141, 0, 0],
	        "85": [0, 0.69141, 0, 0],
	        "86": [0, 0.69141, 0, 0],
	        "87": [0, 0.69141, 0, 0],
	        "88": [0, 0.69141, 0, 0],
	        "89": [0.18906, 0.69141, 0, 0],
	        "90": [0.12604, 0.69141, 0, 0],
	        "91": [0.24982, 0.74947, 0, 0],
	        "93": [0.24982, 0.74947, 0, 0],
	        "94": [0, 0.69141, 0, 0],
	        "97": [0, 0.47534, 0, 0],
	        "98": [0, 0.69141, 0, 0],
	        "99": [0, 0.47534, 0, 0],
	        "100": [0, 0.62119, 0, 0],
	        "101": [0, 0.47534, 0, 0],
	        "102": [0.18906, 0.69141, 0, 0],
	        "103": [0.18906, 0.47534, 0, 0],
	        "104": [0.18906, 0.69141, 0, 0],
	        "105": [0, 0.69141, 0, 0],
	        "106": [0, 0.69141, 0, 0],
	        "107": [0, 0.69141, 0, 0],
	        "108": [0, 0.69141, 0, 0],
	        "109": [0, 0.47534, 0, 0],
	        "110": [0, 0.47534, 0, 0],
	        "111": [0, 0.47534, 0, 0],
	        "112": [0.18906, 0.52396, 0, 0],
	        "113": [0.18906, 0.47534, 0, 0],
	        "114": [0, 0.47534, 0, 0],
	        "115": [0, 0.47534, 0, 0],
	        "116": [0, 0.62119, 0, 0],
	        "117": [0, 0.47534, 0, 0],
	        "118": [0, 0.52396, 0, 0],
	        "119": [0, 0.52396, 0, 0],
	        "120": [0.18906, 0.47534, 0, 0],
	        "121": [0.18906, 0.47534, 0, 0],
	        "122": [0.18906, 0.47534, 0, 0],
	        "8216": [0, 0.69141, 0, 0],
	        "8217": [0, 0.69141, 0, 0],
	        "58112": [0, 0.62119, 0, 0],
	        "58113": [0, 0.62119, 0, 0],
	        "58114": [0.18906, 0.69141, 0, 0],
	        "58115": [0.18906, 0.69141, 0, 0],
	        "58116": [0.18906, 0.47534, 0, 0],
	        "58117": [0, 0.69141, 0, 0],
	        "58118": [0, 0.62119, 0, 0],
	        "58119": [0, 0.47534, 0, 0],
	    },
	    "Main-Bold": {
	        "33": [0, 0.69444, 0, 0],
	        "34": [0, 0.69444, 0, 0],
	        "35": [0.19444, 0.69444, 0, 0],
	        "36": [0.05556, 0.75, 0, 0],
	        "37": [0.05556, 0.75, 0, 0],
	        "38": [0, 0.69444, 0, 0],
	        "39": [0, 0.69444, 0, 0],
	        "40": [0.25, 0.75, 0, 0],
	        "41": [0.25, 0.75, 0, 0],
	        "42": [0, 0.75, 0, 0],
	        "43": [0.13333, 0.63333, 0, 0],
	        "44": [0.19444, 0.15556, 0, 0],
	        "45": [0, 0.44444, 0, 0],
	        "46": [0, 0.15556, 0, 0],
	        "47": [0.25, 0.75, 0, 0],
	        "48": [0, 0.64444, 0, 0],
	        "49": [0, 0.64444, 0, 0],
	        "50": [0, 0.64444, 0, 0],
	        "51": [0, 0.64444, 0, 0],
	        "52": [0, 0.64444, 0, 0],
	        "53": [0, 0.64444, 0, 0],
	        "54": [0, 0.64444, 0, 0],
	        "55": [0, 0.64444, 0, 0],
	        "56": [0, 0.64444, 0, 0],
	        "57": [0, 0.64444, 0, 0],
	        "58": [0, 0.44444, 0, 0],
	        "59": [0.19444, 0.44444, 0, 0],
	        "60": [0.08556, 0.58556, 0, 0],
	        "61": [-0.10889, 0.39111, 0, 0],
	        "62": [0.08556, 0.58556, 0, 0],
	        "63": [0, 0.69444, 0, 0],
	        "64": [0, 0.69444, 0, 0],
	        "65": [0, 0.68611, 0, 0],
	        "66": [0, 0.68611, 0, 0],
	        "67": [0, 0.68611, 0, 0],
	        "68": [0, 0.68611, 0, 0],
	        "69": [0, 0.68611, 0, 0],
	        "70": [0, 0.68611, 0, 0],
	        "71": [0, 0.68611, 0, 0],
	        "72": [0, 0.68611, 0, 0],
	        "73": [0, 0.68611, 0, 0],
	        "74": [0, 0.68611, 0, 0],
	        "75": [0, 0.68611, 0, 0],
	        "76": [0, 0.68611, 0, 0],
	        "77": [0, 0.68611, 0, 0],
	        "78": [0, 0.68611, 0, 0],
	        "79": [0, 0.68611, 0, 0],
	        "80": [0, 0.68611, 0, 0],
	        "81": [0.19444, 0.68611, 0, 0],
	        "82": [0, 0.68611, 0, 0],
	        "83": [0, 0.68611, 0, 0],
	        "84": [0, 0.68611, 0, 0],
	        "85": [0, 0.68611, 0, 0],
	        "86": [0, 0.68611, 0.01597, 0],
	        "87": [0, 0.68611, 0.01597, 0],
	        "88": [0, 0.68611, 0, 0],
	        "89": [0, 0.68611, 0.02875, 0],
	        "90": [0, 0.68611, 0, 0],
	        "91": [0.25, 0.75, 0, 0],
	        "92": [0.25, 0.75, 0, 0],
	        "93": [0.25, 0.75, 0, 0],
	        "94": [0, 0.69444, 0, 0],
	        "95": [0.31, 0.13444, 0.03194, 0],
	        "96": [0, 0.69444, 0, 0],
	        "97": [0, 0.44444, 0, 0],
	        "98": [0, 0.69444, 0, 0],
	        "99": [0, 0.44444, 0, 0],
	        "100": [0, 0.69444, 0, 0],
	        "101": [0, 0.44444, 0, 0],
	        "102": [0, 0.69444, 0.10903, 0],
	        "103": [0.19444, 0.44444, 0.01597, 0],
	        "104": [0, 0.69444, 0, 0],
	        "105": [0, 0.69444, 0, 0],
	        "106": [0.19444, 0.69444, 0, 0],
	        "107": [0, 0.69444, 0, 0],
	        "108": [0, 0.69444, 0, 0],
	        "109": [0, 0.44444, 0, 0],
	        "110": [0, 0.44444, 0, 0],
	        "111": [0, 0.44444, 0, 0],
	        "112": [0.19444, 0.44444, 0, 0],
	        "113": [0.19444, 0.44444, 0, 0],
	        "114": [0, 0.44444, 0, 0],
	        "115": [0, 0.44444, 0, 0],
	        "116": [0, 0.63492, 0, 0],
	        "117": [0, 0.44444, 0, 0],
	        "118": [0, 0.44444, 0.01597, 0],
	        "119": [0, 0.44444, 0.01597, 0],
	        "120": [0, 0.44444, 0, 0],
	        "121": [0.19444, 0.44444, 0.01597, 0],
	        "122": [0, 0.44444, 0, 0],
	        "123": [0.25, 0.75, 0, 0],
	        "124": [0.25, 0.75, 0, 0],
	        "125": [0.25, 0.75, 0, 0],
	        "126": [0.35, 0.34444, 0, 0],
	        "168": [0, 0.69444, 0, 0],
	        "172": [0, 0.44444, 0, 0],
	        "175": [0, 0.59611, 0, 0],
	        "176": [0, 0.69444, 0, 0],
	        "177": [0.13333, 0.63333, 0, 0],
	        "180": [0, 0.69444, 0, 0],
	        "215": [0.13333, 0.63333, 0, 0],
	        "247": [0.13333, 0.63333, 0, 0],
	        "305": [0, 0.44444, 0, 0],
	        "567": [0.19444, 0.44444, 0, 0],
	        "710": [0, 0.69444, 0, 0],
	        "711": [0, 0.63194, 0, 0],
	        "713": [0, 0.59611, 0, 0],
	        "714": [0, 0.69444, 0, 0],
	        "715": [0, 0.69444, 0, 0],
	        "728": [0, 0.69444, 0, 0],
	        "729": [0, 0.69444, 0, 0],
	        "730": [0, 0.69444, 0, 0],
	        "732": [0, 0.69444, 0, 0],
	        "768": [0, 0.69444, 0, 0],
	        "769": [0, 0.69444, 0, 0],
	        "770": [0, 0.69444, 0, 0],
	        "771": [0, 0.69444, 0, 0],
	        "772": [0, 0.59611, 0, 0],
	        "774": [0, 0.69444, 0, 0],
	        "775": [0, 0.69444, 0, 0],
	        "776": [0, 0.69444, 0, 0],
	        "778": [0, 0.69444, 0, 0],
	        "779": [0, 0.69444, 0, 0],
	        "780": [0, 0.63194, 0, 0],
	        "824": [0.19444, 0.69444, 0, 0],
	        "915": [0, 0.68611, 0, 0],
	        "916": [0, 0.68611, 0, 0],
	        "920": [0, 0.68611, 0, 0],
	        "923": [0, 0.68611, 0, 0],
	        "926": [0, 0.68611, 0, 0],
	        "928": [0, 0.68611, 0, 0],
	        "931": [0, 0.68611, 0, 0],
	        "933": [0, 0.68611, 0, 0],
	        "934": [0, 0.68611, 0, 0],
	        "936": [0, 0.68611, 0, 0],
	        "937": [0, 0.68611, 0, 0],
	        "8211": [0, 0.44444, 0.03194, 0],
	        "8212": [0, 0.44444, 0.03194, 0],
	        "8216": [0, 0.69444, 0, 0],
	        "8217": [0, 0.69444, 0, 0],
	        "8220": [0, 0.69444, 0, 0],
	        "8221": [0, 0.69444, 0, 0],
	        "8224": [0.19444, 0.69444, 0, 0],
	        "8225": [0.19444, 0.69444, 0, 0],
	        "8242": [0, 0.55556, 0, 0],
	        "8407": [0, 0.72444, 0.15486, 0],
	        "8463": [0, 0.69444, 0, 0],
	        "8465": [0, 0.69444, 0, 0],
	        "8467": [0, 0.69444, 0, 0],
	        "8472": [0.19444, 0.44444, 0, 0],
	        "8476": [0, 0.69444, 0, 0],
	        "8501": [0, 0.69444, 0, 0],
	        "8592": [-0.10889, 0.39111, 0, 0],
	        "8593": [0.19444, 0.69444, 0, 0],
	        "8594": [-0.10889, 0.39111, 0, 0],
	        "8595": [0.19444, 0.69444, 0, 0],
	        "8596": [-0.10889, 0.39111, 0, 0],
	        "8597": [0.25, 0.75, 0, 0],
	        "8598": [0.19444, 0.69444, 0, 0],
	        "8599": [0.19444, 0.69444, 0, 0],
	        "8600": [0.19444, 0.69444, 0, 0],
	        "8601": [0.19444, 0.69444, 0, 0],
	        "8636": [-0.10889, 0.39111, 0, 0],
	        "8637": [-0.10889, 0.39111, 0, 0],
	        "8640": [-0.10889, 0.39111, 0, 0],
	        "8641": [-0.10889, 0.39111, 0, 0],
	        "8656": [-0.10889, 0.39111, 0, 0],
	        "8657": [0.19444, 0.69444, 0, 0],
	        "8658": [-0.10889, 0.39111, 0, 0],
	        "8659": [0.19444, 0.69444, 0, 0],
	        "8660": [-0.10889, 0.39111, 0, 0],
	        "8661": [0.25, 0.75, 0, 0],
	        "8704": [0, 0.69444, 0, 0],
	        "8706": [0, 0.69444, 0.06389, 0],
	        "8707": [0, 0.69444, 0, 0],
	        "8709": [0.05556, 0.75, 0, 0],
	        "8711": [0, 0.68611, 0, 0],
	        "8712": [0.08556, 0.58556, 0, 0],
	        "8715": [0.08556, 0.58556, 0, 0],
	        "8722": [0.13333, 0.63333, 0, 0],
	        "8723": [0.13333, 0.63333, 0, 0],
	        "8725": [0.25, 0.75, 0, 0],
	        "8726": [0.25, 0.75, 0, 0],
	        "8727": [-0.02778, 0.47222, 0, 0],
	        "8728": [-0.02639, 0.47361, 0, 0],
	        "8729": [-0.02639, 0.47361, 0, 0],
	        "8730": [0.18, 0.82, 0, 0],
	        "8733": [0, 0.44444, 0, 0],
	        "8734": [0, 0.44444, 0, 0],
	        "8736": [0, 0.69224, 0, 0],
	        "8739": [0.25, 0.75, 0, 0],
	        "8741": [0.25, 0.75, 0, 0],
	        "8743": [0, 0.55556, 0, 0],
	        "8744": [0, 0.55556, 0, 0],
	        "8745": [0, 0.55556, 0, 0],
	        "8746": [0, 0.55556, 0, 0],
	        "8747": [0.19444, 0.69444, 0.12778, 0],
	        "8764": [-0.10889, 0.39111, 0, 0],
	        "8768": [0.19444, 0.69444, 0, 0],
	        "8771": [0.00222, 0.50222, 0, 0],
	        "8776": [0.02444, 0.52444, 0, 0],
	        "8781": [0.00222, 0.50222, 0, 0],
	        "8801": [0.00222, 0.50222, 0, 0],
	        "8804": [0.19667, 0.69667, 0, 0],
	        "8805": [0.19667, 0.69667, 0, 0],
	        "8810": [0.08556, 0.58556, 0, 0],
	        "8811": [0.08556, 0.58556, 0, 0],
	        "8826": [0.08556, 0.58556, 0, 0],
	        "8827": [0.08556, 0.58556, 0, 0],
	        "8834": [0.08556, 0.58556, 0, 0],
	        "8835": [0.08556, 0.58556, 0, 0],
	        "8838": [0.19667, 0.69667, 0, 0],
	        "8839": [0.19667, 0.69667, 0, 0],
	        "8846": [0, 0.55556, 0, 0],
	        "8849": [0.19667, 0.69667, 0, 0],
	        "8850": [0.19667, 0.69667, 0, 0],
	        "8851": [0, 0.55556, 0, 0],
	        "8852": [0, 0.55556, 0, 0],
	        "8853": [0.13333, 0.63333, 0, 0],
	        "8854": [0.13333, 0.63333, 0, 0],
	        "8855": [0.13333, 0.63333, 0, 0],
	        "8856": [0.13333, 0.63333, 0, 0],
	        "8857": [0.13333, 0.63333, 0, 0],
	        "8866": [0, 0.69444, 0, 0],
	        "8867": [0, 0.69444, 0, 0],
	        "8868": [0, 0.69444, 0, 0],
	        "8869": [0, 0.69444, 0, 0],
	        "8900": [-0.02639, 0.47361, 0, 0],
	        "8901": [-0.02639, 0.47361, 0, 0],
	        "8902": [-0.02778, 0.47222, 0, 0],
	        "8968": [0.25, 0.75, 0, 0],
	        "8969": [0.25, 0.75, 0, 0],
	        "8970": [0.25, 0.75, 0, 0],
	        "8971": [0.25, 0.75, 0, 0],
	        "8994": [-0.13889, 0.36111, 0, 0],
	        "8995": [-0.13889, 0.36111, 0, 0],
	        "9651": [0.19444, 0.69444, 0, 0],
	        "9657": [-0.02778, 0.47222, 0, 0],
	        "9661": [0.19444, 0.69444, 0, 0],
	        "9667": [-0.02778, 0.47222, 0, 0],
	        "9711": [0.19444, 0.69444, 0, 0],
	        "9824": [0.12963, 0.69444, 0, 0],
	        "9825": [0.12963, 0.69444, 0, 0],
	        "9826": [0.12963, 0.69444, 0, 0],
	        "9827": [0.12963, 0.69444, 0, 0],
	        "9837": [0, 0.75, 0, 0],
	        "9838": [0.19444, 0.69444, 0, 0],
	        "9839": [0.19444, 0.69444, 0, 0],
	        "10216": [0.25, 0.75, 0, 0],
	        "10217": [0.25, 0.75, 0, 0],
	        "10815": [0, 0.68611, 0, 0],
	        "10927": [0.19667, 0.69667, 0, 0],
	        "10928": [0.19667, 0.69667, 0, 0],
	    },
	    "Main-Italic": {
	        "33": [0, 0.69444, 0.12417, 0],
	        "34": [0, 0.69444, 0.06961, 0],
	        "35": [0.19444, 0.69444, 0.06616, 0],
	        "37": [0.05556, 0.75, 0.13639, 0],
	        "38": [0, 0.69444, 0.09694, 0],
	        "39": [0, 0.69444, 0.12417, 0],
	        "40": [0.25, 0.75, 0.16194, 0],
	        "41": [0.25, 0.75, 0.03694, 0],
	        "42": [0, 0.75, 0.14917, 0],
	        "43": [0.05667, 0.56167, 0.03694, 0],
	        "44": [0.19444, 0.10556, 0, 0],
	        "45": [0, 0.43056, 0.02826, 0],
	        "46": [0, 0.10556, 0, 0],
	        "47": [0.25, 0.75, 0.16194, 0],
	        "48": [0, 0.64444, 0.13556, 0],
	        "49": [0, 0.64444, 0.13556, 0],
	        "50": [0, 0.64444, 0.13556, 0],
	        "51": [0, 0.64444, 0.13556, 0],
	        "52": [0.19444, 0.64444, 0.13556, 0],
	        "53": [0, 0.64444, 0.13556, 0],
	        "54": [0, 0.64444, 0.13556, 0],
	        "55": [0.19444, 0.64444, 0.13556, 0],
	        "56": [0, 0.64444, 0.13556, 0],
	        "57": [0, 0.64444, 0.13556, 0],
	        "58": [0, 0.43056, 0.0582, 0],
	        "59": [0.19444, 0.43056, 0.0582, 0],
	        "61": [-0.13313, 0.36687, 0.06616, 0],
	        "63": [0, 0.69444, 0.1225, 0],
	        "64": [0, 0.69444, 0.09597, 0],
	        "65": [0, 0.68333, 0, 0],
	        "66": [0, 0.68333, 0.10257, 0],
	        "67": [0, 0.68333, 0.14528, 0],
	        "68": [0, 0.68333, 0.09403, 0],
	        "69": [0, 0.68333, 0.12028, 0],
	        "70": [0, 0.68333, 0.13305, 0],
	        "71": [0, 0.68333, 0.08722, 0],
	        "72": [0, 0.68333, 0.16389, 0],
	        "73": [0, 0.68333, 0.15806, 0],
	        "74": [0, 0.68333, 0.14028, 0],
	        "75": [0, 0.68333, 0.14528, 0],
	        "76": [0, 0.68333, 0, 0],
	        "77": [0, 0.68333, 0.16389, 0],
	        "78": [0, 0.68333, 0.16389, 0],
	        "79": [0, 0.68333, 0.09403, 0],
	        "80": [0, 0.68333, 0.10257, 0],
	        "81": [0.19444, 0.68333, 0.09403, 0],
	        "82": [0, 0.68333, 0.03868, 0],
	        "83": [0, 0.68333, 0.11972, 0],
	        "84": [0, 0.68333, 0.13305, 0],
	        "85": [0, 0.68333, 0.16389, 0],
	        "86": [0, 0.68333, 0.18361, 0],
	        "87": [0, 0.68333, 0.18361, 0],
	        "88": [0, 0.68333, 0.15806, 0],
	        "89": [0, 0.68333, 0.19383, 0],
	        "90": [0, 0.68333, 0.14528, 0],
	        "91": [0.25, 0.75, 0.1875, 0],
	        "93": [0.25, 0.75, 0.10528, 0],
	        "94": [0, 0.69444, 0.06646, 0],
	        "95": [0.31, 0.12056, 0.09208, 0],
	        "97": [0, 0.43056, 0.07671, 0],
	        "98": [0, 0.69444, 0.06312, 0],
	        "99": [0, 0.43056, 0.05653, 0],
	        "100": [0, 0.69444, 0.10333, 0],
	        "101": [0, 0.43056, 0.07514, 0],
	        "102": [0.19444, 0.69444, 0.21194, 0],
	        "103": [0.19444, 0.43056, 0.08847, 0],
	        "104": [0, 0.69444, 0.07671, 0],
	        "105": [0, 0.65536, 0.1019, 0],
	        "106": [0.19444, 0.65536, 0.14467, 0],
	        "107": [0, 0.69444, 0.10764, 0],
	        "108": [0, 0.69444, 0.10333, 0],
	        "109": [0, 0.43056, 0.07671, 0],
	        "110": [0, 0.43056, 0.07671, 0],
	        "111": [0, 0.43056, 0.06312, 0],
	        "112": [0.19444, 0.43056, 0.06312, 0],
	        "113": [0.19444, 0.43056, 0.08847, 0],
	        "114": [0, 0.43056, 0.10764, 0],
	        "115": [0, 0.43056, 0.08208, 0],
	        "116": [0, 0.61508, 0.09486, 0],
	        "117": [0, 0.43056, 0.07671, 0],
	        "118": [0, 0.43056, 0.10764, 0],
	        "119": [0, 0.43056, 0.10764, 0],
	        "120": [0, 0.43056, 0.12042, 0],
	        "121": [0.19444, 0.43056, 0.08847, 0],
	        "122": [0, 0.43056, 0.12292, 0],
	        "126": [0.35, 0.31786, 0.11585, 0],
	        "163": [0, 0.69444, 0, 0],
	        "305": [0, 0.43056, 0, 0.02778],
	        "567": [0.19444, 0.43056, 0, 0.08334],
	        "768": [0, 0.69444, 0, 0],
	        "769": [0, 0.69444, 0.09694, 0],
	        "770": [0, 0.69444, 0.06646, 0],
	        "771": [0, 0.66786, 0.11585, 0],
	        "772": [0, 0.56167, 0.10333, 0],
	        "774": [0, 0.69444, 0.10806, 0],
	        "775": [0, 0.66786, 0.11752, 0],
	        "776": [0, 0.66786, 0.10474, 0],
	        "778": [0, 0.69444, 0, 0],
	        "779": [0, 0.69444, 0.1225, 0],
	        "780": [0, 0.62847, 0.08295, 0],
	        "915": [0, 0.68333, 0.13305, 0],
	        "916": [0, 0.68333, 0, 0],
	        "920": [0, 0.68333, 0.09403, 0],
	        "923": [0, 0.68333, 0, 0],
	        "926": [0, 0.68333, 0.15294, 0],
	        "928": [0, 0.68333, 0.16389, 0],
	        "931": [0, 0.68333, 0.12028, 0],
	        "933": [0, 0.68333, 0.11111, 0],
	        "934": [0, 0.68333, 0.05986, 0],
	        "936": [0, 0.68333, 0.11111, 0],
	        "937": [0, 0.68333, 0.10257, 0],
	        "8211": [0, 0.43056, 0.09208, 0],
	        "8212": [0, 0.43056, 0.09208, 0],
	        "8216": [0, 0.69444, 0.12417, 0],
	        "8217": [0, 0.69444, 0.12417, 0],
	        "8220": [0, 0.69444, 0.1685, 0],
	        "8221": [0, 0.69444, 0.06961, 0],
	        "8463": [0, 0.68889, 0, 0],
	    },
	    "Main-Regular": {
	        "32": [0, 0, 0, 0],
	        "33": [0, 0.69444, 0, 0],
	        "34": [0, 0.69444, 0, 0],
	        "35": [0.19444, 0.69444, 0, 0],
	        "36": [0.05556, 0.75, 0, 0],
	        "37": [0.05556, 0.75, 0, 0],
	        "38": [0, 0.69444, 0, 0],
	        "39": [0, 0.69444, 0, 0],
	        "40": [0.25, 0.75, 0, 0],
	        "41": [0.25, 0.75, 0, 0],
	        "42": [0, 0.75, 0, 0],
	        "43": [0.08333, 0.58333, 0, 0],
	        "44": [0.19444, 0.10556, 0, 0],
	        "45": [0, 0.43056, 0, 0],
	        "46": [0, 0.10556, 0, 0],
	        "47": [0.25, 0.75, 0, 0],
	        "48": [0, 0.64444, 0, 0],
	        "49": [0, 0.64444, 0, 0],
	        "50": [0, 0.64444, 0, 0],
	        "51": [0, 0.64444, 0, 0],
	        "52": [0, 0.64444, 0, 0],
	        "53": [0, 0.64444, 0, 0],
	        "54": [0, 0.64444, 0, 0],
	        "55": [0, 0.64444, 0, 0],
	        "56": [0, 0.64444, 0, 0],
	        "57": [0, 0.64444, 0, 0],
	        "58": [0, 0.43056, 0, 0],
	        "59": [0.19444, 0.43056, 0, 0],
	        "60": [0.0391, 0.5391, 0, 0],
	        "61": [-0.13313, 0.36687, 0, 0],
	        "62": [0.0391, 0.5391, 0, 0],
	        "63": [0, 0.69444, 0, 0],
	        "64": [0, 0.69444, 0, 0],
	        "65": [0, 0.68333, 0, 0],
	        "66": [0, 0.68333, 0, 0],
	        "67": [0, 0.68333, 0, 0],
	        "68": [0, 0.68333, 0, 0],
	        "69": [0, 0.68333, 0, 0],
	        "70": [0, 0.68333, 0, 0],
	        "71": [0, 0.68333, 0, 0],
	        "72": [0, 0.68333, 0, 0],
	        "73": [0, 0.68333, 0, 0],
	        "74": [0, 0.68333, 0, 0],
	        "75": [0, 0.68333, 0, 0],
	        "76": [0, 0.68333, 0, 0],
	        "77": [0, 0.68333, 0, 0],
	        "78": [0, 0.68333, 0, 0],
	        "79": [0, 0.68333, 0, 0],
	        "80": [0, 0.68333, 0, 0],
	        "81": [0.19444, 0.68333, 0, 0],
	        "82": [0, 0.68333, 0, 0],
	        "83": [0, 0.68333, 0, 0],
	        "84": [0, 0.68333, 0, 0],
	        "85": [0, 0.68333, 0, 0],
	        "86": [0, 0.68333, 0.01389, 0],
	        "87": [0, 0.68333, 0.01389, 0],
	        "88": [0, 0.68333, 0, 0],
	        "89": [0, 0.68333, 0.025, 0],
	        "90": [0, 0.68333, 0, 0],
	        "91": [0.25, 0.75, 0, 0],
	        "92": [0.25, 0.75, 0, 0],
	        "93": [0.25, 0.75, 0, 0],
	        "94": [0, 0.69444, 0, 0],
	        "95": [0.31, 0.12056, 0.02778, 0],
	        "96": [0, 0.69444, 0, 0],
	        "97": [0, 0.43056, 0, 0],
	        "98": [0, 0.69444, 0, 0],
	        "99": [0, 0.43056, 0, 0],
	        "100": [0, 0.69444, 0, 0],
	        "101": [0, 0.43056, 0, 0],
	        "102": [0, 0.69444, 0.07778, 0],
	        "103": [0.19444, 0.43056, 0.01389, 0],
	        "104": [0, 0.69444, 0, 0],
	        "105": [0, 0.66786, 0, 0],
	        "106": [0.19444, 0.66786, 0, 0],
	        "107": [0, 0.69444, 0, 0],
	        "108": [0, 0.69444, 0, 0],
	        "109": [0, 0.43056, 0, 0],
	        "110": [0, 0.43056, 0, 0],
	        "111": [0, 0.43056, 0, 0],
	        "112": [0.19444, 0.43056, 0, 0],
	        "113": [0.19444, 0.43056, 0, 0],
	        "114": [0, 0.43056, 0, 0],
	        "115": [0, 0.43056, 0, 0],
	        "116": [0, 0.61508, 0, 0],
	        "117": [0, 0.43056, 0, 0],
	        "118": [0, 0.43056, 0.01389, 0],
	        "119": [0, 0.43056, 0.01389, 0],
	        "120": [0, 0.43056, 0, 0],
	        "121": [0.19444, 0.43056, 0.01389, 0],
	        "122": [0, 0.43056, 0, 0],
	        "123": [0.25, 0.75, 0, 0],
	        "124": [0.25, 0.75, 0, 0],
	        "125": [0.25, 0.75, 0, 0],
	        "126": [0.35, 0.31786, 0, 0],
	        "160": [0, 0, 0, 0],
	        "168": [0, 0.66786, 0, 0],
	        "172": [0, 0.43056, 0, 0],
	        "175": [0, 0.56778, 0, 0],
	        "176": [0, 0.69444, 0, 0],
	        "177": [0.08333, 0.58333, 0, 0],
	        "180": [0, 0.69444, 0, 0],
	        "215": [0.08333, 0.58333, 0, 0],
	        "247": [0.08333, 0.58333, 0, 0],
	        "305": [0, 0.43056, 0, 0],
	        "567": [0.19444, 0.43056, 0, 0],
	        "710": [0, 0.69444, 0, 0],
	        "711": [0, 0.62847, 0, 0],
	        "713": [0, 0.56778, 0, 0],
	        "714": [0, 0.69444, 0, 0],
	        "715": [0, 0.69444, 0, 0],
	        "728": [0, 0.69444, 0, 0],
	        "729": [0, 0.66786, 0, 0],
	        "730": [0, 0.69444, 0, 0],
	        "732": [0, 0.66786, 0, 0],
	        "768": [0, 0.69444, 0, 0],
	        "769": [0, 0.69444, 0, 0],
	        "770": [0, 0.69444, 0, 0],
	        "771": [0, 0.66786, 0, 0],
	        "772": [0, 0.56778, 0, 0],
	        "774": [0, 0.69444, 0, 0],
	        "775": [0, 0.66786, 0, 0],
	        "776": [0, 0.66786, 0, 0],
	        "778": [0, 0.69444, 0, 0],
	        "779": [0, 0.69444, 0, 0],
	        "780": [0, 0.62847, 0, 0],
	        "824": [0.19444, 0.69444, 0, 0],
	        "915": [0, 0.68333, 0, 0],
	        "916": [0, 0.68333, 0, 0],
	        "920": [0, 0.68333, 0, 0],
	        "923": [0, 0.68333, 0, 0],
	        "926": [0, 0.68333, 0, 0],
	        "928": [0, 0.68333, 0, 0],
	        "931": [0, 0.68333, 0, 0],
	        "933": [0, 0.68333, 0, 0],
	        "934": [0, 0.68333, 0, 0],
	        "936": [0, 0.68333, 0, 0],
	        "937": [0, 0.68333, 0, 0],
	        "8211": [0, 0.43056, 0.02778, 0],
	        "8212": [0, 0.43056, 0.02778, 0],
	        "8216": [0, 0.69444, 0, 0],
	        "8217": [0, 0.69444, 0, 0],
	        "8220": [0, 0.69444, 0, 0],
	        "8221": [0, 0.69444, 0, 0],
	        "8224": [0.19444, 0.69444, 0, 0],
	        "8225": [0.19444, 0.69444, 0, 0],
	        "8230": [0, 0.12, 0, 0],
	        "8242": [0, 0.55556, 0, 0],
	        "8407": [0, 0.71444, 0.15382, 0],
	        "8463": [0, 0.68889, 0, 0],
	        "8465": [0, 0.69444, 0, 0],
	        "8467": [0, 0.69444, 0, 0.11111],
	        "8472": [0.19444, 0.43056, 0, 0.11111],
	        "8476": [0, 0.69444, 0, 0],
	        "8501": [0, 0.69444, 0, 0],
	        "8592": [-0.13313, 0.36687, 0, 0],
	        "8593": [0.19444, 0.69444, 0, 0],
	        "8594": [-0.13313, 0.36687, 0, 0],
	        "8595": [0.19444, 0.69444, 0, 0],
	        "8596": [-0.13313, 0.36687, 0, 0],
	        "8597": [0.25, 0.75, 0, 0],
	        "8598": [0.19444, 0.69444, 0, 0],
	        "8599": [0.19444, 0.69444, 0, 0],
	        "8600": [0.19444, 0.69444, 0, 0],
	        "8601": [0.19444, 0.69444, 0, 0],
	        "8614": [0.011, 0.511, 0, 0],
	        "8617": [0.011, 0.511, 0, 0],
	        "8618": [0.011, 0.511, 0, 0],
	        "8636": [-0.13313, 0.36687, 0, 0],
	        "8637": [-0.13313, 0.36687, 0, 0],
	        "8640": [-0.13313, 0.36687, 0, 0],
	        "8641": [-0.13313, 0.36687, 0, 0],
	        "8652": [0.011, 0.671, 0, 0],
	        "8656": [-0.13313, 0.36687, 0, 0],
	        "8657": [0.19444, 0.69444, 0, 0],
	        "8658": [-0.13313, 0.36687, 0, 0],
	        "8659": [0.19444, 0.69444, 0, 0],
	        "8660": [-0.13313, 0.36687, 0, 0],
	        "8661": [0.25, 0.75, 0, 0],
	        "8704": [0, 0.69444, 0, 0],
	        "8706": [0, 0.69444, 0.05556, 0.08334],
	        "8707": [0, 0.69444, 0, 0],
	        "8709": [0.05556, 0.75, 0, 0],
	        "8711": [0, 0.68333, 0, 0],
	        "8712": [0.0391, 0.5391, 0, 0],
	        "8715": [0.0391, 0.5391, 0, 0],
	        "8722": [0.08333, 0.58333, 0, 0],
	        "8723": [0.08333, 0.58333, 0, 0],
	        "8725": [0.25, 0.75, 0, 0],
	        "8726": [0.25, 0.75, 0, 0],
	        "8727": [-0.03472, 0.46528, 0, 0],
	        "8728": [-0.05555, 0.44445, 0, 0],
	        "8729": [-0.05555, 0.44445, 0, 0],
	        "8730": [0.2, 0.8, 0, 0],
	        "8733": [0, 0.43056, 0, 0],
	        "8734": [0, 0.43056, 0, 0],
	        "8736": [0, 0.69224, 0, 0],
	        "8739": [0.25, 0.75, 0, 0],
	        "8741": [0.25, 0.75, 0, 0],
	        "8743": [0, 0.55556, 0, 0],
	        "8744": [0, 0.55556, 0, 0],
	        "8745": [0, 0.55556, 0, 0],
	        "8746": [0, 0.55556, 0, 0],
	        "8747": [0.19444, 0.69444, 0.11111, 0],
	        "8764": [-0.13313, 0.36687, 0, 0],
	        "8768": [0.19444, 0.69444, 0, 0],
	        "8771": [-0.03625, 0.46375, 0, 0],
	        "8773": [-0.022, 0.589, 0, 0],
	        "8776": [-0.01688, 0.48312, 0, 0],
	        "8781": [-0.03625, 0.46375, 0, 0],
	        "8784": [-0.133, 0.67, 0, 0],
	        "8800": [0.215, 0.716, 0, 0],
	        "8801": [-0.03625, 0.46375, 0, 0],
	        "8804": [0.13597, 0.63597, 0, 0],
	        "8805": [0.13597, 0.63597, 0, 0],
	        "8810": [0.0391, 0.5391, 0, 0],
	        "8811": [0.0391, 0.5391, 0, 0],
	        "8826": [0.0391, 0.5391, 0, 0],
	        "8827": [0.0391, 0.5391, 0, 0],
	        "8834": [0.0391, 0.5391, 0, 0],
	        "8835": [0.0391, 0.5391, 0, 0],
	        "8838": [0.13597, 0.63597, 0, 0],
	        "8839": [0.13597, 0.63597, 0, 0],
	        "8846": [0, 0.55556, 0, 0],
	        "8849": [0.13597, 0.63597, 0, 0],
	        "8850": [0.13597, 0.63597, 0, 0],
	        "8851": [0, 0.55556, 0, 0],
	        "8852": [0, 0.55556, 0, 0],
	        "8853": [0.08333, 0.58333, 0, 0],
	        "8854": [0.08333, 0.58333, 0, 0],
	        "8855": [0.08333, 0.58333, 0, 0],
	        "8856": [0.08333, 0.58333, 0, 0],
	        "8857": [0.08333, 0.58333, 0, 0],
	        "8866": [0, 0.69444, 0, 0],
	        "8867": [0, 0.69444, 0, 0],
	        "8868": [0, 0.69444, 0, 0],
	        "8869": [0, 0.69444, 0, 0],
	        "8872": [0.249, 0.75, 0, 0],
	        "8900": [-0.05555, 0.44445, 0, 0],
	        "8901": [-0.05555, 0.44445, 0, 0],
	        "8902": [-0.03472, 0.46528, 0, 0],
	        "8904": [0.005, 0.505, 0, 0],
	        "8942": [0.03, 0.9, 0, 0],
	        "8943": [-0.19, 0.31, 0, 0],
	        "8945": [-0.1, 0.82, 0, 0],
	        "8968": [0.25, 0.75, 0, 0],
	        "8969": [0.25, 0.75, 0, 0],
	        "8970": [0.25, 0.75, 0, 0],
	        "8971": [0.25, 0.75, 0, 0],
	        "8994": [-0.14236, 0.35764, 0, 0],
	        "8995": [-0.14236, 0.35764, 0, 0],
	        "9136": [0.244, 0.744, 0, 0],
	        "9137": [0.244, 0.744, 0, 0],
	        "9651": [0.19444, 0.69444, 0, 0],
	        "9657": [-0.03472, 0.46528, 0, 0],
	        "9661": [0.19444, 0.69444, 0, 0],
	        "9667": [-0.03472, 0.46528, 0, 0],
	        "9711": [0.19444, 0.69444, 0, 0],
	        "9824": [0.12963, 0.69444, 0, 0],
	        "9825": [0.12963, 0.69444, 0, 0],
	        "9826": [0.12963, 0.69444, 0, 0],
	        "9827": [0.12963, 0.69444, 0, 0],
	        "9837": [0, 0.75, 0, 0],
	        "9838": [0.19444, 0.69444, 0, 0],
	        "9839": [0.19444, 0.69444, 0, 0],
	        "10216": [0.25, 0.75, 0, 0],
	        "10217": [0.25, 0.75, 0, 0],
	        "10222": [0.244, 0.744, 0, 0],
	        "10223": [0.244, 0.744, 0, 0],
	        "10229": [0.011, 0.511, 0, 0],
	        "10230": [0.011, 0.511, 0, 0],
	        "10231": [0.011, 0.511, 0, 0],
	        "10232": [0.024, 0.525, 0, 0],
	        "10233": [0.024, 0.525, 0, 0],
	        "10234": [0.024, 0.525, 0, 0],
	        "10236": [0.011, 0.511, 0, 0],
	        "10815": [0, 0.68333, 0, 0],
	        "10927": [0.13597, 0.63597, 0, 0],
	        "10928": [0.13597, 0.63597, 0, 0],
	    },
	    "Math-BoldItalic": {
	        "47": [0.19444, 0.69444, 0, 0],
	        "65": [0, 0.68611, 0, 0],
	        "66": [0, 0.68611, 0.04835, 0],
	        "67": [0, 0.68611, 0.06979, 0],
	        "68": [0, 0.68611, 0.03194, 0],
	        "69": [0, 0.68611, 0.05451, 0],
	        "70": [0, 0.68611, 0.15972, 0],
	        "71": [0, 0.68611, 0, 0],
	        "72": [0, 0.68611, 0.08229, 0],
	        "73": [0, 0.68611, 0.07778, 0],
	        "74": [0, 0.68611, 0.10069, 0],
	        "75": [0, 0.68611, 0.06979, 0],
	        "76": [0, 0.68611, 0, 0],
	        "77": [0, 0.68611, 0.11424, 0],
	        "78": [0, 0.68611, 0.11424, 0],
	        "79": [0, 0.68611, 0.03194, 0],
	        "80": [0, 0.68611, 0.15972, 0],
	        "81": [0.19444, 0.68611, 0, 0],
	        "82": [0, 0.68611, 0.00421, 0],
	        "83": [0, 0.68611, 0.05382, 0],
	        "84": [0, 0.68611, 0.15972, 0],
	        "85": [0, 0.68611, 0.11424, 0],
	        "86": [0, 0.68611, 0.25555, 0],
	        "87": [0, 0.68611, 0.15972, 0],
	        "88": [0, 0.68611, 0.07778, 0],
	        "89": [0, 0.68611, 0.25555, 0],
	        "90": [0, 0.68611, 0.06979, 0],
	        "97": [0, 0.44444, 0, 0],
	        "98": [0, 0.69444, 0, 0],
	        "99": [0, 0.44444, 0, 0],
	        "100": [0, 0.69444, 0, 0],
	        "101": [0, 0.44444, 0, 0],
	        "102": [0.19444, 0.69444, 0.11042, 0],
	        "103": [0.19444, 0.44444, 0.03704, 0],
	        "104": [0, 0.69444, 0, 0],
	        "105": [0, 0.69326, 0, 0],
	        "106": [0.19444, 0.69326, 0.0622, 0],
	        "107": [0, 0.69444, 0.01852, 0],
	        "108": [0, 0.69444, 0.0088, 0],
	        "109": [0, 0.44444, 0, 0],
	        "110": [0, 0.44444, 0, 0],
	        "111": [0, 0.44444, 0, 0],
	        "112": [0.19444, 0.44444, 0, 0],
	        "113": [0.19444, 0.44444, 0.03704, 0],
	        "114": [0, 0.44444, 0.03194, 0],
	        "115": [0, 0.44444, 0, 0],
	        "116": [0, 0.63492, 0, 0],
	        "117": [0, 0.44444, 0, 0],
	        "118": [0, 0.44444, 0.03704, 0],
	        "119": [0, 0.44444, 0.02778, 0],
	        "120": [0, 0.44444, 0, 0],
	        "121": [0.19444, 0.44444, 0.03704, 0],
	        "122": [0, 0.44444, 0.04213, 0],
	        "915": [0, 0.68611, 0.15972, 0],
	        "916": [0, 0.68611, 0, 0],
	        "920": [0, 0.68611, 0.03194, 0],
	        "923": [0, 0.68611, 0, 0],
	        "926": [0, 0.68611, 0.07458, 0],
	        "928": [0, 0.68611, 0.08229, 0],
	        "931": [0, 0.68611, 0.05451, 0],
	        "933": [0, 0.68611, 0.15972, 0],
	        "934": [0, 0.68611, 0, 0],
	        "936": [0, 0.68611, 0.11653, 0],
	        "937": [0, 0.68611, 0.04835, 0],
	        "945": [0, 0.44444, 0, 0],
	        "946": [0.19444, 0.69444, 0.03403, 0],
	        "947": [0.19444, 0.44444, 0.06389, 0],
	        "948": [0, 0.69444, 0.03819, 0],
	        "949": [0, 0.44444, 0, 0],
	        "950": [0.19444, 0.69444, 0.06215, 0],
	        "951": [0.19444, 0.44444, 0.03704, 0],
	        "952": [0, 0.69444, 0.03194, 0],
	        "953": [0, 0.44444, 0, 0],
	        "954": [0, 0.44444, 0, 0],
	        "955": [0, 0.69444, 0, 0],
	        "956": [0.19444, 0.44444, 0, 0],
	        "957": [0, 0.44444, 0.06898, 0],
	        "958": [0.19444, 0.69444, 0.03021, 0],
	        "959": [0, 0.44444, 0, 0],
	        "960": [0, 0.44444, 0.03704, 0],
	        "961": [0.19444, 0.44444, 0, 0],
	        "962": [0.09722, 0.44444, 0.07917, 0],
	        "963": [0, 0.44444, 0.03704, 0],
	        "964": [0, 0.44444, 0.13472, 0],
	        "965": [0, 0.44444, 0.03704, 0],
	        "966": [0.19444, 0.44444, 0, 0],
	        "967": [0.19444, 0.44444, 0, 0],
	        "968": [0.19444, 0.69444, 0.03704, 0],
	        "969": [0, 0.44444, 0.03704, 0],
	        "977": [0, 0.69444, 0, 0],
	        "981": [0.19444, 0.69444, 0, 0],
	        "982": [0, 0.44444, 0.03194, 0],
	        "1009": [0.19444, 0.44444, 0, 0],
	        "1013": [0, 0.44444, 0, 0],
	    },
	    "Math-Italic": {
	        "47": [0.19444, 0.69444, 0, 0],
	        "65": [0, 0.68333, 0, 0.13889],
	        "66": [0, 0.68333, 0.05017, 0.08334],
	        "67": [0, 0.68333, 0.07153, 0.08334],
	        "68": [0, 0.68333, 0.02778, 0.05556],
	        "69": [0, 0.68333, 0.05764, 0.08334],
	        "70": [0, 0.68333, 0.13889, 0.08334],
	        "71": [0, 0.68333, 0, 0.08334],
	        "72": [0, 0.68333, 0.08125, 0.05556],
	        "73": [0, 0.68333, 0.07847, 0.11111],
	        "74": [0, 0.68333, 0.09618, 0.16667],
	        "75": [0, 0.68333, 0.07153, 0.05556],
	        "76": [0, 0.68333, 0, 0.02778],
	        "77": [0, 0.68333, 0.10903, 0.08334],
	        "78": [0, 0.68333, 0.10903, 0.08334],
	        "79": [0, 0.68333, 0.02778, 0.08334],
	        "80": [0, 0.68333, 0.13889, 0.08334],
	        "81": [0.19444, 0.68333, 0, 0.08334],
	        "82": [0, 0.68333, 0.00773, 0.08334],
	        "83": [0, 0.68333, 0.05764, 0.08334],
	        "84": [0, 0.68333, 0.13889, 0.08334],
	        "85": [0, 0.68333, 0.10903, 0.02778],
	        "86": [0, 0.68333, 0.22222, 0],
	        "87": [0, 0.68333, 0.13889, 0],
	        "88": [0, 0.68333, 0.07847, 0.08334],
	        "89": [0, 0.68333, 0.22222, 0],
	        "90": [0, 0.68333, 0.07153, 0.08334],
	        "97": [0, 0.43056, 0, 0],
	        "98": [0, 0.69444, 0, 0],
	        "99": [0, 0.43056, 0, 0.05556],
	        "100": [0, 0.69444, 0, 0.16667],
	        "101": [0, 0.43056, 0, 0.05556],
	        "102": [0.19444, 0.69444, 0.10764, 0.16667],
	        "103": [0.19444, 0.43056, 0.03588, 0.02778],
	        "104": [0, 0.69444, 0, 0],
	        "105": [0, 0.65952, 0, 0],
	        "106": [0.19444, 0.65952, 0.05724, 0],
	        "107": [0, 0.69444, 0.03148, 0],
	        "108": [0, 0.69444, 0.01968, 0.08334],
	        "109": [0, 0.43056, 0, 0],
	        "110": [0, 0.43056, 0, 0],
	        "111": [0, 0.43056, 0, 0.05556],
	        "112": [0.19444, 0.43056, 0, 0.08334],
	        "113": [0.19444, 0.43056, 0.03588, 0.08334],
	        "114": [0, 0.43056, 0.02778, 0.05556],
	        "115": [0, 0.43056, 0, 0.05556],
	        "116": [0, 0.61508, 0, 0.08334],
	        "117": [0, 0.43056, 0, 0.02778],
	        "118": [0, 0.43056, 0.03588, 0.02778],
	        "119": [0, 0.43056, 0.02691, 0.08334],
	        "120": [0, 0.43056, 0, 0.02778],
	        "121": [0.19444, 0.43056, 0.03588, 0.05556],
	        "122": [0, 0.43056, 0.04398, 0.05556],
	        "915": [0, 0.68333, 0.13889, 0.08334],
	        "916": [0, 0.68333, 0, 0.16667],
	        "920": [0, 0.68333, 0.02778, 0.08334],
	        "923": [0, 0.68333, 0, 0.16667],
	        "926": [0, 0.68333, 0.07569, 0.08334],
	        "928": [0, 0.68333, 0.08125, 0.05556],
	        "931": [0, 0.68333, 0.05764, 0.08334],
	        "933": [0, 0.68333, 0.13889, 0.05556],
	        "934": [0, 0.68333, 0, 0.08334],
	        "936": [0, 0.68333, 0.11, 0.05556],
	        "937": [0, 0.68333, 0.05017, 0.08334],
	        "945": [0, 0.43056, 0.0037, 0.02778],
	        "946": [0.19444, 0.69444, 0.05278, 0.08334],
	        "947": [0.19444, 0.43056, 0.05556, 0],
	        "948": [0, 0.69444, 0.03785, 0.05556],
	        "949": [0, 0.43056, 0, 0.08334],
	        "950": [0.19444, 0.69444, 0.07378, 0.08334],
	        "951": [0.19444, 0.43056, 0.03588, 0.05556],
	        "952": [0, 0.69444, 0.02778, 0.08334],
	        "953": [0, 0.43056, 0, 0.05556],
	        "954": [0, 0.43056, 0, 0],
	        "955": [0, 0.69444, 0, 0],
	        "956": [0.19444, 0.43056, 0, 0.02778],
	        "957": [0, 0.43056, 0.06366, 0.02778],
	        "958": [0.19444, 0.69444, 0.04601, 0.11111],
	        "959": [0, 0.43056, 0, 0.05556],
	        "960": [0, 0.43056, 0.03588, 0],
	        "961": [0.19444, 0.43056, 0, 0.08334],
	        "962": [0.09722, 0.43056, 0.07986, 0.08334],
	        "963": [0, 0.43056, 0.03588, 0],
	        "964": [0, 0.43056, 0.1132, 0.02778],
	        "965": [0, 0.43056, 0.03588, 0.02778],
	        "966": [0.19444, 0.43056, 0, 0.08334],
	        "967": [0.19444, 0.43056, 0, 0.05556],
	        "968": [0.19444, 0.69444, 0.03588, 0.11111],
	        "969": [0, 0.43056, 0.03588, 0],
	        "977": [0, 0.69444, 0, 0.08334],
	        "981": [0.19444, 0.69444, 0, 0.08334],
	        "982": [0, 0.43056, 0.02778, 0],
	        "1009": [0.19444, 0.43056, 0, 0.08334],
	        "1013": [0, 0.43056, 0, 0.05556],
	    },
	    "Math-Regular": {
	        "65": [0, 0.68333, 0, 0.13889],
	        "66": [0, 0.68333, 0.05017, 0.08334],
	        "67": [0, 0.68333, 0.07153, 0.08334],
	        "68": [0, 0.68333, 0.02778, 0.05556],
	        "69": [0, 0.68333, 0.05764, 0.08334],
	        "70": [0, 0.68333, 0.13889, 0.08334],
	        "71": [0, 0.68333, 0, 0.08334],
	        "72": [0, 0.68333, 0.08125, 0.05556],
	        "73": [0, 0.68333, 0.07847, 0.11111],
	        "74": [0, 0.68333, 0.09618, 0.16667],
	        "75": [0, 0.68333, 0.07153, 0.05556],
	        "76": [0, 0.68333, 0, 0.02778],
	        "77": [0, 0.68333, 0.10903, 0.08334],
	        "78": [0, 0.68333, 0.10903, 0.08334],
	        "79": [0, 0.68333, 0.02778, 0.08334],
	        "80": [0, 0.68333, 0.13889, 0.08334],
	        "81": [0.19444, 0.68333, 0, 0.08334],
	        "82": [0, 0.68333, 0.00773, 0.08334],
	        "83": [0, 0.68333, 0.05764, 0.08334],
	        "84": [0, 0.68333, 0.13889, 0.08334],
	        "85": [0, 0.68333, 0.10903, 0.02778],
	        "86": [0, 0.68333, 0.22222, 0],
	        "87": [0, 0.68333, 0.13889, 0],
	        "88": [0, 0.68333, 0.07847, 0.08334],
	        "89": [0, 0.68333, 0.22222, 0],
	        "90": [0, 0.68333, 0.07153, 0.08334],
	        "97": [0, 0.43056, 0, 0],
	        "98": [0, 0.69444, 0, 0],
	        "99": [0, 0.43056, 0, 0.05556],
	        "100": [0, 0.69444, 0, 0.16667],
	        "101": [0, 0.43056, 0, 0.05556],
	        "102": [0.19444, 0.69444, 0.10764, 0.16667],
	        "103": [0.19444, 0.43056, 0.03588, 0.02778],
	        "104": [0, 0.69444, 0, 0],
	        "105": [0, 0.65952, 0, 0],
	        "106": [0.19444, 0.65952, 0.05724, 0],
	        "107": [0, 0.69444, 0.03148, 0],
	        "108": [0, 0.69444, 0.01968, 0.08334],
	        "109": [0, 0.43056, 0, 0],
	        "110": [0, 0.43056, 0, 0],
	        "111": [0, 0.43056, 0, 0.05556],
	        "112": [0.19444, 0.43056, 0, 0.08334],
	        "113": [0.19444, 0.43056, 0.03588, 0.08334],
	        "114": [0, 0.43056, 0.02778, 0.05556],
	        "115": [0, 0.43056, 0, 0.05556],
	        "116": [0, 0.61508, 0, 0.08334],
	        "117": [0, 0.43056, 0, 0.02778],
	        "118": [0, 0.43056, 0.03588, 0.02778],
	        "119": [0, 0.43056, 0.02691, 0.08334],
	        "120": [0, 0.43056, 0, 0.02778],
	        "121": [0.19444, 0.43056, 0.03588, 0.05556],
	        "122": [0, 0.43056, 0.04398, 0.05556],
	        "915": [0, 0.68333, 0.13889, 0.08334],
	        "916": [0, 0.68333, 0, 0.16667],
	        "920": [0, 0.68333, 0.02778, 0.08334],
	        "923": [0, 0.68333, 0, 0.16667],
	        "926": [0, 0.68333, 0.07569, 0.08334],
	        "928": [0, 0.68333, 0.08125, 0.05556],
	        "931": [0, 0.68333, 0.05764, 0.08334],
	        "933": [0, 0.68333, 0.13889, 0.05556],
	        "934": [0, 0.68333, 0, 0.08334],
	        "936": [0, 0.68333, 0.11, 0.05556],
	        "937": [0, 0.68333, 0.05017, 0.08334],
	        "945": [0, 0.43056, 0.0037, 0.02778],
	        "946": [0.19444, 0.69444, 0.05278, 0.08334],
	        "947": [0.19444, 0.43056, 0.05556, 0],
	        "948": [0, 0.69444, 0.03785, 0.05556],
	        "949": [0, 0.43056, 0, 0.08334],
	        "950": [0.19444, 0.69444, 0.07378, 0.08334],
	        "951": [0.19444, 0.43056, 0.03588, 0.05556],
	        "952": [0, 0.69444, 0.02778, 0.08334],
	        "953": [0, 0.43056, 0, 0.05556],
	        "954": [0, 0.43056, 0, 0],
	        "955": [0, 0.69444, 0, 0],
	        "956": [0.19444, 0.43056, 0, 0.02778],
	        "957": [0, 0.43056, 0.06366, 0.02778],
	        "958": [0.19444, 0.69444, 0.04601, 0.11111],
	        "959": [0, 0.43056, 0, 0.05556],
	        "960": [0, 0.43056, 0.03588, 0],
	        "961": [0.19444, 0.43056, 0, 0.08334],
	        "962": [0.09722, 0.43056, 0.07986, 0.08334],
	        "963": [0, 0.43056, 0.03588, 0],
	        "964": [0, 0.43056, 0.1132, 0.02778],
	        "965": [0, 0.43056, 0.03588, 0.02778],
	        "966": [0.19444, 0.43056, 0, 0.08334],
	        "967": [0.19444, 0.43056, 0, 0.05556],
	        "968": [0.19444, 0.69444, 0.03588, 0.11111],
	        "969": [0, 0.43056, 0.03588, 0],
	        "977": [0, 0.69444, 0, 0.08334],
	        "981": [0.19444, 0.69444, 0, 0.08334],
	        "982": [0, 0.43056, 0.02778, 0],
	        "1009": [0.19444, 0.43056, 0, 0.08334],
	        "1013": [0, 0.43056, 0, 0.05556],
	    },
	    "SansSerif-Regular": {
	        "33": [0, 0.69444, 0, 0],
	        "34": [0, 0.69444, 0, 0],
	        "35": [0.19444, 0.69444, 0, 0],
	        "36": [0.05556, 0.75, 0, 0],
	        "37": [0.05556, 0.75, 0, 0],
	        "38": [0, 0.69444, 0, 0],
	        "39": [0, 0.69444, 0, 0],
	        "40": [0.25, 0.75, 0, 0],
	        "41": [0.25, 0.75, 0, 0],
	        "42": [0, 0.75, 0, 0],
	        "43": [0.08333, 0.58333, 0, 0],
	        "44": [0.125, 0.08333, 0, 0],
	        "45": [0, 0.44444, 0, 0],
	        "46": [0, 0.08333, 0, 0],
	        "47": [0.25, 0.75, 0, 0],
	        "48": [0, 0.65556, 0, 0],
	        "49": [0, 0.65556, 0, 0],
	        "50": [0, 0.65556, 0, 0],
	        "51": [0, 0.65556, 0, 0],
	        "52": [0, 0.65556, 0, 0],
	        "53": [0, 0.65556, 0, 0],
	        "54": [0, 0.65556, 0, 0],
	        "55": [0, 0.65556, 0, 0],
	        "56": [0, 0.65556, 0, 0],
	        "57": [0, 0.65556, 0, 0],
	        "58": [0, 0.44444, 0, 0],
	        "59": [0.125, 0.44444, 0, 0],
	        "61": [-0.13, 0.37, 0, 0],
	        "63": [0, 0.69444, 0, 0],
	        "64": [0, 0.69444, 0, 0],
	        "65": [0, 0.69444, 0, 0],
	        "66": [0, 0.69444, 0, 0],
	        "67": [0, 0.69444, 0, 0],
	        "68": [0, 0.69444, 0, 0],
	        "69": [0, 0.69444, 0, 0],
	        "70": [0, 0.69444, 0, 0],
	        "71": [0, 0.69444, 0, 0],
	        "72": [0, 0.69444, 0, 0],
	        "73": [0, 0.69444, 0, 0],
	        "74": [0, 0.69444, 0, 0],
	        "75": [0, 0.69444, 0, 0],
	        "76": [0, 0.69444, 0, 0],
	        "77": [0, 0.69444, 0, 0],
	        "78": [0, 0.69444, 0, 0],
	        "79": [0, 0.69444, 0, 0],
	        "80": [0, 0.69444, 0, 0],
	        "81": [0.125, 0.69444, 0, 0],
	        "82": [0, 0.69444, 0, 0],
	        "83": [0, 0.69444, 0, 0],
	        "84": [0, 0.69444, 0, 0],
	        "85": [0, 0.69444, 0, 0],
	        "86": [0, 0.69444, 0.01389, 0],
	        "87": [0, 0.69444, 0.01389, 0],
	        "88": [0, 0.69444, 0, 0],
	        "89": [0, 0.69444, 0.025, 0],
	        "90": [0, 0.69444, 0, 0],
	        "91": [0.25, 0.75, 0, 0],
	        "93": [0.25, 0.75, 0, 0],
	        "94": [0, 0.69444, 0, 0],
	        "95": [0.35, 0.09444, 0.02778, 0],
	        "97": [0, 0.44444, 0, 0],
	        "98": [0, 0.69444, 0, 0],
	        "99": [0, 0.44444, 0, 0],
	        "100": [0, 0.69444, 0, 0],
	        "101": [0, 0.44444, 0, 0],
	        "102": [0, 0.69444, 0.06944, 0],
	        "103": [0.19444, 0.44444, 0.01389, 0],
	        "104": [0, 0.69444, 0, 0],
	        "105": [0, 0.67937, 0, 0],
	        "106": [0.19444, 0.67937, 0, 0],
	        "107": [0, 0.69444, 0, 0],
	        "108": [0, 0.69444, 0, 0],
	        "109": [0, 0.44444, 0, 0],
	        "110": [0, 0.44444, 0, 0],
	        "111": [0, 0.44444, 0, 0],
	        "112": [0.19444, 0.44444, 0, 0],
	        "113": [0.19444, 0.44444, 0, 0],
	        "114": [0, 0.44444, 0.01389, 0],
	        "115": [0, 0.44444, 0, 0],
	        "116": [0, 0.57143, 0, 0],
	        "117": [0, 0.44444, 0, 0],
	        "118": [0, 0.44444, 0.01389, 0],
	        "119": [0, 0.44444, 0.01389, 0],
	        "120": [0, 0.44444, 0, 0],
	        "121": [0.19444, 0.44444, 0.01389, 0],
	        "122": [0, 0.44444, 0, 0],
	        "126": [0.35, 0.32659, 0, 0],
	        "305": [0, 0.44444, 0, 0],
	        "567": [0.19444, 0.44444, 0, 0],
	        "768": [0, 0.69444, 0, 0],
	        "769": [0, 0.69444, 0, 0],
	        "770": [0, 0.69444, 0, 0],
	        "771": [0, 0.67659, 0, 0],
	        "772": [0, 0.60889, 0, 0],
	        "774": [0, 0.69444, 0, 0],
	        "775": [0, 0.67937, 0, 0],
	        "776": [0, 0.67937, 0, 0],
	        "778": [0, 0.69444, 0, 0],
	        "779": [0, 0.69444, 0, 0],
	        "780": [0, 0.63194, 0, 0],
	        "915": [0, 0.69444, 0, 0],
	        "916": [0, 0.69444, 0, 0],
	        "920": [0, 0.69444, 0, 0],
	        "923": [0, 0.69444, 0, 0],
	        "926": [0, 0.69444, 0, 0],
	        "928": [0, 0.69444, 0, 0],
	        "931": [0, 0.69444, 0, 0],
	        "933": [0, 0.69444, 0, 0],
	        "934": [0, 0.69444, 0, 0],
	        "936": [0, 0.69444, 0, 0],
	        "937": [0, 0.69444, 0, 0],
	        "8211": [0, 0.44444, 0.02778, 0],
	        "8212": [0, 0.44444, 0.02778, 0],
	        "8216": [0, 0.69444, 0, 0],
	        "8217": [0, 0.69444, 0, 0],
	        "8220": [0, 0.69444, 0, 0],
	        "8221": [0, 0.69444, 0, 0],
	    },
	    "Script-Regular": {
	        "65": [0, 0.7, 0.22925, 0],
	        "66": [0, 0.7, 0.04087, 0],
	        "67": [0, 0.7, 0.1689, 0],
	        "68": [0, 0.7, 0.09371, 0],
	        "69": [0, 0.7, 0.18583, 0],
	        "70": [0, 0.7, 0.13634, 0],
	        "71": [0, 0.7, 0.17322, 0],
	        "72": [0, 0.7, 0.29694, 0],
	        "73": [0, 0.7, 0.19189, 0],
	        "74": [0.27778, 0.7, 0.19189, 0],
	        "75": [0, 0.7, 0.31259, 0],
	        "76": [0, 0.7, 0.19189, 0],
	        "77": [0, 0.7, 0.15981, 0],
	        "78": [0, 0.7, 0.3525, 0],
	        "79": [0, 0.7, 0.08078, 0],
	        "80": [0, 0.7, 0.08078, 0],
	        "81": [0, 0.7, 0.03305, 0],
	        "82": [0, 0.7, 0.06259, 0],
	        "83": [0, 0.7, 0.19189, 0],
	        "84": [0, 0.7, 0.29087, 0],
	        "85": [0, 0.7, 0.25815, 0],
	        "86": [0, 0.7, 0.27523, 0],
	        "87": [0, 0.7, 0.27523, 0],
	        "88": [0, 0.7, 0.26006, 0],
	        "89": [0, 0.7, 0.2939, 0],
	        "90": [0, 0.7, 0.24037, 0],
	    },
	    "Size1-Regular": {
	        "40": [0.35001, 0.85, 0, 0],
	        "41": [0.35001, 0.85, 0, 0],
	        "47": [0.35001, 0.85, 0, 0],
	        "91": [0.35001, 0.85, 0, 0],
	        "92": [0.35001, 0.85, 0, 0],
	        "93": [0.35001, 0.85, 0, 0],
	        "123": [0.35001, 0.85, 0, 0],
	        "125": [0.35001, 0.85, 0, 0],
	        "710": [0, 0.72222, 0, 0],
	        "732": [0, 0.72222, 0, 0],
	        "770": [0, 0.72222, 0, 0],
	        "771": [0, 0.72222, 0, 0],
	        "8214": [-0.00099, 0.601, 0, 0],
	        "8593": [1e-05, 0.6, 0, 0],
	        "8595": [1e-05, 0.6, 0, 0],
	        "8657": [1e-05, 0.6, 0, 0],
	        "8659": [1e-05, 0.6, 0, 0],
	        "8719": [0.25001, 0.75, 0, 0],
	        "8720": [0.25001, 0.75, 0, 0],
	        "8721": [0.25001, 0.75, 0, 0],
	        "8730": [0.35001, 0.85, 0, 0],
	        "8739": [-0.00599, 0.606, 0, 0],
	        "8741": [-0.00599, 0.606, 0, 0],
	        "8747": [0.30612, 0.805, 0.19445, 0],
	        "8748": [0.306, 0.805, 0.19445, 0],
	        "8749": [0.306, 0.805, 0.19445, 0],
	        "8750": [0.30612, 0.805, 0.19445, 0],
	        "8896": [0.25001, 0.75, 0, 0],
	        "8897": [0.25001, 0.75, 0, 0],
	        "8898": [0.25001, 0.75, 0, 0],
	        "8899": [0.25001, 0.75, 0, 0],
	        "8968": [0.35001, 0.85, 0, 0],
	        "8969": [0.35001, 0.85, 0, 0],
	        "8970": [0.35001, 0.85, 0, 0],
	        "8971": [0.35001, 0.85, 0, 0],
	        "9168": [-0.00099, 0.601, 0, 0],
	        "10216": [0.35001, 0.85, 0, 0],
	        "10217": [0.35001, 0.85, 0, 0],
	        "10752": [0.25001, 0.75, 0, 0],
	        "10753": [0.25001, 0.75, 0, 0],
	        "10754": [0.25001, 0.75, 0, 0],
	        "10756": [0.25001, 0.75, 0, 0],
	        "10758": [0.25001, 0.75, 0, 0],
	    },
	    "Size2-Regular": {
	        "40": [0.65002, 1.15, 0, 0],
	        "41": [0.65002, 1.15, 0, 0],
	        "47": [0.65002, 1.15, 0, 0],
	        "91": [0.65002, 1.15, 0, 0],
	        "92": [0.65002, 1.15, 0, 0],
	        "93": [0.65002, 1.15, 0, 0],
	        "123": [0.65002, 1.15, 0, 0],
	        "125": [0.65002, 1.15, 0, 0],
	        "710": [0, 0.75, 0, 0],
	        "732": [0, 0.75, 0, 0],
	        "770": [0, 0.75, 0, 0],
	        "771": [0, 0.75, 0, 0],
	        "8719": [0.55001, 1.05, 0, 0],
	        "8720": [0.55001, 1.05, 0, 0],
	        "8721": [0.55001, 1.05, 0, 0],
	        "8730": [0.65002, 1.15, 0, 0],
	        "8747": [0.86225, 1.36, 0.44445, 0],
	        "8748": [0.862, 1.36, 0.44445, 0],
	        "8749": [0.862, 1.36, 0.44445, 0],
	        "8750": [0.86225, 1.36, 0.44445, 0],
	        "8896": [0.55001, 1.05, 0, 0],
	        "8897": [0.55001, 1.05, 0, 0],
	        "8898": [0.55001, 1.05, 0, 0],
	        "8899": [0.55001, 1.05, 0, 0],
	        "8968": [0.65002, 1.15, 0, 0],
	        "8969": [0.65002, 1.15, 0, 0],
	        "8970": [0.65002, 1.15, 0, 0],
	        "8971": [0.65002, 1.15, 0, 0],
	        "10216": [0.65002, 1.15, 0, 0],
	        "10217": [0.65002, 1.15, 0, 0],
	        "10752": [0.55001, 1.05, 0, 0],
	        "10753": [0.55001, 1.05, 0, 0],
	        "10754": [0.55001, 1.05, 0, 0],
	        "10756": [0.55001, 1.05, 0, 0],
	        "10758": [0.55001, 1.05, 0, 0],
	    },
	    "Size3-Regular": {
	        "40": [0.95003, 1.45, 0, 0],
	        "41": [0.95003, 1.45, 0, 0],
	        "47": [0.95003, 1.45, 0, 0],
	        "91": [0.95003, 1.45, 0, 0],
	        "92": [0.95003, 1.45, 0, 0],
	        "93": [0.95003, 1.45, 0, 0],
	        "123": [0.95003, 1.45, 0, 0],
	        "125": [0.95003, 1.45, 0, 0],
	        "710": [0, 0.75, 0, 0],
	        "732": [0, 0.75, 0, 0],
	        "770": [0, 0.75, 0, 0],
	        "771": [0, 0.75, 0, 0],
	        "8730": [0.95003, 1.45, 0, 0],
	        "8968": [0.95003, 1.45, 0, 0],
	        "8969": [0.95003, 1.45, 0, 0],
	        "8970": [0.95003, 1.45, 0, 0],
	        "8971": [0.95003, 1.45, 0, 0],
	        "10216": [0.95003, 1.45, 0, 0],
	        "10217": [0.95003, 1.45, 0, 0],
	    },
	    "Size4-Regular": {
	        "40": [1.25003, 1.75, 0, 0],
	        "41": [1.25003, 1.75, 0, 0],
	        "47": [1.25003, 1.75, 0, 0],
	        "91": [1.25003, 1.75, 0, 0],
	        "92": [1.25003, 1.75, 0, 0],
	        "93": [1.25003, 1.75, 0, 0],
	        "123": [1.25003, 1.75, 0, 0],
	        "125": [1.25003, 1.75, 0, 0],
	        "710": [0, 0.825, 0, 0],
	        "732": [0, 0.825, 0, 0],
	        "770": [0, 0.825, 0, 0],
	        "771": [0, 0.825, 0, 0],
	        "8730": [1.25003, 1.75, 0, 0],
	        "8968": [1.25003, 1.75, 0, 0],
	        "8969": [1.25003, 1.75, 0, 0],
	        "8970": [1.25003, 1.75, 0, 0],
	        "8971": [1.25003, 1.75, 0, 0],
	        "9115": [0.64502, 1.155, 0, 0],
	        "9116": [1e-05, 0.6, 0, 0],
	        "9117": [0.64502, 1.155, 0, 0],
	        "9118": [0.64502, 1.155, 0, 0],
	        "9119": [1e-05, 0.6, 0, 0],
	        "9120": [0.64502, 1.155, 0, 0],
	        "9121": [0.64502, 1.155, 0, 0],
	        "9122": [-0.00099, 0.601, 0, 0],
	        "9123": [0.64502, 1.155, 0, 0],
	        "9124": [0.64502, 1.155, 0, 0],
	        "9125": [-0.00099, 0.601, 0, 0],
	        "9126": [0.64502, 1.155, 0, 0],
	        "9127": [1e-05, 0.9, 0, 0],
	        "9128": [0.65002, 1.15, 0, 0],
	        "9129": [0.90001, 0, 0, 0],
	        "9130": [0, 0.3, 0, 0],
	        "9131": [1e-05, 0.9, 0, 0],
	        "9132": [0.65002, 1.15, 0, 0],
	        "9133": [0.90001, 0, 0, 0],
	        "9143": [0.88502, 0.915, 0, 0],
	        "10216": [1.25003, 1.75, 0, 0],
	        "10217": [1.25003, 1.75, 0, 0],
	        "57344": [-0.00499, 0.605, 0, 0],
	        "57345": [-0.00499, 0.605, 0, 0],
	        "57680": [0, 0.12, 0, 0],
	        "57681": [0, 0.12, 0, 0],
	        "57682": [0, 0.12, 0, 0],
	        "57683": [0, 0.12, 0, 0],
	    },
	    "Typewriter-Regular": {
	        "33": [0, 0.61111, 0, 0],
	        "34": [0, 0.61111, 0, 0],
	        "35": [0, 0.61111, 0, 0],
	        "36": [0.08333, 0.69444, 0, 0],
	        "37": [0.08333, 0.69444, 0, 0],
	        "38": [0, 0.61111, 0, 0],
	        "39": [0, 0.61111, 0, 0],
	        "40": [0.08333, 0.69444, 0, 0],
	        "41": [0.08333, 0.69444, 0, 0],
	        "42": [0, 0.52083, 0, 0],
	        "43": [-0.08056, 0.53055, 0, 0],
	        "44": [0.13889, 0.125, 0, 0],
	        "45": [-0.08056, 0.53055, 0, 0],
	        "46": [0, 0.125, 0, 0],
	        "47": [0.08333, 0.69444, 0, 0],
	        "48": [0, 0.61111, 0, 0],
	        "49": [0, 0.61111, 0, 0],
	        "50": [0, 0.61111, 0, 0],
	        "51": [0, 0.61111, 0, 0],
	        "52": [0, 0.61111, 0, 0],
	        "53": [0, 0.61111, 0, 0],
	        "54": [0, 0.61111, 0, 0],
	        "55": [0, 0.61111, 0, 0],
	        "56": [0, 0.61111, 0, 0],
	        "57": [0, 0.61111, 0, 0],
	        "58": [0, 0.43056, 0, 0],
	        "59": [0.13889, 0.43056, 0, 0],
	        "60": [-0.05556, 0.55556, 0, 0],
	        "61": [-0.19549, 0.41562, 0, 0],
	        "62": [-0.05556, 0.55556, 0, 0],
	        "63": [0, 0.61111, 0, 0],
	        "64": [0, 0.61111, 0, 0],
	        "65": [0, 0.61111, 0, 0],
	        "66": [0, 0.61111, 0, 0],
	        "67": [0, 0.61111, 0, 0],
	        "68": [0, 0.61111, 0, 0],
	        "69": [0, 0.61111, 0, 0],
	        "70": [0, 0.61111, 0, 0],
	        "71": [0, 0.61111, 0, 0],
	        "72": [0, 0.61111, 0, 0],
	        "73": [0, 0.61111, 0, 0],
	        "74": [0, 0.61111, 0, 0],
	        "75": [0, 0.61111, 0, 0],
	        "76": [0, 0.61111, 0, 0],
	        "77": [0, 0.61111, 0, 0],
	        "78": [0, 0.61111, 0, 0],
	        "79": [0, 0.61111, 0, 0],
	        "80": [0, 0.61111, 0, 0],
	        "81": [0.13889, 0.61111, 0, 0],
	        "82": [0, 0.61111, 0, 0],
	        "83": [0, 0.61111, 0, 0],
	        "84": [0, 0.61111, 0, 0],
	        "85": [0, 0.61111, 0, 0],
	        "86": [0, 0.61111, 0, 0],
	        "87": [0, 0.61111, 0, 0],
	        "88": [0, 0.61111, 0, 0],
	        "89": [0, 0.61111, 0, 0],
	        "90": [0, 0.61111, 0, 0],
	        "91": [0.08333, 0.69444, 0, 0],
	        "92": [0.08333, 0.69444, 0, 0],
	        "93": [0.08333, 0.69444, 0, 0],
	        "94": [0, 0.61111, 0, 0],
	        "95": [0.09514, 0, 0, 0],
	        "96": [0, 0.61111, 0, 0],
	        "97": [0, 0.43056, 0, 0],
	        "98": [0, 0.61111, 0, 0],
	        "99": [0, 0.43056, 0, 0],
	        "100": [0, 0.61111, 0, 0],
	        "101": [0, 0.43056, 0, 0],
	        "102": [0, 0.61111, 0, 0],
	        "103": [0.22222, 0.43056, 0, 0],
	        "104": [0, 0.61111, 0, 0],
	        "105": [0, 0.61111, 0, 0],
	        "106": [0.22222, 0.61111, 0, 0],
	        "107": [0, 0.61111, 0, 0],
	        "108": [0, 0.61111, 0, 0],
	        "109": [0, 0.43056, 0, 0],
	        "110": [0, 0.43056, 0, 0],
	        "111": [0, 0.43056, 0, 0],
	        "112": [0.22222, 0.43056, 0, 0],
	        "113": [0.22222, 0.43056, 0, 0],
	        "114": [0, 0.43056, 0, 0],
	        "115": [0, 0.43056, 0, 0],
	        "116": [0, 0.55358, 0, 0],
	        "117": [0, 0.43056, 0, 0],
	        "118": [0, 0.43056, 0, 0],
	        "119": [0, 0.43056, 0, 0],
	        "120": [0, 0.43056, 0, 0],
	        "121": [0.22222, 0.43056, 0, 0],
	        "122": [0, 0.43056, 0, 0],
	        "123": [0.08333, 0.69444, 0, 0],
	        "124": [0.08333, 0.69444, 0, 0],
	        "125": [0.08333, 0.69444, 0, 0],
	        "126": [0, 0.61111, 0, 0],
	        "127": [0, 0.61111, 0, 0],
	        "305": [0, 0.43056, 0, 0],
	        "567": [0.22222, 0.43056, 0, 0],
	        "768": [0, 0.61111, 0, 0],
	        "769": [0, 0.61111, 0, 0],
	        "770": [0, 0.61111, 0, 0],
	        "771": [0, 0.61111, 0, 0],
	        "772": [0, 0.56555, 0, 0],
	        "774": [0, 0.61111, 0, 0],
	        "776": [0, 0.61111, 0, 0],
	        "778": [0, 0.61111, 0, 0],
	        "780": [0, 0.56597, 0, 0],
	        "915": [0, 0.61111, 0, 0],
	        "916": [0, 0.61111, 0, 0],
	        "920": [0, 0.61111, 0, 0],
	        "923": [0, 0.61111, 0, 0],
	        "926": [0, 0.61111, 0, 0],
	        "928": [0, 0.61111, 0, 0],
	        "931": [0, 0.61111, 0, 0],
	        "933": [0, 0.61111, 0, 0],
	        "934": [0, 0.61111, 0, 0],
	        "936": [0, 0.61111, 0, 0],
	        "937": [0, 0.61111, 0, 0],
	        "2018": [0, 0.61111, 0, 0],
	        "2019": [0, 0.61111, 0, 0],
	        "8242": [0, 0.61111, 0, 0],
	    },
	};


/***/ },
/* 272 */
/***/ function(module, exports) {

	/**
	 * This file holds a list of all no-argument functions and single-character
	 * symbols (like 'a' or ';').
	 *
	 * For each of the symbols, there are three properties they can have:
	 * - font (required): the font to be used for this symbol. Either "main" (the
	     normal font), or "ams" (the ams fonts).
	 * - group (required): the ParseNode group type the symbol should have (i.e.
	     "textord", "mathord", etc).
	     See https://github.com/Khan/KaTeX/wiki/Examining-TeX#group-types
	 * - replace: the character that this symbol or function should be
	 *   replaced with (i.e. "\phi" has a replace value of "\u03d5", the phi
	 *   character in the main font).
	 *
	 * The outermost map in the table indicates what mode the symbols should be
	 * accepted in (e.g. "math" or "text").
	 */

	module.exports = {
	    math: {},
	    text: {},
	};

	function defineSymbol(mode, font, group, replace, name) {
	    module.exports[mode][name] = {
	        font: font,
	        group: group,
	        replace: replace,
	    };
	}

	// Some abbreviations for commonly used strings.
	// This helps minify the code, and also spotting typos using jshint.

	// modes:
	var math = "math";
	var text = "text";

	// fonts:
	var main = "main";
	var ams = "ams";

	// groups:
	var accent = "accent";
	var bin = "bin";
	var close = "close";
	var inner = "inner";
	var mathord = "mathord";
	var op = "op";
	var open = "open";
	var punct = "punct";
	var rel = "rel";
	var spacing = "spacing";
	var textord = "textord";

	// Now comes the symbol table

	// Relation Symbols
	defineSymbol(math, main, rel, "\u2261", "\\equiv");
	defineSymbol(math, main, rel, "\u227a", "\\prec");
	defineSymbol(math, main, rel, "\u227b", "\\succ");
	defineSymbol(math, main, rel, "\u223c", "\\sim");
	defineSymbol(math, main, rel, "\u22a5", "\\perp");
	defineSymbol(math, main, rel, "\u2aaf", "\\preceq");
	defineSymbol(math, main, rel, "\u2ab0", "\\succeq");
	defineSymbol(math, main, rel, "\u2243", "\\simeq");
	defineSymbol(math, main, rel, "\u2223", "\\mid");
	defineSymbol(math, main, rel, "\u226a", "\\ll");
	defineSymbol(math, main, rel, "\u226b", "\\gg");
	defineSymbol(math, main, rel, "\u224d", "\\asymp");
	defineSymbol(math, main, rel, "\u2225", "\\parallel");
	defineSymbol(math, main, rel, "\u22c8", "\\bowtie");
	defineSymbol(math, main, rel, "\u2323", "\\smile");
	defineSymbol(math, main, rel, "\u2291", "\\sqsubseteq");
	defineSymbol(math, main, rel, "\u2292", "\\sqsupseteq");
	defineSymbol(math, main, rel, "\u2250", "\\doteq");
	defineSymbol(math, main, rel, "\u2322", "\\frown");
	defineSymbol(math, main, rel, "\u220b", "\\ni");
	defineSymbol(math, main, rel, "\u221d", "\\propto");
	defineSymbol(math, main, rel, "\u22a2", "\\vdash");
	defineSymbol(math, main, rel, "\u22a3", "\\dashv");
	defineSymbol(math, main, rel, "\u220b", "\\owns");

	// Punctuation
	defineSymbol(math, main, punct, "\u002e", "\\ldotp");
	defineSymbol(math, main, punct, "\u22c5", "\\cdotp");

	// Misc Symbols
	defineSymbol(math, main, textord, "\u0023", "\\#");
	defineSymbol(math, main, textord, "\u0026", "\\&");
	defineSymbol(math, main, textord, "\u2135", "\\aleph");
	defineSymbol(math, main, textord, "\u2200", "\\forall");
	defineSymbol(math, main, textord, "\u210f", "\\hbar");
	defineSymbol(math, main, textord, "\u2203", "\\exists");
	defineSymbol(math, main, textord, "\u2207", "\\nabla");
	defineSymbol(math, main, textord, "\u266d", "\\flat");
	defineSymbol(math, main, textord, "\u2113", "\\ell");
	defineSymbol(math, main, textord, "\u266e", "\\natural");
	defineSymbol(math, main, textord, "\u2663", "\\clubsuit");
	defineSymbol(math, main, textord, "\u2118", "\\wp");
	defineSymbol(math, main, textord, "\u266f", "\\sharp");
	defineSymbol(math, main, textord, "\u2662", "\\diamondsuit");
	defineSymbol(math, main, textord, "\u211c", "\\Re");
	defineSymbol(math, main, textord, "\u2661", "\\heartsuit");
	defineSymbol(math, main, textord, "\u2111", "\\Im");
	defineSymbol(math, main, textord, "\u2660", "\\spadesuit");

	// Math and Text
	defineSymbol(math, main, textord, "\u2020", "\\dag");
	defineSymbol(math, main, textord, "\u2021", "\\ddag");

	// Large Delimiters
	defineSymbol(math, main, close, "\u23b1", "\\rmoustache");
	defineSymbol(math, main, open, "\u23b0", "\\lmoustache");
	defineSymbol(math, main, close, "\u27ef", "\\rgroup");
	defineSymbol(math, main, open, "\u27ee", "\\lgroup");

	// Binary Operators
	defineSymbol(math, main, bin, "\u2213", "\\mp");
	defineSymbol(math, main, bin, "\u2296", "\\ominus");
	defineSymbol(math, main, bin, "\u228e", "\\uplus");
	defineSymbol(math, main, bin, "\u2293", "\\sqcap");
	defineSymbol(math, main, bin, "\u2217", "\\ast");
	defineSymbol(math, main, bin, "\u2294", "\\sqcup");
	defineSymbol(math, main, bin, "\u25ef", "\\bigcirc");
	defineSymbol(math, main, bin, "\u2219", "\\bullet");
	defineSymbol(math, main, bin, "\u2021", "\\ddagger");
	defineSymbol(math, main, bin, "\u2240", "\\wr");
	defineSymbol(math, main, bin, "\u2a3f", "\\amalg");

	// Arrow Symbols
	defineSymbol(math, main, rel, "\u27f5", "\\longleftarrow");
	defineSymbol(math, main, rel, "\u21d0", "\\Leftarrow");
	defineSymbol(math, main, rel, "\u27f8", "\\Longleftarrow");
	defineSymbol(math, main, rel, "\u27f6", "\\longrightarrow");
	defineSymbol(math, main, rel, "\u21d2", "\\Rightarrow");
	defineSymbol(math, main, rel, "\u27f9", "\\Longrightarrow");
	defineSymbol(math, main, rel, "\u2194", "\\leftrightarrow");
	defineSymbol(math, main, rel, "\u27f7", "\\longleftrightarrow");
	defineSymbol(math, main, rel, "\u21d4", "\\Leftrightarrow");
	defineSymbol(math, main, rel, "\u27fa", "\\Longleftrightarrow");
	defineSymbol(math, main, rel, "\u21a6", "\\mapsto");
	defineSymbol(math, main, rel, "\u27fc", "\\longmapsto");
	defineSymbol(math, main, rel, "\u2197", "\\nearrow");
	defineSymbol(math, main, rel, "\u21a9", "\\hookleftarrow");
	defineSymbol(math, main, rel, "\u21aa", "\\hookrightarrow");
	defineSymbol(math, main, rel, "\u2198", "\\searrow");
	defineSymbol(math, main, rel, "\u21bc", "\\leftharpoonup");
	defineSymbol(math, main, rel, "\u21c0", "\\rightharpoonup");
	defineSymbol(math, main, rel, "\u2199", "\\swarrow");
	defineSymbol(math, main, rel, "\u21bd", "\\leftharpoondown");
	defineSymbol(math, main, rel, "\u21c1", "\\rightharpoondown");
	defineSymbol(math, main, rel, "\u2196", "\\nwarrow");
	defineSymbol(math, main, rel, "\u21cc", "\\rightleftharpoons");

	// AMS Negated Binary Relations
	defineSymbol(math, ams, rel, "\u226e", "\\nless");
	defineSymbol(math, ams, rel, "\ue010", "\\nleqslant");
	defineSymbol(math, ams, rel, "\ue011", "\\nleqq");
	defineSymbol(math, ams, rel, "\u2a87", "\\lneq");
	defineSymbol(math, ams, rel, "\u2268", "\\lneqq");
	defineSymbol(math, ams, rel, "\ue00c", "\\lvertneqq");
	defineSymbol(math, ams, rel, "\u22e6", "\\lnsim");
	defineSymbol(math, ams, rel, "\u2a89", "\\lnapprox");
	defineSymbol(math, ams, rel, "\u2280", "\\nprec");
	defineSymbol(math, ams, rel, "\u22e0", "\\npreceq");
	defineSymbol(math, ams, rel, "\u22e8", "\\precnsim");
	defineSymbol(math, ams, rel, "\u2ab9", "\\precnapprox");
	defineSymbol(math, ams, rel, "\u2241", "\\nsim");
	defineSymbol(math, ams, rel, "\ue006", "\\nshortmid");
	defineSymbol(math, ams, rel, "\u2224", "\\nmid");
	defineSymbol(math, ams, rel, "\u22ac", "\\nvdash");
	defineSymbol(math, ams, rel, "\u22ad", "\\nvDash");
	defineSymbol(math, ams, rel, "\u22ea", "\\ntriangleleft");
	defineSymbol(math, ams, rel, "\u22ec", "\\ntrianglelefteq");
	defineSymbol(math, ams, rel, "\u228a", "\\subsetneq");
	defineSymbol(math, ams, rel, "\ue01a", "\\varsubsetneq");
	defineSymbol(math, ams, rel, "\u2acb", "\\subsetneqq");
	defineSymbol(math, ams, rel, "\ue017", "\\varsubsetneqq");
	defineSymbol(math, ams, rel, "\u226f", "\\ngtr");
	defineSymbol(math, ams, rel, "\ue00f", "\\ngeqslant");
	defineSymbol(math, ams, rel, "\ue00e", "\\ngeqq");
	defineSymbol(math, ams, rel, "\u2a88", "\\gneq");
	defineSymbol(math, ams, rel, "\u2269", "\\gneqq");
	defineSymbol(math, ams, rel, "\ue00d", "\\gvertneqq");
	defineSymbol(math, ams, rel, "\u22e7", "\\gnsim");
	defineSymbol(math, ams, rel, "\u2a8a", "\\gnapprox");
	defineSymbol(math, ams, rel, "\u2281", "\\nsucc");
	defineSymbol(math, ams, rel, "\u22e1", "\\nsucceq");
	defineSymbol(math, ams, rel, "\u22e9", "\\succnsim");
	defineSymbol(math, ams, rel, "\u2aba", "\\succnapprox");
	defineSymbol(math, ams, rel, "\u2246", "\\ncong");
	defineSymbol(math, ams, rel, "\ue007", "\\nshortparallel");
	defineSymbol(math, ams, rel, "\u2226", "\\nparallel");
	defineSymbol(math, ams, rel, "\u22af", "\\nVDash");
	defineSymbol(math, ams, rel, "\u22eb", "\\ntriangleright");
	defineSymbol(math, ams, rel, "\u22ed", "\\ntrianglerighteq");
	defineSymbol(math, ams, rel, "\ue018", "\\nsupseteqq");
	defineSymbol(math, ams, rel, "\u228b", "\\supsetneq");
	defineSymbol(math, ams, rel, "\ue01b", "\\varsupsetneq");
	defineSymbol(math, ams, rel, "\u2acc", "\\supsetneqq");
	defineSymbol(math, ams, rel, "\ue019", "\\varsupsetneqq");
	defineSymbol(math, ams, rel, "\u22ae", "\\nVdash");
	defineSymbol(math, ams, rel, "\u2ab5", "\\precneqq");
	defineSymbol(math, ams, rel, "\u2ab6", "\\succneqq");
	defineSymbol(math, ams, rel, "\ue016", "\\nsubseteqq");
	defineSymbol(math, ams, bin, "\u22b4", "\\unlhd");
	defineSymbol(math, ams, bin, "\u22b5", "\\unrhd");

	// AMS Negated Arrows
	defineSymbol(math, ams, rel, "\u219a", "\\nleftarrow");
	defineSymbol(math, ams, rel, "\u219b", "\\nrightarrow");
	defineSymbol(math, ams, rel, "\u21cd", "\\nLeftarrow");
	defineSymbol(math, ams, rel, "\u21cf", "\\nRightarrow");
	defineSymbol(math, ams, rel, "\u21ae", "\\nleftrightarrow");
	defineSymbol(math, ams, rel, "\u21ce", "\\nLeftrightarrow");

	// AMS Misc
	defineSymbol(math, ams, rel, "\u25b3", "\\vartriangle");
	defineSymbol(math, ams, textord, "\u210f", "\\hslash");
	defineSymbol(math, ams, textord, "\u25bd", "\\triangledown");
	defineSymbol(math, ams, textord, "\u25ca", "\\lozenge");
	defineSymbol(math, ams, textord, "\u24c8", "\\circledS");
	defineSymbol(math, ams, textord, "\u00ae", "\\circledR");
	defineSymbol(math, ams, textord, "\u2221", "\\measuredangle");
	defineSymbol(math, ams, textord, "\u2204", "\\nexists");
	defineSymbol(math, ams, textord, "\u2127", "\\mho");
	defineSymbol(math, ams, textord, "\u2132", "\\Finv");
	defineSymbol(math, ams, textord, "\u2141", "\\Game");
	defineSymbol(math, ams, textord, "\u006b", "\\Bbbk");
	defineSymbol(math, ams, textord, "\u2035", "\\backprime");
	defineSymbol(math, ams, textord, "\u25b2", "\\blacktriangle");
	defineSymbol(math, ams, textord, "\u25bc", "\\blacktriangledown");
	defineSymbol(math, ams, textord, "\u25a0", "\\blacksquare");
	defineSymbol(math, ams, textord, "\u29eb", "\\blacklozenge");
	defineSymbol(math, ams, textord, "\u2605", "\\bigstar");
	defineSymbol(math, ams, textord, "\u2222", "\\sphericalangle");
	defineSymbol(math, ams, textord, "\u2201", "\\complement");
	defineSymbol(math, ams, textord, "\u00f0", "\\eth");
	defineSymbol(math, ams, textord, "\u2571", "\\diagup");
	defineSymbol(math, ams, textord, "\u2572", "\\diagdown");
	defineSymbol(math, ams, textord, "\u25a1", "\\square");
	defineSymbol(math, ams, textord, "\u25a1", "\\Box");
	defineSymbol(math, ams, textord, "\u25ca", "\\Diamond");
	defineSymbol(math, ams, textord, "\u00a5", "\\yen");
	defineSymbol(math, ams, textord, "\u2713", "\\checkmark");

	// AMS Hebrew
	defineSymbol(math, ams, textord, "\u2136", "\\beth");
	defineSymbol(math, ams, textord, "\u2138", "\\daleth");
	defineSymbol(math, ams, textord, "\u2137", "\\gimel");

	// AMS Greek
	defineSymbol(math, ams, textord, "\u03dd", "\\digamma");
	defineSymbol(math, ams, textord, "\u03f0", "\\varkappa");

	// AMS Delimiters
	defineSymbol(math, ams, open, "\u250c", "\\ulcorner");
	defineSymbol(math, ams, close, "\u2510", "\\urcorner");
	defineSymbol(math, ams, open, "\u2514", "\\llcorner");
	defineSymbol(math, ams, close, "\u2518", "\\lrcorner");

	// AMS Binary Relations
	defineSymbol(math, ams, rel, "\u2266", "\\leqq");
	defineSymbol(math, ams, rel, "\u2a7d", "\\leqslant");
	defineSymbol(math, ams, rel, "\u2a95", "\\eqslantless");
	defineSymbol(math, ams, rel, "\u2272", "\\lesssim");
	defineSymbol(math, ams, rel, "\u2a85", "\\lessapprox");
	defineSymbol(math, ams, rel, "\u224a", "\\approxeq");
	defineSymbol(math, ams, bin, "\u22d6", "\\lessdot");
	defineSymbol(math, ams, rel, "\u22d8", "\\lll");
	defineSymbol(math, ams, rel, "\u2276", "\\lessgtr");
	defineSymbol(math, ams, rel, "\u22da", "\\lesseqgtr");
	defineSymbol(math, ams, rel, "\u2a8b", "\\lesseqqgtr");
	defineSymbol(math, ams, rel, "\u2251", "\\doteqdot");
	defineSymbol(math, ams, rel, "\u2253", "\\risingdotseq");
	defineSymbol(math, ams, rel, "\u2252", "\\fallingdotseq");
	defineSymbol(math, ams, rel, "\u223d", "\\backsim");
	defineSymbol(math, ams, rel, "\u22cd", "\\backsimeq");
	defineSymbol(math, ams, rel, "\u2ac5", "\\subseteqq");
	defineSymbol(math, ams, rel, "\u22d0", "\\Subset");
	defineSymbol(math, ams, rel, "\u228f", "\\sqsubset");
	defineSymbol(math, ams, rel, "\u227c", "\\preccurlyeq");
	defineSymbol(math, ams, rel, "\u22de", "\\curlyeqprec");
	defineSymbol(math, ams, rel, "\u227e", "\\precsim");
	defineSymbol(math, ams, rel, "\u2ab7", "\\precapprox");
	defineSymbol(math, ams, rel, "\u22b2", "\\vartriangleleft");
	defineSymbol(math, ams, rel, "\u22b4", "\\trianglelefteq");
	defineSymbol(math, ams, rel, "\u22a8", "\\vDash");
	defineSymbol(math, ams, rel, "\u22aa", "\\Vvdash");
	defineSymbol(math, ams, rel, "\u2323", "\\smallsmile");
	defineSymbol(math, ams, rel, "\u2322", "\\smallfrown");
	defineSymbol(math, ams, rel, "\u224f", "\\bumpeq");
	defineSymbol(math, ams, rel, "\u224e", "\\Bumpeq");
	defineSymbol(math, ams, rel, "\u2267", "\\geqq");
	defineSymbol(math, ams, rel, "\u2a7e", "\\geqslant");
	defineSymbol(math, ams, rel, "\u2a96", "\\eqslantgtr");
	defineSymbol(math, ams, rel, "\u2273", "\\gtrsim");
	defineSymbol(math, ams, rel, "\u2a86", "\\gtrapprox");
	defineSymbol(math, ams, bin, "\u22d7", "\\gtrdot");
	defineSymbol(math, ams, rel, "\u22d9", "\\ggg");
	defineSymbol(math, ams, rel, "\u2277", "\\gtrless");
	defineSymbol(math, ams, rel, "\u22db", "\\gtreqless");
	defineSymbol(math, ams, rel, "\u2a8c", "\\gtreqqless");
	defineSymbol(math, ams, rel, "\u2256", "\\eqcirc");
	defineSymbol(math, ams, rel, "\u2257", "\\circeq");
	defineSymbol(math, ams, rel, "\u225c", "\\triangleq");
	defineSymbol(math, ams, rel, "\u223c", "\\thicksim");
	defineSymbol(math, ams, rel, "\u2248", "\\thickapprox");
	defineSymbol(math, ams, rel, "\u2ac6", "\\supseteqq");
	defineSymbol(math, ams, rel, "\u22d1", "\\Supset");
	defineSymbol(math, ams, rel, "\u2290", "\\sqsupset");
	defineSymbol(math, ams, rel, "\u227d", "\\succcurlyeq");
	defineSymbol(math, ams, rel, "\u22df", "\\curlyeqsucc");
	defineSymbol(math, ams, rel, "\u227f", "\\succsim");
	defineSymbol(math, ams, rel, "\u2ab8", "\\succapprox");
	defineSymbol(math, ams, rel, "\u22b3", "\\vartriangleright");
	defineSymbol(math, ams, rel, "\u22b5", "\\trianglerighteq");
	defineSymbol(math, ams, rel, "\u22a9", "\\Vdash");
	defineSymbol(math, ams, rel, "\u2223", "\\shortmid");
	defineSymbol(math, ams, rel, "\u2225", "\\shortparallel");
	defineSymbol(math, ams, rel, "\u226c", "\\between");
	defineSymbol(math, ams, rel, "\u22d4", "\\pitchfork");
	defineSymbol(math, ams, rel, "\u221d", "\\varpropto");
	defineSymbol(math, ams, rel, "\u25c0", "\\blacktriangleleft");
	defineSymbol(math, ams, rel, "\u2234", "\\therefore");
	defineSymbol(math, ams, rel, "\u220d", "\\backepsilon");
	defineSymbol(math, ams, rel, "\u25b6", "\\blacktriangleright");
	defineSymbol(math, ams, rel, "\u2235", "\\because");
	defineSymbol(math, ams, rel, "\u22d8", "\\llless");
	defineSymbol(math, ams, rel, "\u22d9", "\\gggtr");
	defineSymbol(math, ams, bin, "\u22b2", "\\lhd");
	defineSymbol(math, ams, bin, "\u22b3", "\\rhd");
	defineSymbol(math, ams, rel, "\u2242", "\\eqsim");
	defineSymbol(math, main, rel, "\u22c8", "\\Join");
	defineSymbol(math, ams, rel, "\u2251", "\\Doteq");

	// AMS Binary Operators
	defineSymbol(math, ams, bin, "\u2214", "\\dotplus");
	defineSymbol(math, ams, bin, "\u2216", "\\smallsetminus");
	defineSymbol(math, ams, bin, "\u22d2", "\\Cap");
	defineSymbol(math, ams, bin, "\u22d3", "\\Cup");
	defineSymbol(math, ams, bin, "\u2a5e", "\\doublebarwedge");
	defineSymbol(math, ams, bin, "\u229f", "\\boxminus");
	defineSymbol(math, ams, bin, "\u229e", "\\boxplus");
	defineSymbol(math, ams, bin, "\u22c7", "\\divideontimes");
	defineSymbol(math, ams, bin, "\u22c9", "\\ltimes");
	defineSymbol(math, ams, bin, "\u22ca", "\\rtimes");
	defineSymbol(math, ams, bin, "\u22cb", "\\leftthreetimes");
	defineSymbol(math, ams, bin, "\u22cc", "\\rightthreetimes");
	defineSymbol(math, ams, bin, "\u22cf", "\\curlywedge");
	defineSymbol(math, ams, bin, "\u22ce", "\\curlyvee");
	defineSymbol(math, ams, bin, "\u229d", "\\circleddash");
	defineSymbol(math, ams, bin, "\u229b", "\\circledast");
	defineSymbol(math, ams, bin, "\u22c5", "\\centerdot");
	defineSymbol(math, ams, bin, "\u22ba", "\\intercal");
	defineSymbol(math, ams, bin, "\u22d2", "\\doublecap");
	defineSymbol(math, ams, bin, "\u22d3", "\\doublecup");
	defineSymbol(math, ams, bin, "\u22a0", "\\boxtimes");

	// AMS Arrows
	defineSymbol(math, ams, rel, "\u21e2", "\\dashrightarrow");
	defineSymbol(math, ams, rel, "\u21e0", "\\dashleftarrow");
	defineSymbol(math, ams, rel, "\u21c7", "\\leftleftarrows");
	defineSymbol(math, ams, rel, "\u21c6", "\\leftrightarrows");
	defineSymbol(math, ams, rel, "\u21da", "\\Lleftarrow");
	defineSymbol(math, ams, rel, "\u219e", "\\twoheadleftarrow");
	defineSymbol(math, ams, rel, "\u21a2", "\\leftarrowtail");
	defineSymbol(math, ams, rel, "\u21ab", "\\looparrowleft");
	defineSymbol(math, ams, rel, "\u21cb", "\\leftrightharpoons");
	defineSymbol(math, ams, rel, "\u21b6", "\\curvearrowleft");
	defineSymbol(math, ams, rel, "\u21ba", "\\circlearrowleft");
	defineSymbol(math, ams, rel, "\u21b0", "\\Lsh");
	defineSymbol(math, ams, rel, "\u21c8", "\\upuparrows");
	defineSymbol(math, ams, rel, "\u21bf", "\\upharpoonleft");
	defineSymbol(math, ams, rel, "\u21c3", "\\downharpoonleft");
	defineSymbol(math, ams, rel, "\u22b8", "\\multimap");
	defineSymbol(math, ams, rel, "\u21ad", "\\leftrightsquigarrow");
	defineSymbol(math, ams, rel, "\u21c9", "\\rightrightarrows");
	defineSymbol(math, ams, rel, "\u21c4", "\\rightleftarrows");
	defineSymbol(math, ams, rel, "\u21a0", "\\twoheadrightarrow");
	defineSymbol(math, ams, rel, "\u21a3", "\\rightarrowtail");
	defineSymbol(math, ams, rel, "\u21ac", "\\looparrowright");
	defineSymbol(math, ams, rel, "\u21b7", "\\curvearrowright");
	defineSymbol(math, ams, rel, "\u21bb", "\\circlearrowright");
	defineSymbol(math, ams, rel, "\u21b1", "\\Rsh");
	defineSymbol(math, ams, rel, "\u21ca", "\\downdownarrows");
	defineSymbol(math, ams, rel, "\u21be", "\\upharpoonright");
	defineSymbol(math, ams, rel, "\u21c2", "\\downharpoonright");
	defineSymbol(math, ams, rel, "\u21dd", "\\rightsquigarrow");
	defineSymbol(math, ams, rel, "\u21dd", "\\leadsto");
	defineSymbol(math, ams, rel, "\u21db", "\\Rrightarrow");
	defineSymbol(math, ams, rel, "\u21be", "\\restriction");

	defineSymbol(math, main, textord, "\u2018", "`");
	defineSymbol(math, main, textord, "$", "\\$");
	defineSymbol(math, main, textord, "%", "\\%");
	defineSymbol(math, main, textord, "_", "\\_");
	defineSymbol(math, main, textord, "\u2220", "\\angle");
	defineSymbol(math, main, textord, "\u221e", "\\infty");
	defineSymbol(math, main, textord, "\u2032", "\\prime");
	defineSymbol(math, main, textord, "\u25b3", "\\triangle");
	defineSymbol(math, main, textord, "\u0393", "\\Gamma");
	defineSymbol(math, main, textord, "\u0394", "\\Delta");
	defineSymbol(math, main, textord, "\u0398", "\\Theta");
	defineSymbol(math, main, textord, "\u039b", "\\Lambda");
	defineSymbol(math, main, textord, "\u039e", "\\Xi");
	defineSymbol(math, main, textord, "\u03a0", "\\Pi");
	defineSymbol(math, main, textord, "\u03a3", "\\Sigma");
	defineSymbol(math, main, textord, "\u03a5", "\\Upsilon");
	defineSymbol(math, main, textord, "\u03a6", "\\Phi");
	defineSymbol(math, main, textord, "\u03a8", "\\Psi");
	defineSymbol(math, main, textord, "\u03a9", "\\Omega");
	defineSymbol(math, main, textord, "\u00ac", "\\neg");
	defineSymbol(math, main, textord, "\u00ac", "\\lnot");
	defineSymbol(math, main, textord, "\u22a4", "\\top");
	defineSymbol(math, main, textord, "\u22a5", "\\bot");
	defineSymbol(math, main, textord, "\u2205", "\\emptyset");
	defineSymbol(math, ams, textord, "\u2205", "\\varnothing");
	defineSymbol(math, main, mathord, "\u03b1", "\\alpha");
	defineSymbol(math, main, mathord, "\u03b2", "\\beta");
	defineSymbol(math, main, mathord, "\u03b3", "\\gamma");
	defineSymbol(math, main, mathord, "\u03b4", "\\delta");
	defineSymbol(math, main, mathord, "\u03f5", "\\epsilon");
	defineSymbol(math, main, mathord, "\u03b6", "\\zeta");
	defineSymbol(math, main, mathord, "\u03b7", "\\eta");
	defineSymbol(math, main, mathord, "\u03b8", "\\theta");
	defineSymbol(math, main, mathord, "\u03b9", "\\iota");
	defineSymbol(math, main, mathord, "\u03ba", "\\kappa");
	defineSymbol(math, main, mathord, "\u03bb", "\\lambda");
	defineSymbol(math, main, mathord, "\u03bc", "\\mu");
	defineSymbol(math, main, mathord, "\u03bd", "\\nu");
	defineSymbol(math, main, mathord, "\u03be", "\\xi");
	defineSymbol(math, main, mathord, "o", "\\omicron");
	defineSymbol(math, main, mathord, "\u03c0", "\\pi");
	defineSymbol(math, main, mathord, "\u03c1", "\\rho");
	defineSymbol(math, main, mathord, "\u03c3", "\\sigma");
	defineSymbol(math, main, mathord, "\u03c4", "\\tau");
	defineSymbol(math, main, mathord, "\u03c5", "\\upsilon");
	defineSymbol(math, main, mathord, "\u03d5", "\\phi");
	defineSymbol(math, main, mathord, "\u03c7", "\\chi");
	defineSymbol(math, main, mathord, "\u03c8", "\\psi");
	defineSymbol(math, main, mathord, "\u03c9", "\\omega");
	defineSymbol(math, main, mathord, "\u03b5", "\\varepsilon");
	defineSymbol(math, main, mathord, "\u03d1", "\\vartheta");
	defineSymbol(math, main, mathord, "\u03d6", "\\varpi");
	defineSymbol(math, main, mathord, "\u03f1", "\\varrho");
	defineSymbol(math, main, mathord, "\u03c2", "\\varsigma");
	defineSymbol(math, main, mathord, "\u03c6", "\\varphi");
	defineSymbol(math, main, bin, "\u2217", "*");
	defineSymbol(math, main, bin, "+", "+");
	defineSymbol(math, main, bin, "\u2212", "-");
	defineSymbol(math, main, bin, "\u22c5", "\\cdot");
	defineSymbol(math, main, bin, "\u2218", "\\circ");
	defineSymbol(math, main, bin, "\u00f7", "\\div");
	defineSymbol(math, main, bin, "\u00b1", "\\pm");
	defineSymbol(math, main, bin, "\u00d7", "\\times");
	defineSymbol(math, main, bin, "\u2229", "\\cap");
	defineSymbol(math, main, bin, "\u222a", "\\cup");
	defineSymbol(math, main, bin, "\u2216", "\\setminus");
	defineSymbol(math, main, bin, "\u2227", "\\land");
	defineSymbol(math, main, bin, "\u2228", "\\lor");
	defineSymbol(math, main, bin, "\u2227", "\\wedge");
	defineSymbol(math, main, bin, "\u2228", "\\vee");
	defineSymbol(math, main, textord, "\u221a", "\\surd");
	defineSymbol(math, main, open, "(", "(");
	defineSymbol(math, main, open, "[", "[");
	defineSymbol(math, main, open, "\u27e8", "\\langle");
	defineSymbol(math, main, open, "\u2223", "\\lvert");
	defineSymbol(math, main, open, "\u2225", "\\lVert");
	defineSymbol(math, main, close, ")", ")");
	defineSymbol(math, main, close, "]", "]");
	defineSymbol(math, main, close, "?", "?");
	defineSymbol(math, main, close, "!", "!");
	defineSymbol(math, main, close, "\u27e9", "\\rangle");
	defineSymbol(math, main, close, "\u2223", "\\rvert");
	defineSymbol(math, main, close, "\u2225", "\\rVert");
	defineSymbol(math, main, rel, "=", "=");
	defineSymbol(math, main, rel, "<", "<");
	defineSymbol(math, main, rel, ">", ">");
	defineSymbol(math, main, rel, ":", ":");
	defineSymbol(math, main, rel, "\u2248", "\\approx");
	defineSymbol(math, main, rel, "\u2245", "\\cong");
	defineSymbol(math, main, rel, "\u2265", "\\ge");
	defineSymbol(math, main, rel, "\u2265", "\\geq");
	defineSymbol(math, main, rel, "\u2190", "\\gets");
	defineSymbol(math, main, rel, ">", "\\gt");
	defineSymbol(math, main, rel, "\u2208", "\\in");
	defineSymbol(math, main, rel, "\u2209", "\\notin");
	defineSymbol(math, main, rel, "\u2282", "\\subset");
	defineSymbol(math, main, rel, "\u2283", "\\supset");
	defineSymbol(math, main, rel, "\u2286", "\\subseteq");
	defineSymbol(math, main, rel, "\u2287", "\\supseteq");
	defineSymbol(math, ams, rel, "\u2288", "\\nsubseteq");
	defineSymbol(math, ams, rel, "\u2289", "\\nsupseteq");
	defineSymbol(math, main, rel, "\u22a8", "\\models");
	defineSymbol(math, main, rel, "\u2190", "\\leftarrow");
	defineSymbol(math, main, rel, "\u2264", "\\le");
	defineSymbol(math, main, rel, "\u2264", "\\leq");
	defineSymbol(math, main, rel, "<", "\\lt");
	defineSymbol(math, main, rel, "\u2260", "\\ne");
	defineSymbol(math, main, rel, "\u2260", "\\neq");
	defineSymbol(math, main, rel, "\u2192", "\\rightarrow");
	defineSymbol(math, main, rel, "\u2192", "\\to");
	defineSymbol(math, ams, rel, "\u2271", "\\ngeq");
	defineSymbol(math, ams, rel, "\u2270", "\\nleq");
	defineSymbol(math, main, spacing, null, "\\!");
	defineSymbol(math, main, spacing, "\u00a0", "\\ ");
	defineSymbol(math, main, spacing, "\u00a0", "~");
	defineSymbol(math, main, spacing, null, "\\,");
	defineSymbol(math, main, spacing, null, "\\:");
	defineSymbol(math, main, spacing, null, "\\;");
	defineSymbol(math, main, spacing, null, "\\enspace");
	defineSymbol(math, main, spacing, null, "\\qquad");
	defineSymbol(math, main, spacing, null, "\\quad");
	defineSymbol(math, main, spacing, "\u00a0", "\\space");
	defineSymbol(math, main, punct, ",", ",");
	defineSymbol(math, main, punct, ";", ";");
	defineSymbol(math, main, punct, ":", "\\colon");
	defineSymbol(math, ams, bin, "\u22bc", "\\barwedge");
	defineSymbol(math, ams, bin, "\u22bb", "\\veebar");
	defineSymbol(math, main, bin, "\u2299", "\\odot");
	defineSymbol(math, main, bin, "\u2295", "\\oplus");
	defineSymbol(math, main, bin, "\u2297", "\\otimes");
	defineSymbol(math, main, textord, "\u2202", "\\partial");
	defineSymbol(math, main, bin, "\u2298", "\\oslash");
	defineSymbol(math, ams, bin, "\u229a", "\\circledcirc");
	defineSymbol(math, ams, bin, "\u22a1", "\\boxdot");
	defineSymbol(math, main, bin, "\u25b3", "\\bigtriangleup");
	defineSymbol(math, main, bin, "\u25bd", "\\bigtriangledown");
	defineSymbol(math, main, bin, "\u2020", "\\dagger");
	defineSymbol(math, main, bin, "\u22c4", "\\diamond");
	defineSymbol(math, main, bin, "\u22c6", "\\star");
	defineSymbol(math, main, bin, "\u25c3", "\\triangleleft");
	defineSymbol(math, main, bin, "\u25b9", "\\triangleright");
	defineSymbol(math, main, open, "{", "\\{");
	defineSymbol(math, main, close, "}", "\\}");
	defineSymbol(math, main, open, "{", "\\lbrace");
	defineSymbol(math, main, close, "}", "\\rbrace");
	defineSymbol(math, main, open, "[", "\\lbrack");
	defineSymbol(math, main, close, "]", "\\rbrack");
	defineSymbol(math, main, open, "\u230a", "\\lfloor");
	defineSymbol(math, main, close, "\u230b", "\\rfloor");
	defineSymbol(math, main, open, "\u2308", "\\lceil");
	defineSymbol(math, main, close, "\u2309", "\\rceil");
	defineSymbol(math, main, textord, "\\", "\\backslash");
	defineSymbol(math, main, textord, "\u2223", "|");
	defineSymbol(math, main, textord, "\u2223", "\\vert");
	defineSymbol(math, main, textord, "\u2225", "\\|");
	defineSymbol(math, main, textord, "\u2225", "\\Vert");
	defineSymbol(math, main, rel, "\u2191", "\\uparrow");
	defineSymbol(math, main, rel, "\u21d1", "\\Uparrow");
	defineSymbol(math, main, rel, "\u2193", "\\downarrow");
	defineSymbol(math, main, rel, "\u21d3", "\\Downarrow");
	defineSymbol(math, main, rel, "\u2195", "\\updownarrow");
	defineSymbol(math, main, rel, "\u21d5", "\\Updownarrow");
	defineSymbol(math, math, op, "\u2210", "\\coprod");
	defineSymbol(math, math, op, "\u22c1", "\\bigvee");
	defineSymbol(math, math, op, "\u22c0", "\\bigwedge");
	defineSymbol(math, math, op, "\u2a04", "\\biguplus");
	defineSymbol(math, math, op, "\u22c2", "\\bigcap");
	defineSymbol(math, math, op, "\u22c3", "\\bigcup");
	defineSymbol(math, math, op, "\u222b", "\\int");
	defineSymbol(math, math, op, "\u222b", "\\intop");
	defineSymbol(math, math, op, "\u222c", "\\iint");
	defineSymbol(math, math, op, "\u222d", "\\iiint");
	defineSymbol(math, math, op, "\u220f", "\\prod");
	defineSymbol(math, math, op, "\u2211", "\\sum");
	defineSymbol(math, math, op, "\u2a02", "\\bigotimes");
	defineSymbol(math, math, op, "\u2a01", "\\bigoplus");
	defineSymbol(math, math, op, "\u2a00", "\\bigodot");
	defineSymbol(math, math, op, "\u222e", "\\oint");
	defineSymbol(math, math, op, "\u2a06", "\\bigsqcup");
	defineSymbol(math, math, op, "\u222b", "\\smallint");
	defineSymbol(math, main, inner, "\u2026", "\\ldots");
	defineSymbol(math, main, inner, "\u22ef", "\\cdots");
	defineSymbol(math, main, inner, "\u22f1", "\\ddots");
	defineSymbol(math, main, textord, "\u22ee", "\\vdots");
	defineSymbol(math, main, accent, "\u00b4", "\\acute");
	defineSymbol(math, main, accent, "\u0060", "\\grave");
	defineSymbol(math, main, accent, "\u00a8", "\\ddot");
	defineSymbol(math, main, accent, "\u007e", "\\tilde");
	defineSymbol(math, main, accent, "\u00af", "\\bar");
	defineSymbol(math, main, accent, "\u02d8", "\\breve");
	defineSymbol(math, main, accent, "\u02c7", "\\check");
	defineSymbol(math, main, accent, "\u005e", "\\hat");
	defineSymbol(math, main, accent, "\u20d7", "\\vec");
	defineSymbol(math, main, accent, "\u02d9", "\\dot");
	defineSymbol(math, main, mathord, "\u0131", "\\imath");
	defineSymbol(math, main, mathord, "\u0237", "\\jmath");

	defineSymbol(text, main, spacing, "\u00a0", "\\ ");
	defineSymbol(text, main, spacing, "\u00a0", " ");
	defineSymbol(text, main, spacing, "\u00a0", "~");

	// There are lots of symbols which are the same, so we add them in afterwards.
	var i;
	var ch;

	// All of these are textords in math mode
	var mathTextSymbols = "0123456789/@.\"";
	for (i = 0; i < mathTextSymbols.length; i++) {
	    ch = mathTextSymbols.charAt(i);
	    defineSymbol(math, main, textord, ch, ch);
	}

	// All of these are textords in text mode
	var textSymbols = "0123456789`!@*()-=+[]'\";:?/.,";
	for (i = 0; i < textSymbols.length; i++) {
	    ch = textSymbols.charAt(i);
	    defineSymbol(text, main, textord, ch, ch);
	}

	// All of these are textords in text mode, and mathords in math mode
	var letters = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ";
	for (i = 0; i < letters.length; i++) {
	    ch = letters.charAt(i);
	    defineSymbol(math, main, mathord, ch, ch);
	    defineSymbol(text, main, textord, ch, ch);
	}


/***/ },
/* 273 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * This file deals with creating delimiters of various sizes. The TeXbook
	 * discusses these routines on page 441-442, in the "Another subroutine sets box
	 * x to a specified variable delimiter" paragraph.
	 *
	 * There are three main routines here. `makeSmallDelim` makes a delimiter in the
	 * normal font, but in either text, script, or scriptscript style.
	 * `makeLargeDelim` makes a delimiter in textstyle, but in one of the Size1,
	 * Size2, Size3, or Size4 fonts. `makeStackedDelim` makes a delimiter out of
	 * smaller pieces that are stacked on top of one another.
	 *
	 * The functions take a parameter `center`, which determines if the delimiter
	 * should be centered around the axis.
	 *
	 * Then, there are three exposed functions. `sizedDelim` makes a delimiter in
	 * one of the given sizes. This is used for things like `\bigl`.
	 * `customSizedDelim` makes a delimiter with a given total height+depth. It is
	 * called in places like `\sqrt`. `leftRightDelim` makes an appropriate
	 * delimiter which surrounds an expression of a given height an depth. It is
	 * used in `\left` and `\right`.
	 */

	var ParseError = __webpack_require__(262);
	var Style = __webpack_require__(266);

	var buildCommon = __webpack_require__(267);
	var fontMetrics = __webpack_require__(270);
	var symbols = __webpack_require__(272);
	var utils = __webpack_require__(269);

	var makeSpan = buildCommon.makeSpan;

	/**
	 * Get the metrics for a given symbol and font, after transformation (i.e.
	 * after following replacement from symbols.js)
	 */
	var getMetrics = function(symbol, font) {
	    if (symbols.math[symbol] && symbols.math[symbol].replace) {
	        return fontMetrics.getCharacterMetrics(
	            symbols.math[symbol].replace, font);
	    } else {
	        return fontMetrics.getCharacterMetrics(
	            symbol, font);
	    }
	};

	/**
	 * Builds a symbol in the given font size (note size is an integer)
	 */
	var mathrmSize = function(value, size, mode) {
	    return buildCommon.makeSymbol(value, "Size" + size + "-Regular", mode);
	};

	/**
	 * Puts a delimiter span in a given style, and adds appropriate height, depth,
	 * and maxFontSizes.
	 */
	var styleWrap = function(delim, toStyle, options) {
	    var span = makeSpan(
	        ["style-wrap", options.style.reset(), toStyle.cls()], [delim]);

	    var multiplier = toStyle.sizeMultiplier / options.style.sizeMultiplier;

	    span.height *= multiplier;
	    span.depth *= multiplier;
	    span.maxFontSize = toStyle.sizeMultiplier;

	    return span;
	};

	/**
	 * Makes a small delimiter. This is a delimiter that comes in the Main-Regular
	 * font, but is restyled to either be in textstyle, scriptstyle, or
	 * scriptscriptstyle.
	 */
	var makeSmallDelim = function(delim, style, center, options, mode) {
	    var text = buildCommon.makeSymbol(delim, "Main-Regular", mode);

	    var span = styleWrap(text, style, options);

	    if (center) {
	        var shift =
	            (1 - options.style.sizeMultiplier / style.sizeMultiplier) *
	            fontMetrics.metrics.axisHeight;

	        span.style.top = shift + "em";
	        span.height -= shift;
	        span.depth += shift;
	    }

	    return span;
	};

	/**
	 * Makes a large delimiter. This is a delimiter that comes in the Size1, Size2,
	 * Size3, or Size4 fonts. It is always rendered in textstyle.
	 */
	var makeLargeDelim = function(delim, size, center, options, mode) {
	    var inner = mathrmSize(delim, size, mode);

	    var span = styleWrap(
	        makeSpan(["delimsizing", "size" + size],
	                 [inner], options.getColor()),
	        Style.TEXT, options);

	    if (center) {
	        var shift = (1 - options.style.sizeMultiplier) *
	            fontMetrics.metrics.axisHeight;

	        span.style.top = shift + "em";
	        span.height -= shift;
	        span.depth += shift;
	    }

	    return span;
	};

	/**
	 * Make an inner span with the given offset and in the given font. This is used
	 * in `makeStackedDelim` to make the stacking pieces for the delimiter.
	 */
	var makeInner = function(symbol, font, mode) {
	    var sizeClass;
	    // Apply the correct CSS class to choose the right font.
	    if (font === "Size1-Regular") {
	        sizeClass = "delim-size1";
	    } else if (font === "Size4-Regular") {
	        sizeClass = "delim-size4";
	    }

	    var inner = makeSpan(
	        ["delimsizinginner", sizeClass],
	        [makeSpan([], [buildCommon.makeSymbol(symbol, font, mode)])]);

	    // Since this will be passed into `makeVList` in the end, wrap the element
	    // in the appropriate tag that VList uses.
	    return {type: "elem", elem: inner};
	};

	/**
	 * Make a stacked delimiter out of a given delimiter, with the total height at
	 * least `heightTotal`. This routine is mentioned on page 442 of the TeXbook.
	 */
	var makeStackedDelim = function(delim, heightTotal, center, options, mode) {
	    // There are four parts, the top, an optional middle, a repeated part, and a
	    // bottom.
	    var top;
	    var middle;
	    var repeat;
	    var bottom;
	    top = repeat = bottom = delim;
	    middle = null;
	    // Also keep track of what font the delimiters are in
	    var font = "Size1-Regular";

	    // We set the parts and font based on the symbol. Note that we use
	    // '\u23d0' instead of '|' and '\u2016' instead of '\\|' for the
	    // repeats of the arrows
	    if (delim === "\\uparrow") {
	        repeat = bottom = "\u23d0";
	    } else if (delim === "\\Uparrow") {
	        repeat = bottom = "\u2016";
	    } else if (delim === "\\downarrow") {
	        top = repeat = "\u23d0";
	    } else if (delim === "\\Downarrow") {
	        top = repeat = "\u2016";
	    } else if (delim === "\\updownarrow") {
	        top = "\\uparrow";
	        repeat = "\u23d0";
	        bottom = "\\downarrow";
	    } else if (delim === "\\Updownarrow") {
	        top = "\\Uparrow";
	        repeat = "\u2016";
	        bottom = "\\Downarrow";
	    } else if (delim === "[" || delim === "\\lbrack") {
	        top = "\u23a1";
	        repeat = "\u23a2";
	        bottom = "\u23a3";
	        font = "Size4-Regular";
	    } else if (delim === "]" || delim === "\\rbrack") {
	        top = "\u23a4";
	        repeat = "\u23a5";
	        bottom = "\u23a6";
	        font = "Size4-Regular";
	    } else if (delim === "\\lfloor") {
	        repeat = top = "\u23a2";
	        bottom = "\u23a3";
	        font = "Size4-Regular";
	    } else if (delim === "\\lceil") {
	        top = "\u23a1";
	        repeat = bottom = "\u23a2";
	        font = "Size4-Regular";
	    } else if (delim === "\\rfloor") {
	        repeat = top = "\u23a5";
	        bottom = "\u23a6";
	        font = "Size4-Regular";
	    } else if (delim === "\\rceil") {
	        top = "\u23a4";
	        repeat = bottom = "\u23a5";
	        font = "Size4-Regular";
	    } else if (delim === "(") {
	        top = "\u239b";
	        repeat = "\u239c";
	        bottom = "\u239d";
	        font = "Size4-Regular";
	    } else if (delim === ")") {
	        top = "\u239e";
	        repeat = "\u239f";
	        bottom = "\u23a0";
	        font = "Size4-Regular";
	    } else if (delim === "\\{" || delim === "\\lbrace") {
	        top = "\u23a7";
	        middle = "\u23a8";
	        bottom = "\u23a9";
	        repeat = "\u23aa";
	        font = "Size4-Regular";
	    } else if (delim === "\\}" || delim === "\\rbrace") {
	        top = "\u23ab";
	        middle = "\u23ac";
	        bottom = "\u23ad";
	        repeat = "\u23aa";
	        font = "Size4-Regular";
	    } else if (delim === "\\lgroup") {
	        top = "\u23a7";
	        bottom = "\u23a9";
	        repeat = "\u23aa";
	        font = "Size4-Regular";
	    } else if (delim === "\\rgroup") {
	        top = "\u23ab";
	        bottom = "\u23ad";
	        repeat = "\u23aa";
	        font = "Size4-Regular";
	    } else if (delim === "\\lmoustache") {
	        top = "\u23a7";
	        bottom = "\u23ad";
	        repeat = "\u23aa";
	        font = "Size4-Regular";
	    } else if (delim === "\\rmoustache") {
	        top = "\u23ab";
	        bottom = "\u23a9";
	        repeat = "\u23aa";
	        font = "Size4-Regular";
	    } else if (delim === "\\surd") {
	        top = "\ue001";
	        bottom = "\u23b7";
	        repeat = "\ue000";
	        font = "Size4-Regular";
	    }

	    // Get the metrics of the four sections
	    var topMetrics = getMetrics(top, font);
	    var topHeightTotal = topMetrics.height + topMetrics.depth;
	    var repeatMetrics = getMetrics(repeat, font);
	    var repeatHeightTotal = repeatMetrics.height + repeatMetrics.depth;
	    var bottomMetrics = getMetrics(bottom, font);
	    var bottomHeightTotal = bottomMetrics.height + bottomMetrics.depth;
	    var middleHeightTotal = 0;
	    var middleFactor = 1;
	    if (middle !== null) {
	        var middleMetrics = getMetrics(middle, font);
	        middleHeightTotal = middleMetrics.height + middleMetrics.depth;
	        middleFactor = 2; // repeat symmetrically above and below middle
	    }

	    // Calcuate the minimal height that the delimiter can have.
	    // It is at least the size of the top, bottom, and optional middle combined.
	    var minHeight = topHeightTotal + bottomHeightTotal + middleHeightTotal;

	    // Compute the number of copies of the repeat symbol we will need
	    var repeatCount = Math.ceil(
	        (heightTotal - minHeight) / (middleFactor * repeatHeightTotal));

	    // Compute the total height of the delimiter including all the symbols
	    var realHeightTotal =
	        minHeight + repeatCount * middleFactor * repeatHeightTotal;

	    // The center of the delimiter is placed at the center of the axis. Note
	    // that in this context, "center" means that the delimiter should be
	    // centered around the axis in the current style, while normally it is
	    // centered around the axis in textstyle.
	    var axisHeight = fontMetrics.metrics.axisHeight;
	    if (center) {
	        axisHeight *= options.style.sizeMultiplier;
	    }
	    // Calculate the depth
	    var depth = realHeightTotal / 2 - axisHeight;

	    // Now, we start building the pieces that will go into the vlist

	    // Keep a list of the inner pieces
	    var inners = [];

	    // Add the bottom symbol
	    inners.push(makeInner(bottom, font, mode));

	    var i;
	    if (middle === null) {
	        // Add that many symbols
	        for (i = 0; i < repeatCount; i++) {
	            inners.push(makeInner(repeat, font, mode));
	        }
	    } else {
	        // When there is a middle bit, we need the middle part and two repeated
	        // sections
	        for (i = 0; i < repeatCount; i++) {
	            inners.push(makeInner(repeat, font, mode));
	        }
	        inners.push(makeInner(middle, font, mode));
	        for (i = 0; i < repeatCount; i++) {
	            inners.push(makeInner(repeat, font, mode));
	        }
	    }

	    // Add the top symbol
	    inners.push(makeInner(top, font, mode));

	    // Finally, build the vlist
	    var inner = buildCommon.makeVList(inners, "bottom", depth, options);

	    return styleWrap(
	        makeSpan(["delimsizing", "mult"], [inner], options.getColor()),
	        Style.TEXT, options);
	};

	// There are three kinds of delimiters, delimiters that stack when they become
	// too large
	var stackLargeDelimiters = [
	    "(", ")", "[", "\\lbrack", "]", "\\rbrack",
	    "\\{", "\\lbrace", "\\}", "\\rbrace",
	    "\\lfloor", "\\rfloor", "\\lceil", "\\rceil",
	    "\\surd",
	];

	// delimiters that always stack
	var stackAlwaysDelimiters = [
	    "\\uparrow", "\\downarrow", "\\updownarrow",
	    "\\Uparrow", "\\Downarrow", "\\Updownarrow",
	    "|", "\\|", "\\vert", "\\Vert",
	    "\\lvert", "\\rvert", "\\lVert", "\\rVert",
	    "\\lgroup", "\\rgroup", "\\lmoustache", "\\rmoustache",
	];

	// and delimiters that never stack
	var stackNeverDelimiters = [
	    "<", ">", "\\langle", "\\rangle", "/", "\\backslash", "\\lt", "\\gt",
	];

	// Metrics of the different sizes. Found by looking at TeX's output of
	// $\bigl| // \Bigl| \biggl| \Biggl| \showlists$
	// Used to create stacked delimiters of appropriate sizes in makeSizedDelim.
	var sizeToMaxHeight = [0, 1.2, 1.8, 2.4, 3.0];

	/**
	 * Used to create a delimiter of a specific size, where `size` is 1, 2, 3, or 4.
	 */
	var makeSizedDelim = function(delim, size, options, mode) {
	    // < and > turn into \langle and \rangle in delimiters
	    if (delim === "<" || delim === "\\lt") {
	        delim = "\\langle";
	    } else if (delim === ">" || delim === "\\gt") {
	        delim = "\\rangle";
	    }

	    // Sized delimiters are never centered.
	    if (utils.contains(stackLargeDelimiters, delim) ||
	        utils.contains(stackNeverDelimiters, delim)) {
	        return makeLargeDelim(delim, size, false, options, mode);
	    } else if (utils.contains(stackAlwaysDelimiters, delim)) {
	        return makeStackedDelim(
	            delim, sizeToMaxHeight[size], false, options, mode);
	    } else {
	        throw new ParseError("Illegal delimiter: '" + delim + "'");
	    }
	};

	/**
	 * There are three different sequences of delimiter sizes that the delimiters
	 * follow depending on the kind of delimiter. This is used when creating custom
	 * sized delimiters to decide whether to create a small, large, or stacked
	 * delimiter.
	 *
	 * In real TeX, these sequences aren't explicitly defined, but are instead
	 * defined inside the font metrics. Since there are only three sequences that
	 * are possible for the delimiters that TeX defines, it is easier to just encode
	 * them explicitly here.
	 */

	// Delimiters that never stack try small delimiters and large delimiters only
	var stackNeverDelimiterSequence = [
	    {type: "small", style: Style.SCRIPTSCRIPT},
	    {type: "small", style: Style.SCRIPT},
	    {type: "small", style: Style.TEXT},
	    {type: "large", size: 1},
	    {type: "large", size: 2},
	    {type: "large", size: 3},
	    {type: "large", size: 4},
	];

	// Delimiters that always stack try the small delimiters first, then stack
	var stackAlwaysDelimiterSequence = [
	    {type: "small", style: Style.SCRIPTSCRIPT},
	    {type: "small", style: Style.SCRIPT},
	    {type: "small", style: Style.TEXT},
	    {type: "stack"},
	];

	// Delimiters that stack when large try the small and then large delimiters, and
	// stack afterwards
	var stackLargeDelimiterSequence = [
	    {type: "small", style: Style.SCRIPTSCRIPT},
	    {type: "small", style: Style.SCRIPT},
	    {type: "small", style: Style.TEXT},
	    {type: "large", size: 1},
	    {type: "large", size: 2},
	    {type: "large", size: 3},
	    {type: "large", size: 4},
	    {type: "stack"},
	];

	/**
	 * Get the font used in a delimiter based on what kind of delimiter it is.
	 */
	var delimTypeToFont = function(type) {
	    if (type.type === "small") {
	        return "Main-Regular";
	    } else if (type.type === "large") {
	        return "Size" + type.size + "-Regular";
	    } else if (type.type === "stack") {
	        return "Size4-Regular";
	    }
	};

	/**
	 * Traverse a sequence of types of delimiters to decide what kind of delimiter
	 * should be used to create a delimiter of the given height+depth.
	 */
	var traverseSequence = function(delim, height, sequence, options) {
	    // Here, we choose the index we should start at in the sequences. In smaller
	    // sizes (which correspond to larger numbers in style.size) we start earlier
	    // in the sequence. Thus, scriptscript starts at index 3-3=0, script starts
	    // at index 3-2=1, text starts at 3-1=2, and display starts at min(2,3-0)=2
	    var start = Math.min(2, 3 - options.style.size);
	    for (var i = start; i < sequence.length; i++) {
	        if (sequence[i].type === "stack") {
	            // This is always the last delimiter, so we just break the loop now.
	            break;
	        }

	        var metrics = getMetrics(delim, delimTypeToFont(sequence[i]));
	        var heightDepth = metrics.height + metrics.depth;

	        // Small delimiters are scaled down versions of the same font, so we
	        // account for the style change size.

	        if (sequence[i].type === "small") {
	            heightDepth *= sequence[i].style.sizeMultiplier;
	        }

	        // Check if the delimiter at this size works for the given height.
	        if (heightDepth > height) {
	            return sequence[i];
	        }
	    }

	    // If we reached the end of the sequence, return the last sequence element.
	    return sequence[sequence.length - 1];
	};

	/**
	 * Make a delimiter of a given height+depth, with optional centering. Here, we
	 * traverse the sequences, and create a delimiter that the sequence tells us to.
	 */
	var makeCustomSizedDelim = function(delim, height, center, options, mode) {
	    if (delim === "<" || delim === "\\lt") {
	        delim = "\\langle";
	    } else if (delim === ">" || delim === "\\gt") {
	        delim = "\\rangle";
	    }

	    // Decide what sequence to use
	    var sequence;
	    if (utils.contains(stackNeverDelimiters, delim)) {
	        sequence = stackNeverDelimiterSequence;
	    } else if (utils.contains(stackLargeDelimiters, delim)) {
	        sequence = stackLargeDelimiterSequence;
	    } else {
	        sequence = stackAlwaysDelimiterSequence;
	    }

	    // Look through the sequence
	    var delimType = traverseSequence(delim, height, sequence, options);

	    // Depending on the sequence element we decided on, call the appropriate
	    // function.
	    if (delimType.type === "small") {
	        return makeSmallDelim(delim, delimType.style, center, options, mode);
	    } else if (delimType.type === "large") {
	        return makeLargeDelim(delim, delimType.size, center, options, mode);
	    } else if (delimType.type === "stack") {
	        return makeStackedDelim(delim, height, center, options, mode);
	    }
	};

	/**
	 * Make a delimiter for use with `\left` and `\right`, given a height and depth
	 * of an expression that the delimiters surround.
	 */
	var makeLeftRightDelim = function(delim, height, depth, options, mode) {
	    // We always center \left/\right delimiters, so the axis is always shifted
	    var axisHeight =
	        fontMetrics.metrics.axisHeight * options.style.sizeMultiplier;

	    // Taken from TeX source, tex.web, function make_left_right
	    var delimiterFactor = 901;
	    var delimiterExtend = 5.0 / fontMetrics.metrics.ptPerEm;

	    var maxDistFromAxis = Math.max(
	        height - axisHeight, depth + axisHeight);

	    var totalHeight = Math.max(
	        // In real TeX, calculations are done using integral values which are
	        // 65536 per pt, or 655360 per em. So, the division here truncates in
	        // TeX but doesn't here, producing different results. If we wanted to
	        // exactly match TeX's calculation, we could do
	        //   Math.floor(655360 * maxDistFromAxis / 500) *
	        //    delimiterFactor / 655360
	        // (To see the difference, compare
	        //    x^{x^{\left(\rule{0.1em}{0.68em}\right)}}
	        // in TeX and KaTeX)
	        maxDistFromAxis / 500 * delimiterFactor,
	        2 * maxDistFromAxis - delimiterExtend);

	    // Finally, we defer to `makeCustomSizedDelim` with our calculated total
	    // height
	    return makeCustomSizedDelim(delim, totalHeight, true, options, mode);
	};

	module.exports = {
	    sizedDelim: makeSizedDelim,
	    customSizedDelim: makeCustomSizedDelim,
	    leftRightDelim: makeLeftRightDelim,
	};


/***/ },
/* 274 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * This file converts a parse tree into a cooresponding MathML tree. The main
	 * entry point is the `buildMathML` function, which takes a parse tree from the
	 * parser.
	 */

	var buildCommon = __webpack_require__(267);
	var fontMetrics = __webpack_require__(270);
	var mathMLTree = __webpack_require__(275);
	var ParseError = __webpack_require__(262);
	var symbols = __webpack_require__(272);
	var utils = __webpack_require__(269);

	var makeSpan = buildCommon.makeSpan;
	var fontMap = buildCommon.fontMap;

	/**
	 * Takes a symbol and converts it into a MathML text node after performing
	 * optional replacement from symbols.js.
	 */
	var makeText = function(text, mode) {
	    if (symbols[mode][text] && symbols[mode][text].replace) {
	        text = symbols[mode][text].replace;
	    }

	    return new mathMLTree.TextNode(text);
	};

	/**
	 * Returns the math variant as a string or null if none is required.
	 */
	var getVariant = function(group, options) {
	    var font = options.font;
	    if (!font) {
	        return null;
	    }

	    var mode = group.mode;
	    if (font === "mathit") {
	        return "italic";
	    }

	    var value = group.value;
	    if (utils.contains(["\\imath", "\\jmath"], value)) {
	        return null;
	    }

	    if (symbols[mode][value] && symbols[mode][value].replace) {
	        value = symbols[mode][value].replace;
	    }

	    var fontName = fontMap[font].fontName;
	    if (fontMetrics.getCharacterMetrics(value, fontName)) {
	        return fontMap[options.font].variant;
	    }

	    return null;
	};

	/**
	 * Functions for handling the different types of groups found in the parse
	 * tree. Each function should take a parse group and return a MathML node.
	 */
	var groupTypes = {};

	groupTypes.mathord = function(group, options) {
	    var node = new mathMLTree.MathNode(
	        "mi",
	        [makeText(group.value, group.mode)]);

	    var variant = getVariant(group, options);
	    if (variant) {
	        node.setAttribute("mathvariant", variant);
	    }
	    return node;
	};

	groupTypes.textord = function(group, options) {
	    var text = makeText(group.value, group.mode);

	    var variant = getVariant(group, options) || "normal";

	    var node;
	    if (/[0-9]/.test(group.value)) {
	        // TODO(kevinb) merge adjacent <mn> nodes
	        // do it as a post processing step
	        node = new mathMLTree.MathNode("mn", [text]);
	        if (options.font) {
	            node.setAttribute("mathvariant", variant);
	        }
	    } else {
	        node = new mathMLTree.MathNode("mi", [text]);
	        node.setAttribute("mathvariant", variant);
	    }

	    return node;
	};

	groupTypes.bin = function(group) {
	    var node = new mathMLTree.MathNode(
	        "mo", [makeText(group.value, group.mode)]);

	    return node;
	};

	groupTypes.rel = function(group) {
	    var node = new mathMLTree.MathNode(
	        "mo", [makeText(group.value, group.mode)]);

	    return node;
	};

	groupTypes.open = function(group) {
	    var node = new mathMLTree.MathNode(
	        "mo", [makeText(group.value, group.mode)]);

	    return node;
	};

	groupTypes.close = function(group) {
	    var node = new mathMLTree.MathNode(
	        "mo", [makeText(group.value, group.mode)]);

	    return node;
	};

	groupTypes.inner = function(group) {
	    var node = new mathMLTree.MathNode(
	        "mo", [makeText(group.value, group.mode)]);

	    return node;
	};

	groupTypes.punct = function(group) {
	    var node = new mathMLTree.MathNode(
	        "mo", [makeText(group.value, group.mode)]);

	    node.setAttribute("separator", "true");

	    return node;
	};

	groupTypes.ordgroup = function(group, options) {
	    var inner = buildExpression(group.value, options);

	    var node = new mathMLTree.MathNode("mrow", inner);

	    return node;
	};

	groupTypes.text = function(group, options) {
	    var inner = buildExpression(group.value.body, options);

	    var node = new mathMLTree.MathNode("mtext", inner);

	    return node;
	};

	groupTypes.color = function(group, options) {
	    var inner = buildExpression(group.value.value, options);

	    var node = new mathMLTree.MathNode("mstyle", inner);

	    node.setAttribute("mathcolor", group.value.color);

	    return node;
	};

	groupTypes.supsub = function(group, options) {
	    var children = [buildGroup(group.value.base, options)];

	    if (group.value.sub) {
	        children.push(buildGroup(group.value.sub, options));
	    }

	    if (group.value.sup) {
	        children.push(buildGroup(group.value.sup, options));
	    }

	    var nodeType;
	    if (!group.value.sub) {
	        nodeType = "msup";
	    } else if (!group.value.sup) {
	        nodeType = "msub";
	    } else {
	        nodeType = "msubsup";
	    }

	    var node = new mathMLTree.MathNode(nodeType, children);

	    return node;
	};

	groupTypes.genfrac = function(group, options) {
	    var node = new mathMLTree.MathNode(
	        "mfrac",
	        [buildGroup(group.value.numer, options),
	         buildGroup(group.value.denom, options)]);

	    if (!group.value.hasBarLine) {
	        node.setAttribute("linethickness", "0px");
	    }

	    if (group.value.leftDelim != null || group.value.rightDelim != null) {
	        var withDelims = [];

	        if (group.value.leftDelim != null) {
	            var leftOp = new mathMLTree.MathNode(
	                "mo", [new mathMLTree.TextNode(group.value.leftDelim)]);

	            leftOp.setAttribute("fence", "true");

	            withDelims.push(leftOp);
	        }

	        withDelims.push(node);

	        if (group.value.rightDelim != null) {
	            var rightOp = new mathMLTree.MathNode(
	                "mo", [new mathMLTree.TextNode(group.value.rightDelim)]);

	            rightOp.setAttribute("fence", "true");

	            withDelims.push(rightOp);
	        }

	        var outerNode = new mathMLTree.MathNode("mrow", withDelims);

	        return outerNode;
	    }

	    return node;
	};

	groupTypes.array = function(group, options) {
	    return new mathMLTree.MathNode(
	        "mtable", group.value.body.map(function(row) {
	            return new mathMLTree.MathNode(
	                "mtr", row.map(function(cell) {
	                    return new mathMLTree.MathNode(
	                        "mtd", [buildGroup(cell, options)]);
	                }));
	        }));
	};

	groupTypes.sqrt = function(group, options) {
	    var node;
	    if (group.value.index) {
	        node = new mathMLTree.MathNode(
	            "mroot", [
	                buildGroup(group.value.body, options),
	                buildGroup(group.value.index, options),
	            ]);
	    } else {
	        node = new mathMLTree.MathNode(
	            "msqrt", [buildGroup(group.value.body, options)]);
	    }

	    return node;
	};

	groupTypes.leftright = function(group, options) {
	    var inner = buildExpression(group.value.body, options);

	    if (group.value.left !== ".") {
	        var leftNode = new mathMLTree.MathNode(
	            "mo", [makeText(group.value.left, group.mode)]);

	        leftNode.setAttribute("fence", "true");

	        inner.unshift(leftNode);
	    }

	    if (group.value.right !== ".") {
	        var rightNode = new mathMLTree.MathNode(
	            "mo", [makeText(group.value.right, group.mode)]);

	        rightNode.setAttribute("fence", "true");

	        inner.push(rightNode);
	    }

	    var outerNode = new mathMLTree.MathNode("mrow", inner);

	    return outerNode;
	};

	groupTypes.accent = function(group, options) {
	    var accentNode = new mathMLTree.MathNode(
	        "mo", [makeText(group.value.accent, group.mode)]);

	    var node = new mathMLTree.MathNode(
	        "mover",
	        [buildGroup(group.value.base, options),
	         accentNode]);

	    node.setAttribute("accent", "true");

	    return node;
	};

	groupTypes.spacing = function(group) {
	    var node;

	    if (group.value === "\\ " || group.value === "\\space" ||
	        group.value === " " || group.value === "~") {
	        node = new mathMLTree.MathNode(
	            "mtext", [new mathMLTree.TextNode("\u00a0")]);
	    } else {
	        node = new mathMLTree.MathNode("mspace");

	        node.setAttribute(
	            "width", buildCommon.spacingFunctions[group.value].size);
	    }

	    return node;
	};

	groupTypes.op = function(group) {
	    var node;

	    // TODO(emily): handle big operators using the `largeop` attribute

	    if (group.value.symbol) {
	        // This is a symbol. Just add the symbol.
	        node = new mathMLTree.MathNode(
	            "mo", [makeText(group.value.body, group.mode)]);
	    } else {
	        // This is a text operator. Add all of the characters from the
	        // operator's name.
	        // TODO(emily): Add a space in the middle of some of these
	        // operators, like \limsup.
	        node = new mathMLTree.MathNode(
	            "mi", [new mathMLTree.TextNode(group.value.body.slice(1))]);
	    }

	    return node;
	};

	groupTypes.katex = function(group) {
	    var node = new mathMLTree.MathNode(
	        "mtext", [new mathMLTree.TextNode("KaTeX")]);

	    return node;
	};

	groupTypes.font = function(group, options) {
	    var font = group.value.font;
	    return buildGroup(group.value.body, options.withFont(font));
	};

	groupTypes.delimsizing = function(group) {
	    var children = [];

	    if (group.value.value !== ".") {
	        children.push(makeText(group.value.value, group.mode));
	    }

	    var node = new mathMLTree.MathNode("mo", children);

	    if (group.value.delimType === "open" ||
	        group.value.delimType === "close") {
	        // Only some of the delimsizing functions act as fences, and they
	        // return "open" or "close" delimTypes.
	        node.setAttribute("fence", "true");
	    } else {
	        // Explicitly disable fencing if it's not a fence, to override the
	        // defaults.
	        node.setAttribute("fence", "false");
	    }

	    return node;
	};

	groupTypes.styling = function(group, options) {
	    var inner = buildExpression(group.value.value, options);

	    var node = new mathMLTree.MathNode("mstyle", inner);

	    var styleAttributes = {
	        "display": ["0", "true"],
	        "text": ["0", "false"],
	        "script": ["1", "false"],
	        "scriptscript": ["2", "false"],
	    };

	    var attr = styleAttributes[group.value.style];

	    node.setAttribute("scriptlevel", attr[0]);
	    node.setAttribute("displaystyle", attr[1]);

	    return node;
	};

	groupTypes.sizing = function(group, options) {
	    var inner = buildExpression(group.value.value, options);

	    var node = new mathMLTree.MathNode("mstyle", inner);

	    // TODO(emily): This doesn't produce the correct size for nested size
	    // changes, because we don't keep state of what style we're currently
	    // in, so we can't reset the size to normal before changing it.  Now
	    // that we're passing an options parameter we should be able to fix
	    // this.
	    node.setAttribute(
	        "mathsize", buildCommon.sizingMultiplier[group.value.size] + "em");

	    return node;
	};

	groupTypes.overline = function(group, options) {
	    var operator = new mathMLTree.MathNode(
	        "mo", [new mathMLTree.TextNode("\u203e")]);
	    operator.setAttribute("stretchy", "true");

	    var node = new mathMLTree.MathNode(
	        "mover",
	        [buildGroup(group.value.body, options),
	         operator]);
	    node.setAttribute("accent", "true");

	    return node;
	};

	groupTypes.underline = function(group, options) {
	    var operator = new mathMLTree.MathNode(
	        "mo", [new mathMLTree.TextNode("\u203e")]);
	    operator.setAttribute("stretchy", "true");

	    var node = new mathMLTree.MathNode(
	        "munder",
	        [buildGroup(group.value.body, options),
	         operator]);
	    node.setAttribute("accentunder", "true");

	    return node;
	};

	groupTypes.rule = function(group) {
	    // TODO(emily): Figure out if there's an actual way to draw black boxes
	    // in MathML.
	    var node = new mathMLTree.MathNode("mrow");

	    return node;
	};

	groupTypes.llap = function(group, options) {
	    var node = new mathMLTree.MathNode(
	        "mpadded", [buildGroup(group.value.body, options)]);

	    node.setAttribute("lspace", "-1width");
	    node.setAttribute("width", "0px");

	    return node;
	};

	groupTypes.rlap = function(group, options) {
	    var node = new mathMLTree.MathNode(
	        "mpadded", [buildGroup(group.value.body, options)]);

	    node.setAttribute("width", "0px");

	    return node;
	};

	groupTypes.phantom = function(group, options, prev) {
	    var inner = buildExpression(group.value.value, options);
	    return new mathMLTree.MathNode("mphantom", inner);
	};

	/**
	 * Takes a list of nodes, builds them, and returns a list of the generated
	 * MathML nodes. A little simpler than the HTML version because we don't do any
	 * previous-node handling.
	 */
	var buildExpression = function(expression, options) {
	    var groups = [];
	    for (var i = 0; i < expression.length; i++) {
	        var group = expression[i];
	        groups.push(buildGroup(group, options));
	    }
	    return groups;
	};

	/**
	 * Takes a group from the parser and calls the appropriate groupTypes function
	 * on it to produce a MathML node.
	 */
	var buildGroup = function(group, options) {
	    if (!group) {
	        return new mathMLTree.MathNode("mrow");
	    }

	    if (groupTypes[group.type]) {
	        // Call the groupTypes function
	        return groupTypes[group.type](group, options);
	    } else {
	        throw new ParseError(
	            "Got group of unknown type: '" + group.type + "'");
	    }
	};

	/**
	 * Takes a full parse tree and settings and builds a MathML representation of
	 * it. In particular, we put the elements from building the parse tree into a
	 * <semantics> tag so we can also include that TeX source as an annotation.
	 *
	 * Note that we actually return a domTree element with a `<math>` inside it so
	 * we can do appropriate styling.
	 */
	var buildMathML = function(tree, texExpression, options) {
	    var expression = buildExpression(tree, options);

	    // Wrap up the expression in an mrow so it is presented in the semantics
	    // tag correctly.
	    var wrapper = new mathMLTree.MathNode("mrow", expression);

	    // Build a TeX annotation of the source
	    var annotation = new mathMLTree.MathNode(
	        "annotation", [new mathMLTree.TextNode(texExpression)]);

	    annotation.setAttribute("encoding", "application/x-tex");

	    var semantics = new mathMLTree.MathNode(
	        "semantics", [wrapper, annotation]);

	    var math = new mathMLTree.MathNode("math", [semantics]);

	    // You can't style <math> nodes, so we wrap the node in a span.
	    return makeSpan(["katex-mathml"], [math]);
	};

	module.exports = buildMathML;


/***/ },
/* 275 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * These objects store data about MathML nodes. This is the MathML equivalent
	 * of the types in domTree.js. Since MathML handles its own rendering, and
	 * since we're mainly using MathML to improve accessibility, we don't manage
	 * any of the styling state that the plain DOM nodes do.
	 *
	 * The `toNode` and `toMarkup` functions work simlarly to how they do in
	 * domTree.js, creating namespaced DOM nodes and HTML text markup respectively.
	 */

	var utils = __webpack_require__(269);

	/**
	 * This node represents a general purpose MathML node of any type. The
	 * constructor requires the type of node to create (for example, `"mo"` or
	 * `"mspace"`, corresponding to `<mo>` and `<mspace>` tags).
	 */
	function MathNode(type, children) {
	    this.type = type;
	    this.attributes = {};
	    this.children = children || [];
	}

	/**
	 * Sets an attribute on a MathML node. MathML depends on attributes to convey a
	 * semantic content, so this is used heavily.
	 */
	MathNode.prototype.setAttribute = function(name, value) {
	    this.attributes[name] = value;
	};

	/**
	 * Converts the math node into a MathML-namespaced DOM element.
	 */
	MathNode.prototype.toNode = function() {
	    var node = document.createElementNS(
	        "http://www.w3.org/1998/Math/MathML", this.type);

	    for (var attr in this.attributes) {
	        if (Object.prototype.hasOwnProperty.call(this.attributes, attr)) {
	            node.setAttribute(attr, this.attributes[attr]);
	        }
	    }

	    for (var i = 0; i < this.children.length; i++) {
	        node.appendChild(this.children[i].toNode());
	    }

	    return node;
	};

	/**
	 * Converts the math node into an HTML markup string.
	 */
	MathNode.prototype.toMarkup = function() {
	    var markup = "<" + this.type;

	    // Add the attributes
	    for (var attr in this.attributes) {
	        if (Object.prototype.hasOwnProperty.call(this.attributes, attr)) {
	            markup += " " + attr + "=\"";
	            markup += utils.escape(this.attributes[attr]);
	            markup += "\"";
	        }
	    }

	    markup += ">";

	    for (var i = 0; i < this.children.length; i++) {
	        markup += this.children[i].toMarkup();
	    }

	    markup += "</" + this.type + ">";

	    return markup;
	};

	/**
	 * This node represents a piece of text.
	 */
	function TextNode(text) {
	    this.text = text;
	}

	/**
	 * Converts the text node into a DOM text node.
	 */
	TextNode.prototype.toNode = function() {
	    return document.createTextNode(this.text);
	};

	/**
	 * Converts the text node into HTML markup (which is just the text itself).
	 */
	TextNode.prototype.toMarkup = function() {
	    return utils.escape(this.text);
	};

	module.exports = {
	    MathNode: MathNode,
	    TextNode: TextNode,
	};


/***/ },
/* 276 */
/***/ function(module, exports) {

	/**
	 * This file contains information about the options that the Parser carries
	 * around with it while parsing. Data is held in an `Options` object, and when
	 * recursing, a new `Options` object can be created with the `.with*` and
	 * `.reset` functions.
	 */

	/**
	 * This is the main options class. It contains the style, size, color, and font
	 * of the current parse level. It also contains the style and size of the parent
	 * parse level, so size changes can be handled efficiently.
	 *
	 * Each of the `.with*` and `.reset` functions passes its current style and size
	 * as the parentStyle and parentSize of the new options class, so parent
	 * handling is taken care of automatically.
	 */
	function Options(data) {
	    this.style = data.style;
	    this.color = data.color;
	    this.size = data.size;
	    this.phantom = data.phantom;
	    this.font = data.font;

	    if (data.parentStyle === undefined) {
	        this.parentStyle = data.style;
	    } else {
	        this.parentStyle = data.parentStyle;
	    }

	    if (data.parentSize === undefined) {
	        this.parentSize = data.size;
	    } else {
	        this.parentSize = data.parentSize;
	    }
	}

	/**
	 * Returns a new options object with the same properties as "this".  Properties
	 * from "extension" will be copied to the new options object.
	 */
	Options.prototype.extend = function(extension) {
	    var data = {
	        style: this.style,
	        size: this.size,
	        color: this.color,
	        parentStyle: this.style,
	        parentSize: this.size,
	        phantom: this.phantom,
	        font: this.font,
	    };

	    for (var key in extension) {
	        if (extension.hasOwnProperty(key)) {
	            data[key] = extension[key];
	        }
	    }

	    return new Options(data);
	};

	/**
	 * Create a new options object with the given style.
	 */
	Options.prototype.withStyle = function(style) {
	    return this.extend({
	        style: style,
	    });
	};

	/**
	 * Create a new options object with the given size.
	 */
	Options.prototype.withSize = function(size) {
	    return this.extend({
	        size: size,
	    });
	};

	/**
	 * Create a new options object with the given color.
	 */
	Options.prototype.withColor = function(color) {
	    return this.extend({
	        color: color,
	    });
	};

	/**
	 * Create a new options object with "phantom" set to true.
	 */
	Options.prototype.withPhantom = function() {
	    return this.extend({
	        phantom: true,
	    });
	};

	/**
	 * Create a new options objects with the give font.
	 */
	Options.prototype.withFont = function(font) {
	    return this.extend({
	        font: font,
	    });
	};

	/**
	 * Create a new options object with the same style, size, and color. This is
	 * used so that parent style and size changes are handled correctly.
	 */
	Options.prototype.reset = function() {
	    return this.extend({});
	};

	/**
	 * A map of color names to CSS colors.
	 * TODO(emily): Remove this when we have real macros
	 */
	var colorMap = {
	    "katex-blue": "#6495ed",
	    "katex-orange": "#ffa500",
	    "katex-pink": "#ff00af",
	    "katex-red": "#df0030",
	    "katex-green": "#28ae7b",
	    "katex-gray": "gray",
	    "katex-purple": "#9d38bd",
	    "katex-blueA": "#c7e9f1",
	    "katex-blueB": "#9cdceb",
	    "katex-blueC": "#58c4dd",
	    "katex-blueD": "#29abca",
	    "katex-blueE": "#1c758a",
	    "katex-tealA": "#acead7",
	    "katex-tealB": "#76ddc0",
	    "katex-tealC": "#5cd0b3",
	    "katex-tealD": "#55c1a7",
	    "katex-tealE": "#49a88f",
	    "katex-greenA": "#c9e2ae",
	    "katex-greenB": "#a6cf8c",
	    "katex-greenC": "#83c167",
	    "katex-greenD": "#77b05d",
	    "katex-greenE": "#699c52",
	    "katex-goldA": "#f7c797",
	    "katex-goldB": "#f9b775",
	    "katex-goldC": "#f0ac5f",
	    "katex-goldD": "#e1a158",
	    "katex-goldE": "#c78d46",
	    "katex-redA": "#f7a1a3",
	    "katex-redB": "#ff8080",
	    "katex-redC": "#fc6255",
	    "katex-redD": "#e65a4c",
	    "katex-redE": "#cf5044",
	    "katex-maroonA": "#ecabc1",
	    "katex-maroonB": "#ec92ab",
	    "katex-maroonC": "#c55f73",
	    "katex-maroonD": "#a24d61",
	    "katex-maroonE": "#94424f",
	    "katex-purpleA": "#caa3e8",
	    "katex-purpleB": "#b189c6",
	    "katex-purpleC": "#9a72ac",
	    "katex-purpleD": "#715582",
	    "katex-purpleE": "#644172",
	    "katex-mintA": "#f5f9e8",
	    "katex-mintB": "#edf2df",
	    "katex-mintC": "#e0e5cc",
	    "katex-grayA": "#fdfdfd",
	    "katex-grayB": "#f7f7f7",
	    "katex-grayC": "#eeeeee",
	    "katex-grayD": "#dddddd",
	    "katex-grayE": "#cccccc",
	    "katex-grayF": "#aaaaaa",
	    "katex-grayG": "#999999",
	    "katex-grayH": "#555555",
	    "katex-grayI": "#333333",
	    "katex-kaBlue": "#314453",
	    "katex-kaGreen": "#639b24",
	};

	/**
	 * Gets the CSS color of the current options object, accounting for the
	 * `colorMap`.
	 */
	Options.prototype.getColor = function() {
	    if (this.phantom) {
	        return "transparent";
	    } else {
	        return colorMap[this.color] || this.color;
	    }
	};

	module.exports = Options;


/***/ },
/* 277 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Provides a single function for parsing an expression using a Parser
	 * TODO(emily): Remove this
	 */

	var Parser = __webpack_require__(278);

	/**
	 * Parses an expression using a Parser, then returns the parsed result.
	 */
	var parseTree = function(toParse, settings) {
	    var parser = new Parser(toParse, settings);

	    return parser.parse();
	};

	module.exports = parseTree;


/***/ },
/* 278 */
/***/ function(module, exports, __webpack_require__) {

	/* eslint no-constant-condition:0 */
	var functions = __webpack_require__(279);
	var environments = __webpack_require__(280);
	var Lexer = __webpack_require__(282);
	var symbols = __webpack_require__(272);
	var utils = __webpack_require__(269);

	var parseData = __webpack_require__(281);
	var ParseError = __webpack_require__(262);

	/**
	 * This file contains the parser used to parse out a TeX expression from the
	 * input. Since TeX isn't context-free, standard parsers don't work particularly
	 * well.
	 *
	 * The strategy of this parser is as such:
	 *
	 * The main functions (the `.parse...` ones) take a position in the current
	 * parse string to parse tokens from. The lexer (found in Lexer.js, stored at
	 * this.lexer) also supports pulling out tokens at arbitrary places. When
	 * individual tokens are needed at a position, the lexer is called to pull out a
	 * token, which is then used.
	 *
	 * The parser has a property called "mode" indicating the mode that
	 * the parser is currently in. Currently it has to be one of "math" or
	 * "text", which denotes whether the current environment is a math-y
	 * one or a text-y one (e.g. inside \text). Currently, this serves to
	 * limit the functions which can be used in text mode.
	 *
	 * The main functions then return an object which contains the useful data that
	 * was parsed at its given point, and a new position at the end of the parsed
	 * data. The main functions can call each other and continue the parsing by
	 * using the returned position as a new starting point.
	 *
	 * There are also extra `.handle...` functions, which pull out some reused
	 * functionality into self-contained functions.
	 *
	 * The earlier functions return ParseNodes.
	 * The later functions (which are called deeper in the parse) sometimes return
	 * ParseFuncOrArgument, which contain a ParseNode as well as some data about
	 * whether the parsed object is a function which is missing some arguments, or a
	 * standalone object which can be used as an argument to another function.
	 */

	/**
	 * Main Parser class
	 */
	function Parser(input, settings) {
	    // Make a new lexer
	    this.lexer = new Lexer(input);
	    // Store the settings for use in parsing
	    this.settings = settings;
	}

	var ParseNode = parseData.ParseNode;

	/**
	 * An initial function (without its arguments), or an argument to a function.
	 * The `result` argument should be a ParseNode.
	 */
	function ParseFuncOrArgument(result, isFunction) {
	    this.result = result;
	    // Is this a function (i.e. is it something defined in functions.js)?
	    this.isFunction = isFunction;
	}

	/**
	 * Checks a result to make sure it has the right type, and throws an
	 * appropriate error otherwise.
	 *
	 * @param {boolean=} consume whether to consume the expected token,
	 *                           defaults to true
	 */
	Parser.prototype.expect = function(text, consume) {
	    if (this.nextToken.text !== text) {
	        throw new ParseError(
	            "Expected '" + text + "', got '" + this.nextToken.text + "'",
	            this.lexer, this.nextToken.position
	        );
	    }
	    if (consume !== false) {
	        this.consume();
	    }
	};

	/**
	 * Considers the current look ahead token as consumed,
	 * and fetches the one after that as the new look ahead.
	 */
	Parser.prototype.consume = function() {
	    this.pos = this.nextToken.position;
	    this.nextToken = this.lexer.lex(this.pos, this.mode);
	};

	/**
	 * Main parsing function, which parses an entire input.
	 *
	 * @return {?Array.<ParseNode>}
	 */
	Parser.prototype.parse = function() {
	    // Try to parse the input
	    this.mode = "math";
	    this.pos = 0;
	    this.nextToken = this.lexer.lex(this.pos, this.mode);
	    var parse = this.parseInput();
	    return parse;
	};

	/**
	 * Parses an entire input tree.
	 */
	Parser.prototype.parseInput = function() {
	    // Parse an expression
	    var expression = this.parseExpression(false);
	    // If we succeeded, make sure there's an EOF at the end
	    this.expect("EOF", false);
	    return expression;
	};

	var endOfExpression = ["}", "\\end", "\\right", "&", "\\\\", "\\cr"];

	/**
	 * Parses an "expression", which is a list of atoms.
	 *
	 * @param {boolean} breakOnInfix Should the parsing stop when we hit infix
	 *                  nodes? This happens when functions have higher precendence
	 *                  than infix nodes in implicit parses.
	 *
	 * @param {?string} breakOnToken The token that the expression should end with,
	 *                  or `null` if something else should end the expression.
	 *
	 * @return {ParseNode}
	 */
	Parser.prototype.parseExpression = function(breakOnInfix, breakOnToken) {
	    var body = [];
	    // Keep adding atoms to the body until we can't parse any more atoms (either
	    // we reached the end, a }, or a \right)
	    while (true) {
	        var lex = this.nextToken;
	        var pos = this.pos;
	        if (endOfExpression.indexOf(lex.text) !== -1) {
	            break;
	        }
	        if (breakOnToken && lex.text === breakOnToken) {
	            break;
	        }
	        var atom = this.parseAtom();
	        if (!atom) {
	            if (!this.settings.throwOnError && lex.text[0] === "\\") {
	                var errorNode = this.handleUnsupportedCmd();
	                body.push(errorNode);

	                pos = lex.position;
	                continue;
	            }

	            break;
	        }
	        if (breakOnInfix && atom.type === "infix") {
	            // rewind so we can parse the infix atom again
	            this.pos = pos;
	            this.nextToken = lex;
	            break;
	        }
	        body.push(atom);
	    }
	    return this.handleInfixNodes(body);
	};

	/**
	 * Rewrites infix operators such as \over with corresponding commands such
	 * as \frac.
	 *
	 * There can only be one infix operator per group.  If there's more than one
	 * then the expression is ambiguous.  This can be resolved by adding {}.
	 *
	 * @returns {Array}
	 */
	Parser.prototype.handleInfixNodes = function(body) {
	    var overIndex = -1;
	    var funcName;

	    for (var i = 0; i < body.length; i++) {
	        var node = body[i];
	        if (node.type === "infix") {
	            if (overIndex !== -1) {
	                throw new ParseError("only one infix operator per group",
	                    this.lexer, -1);
	            }
	            overIndex = i;
	            funcName = node.value.replaceWith;
	        }
	    }

	    if (overIndex !== -1) {
	        var numerNode;
	        var denomNode;

	        var numerBody = body.slice(0, overIndex);
	        var denomBody = body.slice(overIndex + 1);

	        if (numerBody.length === 1 && numerBody[0].type === "ordgroup") {
	            numerNode = numerBody[0];
	        } else {
	            numerNode = new ParseNode("ordgroup", numerBody, this.mode);
	        }

	        if (denomBody.length === 1 && denomBody[0].type === "ordgroup") {
	            denomNode = denomBody[0];
	        } else {
	            denomNode = new ParseNode("ordgroup", denomBody, this.mode);
	        }

	        var value = this.callFunction(
	            funcName, [numerNode, denomNode], null);
	        return [new ParseNode(value.type, value, this.mode)];
	    } else {
	        return body;
	    }
	};

	// The greediness of a superscript or subscript
	var SUPSUB_GREEDINESS = 1;

	/**
	 * Handle a subscript or superscript with nice errors.
	 */
	Parser.prototype.handleSupSubscript = function(name) {
	    var symbol = this.nextToken.text;
	    var symPos = this.pos;
	    this.consume();
	    var group = this.parseGroup();

	    if (!group) {
	        if (!this.settings.throwOnError && this.nextToken.text[0] === "\\") {
	            return this.handleUnsupportedCmd();
	        } else {
	            throw new ParseError(
	                "Expected group after '" + symbol + "'",
	                this.lexer,
	                symPos + 1
	            );
	        }
	    } else if (group.isFunction) {
	        // ^ and _ have a greediness, so handle interactions with functions'
	        // greediness
	        var funcGreediness = functions[group.result].greediness;
	        if (funcGreediness > SUPSUB_GREEDINESS) {
	            return this.parseFunction(group);
	        } else {
	            throw new ParseError(
	                "Got function '" + group.result + "' with no arguments " +
	                    "as " + name,
	                this.lexer, symPos + 1);
	        }
	    } else {
	        return group.result;
	    }
	};

	/**
	 * Converts the textual input of an unsupported command into a text node
	 * contained within a color node whose color is determined by errorColor
	 */
	Parser.prototype.handleUnsupportedCmd = function() {
	    var text = this.nextToken.text;
	    var textordArray = [];

	    for (var i = 0; i < text.length; i++) {
	        textordArray.push(new ParseNode("textord", text[i], "text"));
	    }

	    var textNode = new ParseNode(
	        "text",
	        {
	            body: textordArray,
	            type: "text",
	        },
	        this.mode);

	    var colorNode = new ParseNode(
	        "color",
	        {
	            color: this.settings.errorColor,
	            value: [textNode],
	            type: "color",
	        },
	        this.mode);

	    this.consume();
	    return colorNode;
	};

	/**
	 * Parses a group with optional super/subscripts.
	 *
	 * @return {?ParseNode}
	 */
	Parser.prototype.parseAtom = function() {
	    // The body of an atom is an implicit group, so that things like
	    // \left(x\right)^2 work correctly.
	    var base = this.parseImplicitGroup();

	    // In text mode, we don't have superscripts or subscripts
	    if (this.mode === "text") {
	        return base;
	    }

	    // Note that base may be empty (i.e. null) at this point.

	    var superscript;
	    var subscript;
	    while (true) {
	        // Lex the first token
	        var lex = this.nextToken;

	        if (lex.text === "\\limits" || lex.text === "\\nolimits") {
	            // We got a limit control
	            if (!base || base.type !== "op") {
	                throw new ParseError(
	                    "Limit controls must follow a math operator",
	                    this.lexer, this.pos);
	            } else {
	                var limits = lex.text === "\\limits";
	                base.value.limits = limits;
	                base.value.alwaysHandleSupSub = true;
	            }
	            this.consume();
	        } else if (lex.text === "^") {
	            // We got a superscript start
	            if (superscript) {
	                throw new ParseError(
	                    "Double superscript", this.lexer, this.pos);
	            }
	            superscript = this.handleSupSubscript("superscript");
	        } else if (lex.text === "_") {
	            // We got a subscript start
	            if (subscript) {
	                throw new ParseError(
	                    "Double subscript", this.lexer, this.pos);
	            }
	            subscript = this.handleSupSubscript("subscript");
	        } else if (lex.text === "'") {
	            // We got a prime
	            var prime = new ParseNode("textord", "\\prime", this.mode);

	            // Many primes can be grouped together, so we handle this here
	            var primes = [prime];
	            this.consume();
	            // Keep lexing tokens until we get something that's not a prime
	            while (this.nextToken.text === "'") {
	                // For each one, add another prime to the list
	                primes.push(prime);
	                this.consume();
	            }
	            // Put them into an ordgroup as the superscript
	            superscript = new ParseNode("ordgroup", primes, this.mode);
	        } else {
	            // If it wasn't ^, _, or ', stop parsing super/subscripts
	            break;
	        }
	    }

	    if (superscript || subscript) {
	        // If we got either a superscript or subscript, create a supsub
	        return new ParseNode("supsub", {
	            base: base,
	            sup: superscript,
	            sub: subscript,
	        }, this.mode);
	    } else {
	        // Otherwise return the original body
	        return base;
	    }
	};

	// A list of the size-changing functions, for use in parseImplicitGroup
	var sizeFuncs = [
	    "\\tiny", "\\scriptsize", "\\footnotesize", "\\small", "\\normalsize",
	    "\\large", "\\Large", "\\LARGE", "\\huge", "\\Huge",
	];

	// A list of the style-changing functions, for use in parseImplicitGroup
	var styleFuncs = [
	    "\\displaystyle", "\\textstyle", "\\scriptstyle", "\\scriptscriptstyle",
	];

	/**
	 * Parses an implicit group, which is a group that starts at the end of a
	 * specified, and ends right before a higher explicit group ends, or at EOL. It
	 * is used for functions that appear to affect the current style, like \Large or
	 * \textrm, where instead of keeping a style we just pretend that there is an
	 * implicit grouping after it until the end of the group. E.g.
	 *   small text {\Large large text} small text again
	 * It is also used for \left and \right to get the correct grouping.
	 *
	 * @return {?ParseNode}
	 */
	Parser.prototype.parseImplicitGroup = function() {
	    var start = this.parseSymbol();

	    if (start == null) {
	        // If we didn't get anything we handle, fall back to parseFunction
	        return this.parseFunction();
	    }

	    var func = start.result;
	    var body;

	    if (func === "\\left") {
	        // If we see a left:
	        // Parse the entire left function (including the delimiter)
	        var left = this.parseFunction(start);
	        // Parse out the implicit body
	        body = this.parseExpression(false);
	        // Check the next token
	        this.expect("\\right", false);
	        var right = this.parseFunction();
	        return new ParseNode("leftright", {
	            body: body,
	            left: left.value.value,
	            right: right.value.value,
	        }, this.mode);
	    } else if (func === "\\begin") {
	        // begin...end is similar to left...right
	        var begin = this.parseFunction(start);
	        var envName = begin.value.name;
	        if (!environments.hasOwnProperty(envName)) {
	            throw new ParseError(
	                "No such environment: " + envName,
	                this.lexer, begin.value.namepos);
	        }
	        // Build the environment object. Arguments and other information will
	        // be made available to the begin and end methods using properties.
	        var env = environments[envName];
	        var args = this.parseArguments("\\begin{" + envName + "}", env);
	        var context = {
	            mode: this.mode,
	            envName: envName,
	            parser: this,
	            lexer: this.lexer,
	            positions: args.pop(),
	        };
	        var result = env.handler(context, args);
	        this.expect("\\end", false);
	        var end = this.parseFunction();
	        if (end.value.name !== envName) {
	            throw new ParseError(
	                "Mismatch: \\begin{" + envName + "} matched " +
	                "by \\end{" + end.value.name + "}",
	                this.lexer /* , end.value.namepos */);
	            // TODO: Add position to the above line and adjust test case,
	            // requires #385 to get merged first
	        }
	        result.position = end.position;
	        return result;
	    } else if (utils.contains(sizeFuncs, func)) {
	        // If we see a sizing function, parse out the implict body
	        body = this.parseExpression(false);
	        return new ParseNode("sizing", {
	            // Figure out what size to use based on the list of functions above
	            size: "size" + (utils.indexOf(sizeFuncs, func) + 1),
	            value: body,
	        }, this.mode);
	    } else if (utils.contains(styleFuncs, func)) {
	        // If we see a styling function, parse out the implict body
	        body = this.parseExpression(true);
	        return new ParseNode("styling", {
	            // Figure out what style to use by pulling out the style from
	            // the function name
	            style: func.slice(1, func.length - 5),
	            value: body,
	        }, this.mode);
	    } else {
	        // Defer to parseFunction if it's not a function we handle
	        return this.parseFunction(start);
	    }
	};

	/**
	 * Parses an entire function, including its base and all of its arguments.
	 * The base might either have been parsed already, in which case
	 * it is provided as an argument, or it's the next group in the input.
	 *
	 * @param {ParseFuncOrArgument=} baseGroup optional as described above
	 * @return {?ParseNode}
	 */
	Parser.prototype.parseFunction = function(baseGroup) {
	    if (!baseGroup) {
	        baseGroup = this.parseGroup();
	    }

	    if (baseGroup) {
	        if (baseGroup.isFunction) {
	            var func = baseGroup.result;
	            var funcData = functions[func];
	            if (this.mode === "text" && !funcData.allowedInText) {
	                throw new ParseError(
	                    "Can't use function '" + func + "' in text mode",
	                    this.lexer, baseGroup.position);
	            }

	            var args = this.parseArguments(func, funcData);
	            var result = this.callFunction(func, args, args.pop());
	            return new ParseNode(result.type, result, this.mode);
	        } else {
	            return baseGroup.result;
	        }
	    } else {
	        return null;
	    }
	};

	/**
	 * Call a function handler with a suitable context and arguments.
	 */
	Parser.prototype.callFunction = function(name, args, positions) {
	    var context = {
	        funcName: name,
	        parser: this,
	        lexer: this.lexer,
	        positions: positions,
	    };
	    return functions[name].handler(context, args);
	};

	/**
	 * Parses the arguments of a function or environment
	 *
	 * @param {string} func  "\name" or "\begin{name}"
	 * @param {{numArgs:number,numOptionalArgs:number|undefined}} funcData
	 * @return the array of arguments, with the list of positions as last element
	 */
	Parser.prototype.parseArguments = function(func, funcData) {
	    var totalArgs = funcData.numArgs + funcData.numOptionalArgs;
	    if (totalArgs === 0) {
	        return [[this.pos]];
	    }

	    var baseGreediness = funcData.greediness;
	    var positions = [this.pos];
	    var args = [];

	    for (var i = 0; i < totalArgs; i++) {
	        var argType = funcData.argTypes && funcData.argTypes[i];
	        var arg;
	        if (i < funcData.numOptionalArgs) {
	            if (argType) {
	                arg = this.parseSpecialGroup(argType, true);
	            } else {
	                arg = this.parseOptionalGroup();
	            }
	            if (!arg) {
	                args.push(null);
	                positions.push(this.pos);
	                continue;
	            }
	        } else {
	            if (argType) {
	                arg = this.parseSpecialGroup(argType);
	            } else {
	                arg = this.parseGroup();
	            }
	            if (!arg) {
	                if (!this.settings.throwOnError &&
	                    this.nextToken.text[0] === "\\") {
	                    arg = new ParseFuncOrArgument(
	                        this.handleUnsupportedCmd(this.nextToken.text),
	                        false);
	                } else {
	                    throw new ParseError(
	                        "Expected group after '" + func + "'",
	                        this.lexer, this.pos);
	                }
	            }
	        }
	        var argNode;
	        if (arg.isFunction) {
	            var argGreediness =
	                functions[arg.result].greediness;
	            if (argGreediness > baseGreediness) {
	                argNode = this.parseFunction(arg);
	            } else {
	                throw new ParseError(
	                    "Got function '" + arg.result + "' as " +
	                    "argument to '" + func + "'",
	                    this.lexer, this.pos - 1);
	            }
	        } else {
	            argNode = arg.result;
	        }
	        args.push(argNode);
	        positions.push(this.pos);
	    }

	    args.push(positions);

	    return args;
	};


	/**
	 * Parses a group when the mode is changing. Takes a position, a new mode, and
	 * an outer mode that is used to parse the outside.
	 *
	 * @return {?ParseFuncOrArgument}
	 */
	Parser.prototype.parseSpecialGroup = function(innerMode, optional) {
	    var outerMode = this.mode;
	    // Handle `original` argTypes
	    if (innerMode === "original") {
	        innerMode = outerMode;
	    }

	    if (innerMode === "color" || innerMode === "size") {
	        // color and size modes are special because they should have braces and
	        // should only lex a single symbol inside
	        var openBrace = this.nextToken;
	        if (optional && openBrace.text !== "[") {
	            // optional arguments should return null if they don't exist
	            return null;
	        }
	        // The call to expect will lex the token after the '{' in inner mode
	        this.mode = innerMode;
	        this.expect(optional ? "[" : "{");
	        var inner = this.nextToken;
	        this.mode = outerMode;
	        var data;
	        if (innerMode === "color") {
	            data = inner.text;
	        } else {
	            data = inner.data;
	        }
	        this.consume(); // consume the token stored in inner
	        this.expect(optional ? "]" : "}");
	        return new ParseFuncOrArgument(
	            new ParseNode(innerMode, data, outerMode),
	            false);
	    } else if (innerMode === "text") {
	        // text mode is special because it should ignore the whitespace before
	        // it
	        var whitespace = this.lexer.lex(this.pos, "whitespace");
	        this.pos = whitespace.position;
	    }

	    // By the time we get here, innerMode is one of "text" or "math".
	    // We switch the mode of the parser, recurse, then restore the old mode.
	    this.mode = innerMode;
	    this.nextToken = this.lexer.lex(this.pos, innerMode);
	    var res;
	    if (optional) {
	        res = this.parseOptionalGroup();
	    } else {
	        res = this.parseGroup();
	    }
	    this.mode = outerMode;
	    this.nextToken = this.lexer.lex(this.pos, outerMode);
	    return res;
	};

	/**
	 * Parses a group, which is either a single nucleus (like "x") or an expression
	 * in braces (like "{x+y}")
	 *
	 * @return {?ParseFuncOrArgument}
	 */
	Parser.prototype.parseGroup = function() {
	    // Try to parse an open brace
	    if (this.nextToken.text === "{") {
	        // If we get a brace, parse an expression
	        this.consume();
	        var expression = this.parseExpression(false);
	        // Make sure we get a close brace
	        this.expect("}");
	        return new ParseFuncOrArgument(
	            new ParseNode("ordgroup", expression, this.mode),
	            false);
	    } else {
	        // Otherwise, just return a nucleus
	        return this.parseSymbol();
	    }
	};

	/**
	 * Parses a group, which is an expression in brackets (like "[x+y]")
	 *
	 * @return {?ParseFuncOrArgument}
	 */
	Parser.prototype.parseOptionalGroup = function() {
	    // Try to parse an open bracket
	    if (this.nextToken.text === "[") {
	        // If we get a brace, parse an expression
	        this.consume();
	        var expression = this.parseExpression(false, "]");
	        // Make sure we get a close bracket
	        this.expect("]");
	        return new ParseFuncOrArgument(
	            new ParseNode("ordgroup", expression, this.mode),
	            false);
	    } else {
	        // Otherwise, return null,
	        return null;
	    }
	};

	/**
	 * Parse a single symbol out of the string. Here, we handle both the functions
	 * we have defined, as well as the single character symbols
	 *
	 * @return {?ParseFuncOrArgument}
	 */
	Parser.prototype.parseSymbol = function() {
	    var nucleus = this.nextToken;

	    if (functions[nucleus.text]) {
	        this.consume();
	        // If there exists a function with this name, we return the function and
	        // say that it is a function.
	        return new ParseFuncOrArgument(
	            nucleus.text,
	            true);
	    } else if (symbols[this.mode][nucleus.text]) {
	        this.consume();
	        // Otherwise if this is a no-argument function, find the type it
	        // corresponds to in the symbols map
	        return new ParseFuncOrArgument(
	            new ParseNode(symbols[this.mode][nucleus.text].group,
	                          nucleus.text, this.mode),
	            false);
	    } else {
	        return null;
	    }
	};

	Parser.prototype.ParseNode = ParseNode;

	module.exports = Parser;


/***/ },
/* 279 */
/***/ function(module, exports, __webpack_require__) {

	var utils = __webpack_require__(269);
	var ParseError = __webpack_require__(262);

	/* This file contains a list of functions that we parse, identified by
	 * the calls to defineFunction.
	 *
	 * The first argument to defineFunction is a single name or a list of names.
	 * All functions named in such a list will share a single implementation.
	 *
	 * Each declared function can have associated properties, which
	 * include the following:
	 *
	 *  - numArgs: The number of arguments the function takes.
	 *             If this is the only property, it can be passed as a number
	 *             instead of an element of a properties object.
	 *  - argTypes: (optional) An array corresponding to each argument of the
	 *              function, giving the type of argument that should be parsed. Its
	 *              length should be equal to `numArgs + numOptionalArgs`. Valid
	 *              types:
	 *               - "size": A size-like thing, such as "1em" or "5ex"
	 *               - "color": An html color, like "#abc" or "blue"
	 *               - "original": The same type as the environment that the
	 *                             function being parsed is in (e.g. used for the
	 *                             bodies of functions like \color where the first
	 *                             argument is special and the second argument is
	 *                             parsed normally)
	 *              Other possible types (probably shouldn't be used)
	 *               - "text": Text-like (e.g. \text)
	 *               - "math": Normal math
	 *              If undefined, this will be treated as an appropriate length
	 *              array of "original" strings
	 *  - greediness: (optional) The greediness of the function to use ungrouped
	 *                arguments.
	 *
	 *                E.g. if you have an expression
	 *                  \sqrt \frac 1 2
	 *                since \frac has greediness=2 vs \sqrt's greediness=1, \frac
	 *                will use the two arguments '1' and '2' as its two arguments,
	 *                then that whole function will be used as the argument to
	 *                \sqrt. On the other hand, the expressions
	 *                  \frac \frac 1 2 3
	 *                and
	 *                  \frac \sqrt 1 2
	 *                will fail because \frac and \frac have equal greediness
	 *                and \sqrt has a lower greediness than \frac respectively. To
	 *                make these parse, we would have to change them to:
	 *                  \frac {\frac 1 2} 3
	 *                and
	 *                  \frac {\sqrt 1} 2
	 *
	 *                The default value is `1`
	 *  - allowedInText: (optional) Whether or not the function is allowed inside
	 *                   text mode (default false)
	 *  - numOptionalArgs: (optional) The number of optional arguments the function
	 *                     should parse. If the optional arguments aren't found,
	 *                     `null` will be passed to the handler in their place.
	 *                     (default 0)
	 *
	 * The last argument is that implementation, the handler for the function(s).
	 * It is called to handle these functions and their arguments.
	 * It receives two arguments:
	 *  - context contains information and references provided by the parser
	 *  - args is an array of arguments obtained from TeX input
	 * The context contains the following properties:
	 *  - funcName: the text (i.e. name) of the function, including \
	 *  - parser: the parser object
	 *  - lexer: the lexer object
	 *  - positions: the positions in the overall string of the function
	 *               and the arguments.
	 * The latter three should only be used to produce error messages.
	 *
	 * The function should return an object with the following keys:
	 *  - type: The type of element that this is. This is then used in
	 *          buildHTML/buildMathML to determine which function
	 *          should be called to build this node into a DOM node
	 * Any other data can be added to the object, which will be passed
	 * in to the function in buildHTML/buildMathML as `group.value`.
	 */

	function defineFunction(names, props, handler) {
	    if (typeof names === "string") {
	        names = [names];
	    }
	    if (typeof props === "number") {
	        props = { numArgs: props };
	    }
	    // Set default values of functions
	    var data = {
	        numArgs: props.numArgs,
	        argTypes: props.argTypes,
	        greediness: (props.greediness === undefined) ? 1 : props.greediness,
	        allowedInText: !!props.allowedInText,
	        numOptionalArgs: props.numOptionalArgs || 0,
	        handler: handler,
	    };
	    for (var i = 0; i < names.length; ++i) {
	        module.exports[names[i]] = data;
	    }
	}

	// A normal square root
	defineFunction("\\sqrt", {
	    numArgs: 1,
	    numOptionalArgs: 1,
	}, function(context, args) {
	    var index = args[0];
	    var body = args[1];
	    return {
	        type: "sqrt",
	        body: body,
	        index: index,
	    };
	});

	// Some non-mathy text
	defineFunction("\\text", {
	    numArgs: 1,
	    argTypes: ["text"],
	    greediness: 2,
	}, function(context, args) {
	    var body = args[0];
	    // Since the corresponding buildHTML/buildMathML function expects a
	    // list of elements, we normalize for different kinds of arguments
	    // TODO(emily): maybe this should be done somewhere else
	    var inner;
	    if (body.type === "ordgroup") {
	        inner = body.value;
	    } else {
	        inner = [body];
	    }

	    return {
	        type: "text",
	        body: inner,
	    };
	});

	// A two-argument custom color
	defineFunction("\\color", {
	    numArgs: 2,
	    allowedInText: true,
	    greediness: 3,
	    argTypes: ["color", "original"],
	}, function(context, args) {
	    var color = args[0];
	    var body = args[1];
	    // Normalize the different kinds of bodies (see \text above)
	    var inner;
	    if (body.type === "ordgroup") {
	        inner = body.value;
	    } else {
	        inner = [body];
	    }

	    return {
	        type: "color",
	        color: color.value,
	        value: inner,
	    };
	});

	// An overline
	defineFunction("\\overline", {
	    numArgs: 1,
	}, function(context, args) {
	    var body = args[0];
	    return {
	        type: "overline",
	        body: body,
	    };
	});

	// An underline
	defineFunction("\\underline", {
	    numArgs: 1,
	}, function(context, args) {
	    var body = args[0];
	    return {
	        type: "underline",
	        body: body,
	    };
	});

	// A box of the width and height
	defineFunction("\\rule", {
	    numArgs: 2,
	    numOptionalArgs: 1,
	    argTypes: ["size", "size", "size"],
	}, function(context, args) {
	    var shift = args[0];
	    var width = args[1];
	    var height = args[2];
	    return {
	        type: "rule",
	        shift: shift && shift.value,
	        width: width.value,
	        height: height.value,
	    };
	});

	// A KaTeX logo
	defineFunction("\\KaTeX", {
	    numArgs: 0,
	}, function(context) {
	    return {
	        type: "katex",
	    };
	});

	defineFunction("\\phantom", {
	    numArgs: 1,
	}, function(context, args) {
	    var body = args[0];
	    var inner;
	    if (body.type === "ordgroup") {
	        inner = body.value;
	    } else {
	        inner = [body];
	    }

	    return {
	        type: "phantom",
	        value: inner,
	    };
	});

	// Extra data needed for the delimiter handler down below
	var delimiterSizes = {
	    "\\bigl" : {type: "open",    size: 1},
	    "\\Bigl" : {type: "open",    size: 2},
	    "\\biggl": {type: "open",    size: 3},
	    "\\Biggl": {type: "open",    size: 4},
	    "\\bigr" : {type: "close",   size: 1},
	    "\\Bigr" : {type: "close",   size: 2},
	    "\\biggr": {type: "close",   size: 3},
	    "\\Biggr": {type: "close",   size: 4},
	    "\\bigm" : {type: "rel",     size: 1},
	    "\\Bigm" : {type: "rel",     size: 2},
	    "\\biggm": {type: "rel",     size: 3},
	    "\\Biggm": {type: "rel",     size: 4},
	    "\\big"  : {type: "textord", size: 1},
	    "\\Big"  : {type: "textord", size: 2},
	    "\\bigg" : {type: "textord", size: 3},
	    "\\Bigg" : {type: "textord", size: 4},
	};

	var delimiters = [
	    "(", ")", "[", "\\lbrack", "]", "\\rbrack",
	    "\\{", "\\lbrace", "\\}", "\\rbrace",
	    "\\lfloor", "\\rfloor", "\\lceil", "\\rceil",
	    "<", ">", "\\langle", "\\rangle", "\\lt", "\\gt",
	    "\\lvert", "\\rvert", "\\lVert", "\\rVert",
	    "\\lgroup", "\\rgroup", "\\lmoustache", "\\rmoustache",
	    "/", "\\backslash",
	    "|", "\\vert", "\\|", "\\Vert",
	    "\\uparrow", "\\Uparrow",
	    "\\downarrow", "\\Downarrow",
	    "\\updownarrow", "\\Updownarrow",
	    ".",
	];

	var fontAliases = {
	    "\\Bbb": "\\mathbb",
	    "\\bold": "\\mathbf",
	    "\\frak": "\\mathfrak",
	};

	// Single-argument color functions
	defineFunction([
	    "\\blue", "\\orange", "\\pink", "\\red",
	    "\\green", "\\gray", "\\purple",
	    "\\blueA", "\\blueB", "\\blueC", "\\blueD", "\\blueE",
	    "\\tealA", "\\tealB", "\\tealC", "\\tealD", "\\tealE",
	    "\\greenA", "\\greenB", "\\greenC", "\\greenD", "\\greenE",
	    "\\goldA", "\\goldB", "\\goldC", "\\goldD", "\\goldE",
	    "\\redA", "\\redB", "\\redC", "\\redD", "\\redE",
	    "\\maroonA", "\\maroonB", "\\maroonC", "\\maroonD", "\\maroonE",
	    "\\purpleA", "\\purpleB", "\\purpleC", "\\purpleD", "\\purpleE",
	    "\\mintA", "\\mintB", "\\mintC",
	    "\\grayA", "\\grayB", "\\grayC", "\\grayD", "\\grayE",
	    "\\grayF", "\\grayG", "\\grayH", "\\grayI",
	    "\\kaBlue", "\\kaGreen",
	], {
	    numArgs: 1,
	    allowedInText: true,
	    greediness: 3,
	}, function(context, args) {
	    var body = args[0];
	    var atoms;
	    if (body.type === "ordgroup") {
	        atoms = body.value;
	    } else {
	        atoms = [body];
	    }

	    return {
	        type: "color",
	        color: "katex-" + context.funcName.slice(1),
	        value: atoms,
	    };
	});

	// There are 2 flags for operators; whether they produce limits in
	// displaystyle, and whether they are symbols and should grow in
	// displaystyle. These four groups cover the four possible choices.

	// No limits, not symbols
	defineFunction([
	    "\\arcsin", "\\arccos", "\\arctan", "\\arg", "\\cos", "\\cosh",
	    "\\cot", "\\coth", "\\csc", "\\deg", "\\dim", "\\exp", "\\hom",
	    "\\ker", "\\lg", "\\ln", "\\log", "\\sec", "\\sin", "\\sinh",
	    "\\tan", "\\tanh",
	], {
	    numArgs: 0,
	}, function(context) {
	    return {
	        type: "op",
	        limits: false,
	        symbol: false,
	        body: context.funcName,
	    };
	});

	// Limits, not symbols
	defineFunction([
	    "\\det", "\\gcd", "\\inf", "\\lim", "\\liminf", "\\limsup", "\\max",
	    "\\min", "\\Pr", "\\sup",
	], {
	    numArgs: 0,
	}, function(context) {
	    return {
	        type: "op",
	        limits: true,
	        symbol: false,
	        body: context.funcName,
	    };
	});

	// No limits, symbols
	defineFunction([
	    "\\int", "\\iint", "\\iiint", "\\oint",
	], {
	    numArgs: 0,
	}, function(context) {
	    return {
	        type: "op",
	        limits: false,
	        symbol: true,
	        body: context.funcName,
	    };
	});

	// Limits, symbols
	defineFunction([
	    "\\coprod", "\\bigvee", "\\bigwedge", "\\biguplus", "\\bigcap",
	    "\\bigcup", "\\intop", "\\prod", "\\sum", "\\bigotimes",
	    "\\bigoplus", "\\bigodot", "\\bigsqcup", "\\smallint",
	], {
	    numArgs: 0,
	}, function(context) {
	    return {
	        type: "op",
	        limits: true,
	        symbol: true,
	        body: context.funcName,
	    };
	});

	// Fractions
	defineFunction([
	    "\\dfrac", "\\frac", "\\tfrac",
	    "\\dbinom", "\\binom", "\\tbinom",
	], {
	    numArgs: 2,
	    greediness: 2,
	}, function(context, args) {
	    var numer = args[0];
	    var denom = args[1];
	    var hasBarLine;
	    var leftDelim = null;
	    var rightDelim = null;
	    var size = "auto";

	    switch (context.funcName) {
	        case "\\dfrac":
	        case "\\frac":
	        case "\\tfrac":
	            hasBarLine = true;
	            break;
	        case "\\dbinom":
	        case "\\binom":
	        case "\\tbinom":
	            hasBarLine = false;
	            leftDelim = "(";
	            rightDelim = ")";
	            break;
	        default:
	            throw new Error("Unrecognized genfrac command");
	    }

	    switch (context.funcName) {
	        case "\\dfrac":
	        case "\\dbinom":
	            size = "display";
	            break;
	        case "\\tfrac":
	        case "\\tbinom":
	            size = "text";
	            break;
	    }

	    return {
	        type: "genfrac",
	        numer: numer,
	        denom: denom,
	        hasBarLine: hasBarLine,
	        leftDelim: leftDelim,
	        rightDelim: rightDelim,
	        size: size,
	    };
	});

	// Left and right overlap functions
	defineFunction(["\\llap", "\\rlap"], {
	    numArgs: 1,
	    allowedInText: true,
	}, function(context, args) {
	    var body = args[0];
	    return {
	        type: context.funcName.slice(1),
	        body: body,
	    };
	});

	// Delimiter functions
	defineFunction([
	    "\\bigl", "\\Bigl", "\\biggl", "\\Biggl",
	    "\\bigr", "\\Bigr", "\\biggr", "\\Biggr",
	    "\\bigm", "\\Bigm", "\\biggm", "\\Biggm",
	    "\\big",  "\\Big",  "\\bigg",  "\\Bigg",
	    "\\left", "\\right",
	], {
	    numArgs: 1,
	}, function(context, args) {
	    var delim = args[0];
	    if (!utils.contains(delimiters, delim.value)) {
	        throw new ParseError(
	            "Invalid delimiter: '" + delim.value + "' after '" +
	                context.funcName + "'",
	            context.lexer, context.positions[1]);
	    }

	    // \left and \right are caught somewhere in Parser.js, which is
	    // why this data doesn't match what is in buildHTML.
	    if (context.funcName === "\\left" || context.funcName === "\\right") {
	        return {
	            type: "leftright",
	            value: delim.value,
	        };
	    } else {
	        return {
	            type: "delimsizing",
	            size: delimiterSizes[context.funcName].size,
	            delimType: delimiterSizes[context.funcName].type,
	            value: delim.value,
	        };
	    }
	});

	// Sizing functions (handled in Parser.js explicitly, hence no handler)
	defineFunction([
	    "\\tiny", "\\scriptsize", "\\footnotesize", "\\small",
	    "\\normalsize", "\\large", "\\Large", "\\LARGE", "\\huge", "\\Huge",
	], 0, null);

	// Style changing functions (handled in Parser.js explicitly, hence no
	// handler)
	defineFunction([
	    "\\displaystyle", "\\textstyle", "\\scriptstyle",
	    "\\scriptscriptstyle",
	], 0, null);

	defineFunction([
	    // styles
	    "\\mathrm", "\\mathit", "\\mathbf",

	    // families
	    "\\mathbb", "\\mathcal", "\\mathfrak", "\\mathscr", "\\mathsf",
	    "\\mathtt",

	    // aliases
	    "\\Bbb", "\\bold", "\\frak",
	], {
	    numArgs: 1,
	    greediness: 2,
	}, function(context, args) {
	    var body = args[0];
	    var func = context.funcName;
	    if (func in fontAliases) {
	        func = fontAliases[func];
	    }
	    return {
	        type: "font",
	        font: func.slice(1),
	        body: body,
	    };
	});

	// Accents
	defineFunction([
	    "\\acute", "\\grave", "\\ddot", "\\tilde", "\\bar", "\\breve",
	    "\\check", "\\hat", "\\vec", "\\dot",
	    // We don't support expanding accents yet
	    // "\\widetilde", "\\widehat"
	], {
	    numArgs: 1,
	}, function(context, args) {
	    var base = args[0];
	    return {
	        type: "accent",
	        accent: context.funcName,
	        base: base,
	    };
	});

	// Infix generalized fractions
	defineFunction(["\\over", "\\choose"], {
	    numArgs: 0,
	}, function(context) {
	    var replaceWith;
	    switch (context.funcName) {
	        case "\\over":
	            replaceWith = "\\frac";
	            break;
	        case "\\choose":
	            replaceWith = "\\binom";
	            break;
	        default:
	            throw new Error("Unrecognized infix genfrac command");
	    }
	    return {
	        type: "infix",
	        replaceWith: replaceWith,
	    };
	});

	// Row breaks for aligned data
	defineFunction(["\\\\", "\\cr"], {
	    numArgs: 0,
	    numOptionalArgs: 1,
	    argTypes: ["size"],
	}, function(context, args) {
	    var size = args[0];
	    return {
	        type: "cr",
	        size: size,
	    };
	});

	// Environment delimiters
	defineFunction(["\\begin", "\\end"], {
	    numArgs: 1,
	    argTypes: ["text"],
	}, function(context, args) {
	    var nameGroup = args[0];
	    if (nameGroup.type !== "ordgroup") {
	        throw new ParseError(
	            "Invalid environment name",
	            context.lexer, context.positions[1]);
	    }
	    var name = "";
	    for (var i = 0; i < nameGroup.value.length; ++i) {
	        name += nameGroup.value[i].value;
	    }
	    return {
	        type: "environment",
	        name: name,
	        namepos: context.positions[1],
	    };
	});


/***/ },
/* 280 */
/***/ function(module, exports, __webpack_require__) {

	/* eslint no-constant-condition:0 */
	var fontMetrics = __webpack_require__(270);
	var parseData = __webpack_require__(281);
	var ParseError = __webpack_require__(262);

	var ParseNode = parseData.ParseNode;

	/**
	 * Parse the body of the environment, with rows delimited by \\ and
	 * columns delimited by &, and create a nested list in row-major order
	 * with one group per cell.
	 */
	function parseArray(parser, result) {
	    var row = [];
	    var body = [row];
	    var rowGaps = [];
	    while (true) {
	        var cell = parser.parseExpression(false, null);
	        row.push(new ParseNode("ordgroup", cell, parser.mode));
	        var next = parser.nextToken.text;
	        if (next === "&") {
	            parser.consume();
	        } else if (next === "\\end") {
	            break;
	        } else if (next === "\\\\" || next === "\\cr") {
	            var cr = parser.parseFunction();
	            rowGaps.push(cr.value.size);
	            row = [];
	            body.push(row);
	        } else {
	            // TODO: Clean up the following hack once #385 got merged
	            var pos = Math.min(parser.pos + 1, parser.lexer._input.length);
	            throw new ParseError("Expected & or \\\\ or \\end",
	                                 parser.lexer, pos);
	        }
	    }
	    result.body = body;
	    result.rowGaps = rowGaps;
	    return new ParseNode(result.type, result, parser.mode);
	}

	/*
	 * An environment definition is very similar to a function definition:
	 * it is declared with a name or a list of names, a set of properties
	 * and a handler containing the actual implementation.
	 *
	 * The properties include:
	 *  - numArgs: The number of arguments after the \begin{name} function.
	 *  - argTypes: (optional) Just like for a function
	 *  - allowedInText: (optional) Whether or not the environment is allowed inside
	 *                   text mode (default false) (not enforced yet)
	 *  - numOptionalArgs: (optional) Just like for a function
	 * A bare number instead of that object indicates the numArgs value.
	 *
	 * The handler function will receive two arguments
	 *  - context: information and references provided by the parser
	 *  - args: an array of arguments passed to \begin{name}
	 * The context contains the following properties:
	 *  - envName: the name of the environment, one of the listed names.
	 *  - parser: the parser object
	 *  - lexer: the lexer object
	 *  - positions: the positions associated with these arguments from args.
	 * The handler must return a ParseResult.
	 */

	function defineEnvironment(names, props, handler) {
	    if (typeof names === "string") {
	        names = [names];
	    }
	    if (typeof props === "number") {
	        props = { numArgs: props };
	    }
	    // Set default values of environments
	    var data = {
	        numArgs: props.numArgs || 0,
	        argTypes: props.argTypes,
	        greediness: 1,
	        allowedInText: !!props.allowedInText,
	        numOptionalArgs: props.numOptionalArgs || 0,
	        handler: handler,
	    };
	    for (var i = 0; i < names.length; ++i) {
	        module.exports[names[i]] = data;
	    }
	}

	// Arrays are part of LaTeX, defined in lttab.dtx so its documentation
	// is part of the source2e.pdf file of LaTeX2e source documentation.
	defineEnvironment("array", {
	    numArgs: 1,
	}, function(context, args) {
	    var colalign = args[0];
	    colalign = colalign.value.map ? colalign.value : [colalign];
	    var cols = colalign.map(function(node) {
	        var ca = node.value;
	        if ("lcr".indexOf(ca) !== -1) {
	            return {
	                type: "align",
	                align: ca,
	            };
	        } else if (ca === "|") {
	            return {
	                type: "separator",
	                separator: "|",
	            };
	        }
	        throw new ParseError(
	            "Unknown column alignment: " + node.value,
	            context.lexer, context.positions[1]);
	    });
	    var res = {
	        type: "array",
	        cols: cols,
	        hskipBeforeAndAfter: true, // \@preamble in lttab.dtx
	    };
	    res = parseArray(context.parser, res);
	    return res;
	});

	// The matrix environments of amsmath builds on the array environment
	// of LaTeX, which is discussed above.
	defineEnvironment([
	    "matrix",
	    "pmatrix",
	    "bmatrix",
	    "Bmatrix",
	    "vmatrix",
	    "Vmatrix",
	], {
	}, function(context) {
	    var delimiters = {
	        "matrix": null,
	        "pmatrix": ["(", ")"],
	        "bmatrix": ["[", "]"],
	        "Bmatrix": ["\\{", "\\}"],
	        "vmatrix": ["|", "|"],
	        "Vmatrix": ["\\Vert", "\\Vert"],
	    }[context.envName];
	    var res = {
	        type: "array",
	        hskipBeforeAndAfter: false, // \hskip -\arraycolsep in amsmath
	    };
	    res = parseArray(context.parser, res);
	    if (delimiters) {
	        res = new ParseNode("leftright", {
	            body: [res],
	            left: delimiters[0],
	            right: delimiters[1],
	        }, context.mode);
	    }
	    return res;
	});

	// A cases environment (in amsmath.sty) is almost equivalent to
	// \def\arraystretch{1.2}%
	// \left\{\begin{array}{@{}l@{\quad}l@{}} … \end{array}\right.
	defineEnvironment("cases", {
	}, function(context) {
	    var res = {
	        type: "array",
	        arraystretch: 1.2,
	        cols: [{
	            type: "align",
	            align: "l",
	            pregap: 0,
	            postgap: fontMetrics.metrics.quad,
	        }, {
	            type: "align",
	            align: "l",
	            pregap: 0,
	            postgap: 0,
	        }],
	    };
	    res = parseArray(context.parser, res);
	    res = new ParseNode("leftright", {
	        body: [res],
	        left: "\\{",
	        right: ".",
	    }, context.mode);
	    return res;
	});

	// An aligned environment is like the align* environment
	// except it operates within math mode.
	// Note that we assume \nomallineskiplimit to be zero,
	// so that \strut@ is the same as \strut.
	defineEnvironment("aligned", {
	}, function(context) {
	    var res = {
	        type: "array",
	        cols: [],
	    };
	    res = parseArray(context.parser, res);
	    var emptyGroup = new ParseNode("ordgroup", [], context.mode);
	    var numCols = 0;
	    res.value.body.forEach(function(row) {
	        var i;
	        for (i = 1; i < row.length; i += 2) {
	            row[i].value.unshift(emptyGroup);
	        }
	        if (numCols < row.length) {
	            numCols = row.length;
	        }
	    });
	    for (var i = 0; i < numCols; ++i) {
	        var align = "r";
	        var pregap = 0;
	        if (i % 2 === 1) {
	            align = "l";
	        } else if (i > 0) {
	            pregap = 2; // one \qquad between columns
	        }
	        res.value.cols[i] = {
	            type: "align",
	            align: align,
	            pregap: pregap,
	            postgap: 0,
	        };
	    }
	    return res;
	});


/***/ },
/* 281 */
/***/ function(module, exports) {

	/**
	 * The resulting parse tree nodes of the parse tree.
	 */
	function ParseNode(type, value, mode) {
	    this.type = type;
	    this.value = value;
	    this.mode = mode;
	}

	module.exports = {
	    ParseNode: ParseNode,
	};



/***/ },
/* 282 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * The Lexer class handles tokenizing the input in various ways. Since our
	 * parser expects us to be able to backtrack, the lexer allows lexing from any
	 * given starting point.
	 *
	 * Its main exposed function is the `lex` function, which takes a position to
	 * lex from and a type of token to lex. It defers to the appropriate `_innerLex`
	 * function.
	 *
	 * The various `_innerLex` functions perform the actual lexing of different
	 * kinds.
	 */

	var matchAt = __webpack_require__(283);

	var ParseError = __webpack_require__(262);

	// The main lexer class
	function Lexer(input) {
	    this._input = input;
	}

	// The resulting token returned from `lex`.
	function Token(text, data, position) {
	    this.text = text;
	    this.data = data;
	    this.position = position;
	}

	/* The following tokenRegex
	 * - matches typical whitespace (but not NBSP etc.) using its first group
	 * - matches symbol combinations which result in a single output character
	 * - does not match any control character \x00-\x1f except whitespace
	 * - does not match a bare backslash
	 * - matches any ASCII character except those just mentioned
	 * - does not match the BMP private use area \uE000-\uF8FF
	 * - does not match bare surrogate code units
	 * - matches any BMP character except for those just described
	 * - matches any valid Unicode surrogate pair
	 * - matches a backslash followed by one or more letters
	 * - matches a backslash followed by any BMP character, including newline
	 * Just because the Lexer matches something doesn't mean it's valid input:
	 * If there is no matching function or symbol definition, the Parser will
	 * still reject the input.
	 */
	var tokenRegex = new RegExp(
	    "([ \r\n\t]+)|(" +                                // whitespace
	    "---?" +                                          // special combinations
	    "|[!-\\[\\]-\u2027\u202A-\uD7FF\uF900-\uFFFF]" +  // single codepoint
	    "|[\uD800-\uDBFF][\uDC00-\uDFFF]" +               // surrogate pair
	    "|\\\\(?:[a-zA-Z]+|[^\uD800-\uDFFF])" +           // function name
	    ")"
	);

	var whitespaceRegex = /\s*/;

	/**
	 * This function lexes a single normal token. It takes a position and
	 * whether it should completely ignore whitespace or not.
	 */
	Lexer.prototype._innerLex = function(pos, ignoreWhitespace) {
	    var input = this._input;
	    if (pos === input.length) {
	        return new Token("EOF", null, pos);
	    }
	    var match = matchAt(tokenRegex, input, pos);
	    if (match === null) {
	        throw new ParseError(
	            "Unexpected character: '" + input[pos] + "'",
	            this, pos);
	    } else if (match[2]) { // matched non-whitespace
	        return new Token(match[2], null, pos + match[2].length);
	    } else if (ignoreWhitespace) {
	        return this._innerLex(pos + match[1].length, true);
	    } else { // concatenate whitespace to a single space
	        return new Token(" ", null, pos + match[1].length);
	    }
	};

	// A regex to match a CSS color (like #ffffff or BlueViolet)
	var cssColor = /#[a-z0-9]+|[a-z]+/i;

	/**
	 * This function lexes a CSS color.
	 */
	Lexer.prototype._innerLexColor = function(pos) {
	    var input = this._input;

	    // Ignore whitespace
	    var whitespace = matchAt(whitespaceRegex, input, pos)[0];
	    pos += whitespace.length;

	    var match;
	    if ((match = matchAt(cssColor, input, pos))) {
	        // If we look like a color, return a color
	        return new Token(match[0], null, pos + match[0].length);
	    } else {
	        throw new ParseError("Invalid color", this, pos);
	    }
	};

	// A regex to match a dimension. Dimensions look like
	// "1.2em" or ".4pt" or "1 ex"
	var sizeRegex = /(-?)\s*(\d+(?:\.\d*)?|\.\d+)\s*([a-z]{2})/;

	/**
	 * This function lexes a dimension.
	 */
	Lexer.prototype._innerLexSize = function(pos) {
	    var input = this._input;

	    // Ignore whitespace
	    var whitespace = matchAt(whitespaceRegex, input, pos)[0];
	    pos += whitespace.length;

	    var match;
	    if ((match = matchAt(sizeRegex, input, pos))) {
	        var unit = match[3];
	        // We only currently handle "em" and "ex" units
	        if (unit !== "em" && unit !== "ex") {
	            throw new ParseError("Invalid unit: '" + unit + "'", this, pos);
	        }
	        return new Token(match[0], {
	            number: +(match[1] + match[2]),
	            unit: unit,
	        }, pos + match[0].length);
	    }

	    throw new ParseError("Invalid size", this, pos);
	};

	/**
	 * This function lexes a string of whitespace.
	 */
	Lexer.prototype._innerLexWhitespace = function(pos) {
	    var input = this._input;

	    var whitespace = matchAt(whitespaceRegex, input, pos)[0];
	    pos += whitespace.length;

	    return new Token(whitespace[0], null, pos);
	};

	/**
	 * This function lexes a single token starting at `pos` and of the given mode.
	 * Based on the mode, we defer to one of the `_innerLex` functions.
	 */
	Lexer.prototype.lex = function(pos, mode) {
	    if (mode === "math") {
	        return this._innerLex(pos, true);
	    } else if (mode === "text") {
	        return this._innerLex(pos, false);
	    } else if (mode === "color") {
	        return this._innerLexColor(pos);
	    } else if (mode === "size") {
	        return this._innerLexSize(pos);
	    } else if (mode === "whitespace") {
	        return this._innerLexWhitespace(pos);
	    }
	};

	module.exports = Lexer;


/***/ },
/* 283 */
/***/ function(module, exports) {

	/** @flow */

	"use strict";

	function getRelocatable(re) {
	  // In the future, this could use a WeakMap instead of an expando.
	  if (!re.__matchAtRelocatable) {
	    // Disjunctions are the lowest-precedence operator, so we can make any
	    // pattern match the empty string by appending `|()` to it:
	    // https://people.mozilla.org/~jorendorff/es6-draft.html#sec-patterns
	    var source = re.source + "|()";

	    // We always make the new regex global.
	    var flags = "g" + (re.ignoreCase ? "i" : "") + (re.multiline ? "m" : "") + (re.unicode ? "u" : "")
	    // sticky (/.../y) doesn't make sense in conjunction with our relocation
	    // logic, so we ignore it here.
	    ;

	    re.__matchAtRelocatable = new RegExp(source, flags);
	  }
	  return re.__matchAtRelocatable;
	}

	function matchAt(re, str, pos) {
	  if (re.global || re.sticky) {
	    throw new Error("matchAt(...): Only non-global regexes are supported");
	  }
	  var reloc = getRelocatable(re);
	  reloc.lastIndex = pos;
	  var match = reloc.exec(str);
	  // Last capturing group is our sentinel that indicates whether the regex
	  // matched at the given location.
	  if (match[match.length - 1] == null) {
	    // Original regex matched.
	    match.length = match.length - 1;
	    return match;
	  } else {
	    return null;
	  }
	}

	module.exports = matchAt;

/***/ },
/* 284 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _FigRef = __webpack_require__(245);

	var _FigRef2 = _interopRequireDefault(_FigRef);

	var _Image = __webpack_require__(259);

	var _Image2 = _interopRequireDefault(_Image);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	var _Math = __webpack_require__(260);

	var _Math2 = _interopRequireDefault(_Math);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var DeterministicTagger = function (_Component) {
	    _inherits(DeterministicTagger, _Component);

	    function DeterministicTagger() {
	        _classCallCheck(this, DeterministicTagger);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(DeterministicTagger).apply(this, arguments));
	    }

	    _createClass(DeterministicTagger, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                'div',
	                null,
	                'To compare the performance of CRFs, we also define a deterministic algorithm which uses some straightforward rules to determine token tags based on the patterns that we have identified above. (e.g. \'if it looks like a known title, assign it to ',
	                _react2.default.createElement(
	                    'code',
	                    null,
	                    'title'
	                ),
	                '\'; \'if it looks like a number and is congruent with previous numbers, assign it to ',
	                _react2.default.createElement(
	                    'code',
	                    null,
	                    'nr'
	                ),
	                '\'.)'
	            );
	        }
	    }]);

	    return DeterministicTagger;
	}(_react.Component);

	exports.default = DeterministicTagger;

/***/ },
/* 285 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _FigRef = __webpack_require__(245);

	var _FigRef2 = _interopRequireDefault(_FigRef);

	var _Image = __webpack_require__(259);

	var _Image2 = _interopRequireDefault(_Image);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	var _Math = __webpack_require__(260);

	var _Math2 = _interopRequireDefault(_Math);

	var _GraphicalModels = __webpack_require__(286);

	var _GraphicalModels2 = _interopRequireDefault(_GraphicalModels);

	var _LinearChainCRF = __webpack_require__(287);

	var _LinearChainCRF2 = _interopRequireDefault(_LinearChainCRF);

	var _HMMs = __webpack_require__(288);

	var _HMMs2 = _interopRequireDefault(_HMMs);

	var _LogRes = __webpack_require__(289);

	var _LogRes2 = _interopRequireDefault(_LogRes);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var CRF = function (_Component) {
	    _inherits(CRF, _Component);

	    function CRF() {
	        _classCallCheck(this, CRF);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(CRF).apply(this, arguments));
	    }

	    _createClass(CRF, [{
	        key: 'render',
	        value: function render() {
	            var relativeToRoot = this.props.path.match(/\//g).slice(1).map(function (_) {
	                return "../";
	            }).join("");
	            return _react2.default.createElement(
	                'div',
	                null,
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Conditional Random Fields (CRFs) are a class of statistical modelling methods that were first introduced in ',
	                    _references2.default.cite(_bib2.default.mccallum2000maximum),
	                    ' as a non-generative (i.e., discriminative) alternative to Hidden Markov Models (HMMs). This means that instead of modeling the joint probability ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: 'p(\\mathbf x,\\mathbf y)' }),
	                    ' of the observation vector ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf x' }),
	                    ' and label vector ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf y' }),
	                    ' occurring together, we model the conditional probability ',
	                    _react2.default.createElement(_Math2.default, { l: 'p(\\mathbf y|\\mathbf x)' }),
	                    ' of labels ',
	                    _react2.default.createElement(
	                        'em',
	                        null,
	                        'given'
	                    ),
	                    ' the observations. CRFs do not explicitly model ',
	                    _react2.default.createElement(_Math2.default, { l: 'p(\\mathbf x)' }),
	                    ', just ',
	                    _react2.default.createElement(_Math2.default, { l: 'p(\\mathbf y|\\mathbf x)' }),
	                    ', and so we can use a very rich set of features ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: '\\mathbf x' }),
	                    ' and still have a tractable model. As such, CRFs can model a complex interdependence of observation variables, and are therefore popular in pattern recognition tasks.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'As illustrated in ',
	                    _react2.default.createElement(_FigRef2.default, { fig: _figs2.default.graphicalModels }),
	                    ', CRFs can be understood as a graphical version of logistic regression, in which we have an arbitrary number of dependant variables that are conditioned on a number of explanatory variables (instead of just one dependant variable).'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'In this thesis, we limit ourselves to a subclass of CRFs called Linear-Chain Conditional Random Fields (LC-CRFs or Linear Chain CRFs), which is topologically very similar to HMMs: both model a probability distribution along a chain of input variables, where each input variable is also connected to a single output variable. To clarify, in our experiments we see a input document as a string document elements, where each element should correspond to a label of either ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'title'
	                    ),
	                    ', ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'nr'
	                    ),
	                    ', ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'text'
	                    ),
	                    ' or ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'newline'
	                    ),
	                    '.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'In this section, we provide a definition of Linear-Chain Conditional Random Fields, supported first by introductory sections on ',
	                    _react2.default.createElement(
	                        'a',
	                        { href: "#" + _GraphicalModels2.default.id() },
	                        'graphical models'
	                    ),
	                    ', ',
	                    _react2.default.createElement(
	                        'a',
	                        { href: '#logistic-regression' },
	                        'logistic regression'
	                    ),
	                    ' and ',
	                    _react2.default.createElement(
	                        'a',
	                        { href: '#hmm' },
	                        'Hidden Markov Models'
	                    ),
	                    '.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'For a more thorough tutorial into CRFs, including skip-chain CRFs, one could refer to ',
	                    _references2.default.cite(_bib2.default.sutton2006introduction),
	                    '.'
	                ),
	                _react2.default.createElement(_GraphicalModels2.default, this.props),
	                _react2.default.createElement(_HMMs2.default, this.props),
	                _react2.default.createElement(_LinearChainCRF2.default, this.props)
	            );
	            // <LogRes {...this.props}/>
	        }
	    }]);

	    return CRF;
	}(_react.Component);

	exports.default = CRF;

/***/ },
/* 286 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _FigRef = __webpack_require__(245);

	var _FigRef2 = _interopRequireDefault(_FigRef);

	var _Image = __webpack_require__(259);

	var _Image2 = _interopRequireDefault(_Image);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	var _Math = __webpack_require__(260);

	var _Math2 = _interopRequireDefault(_Math);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var CRF = function (_Component) {
	    _inherits(CRF, _Component);

	    function CRF() {
	        _classCallCheck(this, CRF);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(CRF).apply(this, arguments));
	    }

	    _createClass(CRF, [{
	        key: 'render',
	        value: function render() {
	            var relativeToRoot = this.props.path.match(/\//g).slice(1).map(function (_) {
	                return "../";
	            }).join("");

	            return _react2.default.createElement(
	                'section',
	                { id: CRF.id() },
	                _react2.default.createElement(
	                    'h4',
	                    null,
	                    'Graphical models'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Graphical models are statistical models where the probility function can be represented as a ',
	                    _react2.default.createElement(
	                        'a',
	                        { href: 'https://en.wikipedia.org/wiki/Factor_graph' },
	                        'factor graph'
	                    ),
	                    '. Graphical models include Bayesian networks, HMMs, CRFs and logistic regression models.'
	                ),
	                _react2.default.createElement(
	                    _Image2.default,
	                    { relativeToRoot: relativeToRoot, fig: _figs2.default.graphicalModels },
	                    'Diagram of the relationship between naive Bayes, logistic regression, HMMs, linear-chain CRFs, Bayesian models, and general CRFs. Image adapted from ',
	                    _references2.default.cite(_bib2.default.sutton2006introduction),
	                    '.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'We now formally define graphical models. Assume we have a set ',
	                    _react2.default.createElement(_Math2.default, { l: 'V=X\\cup Y' }),
	                    ' of random variables which can take values from a set ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathcal{V}' }),
	                    '. Here ',
	                    _react2.default.createElement(_Math2.default, { l: 'X' }),
	                    'denotes a set of input variables (for example, word features) and ',
	                    _react2.default.createElement(_Math2.default, { l: 'Y' }),
	                    ' denotes a set of output variables (for example, part-of-speech tags). We denote an assignment to ',
	                    _react2.default.createElement(_Math2.default, { l: 'X' }),
	                    ' and ',
	                    _react2.default.createElement(_Math2.default, { l: 'Y' }),
	                    ' with ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: '\\mathbf x' }),
	                    ' and ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: '\\mathbf y' }),
	                    ', respectively. An undirected graphical model is defined as the set of all probability distributions that can be written as ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: 'p( \\mathbf x_A, \\mathbf y_A)=\\frac{1}{Z}\\prod _A \\Phi_A( \\mathbf x_A,\\mathbf y_A)',
	                        displayMode: true }),
	                    'where ',
	                    _react2.default.createElement(_Math2.default, { l: '\\Phi_A \\in F' }),
	                    ' is a factor defined on some subset of variables ',
	                    _react2.default.createElement(_Math2.default, { l: 'A \\subseteq V' }),
	                    ' and ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: 'Z=\\sum _{\\mathbf x, \\mathbf y} ( \\prod _A \\Phi_A( \\mathbf x_A,\\mathbf y_A))',
	                        displayMode: true })
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Intuitively, ',
	                    _react2.default.createElement(_Math2.default, { l: 'p( \\mathbf x_A, \\mathbf y_A)' }),
	                    ' describes the joint probability of input and output vectors in terms of some set of functions ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: 'F = \\{  \\Phi_A\\}' }),
	                    ', collectively known as the factors. We will see that these factors wrap feature values (for example, ',
	                    _react2.default.createElement(_Math2.default, { l: '1' }),
	                    ' if a word is capitalized), multiplied together. The normalization term ',
	                    _react2.default.createElement(_Math2.default, { l: 'Z' }),
	                    ' ensures that the probability function ranges between ',
	                    _react2.default.createElement(_Math2.default, { l: '0' }),
	                    ' and ',
	                    _react2.default.createElement(_Math2.default, { l: '1' }),
	                    ': it sums every possible value of the the multiplied factors. ',
	                    _react2.default.createElement(_Math2.default, { l: '\\Phi_A \\in F' }),
	                    ' can be any function from ',
	                    _react2.default.createElement(_Math2.default, { l: 'A \\subset V' }),
	                    ' to a positive real number, i.e. ',
	                    _react2.default.createElement(_Math2.default, { l: '\\Phi_A:V^n\\rightarrow\\ \\mathbb{R}^+' }),
	                    '. Individually the functions ',
	                    _react2.default.createElement(_Math2.default, { l: '\\Phi_A \\in F' }),
	                    ' are known as local functions or compatibility functions.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'It is important to note that ',
	                    _react2.default.createElement(_Math2.default, { l: 'F' }),
	                    ' is specific to the modeling application. Our choice of factors is what distinguishes graphical models from each other; they are the functions that determine the probability of a given input to have a certain output.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    _react2.default.createElement(_Math2.default, { l: 'Z' }),
	                    ' is called the partition function, because it normalizes the function ',
	                    _react2.default.createElement(_Math2.default, { l: 'p' }),
	                    ' to ensure that ',
	                    _react2.default.createElement(_Math2.default, { l: '\\sum_A p(\\mathbf x_A,\\mathbf y_A)' }),
	                    ' sums to ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: '1' }),
	                    '. In general, computing ',
	                    _react2.default.createElement(_Math2.default, { l: 'Z' }),
	                    ' is intractable, because we need to sum over all possible assignments ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf x' }),
	                    ' of input vectors and all possible assignments ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf y' }),
	                    ' of output vectors. However, efficient methods to estimate ',
	                    _react2.default.createElement(_Math2.default, { l: 'Z' }),
	                    ' exist.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'The factorization of the function for ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: 'p( \\mathbf x_A,\\mathbf y_A)' }),
	                    ' can be represented as graph, called a ',
	                    _react2.default.createElement(
	                        'a',
	                        { href: 'https://en.wikipedia.org/wiki/Factor_graph' },
	                        'factor graph'
	                    ),
	                    '. (Illustrated in ',
	                    _react2.default.createElement(_FigRef2.default, { fig: _figs2.default.factorGraph }),
	                    '.)'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Factor graphs are ',
	                    _react2.default.createElement(
	                        'a',
	                        { className: 'wiki', href: 'https://en.wikipedia.org/wiki/Bipartite_graph' },
	                        'bipartite graphs'
	                    ),
	                    ' ',
	                    _react2.default.createElement(_Math2.default, { l: 'G=(V,F,E)' }),
	                    ' that link variable nodes ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: 'v_s\\in V' }),
	                    ' to function nodes ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: '\\Phi_A\\in F' }),
	                    ' through edge ',
	                    _react2.default.createElement(_Math2.default, { l: "e^{\\Phi_A}_{v_s}"
	                    }),
	                    ' iff ',
	                    _react2.default.createElement(_Math2.default, { l: 'v_s\\in \\mathbf{arg} ( \\Phi_A )' }),
	                    '. The graph thus allows us to graphically represent how input and output variables interact with our local functions to generate a probability distribution.'
	                ),
	                _react2.default.createElement(_Image2.default, { relativeToRoot: relativeToRoot, width: '55%', fig: _figs2.default.factorGraph }),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'We define a directed model (or Bayesian Network) as a graphical model that factorizes as:',
	                    _react2.default.createElement(_Math2.default, { l: 'p( \\mathbf x_A, \\mathbf y_A)=\\prod _{v\\in V}p(v|\\pi(v))', display: 'true' }),
	                    'where ',
	                    _react2.default.createElement(_Math2.default, { l: '\\pi(v)' }),
	                    ' are the parents of ',
	                    _react2.default.createElement(_Math2.default, { l: 'v' }),
	                    ' in ',
	                    _react2.default.createElement(_Math2.default, { l: 'G' }),
	                    '.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'We define generative models as directed models in which all labels ',
	                    _react2.default.createElement(_Math2.default, { l: 'y \\in Y' }),
	                    ' are parents of ',
	                    _react2.default.createElement(_Math2.default, { l: 'x\\in X' }),
	                    '. This name is due to the labels "generating" the output: the labels are the contingencies upon which the probability of the output depends.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'When we describe the probability distribution ',
	                    _react2.default.createElement(_Math2.default, { l: 'p( \\mathbf y|\\mathbf x)' }),
	                    ', we speak of a discriminative model. Every generative model has a discriminative counterpart. In the words of ',
	                    _references2.default.cite(_bib2.default.jordan2002discriminative),
	                    ', we call these generative-discriminative pairs. Training a generative model to maximize ',
	                    _react2.default.createElement(_Math2.default, { latex: 'p(\\mathbf y|\\mathbf x)' }),
	                    '  yields the same model as training its discriminative counterpart. Conversely, training a discriminative model to maximize the joint probability ',
	                    _react2.default.createElement(_Math2.default, { l: 'p(\\mathbf x,\\mathbf y)' }),
	                    ' (instead of ',
	                    _react2.default.createElement(_Math2.default, { latex: 'p(\\mathbf y|\\mathbf x)' }),
	                    ') results in the same model as training the generative counterpart.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'It turns out that when we model a conditional distribution, we have more parameter freedom for ',
	                    _react2.default.createElement(_Math2.default, { l: 'p(\\mathbf y)' }),
	                    ', because we are not interested in parameter values for ',
	                    _react2.default.createElement(_Math2.default, { l: 'p( \\mathbf x)' }),
	                    '. Modeling ',
	                    _react2.default.createElement(_Math2.default, { l: 'p( \\mathbf y|\\mathbf x)' }),
	                    'unburdens us with modeling the potentially very complicated inter-dependencies of ',
	                    _react2.default.createElement(_Math2.default, { l: 'p(\\mathbf x)' }),
	                    '. In classification tasks, this means that we are better able to use observations, and so discriminative models tend to out-perform generative models in practice. For a thorough explanation of the principle of generative-discriminative pairs, see ',
	                    _references2.default.cite(_bib2.default.jordan2002discriminative),
	                    '.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'One generative-discriminative pair is formed by Hidden Markov Models (HMMs) and Linear Chain CRFs. In the following, we introduce HMMs (generative) to support a definition of Linear-Chain CRFs (discriminative).'
	                )
	            );
	        }
	    }], [{
	        key: 'id',
	        value: function id() {
	            return "graphical-models";
	        }
	    }]);

	    return CRF;
	}(_react.Component);

	exports.default = CRF;

/***/ },
/* 287 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _FigRef = __webpack_require__(245);

	var _FigRef2 = _interopRequireDefault(_FigRef);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	var _Math = __webpack_require__(260);

	var _Math2 = _interopRequireDefault(_Math);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable

	// import FigImg from './../../../../Figures/Image/Image'


	var LinearChainCRF = function (_Component) {
	    _inherits(LinearChainCRF, _Component);

	    function LinearChainCRF() {
	        _classCallCheck(this, LinearChainCRF);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(LinearChainCRF).apply(this, arguments));
	    }

	    _createClass(LinearChainCRF, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                'section',
	                { id: 'linear-chain-crf' },
	                _react2.default.createElement(
	                    'h4',
	                    null,
	                    'Linear Chain CRFs'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'On the surface, linear-chain CRFs (LC-CRFs) look much like Hidden Markov Models: as we see in ',
	                    _react2.default.createElement(_FigRef2.default, {
	                        fig: _figs2.default.graphicalModels }),
	                    ', LC-CRFs also model a sequence of observations along a sequence of labels. The difference between HMMs and Linear Chain CRFs is that instead of modeling the joint probability ',
	                    _react2.default.createElement(_Math2.default, { latex: 'p(\\mathbf x,\\mathbf y)' }),
	                    ', we model the conditional probability ',
	                    _react2.default.createElement(_Math2.default, { latex: 'p(\\mathbf y|\\mathbf x)' }),
	                    '.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'This is a fundamental difference: we don\'t assume that the labels generate observations, but rather that the observations provide support for the probability of labels.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'We define a linear-chain conditional random field as follows:'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Let'
	                ),
	                _react2.default.createElement(
	                    'ul',
	                    null,
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(_Math2.default, { l: 'Y,X' }),
	                        ' be random vectors taking valuations from ',
	                        _react2.default.createElement(_Math2.default, { l: '\\mathcal{V}' })
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(_Math2.default, { l: 'F=\\{\\Phi_1, \\ldots\\Phi_k\\}' }),
	                        ' be a set of local functions ',
	                        _react2.default.createElement(_Math2.default, {
	                            l: 'V^n\\rightarrow \\mathbb{R}^+' })
	                    )
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'We then define the un-normalized CRF distribution as:',
	                    _react2.default.createElement(_Math2.default, {
	                        l: '\\hat{p}(\\mathbf x, \\mathbf y)=\\prod_{i=1}^k\\Phi_i(D_i)',
	                        displayMode: true })
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Recall from ',
	                    _react2.default.createElement(
	                        'a',
	                        {
	                            href: "#graphical-models" },
	                        'our introduction on graphical models'
	                    ),
	                    ' that we need a normalizing constant to ensure that our probability distribution adds up to ',
	                    _react2.default.createElement(_Math2.default, { l: '1' }),
	                    '. We are interested in representing ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: 'p(\\mathbf y|\\mathbf x)' }),
	                    ', so we use a normalization function that assumes ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf x' }),
	                    ' is given, i.e.:',
	                    _react2.default.createElement(_Math2.default, { l: 'Z(\\mathbf x)=\\sum_{\\mathbf{y}}\\hat{p}(\\mathbf x, \\mathbf y)', displayMode: true }),
	                    'and so',
	                    _react2.default.createElement(_Math2.default, {
	                        l: 'p(\\mathbf y|\\mathbf x)= \\frac{1}{Z(\\mathbf x)}\\hat{p}(\\mathbf x, \\mathbf y)',
	                        displayMode: true })
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'In CRFs, each local feature has the form'
	                ),
	                _react2.default.createElement(_Math2.default, { display: true,
	                    l: '\\Phi_{Ak}(\\mathbf x_t,\\mathbf y_t)=\\lambda_{Ak} f_{Ak}(y_{t},y_{t-1},x_t)' }),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'For some'
	                ),
	                _react2.default.createElement(
	                    'ol',
	                    null,
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(_Math2.default, { l: '\\Lambda=\\{\\lambda_k\\} \\in \\mathbb{R}^K' }),
	                        ' is a vector of weight parameters'
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        _react2.default.createElement(_Math2.default, { l: 'F_{eatures}=\\{f_k(y,y\',\\mathbf{x}_t)\\}' }),
	                        ' be a set of real-valued feature functions'
	                    )
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'where ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf{x}_t' }),
	                    ' and ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf{y}_t' }),
	                    ' are the current values of',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf x_t' }),
	                    ' and ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf y_t' }),
	                    ' respectively, i.e., the current observation and the current label, and ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf{y}_{t-1}' }),
	                    ' is the previous label. And so, recalling that the product of exponents equals the logarithm of their sum we can re-write ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: 'p(\\mathbf y|\\mathbf x)' }),
	                    ' as'
	                ),
	                _react2.default.createElement(_Math2.default, {
	                    l: 'p(\\mathbf{x},\\mathbf{y})' }),
	                ' that has the form:',
	                _react2.default.createElement(_Math2.default, { l: 'p(\\mathbf{x},\\mathbf{y})= \\frac{1}{Z(\\mathbf x)}\\exp\\left \\{\\sum_{k=1}^{K} \\lambda_k f_k(y_t, y_{t-1},\\mathbf x_t)\\right \\}',
	                    display: 'true' }),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'This is the canonical form of Conditional Random Fields.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Where ',
	                    _react2.default.createElement(_Math2.default, { l: 'Z(\\mathbf x)' }),
	                    ' is a normalization function that takes the observation vector as a parameter:',
	                    _react2.default.createElement(_Math2.default, { l: 'Z(\\mathbf x)=\\sum_{\\mathbf{y}} \\exp\\left \\{\\sum_{k=1}^{K} \\lambda_k f_k(y_t, y_{t-1},\\mathbf x_t)\\right \\}',
	                        display: 'true' })
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    _references2.default.cite(_bib2.default.sutton2006introduction),
	                    ' show that a logistic regression model is a simple CRF, and also that rewriting the probability distribution ',
	                    _react2.default.createElement(_Math2.default, { latex: 'p(\\mathbf x,\\mathbf y)' }),
	                    ' of a HMM yields a Conditional Random Field with a particular choice of feature functions.'
	                )
	            );
	        }
	    }]);

	    return LinearChainCRF;
	}(_react.Component);

	exports.default = LinearChainCRF;

/***/ },
/* 288 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _FigRef = __webpack_require__(245);

	var _FigRef2 = _interopRequireDefault(_FigRef);

	var _Image = __webpack_require__(259);

	var _Image2 = _interopRequireDefault(_Image);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	var _Math = __webpack_require__(260);

	var _Math2 = _interopRequireDefault(_Math);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var CRF = function (_Component) {
	    _inherits(CRF, _Component);

	    function CRF() {
	        _classCallCheck(this, CRF);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(CRF).apply(this, arguments));
	    }

	    _createClass(CRF, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                'section',
	                { id: 'hmm' },
	                _react2.default.createElement(
	                    'h4',
	                    null,
	                    'Hidden Markov Models'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Hidden Markov Models (HMMs) are a subclass of graphical models in which we model a linear sequence of observations ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf x=\\{x_t\\}_{t=1}^T' }),
	                    ' that are assumed to be generated by a sequence of hidden states ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf y=\\{y_t\\}_{t=1}^T' }),
	                    '. One example of an application would be spoken word recognition, in which samples of the sound waves can be seen as observations, and the actual phonemes as the hidden states.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'To assure computational tractability, HMMs make use of the Markov assumption, which is that:'
	                ),
	                _react2.default.createElement(
	                    'ol',
	                    null,
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        'any output variable (or ',
	                        _react2.default.createElement(
	                            'em',
	                            null,
	                            'label'
	                        ),
	                        ') ',
	                        _react2.default.createElement(_Math2.default, { l: 'y_t' }),
	                        ' only depends on ',
	                        _react2.default.createElement(_Math2.default, { l: 'y_{t-1}' }),
	                        ', where the initial probability ',
	                        _react2.default.createElement(_Math2.default, { l: 'p(y_{1})' }),
	                        ' is given'
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        'any input variable (or ',
	                        _react2.default.createElement(
	                            'em',
	                            null,
	                            'observation'
	                        ),
	                        ') ',
	                        _react2.default.createElement(_Math2.default, { l: 'x_t' }),
	                        ' only depends on the label ',
	                        _react2.default.createElement(_Math2.default, { l: 'y_t' }),
	                        '; the observation ',
	                        _react2.default.createElement(_Math2.default, { l: 'x_t' }),
	                        ' is generated by label ',
	                        _react2.default.createElement(_Math2.default, { l: 'y_t' }),
	                        '.'
	                    )
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'A HMM is then a graphical model which factorizes as follows:',
	                    _react2.default.createElement(_Math2.default, { display: 'true',
	                        l: 'p\\left ( \\mathbf x, \\mathbf y\\right ) = \\prod _{t=1}^T p(x_t)p(y_t) = \\prod _{t=1}^T p(x_t|y_t)p(y_t|y_{t-1})' })
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'It can be shown that HMMs are generative models. If we return to the representation of HMMs in ',
	                    _react2.default.createElement(_FigRef2.default, { fig: _figs2.default.graphicalModels }),
	                    ', we see that the white nodes represent labels and the grey nodes the observations. Typically, observations are given, but the labels need to be guessed. This is done by looping over all ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf y\\in Y' }),
	                    ' and selecting ',
	                    _react2.default.createElement(_Math2.default, { l: '\\mathbf y^*\\in Y' }),
	                    ' with the highest likelihood.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'To find plausible values of ',
	                    _react2.default.createElement(_Math2.default, { l: 'p(x_t|y_t)' }),
	                    ' and ',
	                    _react2.default.createElement(_Math2.default, { l: 'p(y_t|y_{t-1})' }),
	                    ', we typically use a set of known observation-label sequences with a parameter estimation method such as the ',
	                    _react2.default.createElement(
	                        'a',
	                        {
	                            className: 'wiki',
	                            href: 'https://en.wikipedia.org/wiki/Baum%E2%80%93Welch_algorithm' },
	                        'Baum-Welch algorithm'
	                    ),
	                    '[TODO ref?].'
	                )
	            );
	        }
	    }]);

	    return CRF;
	}(_react.Component);

	exports.default = CRF;

/***/ },
/* 289 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _FigRef = __webpack_require__(245);

	var _FigRef2 = _interopRequireDefault(_FigRef);

	var _Image = __webpack_require__(259);

	var _Image2 = _interopRequireDefault(_Image);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	var _Math = __webpack_require__(260);

	var _Math2 = _interopRequireDefault(_Math);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var LogRes = function (_Component) {
	    _inherits(LogRes, _Component);

	    function LogRes() {
	        _classCallCheck(this, LogRes);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(LogRes).apply(this, arguments));
	    }

	    _createClass(LogRes, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                'section',
	                null,
	                _react2.default.createElement(
	                    'h4',
	                    null,
	                    _react2.default.createElement(
	                        'strike',
	                        null,
	                        'Logistic regression'
	                    )
	                ),
	                _react2.default.createElement('p', null),
	                _react2.default.createElement(
	                    'section',
	                    null,
	                    _react2.default.createElement(
	                        'h5',
	                        null,
	                        _react2.default.createElement(
	                            'strike',
	                            null,
	                            'Logit function'
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        _react2.default.createElement(
	                            'strike',
	                            null,
	                            'We assume that the natural logarithm of the odds ratio is equivalent to a linear function of the independent variables. Id est, ',
	                            _react2.default.createElement(_Math2.default, {
	                                l: 'logit(p) = \\ln \\left(\\frac{p}{1-p} \\right) = \\beta_0 + \\beta_1 x_1 + ...' })
	                        )
	                    ),
	                    _react2.default.createElement('p', null)
	                )
	            );
	        }
	    }]);

	    return LogRes;
	}(_react.Component);

	exports.default = LogRes;

/***/ },
/* 290 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _FigRef = __webpack_require__(245);

	var _FigRef2 = _interopRequireDefault(_FigRef);

	var _Image = __webpack_require__(259);

	var _Image2 = _interopRequireDefault(_Image);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	var _Math = __webpack_require__(260);

	var _Math2 = _interopRequireDefault(_Math);

	var _figureInfoTfIdf = __webpack_require__(291);

	var _figureInfoTfIdf2 = _interopRequireDefault(_figureInfoTfIdf);

	var _figTitlePattern = __webpack_require__(295);

	var _figTitlePattern2 = _interopRequireDefault(_figTitlePattern);

	var _figureRelativeTitleCountForTerms = __webpack_require__(298);

	var _figureRelativeTitleCountForTerms2 = _interopRequireDefault(_figureRelativeTitleCountForTerms);

	var _figureTitleTfIdf = __webpack_require__(300);

	var _figureTitleTfIdf2 = _interopRequireDefault(_figureTitleTfIdf);

	var _figureSectionTfIdf = __webpack_require__(303);

	var _figureSectionTfIdf2 = _interopRequireDefault(_figureSectionTfIdf);

	var _figureTitleWordCount = __webpack_require__(308);

	var _figureTitleWordCount2 = _interopRequireDefault(_figureTitleWordCount);

	var _Source = __webpack_require__(220);

	var _Source2 = _interopRequireDefault(_Source);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var FeatureSelection = function (_Component) {
	    _inherits(FeatureSelection, _Component);

	    function FeatureSelection() {
	        _classCallCheck(this, FeatureSelection);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(FeatureSelection).apply(this, arguments));
	    }

	    _createClass(FeatureSelection, [{
	        key: 'render',
	        value: function render() {
	            var relativeToRoot = this.props.path.match(/\//g).slice(1).map(function (_) {
	                return "../";
	            }).join("");
	            return _react2.default.createElement(
	                'div',
	                null,
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'In order to tag tokens with our target labels, we observe a number of patterns in existing documents. We see that a rich case law document typically consists of the following parts:'
	                ),
	                _react2.default.createElement(
	                    'section',
	                    { id: 'info' },
	                    _react2.default.createElement(
	                        'h3',
	                        null,
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            '*.info'
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        'The first element in a document is typically a unique header element with a tagname of either ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'uitspraak.info'
	                        ),
	                        ' or ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'conclusie.info'
	                        ),
	                        ' for two types of case law (judgments and conclusions, respectively). We refer to either of these as ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            '*.info'
	                        ),
	                        '. ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            '*.info'
	                        ),
	                        ' elements contain interesting metadata such as names an court location. The information is generally not semantically marked up, but is reasonably easy to parse thanks to style consistencies in authors (e.g., most units of metadata are on a separate line).'
	                    ),
	                    _react2.default.createElement(
	                        'strike',
	                        null,
	                        _react2.default.createElement(
	                            'p',
	                            null,
	                            'The ',
	                            _react2.default.createElement(
	                                'code',
	                                null,
	                                '*.info'
	                            ),
	                            ' element typically contains metadata about the legal case, such as:'
	                        ),
	                        _react2.default.createElement(
	                            'ul',
	                            null,
	                            _react2.default.createElement(
	                                'li',
	                                null,
	                                'A case law identifier following some identification scheme'
	                            ),
	                            _react2.default.createElement(
	                                'li',
	                                null,
	                                'Heading on the type of judgment (e.g. "U I T S P R A A K")'
	                            ),
	                            _react2.default.createElement(
	                                'li',
	                                null,
	                                'Date of judgment'
	                            ),
	                            _react2.default.createElement(
	                                'li',
	                                null,
	                                'Branch of the judiciary (i.e. type of court)'
	                            ),
	                            _react2.default.createElement(
	                                'li',
	                                null,
	                                'Location of the judgment (i.e. court, jurisdiction)'
	                            ),
	                            _react2.default.createElement(
	                                'li',
	                                null,
	                                'A description of the parties involved, possibly detailing their names, roles, locations, representatives'
	                            ),
	                            _react2.default.createElement(
	                                'li',
	                                null,
	                                'A reference to preceding cases, for example in the case of an appeal to a previous judgment'
	                            )
	                        ),
	                        _react2.default.createElement(
	                            'p',
	                            null,
	                            'The order and formatting of this information appears in a multitude of order and formatting, making it difficult to write a deterministic grammar for recognizing a header section. [TODO figref] suggests that analyzing tf-idf in ',
	                            _react2.default.createElement(
	                                'code',
	                                null,
	                                '*.info'
	                            ),
	                            ' elements does not seem to be a particularly useful method of generating features that select for these metadata items. But it is easy for the human eye to recognize some recurring patterns.'
	                        ),
	                        _react2.default.createElement(_figureInfoTfIdf2.default, null),
	                        _react2.default.createElement(
	                            'p',
	                            null,
	                            'See, e.g., ',
	                            _react2.default.createElement(
	                                'cite',
	                                null,
	                                _react2.default.createElement(
	                                    'a',
	                                    {
	                                        href: 'https://rechtspraak.lawreader.nl/ecli/ECLI:NL:GHARL:2014:9139' },
	                                    'ECLI:NL:GHARL:2014:9139'
	                                )
	                            ),
	                            ' for an example.'
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        'A ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            '*.info'
	                        ),
	                        ' element is followed by any number of sections.'
	                    )
	                ),
	                _react2.default.createElement(
	                    'section',
	                    null,
	                    _react2.default.createElement(
	                        'h3',
	                        null,
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'section'
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'section'
	                        ),
	                        ' tags generally contain a title element, and may contain an attribute denoting the section role. A ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'section'
	                        ),
	                        ' is a generic grouping of running text, and can be nested to create a section hierarchy.'
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        'In practice we see ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'role'
	                        ),
	                        ' attributes of either'
	                    ),
	                    _react2.default.createElement(
	                        'ul',
	                        null,
	                        _react2.default.createElement(
	                            'li',
	                            null,
	                            _react2.default.createElement(
	                                'code',
	                                null,
	                                'beslissing'
	                            ),
	                            ' (judgment)'
	                        ),
	                        _react2.default.createElement(
	                            'li',
	                            null,
	                            _react2.default.createElement(
	                                'code',
	                                null,
	                                'overwegingen'
	                            ),
	                            ' (considerations)'
	                        ),
	                        _react2.default.createElement(
	                            'li',
	                            null,
	                            _react2.default.createElement(
	                                'code',
	                                null,
	                                'procesverloop'
	                            ),
	                            ' (proceedings)'
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        'Many sections have no role, although one may imagine other roles than the above, such as ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'feiten'
	                        ),
	                        ' (facts). The above three are the only roles that currently appear in markup from Rechtspraak.nl. Assigning roles to sections is an interesting avenue of research, but we do not explore this in this thesis. Instead, we limit ourselves to demarcating sections and assigning some hierarchical section structure.'
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        _react2.default.createElement(
	                            'strike',
	                            null,
	                            'Note that, realistically, there are more types of sections than just ',
	                            _react2.default.createElement(
	                                'code',
	                                null,
	                                'beslissing'
	                            ),
	                            ',',
	                            _react2.default.createElement(
	                                'code',
	                                null,
	                                'overwegingen'
	                            ),
	                            ' and ',
	                            _react2.default.createElement(
	                                'code',
	                                null,
	                                'procesverloop'
	                            ),
	                            '. One example of this is in ',
	                            _react2.default.createElement(
	                                'cite',
	                                null,
	                                _react2.default.createElement(
	                                    'a',
	                                    {
	                                        href: 'https://rechtspraak.lawreader.nl/ecli/ECLI:NL:GHARL:2014:9139' },
	                                    'ECLI:NL:GHARL:2014:9139'
	                                )
	                            ),
	                            ', which contains additional sections on facts and on costs. In tagging section roles, we limit ourselves to the above three, since those are the only values that Rechtspraak.nl uses.',
	                            _react2.default.createElement(_Source2.default, {
	                                href: 'https://rechtspraak.cloudant.com/docs/_design/stats/_view/section_roles?group_level=1' })
	                        )
	                    )
	                ),
	                _react2.default.createElement(
	                    'section',
	                    null,
	                    _react2.default.createElement(
	                        'h3',
	                        null,
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'title'
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'title'
	                        ),
	                        ' elements typically occur as the first descendant of a ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'section'
	                        ),
	                        ' element, and contain either a numbering in a ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'nr'
	                        ),
	                        ' node, or some text, or both.'
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        'We assume that ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'title'
	                        ),
	                        ' elements consist of an optional numbering, followed by a handful of words (see ',
	                        _react2.default.createElement(_FigRef2.default, { fig: _figs2.default.figTitleWordCount }),
	                        ').'
	                    ),
	                    _react2.default.createElement(_figureTitleWordCount2.default, null),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        _react2.default.createElement(_FigRef2.default, {
	                            fig: _figs2.default.titleRelativeWordCount }),
	                        ', ',
	                        _react2.default.createElement(_FigRef2.default, {
	                            fig: _figs2.default.figTitleTreemap }),
	                        ', ',
	                        _react2.default.createElement(_FigRef2.default, {
	                            fig: _figs2.default.titleTf }),
	                        ' and ',
	                        _react2.default.createElement(_FigRef2.default, { fig: _figs2.default.tfidf }),
	                        ' show us that titles usually have a distinct form, and display patterns that often recur.'
	                    ),
	                    _react2.default.createElement(_figTitlePattern2.default, null),
	                    _react2.default.createElement(_figureRelativeTitleCountForTerms2.default, null),
	                    _react2.default.createElement(_figureTitleTfIdf2.default, null),
	                    _react2.default.createElement(_figureSectionTfIdf2.default, null)
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Based on the metrics and observations above, we define about 250 binary features for our automatic tagger. The most prominent ones include:'
	                ),
	                _react2.default.createElement(
	                    'ul',
	                    null,
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        'word count (text block contains 1, 2, 3, 4, 5—10 or more than 10 words)'
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        'whether the token is preceded or followed by any of a number of features, such as numberings or inline text'
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        'whether the token contains bracketed text'
	                    ),
	                    _react2.default.createElement(
	                        'li',
	                        null,
	                        'whether the token matches a low or high confidence known title (similar titles are consolidated into a regular expression)'
	                    )
	                ),
	                _react2.default.createElement(
	                    'srike',
	                    null,
	                    'The full set of features is can be accessed from the ',
	                    _react2.default.createElement(
	                        'a',
	                        { href: '' },
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'Features'
	                        ),
	                        ' class in the source code'
	                    ),
	                    '.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'We use these features in a probabilistic tagger for which we train a CRF model, and also for a deterministic tagger. We now introduce these models, and report their results.'
	                )
	            );
	        }

	        // <table>
	        // <thead>
	        // <tr>
	        // <th>Name</th>
	        // <th>Regular expression</th>
	        // <th>Description</th>
	        // </tr>
	        // </thead>
	        // <tbody>
	        // <tr>
	        // <td><code>_NUM</code></td>
	        // <td><code className="prettify">/\b([0-9]+|i&#123;1,3&#125;|i?vi&#123;0,3&#125;|i?xi&#123;
	        // 0,3&#125;)\b/</code></td>
	        // <td><p>describes a number (a sequence of digits, or a roman numeral)</p></td>
	        // </tr>
	        // <tr>
	        // <td><code>_ART</code></td>
	        // <td><code className="prettify">/\b(de|het|een)\b/</code></td>
	        // <td><p>describes an article (the/a)</p></td>
	        // </tr>
	        // <tr>
	        // <td><code>_PREP</code></td>
	        // <td><code className="prettify">/\b(van|in|op)\b/</code></td>
	        // <td><p>describes a preposition</p></td>
	        // </tr>
	        //
	        // <tr>
	        // <td><code>_MISC_LEGAL_TERMS</code></td>
	        // <td><code className="prettify">/(rechtbank|hoger|beroep|eerst|tweed)/</code></td>
	        // <td><p>''</p></td>
	        // </tr>
	        // <tr>
	        // <td><code>_CASE</code></td>
	        // <td><code className="prettify">/(geschil|geding|aanleg)/</code></td>
	        // <td><p>''</p></td>
	        // </tr>
	        // <tr>
	        // <td><code>_FACT</code></td>
	        // <td><code className="prettify">/feit/</code></td>
	        // <td><p>stem of 'feit' ('fact')</p></td>
	        // </tr>
	        // <tr>
	        // <td><code>_GROUND</code></td>
	        // <td><code className="prettify">/grond/</code></td>
	        // <td><p>stem of 'grond' ('ground')</p></td>
	        // </tr>
	        //
	        // <tr>
	        // <td><code>_JUDGMENT</code></td>
	        // <td><code className="prettify">/(slotsom|beslis|motiver)/</code></td>
	        // <td><p>stem of words pertaining to a 'judgment' section</p></td>
	        // </tr>
	        // <tr>
	        // <td><code>_CONSIDERATION</code></td>
	        // <td><code
	        // className="prettify">/((bewijs)?overweg|beoordel|(straf)?motivering|beschouwing)/</code>
	        // </td>
	        // <td><p>stem of words pertaining to a 'considerations' section</p></td>
	        // </tr>
	        // <tr>
	        // <td><code>_PROCEEDINGS</code></td>
	        // <td><code
	        // className="prettify">/voorgeschied|onsta|((proces)?(verlo|gang)|procedu|ontstaan|loop)/</code>
	        // </td>
	        // <td><p>stem of words pertaining to a 'proceedings' section</p></td>
	        // </tr>
	        // <tr>
	        // <td><code>_UP_TO_6</code></td>
	        // <td>
	        // </td>
	        // <td><p>Whether the containing text block contains six words or less</p></td>
	        // </tr>
	        // <tr>
	        // <td><code>_UP_TO_10</code></td>
	        // <td>
	        // </td>
	        // <td><p>Whether the containing text block contains 10 words or less</p></td>
	        // </tr>
	        // </tbody>
	        // </table>

	    }]);

	    return FeatureSelection;
	}(_react.Component);

	exports.default = FeatureSelection;

/***/ },
/* 291 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _getTermFrequencyData = __webpack_require__(292);

	var _getTermFrequencyData2 = _interopRequireDefault(_getTermFrequencyData);

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _underscore = __webpack_require__(210);

	var _underscore2 = _interopRequireDefault(_underscore);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _PercentageBar = __webpack_require__(294);

	var _PercentageBar2 = _interopRequireDefault(_PercentageBar);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var FigureTfIdf = function (_React$Component) {
	    _inherits(FigureTfIdf, _React$Component);

	    function FigureTfIdf() {
	        _classCallCheck(this, FigureTfIdf);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(FigureTfIdf).apply(this, arguments));
	    }

	    _createClass(FigureTfIdf, [{
	        key: 'render',
	        value: function render() {
	            var nTerms = 20;
	            var tfIdfScores = _getTermFrequencyData2.default.tfidfForAllTerms(nTerms);
	            var maxVal = tfIdfScores[0][1];
	            _underscore2.default.map(tfIdfScores, function (arr) {
	                arr[2] = (100 * arr[1] / maxVal).toFixed(2) + "%";
	            });

	            return _react2.default.createElement(
	                'figure',
	                { id: _figs2.default.tfidf.id },
	                _react2.default.createElement(
	                    'table',
	                    { className: ' table' },
	                    _react2.default.createElement(
	                        'thead',
	                        null,
	                        _react2.default.createElement(
	                            'tr',
	                            null,
	                            _react2.default.createElement('th', null),
	                            _react2.default.createElement(
	                                'th',
	                                null,
	                                'term'
	                            ),
	                            _react2.default.createElement(
	                                'th',
	                                null,
	                                'tf-idf score'
	                            )
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'tbody',
	                        null,
	                        _underscore2.default.map(tfIdfScores, function (term, o) {
	                            var tfIdfScoreStr = term[1].toFixed(2);
	                            return _react2.default.createElement(
	                                'tr',
	                                { key: term[0] },
	                                _react2.default.createElement(
	                                    'th',
	                                    { className: 'nr' },
	                                    o + 1
	                                ),
	                                _react2.default.createElement(
	                                    'td',
	                                    null,
	                                    term[0]
	                                ),
	                                _react2.default.createElement(
	                                    'td',
	                                    { className: 'tf-idf-score' },
	                                    _react2.default.createElement(_PercentageBar2.default, { percentage: term[2], text: tfIdfScoreStr + '' }),
	                                    _react2.default.createElement(
	                                        'span',
	                                        { className: 'perc-text' },
	                                        tfIdfScoreStr
	                                    )
	                                )
	                            );
	                        })
	                    )
	                ),
	                _react2.default.createElement(
	                    'figcaption',
	                    null,
	                    _react2.default.createElement(
	                        'span',
	                        { className: 'figure-number' },
	                        'Fig ',
	                        _figs2.default.tfidf.num,
	                        '.'
	                    ),
	                    ' Top ',
	                    nTerms,
	                    ' tf-idf scores for stemmed words in ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        '*.info'
	                    ),
	                    ' elements. Stemming is performed using the ',
	                    _react2.default.createElement(
	                        'a',
	                        { href: 'http://snowball.tartarus.org/algorithms/dutch/stemmer.html' },
	                        'Snowball algorithm for Dutch'
	                    ),
	                    '.'
	                )
	            );
	        }
	    }]);

	    return FigureTfIdf;
	}(_react2.default.Component);

	exports.default = FigureTfIdf;

/***/ },
/* 292 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var _ = __webpack_require__(210);

	var dataTfIdf = __webpack_require__(293);

	function tfidfForAllTerms(topN) {
	    var sorted = _.map(dataTfIdf, function (e) {
	        return [e.term, e.score];
	    });
	    if (topN) sorted.splice(topN);

	    // console.log(sorted)
	    return sorted;
	}

	module.exports = {
	    tfidfForAllTerms: tfidfForAllTerms
	};

/***/ },
/* 293 */
/***/ function(module, exports) {

	"use strict";

	module.exports = [{ "term": "_NUM", "score": 863541.2613095865 }, { "term": "_PUNCT", "score": 849119.0060654248 }, {
	    "term": "_ART",
	    "score": 624798.3127026781
	}, { "term": ",", "score": 606224.7574059183 }, { "term": "[", "score": 462440.5252064248 }, {
	    "term": "van",
	    "score": 438068.95202317025
	}, { "term": "/", "score": 433333.48285467044 }, { "term": "te", "score": 378547.33573643764 }, {
	    "term": "],",
	    "score": 271151.8072226816
	}, { "term": "in", "score": 270082.1073347967 }, { "term": "]", "score": 265397.7547965963 }, {
	    "term": "-",
	    "score": 263263.7804801338
	}, { "term": "mr", "score": 262910.44169437274 }, { "term": "en", "score": 233666.5256017004 }, {
	    "term": "zaaknummer",
	    "score": 205373.72233549555
	}, { "term": "advocat", "score": 186789.6501626982 }, {
	    "term": "rechtbank",
	    "score": 168679.9979446142
	}, { "term": "wonend", "score": 165746.15115876673 }, { "term": "(", "score": 157894.32180234598 }, {
	    "term": "zak",
	    "score": 156488.76634784217
	}, { "term": "teg", "score": 152962.8730120228 }, { "term": "woonplat", "score": 144234.9968097035 }, {
	    "term": "hierna",
	    "score": 141307.63209192973
	}, { "term": "gevestigd", "score": 137097.51857341675 }, {
	    "term": "gemachtigd",
	    "score": 136029.85622169368
	}, { "term": ")", "score": 129446.697784994 }, {
	    "term": "amsterdam",
	    "score": 126881.60051673395
	}, { "term": "uitsprak", "score": 121640.61270640766 }, {
	    "term": "op",
	    "score": 119851.98129427247
	}, { "term": "gerechtshof", "score": 119263.23217349063 }, {
	    "term": "vonnis",
	    "score": 116247.43484509361
	}, { "term": "kamer", "score": 108401.80933697626 }, { "term": "afdel", "score": 106916.94730486015 }, {
	    "term": "m",
	    "score": 103635.12768152743
	}, { "term": "j", "score": 103090.72317496609 }, { "term": "b", "score": 100072.07817053328 }, {
	    "term": "c",
	    "score": 98010.64409801581
	}, { "term": "eiser", "score": 94120.74147300371 }, {
	    "term": "meervoud",
	    "score": 90973.752537538
	}, { "term": "gedaagd", "score": 90014.02190824946 }, {
	    "term": "a",
	    "score": 88214.9108694203
	}, { "term": "vennootschap", "score": 86136.80402385052 }, {
	    "term": "beroep",
	    "score": 86088.65660626792
	}, { "term": "met", "score": 83533.38322507023 }, { "term": "appellant", "score": 82957.94390502897 }, {
	    "term": "noem",
	    "score": 82898.82341830924
	}, { "term": "s", "score": 81614.1650656842 }, { "term": "eiseres", "score": 81597.17713425799 }, {
	    "term": "verdacht",
	    "score": 81514.02299370199
	}, { "term": "arrest", "score": 80662.1537426054 }];

/***/ },
/* 294 */
/***/ function(module, exports, __webpack_require__) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var PercentageBar = function (_React$Component) {
	    _inherits(PercentageBar, _React$Component);

	    function PercentageBar() {
	        _classCallCheck(this, PercentageBar);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(PercentageBar).apply(this, arguments));
	    }

	    _createClass(PercentageBar, [{
	        key: "render",
	        value: function render() {
	            var txt = this.props.text ? this.props.text : this.props.percentage;
	            return _react2.default.createElement(
	                "svg",
	                { width: "80", height: "10" },
	                _react2.default.createElement(
	                    "rect",
	                    { className: "perc-bar bg", width: "100%", height: "100%" },
	                    _react2.default.createElement(
	                        "title",
	                        null,
	                        txt
	                    )
	                ),
	                _react2.default.createElement(
	                    "rect",
	                    { className: "perc-bar", width: this.props.percentage, height: "100%" },
	                    _react2.default.createElement(
	                        "title",
	                        null,
	                        txt
	                    )
	                )
	            );
	        }
	    }]);

	    return PercentageBar;
	}(_react2.default.Component);

	exports.default = PercentageBar;

	PercentageBar.propTypes = {
	    percentage: _react2.default.PropTypes.string.isRequired,
	    text: _react2.default.PropTypes.string.isRequired
	};

/***/ },
/* 295 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _rawData = __webpack_require__(296);

	var _rawData2 = _interopRequireDefault(_rawData);

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _TreeMap = __webpack_require__(297);

	var _TreeMap2 = _interopRequireDefault(_TreeMap);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var FigureTitlePattern = function (_React$Component) {
	    _inherits(FigureTitlePattern, _React$Component);

	    function FigureTitlePattern() {
	        _classCallCheck(this, FigureTitlePattern);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(FigureTitlePattern).apply(this, arguments));
	    }

	    _createClass(FigureTitlePattern, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                'figure',
	                { id: _figs2.default.figTitleTreemap.id },
	                _react2.default.createElement(_TreeMap2.default, { data: _rawData2.default.data, sourceHref: _rawData2.default.href }),
	                _react2.default.createElement(
	                    'figcaption',
	                    null,
	                    _react2.default.createElement(
	                        'span',
	                        { className: 'figure-number' },
	                        'Fig ',
	                        _figs2.default.figTitleTreemap.num,
	                        '.'
	                    ),
	                    ' Absolute frequency of title patterns that occur more than 5 times in the corpus, for all types of section that Rechtspraak.nl divides documents in.'
	                )
	            );
	        }
	    }]);

	    return FigureTitlePattern;
	}(_react2.default.Component);

	exports.default = FigureTitlePattern;

/***/ },
/* 296 */
/***/ function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.default = { "data": { "name": "Title patterns", "children": [{ "name": "null", "children": [{ "name": "_EMPTY", "size": 990 }, { "name": "a", "size": 281 }, { "name": "a hij in de periode van juli tot en met augustus inde boerderij adres te tripscompagnie ter uitvoering van het door verdachte voorgenomen misdrijf om tezamen en in vereniging met anderen met het oogmerk om zich enof een anderen wederrechtelijk te bevoordelen door geweld enof bedreiging met geweld een persoon genaamd benadeelde te dwingen tot de afgifte van geld met dat oogmerk tezamen en in vereniging met verdachtes mededaders", "size": 6 }, { "name": "aanduiding bestreden besluit", "size": 29 }, { "name": "aanduiding van het geschil", "size": 8 }, { "name": "aanhechten draagkrachtberekeningen", "size": 46 }, { "name": "aanleiding", "size": 8 }, { "name": "aanleiding van het geschil", "size": 14 }, { "name": "aanslag beschikking bezwaar en geding in eerste aanleg", "size": 58 }, { "name": "aanslag beschikkingen bezwaar en geding in eerste aanleg", "size": 21 }, { "name": "aanslag bezwaar en beroep", "size": 6 }, { "name": "aanslag bezwaar en geding in eerste aanleg", "size": 67 }, { "name": "aanslagen beschikkingen bezwaar en geding in eerste aanleg", "size": 31 }, { "name": "aanslagen beschikkingen bezwaren en geding in eerste aanleg", "size": 6 }, { "name": "aanslagen bezwaar en geding in eerste aanleg", "size": 17 }, { "name": "aanvraag tot herziening", "size": 61 }, { "name": "achtergrond", "size": 6 }, { "name": "achtergrond van de geschillen", "size": 8 }, { "name": "achtergrond van het geschil", "size": 6 }, { "name": "ad", "size": 12 }, { "name": "ad a", "size": 12 }, { "name": "ad b", "size": 9 }, { "name": "adoptie", "size": 9 }, { "name": "adres", "size": 18 }, { "name": "advies", "size": 20 }, { "name": "afdeling civiel recht", "size": 40 }, { "name": "afdeling strafrecht", "size": 98 }, { "name": "afdoening", "size": 11 }, { "name": "afronding", "size": 12 }, { "name": "afschrift verzonden aan partijen op", "size": 7 }, { "name": "afschrift verzonden op", "size": 16 }, { "name": "afstand van rechtsmiddelen", "size": 7 }, { "name": "afwijzing verlenging ondertoezichtstelling", "size": 6 }, { "name": "ag", "size": 24 }, { "name": "agreement", "size": 12 }, { "name": "algemeen", "size": 35 }, { "name": "algemeen rechtskader", "size": 14 }, { "name": "algemene uiteenzetting gang van zaken en december", "size": 8 }, { "name": "algemene voorwaarden", "size": 8 }, { "name": "alimentatie", "size": 17 }, { "name": "als verklaring van betrokkene", "size": 10 }, { "name": "als verklaring van betrokkene afgelegd op juni", "size": 6 }, { "name": "als verklaring van verbalisant", "size": 9 }, { "name": "als verklaring van verdachte", "size": 9 }, { "name": "analyse", "size": 6 }, { "name": "ap", "size": 239 }, { "name": "appellant", "size": 499 }, { "name": "appellant en", "size": 14 }, { "name": "appellant sub", "size": 165 }, { "name": "appellant sub en", "size": 19 }, { "name": "appellant sub wonende te woonplaats", "size": 8 }, { "name": "appellant wonende te woonplaats", "size": 74 }, { "name": "appellante", "size": 152 }, { "name": "appellante sub", "size": 35 }, { "name": "appellante wonende te woonplaats", "size": 16 }, { "name": "arrest", "size": 577 }, { "name": "arrest dd april", "size": 33 }, { "name": "arrest dd augustus", "size": 10 }, { "name": "arrest dd december", "size": 12 }, { "name": "arrest dd februari", "size": 21 }, { "name": "arrest dd januari", "size": 20 }, { "name": "arrest dd juli", "size": 24 }, { "name": "arrest dd juni", "size": 22 }, { "name": "arrest dd maart", "size": 28 }, { "name": "arrest dd mei", "size": 22 }, { "name": "arrest dd november", "size": 12 }, { "name": "arrest dd oktober", "size": 18 }, { "name": "arrest dd september", "size": 23 }, { "name": "arrest in dit geding", "size": 7 }, { "name": "arrest van april", "size": 46 }, { "name": "arrest van augustus", "size": 31 }, { "name": "arrest van de economische kamer van het gerechtshof shertogenbosch", "size": 9 }, { "name": "arrest van de familiekamer dd april", "size": 7 }, { "name": "arrest van de familiekamer dd juni", "size": 9 }, { "name": "arrest van de familiekamer dd november", "size": 7 }, { "name": "arrest van de meervoudige kamer voor strafzaken", "size": 160 }, { "name": "arrest van de meervoudige kamer voor strafzaken van het gerechtshof", "size": 428 }, { "name": "arrest van de meervoudige kamer voor strafzaken van het gerechtshof te", "size": 13 }, { "name": "arrest van december", "size": 29 }, { "name": "arrest van februari", "size": 39 }, { "name": "arrest van januari", "size": 31 }, { "name": "arrest van juli", "size": 67 }, { "name": "arrest van juni", "size": 60 }, { "name": "arrest van maart", "size": 56 }, { "name": "arrest van mei", "size": 45 }, { "name": "arrest van november", "size": 36 }, { "name": "arrest van oktober", "size": 35 }, { "name": "arrest van september", "size": 49 }, { "name": "article echr", "size": 6 }, { "name": "artikel", "size": 52 }, { "name": "artikel belastbaar feit", "size": 12 }, { "name": "artikel belastingplicht", "size": 6 }, { "name": "artikel bw", "size": 12 }, { "name": "artikel tarieven", "size": 7 }, { "name": "artikel van de olw", "size": 7 }, { "name": "asielrelaas", "size": 6 }, { "name": "aw", "size": 6 }, { "name": "awb nummers en namen eisers", "size": 6 }, { "name": "b", "size": 183 }, { "name": "b e s l i s s i n g", "size": 12 }, { "name": "beantwoording van de prejudicile vraag", "size": 8 }, { "name": "beantwoording van de prejudicile vragen", "size": 13 }, { "name": "beantwoording van de vragen", "size": 10 }, { "name": "bedreiging met enig misdrijf tegen het leven gericht", "size": 42 }, { "name": "bedreiging met enig misdrijf tegen het leven gericht meermalen gepleegd", "size": 19 }, { "name": "bedreiging met zware mishandeling", "size": 9 }, { "name": "bedrijf", "size": 20 }, { "name": "behandeling", "size": 9 }, { "name": "behandeling in raadkamer", "size": 38 }, { "name": "behandeling ter terechtzitting", "size": 11 }, { "name": "behandeling van de klachten", "size": 9 }, { "name": "behandeling van de middelen", "size": 22 }, { "name": "behandeling van de zaak", "size": 7 }, { "name": "behandeling van het geschil door het hof", "size": 6 }, { "name": "behandeling van het hoger beroep", "size": 13 }, { "name": "behandeling van het middel", "size": 10 }, { "name": "behoefte", "size": 9 }, { "name": "beklaagde", "size": 16 }, { "name": "beklag", "size": 31 }, { "name": "belaging", "size": 11 }, { "name": "belanghebbende", "size": 100 }, { "name": "belanghebbende sub", "size": 24 }, { "name": "belanghebbenden", "size": 9 }, { "name": "benadeelde partij", "size": 547 }, { "name": "benadeelde partij benadeelde", "size": 29 }, { "name": "benadeelde partij en de schadevergoedingsmaatregel", "size": 52 }, { "name": "benadeelde partij minderjarige zaak", "size": 9 }, { "name": "benadeelde partij slachtoffer", "size": 68 }, { "name": "benadeelde partij slachtoffer feit", "size": 9 }, { "name": "benadeelde partijen", "size": 256 }, { "name": "benadeelde partijen en de schadevergoedingsmaatregel", "size": 7 }, { "name": "beoordeling", "size": 196 }, { "name": "beoordeling van de zaak", "size": 11 }, { "name": "beoordeling van het bewijs", "size": 100 }, { "name": "beoordeling van het eerste middel", "size": 9 }, { "name": "beoordeling van het geschil", "size": 8 }, { "name": "beoordeling van het geschil en de motivering van de beslissing", "size": 8 }, { "name": "beoordeling van het hoger beroep", "size": 368 }, { "name": "beoordeling van het incident", "size": 7 }, { "name": "beoordeling van het middel", "size": 12 }, { "name": "beoordeling van het principale en het incidentele hoger beroep", "size": 7 }, { "name": "beoordeling van het verzoek", "size": 13 }, { "name": "berechtingsrapport", "size": 6 }, { "name": "beroep", "size": 6 }, { "name": "beroep is ongegrond", "size": 12 }, { "name": "beroep op noodweerexces", "size": 6 }, { "name": "beschikkende", "size": 29 }, { "name": "beschikking", "size": 41 }, { "name": "beschikking aanslag bezwaar en geding in eerste aanleg", "size": 69 }, { "name": "beschikking bezwaar en beroep", "size": 10 }, { "name": "beschikking bezwaar en geding in eerste aanleg", "size": 31 }, { "name": "beschikking dd augustus", "size": 6 }, { "name": "beschikking in de zaak van", "size": 38 }, { "name": "beschikking ontbinding arbeidsovereenkomst", "size": 20 }, { "name": "beschikking op het op april ingekomen verzoek van", "size": 12 }, { "name": "beschikking op het op april ingekomen verzoekschrift van", "size": 11 }, { "name": "beschikking op het op augustus ingekomen verzoek van", "size": 9 }, { "name": "beschikking op het op augustus ingekomen verzoekschrift van", "size": 13 }, { "name": "beschikking op het op december ingekomen verzoek van", "size": 13 }, { "name": "beschikking op het op december ingekomen verzoekschrift van", "size": 10 }, { "name": "beschikking op het op februari ingekomen verzoek van", "size": 8 }, { "name": "beschikking op het op februari ingekomen verzoekschrift van", "size": 11 }, { "name": "beschikking op het op januari ingekomen verzoek van", "size": 6 }, { "name": "beschikking op het op januari ingekomen verzoekschrift van", "size": 12 }, { "name": "beschikking op het op juli ingekomen verzoek van", "size": 16 }, { "name": "beschikking op het op juli ingekomen verzoekschrift van", "size": 10 }, { "name": "beschikking op het op juni ingekomen verzoek van", "size": 15 }, { "name": "beschikking op het op juni ingekomen verzoekschrift van", "size": 12 }, { "name": "beschikking op het op maart ingekomen verzoek van", "size": 10 }, { "name": "beschikking op het op maart ingekomen verzoekschrift van", "size": 11 }, { "name": "beschikking op het op mei ingekomen verzoek van", "size": 12 }, { "name": "beschikking op het op mei ingekomen verzoekschrift van", "size": 16 }, { "name": "beschikking op het op november ingekomen verzoek van", "size": 10 }, { "name": "beschikking op het op november ingekomen verzoekschrift van", "size": 7 }, { "name": "beschikking op het op oktober ingekomen verzoek van", "size": 14 }, { "name": "beschikking op het op oktober ingekomen verzoekschrift van", "size": 7 }, { "name": "beschikking op het op september ingekomen verzoek van", "size": 7 }, { "name": "beschikking op het op september ingekomen verzoekschrift van", "size": 10 }, { "name": "beschikking van april", "size": 8 }, { "name": "beschikking van augustus", "size": 8 }, { "name": "beschikking van de kantonrechter", "size": 15 }, { "name": "beschikking van juli", "size": 9 }, { "name": "beschikking van mei", "size": 6 }, { "name": "beschikking van oktober", "size": 7 }, { "name": "beschikking van september", "size": 8 }, { "name": "beschikkingen aanslagen bezwaar en geding in eerste aanleg", "size": 19 }, { "name": "beschikkingen bezwaar en geding in eerste aanleg", "size": 9 }, { "name": "beschikt", "size": 7 }, { "name": "beschouwing", "size": 44 }, { "name": "beschouwing rechtbank", "size": 16 }, { "name": "beslag", "size": 745 }, { "name": "beslissing", "size": 3067 }, { "name": "beslissing het hof", "size": 10 }, { "name": "beslissing inzake het bewijs", "size": 10 }, { "name": "beslist", "size": 12 }, { "name": "besloten vennootschap", "size": 11 }, { "name": "besloten vennootschap met beperkte aansprakelijkheid", "size": 117 }, { "name": "beslssing", "size": 7 }, { "name": "bespreking cassatiemiddel", "size": 8 }, { "name": "bespreking en waardering van bewijsmiddelen", "size": 10 }, { "name": "bespreking middel onafhankelijkheid en onpartijdigheid van de rechter", "size": 6 }, { "name": "bespreking van bewijsverweren", "size": 8 }, { "name": "bespreking van de cassatieklachten", "size": 7 }, { "name": "bespreking van de cassatiemiddelen", "size": 52 }, { "name": "bespreking van de gevoerde verweren", "size": 8 }, { "name": "bespreking van de grieven", "size": 63 }, { "name": "bespreking van de klachten", "size": 18 }, { "name": "bespreking van de middelen", "size": 10 }, { "name": "bespreking van de overige grieven", "size": 6 }, { "name": "bespreking van de verweren", "size": 8 }, { "name": "bespreking van een verweer", "size": 10 }, { "name": "bespreking van het bewijsverweer", "size": 6 }, { "name": "bespreking van het cassatieberoep", "size": 27 }, { "name": "bespreking van het cassatiemiddel", "size": 348 }, { "name": "bespreking van het incidenteel cassatieberoep", "size": 8 }, { "name": "bespreking van het incidentele cassatieberoep", "size": 7 }, { "name": "bespreking van het middel", "size": 31 }, { "name": "bespreking van het principaal cassatieberoep", "size": 13 }, { "name": "bespreking van het principaal cassatiemiddel", "size": 8 }, { "name": "bespreking van het principale cassatieberoep", "size": 11 }, { "name": "bespreking van het principale cassatiemiddel", "size": 13 }, { "name": "bespreking van middel op de zaak betrekking hebbende stukken", "size": 6 }, { "name": "bestreden beschikking", "size": 14 }, { "name": "bestreden besluit", "size": 8 }, { "name": "bestreden tuchtuitspraak", "size": 7 }, { "name": "bestreden uitspraak", "size": 23 }, { "name": "bestreden uitspraak op bezwaar", "size": 79 }, { "name": "bestreden uitspraken op bezwaar", "size": 9 }, { "name": "betrokkene", "size": 56 }, { "name": "betrokkene betrokkene", "size": 7 }, { "name": "betrokkene feit", "size": 6 }, { "name": "bevestigt de aangevallen uitspraak", "size": 33 }, { "name": "bevindingen", "size": 6 }, { "name": "bevoegdheid", "size": 12 }, { "name": "bevoegdheid van de rechtbank", "size": 20 }, { "name": "bevoegdheid van het hof", "size": 8 }, { "name": "bewezenverklaring", "size": 3300 }, { "name": "bewezenverklaring en bewijsvoering", "size": 79 }, { "name": "bewijs", "size": 555 }, { "name": "bewijs en bewijsvoering", "size": 6 }, { "name": "bewijsaanbod", "size": 15 }, { "name": "bewijsbeslissingen", "size": 35 }, { "name": "bewijsmiddelen", "size": 269 }, { "name": "bewijsmiddelen en de beoordeling daarvan", "size": 8 }, { "name": "bewijsmiddelen feit", "size": 7 }, { "name": "bewijsmotivering", "size": 6 }, { "name": "bewijsoverweging", "size": 139 }, { "name": "bewijsoverweging feit", "size": 9 }, { "name": "bewijsoverweging ten aanzien van feit", "size": 9 }, { "name": "bewijsverweer", "size": 13 }, { "name": "bewijsverweren", "size": 10 }, { "name": "bewijsvoering", "size": 34 }, { "name": "bewijsvraag", "size": 216 }, { "name": "bewijswaarde van de verklaringen van medeverdachte", "size": 7 }, { "name": "bezwaar en beroep", "size": 15 }, { "name": "bijlage", "size": 43 }, { "name": "bijlage a", "size": 6 }, { "name": "bijlage i", "size": 8 }, { "name": "bijlage i bij vonnis van november", "size": 6 }, { "name": "bijlage i bij vonnis van oktober", "size": 7 }, { "name": "bijlage i bij vonnis van september", "size": 6 }, { "name": "bijlagen", "size": 35 }, { "name": "bijzondere bewijsoverweging", "size": 8 }, { "name": "bijzondere voorwaarden", "size": 7 }, { "name": "bnb", "size": 8 }, { "name": "bureau jeugdzorg drenthe", "size": 7 }, { "name": "bureau jeugdzorg flevoland", "size": 8 }, { "name": "bureau jeugdzorg friesland", "size": 11 }, { "name": "bv", "size": 14 }, { "name": "c", "size": 128 }, { "name": "cak", "size": 40 }, { "name": "cassatieberoep", "size": 11 }, { "name": "cassatiemiddel", "size": 30 }, { "name": "centrale raad van beroep", "size": 7 }, { "name": "ciz", "size": 55 }, { "name": "college", "size": 6 }, { "name": "conclusie", "size": 996 }, { "name": "conclusie in het principale en incidentele cassatieberoep", "size": 6 }, { "name": "conclusie van de advocaatgeneraal", "size": 21 }, { "name": "conclusies", "size": 13 }, { "name": "conclusies van partijen", "size": 439 }, { "name": "curator", "size": 6 }, { "name": "cvg", "size": 127 }, { "name": "d", "size": 95 }, { "name": "dadelijke uitvoerbaarheid", "size": 7 }, { "name": "dcision", "size": 46 }, { "name": "de man", "size": 102 }, { "name": "de man wonende te woonplaats", "size": 27 }, { "name": "de minderjarige geboren op geboortedatum te geboorteplaats", "size": 6 }, { "name": "de moeder", "size": 94 }, { "name": "de pleegouders", "size": 12 }, { "name": "de vader", "size": 103 }, { "name": "de vrouw", "size": 87 }, { "name": "deelgeschil", "size": 53 }, { "name": "deelneming aan een organisatie die tot oogmerk heeft het plegen van misdrijven", "size": 14 }, { "name": "deprocedure", "size": 23 }, { "name": "derde middel", "size": 49 }, { "name": "diefstal", "size": 39 }, { "name": "diefstal door twee of meer verenigde personen", "size": 21 }, { "name": "diefstal meermalen gepleegd", "size": 10 }, { "name": "dochter m", "size": 8 }, { "name": "doodslag", "size": 38 }, { "name": "door de rechtbank gebruikte bewijsmiddelen", "size": 6 }, { "name": "door de verdediging ingediende verzoeken", "size": 7 }, { "name": "door het hof gebruikte bewijsmiddelen", "size": 7 }, { "name": "draagkracht", "size": 15 }, { "name": "draagkracht man", "size": 9 }, { "name": "draagkracht van de man", "size": 18 }, { "name": "draagkracht van de vrouw", "size": 6 }, { "name": "e", "size": 37 }, { "name": "eendaadse samenloop van", "size": 18 }, { "name": "eerste en het tweede middel", "size": 7 }, { "name": "eerste geding in cassatie", "size": 91 }, { "name": "eerste middel", "size": 78 }, { "name": "eerste middelfaalt", "size": 8 }, { "name": "eh", "size": 102 }, { "name": "eigen schuld", "size": 6 }, { "name": "eis officier van justitie", "size": 111 }, { "name": "eis van de officier van justitie", "size": 148 }, { "name": "eisende partij", "size": 6 }, { "name": "eiser", "size": 475 }, { "name": "eiser a", "size": 8 }, { "name": "eiser sub", "size": 143 }, { "name": "eiser te woonplaats eiser", "size": 6 }, { "name": "eiser wonende te plaats eiser", "size": 7 }, { "name": "eiser wonende te woonplaats eiser", "size": 8 }, { "name": "eiseres", "size": 95 }, { "name": "eiseres sub", "size": 37 }, { "name": "eiseres te y eiser", "size": 7 }, { "name": "eisers", "size": 25 }, { "name": "eiswijziging", "size": 12 }, { "name": "ek", "size": 9 }, { "name": "en", "size": 129 }, { "name": "en alvorens verder te beslissen", "size": 7 }, { "name": "enof", "size": 13 }, { "name": "f", "size": 32 }, { "name": "fed", "size": 6 }, { "name": "feit", "size": 260 }, { "name": "feit en", "size": 7 }, { "name": "feit parketnummer", "size": 6 }, { "name": "feit primair", "size": 36 }, { "name": "feit primair poging tot doodslag", "size": 6 }, { "name": "feitelijke gang van zaken", "size": 15 }, { "name": "feitelijke uitgangspunten", "size": 10 }, { "name": "feiten", "size": 11049 }, { "name": "feiten en", "size": 9 }, { "name": "feiten en het geding in feitelijke instantie", "size": 29 }, { "name": "feiten en het geding in feitelijke instanties", "size": 153 }, { "name": "feiten en het geschil", "size": 6 }, { "name": "feiten en omstandigheden", "size": 199 }, { "name": "feiten en omstandigheden in conventie en in reconventie", "size": 11 }, { "name": "feiten in beide zaken", "size": 28 }, { "name": "feiten in conventie en in reconventie", "size": 56 }, { "name": "feiten in conventie en reconventie", "size": 17 }, { "name": "feiten in conventie en voorwaardelijke reconventie", "size": 7 }, { "name": "feiten in de hoofdzaak en in de vrijwaringszaak", "size": 8 }, { "name": "feiten in het incident", "size": 27 }, { "name": "feiten voor zover van belang in het incident", "size": 16 }, { "name": "formaliteiten met betrekking tot het bewijs in alle zaken", "size": 8 }, { "name": "formele voorvragen", "size": 543 }, { "name": "g", "size": 15 }, { "name": "gang van zaken", "size": 15 }, { "name": "garantie als bedoeld in artikel eerste lid olw", "size": 16 }, { "name": "garantie als bedoeld in artikel eerste lid van de olw", "size": 34 }, { "name": "gedaagde", "size": 536 }, { "name": "gedaagde sub", "size": 229 }, { "name": "gedaagden", "size": 15 }, { "name": "geding", "size": 952 }, { "name": "geding in cassatie", "size": 4330 }, { "name": "geding in eerste aanleg", "size": 2896 }, { "name": "geding in eerste aanleg zaaknr", "size": 55 }, { "name": "geding in eerste aanleg zaaknr c ha za", "size": 31 }, { "name": "geding in eerste aanleg zaaknr c kg za", "size": 37 }, { "name": "geding in eerste aanleg zaaknr cha za", "size": 92 }, { "name": "geding in eerste aanleg zaaknr chaza", "size": 28 }, { "name": "geding in eerste aanleg zaaknr ckg za", "size": 56 }, { "name": "geding in eerste aanleg zaaknr cv", "size": 12 }, { "name": "geding in eerste aanleg zaaknr cv expl", "size": 198 }, { "name": "geding in eerste aanleg zaaknr ha za", "size": 204 }, { "name": "geding in eerste aanleg zaaknr haza", "size": 34 }, { "name": "geding in eerste aanleg zaaknr kg za", "size": 23 }, { "name": "geding in eerste aanleg zaaknr rolnr", "size": 34 }, { "name": "geding in eerste aanleg zaaknr rolnr cv expl", "size": 8 }, { "name": "geding in eerste aanleg zaaknr vv expl", "size": 8 }, { "name": "geding in eerste aanleg zaaknrrolnr ha za", "size": 10 }, { "name": "geding in eerste aanleg zaaknummer", "size": 8 }, { "name": "geding in eerste aanleg zaaknummer c ha za", "size": 7 }, { "name": "geding in eerste aanleg zaaknummer cha za", "size": 15 }, { "name": "geding in eerste aanleg zaaknummer ckg za", "size": 10 }, { "name": "geding in eerste aanleg zaaknummer cv expl", "size": 82 }, { "name": "geding in eerste aanleg zaaknummer ha za", "size": 23 }, { "name": "geding in eerste aanleg zaaknummer rolnummer", "size": 42 }, { "name": "geding in eerste aanleg zaaknummer rolnummer ha za", "size": 11 }, { "name": "geding in eerste aanleg zaaknummer vv expl", "size": 7 }, { "name": "geding in eerste aanleg zaaknummerrolnummer ha za", "size": 28 }, { "name": "geding in eerste aanleg zaakrolnr", "size": 14 }, { "name": "geding in eerste aanleg zaakrolnr c ha za", "size": 7 }, { "name": "geding in eerste aanleg zaakrolnr cha za", "size": 6 }, { "name": "geding in eerste aanleg zaakrolnr ckg za", "size": 6 }, { "name": "geding in eerste aanleg zaakrolnr cv expl", "size": 16 }, { "name": "geding in eerste aanleg zaakrolnr ha za", "size": 23 }, { "name": "geding in eerste aanleg zaakrolnummer", "size": 11 }, { "name": "geding in eerste aanleg zaakrolnummer cha za", "size": 7 }, { "name": "geding in eerste aanleg zaakrolnummer ckg za", "size": 6 }, { "name": "geding in eerste aanleg zaakrolnummer cv expl", "size": 11 }, { "name": "geding in eerste instantie", "size": 102 }, { "name": "geding in feitelijke instantie", "size": 198 }, { "name": "geding in feitelijke instanties", "size": 1186 }, { "name": "geding in feitelijke instanties en in cassatie", "size": 7 }, { "name": "geding in het principaal en het incidenteel hoger beroep", "size": 56 }, { "name": "geding in hoger beroep", "size": 6681 }, { "name": "geding na cassatie", "size": 26 }, { "name": "geding na verwijzing", "size": 15 }, { "name": "geding na verwijzing door de hoge raad", "size": 12 }, { "name": "geding voor de rechtbank en het hof", "size": 20 }, { "name": "gedingen in hoger beroep", "size": 16 }, { "name": "gedoogbeleid", "size": 7 }, { "name": "geintimeerde", "size": 24 }, { "name": "geintimeerde sub", "size": 17 }, { "name": "geintimeerde wonende te woonplaats", "size": 14 }, { "name": "gelast dat de verdachte ter beschikking wordt gesteld", "size": 7 }, { "name": "geldboete van", "size": 10 }, { "name": "geldigheid dagvaarding", "size": 13 }, { "name": "geldigheid dagvaarding en bevoegdheid rechtbank", "size": 7 }, { "name": "geldigheid van de dagvaarding", "size": 50 }, { "name": "geldigheid van de inleidende dagvaarding", "size": 8 }, { "name": "gemeenschappelijk hof van justitie", "size": 6 }, { "name": "gemeente leiden", "size": 7 }, { "name": "gemeente rotterdam", "size": 8 }, { "name": "genoegzaamheid van het eab", "size": 7 }, { "name": "gentimeerde", "size": 463 }, { "name": "gentimeerde sub", "size": 182 }, { "name": "gentimeerde sub en", "size": 14 }, { "name": "gentimeerde sub wonende te woonplaats", "size": 6 }, { "name": "gentimeerde wonende te woonplaats", "size": 82 }, { "name": "gentimeerde wonende te woonplaats duitsland", "size": 7 }, { "name": "gentimeerden", "size": 9 }, { "name": "gerechtshof amsterdam", "size": 13 }, { "name": "gerechtshof arnhemleeuwarden", "size": 58 }, { "name": "gerechtshof den haag", "size": 1509 }, { "name": "gerechtshof sgravenhage", "size": 42 }, { "name": "gerechtshof shertogenbosch", "size": 25 }, { "name": "gerechtshof te sgravenhage", "size": 12 }, { "name": "geschil", "size": 5498 }, { "name": "geschil alsmede standpunten en conclusies van partijen", "size": 374 }, { "name": "geschil de standpunten en conclusies van partijen", "size": 692 }, { "name": "geschil en de procedure in eerste aanleg", "size": 7 }, { "name": "geschil en standpunten", "size": 28 }, { "name": "geschil en standpunten van partijen", "size": 73 }, { "name": "geschil in conventie", "size": 208 }, { "name": "geschil in conventie en in reconventie", "size": 38 }, { "name": "geschil in conventie en reconventie", "size": 6 }, { "name": "geschil in de hoofdzaak", "size": 47 }, { "name": "geschil in de hoofdzaak en in het incident", "size": 6 }, { "name": "geschil in de incidenten", "size": 7 }, { "name": "geschil in de vrijwaringszaak", "size": 6 }, { "name": "geschil in eerste aanleg", "size": 7 }, { "name": "geschil in het incident", "size": 73 }, { "name": "geschil in hoger beroep", "size": 1043 }, { "name": "geschil in hoger beroep en incidenteel hoger beroep", "size": 10 }, { "name": "geschil in hoger beroep en standpunten en conclusies van partijen", "size": 12 }, { "name": "geschil in hoger beroep en standpunten van partijen", "size": 113 }, { "name": "geschil in hoger beroep na verwijzing", "size": 17 }, { "name": "geschil in principaal en incidenteel hoger beroep", "size": 9 }, { "name": "geschil in reconventie", "size": 177 }, { "name": "geschil in voorwaardelijke reconventie", "size": 25 }, { "name": "geschil na verwijzing", "size": 8 }, { "name": "geschil na verwijzing en standpunten van partijen", "size": 35 }, { "name": "geschil standpunten en conclusies", "size": 30 }, { "name": "geschil standpunten en conclusies van partijen", "size": 24 }, { "name": "geschillen", "size": 31 }, { "name": "gevangenisstraf voor de duur van dagen", "size": 9 }, { "name": "gevangenisstraf voor de duur van jaren", "size": 9 }, { "name": "gevangenisstraf voor de duur van maanden", "size": 17 }, { "name": "gevoerde verweren", "size": 17 }, { "name": "gezag", "size": 8 }, { "name": "gezagsuitoefening", "size": 14 }, { "name": "grief i", "size": 6 }, { "name": "grieven", "size": 131 }, { "name": "griffierecht", "size": 11 }, { "name": "gronden", "size": 14 }, { "name": "gronden van de beslissing", "size": 44 }, { "name": "grondslag en inhoud van het eab", "size": 190 }, { "name": "grondslag van de vordering", "size": 15 }, { "name": "grondslag van de vordering en het verweer", "size": 7 }, { "name": "grondslag van het geschil", "size": 265 }, { "name": "h", "size": 22 }, { "name": "handelen in strijd met artikel eerste lid van de wet wapens en munitie", "size": 49 }, { "name": "hd", "size": 1069 }, { "name": "heropening van het onderzoek", "size": 13 }, { "name": "hetgeen de rechtbank bewezen acht", "size": 99 }, { "name": "hof", "size": 26 }, { "name": "hof wijst het beklag af", "size": 9 }, { "name": "hoger beroep", "size": 656 }, { "name": "hoofdzaak", "size": 17 }, { "name": "identiteit van de opgeiste persoon", "size": 200 }, { "name": "identiteit veroordeelde", "size": 6 }, { "name": "ij", "size": 220 }, { "name": "immaterile schadevergoeding", "size": 26 }, { "name": "in aanmerking nemende dat", "size": 7 }, { "name": "in beslag genomen goederen", "size": 24 }, { "name": "in beslag genomen voorwerpen", "size": 34 }, { "name": "in conventie", "size": 91 }, { "name": "in conventie en in reconventie", "size": 45 }, { "name": "in conventie en reconventie", "size": 9 }, { "name": "in de hoofdzaak", "size": 37 }, { "name": "in de zaak met nummer", "size": 8 }, { "name": "in de zaak met parketnummer", "size": 10 }, { "name": "in de zaak met zaaknummer", "size": 10 }, { "name": "in elk geval zijnde telkens handelingen waarvan hij enof zijn mededaders", "size": 6 }, { "name": "in het incident", "size": 12 }, { "name": "in het principaal en incidenteel appel", "size": 8 }, { "name": "in reconventie", "size": 92 }, { "name": "inbeslaggenomen goederen", "size": 133 }, { "name": "inbeslaggenomen voorwerpen", "size": 60 }, { "name": "inboedel", "size": 6 }, { "name": "incident tot tussenkomst", "size": 26 }, { "name": "incident tot tussenkomst cq voeging", "size": 8 }, { "name": "incident tot tussenkomst dan wel voeging", "size": 12 }, { "name": "incident tot tussenkomst subsidiair voeging", "size": 6 }, { "name": "incident tot voeging", "size": 7 }, { "name": "ingangsdatum", "size": 28 }, { "name": "inhoud van de tenlastelegging", "size": 596 }, { "name": "inhoud van de vordering", "size": 14 }, { "name": "inhoud van het bezwaarschrift", "size": 12 }, { "name": "inhoud van het verzoek", "size": 16 }, { "name": "inhoud van het verzoekschrift", "size": 9 }, { "name": "inkoopadministratie", "size": 6 }, { "name": "inleidende beschouwing", "size": 6 }, { "name": "inleidende beschouwingen", "size": 8 }, { "name": "inleidende klacht", "size": 7 }, { "name": "inleidende opmerkingen", "size": 8 }, { "name": "inleiding", "size": 480 }, { "name": "inspecteur van de belastingdienstbelastingen kantoor te p verweerder", "size": 6 }, { "name": "inspecteur van de belastingdienstte p verweerder", "size": 28 }, { "name": "internationale kinderontvoering", "size": 49 }, { "name": "inzake", "size": 6 }, { "name": "j", "size": 11 }, { "name": "jl", "size": 80 }, { "name": "js", "size": 39 }, { "name": "juridisch kader", "size": 16 }, { "name": "jurisprudentie", "size": 12 }, { "name": "k", "size": 7 }, { "name": "kantonrechter", "size": 11 }, { "name": "kinderalimentatie", "size": 31 }, { "name": "klaagster", "size": 11 }, { "name": "klacht", "size": 53 }, { "name": "klacht en het verweer", "size": 9 }, { "name": "klacht en het verweer daartegen", "size": 14 }, { "name": "klager", "size": 18 }, { "name": "komen overeen dat", "size": 6 }, { "name": "kosten", "size": 842 }, { "name": "kosten en griffierecht", "size": 12 }, { "name": "kostenveroordeling", "size": 13 }, { "name": "kwalificatie", "size": 291 }, { "name": "kwalificatie en strafbaarheid van de feiten", "size": 127 }, { "name": "kwalificatie en strafbaarheid van het bewezenverklaarde", "size": 44 }, { "name": "kwalificatie en strafbaarheid van het feit", "size": 122 }, { "name": "kwalificatie van het bewezen verklaarde", "size": 8 }, { "name": "kwalificatie van het bewezenverklaarde", "size": 559 }, { "name": "kwalificaties", "size": 99 }, { "name": "l", "size": 12 }, { "name": "legt op de volgende straffen", "size": 7 }, { "name": "lid onder", "size": 9 }, { "name": "literatuur", "size": 9 }, { "name": "loop van het geding", "size": 67 }, { "name": "loop van het geding in hoger beroep", "size": 392 }, { "name": "loop van het geding na verwijzing", "size": 11 }, { "name": "m", "size": 10 }, { "name": "maatregel", "size": 8 }, { "name": "maatregel van schadevergoeding van eur subsidiair dagen hechtenis", "size": 22 }, { "name": "maatregel van schadevergoeding van subsidiair dagen hechtenis", "size": 33 }, { "name": "machtiging tot uithuisplaatsing", "size": 13 }, { "name": "machtiging tot uithuisplaatsing in een accommodatie voor gesloten jeugdzorg", "size": 6 }, { "name": "man", "size": 6 }, { "name": "medeplegen", "size": 7 }, { "name": "medeplegen en medeplichtigheid", "size": 6 }, { "name": "medeplegen van doodslag", "size": 8 }, { "name": "medeplegen van oplichting meermalen gepleegd", "size": 9 }, { "name": "medeplegen van poging tot zware mishandeling", "size": 8 }, { "name": "meer subsidiair", "size": 16 }, { "name": "meest subsidiair", "size": 7 }, { "name": "mensenhandel", "size": 12 }, { "name": "mensenhandel meermalen gepleegd", "size": 7 }, { "name": "met betrekking tot de grieven", "size": 18 }, { "name": "met betrekking tot feit", "size": 17 }, { "name": "michael david ilhan van waveren", "size": 6 }, { "name": "middel", "size": 49 }, { "name": "middel van de benadeelde partij", "size": 6 }, { "name": "minister van onderwijs cultuur en wetenschap verweerder", "size": 6 }, { "name": "mishandeling", "size": 41 }, { "name": "mishandeling meermalen gepleegd", "size": 8 }, { "name": "mishandeling terwijl het feit zwaar lichamelijk letsel ten gevolge heeft", "size": 6 }, { "name": "mk", "size": 122 }, { "name": "moeder", "size": 11 }, { "name": "mondelinge behandeling van het wrakingsverzoek", "size": 103 }, { "name": "motivering", "size": 46 }, { "name": "motivering van de sancties", "size": 6 }, { "name": "mr rlh ijzerman", "size": 7 }, { "name": "n", "size": 6 }, { "name": "naam", "size": 215 }, { "name": "naam betrokkene hierna betrokkene", "size": 7 }, { "name": "naam eiser", "size": 15 }, { "name": "naam gedaagde", "size": 23 }, { "name": "naam van de verdachte", "size": 15 }, { "name": "naam verdachte", "size": 47 }, { "name": "naam verzoeker", "size": 13 }, { "name": "naam wonende te woonplaats", "size": 16 }, { "name": "naamloze vennootschap", "size": 9 }, { "name": "naamloze vennootschap sns bank nv gevestigd te utrecht", "size": 6 }, { "name": "nadere bewijsoverweging", "size": 73 }, { "name": "nadere feiten", "size": 6 }, { "name": "naheffingsaanslag beschikking bezwaar en geding in eerste aanleg", "size": 8 }, { "name": "naheffingsaanslag bezwaar en geding in eerste aanleg", "size": 8 }, { "name": "naheffingsaanslag boetebeschikking bezwaar en geding in eerste aanleg", "size": 6 }, { "name": "naheffingsaanslagen beschikkingen bezwaar en geding in eerste aanleg", "size": 7 }, { "name": "navorderingsaanslag beschikking bezwaar en geding in eerste aanleg", "size": 6 }, { "name": "navorderingsaanslagen beschikkingen bezwaar en geding in eerste aanleg", "size": 9 }, { "name": "navorderingsaanslagen kwijtscheldingsbesluiten boetebeschikkingen en bezwaar", "size": 38 }, { "name": "nk", "size": 253 }, { "name": "noot verbalisanten", "size": 18 }, { "name": "ntfr", "size": 6 }, { "name": "nw", "size": 41 }, { "name": "officier van justitie", "size": 10 }, { "name": "officier van justitie bij het arrondissementsparket oostnederland", "size": 8 }, { "name": "officier van justitie heeft aangevoerd verkort en zakelijk weergegeven", "size": 34 }, { "name": "omschrijving geschil en standpunten van partijen", "size": 28 }, { "name": "omschrijving geschil in hoger beroep en standpunten van partijen", "size": 287 }, { "name": "omschrijving van het geschil", "size": 8 }, { "name": "omschrijving verzoek", "size": 12 }, { "name": "omvang hoger beroep", "size": 7 }, { "name": "omvang van het appel", "size": 7 }, { "name": "omvang van het geschil", "size": 333 }, { "name": "omvang van het geschil in hoger beroep", "size": 6 }, { "name": "omvang van het hoger beroep", "size": 51 }, { "name": "onder parketnummer", "size": 7 }, { "name": "ondergetekenden", "size": 10 }, { "name": "onderlinge waarborgmaatschappij", "size": 6 }, { "name": "ondertoezichtstelling", "size": 9 }, { "name": "onderzoek", "size": 10 }, { "name": "onderzoek op de terechtzitting", "size": 643 }, { "name": "onderzoek ter terechtzitting", "size": 2291 }, { "name": "onderzoek ter zitting", "size": 7 }, { "name": "onderzoek van de zaak", "size": 2517 }, { "name": "onderzoek wagner", "size": 10 }, { "name": "onrechtmatigheid ontruiming", "size": 6 }, { "name": "onschuldverweer", "size": 21 }, { "name": "ontbinding", "size": 7 }, { "name": "onttrekking aan het verkeer", "size": 18 }, { "name": "onttrekking aan het verkeer van de inbeslaggenomen goederen te weten", "size": 6 }, { "name": "ontvankelijkheid", "size": 125 }, { "name": "ontvankelijkheid in hoger beroep", "size": 7 }, { "name": "ontvankelijkheid officier van justitie", "size": 14 }, { "name": "ontvankelijkheid openbaar ministerie", "size": 30 }, { "name": "ontvankelijkheid van de officier van justitie", "size": 73 }, { "name": "ontvankelijkheid van de verdachte in het hoger beroep", "size": 12 }, { "name": "ontvankelijkheid van het beroep", "size": 9 }, { "name": "ontvankelijkheid van het cassatieberoep", "size": 32 }, { "name": "ontvankelijkheid van het hoger beroep", "size": 167 }, { "name": "ontvankelijkheid van het ingestelde hoger beroep", "size": 8 }, { "name": "ontvankelijkheid van het openbaar ministerie", "size": 174 }, { "name": "ontvankelijkheid van het openbaar ministerie in de strafvervolging", "size": 6 }, { "name": "ontvankelijkheid van het openbaar ministerie in de vervolging", "size": 20 }, { "name": "ontvankelijkheid van het openbaar ministerie in zijn vervolging", "size": 6 }, { "name": "ontvankelijkheid van het verzet", "size": 13 }, { "name": "ontvankelijkheid van het verzoek", "size": 40 }, { "name": "ontvankelijkheid van het wrakingsverzoek", "size": 6 }, { "name": "ontvankelijkheid van klager in het hoger beroep", "size": 7 }, { "name": "oordeel", "size": 8 }, { "name": "oordeel hof", "size": 6 }, { "name": "oordeel van de kantonrechter", "size": 8 }, { "name": "oordeel van de rechtbank", "size": 1034 }, { "name": "oordeel van het hof", "size": 23 }, { "name": "op te leggen maatregel", "size": 9 }, { "name": "op te leggen straf", "size": 18 }, { "name": "op te leggen straf of maatregel", "size": 430 }, { "name": "openbaar ministerie", "size": 7 }, { "name": "openlijk in vereniging geweld plegen tegen personen", "size": 31 }, { "name": "oplegging van de maatregel", "size": 8 }, { "name": "oplegging van een maatregel", "size": 6 }, { "name": "oplegging van maatregel", "size": 6 }, { "name": "oplegging van straf", "size": 551 }, { "name": "oplegging van straf en maatregel", "size": 97 }, { "name": "oplegging van straf en maatregelen", "size": 8 }, { "name": "oplegging van straf enof maatregel", "size": 789 }, { "name": "oplegging van straf of maatregel", "size": 60 }, { "name": "oplegging van straffen", "size": 66 }, { "name": "oplegging van straffen en maatregel", "size": 18 }, { "name": "oplichting", "size": 7 }, { "name": "oplichting meermalen gepleegd", "size": 9 }, { "name": "opmerking vooraf", "size": 6 }, { "name": "overig", "size": 12 }, { "name": "overige verweren", "size": 13 }, { "name": "overtreding van artikel eerste lid van de wegenverkeerswet", "size": 11 }, { "name": "overtreding van artikel van de wegenverkeerswet", "size": 20 }, { "name": "overweegt", "size": 29 }, { "name": "overwegende dat", "size": 7 }, { "name": "overweging", "size": 17 }, { "name": "overweging met betrekking tot het bewijs", "size": 173 }, { "name": "overweging met betrekking tot het bewijs van feit", "size": 11 }, { "name": "overweging omtrent het bewijs", "size": 6 }, { "name": "overweging van de rechtbank", "size": 6 }, { "name": "overwegingen", "size": 3165 }, { "name": "overwegingen ten aanzien van straf enof maatregel", "size": 7 }, { "name": "overzicht", "size": 60 }, { "name": "p verweerder", "size": 14 }, { "name": "parketnummer", "size": 82 }, { "name": "partile nietontvankelijkheid van het openbaar ministerie in de vervolging", "size": 8 }, { "name": "partile vrijspraak", "size": 16 }, { "name": "partneralimentatie", "size": 33 }, { "name": "pleegouders", "size": 7 }, { "name": "poging tot doodslag", "size": 35 }, { "name": "poging tot doodslag meermalen gepleegd", "size": 6 }, { "name": "poging tot moord", "size": 8 }, { "name": "poging tot zware mishandeling", "size": 22 }, { "name": "prejudicile procedure", "size": 20 }, { "name": "prejudicile vraag", "size": 7 }, { "name": "prejudicile vragen", "size": 9 }, { "name": "primair", "size": 54 }, { "name": "procedure", "size": 7921 }, { "name": "procedure in conventie en in reconventie", "size": 10 }, { "name": "procedure in de hoofdzaak", "size": 53 }, { "name": "procedure in de vrijwaringszaak", "size": 43 }, { "name": "procedure in de zaak", "size": 49 }, { "name": "procedure in eerste aanleg", "size": 24 }, { "name": "procedure in hoger beroep", "size": 7 }, { "name": "procedures", "size": 10 }, { "name": "procesgang", "size": 8 }, { "name": "proceskosten", "size": 3270 }, { "name": "proceskosten en griffierecht", "size": 264 }, { "name": "proceskosten en griffierechten", "size": 29 }, { "name": "proceskosten en schadevergoeding", "size": 11 }, { "name": "proceskostenvergoeding", "size": 6 }, { "name": "proceskostenveroordeling", "size": 26 }, { "name": "processtukken", "size": 6 }, { "name": "procesverbaal van de mondelinge uitspraak van de enkelvoudige kamer van", "size": 29 }, { "name": "procesverbaal van de mondelinge uitspraak van de enkelvoudige kamer van november in de zaak tussen", "size": 8 }, { "name": "procesverbaal van de mondelinge uitspraak van de enkelvoudige kamer van oktober in de zaak tussen", "size": 6 }, { "name": "procesverbaal van de mondelinge uitspraak van de voorzieningenrechter op", "size": 6 }, { "name": "procesverloop", "size": 87 }, { "name": "procureurgeneraal bij de hoge raad der nederlanden", "size": 16 }, { "name": "qh", "size": 156 }, { "name": "raadsman", "size": 6 }, { "name": "rb", "size": 78 }, { "name": "reactie van de kantonrechter", "size": 6 }, { "name": "reactie van de officier van justitie", "size": 7 }, { "name": "reactie van de rechter", "size": 56 }, { "name": "reactie van de rechters", "size": 24 }, { "name": "reactie van het openbaar ministerie", "size": 7 }, { "name": "recapitulatie", "size": 8 }, { "name": "rechtbank", "size": 169 }, { "name": "rechtbank amsterdam", "size": 82 }, { "name": "rechtbank amsterdamkort geding", "size": 9 }, { "name": "rechtbank den haag", "size": 88 }, { "name": "rechtbank gelderland", "size": 278 }, { "name": "rechtbank haarlem", "size": 6 }, { "name": "rechtbank leeuwarden", "size": 13 }, { "name": "rechtbank limburg", "size": 97 }, { "name": "rechtbank middennederland", "size": 73 }, { "name": "rechtbank noordholland", "size": 120 }, { "name": "rechtbank noordnederland", "size": 770 }, { "name": "rechtbank oostbrabant", "size": 835 }, { "name": "rechtbank overijssel", "size": 242 }, { "name": "rechtbank overweegt als volgt", "size": 13 }, { "name": "rechtbank overweegt het volgende", "size": 7 }, { "name": "rechtbank rechtdoende", "size": 6 }, { "name": "rechtbank rotterdam", "size": 17 }, { "name": "rechtbank zeelandwestbrabant", "size": 24 }, { "name": "rechtdoende", "size": 17 }, { "name": "rechthebbende", "size": 14 }, { "name": "rechtsmiddel", "size": 5698 }, { "name": "rechtsmiddel tegen deze uitspraak kan binnen vier weken na de dag van verzending daarvan hoger beroep worden ingesteld bij de afdeling bestuursrechtspraak van de raad van state", "size": 12 }, { "name": "rechtsmiddel tegen deze uitspraak kan binnen zes weken na de dag van verzending daarvan hoger beroep worden ingesteld bij de centrale raad van beroep", "size": 6 }, { "name": "rechtsmiddel tegen deze uitspraak staat geen rechtsmiddel open", "size": 7 }, { "name": "recidivegevaar", "size": 6 }, { "name": "redelijke termijn", "size": 6 }, { "name": "redengeving bewezenverklaring", "size": 74 }, { "name": "regelgeving", "size": 6 }, { "name": "regelgeving wetsgeschiedenis jurisprudentie en literatuur", "size": 10 }, { "name": "relevante regelgeving", "size": 14 }, { "name": "relevante teksten en toelichtingen van de gn", "size": 7 }, { "name": "relevante wet en regelgeving", "size": 8 }, { "name": "relevante wetgeving", "size": 8 }, { "name": "relevante wettelijke bepalingen", "size": 14 }, { "name": "rg", "size": 16 }, { "name": "rh", "size": 35 }, { "name": "rk", "size": 52 }, { "name": "rol van de verdachte", "size": 8 }, { "name": "samenstelling raadkamer en uitspraakdatum", "size": 25 }, { "name": "samenstelling rechtbank en uitspraakdatum", "size": 6 }, { "name": "schade van benadeelde", "size": 10 }, { "name": "schade van benadeelden", "size": 227 }, { "name": "schadevergoeding", "size": 72 }, { "name": "schadevergoeding nabestaande", "size": 6 }, { "name": "schadevergoedingsmaatregel", "size": 81 }, { "name": "schadevergoedingsmaatregel benadeelde", "size": 13 }, { "name": "schadevergoedingsmaatregel minderjarige zaak", "size": 10 }, { "name": "schatting van de hoogte van het wederrechtelijk verkregen voordeel", "size": 10 }, { "name": "schatting van het wederrechtelijk verkregen voordeel", "size": 26 }, { "name": "scheiding", "size": 15 }, { "name": "schorsing vervolging", "size": 8 }, { "name": "schriftelijke ronde standpunten vorderingen schadevergoeding", "size": 6 }, { "name": "situatie zonder ongeval", "size": 6 }, { "name": "slachtoffer", "size": 29 }, { "name": "spoedeisend belang", "size": 11 }, { "name": "staat der nederlanden", "size": 15 }, { "name": "staat der nederlanden ministerie van financin", "size": 7 }, { "name": "staat der nederlanden ministerie van veiligheid en justitie", "size": 35 }, { "name": "staatssecretaris van veiligheid en justitie verweerder", "size": 6 }, { "name": "standpunt officier van justitie", "size": 7 }, { "name": "standpunt van a", "size": 16 }, { "name": "standpunt van appellante", "size": 11 }, { "name": "standpunt van belanghebbende", "size": 12 }, { "name": "standpunt van belanghebbenden", "size": 29 }, { "name": "standpunt van de advocaatgeneraal", "size": 16 }, { "name": "standpunt van de gemeente", "size": 7 }, { "name": "standpunt van de gerechtsdeurwaarder", "size": 47 }, { "name": "standpunt van de gerechtsdeurwaarders", "size": 8 }, { "name": "standpunt van de gewraakte rechter", "size": 8 }, { "name": "standpunt van de gewraakte rechters", "size": 9 }, { "name": "standpunt van de ind en van de officier van justitie", "size": 18 }, { "name": "standpunt van de inrichting", "size": 14 }, { "name": "standpunt van de kandidaatnotaris", "size": 8 }, { "name": "standpunt van de kantonrechter", "size": 41 }, { "name": "standpunt van de kbvg", "size": 21 }, { "name": "standpunt van de kinderrechter", "size": 9 }, { "name": "standpunt van de moeder", "size": 6 }, { "name": "standpunt van de notaris", "size": 127 }, { "name": "standpunt van de notarissen", "size": 19 }, { "name": "standpunt van de officier van justitie", "size": 203 }, { "name": "standpunt van de oudnotaris", "size": 19 }, { "name": "standpunt van de raadsman", "size": 12 }, { "name": "standpunt van de rechter", "size": 105 }, { "name": "standpunt van de rechtercommissaris", "size": 10 }, { "name": "standpunt van de rechters", "size": 13 }, { "name": "standpunt van de reclassering", "size": 13 }, { "name": "standpunt van de toegevoegd kandidaatgerechtsdeurwaarder", "size": 15 }, { "name": "standpunt van de verdediging", "size": 381 }, { "name": "standpunt van eiser", "size": 19 }, { "name": "standpunt van eiseres", "size": 9 }, { "name": "standpunt van eisers", "size": 7 }, { "name": "standpunt van gedaagde", "size": 26 }, { "name": "standpunt van het bft", "size": 9 }, { "name": "standpunt van het openbaar ministerie", "size": 246 }, { "name": "standpunt van klaagster", "size": 71 }, { "name": "standpunt van klager", "size": 86 }, { "name": "standpunt van klagers", "size": 43 }, { "name": "standpunt van partijen", "size": 16 }, { "name": "standpunt van verzoeker", "size": 167 }, { "name": "standpunt van verzoekers", "size": 9 }, { "name": "standpunt van verzoekster", "size": 29 }, { "name": "standpunt verdediging", "size": 16 }, { "name": "standpunten", "size": 113 }, { "name": "standpunten dexia", "size": 11 }, { "name": "standpunten partijen", "size": 10 }, { "name": "standpunten van de officier van justitie en de verdediging", "size": 28 }, { "name": "standpunten van partijen", "size": 489 }, { "name": "standpunten van partijen in conventie en in reconventie", "size": 9 }, { "name": "standpunten van partijen in hoger beroep", "size": 67 }, { "name": "stellingen van partijen", "size": 22 }, { "name": "stelt als algemene voorwaarden dat de veroordeelde", "size": 16 }, { "name": "stelt als bijzondere voorwaarden", "size": 10 }, { "name": "stelt als bijzondere voorwaarden dat", "size": 7 }, { "name": "stelt als bijzondere voorwaarden dat de veroordeelde", "size": 16 }, { "name": "stichting", "size": 6 }, { "name": "stichting bureau jeugdzorg friesland", "size": 9 }, { "name": "stichting pensioenfonds metaal en techniek", "size": 6 }, { "name": "stichting william schrikker jeugdbescherming en jeugdreclassering", "size": 7 }, { "name": "straf", "size": 68 }, { "name": "straf enof de maatregel", "size": 29 }, { "name": "strafbaarheid", "size": 916 }, { "name": "strafbaarheid feit", "size": 44 }, { "name": "strafbaarheid feiten", "size": 42 }, { "name": "strafbaarheid feiten vermeld op bijlage bij de olw", "size": 16 }, { "name": "strafbaarheid van de dader", "size": 6 }, { "name": "strafbaarheid van de feiten", "size": 556 }, { "name": "strafbaarheid van de feiten en van verdachte", "size": 10 }, { "name": "strafbaarheid van de verdachte", "size": 2128 }, { "name": "strafbaarheid van het bewezen verklaarde", "size": 759 }, { "name": "strafbaarheid van het bewezenverklaarde", "size": 1166 }, { "name": "strafbaarheid van het bewezenverklaarde en de kwalificatie", "size": 243 }, { "name": "strafbaarheid van het bewezenverklaarde en van de verdachte", "size": 20 }, { "name": "strafbaarheid van het feit", "size": 1613 }, { "name": "strafbaarheid van het feit en van verdachte", "size": 8 }, { "name": "strafbaarheid van verdachte", "size": 2822 }, { "name": "strafbaarheid verdachte", "size": 91 }, { "name": "strafmaatregel", "size": 47 }, { "name": "strafoplegging", "size": 1085 }, { "name": "stukken", "size": 41 }, { "name": "stukken van het geding", "size": 309 }, { "name": "subsidiair", "size": 60 }, { "name": "taakstraf bestaande uit het verrichten van uren onbetaalde arbeid", "size": 8 }, { "name": "tav feit", "size": 28 }, { "name": "tav feit maatregel van schadevergoeding van subsidiair dagen hechtenis", "size": 7 }, { "name": "tav feit subsidiair", "size": 9 }, { "name": "tav parketnummer", "size": 33 }, { "name": "tegen deze uitspraak staat geen rechtsmiddel open", "size": 19 }, { "name": "tegenwoordig", "size": 7 }, { "name": "tekst gewijzigde tenlastelegging", "size": 24 }, { "name": "tekst tenlastelegging", "size": 53 }, { "name": "ten aanzien van de benadeelde partij", "size": 32 }, { "name": "ten aanzien van de benadeelde partij en de schadevergoedingsmaatregel", "size": 232 }, { "name": "ten aanzien van de benadeelde partijen en de schadevergoedingsmaatregel", "size": 23 }, { "name": "ten aanzien van de feiten", "size": 63 }, { "name": "ten aanzien van de ontvankelijkheid", "size": 6 }, { "name": "ten aanzien van de tenlastelegging", "size": 11 }, { "name": "ten aanzien van feit", "size": 63 }, { "name": "ten aanzien van feit van", "size": 9 }, { "name": "ten aanzien van het onder ten laste gelegde", "size": 8 }, { "name": "ten aanzien van parketnummer", "size": 15 }, { "name": "tenlastelegging", "size": 6186 }, { "name": "tenlastelegging aan de verdachte is na wijziging van de tenlastelegging ter terechtzitting ten laste gelegd dat", "size": 7 }, { "name": "tenlastelegging aan de verdachte is ten laste gelegd dat", "size": 33 }, { "name": "tenlastelegging aan verdachte is ten laste gelegd dat", "size": 7 }, { "name": "tenlasteleggingen", "size": 67 }, { "name": "tenuitvoerlegging voorwaardelijke veroordeling", "size": 77 }, { "name": "tenuitvoerlegging voorwaardelijke veroordelingen", "size": 7 }, { "name": "ter beschikking gestelde heeft verklaard kort en zakelijk weergegeven", "size": 15 }, { "name": "ter zake het verzoekschrift ex artikel a sv", "size": 9 }, { "name": "ter zake het verzoekschrift ex artikel sv", "size": 9 }, { "name": "terbeschikkinggestelde", "size": 215 }, { "name": "terbeschikkinggestelde heeft verklaard kort en zakelijk weergegeven", "size": 24 }, { "name": "terbeschikkinggestelde heeft verklaard verkort en zakelijk weergegeven", "size": 10 }, { "name": "terechtzitting", "size": 73 }, { "name": "teruggave", "size": 7 }, { "name": "tm", "size": 143 }, { "name": "toegepaste wetsartikelen", "size": 27 }, { "name": "toegepaste wettelijke bepalingen", "size": 581 }, { "name": "toegepaste wettelijke voorschriften", "size": 465 }, { "name": "toelichting", "size": 7 }, { "name": "toepasselijk recht", "size": 16 }, { "name": "toepasselijk wettelijk voorschrift", "size": 21 }, { "name": "toepasselijke recht", "size": 11 }, { "name": "toepasselijke regelgeving", "size": 7 }, { "name": "toepasselijke wetsartikelen", "size": 1014 }, { "name": "toepasselijke wetsbepalingen", "size": 90 }, { "name": "toepasselijke wettelijke bepaling", "size": 15 }, { "name": "toepasselijke wettelijke voorschriften", "size": 2555 }, { "name": "toepasselijkheid wettelijke voorschriften", "size": 150 }, { "name": "toepassing van de wetsartikelen", "size": 8 }, { "name": "toepassing van wetsartikelen", "size": 502 }, { "name": "totaal wederrechtelijk verkregen voordeel", "size": 6 }, { "name": "tussen partijen vaststaande feiten", "size": 263 }, { "name": "tussenarrest van de meervoudige kamer voor strafzaken", "size": 14 }, { "name": "tussenarrest van december", "size": 9 }, { "name": "tussenarrest van februari", "size": 7 }, { "name": "tussenarrest van januari", "size": 6 }, { "name": "tussenarrest van juli", "size": 11 }, { "name": "tussenarrest van juni", "size": 6 }, { "name": "tussenarrest van november", "size": 8 }, { "name": "tussenarrest van oktober", "size": 9 }, { "name": "tussenarrest van september", "size": 15 }, { "name": "tweede geding in cassatie", "size": 117 }, { "name": "tweede middel", "size": 71 }, { "name": "tweede middelfaalt", "size": 6 }, { "name": "uitgangspunten", "size": 113 }, { "name": "uitgangspunten in cassatie", "size": 136 }, { "name": "uitspraak", "size": 3002 }, { "name": "uitspraak dd april", "size": 25 }, { "name": "uitspraak dd augustus", "size": 24 }, { "name": "uitspraak dd december", "size": 27 }, { "name": "uitspraak dd februari", "size": 15 }, { "name": "uitspraak dd januari", "size": 17 }, { "name": "uitspraak dd juli", "size": 37 }, { "name": "uitspraak dd juni", "size": 38 }, { "name": "uitspraak dd maart", "size": 27 }, { "name": "uitspraak dd mei", "size": 31 }, { "name": "uitspraak dd november", "size": 16 }, { "name": "uitspraak dd oktober", "size": 31 }, { "name": "uitspraak dd september", "size": 24 }, { "name": "uitspraak van april", "size": 82 }, { "name": "uitspraak van augustus", "size": 12 }, { "name": "uitspraak van de accountantskamer", "size": 82 }, { "name": "uitspraak van de enkelvoudige kamer in de zaak tussen", "size": 41 }, { "name": "uitspraak van de enkelvoudige kamer in het geschil tussen", "size": 11 }, { "name": "uitspraak van de rechtbank", "size": 188 }, { "name": "uitspraak van de rechtbank luidt", "size": 254 }, { "name": "uitspraak van de rechtbank luidt rechtdoende", "size": 74 }, { "name": "uitspraak van de voorzieningenrechter in de zaak tussen", "size": 12 }, { "name": "uitspraak van december", "size": 11 }, { "name": "uitspraak van februari", "size": 10 }, { "name": "uitspraak van januari", "size": 25 }, { "name": "uitspraak van juli", "size": 22 }, { "name": "uitspraak van juni", "size": 33 }, { "name": "uitspraak van maart", "size": 23 }, { "name": "uitspraak van mei", "size": 21 }, { "name": "uitspraak van november", "size": 11 }, { "name": "uitspraak van oktober", "size": 14 }, { "name": "uitspraak waarvan herziening is gevraagd", "size": 79 }, { "name": "uitspraken van de rechtbank", "size": 9 }, { "name": "um", "size": 103 }, { "name": "vader", "size": 14 }, { "name": "valsheid in geschrift meermalen gepleegd", "size": 10 }, { "name": "van het plegen van witwassen een gewoonte maken", "size": 7 }, { "name": "vaststaande feiten", "size": 3357 }, { "name": "vaststaande feiten en de procedure in eerste aanleg", "size": 13 }, { "name": "vaststaande feiten in beide zaken", "size": 6 }, { "name": "vaststaande feiten in conventie en in reconventie", "size": 7 }, { "name": "vaststelling feiten", "size": 13 }, { "name": "vaststelling van het wederrechtelijk verkregen voordeel", "size": 25 }, { "name": "vennootschap onder firma", "size": 9 }, { "name": "verbeurdverklaring", "size": 13 }, { "name": "verdachte", "size": 3315 }, { "name": "verdediging", "size": 12 }, { "name": "verder verloop van de procedure", "size": 29 }, { "name": "verder verloop van het geding", "size": 45 }, { "name": "verder verloop van het geding in hoger beroep", "size": 10 }, { "name": "verdere beoordeling", "size": 9 }, { "name": "verdere beoordeling van het hoger beroep", "size": 22 }, { "name": "verdere feiten", "size": 13 }, { "name": "verdere geding", "size": 24 }, { "name": "verdere geding in hoger beroep", "size": 27 }, { "name": "verdere loop van de procedure", "size": 8 }, { "name": "verdere loop van het geding", "size": 30 }, { "name": "verdere loop van het geding in hoger beroep", "size": 21 }, { "name": "verdere procedure", "size": 72 }, { "name": "verdere verloop van de procedure", "size": 260 }, { "name": "verdere verloop van de procedure in hoger beroep", "size": 8 }, { "name": "verdere verloop van het geding", "size": 134 }, { "name": "verdere verloop van het geding in cassatie", "size": 6 }, { "name": "verdere verloop van het geding in hoger beroep", "size": 674 }, { "name": "verduistering meermalen gepleegd", "size": 8 }, { "name": "verklaring rechtsvermoeden van overlijden", "size": 8 }, { "name": "verklaring van verdachte ter terechtzitting van juni", "size": 22 }, { "name": "verkort arrest van de economische kamer", "size": 21 }, { "name": "verkort arrest van de meervoudige kamer voor strafzaken", "size": 308 }, { "name": "verkort vonnis", "size": 61 }, { "name": "verkrachting", "size": 8 }, { "name": "verlenging ondertoezichtstelling en verlenging machtiging tot uithuisplaatsing", "size": 20 }, { "name": "verloop van de procedure", "size": 1631 }, { "name": "verloop van de procedure blijkt uit", "size": 10 }, { "name": "verloop van de procedure in hoger beroep", "size": 8 }, { "name": "verloop van de procedures", "size": 7 }, { "name": "verloop van het geding", "size": 584 }, { "name": "verloop van het geding in hoger beroep", "size": 116 }, { "name": "verloop van het onderzoek", "size": 6 }, { "name": "verloop van het proces", "size": 30 }, { "name": "vermeerdering van eis", "size": 17 }, { "name": "vermogensmaatregel", "size": 11 }, { "name": "vernietigt het vonnis waarvan beroep en doet opnieuw recht", "size": 30 }, { "name": "veroordeelde", "size": 90 }, { "name": "veroordeelt de verdachte tot een gevangenisstraf voor de duur van", "size": 24 }, { "name": "veroordeelt de verdachte tot een gevangenisstraf voor de duur van maanden", "size": 8 }, { "name": "veroordeelt de verdachte tot een gevangenisstraf voor de duur van n dag", "size": 7 }, { "name": "veroordeelt de verdachte tot een gevangenisstraf voor de duur van n jaar", "size": 6 }, { "name": "veroordeelt de verdachte tot een gevangenisstraf voor de duur van n week", "size": 7 }, { "name": "verordening", "size": 20 }, { "name": "verordeningen", "size": 6 }, { "name": "verplichting tot betaling", "size": 7 }, { "name": "verplichting tot betaling aan de staat", "size": 30 }, { "name": "verschenen", "size": 7 }, { "name": "verslag van de advocaatgeneraal", "size": 20 }, { "name": "vervolg van de procedure", "size": 6 }, { "name": "verweer", "size": 701 }, { "name": "verweer en het tegenverzoek", "size": 10 }, { "name": "verweer en zelfstandig verzoek", "size": 11 }, { "name": "verweer in conventie", "size": 43 }, { "name": "verweer in reconventie", "size": 43 }, { "name": "verweer op het zelfstandig verzoek", "size": 13 }, { "name": "verweer tevens zelfstandig verzoek", "size": 13 }, { "name": "verweer van de gerechtsdeurwaarder", "size": 12 }, { "name": "verweer van gedaagde", "size": 9 }, { "name": "verweerder", "size": 88 }, { "name": "verweerster", "size": 42 }, { "name": "verweren", "size": 26 }, { "name": "verweren strekkende tot bewijsuitsluiting", "size": 7 }, { "name": "verweren van de verdediging", "size": 6 }, { "name": "verwijzingsarrest", "size": 12 }, { "name": "verwijzingsopdracht", "size": 37 }, { "name": "verzet", "size": 9 }, { "name": "verzoek", "size": 840 }, { "name": "verzoek en de grondslag daarvan", "size": 25 }, { "name": "verzoek en het standpunt van de ind", "size": 8 }, { "name": "verzoek en het verweer", "size": 230 }, { "name": "verzoek en het verweer daartegen", "size": 95 }, { "name": "verzoek en verweer", "size": 228 }, { "name": "verzoek tot uitlevering", "size": 6 }, { "name": "verzoek tot verbetering", "size": 13 }, { "name": "verzoeken", "size": 27 }, { "name": "verzoeker", "size": 166 }, { "name": "verzoeker en verzoekster", "size": 8 }, { "name": "verzoeker hierna te noemen verzoeker", "size": 6 }, { "name": "verzoeker sub", "size": 12 }, { "name": "verzoekers", "size": 6 }, { "name": "verzoekster", "size": 79 }, { "name": "verzonden dd", "size": 6 }, { "name": "vierde middel", "size": 18 }, { "name": "vijfde middel", "size": 7 }, { "name": "vof vof gevestigd te vestigingsplaats", "size": 6 }, { "name": "voldoening op aangifte bezwaar en beroep", "size": 10 }, { "name": "vonnis", "size": 238 }, { "name": "vonnis in de hoofdzaak", "size": 6 }, { "name": "vonnis in de strafzaak van", "size": 104 }, { "name": "vonnis in de zaak van", "size": 40 }, { "name": "vonnis in het kort geding van", "size": 10 }, { "name": "vonnis in kort geding in de zaak van", "size": 6 }, { "name": "vonnis in kort geding van de kantonrechter", "size": 12 }, { "name": "vonnis van de kantonrechter", "size": 97 }, { "name": "vonnis van de kantonrechter in kort geding", "size": 16 }, { "name": "vonnis van de kantonrechter van juli", "size": 7 }, { "name": "vonnis van oktober", "size": 7 }, { "name": "vonnis waartegen het hoger beroep is gericht", "size": 10 }, { "name": "vonnis waarvan beroep", "size": 1188 }, { "name": "voor een proceskostenveroordeling bestaat geen aanleiding", "size": 17 }, { "name": "vooraf", "size": 8 }, { "name": "voorbedachte raad", "size": 8 }, { "name": "voorgeschiedenis", "size": 27 }, { "name": "voorhanden stukken", "size": 15 }, { "name": "voorlopige hechtenis", "size": 46 }, { "name": "voortgang van de behandeling van de zaak", "size": 7 }, { "name": "voortgezette handeling van", "size": 8 }, { "name": "voorvragen", "size": 3060 }, { "name": "voorwaardelijk verzoek", "size": 9 }, { "name": "voorwaardelijke incidentele beroep", "size": 9 }, { "name": "voorwaardelijke verzoeken", "size": 7 }, { "name": "vordering", "size": 645 }, { "name": "vordering benadeelde partij", "size": 53 }, { "name": "vordering benadeelde partij en schadevergoedingsmaatregel", "size": 66 }, { "name": "vordering benadeelde partij schadevergoedingsmaatregel", "size": 12 }, { "name": "vordering en de grondslag daarvan", "size": 11 }, { "name": "vordering en de standpunten van partijen", "size": 28 }, { "name": "vordering en het geschil", "size": 15 }, { "name": "vordering en het verweer", "size": 173 }, { "name": "vordering en het verweer in conventie", "size": 24 }, { "name": "vordering en het verweer in het incident", "size": 11 }, { "name": "vordering en het verweer in reconventie", "size": 18 }, { "name": "vordering en het verweer samengevat en zakelijk weergegeven", "size": 8 }, { "name": "vordering en verweer", "size": 111 }, { "name": "vordering en verweer in conventie", "size": 19 }, { "name": "vordering en verweer in reconventie", "size": 13 }, { "name": "vordering grondslag en verweer", "size": 7 }, { "name": "vordering herroeping voorwaardelijke invrijheidstelling", "size": 9 }, { "name": "vordering in conventie", "size": 98 }, { "name": "vordering in de hoofdzaak", "size": 41 }, { "name": "vordering in het incident", "size": 30 }, { "name": "vordering in reconventie", "size": 80 }, { "name": "vordering in voorwaardelijke reconventie", "size": 16 }, { "name": "vordering na voorwaardelijke veroordeling", "size": 97 }, { "name": "vordering officier van justitie", "size": 311 }, { "name": "vordering tenuitvoerlegging", "size": 177 }, { "name": "vordering tenuitvoerlegging voorwaardelijke veroordeling", "size": 12 }, { "name": "vordering tot herroeping van de voorwaardelijke invrijheidsstelling", "size": 7 }, { "name": "vordering tot herroeping van de voorwaardelijke invrijheidstelling", "size": 12 }, { "name": "vordering tot opheffing van de schorsing van de voorlopige hechtenis", "size": 10 }, { "name": "vordering tot schadevergoeding benadeelde partij", "size": 19 }, { "name": "vordering tot tenuitvoerlegging", "size": 81 }, { "name": "vordering tot tenuitvoerlegging in de zaak met parketnummer", "size": 6 }, { "name": "vordering van benadeelde partij slachtoffer", "size": 14 }, { "name": "vordering van de advocaatgeneraal", "size": 33 }, { "name": "vordering van de benadeelde partij", "size": 187 }, { "name": "vordering van de benadeelde partij aangeefster", "size": 32 }, { "name": "vordering van de benadeelde partij aangever", "size": 36 }, { "name": "vordering van de benadeelde partij bedrijf", "size": 7 }, { "name": "vordering van de benadeelde partij benadeelde", "size": 522 }, { "name": "vordering van de benadeelde partij benadeelde partij", "size": 300 }, { "name": "vordering van de benadeelde partij betrokkene", "size": 25 }, { "name": "vordering van de benadeelde partij bp bedrijf", "size": 6 }, { "name": "vordering van de benadeelde partij de schadevergoedingsmaatregel", "size": 108 }, { "name": "vordering van de benadeelde partij en de schadevergoedingsmaatregel", "size": 12 }, { "name": "vordering van de benadeelde partij mbt het onder bewezen verklaarde", "size": 27 }, { "name": "vordering van de benadeelde partij museum catharijneconvent", "size": 8 }, { "name": "vordering van de benadeelde partij persoon", "size": 16 }, { "name": "vordering van de benadeelde partij slachtoffer", "size": 675 }, { "name": "vordering van de benadeelde partij slachtoffer feit", "size": 19 }, { "name": "vordering van de benadeelde partij slachtoffer nummer", "size": 11 }, { "name": "vordering van de benadeelde partij verbalisant", "size": 16 }, { "name": "vordering van de benadeelde partijbenadeelde", "size": 18 }, { "name": "vordering van de benadeelde partijen", "size": 15 }, { "name": "vordering van de benadeelde partijslachtoffer", "size": 27 }, { "name": "vordering van de officier van justitie", "size": 691 }, { "name": "vordering van de procureurgeneraal", "size": 6 }, { "name": "vordering van het openbaar ministerie", "size": 90 }, { "name": "vorderingen", "size": 57 }, { "name": "vorderingen benadeelde partijen", "size": 35 }, { "name": "vorderingen en grondslagen in de hoofdzaak", "size": 12 }, { "name": "vorderingen en het verweer", "size": 8 }, { "name": "vorderingen na voorwaardelijke veroordeling", "size": 11 }, { "name": "vorderingen tenuitvoerlegging", "size": 7 }, { "name": "vorderingen tot tenuitvoerlegging", "size": 13 }, { "name": "vorderingen van de benadeelde partijen", "size": 82 }, { "name": "vorderingen van de benadeelde partijen de schadevergoedingsmaatregel", "size": 11 }, { "name": "vorderingen van de benadeelde partijen en de schadevergoedingsmaatregel", "size": 13 }, { "name": "vraagstelling", "size": 7 }, { "name": "vragen van uitleg", "size": 7 }, { "name": "vrijspraak", "size": 639 }, { "name": "vrijspraak feit", "size": 34 }, { "name": "vrijspraak feit primair", "size": 7 }, { "name": "vrijspraak ten aanzien van feit", "size": 12 }, { "name": "vrijspraak ten aanzien van feit slachtoffer", "size": 7 }, { "name": "vrijspraak van feit", "size": 6 }, { "name": "vrijspraak van het onder ten laste gelegde", "size": 6 }, { "name": "vrijspraak van het primair ten laste gelegde", "size": 7 }, { "name": "vrijspraken", "size": 24 }, { "name": "vrouw", "size": 7 }, { "name": "waardering bewijs", "size": 6 }, { "name": "waardering van de bewijsmiddelen", "size": 8 }, { "name": "waardering van het bewijs", "size": 1032 }, { "name": "waardering van het bewijs ten aanzien van feit", "size": 9 }, { "name": "waarvan het hof uitgaat", "size": 14 }, { "name": "waarvan in hoger beroep moet worden uitgegaan", "size": 6 }, { "name": "wao", "size": 9 }, { "name": "wederrechtelijk verkregen voordeel", "size": 11 }, { "name": "wederspannigheid", "size": 12 }, { "name": "weigeringsgrond als bedoeld in artikel eerste lid aanhef en onder a olw", "size": 9 }, { "name": "weigeringsgrond als bedoeld in artikel eerste lid onder a olw", "size": 19 }, { "name": "weigeringsgrond als bedoeld in artikel eerste lid onder a van de olw", "size": 10 }, { "name": "weigeringsgrond als bedoeld in artikel olw", "size": 22 }, { "name": "werknemer", "size": 9 }, { "name": "wet en regelgeving", "size": 7 }, { "name": "wetgeving jurisprudentie en literatuur", "size": 8 }, { "name": "wetgeving wetsgeschiedenis jurisprudentie en literatuur", "size": 8 }, { "name": "wettelijk kader", "size": 52 }, { "name": "wettelijk kader en wetsgeschiedenis", "size": 6 }, { "name": "wettelijke rente", "size": 7 }, { "name": "wettelijke voorschriften", "size": 795 }, { "name": "wia", "size": 27 }, { "name": "wijziging van eis", "size": 22 }, { "name": "wijziging van omstandigheden", "size": 14 }, { "name": "witwassen", "size": 9 }, { "name": "wjm schram", "size": 6 }, { "name": "wrakingskamer", "size": 16 }, { "name": "wrakingsverzoek", "size": 208 }, { "name": "wrakingsverzoek en het standpunt van de rechter", "size": 7 }, { "name": "wrakingsverzoek en het verweer", "size": 12 }, { "name": "ww", "size": 6 }, { "name": "wwb", "size": 13 }, { "name": "x", "size": 35 }, { "name": "x bv gevestigd te z belanghebbende", "size": 9 }, { "name": "x bv statutair gevestigd te z belanghebbende", "size": 18 }, { "name": "x bv te z belanghebbende", "size": 33 }, { "name": "x te z belanghebbende", "size": 361 }, { "name": "x te z hierna belanghebbende", "size": 21 }, { "name": "x wonende te z belanghebbende", "size": 25 }, { "name": "x wonende te z eiser", "size": 6 }, { "name": "x wonende te z eiser gemachtigde a", "size": 12 }, { "name": "x wonende te z hierna belanghebbende", "size": 17 }, { "name": "y", "size": 8 }, { "name": "zitting", "size": 132 }, { "name": "zitting hebben", "size": 13 }, { "name": "zorgregeling", "size": 10 }, { "name": "zw", "size": 21 }, { "name": "zware mishandeling", "size": 7 }] }, { "name": "beslissing", "children": [{ "name": "_EMPTY", "size": 41 }, { "name": "beoordeling van het geschil en de motivering van de beslissing", "size": 46 }, { "name": "beoordeling van het verzoek en de motivering van de beslissing", "size": 41 }, { "name": "beslissing", "size": 49530 }, { "name": "beslissing afwijzing vordering verlenging terbeschikkingstelling", "size": 6 }, { "name": "beslissing de rechtbank", "size": 8 }, { "name": "beslissing het gerechtshof", "size": 28 }, { "name": "beslissing in conventie en in reconventie", "size": 9 }, { "name": "beslissing in eerste aanleg", "size": 17 }, { "name": "beslissing in het incident", "size": 15 }, { "name": "beslissing in kort geding", "size": 56 }, { "name": "beslissing in reconventie", "size": 7 }, { "name": "beslissing inzake het bewijs", "size": 93 }, { "name": "beslissing met betrekking tot de voorlopige hechtenis", "size": 8 }, { "name": "beslissing na voorwaardelijke veroordeling", "size": 17 }, { "name": "beslissing op de vordering van de benadeelde partij", "size": 44 }, { "name": "beslissing op de vordering van de benadeelde partij feit", "size": 23 }, { "name": "beslissing op de vordering van de benadeelde partij slachtoffer", "size": 9 }, { "name": "beslissing op het hoger beroep", "size": 221 }, { "name": "beslissing op het principale en het incidentele hoger beroep", "size": 25 }, { "name": "beslissing van de kantonrechter", "size": 15 }, { "name": "beslissing van de rechtbank", "size": 198 }, { "name": "beslissing van de voorzitter", "size": 11 }, { "name": "beslissing verlenging terbeschikkingstelling", "size": 178 }, { "name": "beslissing voorwaardelijk einde verpleging van overheidswege", "size": 18 }, { "name": "beslissingen", "size": 49 }, { "name": "beslissingen in eerste aanleg", "size": 6 }, { "name": "beslissingen op de vorderingen van de benadeelde partijen", "size": 6 }, { "name": "bestreden beslissing op bezwaar", "size": 7 }, { "name": "bewijsbeslissing", "size": 7 }, { "name": "bewijsbeslissingen", "size": 39 }, { "name": "feiten het geschil en de beslissing in eerste aanleg", "size": 49 }, { "name": "feiten het geschil en de motivering van de beslissing", "size": 6 }, { "name": "geschil en de beslissing in eerste aanleg", "size": 125 }, { "name": "geschil en de beslissing van de kantonrechter", "size": 10 }, { "name": "geschil en de beslissing van de rechtbank", "size": 24 }, { "name": "geschil en de beslissing van de voorzieningenrechter", "size": 11 }, { "name": "geschil in eerste aanleg en de beslissing van de voorzieningenrechter", "size": 8 }, { "name": "gronden van de beslissing", "size": 694 }, { "name": "gronden voor de beslissing", "size": 8 }, { "name": "motivering van de beslissing", "size": 820 }, { "name": "motivering van de beslissing in het incident", "size": 31 }, { "name": "motivering van de beslissing in hoger beroep", "size": 679 }, { "name": "motivering van de beslissing na voorwaardelijke veroordeling", "size": 37 }, { "name": "slotsom", "size": 2423 }, { "name": "slotsom en conclusie", "size": 8 }, { "name": "slotsom en kosten", "size": 25 }, { "name": "slotsom en proceskosten", "size": 32 }, { "name": "verdere beoordeling van het geschil en de gronden van de beslissing", "size": 7 }, { "name": "verdere motivering van de beslissing", "size": 7 }, { "name": "verdere motivering van de beslissing in hoger beroep", "size": 53 }, { "name": "voortgezette motivering van de beslissing in hoger beroep", "size": 12 }, { "name": "vordering en de beslissing daarop in eerste aanleg", "size": 9 }, { "name": "vordering en de beslissing in eerste aanleg", "size": 28 }, { "name": "vordering in eerste aanleg en de beslissing daarop", "size": 10 }, { "name": "vorderingen en de beslissing in eerste aanleg", "size": 10 }, { "name": "vorderingen en de beslissingen in eerste aanleg", "size": 6 }, { "name": "vorderingen in eerste aanleg en de beslissing daarop", "size": 6 }] }, { "name": "overwegingen", "children": [{ "name": "_EMPTY", "size": 32 }, { "name": "achtergronden van de zaak", "size": 7 }, { "name": "algemene bewijsoverwegingen", "size": 6 }, { "name": "algemene overwegingen in alle zaken", "size": 6 }, { "name": "ambtshalve beoordeling van de bestreden uitspraak", "size": 178 }, { "name": "beoordeling", "size": 13418 }, { "name": "beoordeling door de rechtbank", "size": 43 }, { "name": "beoordeling en de gronden daarvoor", "size": 31 }, { "name": "beoordeling in beide zaken", "size": 7 }, { "name": "beoordeling in conventie", "size": 176 }, { "name": "beoordeling in conventie en in reconventie", "size": 94 }, { "name": "beoordeling in conventie en in voorwaardelijke reconventie", "size": 13 }, { "name": "beoordeling in conventie en reconventie", "size": 20 }, { "name": "beoordeling in de hoofdzaak", "size": 72 }, { "name": "beoordeling in de incidenten", "size": 19 }, { "name": "beoordeling in de vrijwaringszaak", "size": 11 }, { "name": "beoordeling in eerste aanleg", "size": 14 }, { "name": "beoordeling in het incident", "size": 226 }, { "name": "beoordeling in het incident en in de hoofdzaak", "size": 6 }, { "name": "beoordeling in het incident ex art rv", "size": 6 }, { "name": "beoordeling in het incident tot verzet tegen de eiswijziging", "size": 11 }, { "name": "beoordeling in het principaal en incidenteel appel", "size": 6 }, { "name": "beoordeling in hoger beroep", "size": 156 }, { "name": "beoordeling in reconventie", "size": 150 }, { "name": "beoordeling in voorwaardelijke reconventie", "size": 15 }, { "name": "beoordeling rechtbank", "size": 7 }, { "name": "beoordeling van de aanvraag", "size": 76 }, { "name": "beoordeling van de door belanghebbende voorgestelde middelen", "size": 31 }, { "name": "beoordeling van de door de staatssecretaris voorgestelde middelen", "size": 6 }, { "name": "beoordeling van de geschillen", "size": 11 }, { "name": "beoordeling van de geschillen in hoger beroep", "size": 6 }, { "name": "beoordeling van de grieven", "size": 127 }, { "name": "beoordeling van de grieven en de vordering", "size": 14 }, { "name": "beoordeling van de gronden van het verzet", "size": 11 }, { "name": "beoordeling van de hoger beroepen", "size": 9 }, { "name": "beoordeling van de in het incidentele beroep voorgestelde middelen", "size": 10 }, { "name": "beoordeling van de in het principale beroep voorgestelde middelen", "size": 18 }, { "name": "beoordeling van de incidentele vordering", "size": 8 }, { "name": "beoordeling van de klacht", "size": 40 }, { "name": "beoordeling van de klachten", "size": 403 }, { "name": "beoordeling van de middelen", "size": 899 }, { "name": "beoordeling van de middelen in het principale beroep", "size": 10 }, { "name": "beoordeling van de middelen in het principale en in het incidentele beroep", "size": 9 }, { "name": "beoordeling van de middelen voor het overige", "size": 62 }, { "name": "beoordeling van de namens de verdachte voorgestelde middelen", "size": 9 }, { "name": "beoordeling van de ontvankelijkheid", "size": 98 }, { "name": "beoordeling van de ontvankelijkheid van het beroep", "size": 200 }, { "name": "beoordeling van de ontvankelijkheid van het beroep in cassatie", "size": 661 }, { "name": "beoordeling van de ontvankelijkheid van het verzoek tot herziening", "size": 39 }, { "name": "beoordeling van de overige middelen", "size": 90 }, { "name": "beoordeling van de standpunten", "size": 10 }, { "name": "beoordeling van de ten laste gelegde feiten", "size": 7 }, { "name": "beoordeling van de verzoeken", "size": 19 }, { "name": "beoordeling van de vordering", "size": 107 }, { "name": "beoordeling van de vorderingen", "size": 7 }, { "name": "beoordeling van de zaak", "size": 71 }, { "name": "beoordeling van het achtste middel", "size": 7 }, { "name": "beoordeling van het beklag", "size": 24 }, { "name": "beoordeling van het beroep", "size": 16 }, { "name": "beoordeling van het beroep na verwijzing", "size": 45 }, { "name": "beoordeling van het bewijs", "size": 1516 }, { "name": "beoordeling van het bezwaar", "size": 8 }, { "name": "beoordeling van het cassatieberoep", "size": 57 }, { "name": "beoordeling van het cassatiemiddel", "size": 8 }, { "name": "beoordeling van het derde middel", "size": 142 }, { "name": "beoordeling van het door belanghebbende voorgestelde middel", "size": 9 }, { "name": "beoordeling van het door de staatssecretaris voorgestelde middel", "size": 38 }, { "name": "beoordeling van het eerste en het tweede middel", "size": 46 }, { "name": "beoordeling van het eerste het tweede en het derde middel", "size": 13 }, { "name": "beoordeling van het eerste het tweede het derde en het vierde middel", "size": 7 }, { "name": "beoordeling van het eerste middel", "size": 370 }, { "name": "beoordeling van het eerste namens de verdachte voorgestelde middel", "size": 12 }, { "name": "beoordeling van het geschil", "size": 2622 }, { "name": "beoordeling van het geschil in conventie en in reconventie", "size": 8 }, { "name": "beoordeling van het geschil in hoger beroep", "size": 290 }, { "name": "beoordeling van het geschil vaststaande feiten", "size": 10 }, { "name": "beoordeling van het geschil wijziging van eis", "size": 7 }, { "name": "beoordeling van het hoger beroep", "size": 1655 }, { "name": "beoordeling van het hoger beroep en het incidenteel hoger beroep", "size": 9 }, { "name": "beoordeling van het hoger beroep en het incidentele hoger beroep", "size": 7 }, { "name": "beoordeling van het hoger beroep in de zaak met zaaknummer", "size": 9 }, { "name": "beoordeling van het hoger beroep na verwijzing", "size": 10 }, { "name": "beoordeling van het hoger beroep zaaknummer", "size": 10 }, { "name": "beoordeling van het in het incidentele beroep voorgestelde middel", "size": 22 }, { "name": "beoordeling van het in het principale beroep voorgestelde middel", "size": 43 }, { "name": "beoordeling van het incident", "size": 11 }, { "name": "beoordeling van het incidentele beroep in cassatie", "size": 22 }, { "name": "beoordeling van het middel", "size": 1375 }, { "name": "beoordeling van het middel in het incidentele beroep", "size": 34 }, { "name": "beoordeling van het middel in het principale beroep", "size": 87 }, { "name": "beoordeling van het namens de benadeelde partij voorgestelde middel", "size": 14 }, { "name": "beoordeling van het negende middel", "size": 6 }, { "name": "beoordeling van het principaal en incidenteel hoger beroep", "size": 23 }, { "name": "beoordeling van het principale en het incidentele hoger beroep", "size": 16 }, { "name": "beoordeling van het ten laste gelegde", "size": 6 }, { "name": "beoordeling van het tiende middel", "size": 8 }, { "name": "beoordeling van het tweede en het derde middel", "size": 16 }, { "name": "beoordeling van het tweede middel", "size": 303 }, { "name": "beoordeling van het tweede namens de verdachte voorgestelde middel", "size": 9 }, { "name": "beoordeling van het verzet", "size": 16 }, { "name": "beoordeling van het verzochte", "size": 42 }, { "name": "beoordeling van het verzoek", "size": 292 }, { "name": "beoordeling van het verzoek tot het vaststellen van een dwangakkoord", "size": 7 }, { "name": "beoordeling van het verzoek zaaknummer", "size": 8 }, { "name": "beoordeling van het vierde middel", "size": 52 }, { "name": "beoordeling van het vijfde middel", "size": 28 }, { "name": "beoordeling van het wrakingsverzoek", "size": 41 }, { "name": "beoordeling van het zesde middel", "size": 13 }, { "name": "beoordeling van het zevende middel", "size": 12 }, { "name": "beoordeling vaststaande feiten", "size": 6 }, { "name": "beschouwing en beoordeling", "size": 15 }, { "name": "beschouwing en beoordeling van de middelen", "size": 12 }, { "name": "beschouwing en beoordeling van het middel", "size": 6 }, { "name": "bewijs en bewijsoverwegingen", "size": 7 }, { "name": "bewijsmiddelen en de beoordeling daarvan", "size": 204 }, { "name": "bewijsmotivering", "size": 153 }, { "name": "bewijsoverwegingen", "size": 540 }, { "name": "bewijsoverwegingen met betrekking tot zaaksdossier adres", "size": 7 }, { "name": "bewijsoverwegingen ten aanzien van feit", "size": 6 }, { "name": "bijzondere overwegingen omtrent het bewijs", "size": 14 }, { "name": "feiten en achtergronden", "size": 24 }, { "name": "geschil en beoordeling", "size": 21 }, { "name": "geschil en de beoordeling", "size": 61 }, { "name": "geschil en de beoordeling daarvan", "size": 100 }, { "name": "geschil en de beoordeling in eerste aanleg", "size": 37 }, { "name": "grieven en beoordeling in hoger beroep", "size": 7 }, { "name": "grieven en de beoordeling in hoger beroep", "size": 23 }, { "name": "gronden", "size": 695 }, { "name": "gronden van het hoger beroep", "size": 861 }, { "name": "gronden van het verzet", "size": 14 }, { "name": "gronden van het verzoek", "size": 19 }, { "name": "gronden van het wrakingsverzoek", "size": 13 }, { "name": "gronden van het wrakingsverzoek en het standpunt van verzoeker", "size": 7 }, { "name": "gronden van wraking", "size": 7 }, { "name": "inhoudelijke beoordeling", "size": 8 }, { "name": "inleidende overwegingen", "size": 6 }, { "name": "motivering", "size": 266 }, { "name": "motivering maatregel", "size": 7 }, { "name": "motivering straf", "size": 42 }, { "name": "motivering van de bewezenverklaring", "size": 9 }, { "name": "motivering van de hoofdelijkheid", "size": 41 }, { "name": "motivering van de maatregel", "size": 20 }, { "name": "motivering van de maatregel onttrekking aan het verkeer", "size": 9 }, { "name": "motivering van de sanctie", "size": 159 }, { "name": "motivering van de sancties", "size": 409 }, { "name": "motivering van de straf", "size": 156 }, { "name": "motivering van de straf en maatregel", "size": 26 }, { "name": "motivering van de straf en maatregelen", "size": 17 }, { "name": "motivering van de straffen", "size": 48 }, { "name": "motivering van de straffen en maatregel", "size": 7 }, { "name": "motivering van de straffen en maatregelen", "size": 768 }, { "name": "motivering van de verbeurdverklaring", "size": 6 }, { "name": "motivering van straf of maatregel", "size": 89 }, { "name": "motivering vrijspraak", "size": 35 }, { "name": "nadere beoordeling", "size": 30 }, { "name": "nadere beoordeling van het hoger beroep", "size": 21 }, { "name": "nadere beoordeling van het middel", "size": 7 }, { "name": "nadere bewijsmotivering", "size": 22 }, { "name": "nadere bewijsoverwegingen", "size": 23 }, { "name": "op te leggen straf of maatregel en de gronden daarvoor", "size": 14 }, { "name": "overwegingen", "size": 14304 }, { "name": "overwegingen en oordeel van het hof", "size": 6 }, { "name": "overwegingen met betrekking tot het bewijs", "size": 37 }, { "name": "overwegingen omtrent het geschil", "size": 6 }, { "name": "overwegingen ten aanzien van het bewijs", "size": 64 }, { "name": "overwegingen ten aanzien van straf enof maatregel", "size": 190 }, { "name": "overwegingen van de kantonrechter", "size": 35 }, { "name": "overwegingen van de rechtbank", "size": 107 }, { "name": "overwegingen van het hof", "size": 9 }, { "name": "rechtsoverwegingen", "size": 40 }, { "name": "standpunten van partijen en de beoordeling daarvan", "size": 15 }, { "name": "strafmotivering", "size": 577 }, { "name": "tenlastelegging en motivering van de gegeven vrijspraak", "size": 6 }, { "name": "uitgangspunten voor de beoordeling", "size": 10 }, { "name": "vaststellingen en overwegingen", "size": 19 }, { "name": "verdere beoordeling", "size": 1563 }, { "name": "verdere beoordeling in conventie en in reconventie", "size": 18 }, { "name": "verdere beoordeling in de hoofdzaak", "size": 7 }, { "name": "verdere beoordeling in hoger beroep", "size": 35 }, { "name": "verdere beoordeling in reconventie", "size": 7 }, { "name": "verdere beoordeling van de zaak", "size": 15 }, { "name": "verdere beoordeling van het geschil", "size": 56 }, { "name": "verdere beoordeling van het geschil in hoger beroep", "size": 10 }, { "name": "verdere beoordeling van het hoger beroep", "size": 107 }, { "name": "verdere beoordeling van het middel", "size": 8 }, { "name": "verdere beoordeling vaststaande feiten", "size": 9 }, { "name": "verzoek en de beoordeling", "size": 53 }, { "name": "verzoek en de beoordeling daarvan", "size": 14 }, { "name": "verzoek en de gronden daarvan", "size": 78 }, { "name": "vordering en beoordeling daarvan in eerste aanleg", "size": 9 }, { "name": "vordering en beoordeling in eerste aanleg", "size": 55 }, { "name": "vordering en de beoordeling in eerste aanleg", "size": 16 }, { "name": "vorderingen en beoordeling in eerste aanleg", "size": 27 }] }, { "name": "procesverloop", "children": [{ "name": "feiten en het procesverloop", "size": 121 }, { "name": "feiten en procesverloop", "size": 250 }, { "name": "inleiding feiten en procesverloop", "size": 10 }, { "name": "ontstaan en loop van de gedingen", "size": 31 }, { "name": "ontstaan en loop van de procedure", "size": 45 }, { "name": "ontstaan en loop van het geding", "size": 2384 }, { "name": "ontstaan en loop van het geding voor verwijzing", "size": 6 }, { "name": "procesgang", "size": 489 }, { "name": "procesverloop", "size": 9511 }, { "name": "procesverloop en de processtukken", "size": 93 }, { "name": "procesverloop in eerste aanleg en vaststaande feiten", "size": 226 }, { "name": "procesverloop in hoger beroep", "size": 529 }, { "name": "verder procesverloop in hoger beroep", "size": 19 }, { "name": "verdere procesverloop", "size": 98 }, { "name": "verdere procesverloop in hoger beroep", "size": 16 }, { "name": "voorgeschiedenis en het procesverloop", "size": 111 }] }] }, "href": "https://rechtspraak.cloudant.com/docs/_design/stats/_list/value_large_enough/section_roles?group_level=2&stale=ok" };

/***/ },
/* 297 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _d = __webpack_require__(227);

	var _d2 = _interopRequireDefault(_d);

	var _underscore = __webpack_require__(210);

	var _underscore2 = _interopRequireDefault(_underscore);

	var _reactFauxDom = __webpack_require__(229);

	var _reactFauxDom2 = _interopRequireDefault(_reactFauxDom);

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var OTHER = '_OTHER';

	var createLegend = function createLegend(labels, textSize, color) {
	    var legendHeight = 15;
	    var legendWidth = 25;
	    var legendSpacing = 4;
	    var labelMargin = {
	        top: 5,
	        right: 5,
	        bottom: 5,
	        left: 5
	    };
	    var labelOffsetLeft = labelMargin.left;
	    var labelOffsetTop = labelMargin.top;

	    var legendItems = _underscore2.default.map(labels, function (label, i) {
	        var labelEl = '';

	        var labelText = label;
	        if (label == 'null') {
	            labelText = "No role";
	        }

	        if (labelText.match(/beslissing|overwegingen|procesverloop/)) {
	            labelEl = _react2.default.createElement(
	                'span',
	                { style: {
	                        display: 'table-cell',
	                        fontSize: 'x-small'
	                    },
	                    className: "cell"
	                },
	                labelText
	            );
	        } else {
	            labelEl = _react2.default.createElement(
	                'span',
	                { style: {
	                        display: 'table-cell',
	                        fontSize: 'x-small'
	                    },
	                    className: "cell"
	                },
	                labelText
	            );
	        }
	        var yPos = i * (legendHeight + legendSpacing) + labelOffsetTop;
	        //console.log(label, i);
	        return _react2.default.createElement(
	            'div',
	            {
	                key: i,
	                style: { display: "table-row" },
	                className: "label series-" + labelText },
	            _react2.default.createElement(
	                'div',
	                { style: {
	                        display: 'table-cell'
	                    },
	                    className: "cell series-" + (i + 1)
	                },
	                _react2.default.createElement('div', { style: {
	                        width: "26px",
	                        height: "14px",
	                        display: 'block',
	                        backgroundColor: color(label),
	                        margin: "1px 5px"
	                    } })
	            ),
	            labelEl
	        );
	    });

	    var style = {
	        //width:"115px",
	        float: "right",
	        marginBottom: "6px",
	        display: "table",
	        height: (legendSpacing + legendHeight) * legendItems.length - legendSpacing + labelMargin.top + labelMargin.bottom + "px"
	        //position:  "absolute",
	        //top:0,
	        //right:0
	    };
	    return _react2.default.createElement(
	        'div',
	        {
	            style: style, className: 'legend' },
	        legendItems
	    );
	};

	var TreeMap = function (_React$Component) {
	    _inherits(TreeMap, _React$Component);

	    function TreeMap() {
	        _classCallCheck(this, TreeMap);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(TreeMap).apply(this, arguments));
	    }

	    _createClass(TreeMap, [{
	        key: 'render',
	        value: function render() {
	            var root = this.props.data;

	            var outerWidth = this.props.width ? this.props.width : 600;
	            var outerHeight = this.props.height ? this.props.height : 400;

	            var color = _d2.default.scale.ordinal().range(this.props.colorRange);
	            //var color = d3.scale.category20c();

	            var el = _reactFauxDom2.default.createElement('div');

	            var treemap = _d2.default.layout.treemap().size([outerWidth, outerHeight]).sticky(true).value(function (d) {
	                return d.size;
	            });

	            var div = _d2.default.select(el);

	            function position() {
	                this.style("left", function (d) {
	                    return d.x + "px";
	                }).style("top", function (d) {
	                    return d.y + "px";
	                }).style("width", function (d) {
	                    return Math.max(0, d.dx - 1) + "px";
	                }).style("min-width", function (d) {
	                    return Math.max(0, d.dx - 1) + "px";
	                }).style("height", function (d) {
	                    return Math.max(0, d.dy - 1) + "px";
	                }).style("min-height", function (d) {
	                    return Math.max(0, d.dy - 1) + "px";
	                });
	            }

	            var node = div.datum(root).selectAll(".node").data(treemap.nodes).enter().append("div").attr("class", "node").call(position).style("background", function (d) {
	                return d.children ? color(d.name) : null;
	            });
	            node.append("span").attr("class", "label-pattern").text(function (d) {
	                return d.children ? null : d.name;
	            });
	            node.append("span").attr("class", "label-pattern-count").text(function (d) {
	                return d.children ? null : d.size;
	            });

	            var labels = _underscore2.default.map(root.children, function (el) {
	                return el.name;
	            });
	            var legend = createLegend(labels, 15, color);

	            div.style("position", "relative").style("display", "inline-block").style("width", outerWidth).style("height", outerHeight);
	            //SVGReact.props.children = this.props.children;
	            return _react2.default.createElement(
	                'div',
	                {
	                    className: 'chart tree-map' },
	                legend,
	                _react2.default.createElement(
	                    'a',
	                    { style: { display: "block", fontSize: 'x-small' }, href: this.props.sourceHref },
	                    'Data source'
	                ),
	                el.toReact()
	            );
	        }
	    }]);

	    return TreeMap;
	}(_react2.default.Component);

	exports.default = TreeMap;

	TreeMap.defaultProps = {
	    colorRange: ["#1E405C", "#FFB4B4", "#2C70A9", "#56a0dd", "#9DCBF1", "#a05d56", "#d0743c", "#ff8c00"]
	};
	TreeMap.propTypes = {
	    margin: _react2.default.PropTypes.shape({
	        top: _react2.default.PropTypes.number,
	        bottom: _react2.default.PropTypes.number,
	        left: _react2.default.PropTypes.number,
	        right: _react2.default.PropTypes.number
	    }),
	    data: _react2.default.PropTypes.shape({
	        name: _react2.default.PropTypes.string,
	        children: _react2.default.PropTypes.arrayOf(_react2.default.PropTypes.object)
	    }).isRequired,
	    width: _react2.default.PropTypes.number,
	    height: _react2.default.PropTypes.number,
	    sourceHref: _react2.default.PropTypes.string.isRequired
	};

/***/ },
/* 298 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _underscore = __webpack_require__(210);

	var _underscore2 = _interopRequireDefault(_underscore);

	var _PercentageBar = __webpack_require__(294);

	var _PercentageBar2 = _interopRequireDefault(_PercentageBar);

	var _data = __webpack_require__(299);

	var _data2 = _interopRequireDefault(_data);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var FigureRelativeTitleCountForTerms = function (_React$Component) {
	    _inherits(FigureRelativeTitleCountForTerms, _React$Component);

	    function FigureRelativeTitleCountForTerms() {
	        _classCallCheck(this, FigureRelativeTitleCountForTerms);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(FigureRelativeTitleCountForTerms).apply(this, arguments));
	    }

	    _createClass(FigureRelativeTitleCountForTerms, [{
	        key: 'render',

	        //noinspection JSMethodCanBeStatic
	        value: function render() {
	            return _react2.default.createElement(
	                'figure',
	                { id: _figs2.default.titleRelativeWordCount.id },
	                _react2.default.createElement(
	                    'table',
	                    { className: 'chart table' },
	                    _react2.default.createElement(
	                        'thead',
	                        null,
	                        _react2.default.createElement(
	                            'tr',
	                            null,
	                            _react2.default.createElement('th', null),
	                            _react2.default.createElement(
	                                'th',
	                                null,
	                                'term'
	                            ),
	                            _react2.default.createElement(
	                                'th',
	                                null,
	                                'relative title frequency'
	                            )
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'tbody',
	                        null,
	                        _underscore2.default.map(_data2.default.data, function (term, o) {
	                            var percentageTxt = term[1].toFixed(2) + "%";
	                            return _react2.default.createElement(
	                                'tr',
	                                { key: term[0] },
	                                _react2.default.createElement(
	                                    'th',
	                                    { className: 'nr' },
	                                    o + 1
	                                ),
	                                _react2.default.createElement(
	                                    'td',
	                                    null,
	                                    term[0]
	                                ),
	                                _react2.default.createElement(
	                                    'td',
	                                    null,
	                                    _react2.default.createElement(_PercentageBar2.default, { percentage: percentageTxt, text: percentageTxt }),
	                                    _react2.default.createElement(
	                                        'span',
	                                        { className: 'perc-text' },
	                                        percentageTxt
	                                    )
	                                )
	                            );
	                        })
	                    )
	                ),
	                _react2.default.createElement(
	                    'figcaption',
	                    null,
	                    _react2.default.createElement(
	                        'span',
	                        { className: 'figure-number' },
	                        'Fig ',
	                        _figs2.default.titleTf.num,
	                        '.'
	                    ),
	                    ' Top ',
	                    _data2.default.numberOfTerms,
	                    ' word stems in occurring section titles. Percentages are the percentage of titles texts that the word stem occurs in. Stemming is performed using the ',
	                    _react2.default.createElement(
	                        'a',
	                        { href: 'http://snowball.tartarus.org/algorithms/dutch/stemmer.html' },
	                        'Snowball algorithm for Dutch'
	                    ),
	                    '.'
	                )
	            );
	        }
	    }]);

	    return FigureRelativeTitleCountForTerms;
	}(_react2.default.Component);

	exports.default = FigureRelativeTitleCountForTerms;

/***/ },
/* 299 */
/***/ function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	exports.default = {
	    "numberOfTerms": 10,
	    "data": [["_NUM", 29.24996810836629], ["_ART", 24.084858445646272], ["van", 10.043046288852167], ["besliss", 8.428736290305833], ["beoordel", 4.71951678983265], ["in", 4.546708635067744], ["geding", 3.546794668312176], ["feit", 3.0420168565825816], ["_PUNCT", 2.9803102536794044], ["beroep", 2.839690398986588]]
	};

/***/ },
/* 300 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _getTermFrequencyData = __webpack_require__(301);

	var _getTermFrequencyData2 = _interopRequireDefault(_getTermFrequencyData);

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _underscore = __webpack_require__(210);

	var _underscore2 = _interopRequireDefault(_underscore);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _PercentageBar = __webpack_require__(294);

	var _PercentageBar2 = _interopRequireDefault(_PercentageBar);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	var FigureTitleTfIdf = function (_React$Component) {
	    _inherits(FigureTitleTfIdf, _React$Component);

	    function FigureTitleTfIdf() {
	        _classCallCheck(this, FigureTitleTfIdf);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(FigureTitleTfIdf).apply(this, arguments));
	    }

	    _createClass(FigureTitleTfIdf, [{
	        key: 'render',
	        value: function render() {
	            var nTerms = 20;
	            var tfIdfScores = (0, _getTermFrequencyData2.default)(nTerms);
	            var maxVal = tfIdfScores[0][1];
	            _underscore2.default.map(tfIdfScores, function (arr) {
	                arr[2] = (100 * arr[1] / maxVal).toFixed(2) + "%";
	            });

	            return _react2.default.createElement(
	                'figure',
	                { className: 'chart', id: _figs2.default.tfidf.id },
	                _react2.default.createElement(
	                    'table',
	                    { className: 'chart table' },
	                    _react2.default.createElement(
	                        'thead',
	                        null,
	                        _react2.default.createElement(
	                            'tr',
	                            null,
	                            _react2.default.createElement('th', null),
	                            _react2.default.createElement(
	                                'th',
	                                null,
	                                'term'
	                            ),
	                            _react2.default.createElement(
	                                'th',
	                                null,
	                                'tf-idf score'
	                            )
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'tbody',
	                        null,
	                        _underscore2.default.map(tfIdfScores, function (term, o) {
	                            var tfIdfScoreStr = term[1].toFixed(2);return _react2.default.createElement(
	                                'tr',
	                                { key: term[0] },
	                                _react2.default.createElement(
	                                    'th',
	                                    { className: 'nr' },
	                                    o + 1
	                                ),
	                                _react2.default.createElement(
	                                    'td',
	                                    null,
	                                    term[0]
	                                ),
	                                _react2.default.createElement(
	                                    'td',
	                                    { className: 'tf-idf-score' },
	                                    _react2.default.createElement(_PercentageBar2.default, { percentage: term[2], text: tfIdfScoreStr + '' }),
	                                    _react2.default.createElement(
	                                        'span',
	                                        { className: 'perc-text' },
	                                        tfIdfScoreStr
	                                    )
	                                )
	                            );
	                        })
	                    )
	                ),
	                _react2.default.createElement(
	                    'figcaption',
	                    null,
	                    _react2.default.createElement(
	                        'span',
	                        { className: 'figure-number' },
	                        'Fig ',
	                        _figs2.default.tfidf.num,
	                        '.'
	                    ),
	                    ' Top ',
	                    nTerms,
	                    ' tf-idf scores for stemmed words in section titles. We take as document here any string block (such as a paragraph, or title). Stemming is performed using the ',
	                    _react2.default.createElement(
	                        'a',
	                        { href: 'http://snowball.tartarus.org/algorithms/dutch/stemmer.html' },
	                        'Snowball algorithm for Dutch'
	                    ),
	                    '.'
	                )
	            );
	        }
	    }]);

	    return FigureTitleTfIdf;
	}(_react2.default.Component);

	exports.default = FigureTitleTfIdf;

/***/ },
/* 301 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _underscore = __webpack_require__(210);

	var _underscore2 = _interopRequireDefault(_underscore);

	var _rawData = __webpack_require__(302);

	var _rawData2 = _interopRequireDefault(_rawData);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function tfidfForAllTerms(topN) {
	    var sorted = _underscore2.default.map(_rawData2.default, function (e) {
	        return [e.term, e.score];
	    });
	    if (topN) sorted.splice(topN);
	    return sorted;
	}

	exports.default = tfidfForAllTerms;

/***/ },
/* 302 */
/***/ function(module, exports) {

	"use strict";module.exports=[{"term":"_NUM","score":393148.64984127437},{"term":"_ART","score":384454.94465026655},{"term":"besliss","score":285029.4092010561},{"term":"beoordel","score":173667.48901928248},{"term":"van","score":160068.16835130303},{"term":"geding","score":132553.36627456188},{"term":"overweg","score":114450.42138161842},{"term":"feit","score":94987.7575340151},{"term":"in","score":84479.5955831566},{"term":"geschil","score":83552.951798064},{"term":"beroep","score":80581.06155389751},{"term":"hoger","score":78759.22665234478},{"term":"procesverlop","score":78414.24015429006},{"term":"strafbar","score":64106.45720681872},{"term":"procedur","score":60542.15965272519},{"term":"[","score":50862.212958176715},{"term":"en","score":48121.63668133538},{"term":"tenlastelegg","score":47570.337113122696},{"term":"],","score":45122.38467212454},{"term":"_PUNCT","score":44995.937026929},{"term":"rechtsmiddel","score":43423.98725925943},{"term":"verdacht","score":37414.63723701132},{"term":"partij","score":36229.81445717601},{"term":"aanleg","score":34518.122715103396},{"term":"cassatie","score":34318.58420647567},{"term":"vorder","score":34130.27882844579},{"term":"eerst","score":32325.21297540525},{"term":"voorvrag","score":31468.69911651183},{"term":"vaststaand","score":30947.28653615719},{"term":"benadeeld","score":30859.55069801901},{"term":"onderzoek","score":29965.315476095308},{"term":"motiver","score":29003.672700981006},{"term":"voorschrift","score":28846.963724279987},{"term":"standpunt","score":28590.09431692155},{"term":"middel","score":27397.0297607832},{"term":"wettelijk","score":26790.96178786555},{"term":"verlop","score":25548.78138054091},{"term":"toepass","score":25315.994199633806},{"term":",","score":24636.858078484336},{"term":"bewezenverklar","score":24344.144910443094},{"term":"bewijs","score":24254.874937055618},{"term":"uitsprak","score":23000.678655192387},{"term":"lop","score":22226.884508587253},{"term":"proceskost","score":21779.743390281168},{"term":"rechtbank","score":21027.646269244677},{"term":"verder","score":20395.114609103864},{"term":"/","score":19623.631328836702},{"term":"slotsom","score":19097.139716256414},{"term":"maatregel","score":18678.706608807188},{"term":"]","score":18281.82578795572},{"term":"terechtzit","score":18257.716523616506},{"term":"straf","score":17984.994367117954},{"term":"-","score":17872.335611780854},{"term":"zak","score":17795.235905653513},{"term":"ontstan","score":16508.310609239732},{"term":"verzoek","score":15777.680847909283},{"term":"verwer","score":14876.889404205815},{"term":"bewezenverklaard","score":14205.485394996227},{"term":"gerechtshof","score":14102.67830413882},{"term":"wetsartikel","score":13853.527654867596},{"term":"arrest","score":13783.8271845514},{"term":"(","score":13172.163086607885},{"term":"of","score":13016.119754741998},{"term":"grond","score":12870.120229629722},{"term":"conclusies","score":12839.988899653486},{"term":")","score":12711.700938706379},{"term":"ontvank","score":12706.016761899218},{"term":"kwalificatie","score":12287.588335969971},{"term":"ter","score":11847.037047359183},{"term":"hag","score":11167.28504490736},{"term":"oplegg","score":11113.414502283445},{"term":"vonnis","score":10866.9936912091},{"term":"instanties","score":10761.396167106896},{"term":"den","score":10565.709036479593},{"term":"te","score":10488.277302703456},{"term":"justitie","score":10389.54792603545},{"term":"officier","score":10381.56346133334},{"term":"feitelijk","score":10376.454231194171},{"term":"op","score":10033.774456076591},{"term":"d","score":9935.444264565063},{"term":"strafoplegg","score":8944.406590996019},{"term":"hd","score":8803.113433348472},{"term":"conventie","score":8618.266400955583},{"term":"waarder","score":8607.55589745038},{"term":"zaaknr","score":8599.556527091627},{"term":"reconventie","score":8508.12780480283},{"term":"bewijsoverweg","score":8307.359486274245},{"term":"waarvan","score":8137.758118655883},{"term":"toegepast","score":7814.787603379457},{"term":"besprek","score":7727.537005984668},{"term":"strafzak","score":7631.219433865774},{"term":"ten","score":7615.142548285799},{"term":"aanzien","score":7539.929192303387},{"term":"kamer","score":7496.831079757541},{"term":"tot","score":7485.371965237582},{"term":"b","score":7477.083576515316},{"term":"] ,","score":7462.699800534515},{"term":"vrijsprak","score":7452.772952100351},{"term":"straff","score":7177.592281379058},{"term":"sub","score":6912.785082521852},{"term":"vor","score":6858.6546646982715},{"term":"slachtoffer","score":6807.847970052481},{"term":"met","score":6785.859523276516},{"term":"noord","score":6748.262398784699},{"term":"nederland","score":6719.476219528733},{"term":"meervoud","score":6705.195976936331},{"term":"oost","score":6629.2268979899545},{"term":"brabant","score":6623.235932022531},{"term":"conclusie","score":6607.645516330463},{"term":"beslag","score":6341.007775151378},{"term":"incident","score":6075.344912263712},{"term":"schadevergoedingsmaatregel","score":5919.073338164227},{"term":"beschik","score":5882.363422109723},{"term":"oordel","score":5752.091856042816},{"term":"strafmotiver","score":5713.624979661643},{"term":"kost","score":5506.64358865736},{"term":"gedaagd","score":5479.863311960169},{"term":"bewez","score":5442.284917780741},{"term":"appellant","score":5438.982365730559},{"term":"inhoud","score":5409.803760649346},{"term":"ntimeerd","score":5339.461656511547},{"term":"ministerie","score":5210.981328189455},{"term":"i","score":5156.038742748002},{"term":"ge","score":5140.665884812895},{"term":"za","score":5139.784141647311},{"term":"eiser","score":5139.682374677637},{"term":"cassatiemiddel","score":5076.750674140494},{"term":"procesgang","score":4904.05067569501},{"term":"inleid","score":4840.352535708726},{"term":"a","score":4837.923933328768},{"term":"verklaard","score":4765.13684196585},{"term":"belanghebb","score":4646.635747218052},{"term":"principal","score":4556.227253298328},{"term":"formel","score":4553.262950536578},{"term":"klacht","score":4547.603471297724},{"term":"sanctie","score":4539.540194392784},{"term":"voorwaard","score":4538.122020821238},{"term":"twed","score":4456.897607807204},{"term":"z","score":4307.950407821482},{"term":"vennootschap","score":4298.243713764996},{"term":"artikel","score":4262.120751334078},{"term":"incidentel","score":4256.152885724186},{"term":"open","score":4202.188992593077},{"term":"bezwar","score":4164.312618342908},{"term":"e","score":4000.467187966783},{"term":"ha","score":3999.03010231653},{"term":"s","score":3977.1761792018324},{"term":"verkort","score":3917.5473110865064},{"term":"griev","score":3903.3251398266398},{"term":"bewijsmiddel","score":3879.3070030216463},{"term":"bepal","score":3850.4846417178837},{"term":"hoofdzak","score":3776.7213062998844},{"term":"wonend","score":3720.922674751458},{"term":"nam","score":3717.6808138820315},{"term":"grondslag","score":3683.061387664469},{"term":"c","score":3673.090995271635},{"term":"verzoeker","score":3571.638932677626},{"term":"daarvan","score":3567.6225909370714},{"term":"wrakingsverzoek","score":3541.549030679061},{"term":"is","score":3440.387661234708},{"term":"omvang","score":3366.311981904802},{"term":"legg","score":3329.0480760128844},{"term":"aan","score":3223.9112097026887},{"term":"verded","score":3165.7178872487307},{"term":"cv","score":3042.128953373962},{"term":"expl","score":3040.1697507354784},{"term":"onder","score":2974.8053438673605},{"term":"betrek","score":2970.193085805556},{"term":"omschrijv","score":2957.203054967436},{"term":"gevestigd","score":2943.512769029707},{"term":"subsidiair","score":2909.8621546381974},{"term":"na","score":2901.336084122622},{"term":"tenuitvoerlegg","score":2896.3042311756017},{"term":"woonplat","score":2833.360414159723},{"term":"dat","score":2820.4667119671444},{"term":"instantie","score":2788.178718727725},{"term":"kantonrechter","score":2708.708425866546},{"term":"dor","score":2674.2534345203103},{"term":"nk","score":2644.4288614199518},{"term":"beslot","score":2594.0055533089735},{"term":"heeft","score":2585.953525644552},{"term":"eis","score":2578.738104437394},{"term":"luidt","score":2553.12681109995},{"term":"alsmed","score":2550.435332623233},{"term":"primair","score":2528.0815655096303},{"term":"stuk","score":2527.537656736395},{"term":"als","score":2521.283674039854},{"term":"tuss","score":2520.186759138424},{"term":"ap","score":2496.232541867445},{"term":"“","score":2441.7957884967022},{"term":"stichting","score":2410.767689829296},{"term":"ij","score":2394.1576667993104},{"term":"terbeschikkinggesteld","score":2387.0734176402975},{"term":"aanslag","score":2386.2247256341752},{"term":"bewijsvrag","score":2360.862191553694},{"term":"n","score":2334.655442088123},{"term":"person","score":2323.1197057322647},{"term":"cassatieberoep","score":2319.71580871497},{"term":"gelderland","score":2306.6486909328546},{"term":"vestigingsplat","score":2292.2788449756167},{"term":"bestred","score":2277.3010645012373},{"term":"zaaknummer","score":2268.8612644956634},{"term":"ingekom","score":2246.0574800091727},{"term":"bij","score":2240.8231145121954},{"term":"griffierecht","score":2229.43808238686},{"term":"overijssel","score":2196.0207518717125},{"term":"inbeslaggenom","score":2187.4770550255007},{"term":"zijn","score":2165.1647663759127},{"term":"verleng","score":2133.062319255556},{"term":"behandel","score":2130.527223738586},{"term":"over","score":2082.0138310383336},{"term":"bewijsmotiver","score":2079.7717413985188},{"term":"nader","score":2064.8293751468455},{"term":"t","score":2057.9619814038474},{"term":"voorgesteld","score":2048.983522520914},{"term":"uitgangspunt","score":2041.9237248327188},{"term":"plat","score":2012.9336899709556},{"term":"derd","score":1996.7567921198088},{"term":"teg","score":1967.324515410973},{"term":"schad","score":1926.8441904463552},{"term":"verweerder","score":1920.7356675293488},{"term":"gemeent","score":1920.6925684857933},{"term":"eab","score":1903.141040758483},{"term":"eiseres","score":1901.1867534311964},{"term":"veroordel","score":1879.9760513373865},{"term":"rolnr","score":1867.9807166808514},{"term":"parketnummer","score":1865.8387854374225},{"term":"olw","score":1855.5875215325973},{"term":"ander","score":1847.2427167688334},{"term":"o","score":1832.4200426625455},{"term":"terbeschikkingstell","score":1832.1131822014731},{"term":"benadeelde_NUM","score":1819.4234511237748},{"term":"juni","score":1813.2329361982352},{"term":"april","score":1807.893785195503},{"term":"schadevergoed","score":1791.8692964830223},{"term":"dur","score":1779.2204367750915},{"term":"aansprak","score":1775.4545696885104},{"term":"gepleegd","score":1773.5163007915373},{"term":"identiteit","score":1772.5264790682143},{"term":"opg","score":1771.3899432814508},{"term":"rechter","score":1769.9626345359134},{"term":"verwijz","score":1762.6379560837859},{"term":"juli","score":1761.0719895496948},{"term":"),","score":1729.7835426409488},{"term":"verklar","score":1729.4075860307767},{"term":"recht","score":1725.9733200529502},{"term":"beperkt","score":1718.7241440457565},{"term":"qh","score":1718.1032905663174},{"term":"herzien","score":1710.1327201958204},{"term":"goeder","score":1683.8538959240004},{"term":"betrok","score":1681.7147293915848},{"term":"wet","score":1680.3940694717726},{"term":"klager","score":1673.2364660280082},{"term":"lid","score":1672.2825305388537},{"term":"ist","score":1671.2278127188724},{"term":"die","score":1640.4600898856775},{"term":"veren","score":1624.6236404515444},{"term":"last","score":1612.7279897024498},{"term":"gelegd","score":1600.3869901105836},{"term":"bijlag","score":1590.173332108258},{"term":"tm","score":1589.185265926683},{"term":"ambtshalv","score":1578.5044301925495},{"term":"le","score":1546.2858116431446},{"term":"januari","score":1543.5880300057163},{"term":"meermal","score":1543.4105497169303},{"term":"voorwerp","score":1523.3105588955923},{"term":"niet","score":1507.82310528044},{"term":"verzoekschrift","score":1504.2662494500987},{"term":"maart","score":1502.5584038586903},{"term":"rolnummer","score":1492.5627625241566},{"term":"omstand","score":1489.6055422913869},{"term":"mishandel","score":1467.859675392109},{"term":"cvg","score":1466.832388913299},{"term":"september","score":1462.525859295831},{"term":"stat","score":1460.0320363643648},{"term":"€","score":1459.5059146166043},{"term":"poging","score":1453.0913193919928},{"term":"hechtenis","score":1449.360795900635},{"term":"f","score":1449.044888188322},{"term":"mondel","score":1425.9052377143755},{"term":"notaris","score":1407.749272052673},{"term":"voorgeschiedenis","score":1399.3324315721618},{"term":"oktober","score":1390.8633820949485},{"term":"mei","score":1390.7568542084607},{"term":"hof","score":1388.611853116443},{"term":"mr","score":1386.968950624051},{"term":"mer","score":1382.336583562004},{"term":"kg","score":1380.5413898733332},{"term":"man","score":1369.8042932388487},{"term":"december","score":1348.0670460252231},{"term":"mk","score":1345.4222381990767},{"term":"hij","score":1343.0443169314335},{"term":"gevangenisstraf","score":1341.2581550001967},{"term":"misdrijf","score":1305.0102735936412},{"term":"afdel","score":1295.3349564436655},{"term":"appel","score":1293.612790932979},{"term":"dag","score":1292.8954843945407},{"term":"bewijsvoer","score":1292.6955895590802},{"term":"november","score":1289.7871718962772},{"term":"veroordeeld","score":1285.2679887237048},{"term":"medepleg","score":1266.8506544441093},{"term":"kort","score":1259.100845443319},{"term":"wederrecht","score":1255.927510449973},{"term":"?","score":1250.5263308202143},{"term":"bedoeld","score":1245.1817471689258},{"term":"algemen","score":1216.0599447510867},{"term":"] (","score":1209.7288099275604},{"term":"enkelvoud","score":1206.2561212565026},{"term":"zitting","score":1197.8687678412261},{"term":"beschouw","score":1195.1796540002676},{"term":"holland","score":1174.2965029012043},{"term":"verzoekster","score":1173.3746257318464},{"term":"althan","score":1163.7974305632813},{"term":"februari","score":1163.2221030169242},{"term":"hierna","score":1162.6144481708227},{"term":"m","score":1160.6286352469706},{"term":"voorlop","score":1152.9354373890399},{"term":"vrijwaringszak","score":1142.6858705493323},{"term":"aanvrag","score":1137.0996131023473},{"term":"nar","score":1129.3410009116872},{"term":"zakelijk","score":1122.5397188054853},{"term":"dez","score":1118.2597966131818},{"term":"augustus","score":1117.0690210280136},{"term":"omstrek","score":1114.9057955194942},{"term":"um","score":1106.0270108142554},{"term":"om","score":1097.1185135222324},{"term":"vader","score":1097.0035827943027},{"term":"opzet","score":1095.3843200387223},{"term":"hebb","score":1092.1226665332822},{"term":"word","score":1090.6596388399623},{"term":"bijzonder","score":1083.6300210197626},{"term":"kwalificaties","score":1081.0230034995752},{"term":"vrouw","score":1072.492149373527},{"term":"doodslag","score":1065.5474881173645},{"term":"weergegev","score":1064.7392504158981},{"term":"proces","score":1060.7886447613039},{"term":"moeder","score":1060.7430447614795},{"term":"geval","score":1043.6919825153766},{"term":"amsterdam","score":1043.4846929439684},{"term":"handel","score":1039.7219751941002},{"term":"advocat","score":1030.705008986981},{"term":"bedreig","score":1021.0161219856661},{"term":"vierd","score":1017.626658092702},{"term":"the","score":1007.9636768281987},{"term":"haza","score":996.728533159012},{"term":"[…],","score":993.584263214181},{"term":"inzak","score":992.9807437978378},{"term":"beid","score":992.4676499017954},{"term":"rad","score":990.1332577373364},{"term":"general","score":988.3618506556724},{"term":"bewijsbesliss","score":987.9415840555649},{"term":"’","score":984.858794034348},{"term":"dagvaard","score":983.1980130999727},{"term":"strafrecht","score":974.8382511295627},{"term":"verzet","score":973.6893232100468},{"term":"elk","score":964.8031060946076},{"term":"getuig","score":964.0152788949001},{"term":"der","score":963.9039506529509},{"term":"wetsbepal","score":955.4646917898447},{"term":"processtuk","score":955.2612385828272},{"term":"reactie","score":955.1709679738574},{"term":"grief","score":943.0748840228412},{"term":"rechtdoend","score":940.1737664661623},{"term":"diefstal","score":937.6373104572249},{"term":"daarteg","score":928.7921083650684},{"term":"firma","score":927.8666153175226},{"term":"vrag","score":925.8195279216854},{"term":"mededader","score":920.4608561648025},{"term":"tussenarrest","score":909.2360404511369},{"term":"weigeringsgrond","score":905.8168983340948},{"term":"beklag","score":904.2223661626373},{"term":"accountantskamer","score":898.4091982781539},{"term":"uit","score":894.5683778024135},{"term":"limburg","score":893.3537857646472},{"term":"eh","score":890.4569272346322},{"term":"belastingdienst","score":887.1619466489683},{"term":"klaagster","score":881.0695754018958},{"term":"h","score":875.5175096634146},{"term":"dan","score":863.3578108647145},{"term":"voordel","score":856.9840162989066},{"term":"zwar","score":855.371476792795},{"term":"vaststell","score":854.5874769996149},{"term":"inricht","score":848.1420996303848},{"term":"relevant","score":842.2766905114867},{"term":"verklaart","score":839.3021964548965},{"term":"veroordeelt","score":836.8193791582003},{"term":"geldig","score":836.5759900943393},{"term":"enig","score":835.8846348747807},{"term":"bewijsverwer","score":832.8504652424973},{"term":"ex","score":830.9242227215549},{"term":"verbal","score":830.1144032435243},{"term":"maand","score":828.1956956807063},{"term":"-/","score":825.9199649211013},{"term":"geweld","score":823.1011401819491},{"term":"verkreg","score":820.5006789908276},{"term":"gen","score":819.66409507469},{"term":"wijzig","score":817.5952019458235},{"term":"zich","score":816.6232991520217},{"term":"jar","score":814.4768558800248},{"term":"inspecteur","score":808.1580510959667},{"term":"gevoerd","score":801.7912444307065},{"term":"tussenkomst","score":791.8944091309063},{"term":"prejudici","score":789.7382137145994},{"term":"naamloz","score":786.3363821339237},{"term":"lev","score":781.7829586148426},{"term":"noem","score":781.3771997252569},{"term":"gericht","score":778.6597464863314},{"term":"jl","score":778.1472547837599},{"term":"rb","score":777.9124685388327},{"term":"regelgev","score":775.9916287342528},{"term":"acht","score":773.7173519821865},{"term":"besluit","score":773.0488041622958},{"term":"gerechtsdeurwaarder","score":770.5250981538928},{"term":"vervolg","score":770.4356034226237},{"term":"minderjar","score":769.437226528863},{"term":"&","score":763.6937224388913},{"term":"bevoegd","score":760.8141469851287},{"term":"strijd","score":756.8741637095698},{"term":"tekst","score":747.6431422925235},{"term":"bedrijf","score":741.43151958153},{"term":"\"","score":738.7530741243386},{"term":"p","score":733.3297424353984},{"term":"geschill","score":730.5664419971836},{"term":"pleg","score":721.5294386546618},{"term":"period","score":721.0773952123203},{"term":"vijfd","score":712.433865071095},{"term":"jeugdzorg","score":708.6750294040154},{"term":"genom","score":708.5364072913942},{"term":"medeverdacht","score":702.5203993766967},{"term":"tezam","score":701.8385238944029},{"term":"kader","score":699.8965326452535},{"term":"international","score":699.3922379475102},{"term":"adres","score":698.2354809686284},{"term":"raadkamer","score":689.9114087286275},{"term":"maatschap","score":678.9684699734187},{"term":"navorderingsaanslag","score":674.2010071865097},{"term":"oogmerk","score":667.2642974151355},{"term":"hetgen","score":663.8096932518783},{"term":"literatur","score":663.1011644115395},{"term":"stelt","score":655.8713041006193},{"term":"eiser_NUM","score":655.1642822644808},{"term":"omtrent","score":654.5647053053838},{"term":"naheffingsaanslag","score":653.48493653467},{"term":"betal","score":653.4849353958012},{"term":"woning","score":650.8270995027758},{"term":"j","score":644.0394166467075},{"term":"mid","score":640.2449747505261},{"term":"nummer","score":639.6060080013352},{"term":"wordt","score":637.5136487705133},{"term":"leeuward","score":637.2113527450342},{"term":"familiekamer","score":636.7620993253548},{"term":"rechtsperson","score":636.5037799152002},{"term":"gemachtigd","score":633.3548137977782},{"term":"redengev","score":630.2149155399993},{"term":"overeenkomst","score":628.9494529166499},{"term":"geintimeerd","score":624.5635019697927},{"term":"])","score":624.3444042291843},{"term":"ondertoezichtstell","score":623.1744206287862},{"term":"tenlastegelegd","score":623.095986002303},{"term":"volgend","score":621.8700662514041},{"term":"gevraagd","score":621.0504995394901},{"term":"uitvoer","score":618.0767975253199},{"term":"aanduid","score":616.3502842479105},{"term":"l","score":614.1090553959664},{"term":"munitie","score":614.0960761207887},{"term":"aanhecht","score":612.3468894362906},{"term":"bv","score":609.4153593877396},{"term":"arnhem","score":609.0418421084872},{"term":"gesteld","score":608.2029074651546},{"term":"rotterdam","score":606.3231811585703},{"term":"'","score":605.830855067},{"term":"dit","score":604.9508819276857},{"term":"curator","score":600.0613396444294},{"term":"bureau","score":598.4523453093136},{"term":"voeging","score":598.2515251222322},{"term":"har","score":596.4270181611395},{"term":"goed","score":587.3564253045378},{"term":"kan","score":586.1808740673662},{"term":"kinderontvoer","score":585.940106944472},{"term":"allen","score":582.8328454448135},{"term":"belang","score":582.4847412921349},{"term":"sancties","score":581.3312667381889},{"term":"overzicht","score":576.6449214372235},{"term":"g","score":574.56522590852},{"term":"machtig","score":573.5533997993438},{"term":"jurisprudentie","score":570.8411614637524},{"term":"gravenhag","score":570.5221096201791},{"term":"deelgeschil","score":569.1534331665874},{"term":"ad","score":568.7585562663747},{"term":"achtergrond","score":561.504294808184},{"term":"staatssecretaris","score":560.7048543477902},{"term":"draagkracht","score":559.4363692429104},{"term":"witwass","score":554.955538189179},{"term":"bedrag","score":554.2795222998475},{"term":"wapen","score":553.4897557872595},{"term":"cision","score":552.7337487577265},{"term":"beher","score":551.8292547447008},{"term":"schorsing","score":551.0138595796418},{"term":"namen","score":550.4604374402452},{"term":"], [","score":548.9279401635304},{"term":"all","score":546.9157683931878},{"term":"rk","score":544.9864630165564},{"term":"advies","score":541.4493793624067},{"term":"hog","score":541.3539391143588},{"term":"telken","score":539.8809442437997},{"term":"raadsman","score":538.9765051803399},{"term":"onttrek","score":537.5771071149743},{"term":"aangevoerd","score":537.2080700573915},{"term":"aangever","score":536.6643690183207},{"term":"gedaagde_NUM","score":536.56075253647},{"term":"heffingsambtenar","score":535.727360187525},{"term":"was","score":530.428044317723},{"term":"herroep","score":528.8903258073703},{"term":"economisch","score":528.8205353259981},{"term":"parti","score":521.9520356955178},{"term":"draagkrachtbereken","score":520.3256573758237},{"term":"art","score":519.1894039839929},{"term":"burgemeester","score":517.4074681242479},{"term":"verker","score":513.3714393409896},{"term":"garantie","score":513.2664594963659},{"term":"zelfstand","score":512.5058141843222},{"term":"ingesteld","score":511.4372032533813},{"term":"‘","score":509.36958841190216},{"term":"uithuisplaats","score":508.88827371118725},{"term":"kwijtscheldingsbesluit","score":508.5403570041028},{"term":"immateri","score":505.79870888610156},{"term":"ciz","score":504.38731035229875},{"term":"verenigd","score":500.43707777436686},{"term":"wetboek","score":496.71190465121293},{"term":"zij","score":496.317170072535},{"term":"af","score":494.99425246579864},{"term":"zover","score":490.06319531563946},{"term":"overweegt","score":489.0017559048459},{"term":"binn","score":488.9353496322516},{"term":"twee","score":488.40097305160674},{"term":"verweerster","score":486.8639700662093},{"term":"–","score":486.79226932218194},{"term":"boetebeschik","score":486.6381008891518},{"term":"veilig","score":485.1675220985274},{"term":"voorzieningenrechter","score":483.7391521745307},{"term":"moet","score":480.33043242895036},{"term":"verbalisant","score":480.0239056277844},{"term":"wek","score":477.69836542575115},{"term":"verzocht","score":474.3518495152219},{"term":"vrijwar","score":474.0806827067163},{"term":"wijst","score":473.5376354099457},{"term":"verwijzingsopdracht","score":472.1143137795946},{"term":"nw","score":471.9671970171254},{"term":"co","score":469.6137295060621},{"term":"zal","score":467.9193198730231},{"term":"_NUMa","score":464.7465217753608},{"term":"js","score":464.4643116708004},{"term":"holding","score":462.86029773435325},{"term":"belast","score":460.77918229675424},{"term":"rabobank","score":452.9881004910555},{"term":"aangift","score":451.98819861686445},{"term":"volgt","score":451.6042200246151},{"term":"u","score":449.9903645618793},{"term":"terwijl","score":449.1692491216147},{"term":"bank","score":448.67445803490375},{"term":"deskund","score":447.90412888591055},{"term":"des","score":447.24459917252307},{"term":"civiel","score":445.09694586401696},{"term":"voornoemd","score":443.3643528316475},{"term":"weg","score":440.4656765806722},{"term":"wetgev","score":438.3737631786592},{"term":"toe","score":437.2731329866049},{"term":"overtred","score":436.65759736716745},{"term":"terecht","score":436.6024584992876},{"term":"hertogenbosch","score":436.426710930923},{"term":"cak","score":434.47793535766317},{"term":"aanvull","score":432.3331690438589},{"term":"er","score":431.9750775788164},{"term":"behoeft","score":428.30815401483636},{"term":"teruggav","score":428.0803967039666},{"term":"voorzien","score":428.0363140877356},{"term":"welk","score":423.9770209852938},{"term":"ook","score":420.50325389192324},{"term":"(…)","score":419.07785622805136},{"term":"wat","score":416.31133001047175},{"term":"colleg","score":414.59988998606514},{"term":"ee","score":414.06576448373175},{"term":"rechtsoverweg","score":414.00299127403093},{"term":"hun","score":412.46281988171586},{"term":",-","score":411.51298173689526},{"term":"partneralimentatie","score":410.96465871167266},{"term":"rh","score":408.3304973467979},{"term":"proceskostenveroordel","score":403.18032802585327},{"term":"wethouder","score":399.57463125963204},{"term":"stelling","score":398.1650893091281},{"term":"gang","score":396.3730092092584},{"term":"wegenverkeerswet","score":395.6215637183312},{"term":"vof","score":394.2092039677971},{"term":"vergoed","score":393.0427832988785},{"term":"toebehor","score":392.77019133749394},{"term":"wel","score":392.3222748867563},{"term":"samenstell","score":391.8786177248503},{"term":"daarvor","score":391.62718175033757},{"term":"kinderalimentatie","score":389.69479757601647},{"term":"y","score":389.3653388689354},{"term":"began","score":388.991021171516},{"term":"verander","score":388.92561003134864},{"term":"aanleid","score":386.4772864583132},{"term":"kantoorhoud","score":385.2000649904942},{"term":"wetsgeschiedenis","score":384.25014945703737},{"term":"hiervor","score":381.7363481795622},{"term":"afgelegd","score":381.15188221640244},{"term":"\\","score":379.73373924575407},{"term":"opheff","score":379.35866062609165},{"term":"aftrek","score":379.0570842434302},{"term":"zoal","score":377.4218905327669},{"term":"q","score":377.2407764084681},{"term":"ontbind","score":375.80359737935095},{"term":"put","score":375.7899251908055},{"term":"eiswijz","score":374.17524770923626},{"term":"overheidsweg","score":373.15817072184785},{"term":"schatting","score":366.42016174498247},{"term":"per","score":366.2447547237079},{"term":"and","score":365.98991131253064},{"term":"duitsland","score":365.81062225108974},{"term":"vermeerder","score":365.31673770175325},{"term":"werking","score":364.9057405615326},{"term":"verpleg","score":363.88669310443345},{"term":"milieubeher","score":362.6560310881297},{"term":"wraking","score":362.13504709263583},{"term":"ind","score":361.43494997957},{"term":"del","score":360.1103439118049},{"term":"r","score":360.0719796216026},{"term":"waard","score":359.55170246368624},{"term":"hor","score":359.49207933501873},{"term":"vernietigt","score":359.3668683819638},{"term":"oplicht","score":357.25856552484106},{"term":"nem","score":356.33122138731727},{"term":"verbeurdverklar","score":355.6378946406262},{"term":"aangeefster","score":353.79699454205297},{"term":"gegev","score":351.83498164403386},{"term":"uren","score":351.73256787563713},{"term":"(ee","score":350.8067189480214},{"term":"gehel","score":350.4128511412351},{"term":"opmerk","score":349.656209240452},{"term":"bevestigt","score":349.5194432461981},{"term":"organisatie","score":347.4907240797172},{"term":"toegang","score":347.3515709917515},{"term":"burger","score":345.6601054164388},{"term":"nieuw","score":343.9620636919424},{"term":"verorden","score":343.9598572123816},{"term":"beantwoord","score":342.8304222846564},{"term":"kantor","score":342.6890567734591},{"term":"weggenom","score":341.51557136534774},{"term":"hoofdelijk","score":341.1872983597647},{"term":"moord","score":340.8686745160941},{"term":"geschrift","score":340.3500469392919},{"term":"ingangsdatum","score":340.0393407769185},{"term":"tijdstipp","score":338.27233215451986},{"term":"eigen","score":337.3612484126243},{"term":"licham","score":336.3889365354889},{"term":"”","score":335.7396626948242},{"term":"on","score":335.70842531902224},{"term":"gewijzigd","score":335.6042151322383},{"term":"euro","score":334.0500229751869},{"term":"uitspraakdatum","score":333.77358862140517},{"term":"termijn","score":333.56038658523624},{"term":"rv","score":332.87957455979887},{"term":"buitenland","score":332.4879666810868},{"term":"middelfaalt","score":331.9926800919101},{"term":"afwijz","score":330.69806524611704},{"term":"auto","score":327.88053870144364},{"term":"omdat","score":327.700071310331},{"term":"valsheid","score":326.86754696977334},{"term":"voorhen","score":325.6047560366894},{"term":"a_NUM","score":325.12314228237204},{"term":"vergunn","score":324.3234746413713},{"term":"betreft","score":324.2760622210038},{"term":"samenlop","score":323.38246597832807},{"term":"werknemer","score":322.3332255714487},{"term":"verdel","score":322.09397405525425},{"term":"percel","score":321.30167101591854},{"term":"verplicht","score":320.5426478182347},{"term":"jeugdbescherm","score":320.4321440465896},{"term":"deprocedur","score":320.0006340312385},{"term":"arbeidsgehandicapt","score":317.2781727426363},{"term":"opnieuw","score":315.7096937172872},{"term":"oud","score":315.00674691847047},{"term":"vreemd","score":314.6616738977354},{"term":"k","score":314.65531585644305},{"term":"bewijsuitsluit","score":313.60480310532546},{"term":"tegenverzoek","score":313.56550007953814},{"term":"meerder","score":310.8242914609809},{"term":"klimmenderstrat","score":309.02525362414156},{"term":"verzend","score":308.55321694181544},{"term":"afpers","score":308.4649799447142},{"term":"onschuldverwer","score":307.6837145950422},{"term":"voorhand","score":306.4797617581732},{"term":"invrijheidstell","score":306.1068770427232},{"term":"verschaft","score":305.70491072582877},{"term":"mogelijk","score":305.67462975680684},{"term":"ik","score":305.63530768139447},{"term":"bestuursrechtsprak","score":305.25944429044125},{"term":"notariss","score":303.7832615892038},{"term":"rent","score":303.4023313797486},{"term":"blijkt","score":302.9868309858422},{"term":"eur","score":302.568159665248},{"term":"gedrag","score":302.12191014708276},{"term":"onderdel","score":301.984974455556},{"term":"be","score":301.4479473896482},{"term":"rechtmat","score":299.42044320411077},{"term":"gebracht","score":297.8310213656799},{"term":"minister","score":296.9774473914194},{"term":"arbeidsovereenkomst","score":296.38178264611946},{"term":"ongegrond","score":296.3401803835656},{"term":"redelijk","score":296.1912450572859},{"term":"waarbij","score":295.7271620413624},{"term":"reclasser","score":295.6580737873853},{"term":"brak","score":295.31178462214325},{"term":"zaaksdossier","score":293.85249244346414},{"term":"([","score":293.6390858877584},{"term":"voorgenom","score":293.28691472893337},{"term":"had","score":292.762539095872},{"term":"aangevall","score":292.57320660325524},{"term":"verzeker","score":291.39016533712004},{"term":"categorie","score":289.76617569049614},{"term":"[…]","score":289.20746184291426},{"term":"services","score":288.1981237531918},{"term":"vast","score":286.56707633646704},{"term":"eendad","score":284.0138179916976},{"term":"voorbedacht","score":283.465308532508},{"term":"zijnd","score":282.17920031147264},{"term":"vanaf","score":281.41409572060644},{"term":"voorrad","score":280.34846313703594},{"term":"toelicht","score":278.6734605190009},{"term":"bw","score":278.10528544725764},{"term":"opiumwet","score":277.26486651647207},{"term":"gelast","score":277.0790685705858},{"term":"sv","score":276.861070703499},{"term":"daarop","score":276.6029028224582},{"term":"vv","score":276.053172949508},{"term":"letsel","score":275.4531962607952},{"term":"kpn","score":275.30258722246845},{"term":"]),","score":275.29601287591123},{"term":"situatie","score":274.7833438966826},{"term":"geldboet","score":274.0799400825197},{"term":"west","score":273.31614393876407},{"term":"eig","score":273.26894777540105},{"term":"procedures","score":273.158743133574},{"term":"kentek","score":273.1452803607124},{"term":"schending","score":272.55154579193777},{"term":"betreff","score":272.11999037685337},{"term":"central","score":271.48471027732495},{"term":"ingedi","score":271.1180678594969},{"term":"jeugddetentie","score":271.09806796941325},{"term":"voort","score":270.849116387582},{"term":"fiscal","score":270.55360052445747},{"term":"schadeverzeker","score":270.3907186129922},{"term":"or","score":269.81558888716853},{"term":"vermeld","score":268.71350672260144},{"term":"sprak","score":268.64975522772767},{"term":"mensenhandel","score":267.6302026606619},{"term":"voortgezet","score":266.1670882993762},{"term":"dictum","score":265.30591133587785},{"term":"nog","score":263.3827291034943},{"term":"vastgesteld","score":262.71911864128805},{"term":"wia","score":262.54647880551545},{"term":"utrecht","score":261.462452408857},{"term":"zesd","score":260.3721000567441},{"term":"w","score":260.29770527061595},{"term":"juridisch","score":258.61296447516486},{"term":"partij_NUM","score":258.5750445224908},{"term":"mak","score":257.9826495910346},{"term":"dienst","score":257.171755198807},{"term":"doel","score":257.05237078596923},{"term":"vooraf","score":256.8778852231326},{"term":"doet","score":256.24769355491935},{"term":"politie","score":255.57708832515547},{"term":"verband","score":255.20167709417507},{"term":"reken","score":254.61830787933474},{"term":", (","score":254.36091350080702},{"term":"b_NUM","score":253.8664566230359},{"term":"antwoord","score":253.83497359224538},{"term":"friesland","score":253.10596996447498},{"term":"gevangennem","score":252.94376741820312},{"term":"opgelegd","score":252.57035427400982},{"term":"verleend","score":252.2043304373499},{"term":"project","score":251.21045951308494},{"term":"] [","score":249.5353225449673},{"term":"krachten","score":247.23346376080553},{"term":"werd","score":246.74334579280114},{"term":"dient","score":246.716890511005},{"term":"bevel","score":246.48174845816067},{"term":"afschrift","score":245.82476103188907},{"term":"immer","score":245.44273943872943},{"term":"wrakingskamer","score":244.6565619468934},{"term":"motorrijtu","score":244.50028169036887},{"term":"articl","score":243.83408175605106},{"term":"kandidat","score":243.34416283206284},{"term":"voldoen","score":243.02816707725705},{"term":"regel","score":241.57525908604512},{"term":"proeftijd","score":241.30569959917835},{"term":"zeeland","score":240.79047530554507},{"term":"ongeval","score":240.38836220431676},{"term":"red","score":239.93760770705362},{"term":"geld","score":239.8174099193531},{"term":"inklimm","score":239.73500279759395},{"term":"ag","score":239.24517237462902},{"term":"verslag","score":239.22319981318776},{"term":"bereken","score":238.91598833830082},{"term":"procureur","score":237.63602588407443},{"term":"haagland","score":237.61077218045267},{"term":"strafvorder","score":236.59600404448386},{"term":"werkzam","score":236.15859179721045},{"term":"genoemd","score":235.80217181774768},{"term":"hoevel","score":235.3704923520559},{"term":"pleegouder","score":235.2391128156216},{"term":"al","score":235.1393179997924},{"term":"verbod","score":234.48653796945254},{"term":"producties","score":234.30732866422414},{"term":"gevolg","score":233.76140268890535},{"term":"nietig","score":233.61674238244055},{"term":"to","score":233.44355779051222},{"term":"kbvg","score":233.41025328361582},{"term":"war","score":232.8311747640093},{"term":"delict","score":232.54803635479777},{"term":"kunn","score":231.36418176788916},{"term":"financi","score":229.5602596330669},{"term":"gwd","score":229.34214737994125},{"term":"commissaris","score":228.7971993783246},{"term":"recapitulatie","score":228.54016296921534},{"term":"her","score":228.25973084932866},{"term":"schuld","score":227.99270563034023},{"term":"awb","score":227.69163403514793},{"term":"gedan","score":227.52891062882233},{"term":"vergunningenbesluit","score":227.4619964207561},{"term":"behor","score":227.44566648679358},{"term":"schip","score":227.1721980559512},{"term":"rechthebb","score":227.0397859219548},{"term":"bepaald","score":226.880302385988},{"term":"gebor","score":226.416398041589},{"term":"aanwez","score":226.10340175845667},{"term":"limited","score":226.071548997078},{"term":"slachtoffer_NUM","score":225.35747109713986},{"term":"evrm","score":224.68660665383638},{"term":"samenvat","score":224.2757811108711},{"term":"zonder","score":224.10066457019275},{"term":"teven","score":224.07115565755666},{"term":"gn","score":223.74044162909135},{"term":"verzond","score":223.72745388212746},{"term":"gezag","score":222.40995394608905},{"term":"erfrecht","score":222.13822179914666},{"term":"gevorderd","score":221.834221940096},{"term":"scheiding","score":220.406380311632},{"term":"plaatsing","score":220.0407593456613},{"term":"verbrek","score":219.9894591325123},{"term":"bereik","score":219.91930801194985},{"term":"datum","score":219.90992725929706},{"term":"hem","score":219.02452566576932},{"term":"akt","score":218.90149762670683},{"term":"informatie","score":218.8146865009596},{"term":"eind","score":218.74846390547512},{"term":"statutair","score":218.73690640707738},{"term":"vastgoed","score":218.29389422404327},{"term":"gedeelt","score":218.1990341575608},{"term":"verkracht","score":218.0121248743831},{"term":"beklaagd","score":217.82645949764907},{"term":"peratiev","score":217.2143053154568},{"term":"uur","score":217.156565406894},{"term":"tijd","score":216.83140485776343},{"term":"vervroeg","score":216.41628183370594},{"term":"dossier","score":214.98570587786557},{"term":"gemaakt","score":214.81758548276346},{"term":"company","score":214.19700821545393},{"term":"eisend","score":213.96710079527531},{"term":"gebruik","score":213.96690346619505},{"term":"zes","score":213.93661103479164},{"term":"total","score":213.82951262035485},{"term":"woningsticht","score":213.29128097037272},{"term":"navolg","score":212.85415117987392},{"term":"volled","score":212.77180357738288},{"term":"vrijspraakmotiver","score":212.20028844026942},{"term":"enkel","score":211.84369615007745},{"term":"groning","score":211.739814151717},{"term":"indien","score":211.25794334010064},{"term":"zijd","score":211.21646745104684},{"term":"eigenar","score":211.20040301513606},{"term":"verbeter","score":211.12479640309073},{"term":"verschen","score":210.36692299488402},{"term":"vier","score":209.71774111367813},{"term":"wijz","score":209.6700251762507},{"term":"goednummer","score":209.16474694264915},{"term":"lat","score":209.13794440474905},{"term":"misdrijv","score":209.04319381230852},{"term":"begrot","score":208.57401295442855},{"term":"heropen","score":206.79984991089734},{"term":"personenauto","score":206.6561681600473},{"term":"*","score":206.61342611026834},{"term":"juist","score":206.49332576570882},{"term":"alimentatie","score":206.47906512837943},{"term":"dien","score":206.194847085454},{"term":"taakstraf","score":205.6030059962709},{"term":"gedur","score":205.54138135983013},{"term":"opzethel","score":205.0973119581467},{"term":"tijdstip","score":204.5042104467341},{"term":"kind","score":204.27764123095173},{"term":"verhor","score":204.27412399202888},{"term":"? (","score":204.20859032359647},{"term":"hoogt","score":203.29847645241617},{"term":"bezit","score":203.18064588216174},{"term":"drie","score":203.06872761723204},{"term":"ç","score":202.9445460882297},{"term":"cli","score":202.86279136475594},{"term":"stand","score":202.06639079336568},{"term":"geleg","score":200.95328797016498},{"term":"ondernem","score":200.62951827884135},{"term":"bezwaarschrift","score":200.62568154090363},{"term":"ouder","score":200.50132173609848},{"term":"mevrouw","score":199.58575909870677},{"term":"bestat","score":199.5544312541504},{"term":"doen","score":199.47580261892023},{"term":"geldbedrag","score":199.4342629132285},{"term":"hoedan","score":199.26773026840516},{"term":"bevind","score":199.24264269229963},{"term":"zorg","score":198.78234764689418},{"term":"afdoen","score":198.41409423405685},{"term":"directeur","score":197.90947956572404},{"term":"national","score":197.688481012926},{"term":"verdedigingsbeginsel","score":195.88562728357994},{"term":"zw","score":195.62258135433936},{"term":"mar","score":195.47536444081186},{"term":"indig","score":194.99039588337124},{"term":"aanmerk","score":194.92890125434505},{"term":"nt","score":194.44163767727406},{"term":"waarop","score":194.03679128476594},{"term":"dader","score":193.58541121611182},{"term":"wie","score":193.4897034913813},{"term":"elder","score":193.00824364097858},{"term":"gewraakt","score":192.56899522554514},{"term":"not","score":192.3037396089418},{"term":"bevoegdheidsincident","score":191.35734034807055},{"term":"nu","score":191.32438282344614},{"term":"product","score":190.86764951528784},{"term":"verduister","score":190.73580102917307},{"term":"noodwer","score":190.65216264277512},{"term":"rechtskader","score":190.32464239205092},{"term":"leid","score":190.17527561405308},{"term":"merk","score":190.12760459830926},{"term":"nabestaand","score":189.88460874079718},{"term":"schriftelijk","score":188.78955232836233},{"term":"management","score":188.46886385019414},{"term":"aanhef","score":188.44172601066177},{"term":"kernpunt","score":187.14006537514715},{"term":"wij","score":186.8311812886901},{"term":"schrikker","score":186.09796709799883},{"term":"utr","score":185.33768929754225},{"term":"vatbar","score":185.09732917196973},{"term":"beslist","score":184.86073692635358},{"term":"aw","score":184.85162785455444},{"term":"than","score":184.3963886147676},{"term":"beveelt","score":184.10691527990394},{"term":"boet","score":183.5666573863507},{"term":"inc","score":183.49974978411043},{"term":"tussenvonnis","score":183.25734999823388},{"term":"belgi","score":182.34263773973532},{"term":"uitgat","score":182.19060194821182},{"term":"groep","score":181.7768488329411},{"term":"voorafgaand","score":181.7671207707576},{"term":"gewoont","score":181.75523633108907},{"term":"zou","score":181.35896281086056},{"term":"william","score":181.21649448526568},{"term":"genaamd","score":180.2632696079198},{"term":"salaris","score":180.1994886996175},{"term":"opricht","score":180.02058184006884},{"term":"verricht","score":179.29768813951128},{"term":"eren","score":179.08404671124686},{"term":"belag","score":178.7018522739631},{"term":"bewindvoerder","score":178.58822798659406},{"term":"vuurwap","score":178.46740101493597},{"term":"je","score":178.3541099746552},{"term":"heden","score":178.31012702465196},{"term":"wwb","score":178.29620381368332},{"term":"bestuurder","score":178.0764907975478},{"term":"opzegg","score":177.3364744894269},{"term":"dexia","score":177.19073537498795},{"term":"ieder","score":177.16513861607868},{"term":"bewijsaanbod","score":177.1630616918193},{"term":"erv","score":175.4981296261087},{"term":"schuldig","score":175.43260012915965},{"term":"rechtsbevoegd","score":175.21984516733033},{"term":"bestur","score":175.07040961803511},{"term":"voornam","score":174.7997102751974},{"term":"daarbij","score":174.65009010564782},{"term":"zodan","score":174.33189498311648},{"term":"daartoe","score":173.51264423339452},{"term":"gmbh","score":172.12060113375873},{"term":"zaakdossier","score":171.76178867911798},{"term":"rechtsmacht","score":171.30743765661964},{"term":"geschond","score":171.10513538260378},{"term":"wao","score":170.87192967944185},{"term":"plaatsnam","score":170.69614068424622},{"term":"onrechtmat","score":169.75258962165972},{"term":"rapport","score":169.66707876352342},{"term":"status","score":169.4227251033437},{"term":"zo","score":169.01446093917025},{"term":"rg","score":169.01067132098962},{"term":"voormeld","score":168.95081500884072},{"term":"buitengerecht","score":168.7622182319252},{"term":"heffing","score":168.6686395406917},{"term":"verwijzingsarrest","score":167.94738264695397},{"term":"grot","score":167.85466558943736},{"term":"onderwijs","score":167.40267658343498},{"term":"siod","score":167.29245846154438},{"term":"uw","score":167.17600718194097},{"term":"], (","score":166.7305029892721},{"term":"faillissement","score":166.51413984253213},{"term":"extradition","score":166.04022761661741},{"term":"gehad","score":165.20880659616122},{"term":", [","score":165.1943551951217},{"term":"toegebracht","score":164.319551166621},{"term":"onderhav","score":163.96313947637117},{"term":"rol","score":163.94728326984057},{"term":"commanditair","score":163.28984328239335},{"term":"aard","score":163.1901879637789},{"term":"wederspann","score":162.78210298222308},{"term":"behoev","score":162.75668938621277},{"term":"markt","score":162.72127849105},{"term":"vervang","score":162.55457426279293},{"term":"gezagsuitoefen","score":162.54680332579375},{"term":"ontslag","score":162.52750392298088},{"term":"vervolgen","score":162.25862615571654},{"term":"investment","score":162.0100558561208},{"term":"iemand","score":161.7641931239143},{"term":"abn","score":161.69476720258575},{"term":"geliquideerd","score":161.6877289207231},{"term":"ne","score":161.2006158697049},{"term":"administratie","score":160.96897676267812},{"term":"jong","score":160.82558543558835},{"term":"medisch","score":160.70723803755322},{"term":"bekrachtigt","score":160.30216834608066},{"term":"gevoegd","score":160.14852757890478},{"term":"publiekrecht","score":160.13417826133877},{"term":"geboortedatum","score":160.09192738894203},{"term":"houdend","score":160.0046017663195},{"term":"zuid","score":159.54856529208448},{"term":"onroer","score":159.05591683404413},{"term":"buit","score":158.7619095487356},{"term":"omzetbelast","score":158.75979953957997},{"term":"bestan","score":158.64355007886198},{"term":"europ","score":158.56469881823753},{"term":"gebruikt","score":158.56164994797814},{"term":"dna","score":158.43833167018494},{"term":"verbond","score":158.0336239070846},{"term":"legt","score":157.95889429379372},{"term":"system","score":157.95657875634956},{"term":"dier","score":157.9314768735149},{"term":"cao","score":157.8759853268837},{"term":"materi","score":157.73713316673633},{"term":"med","score":157.57895828545824},{"term":"berov","score":157.4991563042455},{"term":"mes","score":157.39293517595527},{"term":"onderl","score":157.10808681593798},{"term":"adoptie","score":156.98598760764668},{"term":"hennep","score":156.84026794088322},{"term":"lijst","score":156.70857602974843},{"term":"zin","score":156.6541239549455},{"term":"uwv","score":156.2829337599998},{"term":"vermogensmaatregel","score":156.2082842901706},{"term":"gat","score":156.06752963321387},{"term":"resultat","score":154.52693851619966},{"term":"vormverzuim","score":154.19893732988632},{"term":"klaagschrift","score":154.00751304080552},{"term":"uitgegan","score":153.56834417241433},{"term":"ervan","score":153.28761027099497},{"term":"faalt","score":152.95555198967787},{"term":"dochter","score":152.41461195747826},{"term":"toestemm","score":151.90290903883354},{"term":"persoonsnam","score":151.77531814873976},{"term":"echtgenot","score":151.5033117584444},{"term":"uitlever","score":151.45787849941817},{"term":"brand","score":151.44971235152983},{"term":"wetenschap","score":151.3545615212639},{"term":"nr","score":150.83560582853062},{"term":"samengevat","score":150.75431057853763},{"term":"uitsluit","score":150.28883162115446},{"term":"intern","score":150.17933280052188},{"term":"spoedeis","score":149.98034151301277},{"term":"kostenveroordel","score":149.85473914564102},{"term":"interpretatie","score":149.8527173962763},{"term":"won","score":149.38012066086924},{"term":"aanhoud","score":149.14932285649832},{"term":"gemeenschapp","score":148.99300169191537},{"term":"lever","score":148.74770004511785},{"term":"geslot","score":148.04936333691487},{"term":"vereist","score":148.00696540893597},{"term":"kom","score":147.9656734842207},{"term":"indel","score":147.68269616492822},{"term":"agreement","score":147.68070649185464},{"term":"loonbedrijf","score":147.64359758882827},{"term":"waardor","score":147.60582214308286},{"term":"aandel","score":147.38110263806365},{"term":"omgang","score":147.23355948542707},{"term":"amro","score":147.1624671735313},{"term":"rechtstrek","score":147.0962828423155},{"term":"hoe","score":147.09238990046927},{"term":"plaatsgevond","score":147.05224764311382},{"term":"overled","score":146.87231235281783},{"term":"levert","score":146.43137590588273},{"term":"afrond","score":146.3715279119688},{"term":"verschuldigd","score":146.27633229869863},{"term":"dubbel","score":146.22564367615277},{"term":"analys","score":146.12076754192805},{"term":"gevall","score":146.06288486348026},{"term":"ing","score":145.08270777917775},{"term":"ontvankelijkverklar","score":145.04450449653598},{"term":"voortgang","score":144.95079051977262},{"term":"wegen","score":144.7257364538747},{"term":"beker","score":144.72510236801006},{"term":"bp","score":144.66242519059338},{"term":"houdt","score":144.35561098408996},{"term":"dwangsom","score":144.33514442444252},{"term":"eindhov","score":144.164406534394},{"term":"materiaal","score":144.12650813608442},{"term":"deelnem","score":143.91851542778096},{"term":"wettig","score":143.24717958377315},{"term":"fnv","score":143.17017902157568},{"term":"fund","score":142.83573381866654},{"term":"melding","score":142.81592475951288},{"term":"konink","score":142.66403185859792},{"term":"exploitatie","score":142.54160059577623},{"term":"la","score":142.19665736671172},{"term":"nv","score":142.13739803037882},{"term":"aangetroff","score":142.0915872079841},{"term":"snelheid","score":142.02061442881435},{"term":"genoegzam","score":141.8756719200893},{"term":"wil","score":141.7668578506751},{"term":"definities","score":141.73718977732355},{"term":"voorbereid","score":141.65364165835973},{"term":"jeugdreclasser","score":141.58075585575185},{"term":"werk","score":141.3531563797913},{"term":"omstreeksd","score":140.9539260163041},{"term":"verschot","score":140.89857411630166},{"term":"afgift","score":140.37573012605455},{"term":"bouw","score":140.3260566514246},{"term":"pen","score":140.24922508836806},{"term":"haarlem","score":140.22484670660842},{"term":"inzag","score":139.9158136494275},{"term":"insuranc","score":139.71417773375492},{"term":"strekkend","score":139.7120748696489},{"term":"rechtsvervolg","score":139.53126710290388},{"term":"strat","score":139.52043212978285},{"term":"gebezigd","score":139.49192279902883},{"term":"drenth","score":139.20991845592926},{"term":"partner","score":138.47062094829812},{"term":"sam","score":138.38811379036278},{"term":"toegevoegd","score":138.38431839394906},{"term":"aegon","score":138.38329676249418},{"term":"techniek","score":138.1190076336295},{"term":"uiteenzet","score":138.0073723918815},{"term":"vennot","score":137.8794509726703},{"term":"zevend","score":137.86844328067008},{"term":"bijkom","score":137.40838531425655},{"term":"reikwijdt","score":137.39221280595137},{"term":"zorgregel","score":137.38682584234527},{"term":"rijdend","score":137.07764645300105},{"term":"geschilpunt","score":137.00396209430443},{"term":"overleg","score":136.96350695260494},{"term":"kinder","score":136.83419854512613},{"term":"voorzitter","score":136.7256778567649},{"term":"afdreig","score":136.68817562152458},{"term":"uitleg","score":136.560563695502},{"term":"cura","score":136.4170649958269},{"term":"zorgverzekerar","score":136.41355244217732},{"term":"amp","score":136.29708960331186},{"term":"ao","score":135.86199670947377},{"term":"group","score":135.76983797171877},{"term":"hetzij","score":135.70747519648566},{"term":"val","score":135.63507125290928},{"term":"toegewez","score":135.41871165228244},{"term":"sierad","score":135.3973259911527},{"term":"dirk","score":135.35980658649243},{"term":"zon","score":135.245601169591},{"term":"dadelijk","score":134.56555332362194},{"term":"gewez","score":134.3853721802475},{"term":"_NUMb","score":134.35170993785357},{"term":"mijn","score":134.35142390711164},{"term":"dod","score":133.85306302211637},{"term":"hand","score":133.54217119762876},{"term":"beginsel","score":133.17138222690005},{"term":"middeltreft","score":133.09685803971323},{"term":"gerecht","score":132.89159493037857},{"term":"specifiek","score":132.59421748570307},{"term":"afhank","score":132.43672355916914},{"term":"opgenom","score":132.2776334611107},{"term":"hkov","score":132.17546165945632},{"term":"informatiebeschik","score":131.57977317963093},{"term":"ontbond","score":131.38866720637398},{"term":"pensioenfond","score":131.36428680688476},{"term":"reacties","score":131.35925940511368},{"term":"jeugdig","score":131.1954970865302},{"term":"transport","score":131.19216759620747},{"term":"werkgever","score":130.82559204682622},{"term":"amsterdamkort","score":130.7355796595058},{"term":"social","score":130.66548583331897},{"term":"stell","score":130.58043975294865},{"term":"belastingkamer","score":130.15844574114976},{"term":"kracht","score":130.04863033381724},{"term":"fout","score":130.0003842850746},{"term":"verwet","score":129.94939960105594},{"term":"peratie","score":129.74557377867245},{"term":"aangezien","score":129.5099883185934},{"term":"komt","score":129.4596976514551},{"term":"cassatieklacht","score":129.44926385650592},{"term":"binnendr","score":129.2385597247121},{"term":"dreigend","score":129.193792003014},{"term":"sns","score":129.12018853529307},{"term":"vordert","score":129.08909423869372},{"term":"flevoland","score":128.53202805941564},{"term":"btw","score":128.4745273694756},{"term":"sr","score":128.29318267568448},{"term":"procesdossier","score":127.99340988519107},{"term":"inzet","score":127.92290688141465},{"term":"tijdig","score":127.46577894843524},{"term":"e),","score":127.46249470066356},{"term":"netherland","score":127.01777286785162},{"term":"acm","score":126.88495718514098},{"term":"lon","score":126.74045204608721},{"term":"huwelijk","score":126.00479316764239},{"term":"gev","score":125.8906667549891},{"term":"hebbend","score":125.8149738379777},{"term":"bepaalt","score":125.52919280321885},{"term":"jegen","score":125.12051962188168},{"term":"toetsing","score":124.9671357031068},{"term":"verdrag","score":124.83157316454287},{"term":"è","score":124.65745330977256},{"term":"by","score":124.59066357966846},{"term":"slecht","score":124.52225061842819},{"term":"geblek","score":124.15805826508652},{"term":"verstrek","score":124.09397762005754},{"term":"beperk","score":123.50447518386834},{"term":"gegeven","score":123.50296711058414},{"term":"e,","score":123.47049181641572},{"term":"gelijkheidsbeginsel","score":122.91273297353992},{"term":"dus","score":122.56710172888207},{"term":"mag","score":122.3517544896658},{"term":"waarborgmaatschappij","score":121.85850096745973},{"term":"cultur","score":121.70673183445307},{"term":"maastricht","score":121.60775020864651},{"term":"houd","score":121.34370062448347},{"term":"gestok","score":121.1568081954643},{"term":"ongecontroleerd","score":120.91572628690628},{"term":"geweest","score":120.89078945811006},{"term":"aantal","score":120.52503286254097},{"term":"behulp","score":120.45281536090859},{"term":"toevoeg","score":120.1136639157532},{"term":"overtu","score":120.00920849845281},{"term":"namelijk","score":119.98025818585099},{"term":"ord","score":119.95367267654116},{"term":"procedurel","score":119.8339131229747},{"term":"ongever","score":119.73277092257158},{"term":"betwist","score":119.6276615877896},{"term":"schriftur","score":119.49080524097866},{"term":"pand","score":119.47868613775017},{"term":"c_NUM","score":119.25665077613002},{"term":"kaliber","score":119.2424508839481},{"term":"doorzoek","score":119.182346484105},{"term":"tennisshop","score":119.15120238278546},{"term":"deur","score":119.04921582253334},{"term":"overschrijd","score":119.01594408985302},{"term":"gevolgd","score":118.93455987236553},{"term":"uitlat","score":118.92285938408233},{"term":"alvoren","score":118.76448248699582},{"term":"nodig","score":118.6662898530172},{"term":"gezicht","score":118.4918665271076},{"term":"eerder","score":118.47664165763715},{"term":"geboorteplat","score":118.25271104048586},{"term":"vrijstell","score":118.18591880775338},{"term":"bereid","score":117.91902207427562},{"term":"huishoud","score":117.77858315486765},{"term":"slager","score":117.53586795483085},{"term":"eerbar","score":117.41831851939284},{"term":"bovenvermeld","score":117.35252786514063},{"term":"kinderbescherm","score":117.16974091379655},{"term":"jan","score":117.01125647837142},{"term":"meest","score":116.93975852273631},{"term":"waarvor","score":116.33021710642687},{"term":"bondsrepubliek","score":116.27293070158792},{"term":"memorie","score":115.97025437529311},{"term":"feitelijkh","score":115.81173833426055},{"term":"achtbunderstrat","score":115.61654036455315},{"term":"vorm","score":115.5397067213353},{"term":"metal","score":115.38262686980406},{"term":"]/ [","score":115.2059889063241},{"term":"))","score":115.1975126263327},{"term":"voorgaand","score":115.17378841874016},{"term":"misbruik","score":115.07001418539018},{"term":"bestemd","score":115.04205971471383},{"term":"bft","score":115.02140669054988},{"term":"vel","score":114.82030529938632},{"term":"zwoll","score":114.62233528846073},{"term":"invrijheidsstell","score":114.59913093363036},{"term":"moment","score":114.58376891638507},{"term":"kinderrechter","score":114.50489446989401},{"term":"re","score":114.45038677291555},{"term":"alkmar","score":114.42714109977693},{"term":"betrouw","score":113.92840764224023},{"term":"hoofdverblijfplat","score":113.83476473247339},{"term":"medeplicht","score":113.76920708245876},{"term":"diver","score":113.66377200244261},{"term":"afwikkel","score":113.30443396201866},{"term":"aanwijz","score":113.06158920705718},{"term":"awr","score":113.03097906684579},{"term":"uitgesprok","score":112.95274661710198},{"term":"tijden","score":112.91053783702742},{"term":"ambtenar","score":112.64583338114696},{"term":"rijswijk","score":112.63933942459725},{"term":"kruithof","score":112.55541656862447},{"term":"autoriteit","score":112.43111278748898},{"term":"rolno","score":112.37281949968784},{"term":"wagner","score":112.33191720700943},{"term":"•","score":112.19377542668812},{"term":"link","score":112.01619569791664},{"term":"vigorflus","score":112.00065937460869},{"term":"gelet","score":111.87282290298964},{"term":"algehel","score":111.86022764668226},{"term":"productie","score":111.84683445376909},{"term":"belanghebbendes","score":111.7503048053013},{"term":"dordrecht","score":111.68981134755778},{"term":"ltd","score":111.58531141285638},{"term":"tabel","score":111.29165891303819},{"term":"estland","score":111.03781215670233},{"term":"vlucht","score":110.98378331082901},{"term":"politierechter","score":110.82931618111733},{"term":"waarnem","score":110.56818113513944},{"term":"dar","score":110.29043244240651},{"term":"it","score":110.04600462512907},{"term":"dwangakkoord","score":109.87490657338228},{"term":"tbs","score":109.78941680033454},{"term":"opnam","score":109.60357455085501},{"term":"ziekenhuis","score":109.46458374591823},{"term":"tarief","score":109.2340975151614},{"term":"criminel","score":109.1082765498355},{"term":"volgen","score":109.05937244978605},{"term":"geldt","score":108.8646726633773},{"term":"echtelijk","score":108.82660750053438},{"term":"eenheid","score":108.78261680463962},{"term":"procedurenummer","score":108.7567314146331},{"term":"gehoud","score":108.71394487996392},{"term":"ingevolg","score":108.71319301678773},{"term":"opposant","score":108.53008616358073},{"term":"vraagstell","score":108.25319272313055},{"term":"%","score":108.09591861137429},{"term":"toelat","score":107.40848750090306},{"term":"vpb","score":107.2844208589062},{"term":"inboedel","score":107.26031843850714},{"term":"raadsvrouw","score":107.25409253118436},{"term":"delta","score":107.1878686138848},{"term":"fotograf","score":107.18407192403086},{"term":"kc","score":106.82764205520827},{"term":"aangewez","score":106.81164774675878},{"term":"vermeerderd","score":106.78076680775514},{"term":"overlijd","score":106.61644873634802},{"term":"motorfiet","score":106.59793793250924},{"term":"zowel","score":106.51685171990785},{"term":"breng","score":106.38185355571015},{"term":"airlines","score":106.3631278495859},{"term":"] ([","score":106.36178600735967},{"term":"aanrand","score":106.17368611067394},{"term":"aangemerkt","score":106.08175083407636},{"term":"webbr","score":106.01650308290363},{"term":"hotel","score":105.97068700292526},{"term":"gebrek","score":105.85929441235879},{"term":"onrecht","score":105.84484419688123},{"term":"waarteg","score":105.71357235415554},{"term":"arbeid","score":105.56049993747644},{"term":"ab","score":105.36358063560881},{"term":"busines","score":105.32160729495646},{"term":"geword","score":105.30562438525267},{"term":"zodat","score":105.24566201697853},{"term":"geopposeerd","score":104.71051668654044},{"term":"oorspronk","score":104.59663530253366},{"term":"bewijsbijlag","score":104.52618547559402},{"term":"hen","score":104.29233073205971},{"term":"!","score":104.2801939534676},{"term":"beslssing","score":104.25730208493728},{"term":"opgemaakt","score":104.21428279346227},{"term":"benoem","score":104.12681349856733},{"term":"verkrijg","score":104.09730695316384},{"term":"ren","score":104.0453110097881},{"term":"bruikbar","score":104.01403087817486},{"term":"testis","score":103.9742425036984},{"term":"property","score":103.79384480128222},{"term":"benelux","score":103.78786943325179},{"term":"verdachtes","score":103.63772305467782},{"term":"ring","score":103.52552358739833},{"term":"instell","score":103.51602554225948},{"term":"direct","score":103.51095825996366},{"term":"telfort","score":103.5012410520619},{"term":"bewar","score":103.3420950820966},{"term":"gerekwestreerd","score":103.33487771999498},{"term":"ja","score":103.23384996637189},{"term":"zorgplicht","score":102.9884059510712},{"term":"nuen","score":102.91619757928095},{"term":"zender","score":102.8534795554361},{"term":"damag","score":102.72933965624509},{"term":"intrek","score":102.64139771704642},{"term":"belastingplicht","score":102.50230281987486},{"term":"ernstig","score":102.49408997390644},{"term":"nemend","score":102.42363736125267},{"term":"strafvervolg","score":102.31837340007502},{"term":"seksueel","score":102.23104946972961},{"term":"via","score":102.13698594184339},{"term":"overen","score":101.76529608816989},{"term":"schaderegelingskantor","score":101.68322862406006},{"term":"ek","score":101.60105849826606},{"term":"toewijs","score":101.5657772066131},{"term":"box","score":101.42634733744359},{"term":"allianz","score":101.36486010303415},{"term":"negend","score":101.33627504307452},{"term":"hierbov","score":101.25468915969999},{"term":"betrapp","score":100.90546717742133},{"term":"personel","score":100.88880975456641},{"term":"/[","score":100.82619693102123},{"term":"inspectie","score":100.71091238554723},{"term":"catharijneconvent","score":100.64212294934042},{"term":"maatstaf","score":100.49113786367516},{"term":"nalev","score":100.44209815504973},{"term":"nl","score":100.35371007552844},{"term":"+","score":100.09480703578973},{"term":"zichzelf","score":99.85216118661153},{"term":"] , [","score":99.7902519049437},{"term":"werkgeleg","score":99.76355426817939},{"term":"gedeputeerd","score":99.76355426817939},{"term":"[ ]","score":99.76294544024401},{"term":"onderzoekswens","score":99.74080520767626},{"term":"paspoort","score":99.64641199985893},{"term":"heterdad","score":99.57164233182282},{"term":"blz","score":98.94083816222779},{"term":"bewijswaard","score":98.79698425816953},{"term":"omgangsregel","score":98.74809376110409},{"term":"_NUMwetboek","score":98.66774821141288},{"term":"stoff","score":98.48780359227476},{"term":"kerngetu","score":98.42640103051185},{"term":"bericht","score":98.10654087370769},{"term":"delft","score":97.78571377081803},{"term":"wrakingsgrond","score":97.67520225130404},{"term":"achtst","score":97.65317372588629},{"term":"voltooid","score":97.64826696761938},{"term":"novum","score":97.38122798993493},{"term":"lelystad","score":97.30025771741596},{"term":"europes","score":97.28968739869345},{"term":"hervat","score":97.20255592569123},{"term":"verminderd","score":97.09277308095801},{"term":"°)","score":96.9819018629586},{"term":"voorafgegan","score":96.87560385318734},{"term":"contant","score":96.6174504871407},{"term":"beschikt","score":96.61605863538344},{"term":"volkswag","score":96.24423881911406},{"term":"achmea","score":96.16064782079374},{"term":"brief","score":96.03496449593497},{"term":"relatie","score":96.02390436562854},{"term":"blijken","score":95.72561586105134},{"term":"gezamen","score":95.71230482914797},{"term":"opbrengst","score":95.70641807704835},{"term":"richting","score":95.67419168746852},{"term":"beled","score":95.62793108477155},{"term":"vragenformulier","score":95.59569054945393},{"term":"ondergetek","score":95.49972420048093},{"term":"recidivegevar","score":95.49972281931973},{"term":"gan","score":95.48780862299728},{"term":"vermog","score":95.45679275670167},{"term":"museum","score":95.40361206613706},{"term":"_NUMtot","score":95.37772280669273},{"term":"onvolled","score":95.30001356537923},{"term":"vijf","score":95.04510351613902},{"term":"gelijk","score":94.9648851498257},{"term":"plan","score":94.81674548534812},{"term":"duid","score":94.70286791899368},{"term":"reaal","score":94.68460923860806},{"term":"promis","score":94.66017726945502},{"term":"instructie","score":94.65042117112021},{"term":"bestaand","score":94.5828778075446},{"term":"bevoordel","score":94.45718615648502},{"term":"wetterskip","score":94.42286275591893},{"term":"begripsomschrijv","score":94.42286275591893},{"term":"noodzak","score":94.31275559417794},{"term":"ducht","score":94.16043017468068},{"term":"wsw","score":94.0848015950995},{"term":"huurder","score":94.0699539422088},{"term":"voogdij","score":94.03774266435431},{"term":"raadsher","score":94.03202832523591},{"term":"slot","score":93.76749048189134},{"term":"woord","score":93.70391033776951},{"term":"inschrijv","score":93.58934825477033},{"term":"karakter","score":93.58160952862806},{"term":"commentar","score":93.54817778636117},{"term":"hoofdstuk","score":93.50136132285789},{"term":"uitker","score":93.2798947341272},{"term":"gemak","score":93.1990337551492},{"term":"eenvoud","score":93.15934401337074},{"term":"verreken","score":93.05405204757467},{"term":"berechtingsrapport","score":92.99123669789857},{"term":"verzorg","score":92.98865679036389},{"term":"coca","score":92.96106970623953},{"term":"tiend","score":92.92439844471647},{"term":"voldan","score":92.91717219776312},{"term":"psychiatrisch","score":92.89318621477129},{"term":"richtlijn","score":92.64032549196126},{"term":"aanteken","score":92.60879456147377},{"term":"bijdrag","score":92.59622255427254},{"term":"trading","score":92.42034736107522},{"term":"vooronderzoek","score":92.26483593260004},{"term":"willem","score":92.25520511306144},{"term":"ontvanger","score":91.99146785057047},{"term":"vergezeld","score":91.86923452903987},{"term":"klant","score":91.77323798518496},{"term":"werkstraf","score":91.69272744539505},{"term":"onbek","score":91.66329483162845},{"term":"restaurant","score":91.60918668499718},{"term":"overeengekom","score":91.57949423749909},{"term":"gegrond","score":91.52137510818346},{"term":"accommodatie","score":91.2541743970694},{"term":"overval","score":91.21605121557016},{"term":"proceskostenvergoed","score":91.20384485177306},{"term":"rechtsvermoed","score":91.12545041434973},{"term":"dwing","score":91.06662142922205},{"term":"risico","score":91.03988781307991},{"term":"“[","score":90.96331743674956},{"term":"prognos","score":90.86647400416199},{"term":")]","score":90.59027904491293},{"term":"inclusief","score":90.53089008679771},{"term":"financial","score":90.43954832920193},{"term":"maria","score":90.38489013440207},{"term":"locatie","score":90.2346752973813},{"term":"media","score":90.22408682469619},{"term":"sleutel","score":90.21998210875711},{"term":"taxi","score":90.14545509496523},{"term":"ontbrek","score":90.12721451093634},{"term":"zestien","score":90.0010983347387},{"term":"daarom","score":89.92801209211002},{"term":"conform","score":89.90551816099723},{"term":"eu","score":89.63386561747805},{"term":"verdragsrecht","score":89.52982017281528},{"term":"gevar","score":89.51335255132518},{"term":"gestol","score":89.47117453561776},{"term":"oss","score":89.37251071668092},{"term":"rechtsprak","score":89.34524424741822},{"term":"robert","score":89.32274074428649},{"term":"team","score":89.22241978204998},{"term":"gesprek","score":89.21410340619057},{"term":",--","score":89.10513613450557},{"term":"verkop","score":89.02574161283229},{"term":"bevindt","score":88.87561498784507},{"term":"onz","score":88.59342934439148},{"term":"gvb","score":88.42603164209811},{"term":"nijmeg","score":88.33220681718727},{"term":"ontvang","score":88.29183085141808},{"term":"berad","score":88.0359645207264},{"term":"arrondissement","score":87.98826768121181},{"term":"heffingsrent","score":87.88064480674979},{"term":"bedrijfstakpensioenfond","score":87.85501833936029},{"term":"strafdossier","score":87.82880102329806},{"term":"onvoldo","score":87.80178567490401},{"term":"bis","score":87.56774501586348},{"term":"+ €","score":87.53920074835884},{"term":"punt","score":87.52765600732808},{"term":"verzuim","score":87.31788974676898},{"term":"opsporingsonderzoek","score":87.25862939924122},{"term":"meldplicht","score":87.25310623245234},{"term":"toen","score":87.14248206825376},{"term":"bewijslast","score":86.9344560355934},{"term":"extern","score":86.92264870730028},{"term":"ambtsed","score":86.89791569862699},{"term":"bondgenot","score":86.77566379616533},{"term":"linkerrijstrok","score":86.7345115657439},{"term":"ilhan","score":86.71240527341486},{"term":"”,","score":86.66226116469348},{"term":"mat","score":86.6582617914531},{"term":"tuchtuitsprak","score":86.60838202722515},{"term":"vrijheid","score":86.59334343142524},{"term":"onbetaald","score":86.59053937185},{"term":"\"[","score":86.51151044654874},{"term":"almelo","score":86.43067422603026},{"term":"weergav","score":86.42853129234587},{"term":"pro","score":86.30078844391166},{"term":"unierecht","score":86.21148266414042},{"term":"]]","score":86.18692603226158},{"term":"_NUMjar","score":85.91121691766773},{"term":"inkomstenbelast","score":85.83187761006263},{"term":"compensatie","score":85.79949756093727},{"term":"geblev","score":85.70138732171073},{"term":"vaderschap","score":85.64744380949341},{"term":"bosch","score":85.42551187591727},{"term":"rond","score":85.42434961627731},{"term":"provincie","score":85.41270901528617},{"term":"= €","score":85.30019906079386},{"term":"sms","score":85.16228973639875},{"term":"pleegmoeder","score":85.01229257195553},{"term":"caddy","score":85.01229257195553},{"term":"sport","score":84.96004229533533},{"term":"ontucht","score":84.9586014834801},{"term":"overgelegd","score":84.93187567973148},{"term":"rechterrijstrok","score":84.89796171447146},{"term":"samenhang","score":84.77123890437588},{"term":"winst","score":84.75482579744983},{"term":"kwaliteit","score":84.63822461088135},{"term":"formaliteit","score":84.54050055227911},{"term":"asielrelas","score":84.33179306360474},{"term":"woonplaats_NUM","score":84.28888207554405},{"term":"theodorus","score":84.27961462476588},{"term":"erkenn","score":84.10962855490025},{"term":"ingang","score":84.0681101457833},{"term":"_NUMe","score":83.86711903064014},{"term":"document","score":83.64551910414373},{"term":"jongmeerderjar","score":83.54749201296858},{"term":"aannemersbedrijf","score":83.50609476664845},{"term":"arrondissementsparket","score":83.38317301413599},{"term":"solution","score":83.29220551472015},{"term":"hendrik","score":83.07410030865324},{"term":"mallorca","score":83.0576474102984},{"term":"heil","score":82.95759339515007},{"term":"uitvoerbaarverklar","score":82.92872986414048},{"term":"herstel","score":82.79025766227065},{"term":"pl_NUMkc","score":82.77172805508044},{"term":"tarieventabel","score":82.65495683616567},{"term":"ontruim","score":82.63510124346894},{"term":"militair","score":82.58199035329191},{"term":"jitta","score":82.55352219005519},{"term":"voerendal","score":82.54965256045455},{"term":"zetel","score":82.25608989626235},{"term":"]-","score":82.15838275557005},{"term":"winstbereken","score":82.14177020358672},{"term":"bestelauto","score":82.03892478325868},{"term":"opdracht","score":82.03489545196742},{"term":"geslag","score":82.00134200906113},{"term":"hoofdaanvrager","score":81.94282602419553},{"term":"inkomst","score":81.9297411574145},{"term":"leger","score":81.89416298343504},{"term":"atos","score":81.88248130182428},{"term":"problem","score":81.85789429546944},{"term":"psychiater","score":81.67666895489435},{"term":"integratie","score":81.58459212214179},{"term":"contact","score":81.5755548053572},{"term":"josephus","score":81.56770388230754},{"term":"vertrouwensbeginsel","score":81.54643781748526},{"term":"aanpass","score":81.52330793750596},{"term":"verstrekt","score":81.45425997210728},{"term":"waarin","score":81.31203622771795},{"term":"argument","score":81.29502305641859},{"term":"vestia","score":81.24603543335434},{"term":"wanner","score":81.15706560076612},{"term":"familie","score":80.86825146622766},{"term":"vo","score":80.83439293888377},{"term":"premiejar","score":80.7212322927481},{"term":"reglement","score":80.69477200034991},{"term":"fa","score":80.68146035763323},{"term":"defensie","score":80.53892736356767},{"term":"honorariumomzetplafond","score":80.4134725264228},{"term":"deelnam","score":80.3626116814414},{"term":"bedraagt","score":80.32550815005078},{"term":"] €","score":80.32162100426416},{"term":"hoofd","score":80.32005001007542},{"term":"mij","score":80.30030233694083},{"term":"kilometer","score":80.23259950695547},{"term":"teruggegev","score":80.15197186805143},{"term":"maatschappij","score":80.11692314503651},{"term":"horloges","score":80.11576847792361},{"term":"ijzerman","score":80.07044986441026},{"term":"demand","score":80.07044986441026},{"term":"claim","score":80.0397975866688},{"term":"onbevoegd","score":80.027419644957},{"term":"hennepplant","score":80.01258855124098},{"term":"regio","score":79.97069935857749},{"term":"onjuist","score":79.9056576070779},{"term":"financier","score":79.84257911684496},{"term":"when","score":79.83037944378792},{"term":"slotopmerk","score":79.7067743124845},{"term":"hattem","score":79.68810227304868},{"term":"deskundigenbericht","score":79.65665783701262},{"term":"tpi","score":79.57490087217184},{"term":"scherp","score":79.5551485642093},{"term":"tussenbesliss","score":79.45932374103617},{"term":"medewerker","score":79.300841853185},{"term":"vermoed","score":79.30027989757939},{"term":"bevestigd","score":79.20182020548098},{"term":"tariev","score":79.16243414113748},{"term":"aangaand","score":79.14223211245306},{"term":"gehoord","score":79.11030784164561},{"term":"nn","score":79.09303944339813},{"term":"caf","score":78.94486064467995},{"term":"bestond","score":78.9442152379881},{"term":"woningcorporaties","score":78.75338252467702},{"term":"zoud","score":78.70098621485499},{"term":"typ","score":78.65600367883178},{"term":"verhog","score":78.47736649162019},{"term":"plaats","score":78.42044674892288},{"term":"correctie","score":78.34046448410353},{"term":"waaronder","score":78.3247798562461},{"term":"processuel","score":78.28749327833091},{"term":"kennis","score":78.28691731386836},{"term":"verzwar","score":78.15949054755939},{"term":"waver","score":78.0815245876721},{"term":"causal","score":77.96683350541508},{"term":"erflater","score":77.86314797649662},{"term":"boerderij","score":77.80249630379944},{"term":"enexis","score":77.61248737205413},{"term":"]/[","score":77.59767712407235},{"term":"bekend","score":77.592873212722},{"term":"zag","score":77.42380690066476},{"term":"gepleegdt","score":77.4084348137515},{"term":"afbaken","score":77.32283692803874},{"term":"fran","score":77.2880858929808},{"term":"dam","score":77.278601600228},{"term":"aannem","score":77.22898611316812},{"term":"priv","score":77.15015859755333},{"term":"didi","score":76.97066998885852},{"term":"medewerk","score":76.77680407231736},{"term":"opleid","score":76.67328710229721},{"term":"gedoogbeleid","score":76.58365474716686},{"term":"voorbehoud","score":76.56992533363047},{"term":"tripscompagnie","score":76.56234920938843},{"term":"one","score":76.54114850088723},{"term":"aftrekovereenkomst","score":76.49682702978174},{"term":"_NUMpoint","score":76.49682702978174},{"term":"_NUMbeoordel","score":76.49682702978174},{"term":"aanvraagformulier","score":76.48945203666251},{"term":"twaalf","score":76.4477704417438},{"term":"douanewaard","score":76.3777575507228},{"term":"idem","score":76.22339426261065},{"term":"kalm","score":76.18609815776416},{"term":"passiev","score":75.95594321009543},{"term":"ondernemingsrad","score":75.95384349631887},{"term":"splitsing","score":75.92978332780723},{"term":"sol","score":75.82080387783635},{"term":"zeker","score":75.78937344076745},{"term":"basis","score":75.77530873000481},{"term":"npb","score":75.75130053917891},{"term":"strafkamer","score":75.6742299131451},{"term":"zoverr","score":75.62842561212106},{"term":"partiel","score":75.54789122912482},{"term":"wijt","score":75.4855753994474},{"term":"geschorst","score":75.4855753994474},{"term":"rester","score":75.38015474348927},{"term":"delivery","score":75.32982778763194},{"term":"nihil","score":75.3245427980505},{"term":"bevat","score":75.18279705134876},{"term":"definitiev","score":75.10826588228001},{"term":"max","score":75.03659119566782},{"term":"behoud","score":75.01848602290139},{"term":"laatst","score":75.00795893355964},{"term":"alsnog","score":74.93252140174823},{"term":"gemen","score":74.89463360699136},{"term":"ww","score":74.72827331087942},{"term":"voorkom","score":74.5687505600795},{"term":"melkbus","score":74.46950148924091},{"term":"ireland","score":74.45396188836109},{"term":"kruising","score":74.44323182919823},{"term":"inkoopadministratie","score":74.2895680432324},{"term":"totstandkom","score":74.26141778179024},{"term":"right","score":74.25266840424793},{"term":"gewoontewitwass","score":74.21343960701358},{"term":"verschill","score":74.12988876820077},{"term":"wap","score":73.99427488191596},{"term":"tribunal","score":73.8911852291952},{"term":"procesbevoegd","score":73.87200829243724},{"term":"bevest","score":73.85844388672452},{"term":"leeftijd","score":73.8000921529573},{"term":"administratieplicht","score":73.75037588361616},{"term":"voortzet","score":73.72991672451},{"term":"minder","score":73.72511701341548},{"term":"carolin","score":73.46614801193016},{"term":"eisvermeerder","score":73.43878063180095},{"term":"navorder","score":73.41790528262656},{"term":"expertis","score":73.33931632972131},{"term":"impliciet","score":73.28159602160811},{"term":"vervoer","score":73.23736354246145},{"term":"programma","score":73.2242891862996},{"term":"verlies","score":73.13326265172638},{"term":"men","score":73.05801680998916},{"term":"individuel","score":72.9319781233562},{"term":"fiod","score":72.89976916881336},{"term":"handelsnam","score":72.76654603914864},{"term":"heft","score":72.70476967039735},{"term":"bijtell","score":72.64718072155132},{"term":"noodweerexces","score":72.61324126112498},{"term":"vrijgesprok","score":72.56130319093575},{"term":"_NUMd","score":72.5134002176122},{"term":"dad","score":72.43431171436771},{"term":"echtpar","score":72.36380869610636},{"term":"any","score":72.36172906330899},{"term":"storting","score":72.31062700516604},{"term":"evenred","score":72.26491259505853},{"term":"forensisch","score":72.24999774253992},{"term":"broer","score":72.10316544165302},{"term":"aldus","score":72.07478718048034},{"term":"opvang","score":71.94715441216145},{"term":"bedrijfspand","score":71.91800163164656},{"term":"los","score":71.91539432293884},{"term":"gl","score":71.91107640770292},{"term":"megahom","score":71.76735696156797},{"term":"inlicht","score":71.67456070510991},{"term":"rustig","score":71.65325843925471},{"term":"coninck","score":71.5926807647231},{"term":"david","score":71.59241745581923},{"term":"rijnmond","score":71.56934113505764},{"term":"schoen","score":71.4585790759232},{"term":"havr","score":71.28955765564092},{"term":"juwelier","score":71.28358717341585},{"term":"ondersteun","score":71.26544280261184},{"term":"led","score":71.09334589391099},{"term":"bnb","score":71.01933191705314},{"term":"year","score":70.9554020721239},{"term":"informatieplicht","score":70.90216987642737},{"term":"verstan","score":70.83483468081002},{"term":"contra","score":70.83339861698357},{"term":"procedureverlop","score":70.73342948008981},{"term":"verblijf","score":70.70570510717555},{"term":"ontslat","score":70.6419168537351},{"term":"pieter","score":70.61005516923206},{"term":"ker","score":70.53150136939522},{"term":"afstand","score":70.45885327626961},{"term":"kon","score":70.44737389833384},{"term":"grondgebied","score":70.33471174629702},{"term":"openbrek","score":70.32292108234043},{"term":"(…)”","score":70.23441115864881},{"term":"azewijn","score":70.2330121873049},{"term":"air","score":70.22656705866015},{"term":"oproep","score":70.18491678110024},{"term":"overwog","score":70.14886852597452},{"term":"schuldwitwass","score":70.13068495987272},{"term":"zie","score":70.0014583880185},{"term":"term","score":69.91996869639729},{"term":"erfgenam","score":69.84842751632301},{"term":"verzekerd","score":69.62704108958872},{"term":"bevorder","score":69.61048490478396},{"term":"afsprak","score":69.60780673422181},{"term":"stek","score":69.57602945086526},{"term":"kliniek","score":69.56698683658726},{"term":"smolder","score":69.56535522418228},{"term":"inkom","score":69.49841640001749},{"term":"opsporingsberichtgev","score":69.49041923133855},{"term":"mening","score":69.48699165435423},{"term":"michael","score":69.46617677411652},{"term":"fiet","score":69.43967270730955},{"term":"bankbreuk","score":69.39403161631571},{"term":"aha","score":69.36124525158101},{"term":"( [","score":69.25058908657721},{"term":"financ","score":69.23489987567612},{"term":"verschoningsrecht","score":69.22967794331906},{"term":"vastned","score":69.1651416858146},{"term":"integral","score":69.05418146766411},{"term":"beschrijv","score":69.00179982524573},{"term":"doend","score":68.99843211313537},{"term":"voert","score":68.96311895281694},{"term":"geled","score":68.90017295657535},{"term":"lening","score":68.89360378132808},{"term":"preliminair","score":68.867980612778},{"term":"toezicht","score":68.76174634461259},{"term":"overmacht","score":68.73108537830049},{"term":"visie","score":68.65409810306683},{"term":"werkloosheidswet","score":68.53929255898369},{"term":"eiseres_NUM","score":68.5274637132233},{"term":"hoist","score":68.50680964807746},{"term":"onoplet","score":68.49685389750275},{"term":"schram","score":68.48622643749673},{"term":"zer","score":68.48490095577758},{"term":"strekking","score":68.39502766484651},{"term":"for","score":68.26553873201928},{"term":"overbreng","score":68.24295201219776},{"term":"evenement","score":68.14391499043558},{"term":"\",","score":68.14041080265646},{"term":"forma","score":68.08161324414372},{"term":"hvj","score":68.05018428235479},{"term":"belegg","score":67.71918338195233},{"term":"deskundigenonderzoek","score":67.714811309389},{"term":"non","score":67.70484902943424},{"term":"concurrentie","score":67.68963142612306},{"term":"moest","score":67.68696751138685},{"term":"tel","score":67.62292454846903},{"term":"mocht","score":67.43913502423018},{"term":"advistal","score":67.40103481270089},{"term":"tmg","score":67.34794090950663},{"term":"treff","score":67.23537505888254},{"term":"eiseress","score":67.20789831029445},{"term":"parker","score":67.1574235638676},{"term":"kessel","score":67.14736512961146},{"term":"exces","score":67.114578162779},{"term":"echr","score":67.114578162779},{"term":"bov","score":67.06208237146745},{"term":") (","score":66.95320185866758},{"term":"administratief","score":66.95085119682187},{"term":"brigadier","score":66.95085119682187},{"term":"stap","score":66.83866930871096},{"term":"klimm","score":66.79620109292772},{"term":"park","score":66.77588861883164},{"term":"ontvankelijkheidsverwer","score":66.70389157989084},{"term":"binnenland","score":66.69910539923762},{"term":"fortis","score":66.68820480009738},{"term":"ontwikkel","score":66.64374838306344},{"term":"object","score":66.60283464957716},{"term":"ikea","score":66.52342684846718},{"term":"netbeher","score":66.50863029349601},{"term":"winkel","score":66.38617269234967},{"term":"partijd","score":66.3307857475101},{"term":"vaststellingsovereenkomst","score":66.32567633630782},{"term":"tegenwoord","score":66.16008269523458},{"term":"vard","score":66.12232288309147},{"term":"eventuel","score":66.08915149268563},{"term":"rechtsopvolgster","score":66.06715008053287},{"term":"schiphol","score":66.05013961250775},{"term":"dekker","score":65.99402047203645},{"term":"go","score":65.9046119246529},{"term":"correcties","score":65.90255860156671},{"term":"vriend","score":65.86660118406797},{"term":"tariefpost","score":65.7855341464213},{"term":"car","score":65.75802490531242},{"term":"verschaff","score":65.59030668437111},{"term":"omker","score":65.4715080424656},{"term":"vergrijpboet","score":65.45626745294854},{"term":")/","score":65.44409882754601},{"term":"behandeld","score":65.42011448531314},{"term":"beslaggenom","score":65.31940267569428},{"term":"invoercertificat","score":65.24036803674926},{"term":"hond","score":65.21388375963217},{"term":"omschrev","score":65.21121228687201},{"term":"vergelijk","score":65.1286124138737},{"term":"griffier","score":65.11612809249813},{"term":"vreemdel","score":65.08747880526012},{"term":"luchtvaartmaatschappij","score":65.05849200405584},{"term":"levensonderhoud","score":65.01962079655509},{"term":"veertien","score":64.97368179454581},{"term":"afslaand","score":64.81995245069655},{"term":"ongeoorloofd","score":64.70627127317229},{"term":"buitengewon","score":64.66264043460596},{"term":"overflakkee","score":64.66120895912366},{"term":"vrijgesteld","score":64.6599225157211},{"term":"as","score":64.63290798518392},{"term":"taxatie","score":64.55206213825119},{"term":"tenant","score":64.50735066578989},{"term":"goeree","score":64.50735066578989},{"term":"oorzak","score":64.49078085589848},{"term":"verblijv","score":64.46017345366619},{"term":"deventer","score":64.34120282206543},{"term":"pagina","score":64.33559684691421},{"term":"aangebracht","score":64.31917979892233},{"term":"ging","score":64.21455000332341},{"term":"echt","score":64.20648220206782},{"term":"card","score":64.11537850292137},{"term":"contract","score":64.08441249013828},{"term":"wild","score":64.08106983825705},{"term":"mog","score":63.85962630395221},{"term":"zull","score":63.74930907030689},{"term":"debat","score":63.74425039814997},{"term":"kredit","score":63.73659726665359},{"term":"discriminatieverbod","score":63.73124735033178},{"term":"verdienvermog","score":63.71267148410101},{"term":"verlengd","score":63.67169827417794},{"term":"toegek","score":63.62908859181617},{"term":"ontzegg","score":63.616673461597685},{"term":"vve","score":63.5328958658527},{"term":"noordwijkerhout","score":63.48337860255982},{"term":"schijnrelatie","score":63.36142133693902},{"term":"premie","score":63.28582703665033},{"term":") [","score":63.28582703665033},{"term":"verwez","score":63.26455522425986},{"term":"kond","score":63.12748683411675},{"term":"hanos","score":63.12608378264909},{"term":"onderbouw","score":62.952721330545764},{"term":"conver","score":62.9149617254172},{"term":"kredietfaciliteit","score":62.89065379210213},{"term":"av","score":62.87450304210786},{"term":"soortgelijk","score":62.826911261057816},{"term":"verminder","score":62.71714047901635},{"term":"method","score":62.700707910646365},{"term":"à","score":62.649545572086964},{"term":"party","score":62.58329835206829},{"term":"montferland","score":62.42977344598155},{"term":"computervredebreuk","score":62.38002179171572},{"term":"menzis","score":62.31291468194639},{"term":"point","score":62.298244747987994},{"term":"voorbereidingshandel","score":62.25444893309833},{"term":"aldar","score":62.23810711319593},{"term":"legal","score":62.191788826045546},{"term":"bestand","score":62.186061292656326},{"term":"alternatief","score":62.1822455389505},{"term":"rijnsburg","score":62.13842890536167},{"term":"model","score":62.091285948321094},{"term":"tele_NUM","score":62.04014043748585},{"term":"verwijst","score":62.01650698994624},{"term":"bestuursrechter","score":62.00149316177114},{"term":"geldigheidsdur","score":61.99121762712425},{"term":"ingetrok","score":61.96164167173117},{"term":"stoei","score":61.953222546959935},{"term":"destuk","score":61.9267478510012},{"term":"middelis","score":61.9267478510012},{"term":"_NUMinleid","score":61.9267478510012},{"term":"uitzendkracht","score":61.89455270548807},{"term":"zat","score":61.892088112414356},{"term":"bezwaarfas","score":61.87805266862692},{"term":"verhoud","score":61.87064872201645},{"term":"onderteken","score":61.81352774296655},{"term":"maasdelta","score":61.77463213394954},{"term":"balkon","score":61.73905104427669},{"term":"looptijd","score":61.70845674424474},{"term":"huwelijksgemeenschap","score":61.60703251697228},{"term":"duit","score":61.590034528460485},{"term":"sectorindel","score":61.560006910364365},{"term":"dinsdag","score":61.55158249668986},{"term":"toeslag","score":61.515074280320235},{"term":"kuiper","score":61.47785637585683},{"term":"aruba","score":61.41180218346271},{"term":"eenmal","score":61.37716510477513},{"term":"trouw","score":61.360731918022736},{"term":"verniet","score":61.357707826587315},{"term":"tui","score":61.23468894362905},{"term":"dutch","score":61.233691230897506},{"term":"hoogheemraadschap","score":61.233691230897506},{"term":"tapgesprek","score":61.14199020811684},{"term":"condities","score":61.10622057921509},{"term":"rijvaardigheidsbewijz","score":61.07963775991625},{"term":"rechtsbijstandverzeker","score":61.00387873481324},{"term":"amerika","score":60.99907487481505},{"term":"bitum","score":60.92925048372987},{"term":"bekanntwerden","score":60.580858904516354},{"term":"from","score":60.548133780291124},{"term":"payment","score":60.53927551868956},{"term":"opvoedingstak","score":60.53381352468469},{"term":"pol","score":60.52852803656858},{"term":"fed","score":60.49571815861273},{"term":"xtc","score":60.49029565485459},{"term":"sticht","score":60.447091147741105},{"term":"asielzoeker","score":60.42560500123486},{"term":"tv","score":60.39351936327529},{"term":"borst","score":60.36185349571255},{"term":"aangegev","score":60.20661077648657},{"term":"daaruit","score":60.183202325836014},{"term":"hoogstrat","score":60.11110336482085},{"term":"twent","score":60.10204835392224},{"term":"omstreeks_NUM","score":60.04673333401826},{"term":"wonendet","score":60.04673333401826},{"term":"vorig","score":60.027328652786544},{"term":"huidig","score":59.90237533288934},{"term":"dienstbetrek","score":59.811969525967825},{"term":"voorbelast","score":59.7230786275441},{"term":"bam","score":59.704030992499455},{"term":"meerwerk","score":59.647248234313764},{"term":"hav","score":59.638756506847926},{"term":"ib","score":59.609177945032556},{"term":"daarmed","score":59.595665267130485},{"term":"aanbied","score":59.56750235720062},{"term":"market","score":59.420806266416676},{"term":"kern","score":59.36563994045076},{"term":"liquidatie","score":59.36213712652687},{"term":"uiteind","score":59.349496364268646},{"term":"wer","score":59.32942289339838},{"term":"drag","score":59.32835762910066},{"term":"strom","score":59.32631612460784},{"term":"verjar","score":59.31987152458653},{"term":"gemotiveerd","score":59.29426369712523},{"term":"opsporingsdienst","score":59.2468893006238},{"term":"pensioen","score":59.18060714097368},{"term":"belastingrent","score":59.11633358060537},{"term":"belastingjar","score":59.08567140311352},{"term":"vestigingsnam","score":59.06505107976942},{"term":"vos","score":59.051458908420074},{"term":"beteken","score":58.989807728091776},{"term":"rechtsvorder","score":58.96772810034308},{"term":"krijg","score":58.93650461064162},{"term":"zulk","score":58.91165541797275},{"term":"terugwerk","score":58.886262180002255},{"term":"hilversum","score":58.83782377569479},{"term":"praktijk","score":58.771670134858425},{"term":"eneco","score":58.75607864205753},{"term":"klm","score":58.73177070874245},{"term":"toedracht","score":58.70353536251798},{"term":"carrier","score":58.64916354737488},{"term":"overeenstemm","score":58.60600499822326},{"term":"afkomst","score":58.55199712676081},{"term":"bp_NUM","score":58.49395886759808},{"term":"vonniss","score":58.257854137299006},{"term":"mas","score":58.25400305668134},{"term":"gi","score":58.23908695204456},{"term":"omgevingsvergunn","score":58.22418194885428},{"term":"tran","score":58.221937517590455},{"term":"var","score":58.15403159858673},{"term":"](","score":58.128382704565865},{"term":"leiding","score":58.112385960820944},{"term":"opstell","score":58.053977065458355},{"term":"variant","score":58.026869709135404},{"term":"welzijn","score":58.01968407953405},{"term":"brink","score":57.870740261161686},{"term":"beleidsmat","score":57.86835989359847},{"term":"articles","score":57.82835968026309},{"term":"wit","score":57.80261474745457},{"term":"technology","score":57.53814283201351},{"term":"aanhoudingsverzoek","score":57.53814283201351},{"term":"pleegouders_NUM","score":57.532298696328766},{"term":"ntfr","score":57.527011467055914},{"term":"schadeloosstell","score":57.527011467055914},{"term":"pseudokop","score":57.46351458093014},{"term":"adviseur","score":57.350271227082224},{"term":"rechtsherstel","score":57.33553684498531},{"term":"et","score":57.30302007916349},{"term":"ziekt","score":57.22421000424046},{"term":"redelijkerwijs","score":57.1946439079495},{"term":"belgium","score":57.19317847457876},{"term":"exploiteert","score":57.16443124062022},{"term":"behulpzam","score":57.139573618323126},{"term":"titel","score":57.11701035942916},{"term":"waarom","score":57.08149758117884},{"term":"tankstation","score":57.06867631256368},{"term":"ois","score":57.03164612451274},{"term":"bedreigd","score":57.021458278575736},{"term":"terugbetal","score":57.01142766394987},{"term":"afdrachtverminder","score":56.98696368190851},{"term":"afgewez","score":56.88218484505753},{"term":"meerderjar","score":56.81076554316484},{"term":"geregeld","score":56.79343093507244},{"term":"supermarkt","score":56.76980975274245},{"term":"cumulatief","score":56.758159263644984},{"term":"softwar","score":56.64859350883732},{"term":"rijnland","score":56.58286664819997},{"term":"verantwoord","score":56.565948569414196},{"term":"gehandeld","score":56.56167924617848},{"term":"schenking","score":56.540992701596466},{"term":"law","score":56.496258104808405},{"term":"bedrieg","score":56.410522605928264},{"term":"wist","score":56.39274810560172},{"term":"leas","score":56.34497803058571},{"term":"gezien","score":56.339743794773526},{"term":"garantstell","score":56.29621198950442},{"term":"gehev","score":56.28010310145143},{"term":"gebaseerd","score":56.251566955786906},{"term":"servic","score":56.231970205666016},{"term":"onvoorzicht","score":56.21068415229839},{"term":"verlengingsadvies","score":56.19461197704964},{"term":"poorthuis","score":56.18640974984392},{"term":"miscellaneous","score":56.18640974984392},{"term":"marthedes","score":56.18640974984392},{"term":"betalingsverplicht","score":56.184719339469076},{"term":"bewijsconstructie","score":56.12328409125553},{"term":"lloyd","score":56.087131046132384},{"term":"vur","score":55.948247862988474},{"term":"maatschapp","score":55.89524167207413},{"term":"nomenclatur","score":55.87461406288483},{"term":"sint","score":55.85490984450011},{"term":"hulp","score":55.853413507335915},{"term":"belon","score":55.839909836687596},{"term":"risiko","score":55.82252263702101},{"term":"vanuit","score":55.81771468594756},{"term":"cas","score":55.78770316797117},{"term":"wassenar","score":55.767943350509874},{"term":"bvba","score":55.71544943001878},{"term":"combinatie","score":55.69836925518397},{"term":"athena","score":55.65228417934583},{"term":"sterrenkwartier","score":55.65228417934583},{"term":"noch","score":55.598733120013996},{"term":"knb","score":55.57386982943234},{"term":"betaald","score":55.53992830514194},{"term":"ticaret","score":55.488996201264804},{"term":"ars","score":55.48581723938916},{"term":"milieu","score":55.4742530315515},{"term":"regional","score":55.47190423527958},{"term":"drank","score":55.43211473896135},{"term":"ve","score":55.387048595708},{"term":"schuldhel","score":55.326294506795435},{"term":"]-[","score":55.31423177476852},{"term":"infra","score":55.26626977631132},{"term":"brengt","score":55.24617708414446},{"term":"bewijsmateriaal","score":55.17413320944862},{"term":"gehuurd","score":55.14941000911675},{"term":"overigen","score":55.0949302852382},{"term":"daarmee","score":55.09284938578898},{"term":"- €","score":55.08760924748999},{"term":"claimant","score":54.976501563760884},{"term":"sdi","score":54.89531618079171},{"term":"inhal","score":54.83220742145099},{"term":"connexxion","score":54.81037332561928},{"term":"productschap","score":54.766989105490836},{"term":"bewijsuitsluitingsverwer","score":54.759709974088985},{"term":"normal","score":54.73782582693886},{"term":"bestuursrecht","score":54.72660690667162},{"term":"materiel","score":54.68133390583992},{"term":"wederpartij","score":54.62741241266149},{"term":"afm","score":54.56682599833077},{"term":"rudolf","score":54.5015558895387},{"term":"interventie","score":54.49381192854765},{"term":"scenario","score":54.47463981615204},{"term":"volkenrecht","score":54.46323079346047},{"term":"uitsluitingsgrond","score":54.4429467899824},{"term":"control","score":54.36032578647606},{"term":"toegestan","score":54.357830465177024},{"term":"later","score":54.321903911619806},{"term":")] ,","score":54.259057402272965},{"term":"groepslid","score":54.259057402272965},{"term":"valt","score":54.225441546164575},{"term":"medical","score":54.22509054851419},{"term":"zus","score":54.21770792450027},{"term":"]€","score":54.143107254779956},{"term":"finan","score":54.143107254779956},{"term":"vtw","score":54.143107254779956},{"term":"overgang","score":54.08392941987569},{"term":"telastelegg","score":54.072406929213784},{"term":"nullus","score":54.030423746913165},{"term":"verzoekt","score":54.01698247029743},{"term":"eventueel","score":53.935476318264804},{"term":"organ","score":53.93486485998726},{"term":"professional","score":53.92424794118193},{"term":"bestuurster","score":53.92424794118193},{"term":"wilson","score":53.92082785016071},{"term":"sedert","score":53.860800996174376},{"term":"verhandeld","score":53.8109302991659},{"term":"_NUMde","score":53.78035311843171},{"term":"intrekt","score":53.753827857283255},{"term":"databank","score":53.675086072442554},{"term":"leon","score":53.64911198805704},{"term":"portierslot","score":53.608981684281865},{"term":"westland","score":53.58902542803695},{"term":"projectontwikkel","score":53.55501169142318},{"term":"kasregister","score":53.51021123392038},{"term":"petrus","score":53.51021123392038},{"term":"koopmansgebruik","score":53.446038392286084},{"term":"apollo","score":53.413821027604136},{"term":"evert","score":53.31969903796336},{"term":"consument","score":53.313799572384994},{"term":"vervall","score":53.30850997989942},{"term":"gedagvaard","score":53.2729664217278},{"term":"land","score":53.257690147194694},{"term":"awbz","score":53.144136995803834},{"term":"vennootschapsbelast","score":53.13458538858633},{"term":"zuidlijn","score":53.04993391478123},{"term":"pgb","score":52.99621447635316},{"term":"vergrijpboetes","score":52.89689772769837},{"term":"behoort","score":52.82770777763587},{"term":"bekomt","score":52.7972183082951},{"term":"rechtsweg","score":52.78545216074434},{"term":"bindend","score":52.78245141049425},{"term":"bijl","score":52.772166387965484},{"term":"estat","score":52.75769459783366},{"term":"baby","score":52.72887607424991},{"term":"recreatie","score":52.72169732211939},{"term":"ondergan","score":52.56590648962812},{"term":"lez","score":52.499718768081834},{"term":"boetes","score":52.43425322299059},{"term":"administratiev","score":52.361098741157264},{"term":"seat","score":52.28245758661358},{"term":"post","score":52.27562648539449},{"term":"plac","score":52.243190369085724},{"term":"gestart","score":52.22971167723941},{"term":"waarmee","score":52.218419286352145},{"term":"onttrok","score":52.20241408693268},{"term":"johanna","score":52.192294429399404},{"term":"daarnaast","score":52.16728897956903},{"term":"armbandjes","score":52.12272745855193},{"term":"sa","score":52.095459648616675},{"term":"hierin","score":52.09364366937464},{"term":"procesord","score":52.07308712230634},{"term":"tussentijd","score":52.06509600014841},{"term":"kansspelbelast","score":52.06391380265035},{"term":"gerek","score":52.03721058976913},{"term":"ontheff","score":52.01473122463148},{"term":"afzonder","score":52.009745853654806},{"term":"schadeverdel","score":51.9871212518492},{"term":"indigd","score":51.968836068811655},{"term":"uitstel","score":51.92824796277271},{"term":"unus","score":51.92100404404436},{"term":"centrum","score":51.819951082058815},{"term":"revolver","score":51.79457084358597},{"term":"decision","score":51.77078958591273},{"term":"protocol","score":51.70980978902463},{"term":"mrs","score":51.677480092982336},{"term":"aanvaard","score":51.672803364089006},{"term":"incassokost","score":51.59795899404537},{"term":"es","score":51.56169988749612},{"term":"sw","score":51.53899837290221},{"term":"begrep","score":51.48659540389564},{"term":"medeveroordeeld","score":51.47150578454773},{"term":"hypothek","score":51.43999118217718},{"term":"verantwoordingsformulier","score":51.43255177560889},{"term":"geleid","score":51.418639702956476},{"term":"getuigenverklar","score":51.395781084713924},{"term":"rechtsgang","score":51.37194497294716},{"term":"berust","score":51.36596642965785},{"term":"tenzij","score":51.35174332528989},{"term":"activiteit","score":51.33268907554945},{"term":"berek","score":51.318533357884085},{"term":"jij","score":51.27673189877338},{"term":"verbeurd","score":51.25808748680066},{"term":"’?","score":51.25783502467338},{"term":"montecorona","score":51.25783502467338},{"term":"wattel","score":51.1996997384533},{"term":"budgethouder","score":51.1996997384533},{"term":"waartoe","score":51.14970636927925},{"term":"leschadevergoed","score":51.09454704659237},{"term":"relatiebed","score":51.079463939539124},{"term":"tussenuitsprak","score":51.075890732107595},{"term":"nakom","score":51.07006640759332},{"term":"koopsom","score":50.99739961087714},{"term":"gedachtestreepj","score":50.95700883949765},{"term":"without","score":50.926857994364696},{"term":"reijn","score":50.88670809086952},{"term":"//","score":50.79826534792474},{"term":"verzekeringsmaatschappij","score":50.788509247268436},{"term":"count","score":50.786702882047855},{"term":"hoofdlijn","score":50.77391069675176},{"term":"renn","score":50.63022264791498},{"term":"ehrm","score":50.60606201736667},{"term":"god","score":50.60197398520438},{"term":"baking","score":50.59389447477634},{"term":"voet","score":50.454523076013885},{"term":"oploss","score":50.444404877283205},{"term":"cooking","score":50.40995402180905},{"term":"concept","score":50.382751845973914},{"term":"infrastructur","score":50.38155261954429},{"term":"eindvonnis","score":50.33567706981556},{"term":"gall","score":50.32106147467021},{"term":"zorginstell","score":50.31461721563198},{"term":"club","score":50.27928137951653},{"term":"aangenom","score":50.20820656475512},{"term":"boordel","score":50.19132037165759},{"term":"harm","score":50.06565558672408},{"term":"berghem","score":50.06565558672408},{"term":"stedin","score":50.04288152914551},{"term":",- (","score":50.003822085166775},{"term":"repliek","score":49.99062075970915},{"term":"landsadvocat","score":49.984020100199245},{"term":"vestig","score":49.97838833957007},{"term":"gekom","score":49.95919536607416},{"term":"requisitoir","score":49.9307971345728},{"term":"aspect","score":49.89437949983335},{"term":"tiel","score":49.86959686010029},{"term":"verkeersdeelnemer","score":49.85744618583383},{"term":"voornem","score":49.785987084790094},{"term":"fujitsu","score":49.74865810010751},{"term":"levensverzeker","score":49.745441502388},{"term":"ispc","score":49.71074312428934},{"term":"geheimhoud","score":49.666946954672476},{"term":"volksgezond","score":49.60118198335948},{"term":"fas","score":49.590745978208616},{"term":"medeverdachte_NUM","score":49.58966566348902},{"term":"lag","score":49.58704145102274},{"term":"jusvergelijk","score":49.562578037567945},{"term":"geldigheidsdatum__NUM","score":49.52637869548827},{"term":"] ),","score":49.521119273863846},{"term":"gegan","score":49.51391324990776},{"term":"matig","score":49.494590074113546},{"term":"_NUMvan","score":49.49050401555723},{"term":"kleding","score":49.47035810694912},{"term":"comm","score":49.454948225888245},{"term":"zoetermer","score":49.438596770506535},{"term":"kwad","score":49.42003009488323},{"term":"gunningcriteria","score":49.384770987284604},{"term":"rixtel","score":49.384770987284604},{"term":"rechtsgeld","score":49.3650546977627},{"term":"sind","score":49.36379497858718},{"term":"bedenk","score":49.335500334492856},{"term":"pill","score":49.29918935428393},{"term":"slan","score":49.27576482867461},{"term":"breda","score":49.25823194205959},{"term":"opvat","score":49.1770824853651},{"term":"paul","score":49.148415321804585},{"term":"even","score":49.126304131512825},{"term":"technisch","score":49.11489728397068},{"term":"will","score":49.10020126422614},{"term":"tien","score":49.076760702809196},{"term":"kwam","score":49.0684884703571},{"term":"bevelschrift","score":49.051244551528406},{"term":"ti","score":49.051244551528406},{"term":"middengeleider","score":49.01937187293169},{"term":"gunning","score":49.01098590331572},{"term":"neerijn","score":48.98775115490324},{"term":"bwbr_NUM","score":48.98775115490324},{"term":"sigaret","score":48.94651266523235},{"term":"zelfd","score":48.919612802098314},{"term":"specialiteitsbeginsel","score":48.894361698915276},{"term":"wederhor","score":48.8895224404705},{"term":"machinal","score":48.863710207933},{"term":"johannes","score":48.833291810392126},{"term":"marokkan","score":48.806875930714455},{"term":"afbeeld","score":48.80555042598158},{"term":"mission","score":48.80310298785059},{"term":"survey","score":48.77314030093396},{"term":"derhalv","score":48.74988890968227},{"term":"smaadschrift","score":48.7138799577934},{"term":"toereken","score":48.69524475324498},{"term":"behart","score":48.69021477334485},{"term":"variabel","score":48.69021477334485},{"term":"vervat","score":48.682355382015245},{"term":"procespartij","score":48.66784337137024},{"term":"inventiviteit","score":48.65548476010879},{"term":"ganz","score":48.65548476010879},{"term":"voorstel","score":48.63783527585035},{"term":"woonsticht","score":48.59792981030039},{"term":"wvw","score":48.566829834782645},{"term":"linder","score":48.54119127033256},{"term":"hier","score":48.515439896998274},{"term":"lc","score":48.51312097969798},{"term":"ketting","score":48.51089388782982},{"term":"voorraadadministratie","score":48.4852463024336},{"term":"dierziekt","score":48.4852463024336},{"term":"(€","score":48.45787732493673},{"term":"overdracht","score":48.448400377458924},{"term":"grondwerk","score":48.43007301390426},{"term":",-,","score":48.41064156955726},{"term":"ervar","score":48.38824404821489},{"term":"bewaartermijn","score":48.37565040568115},{"term":"collection","score":48.34871427695577},{"term":"verwacht","score":48.323790703819775},{"term":"licht","score":48.31931424270459},{"term":"gedwong","score":48.270418185096446},{"term":"voorarrest","score":48.225331674949004},{"term":"michielsgestel","score":48.21668909108309},{"term":"g_NUM","score":48.19941595098363},{"term":"[ ],","score":48.165075471739456},{"term":"discharg","score":48.165075471739456},{"term":"tussenperson","score":48.13307520273937},{"term":"toetsingskader","score":48.064775905123085},{"term":"llc","score":48.038882040799756},{"term":"tram","score":48.038882040799756},{"term":"brandsticht","score":48.00856395837841},{"term":"samsung","score":47.941933322147754},{"term":"getuigenverzoek","score":47.91654834731143},{"term":"gesmokkeld","score":47.89252425107059},{"term":"voldoend","score":47.886334234708094},{"term":"horlog","score":47.88162557347866},{"term":"evenal","score":47.791521959008016},{"term":"recidiverisico","score":47.76581107708097},{"term":"wajong","score":47.76314937885468},{"term":"samenwon","score":47.726033619875395},{"term":"hierbij","score":47.70889660991785},{"term":"fictiev","score":47.64741564797811},{"term":"resum","score":47.63736529956927},{"term":"groesbek","score":47.4615128058848},{"term":"omzet","score":47.35107270340586},{"term":"breugel","score":47.27250126648262},{"term":"samenwerk","score":47.23451752125955},{"term":"gewon","score":47.18887640219112},{"term":"gebeurd","score":47.17531040968296},{"term":"werkneemster","score":47.1739385437711},{"term":"daaran","score":47.17155443866715},{"term":"ann","score":47.151279868501305},{"term":"mccarthyen","score":47.114491542193534},{"term":"stichtingsticht","score":47.114491542193534},{"term":"meerderetijdstipp","score":47.114491542193534},{"term":"koperbedrag","score":47.114491542193534},{"term":"obj","score":47.114491542193534},{"term":"iprocedur","score":47.114491542193534},{"term":"primairpog","score":47.114491542193534},{"term":"rechtmiddel","score":47.114491542193534},{"term":"primairmedepleg","score":47.114491542193534},{"term":"primairoplicht","score":47.114491542193534},{"term":"eenproeftijd","score":47.114491542193534},{"term":"iiibesliss","score":47.114491542193534},{"term":"iibesliss","score":47.114491542193534},{"term":"ioverweg","score":47.114491542193534},{"term":"optie","score":47.020223597006336},{"term":"helft","score":47.01085287197711},{"term":"ep","score":46.99505767794467},{"term":"bemiddel","score":46.98821617174613},{"term":"vrijwaringsprocedur","score":46.95706696683334},{"term":"heropent","score":46.919330837899906},{"term":"hellevoetsluis","score":46.881947388226955},{"term":"lux","score":46.87777670332097},{"term":"shall","score":46.85997517138834},{"term":"koninkrijk","score":46.78501785928049},{"term":"balan","score":46.67678852540703},{"term":"rechtsbijstand","score":46.675327126792666},{"term":"draagkrachtvergelijk","score":46.62961148652065},{"term":"stk","score":46.625658978883635},{"term":"efficiency","score":46.61218226504483},{"term":"werkelijk","score":46.587616493512854},{"term":"middelburg","score":46.56038115129423},{"term":"vp","score":46.52615744416097},{"term":"intentie","score":46.524942187006644},{"term":"bescheid","score":46.513292414481064},{"term":"amar","score":46.49225893619708},{"term":"verkeersongeval","score":46.463009107795976},{"term":"beleid","score":46.44770078658397},{"term":"_NUMtotal","score":46.4450608882509},{"term":"imexho","score":46.4450608882509},{"term":"mensensmokkel","score":46.44194380736964},{"term":"deskundigenrapport","score":46.440491157760924},{"term":"zaterdag","score":46.438449091161175},{"term":"finding","score":46.42531176677499},{"term":"test","score":46.39980745079562},{"term":"maatwerkvoorschrift","score":46.3594666585587},{"term":"onderzocht","score":46.33909585622838},{"term":"leidt","score":46.33799300789685},{"term":"varkensbedrijf","score":46.3269461542257},{"term":"merkt","score":46.294672011743714},{"term":"prijs","score":46.23493980492334},{"term":"bouwnijver","score":46.23094154582753},{"term":"risicotaxatie","score":46.183789722819355},{"term":"kennelijk","score":46.144141989305794},{"term":"opgeworp","score":46.11818153242637},{"term":"stelselmat","score":46.09906835930498},{"term":"blijv","score":46.052905517967915},{"term":"positie","score":46.04164647249653},{"term":"voorzet","score":45.956024217218804},{"term":"kb","score":45.93210480655328},{"term":"destijd","score":45.92428449830327},{"term":"], [ ]","score":45.89809621786904},{"term":"strafoverweg","score":45.89809621786904},{"term":"antonius","score":45.82534108806061},{"term":"oog","score":45.78809770763965},{"term":"stret","score":45.78271191016466},{"term":"maximal","score":45.75266912206616},{"term":"] &","score":45.68497580881553},{"term":"eigendom","score":45.604388747525576},{"term":"assuranti","score":45.60306168344137},{"term":"huwelijksgerelateerd","score":45.58957094552681},{"term":"customer","score":45.58957094552681},{"term":"wehkamp","score":45.576125554715986},{"term":"teneind","score":45.57002542827621},{"term":"brp","score":45.562725207174864},{"term":"hilt","score":45.49638869511272},{"term":"opgevoerd","score":45.48414944384212},{"term":"yersek","score":45.435644178387264},{"term":"salmon","score":45.435644178387264},{"term":"noticiero","score":45.435644178387264},{"term":"plausibiliteitscriterium","score":45.435644178387264},{"term":"beraadslag","score":45.40532785202067},{"term":"schijnhandel","score":45.392486749499675},{"term":"stemm","score":45.334586947029116},{"term":"hel","score":45.30500090146363},{"term":"waardepeildatum","score":45.29869772279434},{"term":"herzieningsverzoek","score":45.09605486088479},{"term":"kassalad","score":45.09605486088479},{"term":"onmiddel","score":45.08685666180749},{"term":"visa","score":45.04871703029678},{"term":"’) [","score":45.0350500005137},{"term":"voorop","score":45.03096142266645},{"term":"aangehoud","score":44.99890216020273},{"term":"vluchtauto","score":44.99032183261217},{"term":"neg","score":44.96849288293595},{"term":"aanpak","score":44.96241015712492},{"term":"aannemingsbedrijf","score":44.93276688280377},{"term":"geplaatst","score":44.91828205475688},{"term":"belgisch","score":44.876525869865375},{"term":"werkwijz","score":44.87204625182645},{"term":"peildatum","score":44.761340622440386},{"term":"cautie","score":44.7213129245755},{"term":"dwaling","score":44.716288726302274},{"term":"rechtsvrag","score":44.69607501121749},{"term":"inzageverlen","score":44.681700893544544},{"term":"barendrecht","score":44.67818582216235},{"term":"unigarant","score":44.67818582216235},{"term":"onderbewindstell","score":44.66747631107948},{"term":"bescherm","score":44.630454246479744},{"term":"applicabl","score":44.5619292903867},{"term":"gedragsinterventie","score":44.48966926079614},{"term":"meisj","score":44.45281039087919},{"term":"afwez","score":44.42816631032083},{"term":"adidas","score":44.41869147959986},{"term":"rijksbelast","score":44.41861520904023},{"term":"inkomensgegeven","score":44.38865379151133},{"term":"laptop","score":44.373832713991234},{"term":"ghislain","score":44.36561934657107},{"term":"pueblo","score":44.36561934657107},{"term":"deelgenom","score":44.36438474249328},{"term":"termination","score":44.31943080312096},{"term":"‑","score":44.30806878109973},{"term":"functionel","score":44.25765710439216},{"term":"geldend","score":44.223594505977914},{"term":"bereikt","score":44.190186247235594},{"term":"tuin","score":44.186882659279505},{"term":"kler","score":44.1750107845525},{"term":"ê","score":44.16739958488087},{"term":"carri","score":44.11867182502554},{"term":"vakantie","score":44.10171931010864},{"term":"opa","score":44.100067140322615},{"term":"abab","score":44.07232136433599},{"term":"incom","score":44.04476672035525},{"term":"pension","score":44.04476672035525},{"term":"dsm","score":44.035623859231826},{"term":"verpleegd","score":44.01740059308838},{"term":"foto","score":44.01021509791799},{"term":"bos","score":43.99914511639498},{"term":"kleur","score":43.97289177441894},{"term":"kanker","score":43.945326571077665},{"term":"mist","score":43.93191302771815},{"term":"nemo","score":43.9186301427269},{"term":"verkocht","score":43.91023576782477},{"term":"xvi","score":43.90977078140868},{"term":"knelpunt","score":43.874528262680066},{"term":"stan","score":43.8725976132776},{"term":"doelstell","score":43.85216652067976},{"term":"rioleringswerkzam","score":43.818654676189205},{"term":"totstandkomingsgeschiedenis","score":43.65272304453521},{"term":"openstaand","score":43.63774586216883},{"term":"alles","score":43.6085306822754},{"term":"onthoud","score":43.59132027329042},{"term":"papia","score":43.5785265531686},{"term":"onbepaald","score":43.56596875550836},{"term":"woonbron","score":43.56246280456685},{"term":"thema","score":43.458352823587745},{"term":"landbouwvrijstell","score":43.42685659786412},{"term":"deelt","score":43.42319902876292},{"term":"operandi","score":43.411201000579574},{"term":"werkgemeenschap","score":43.35620263670743},{"term":"functie","score":43.35172362060108},{"term":"hoorplicht","score":43.34918425443571},{"term":"kennisnem","score":43.32147061924278},{"term":"bacardi","score":43.27299147455294},{"term":"] )","score":43.27245595813291},{"term":"_NUMter","score":43.2279628564544},{"term":"pistol","score":43.17797172080135},{"term":"verhuiz","score":43.1620430676357},{"term":"deelconclusie","score":43.14922402224657},{"term":"amstelland","score":43.11756026412595},{"term":"nedam","score":43.038492101162845},{"term":"traject","score":43.03124142622353},{"term":"artt","score":43.0030622858266},{"term":"modus","score":43.0030622858266},{"term":"”)","score":42.98907312115748},{"term":"provisionel","score":42.98195405742322},{"term":"doter","score":42.955608458833865},{"term":"bvr","score":42.953981915459345},{"term":"sur","score":42.940068857954046},{"term":"gunningscriterium","score":42.9262040260481},{"term":"jvc","score":42.91238708657575},{"term":"toewijz","score":42.877532909217024},{"term":"hul","score":42.8575917091431},{"term":"bened","score":42.83974806045308},{"term":"echtscheid","score":42.8179269585701},{"term":"amstelhuy","score":42.77373459338455},{"term":"enk","score":42.77373459338455},{"term":"vres","score":42.77162577064623},{"term":"makelaardij","score":42.77011976259678},{"term":"accountant","score":42.76280829015985},{"term":"consultant","score":42.750202708998344},{"term":"telecom","score":42.71722642989802},{"term":"vind","score":42.703073153998275},{"term":"ben","score":42.667114022927656},{"term":"distributie","score":42.60709801633721},{"term":"terugker","score":42.564182922041766},{"term":"fw","score":42.56136151690718},{"term":"hoofdsom","score":42.55948146424356},{"term":"deelgeschilprocedur","score":42.48749823355452},{"term":"concret","score":42.45872276212595},{"term":"xxx","score":42.432070094908816},{"term":"verlen","score":42.40554111203294},{"term":"gemeenschap","score":42.4040285288934},{"term":"effectiv","score":42.40160536272758},{"term":"start","score":42.3803964180033},{"term":"gekwalificeerd","score":42.3803964180033},{"term":"plek","score":42.365908903340106},{"term":"stoornis","score":42.34785834221961},{"term":"tactisch","score":42.33538865119756},{"term":"kledingstuk","score":42.32346613864501},{"term":"son","score":42.30564873724749},{"term":"waarheid","score":42.2896400400593},{"term":"fiducia","score":42.28617780489124},{"term":"asset","score":42.246824147688706},{"term":"ballast","score":42.240988989834065},{"term":"peter","score":42.240988989834065},{"term":"luidend","score":42.229344149004234},{"term":"echter","score":42.22300152646676},{"term":"vertegenwoordiger","score":42.208380523238475},{"term":"armband","score":42.17735785769551},{"term":"real","score":42.17162308968812},{"term":"_NUMj","score":42.16589653180237},{"term":"verkeersteken","score":42.16589653180237},{"term":"philip","score":42.143071933494944},{"term":"uitgav","score":42.12842053653981},{"term":"vernietigd","score":42.115981063439385},{"term":"briefj","score":42.05862122454471},{"term":"nten","score":42.02939049385957},{"term":"opengebrok","score":42.00879359060353},{"term":"shell","score":42.003295307950864},{"term":"lsg","score":42.00024726547826},{"term":"hoofddorp","score":41.997804572702975},{"term":"purchas","score":41.997804572702975},{"term":"legesverorden","score":41.98137744489236},{"term":"volvo","score":41.9595790258221},{"term":"verdeeld","score":41.9555512596628},{"term":"further","score":41.95414793198026},{"term":"ondernemer","score":41.95388570335159},{"term":"this","score":41.94639755817963},{"term":"obligation","score":41.9433078169448},{"term":"gebouw","score":41.934771577190084},{"term":"onafhank","score":41.9256558043825},{"term":"beroepschrift","score":41.925100670949625},{"term":"dringend","score":41.885249878214395},{"term":"roosendal","score":41.87887689368107},{"term":"],,","score":41.86689197776576},{"term":"stomaveren","score":41.86689197776576},{"term":"zev","score":41.86148907970918},{"term":"nationaliteit","score":41.85985455860931},{"term":"verwerp","score":41.8541379389584},{"term":"kwijting","score":41.832149142219556},{"term":"verhoogd","score":41.80783002812917},{"term":"my","score":41.77374600648429},{"term":"vrouwelijk","score":41.742738099267015},{"term":"luchthav","score":41.72219890137886},{"term":"unie","score":41.641611938691454},{"term":"“ [","score":41.61606506927155},{"term":"woz","score":41.58870139203877},{"term":"verkeersregel","score":41.57636251007167},{"term":"psycholog","score":41.56168473039851},{"term":"rijbevoegd","score":41.55174704777416},{"term":"semiconductor","score":41.49908501148876},{"term":"maximumsnel","score":41.454772601334184},{"term":"geeft","score":41.446639386747364},{"term":"invoer","score":41.4460198557141},{"term":"ivrk","score":41.43565667486878},{"term":"ligt","score":41.42875156852812},{"term":"vak","score":41.42500195957976},{"term":"zorgkort","score":41.40715299130891},{"term":"milieugevar","score":41.38586402754022},{"term":"water","score":41.32782510061776},{"term":"vennootschapp","score":41.321951384817446},{"term":"gedragsdeskund","score":41.31358147301009},{"term":"star","score":41.29973264209621},{"term":"gesned","score":41.2767572773792},{"term":"european","score":41.23573127671044},{"term":"maandelijk","score":41.21595060927969},{"term":"aflop","score":41.20590323974403},{"term":"mauric","score":41.17148713559378},{"term":"zorgleefplan","score":41.17148713559378},{"term":"http","score":41.15936736285113},{"term":"hypothetisch","score":41.15047847030426},{"term":"inbewaringstell","score":41.14160928706966},{"term":"besmet","score":41.12834227656569},{"term":"rechtsverwerk","score":41.11511912402769},{"term":"geschot","score":41.103011650402635},{"term":"vond","score":41.07709215277896},{"term":"ondervrijwar","score":41.06978248056674},{"term":"geschilomschrijv","score":41.06978248056674},{"term":"institutional","score":41.06978248056674},{"term":"agroenergy","score":41.06978248056674},{"term":"standplat","score":41.0237611561484},{"term":"verstur","score":41.01946239162934},{"term":"dienstverband","score":41.00288314789117},{"term":"deutsch","score":41.00231338358286},{"term":"fen","score":40.97247840243215},{"term":"massachusett","score":40.97141301209776},{"term":"lippen","score":40.97141301209776},{"term":"xengo","score":40.97141301209776},{"term":"_NUMvor","score":40.97141301209776},{"term":"verbruik","score":40.96399483615561},{"term":"nasalaris","score":40.93023936157215},{"term":"verkoper","score":40.918916398859466},{"term":"antwerp","score":40.91765413881469},{"term":"boeking","score":40.90928593492579},{"term":"soort","score":40.88047801412245},{"term":"rigas","score":40.87616691715402},{"term":"boetebed","score":40.8635711516313},{"term":"ze","score":40.822213998542836},{"term":"maatstav","score":40.78400772064748},{"term":"poeldijk","score":40.78385194115377},{"term":"wilhelmus","score":40.78385194115377},{"term":"geldlen","score":40.732873146219504},{"term":"hard","score":40.720496096058525},{"term":"app","score":40.701605706212355},{"term":"d_NUM","score":40.701605706212355},{"term":"votron","score":40.694293051704726},{"term":"indicatiebesluit","score":40.685764101347964},{"term":"poort","score":40.68181348374287},{"term":"nn_NUM","score":40.666049916543365},{"term":"schuldsaner","score":40.66211871229574},{"term":"gestuurd","score":40.657138641125066},{"term":"kl","score":40.61136156466977},{"term":"retail","score":40.60748371657157},{"term":"binnentred","score":40.58429524583427},{"term":"ongeschrev","score":40.57659553219593},{"term":"voormal","score":40.53528973765678},{"term":"geluidsmateriaal","score":40.522817810184875},{"term":"vertolk","score":40.522817810184875},{"term":"ntimeerde_NUM","score":40.522817810184875},{"term":"deterink","score":40.44062088762053},{"term":"rapportag","score":40.371239502105084},{"term":"vrij","score":40.35078284503747},{"term":"gedod","score":40.32325203150869},{"term":"ams","score":40.31603832351209},{"term":"bord","score":40.30164981878869},{"term":"warranties","score":40.282689687164265},{"term":"cedh","score":40.282689687164265},{"term":"waarover","score":40.25371400509387},{"term":"oplever","score":40.22004782155592},{"term":"tsb","score":40.2067362632114},{"term":"muziekvri","score":40.2067362632114},{"term":"intrak","score":40.13265842544028},{"term":"opgav","score":40.123274368139604},{"term":"schol","score":40.11923443025029},{"term":"vervoerder","score":40.119162033095854},{"term":"daarin","score":40.09740117733705},{"term":"hr","score":40.08600275702243},{"term":"toebehoord","score":40.08500298690332},{"term":", (…)","score":40.07481189858812},{"term":"opinion","score":40.06464670887343},{"term":"rentray","score":40.0603657707031},{"term":"werkingssfer","score":40.04776192229893},{"term":"span","score":40.03094811023643},{"term":"condition","score":40.01420467850571},{"term":"bedrijfsgroep","score":39.989774278472524},{"term":"that","score":39.9574631259632},{"term":"rechtsstrijd","score":39.898922924363745},{"term":"emm","score":39.88272201116766},{"term":"jeugd","score":39.86658645076391},{"term":"geacht","score":39.831375668703515},{"term":"gesticht","score":39.80585815687793},{"term":"markestat","score":39.78745043608592},{"term":"kostenvergoed","score":39.764832156209174},{"term":"onderhoud","score":39.729816146166065},{"term":"zei","score":39.7138314778205},{"term":"afdracht","score":39.70561798565018},{"term":"vasthoud","score":39.702525600494226},{"term":"organisaties","score":39.702525600494226},{"term":"inhoudingsplicht","score":39.69634799308873},{"term":"#","score":39.69326276347183},{"term":"covenant","score":39.659771592829536},{"term":"belverlies","score":39.659771592829536},{"term":"keuz","score":39.65723209005223},{"term":"bovendien","score":39.614958207839905},{"term":"zilver","score":39.58975622715716},{"term":"liander","score":39.58375022002277},{"term":"garanties","score":39.57775321740021},{"term":"tijdelijk","score":39.56240794975737},{"term":"oordeelt","score":39.55415712280258},{"term":"verstekverlen","score":39.53730560926877},{"term":"eindexamen","score":39.53730560926877},{"term":"uitgegled","score":39.53730560926877},{"term":"gemiddeld","score":39.49983661312496},{"term":"reclam","score":39.497661553899775},{"term":"das","score":39.42779078173821},{"term":"cockpitbelang","score":39.419643469808925},{"term":"kunstfout","score":39.419643469808925},{"term":"procestuk","score":39.36249888489684},{"term":"travelex","score":39.36249888489684},{"term":"botsing","score":39.3506179699995},{"term":"capital","score":39.34778811491154},{"term":"set","score":39.33085089835638},{"term":"oplevert","score":39.330464772326145},{"term":"ljn","score":39.313740703610605},{"term":"hefco","score":39.30642248586038},{"term":"radiostation","score":39.30642248586038},{"term":"beoordelingsmethodiek","score":39.30642248586038},{"term":"beroepsgrond","score":39.289247659628614},{"term":"buitenspor","score":39.283247956421505},{"term":"kopie","score":39.22708679685464},{"term":"overlegg","score":39.21648029398614},{"term":"zaandijk","score":39.197319553347754},{"term":"vidomes","score":39.197319553347754},{"term":"stadsbeher","score":39.197319553347754},{"term":"verkeerd","score":39.18718849352427},{"term":"gatt","score":39.144220822049554},{"term":"appeldagvaard","score":39.14105937000157},{"term":"aankond","score":39.106288171441285},{"term":"villapark","score":39.092045593913944},{"term":"geheim","score":39.01154690047525},{"term":"uitvaard","score":38.95986659501483},{"term":"schuldenaarschap","score":38.94075303303327},{"term":"reparatie","score":38.93937999453461},{"term":"gecr","score":38.93682653131352},{"term":"sen","score":38.91645729569868},{"term":"anderzijd","score":38.909222776766576},{"term":"passagier","score":38.90631144371504},{"term":"verschoningsverzoek","score":38.89197147041793},{"term":"ontploff","score":38.88861789195843},{"term":"levensgevar","score":38.873514035120294},{"term":"tramban","score":38.843970446378606},{"term":"mor","score":38.831026912768074},{"term":"hal","score":38.818119417173214},{"term":"vermop","score":38.79672537547419},{"term":"parkwijk","score":38.79672537547419},{"term":"kilo","score":38.78652707186146},{"term":"verstekvonnis","score":38.76690716741219},{"term":"geproduceerd","score":38.75713318576229},{"term":"planning","score":38.71827434949295},{"term":"nvbw","score":38.70441039947393},{"term":"sfer","score":38.70139159907603},{"term":"gezegd","score":38.68635502867467},{"term":"kenmerk","score":38.67347398668162},{"term":"svea","score":38.659296767380305},{"term":"ambulancedienst","score":38.659296767380305},{"term":"tak","score":38.6480483770555},{"term":"koopovereenkomst","score":38.5924780008225},{"term":")(","score":38.580493507441126},{"term":"mee","score":38.5760077429958},{"term":"kinderopvangtoeslag","score":38.571164057147705},{"term":"hala","score":38.57105511176143},{"term":"erd","score":38.56650747920621},{"term":"opdrachtnemer","score":38.53868128003277},{"term":"gecombineerd","score":38.52944873121377},{"term":"beoordelingsprocedur","score":38.52788889940513},{"term":"holdi","score":38.52788889940513},{"term":"com","score":38.52714391985643},{"term":"studiefinancier","score":38.520237443320475},{"term":"gefailleerd","score":38.499589275875685},{"term":"beeld","score":38.49773433488651},{"term":"verniel","score":38.49501523306737},{"term":"jouw","score":38.49501523306737},{"term":"gsm","score":38.463142554470664},{"term":"synthes","score":38.443376268505034},{"term":"extendas","score":38.443376268505034},{"term":"kamp","score":38.41804624817779},{"term":"brabanthall","score":38.40199630210803},{"term":"observatie","score":38.39123050528903},{"term":"rot","score":38.38900395360337},{"term":"ingaand","score":38.37123601766968},{"term":"hierover","score":38.35464423110472},{"term":"mdma","score":38.34033078666512},{"term":"gdf","score":38.320910284944276},{"term":"finish","score":38.320910284944276},{"term":"luxemburg","score":38.318400907174144},{"term":"waterschap","score":38.24257951975546},{"term":"zittingsvertegenwoordiger","score":38.241958359992154},{"term":"bevond","score":38.22184704850385},{"term":"grenkefinanc","score":38.20324814548443},{"term":"removal","score":38.20324814548443},{"term":"verhal","score":38.18801529078751},{"term":"extra","score":38.18605535191251},{"term":"bergschenhoek","score":38.16503106815214},{"term":"rijnvaartakt","score":38.16503106815214},{"term":"bankreken","score":38.164936822276765},{"term":"tevergef","score":38.1555503504944},{"term":"aangeduid","score":38.138081193941574},{"term":"special","score":38.13669721268601},{"term":"xploiteerd","score":38.128346478964076},{"term":"gebied","score":38.116004392902354},{"term":"zit","score":38.1098248772077},{"term":"emmaus","score":38.09002716153589},{"term":"stomerij","score":38.05321688376045},{"term":"vuurwerk","score":38.04170049183936},{"term":"prestaties","score":38.03558272363176},{"term":"gebrok","score":38.00917998750636},{"term":"historisch","score":37.98898812231714},{"term":"onregelmat","score":37.964891663903614},{"term":"beroepsarbeid","score":37.94542085608226},{"term":"achternam","score":37.932987207623576},{"term":"hypotheekbedrag","score":37.91033273679268},{"term":"verrekenvorder","score":37.91033273679268},{"term":"uiter","score":37.903399553789406},{"term":"hetzelfd","score":37.89710721850287},{"term":"hur","score":37.89303992302993},{"term":"bezien","score":37.88786814569406},{"term":"_NUMbis","score":37.88364041843979},{"term":"ondergeschikt","score":37.88364041843979},{"term":"hallmark","score":37.87565026958946},{"term":"laum","score":37.87565026958946},{"term":"culpa","score":37.87565026958946},{"term":"artos","score":37.87565026958946},{"term":"jean","score":37.807465516356785},{"term":"doordat","score":37.80729212710988},{"term":"ingedeeld","score":37.80208419516307},{"term":"pag","score":37.801482395156306},{"term":"stil","score":37.79824173569372},{"term":"uitoefen","score":37.76640468284883},{"term":"ven","score":37.75431672996192},{"term":"horeca","score":37.724042224041895},{"term":"gwk","score":37.708008894406085},{"term":"kenbaarheidsvereist","score":37.675576146093434},{"term":"prestatie","score":37.671607016250775},{"term":"veroorzaakt","score":37.66898162429516},{"term":", -","score":37.63272682122444},{"term":"srlev","score":37.61174395075158},{"term":"computerbestand","score":37.61174395075158},{"term":"voegingsincident","score":37.61174395075158},{"term":"bekendmak","score":37.56875470148382},{"term":"slotbepal","score":37.54924169004306},{"term":"spanj","score":37.48620354369928},{"term":"ca","score":37.45788484134704},{"term":"amfetamin","score":37.41577918939909},{"term":"rijnvaart","score":37.39845618570039},{"term":"opzicht","score":37.383601880078416},{"term":"molenbek","score":37.3691876608643},{"term":"sarl","score":37.3691876608643},{"term":"meerdad","score":37.3691876608643},{"term":"summier","score":37.315817072184785},{"term":"hypotheekbank","score":37.31149357508063},{"term":"motor","score":37.305617321651695},{"term":"telefon","score":37.284778555415485},{"term":"maakt","score":37.27869251526729},{"term":"daisycon","score":37.254888122167486},{"term":"strekt","score":37.22395095032616},{"term":"openbaarmak","score":37.213311988012926},{"term":"asr","score":37.18850314272163},{"term":"aux","score":37.171933528175956},{"term":"huurovereenkomst","score":37.168648476704455},{"term":"onderzoeksopdracht","score":37.1447840216162},{"term":"bezocht","score":37.13771401065289},{"term":"installned","score":37.11787801166792},{"term":"erf","score":37.10528578278017},{"term":"voordeur","score":37.102057372596846},{"term":"second","score":37.09399772852413},{"term":"heerl","score":37.07472058278034},{"term":"gom","score":37.03857824046345},{"term":"doesburg","score":37.03857824046345},{"term":"opgeslag","score":37.01428205721327},{"term":"aa","score":37.012703836636476},{"term":"zvw","score":37.00324756745695},{"term":"e-","score":36.9609688414982},{"term":"dijk","score":36.94697465812815},{"term":"aflever","score":36.945422766741075},{"term":"publicatie","score":36.936124041127634},{"term":"dermat","score":36.936124041127634},{"term":"samenwerkingsverband","score":36.89148243865516},{"term":"recron","score":36.88600298876298},{"term":"hofstad","score":36.86131149135344},{"term":"uitgevoerd","score":36.829933893449144},{"term":"johan","score":36.76452890469877},{"term":"commerci","score":36.76195304866559},{"term":"econvert","score":36.740813366177434},{"term":"kantoorplat","score":36.69393741246819},{"term":"fax","score":36.67835697220341},{"term":"aspo","score":36.647782655949754},{"term":"eenmanszak","score":36.640798010804055},{"term":"final","score":36.613572399697176},{"term":"paper","score":36.602327240887945},{"term":"other","score":36.59931721711148},{"term":"plant","score":36.589368730903104},{"term":"ontwikkelingsmaatschappij","score":36.57985522570047},{"term":"definition","score":36.53540996834505},{"term":"arbeidskost","score":36.53540996834505},{"term":"gunst","score":36.5037063382889},{"term":"beroepsvervoer","score":36.46995282616113},{"term":"straatverbod","score":36.46995282616113},{"term":"languag","score":36.4270949544187},{"term":"tap","score":36.41304311639486},{"term":"lr","score":36.38484073477348},{"term":"autoband","score":36.38484073477348},{"term":"dak","score":36.365785182739806},{"term":"gerling","score":36.3639347268252},{"term":"landlord","score":36.34317339829148},{"term":"zelf","score":36.34283307070706},{"term":"juncto","score":36.31377578191025},{"term":"toevertrouw","score":36.302076865216996},{"term":"apeldoorn","score":36.2558990734065},{"term":"almer","score":36.25198326301057},{"term":"schetsontwerp","score":36.24146874326444},{"term":"spar","score":36.21561937620556},{"term":"periodiek","score":36.20529012579167},{"term":"adviez","score":36.18856177585139},{"term":"sluit","score":36.1733826032703},{"term":"recorded","score":36.16251681831232},{"term":")],","score":36.16251681831232},{"term":"knipper","score":36.16251681831232},{"term":"voith","score":36.16251681831232},{"term":"rijnvaartverklar","score":36.16251681831232},{"term":"marktrent","score":36.14309927479547},{"term":"jos","score":36.14309927479547},{"term":"beroofd","score":36.14006086255736},{"term":"parlementair","score":36.13498954650564},{"term":"elektriciteit","score":36.12613018518743},{"term":"afzien","score":36.10092531288051},{"term":"gren","score":36.09966922871255},{"term":"vermeend","score":36.09088766645881},{"term":"taxateur","score":36.082125340939534},{"term":"uitdruk","score":36.069649823015986},{"term":"wetgeschiedenis","score":36.066662018892515},{"term":"vandag","score":36.055952946583794},{"term":"bua","score":36.04785317985173},{"term":"disputes","score":36.029161530599815},{"term":"coltex","score":36.029161530599815},{"term":"gebeurteniss","score":36.011480592247885},{"term":"roep","score":35.996765676818036},{"term":"allocatie","score":35.99212402313292},{"term":"nrs","score":35.96506785778727},{"term":"zijden","score":35.95900081582328},{"term":"certificat","score":35.944477350548624},{"term":"delfland","score":35.93741126048357},{"term":"conservatoir","score":35.93482425385771},{"term":"zuidlar","score":35.91939318830294},{"term":"broek","score":35.90361161048829},{"term":"fitnes","score":35.883678480783985},{"term":"gemeentebelast","score":35.86597931440242},{"term":"schot","score":35.83249862640316},{"term":"reden","score":35.82840077648116},{"term":"overvloed","score":35.80207132930381},{"term":"getracht","score":35.799740326906836},{"term":"dekking","score":35.77650471117764},{"term":"neder","score":35.76192264043875},{"term":"activa","score":35.7614736196354},{"term":"wens","score":35.75801291285861},{"term":"onderligg","score":35.75110046874204},{"term":"reiskost","score":35.7292895379616},{"term":"hebt","score":35.72471287842342},{"term":"beheersmaatschappij","score":35.711217208030945},{"term":"ah","score":35.688286901028235},{"term":"onbevoegdverklar","score":35.67788353175573},{"term":"houder","score":35.67697147622633},{"term":"verlaagd","score":35.670197554200996},{"term":"olympia","score":35.66135456432282},{"term":"court","score":35.658933114202426},{"term":"binnengekom","score":35.613077725955115},{"term":"goedkeur","score":35.58758228145834},{"term":"communicatie","score":35.57544601508027},{"term":"maw","score":35.56404873706336},{"term":"betuw","score":35.56404873706336},{"term":"daadwerk","score":35.52775275825083},{"term":"ro","score":35.49150779399462},{"term":"inbrak","score":35.490429190779246},{"term":"overnam","score":35.46570125286867},{"term":"beach","score":35.45437594986197},{"term":"honderdtacht","score":35.43903064786166},{"term":"krijger","score":35.43903064786166},{"term":"uitbetal","score":35.42730095795259},{"term":"bewijslever","score":35.42305692572422},{"term":"pensioenvereven","score":35.4085735334696},{"term":"leverancier","score":35.3966332598104},{"term":"brasserie","score":35.39346015137973},{"term":"opmak","score":35.380862441641234},{"term":"zegg","score":35.375786057821365},{"term":"toegangspoort","score":35.33375668842784},{"term":"voeg","score":35.31942689842702},{"term":"delphi","score":35.319014644020555},{"term":"huisartsenpraktijk","score":35.30434468813798},{"term":"gebiedsverbod","score":35.30434468813798},{"term":"varend","score":35.30434468813798},{"term":"brv","score":35.289746119184464},{"term":"pas","score":35.23285941184835},{"term":"budget","score":35.21539247747921},{"term":"schorsingsverzoek","score":35.20361580153717},{"term":"at","score":35.1773160376574},{"term":"vloeistoff","score":35.161460541170214},{"term":"toegangsweiger","score":35.161460541170214},{"term":"cornelis","score":35.161460541170214},{"term":"hierop","score":35.15787093508343},{"term":"respondent","score":35.14753940250071},{"term":"abstracto","score":35.14753940250071},{"term":"lang","score":35.12121887869919},{"term":"analysis","score":35.11988943718594},{"term":"are","score":35.085629953335186},{"term":"aon","score":35.078886520999944},{"term":"data","score":35.07297941365799},{"term":"leasing","score":35.06534247993636},{"term":"naast","score":35.057088390228934},{"term":"ref","score":34.998524573109},{"term":"baseert","score":34.99695162267285},{"term":"charges","score":34.985337738689886},{"term":"inkoopprocedur","score":34.97220861489049},{"term":"opgebouwd","score":34.95994507239299},{"term":"ppg","score":34.946121493987825},{"term":"aanbested","score":34.94203951769688},{"term":"transacties","score":34.91860103507034},{"term":"onverwijld","score":34.91392977446705},{"term":"misleid","score":34.90646708573525},{"term":"factur","score":34.8941806652497},{"term":"brandveil","score":34.88187921047638},{"term":"] & [","score":34.88187921047638},{"term":"watersysteemheff","score":34.88187921047638},{"term":"verpachter","score":34.86919420214781},{"term":"behaald","score":34.79797962697404},{"term":"taxatierapport","score":34.79707270031785},{"term":"terug","score":34.79295612840725},{"term":"wast","score":34.75738001775612},{"term":"zienswijz","score":34.743029995801685},{"term":"dung","score":34.73308838705826},{"term":"ram","score":34.72250568975651},{"term":"intrum","score":34.70899187196647},{"term":"salduz","score":34.69701580815786},{"term":"alv","score":34.69701580815786},{"term":"goederencod","score":34.69701580815786},{"term":"’ [","score":34.68508736301894},{"term":"afnemer","score":34.65017736849907},{"term":"kredietrelatie","score":34.64958399007793},{"term":"lindorff","score":34.63784229211451},{"term":"fort","score":34.63784229211451},{"term":"pouvoir","score":34.626146370867545},{"term":"pour","score":34.626146370867545},{"term":"stond","score":34.618294884478644},{"term":"hieromtrent","score":34.61536426798587},{"term":"strafprocesrecht","score":34.61449587078835},{"term":"wuo","score":34.61449587078835},{"term":"vormvoorschrift","score":34.602890440454594},{"term":"afgetapt","score":34.59132973250662},{"term":"rijnvar","score":34.59132973250662},{"term":"hoofdverblijf","score":34.5757002223548},{"term":"uitgeoef","score":34.56456173384646},{"term":"pl_NUM","score":34.56028594509084},{"term":"exoneratie","score":34.53418514759454},{"term":"glad","score":34.53418514759454},{"term":"vandor","score":34.52288569920811},{"term":"mv","score":34.511628650352456},{"term":"beslissingsbevoegd","score":34.511628650352456},{"term":"inkoopverklar","score":34.4670181629141},{"term":"vanweg","score":34.45956339276654},{"term":"mexico","score":34.455968426665216},{"term":"ijs","score":34.444959239998326},{"term":"properties","score":34.444959239998326},{"term":"horizon","score":34.444959239998326},{"term":"risicomanagement","score":34.433990306389},{"term":"computer","score":34.43243654227901},{"term":"laatstelijk","score":34.4258195018949},{"term":"thialf","score":34.42306133255349},{"term":"heijman","score":34.42306133255349},{"term":"no","score":34.42251508204568},{"term":"bedrijv","score":34.392897505348834},{"term":"bewoner","score":34.36756803658063},{"term":"andel","score":34.35831061755197},{"term":"obstakel","score":34.35831061755197},{"term":"caus","score":34.32645191106959},{"term":"ingenieursbureau","score":34.32645191106959},{"term":"digital","score":34.32301963646516},{"term":"immuniteit","score":34.31590708474725},{"term":", (…),","score":34.31590708474725},{"term":"vrijwill","score":34.298928015755735},{"term":"travel","score":34.284493185145365},{"term":"lodder","score":34.26373185661165},{"term":"rabohypotheekbank","score":34.25340482403873},{"term":"bouwhoogt","score":34.24311321874836},{"term":"ta","score":34.24311321874836},{"term":"familiezak","score":34.192178209145105},{"term":"verblijft","score":34.17982230212659},{"term":"internet","score":34.17516210742471},{"term":"uitgeverij","score":34.172043905381145},{"term":"ruiz","score":34.172043905381145},{"term":"winstoogmerk","score":34.16202720158461},{"term":"verblijfplat","score":34.13884388245953},{"term":"valselijk","score":34.11965919340556},{"term":"abvakabo","score":34.11243929573097},{"term":"]\",","score":34.07335079485997},{"term":"lotsverbond","score":34.06365773311563},{"term":"`","score":34.044365062124754},{"term":"precies","score":34.039323033446564},{"term":"verhuurder","score":33.99678135061659},{"term":"bouwactiviteit","score":33.97780131719867},{"term":"personenvervoer","score":33.97780131719867},{"term":"invester","score":33.97604824236917},{"term":"coating","score":33.96841163817189},{"term":"uitrit","score":33.95905125616316},{"term":"releas","score":33.95905125616316},{"term":"getuigenverhor","score":33.953952807802786},{"term":"appelant","score":33.940417655885234},{"term":"huissleutel","score":33.93114407817622},{"term":"vermeent","score":33.91268248145308},{"term":"langdur","score":33.90794591084674},{"term":"criterium","score":33.90141648162868},{"term":"explot","score":33.897793621132855},{"term":"register","score":33.89128072959901},{"term":"zn","score":33.88520137325709},{"term":"hoofdprocedur","score":33.88520137325709},{"term":"tenetur","score":33.876096662171626},{"term":"fm","score":33.848947155611874},{"term":"onderworp","score":33.833849735848204},{"term":"inhoudt","score":33.8132329657777},{"term":"publiek","score":33.79766323628239},{"term":"zekerheidsstell","score":33.795374303411855},{"term":"fashion","score":33.77772719370265},{"term":"verweerschrift","score":33.77424459445609},{"term":"katwijk","score":33.734058300380035},{"term":"pijn","score":33.733202216113675},{"term":"criteria","score":33.69791092239931},{"term":"nogmal","score":33.69171524092655},{"term":"sauna","score":33.68248109875891},{"term":"windpark","score":33.67397045475332},{"term":"spor","score":33.66019743483189},{"term":"vernietigingsgrond","score":33.65702125712696},{"term":"bouwplann","score":33.65702125712696},{"term":"zambrano","score":33.640167280214285},{"term":"besluitvorm","score":33.633668981162366},{"term":"indirect","score":33.632313278726066},{"term":"distribution","score":33.61506253120274},{"term":"woonstad","score":33.606740750554565},{"term":"ontkenn","score":33.58113412936208},{"term":"documentatie","score":33.579796112958896},{"term":"verblijfsvergunn","score":33.57578474708703},{"term":"politieonderzoek","score":33.57368256480573},{"term":"kwestie","score":33.56310856268127},{"term":"verdenk","score":33.525979172932466},{"term":"exclusiviteit","score":33.5247684377309},{"term":"gehuwd","score":33.50622929739167},{"term":"schoongemaakt","score":33.50060723330961},{"term":"guarantee","score":33.452860867391905},{"term":"dynamisch","score":33.444976500173105},{"term":"delay","score":33.421446967790025},{"term":"circa","score":33.40955765528371},{"term":"wachtgeld","score":33.367251945597104},{"term":"heb","score":33.35121659002126},{"term":"geschiktheidseis","score":33.3443218976594},{"term":"bewijsregel","score":33.321565783491636},{"term":"rechtsverker","score":33.26171342423359},{"term":"meppel","score":33.24693506922542},{"term":"gedingstuk","score":33.23214081355322},{"term":"opdrachtgever","score":33.225401132093545},{"term":"abstract","score":33.224903146458146},{"term":"economic","score":33.21030457750463},{"term":"wm","score":33.19577670407726},{"term":"opvoed","score":33.18519978867954},{"term":"daaronder","score":33.18095961261807},{"term":"postbank","score":33.1741159601568},{"term":"nova","score":33.16693032797894},{"term":"causaliteit","score":33.152610491720964},{"term":"vermist","score":33.12417425985733},{"term":"kantoorkost","score":33.117107118779955},{"term":"franc","score":33.103022585867336},{"term":"hema","score":33.103022585867336},{"term":"handelsondernem","score":33.103022585867336},{"term":"option","score":33.08201899949038},{"term":"pp","score":33.08201899949038},{"term":"geboortegegeven","score":33.061161441545735},{"term":"tour","score":33.05424102325199},{"term":"ierland","score":32.97241776949301},{"term":"som","score":32.96988795588198},{"term":"pm","score":32.95899492830825},{"term":"zacht","score":32.95899492830825},{"term":"conclusion","score":32.95230596232645},{"term":"manag","score":32.93897260704517},{"term":"stein","score":32.91908303142917},{"term":"zien","score":32.90828038237942},{"term":"hit","score":32.90589619701005},{"term":"letselschad","score":32.87969515710378},{"term":"terrein","score":32.86614799562033},{"term":"lond","score":32.86019346329451},{"term":"aankop","score":32.83882129470639},{"term":"grondboorbedrijf","score":32.79595538924891},{"term":"parketgroothandel","score":32.79595538924891},{"term":"parketleverancier","score":32.79595538924891},{"term":"autodemontag","score":32.79595538924891},{"term":"moordt","score":32.79595538924891},{"term":"onevenred","score":32.79075773734761},{"term":"cour","score":32.78343021344928},{"term":"kasadministratie","score":32.777121062858946},{"term":"sp","score":32.739542283401406},{"term":"levensgezel","score":32.72092795274075},{"term":"handelszak","score":32.702428407993764},{"term":"rijd","score":32.7000853785227},{"term":"vierentwint","score":32.69628715712859},{"term":"kalendermaand","score":32.69628715712859},{"term":"uden","score":32.69015845223919},{"term":"rechtsgevolg","score":32.6893629488344},{"term":"sector","score":32.67386630264},{"term":"kennisgenom","score":32.61037290601484},{"term":"diem","score":32.58193028281776},{"term":"assessment","score":32.570142448398094},{"term":"hennepstek","score":32.55840075043468},{"term":"energy","score":32.55840075043468},{"term":"geregistreerd","score":32.53136825874829},{"term":"natur","score":32.50418343910419},{"term":"excuses","score":32.494630233358436},{"term":"bezig","score":32.481754426597675},{"term":"dergelijk","score":32.43285788422016},{"term":"defendant","score":32.40979894506785},{"term":"syri","score":32.404227912826215},{"term":"tussenvonniss","score":32.36002821828726},{"term":"chang","score":32.354548764709165},{"term":"behouden","score":32.32182279334915},{"term":"center","score":32.284211908985114},{"term":"duidelijk","score":32.221599890878764},{"term":"kunst","score":32.210264503161156},{"term":"bewijsbeslag","score":32.210264503161156},{"term":"ontvangst","score":32.196339274559676},{"term":"begrip","score":32.167570654411904},{"term":"marin","score":32.16367167706852},{"term":"ambulanc","score":32.15853908385764},{"term":"vrijheidsontnem","score":32.148300166068914},{"term":"kol","score":32.12792657333491},{"term":"import","score":32.11273666746527},{"term":"operation","score":32.10769040891631},{"term":"- (","score":32.09584909153819},{"term":"afgegev","score":32.08570733204274},{"term":"zwart","score":32.07008410712676},{"term":"ev","score":32.03299775405114},{"term":"beweg","score":32.00909760318372},{"term":"nota","score":32.00097577298219},{"term":"taxaties","score":31.993909253180135},{"term":"zaanstad","score":31.989058807499415},{"term":"forcer","score":31.989058807499415},{"term":"detenlastelegg","score":31.985025173032586},{"term":"zaaksverlop","score":31.985025173032586},{"term":"overboet","score":31.985025173032586},{"term":"vproceskost","score":31.985025173032586},{"term":"omgevingsgetu","score":31.985025173032586},{"term":"scoreklassetoeslag","score":31.985025173032586},{"term":"gevestigdt","score":31.985025173032586},{"term":"_NUMdiefstal","score":31.985025173032586},{"term":"althansin","score":31.985025173032586},{"term":"meervoorschriftenverbond","score":31.985025173032586},{"term":"beloningskeuz","score":31.985025173032586},{"term":"waardenberg","score":31.985025173032586},{"term":"resolucion","score":31.985025173032586},{"term":"overwegin","score":31.985025173032586},{"term":"rechtsbevoegdheidfnv","score":31.985025173032586},{"term":"marketscan","score":31.985025173032586},{"term":"barges","score":31.985025173032586},{"term":"aantalomschrijvingstuksprijstotal","score":31.985025173032586},{"term":"personent","score":31.985025173032586},{"term":"kapgier","score":31.985025173032586},{"term":"isof","score":31.985025173032586},{"term":"tijdstippenin","score":31.985025173032586},{"term":"denizcilik","score":31.985025173032586},{"term":"hifiset","score":31.985025173032586},{"term":"longhi","score":31.985025173032586},{"term":"ofberoofd","score":31.985025173032586},{"term":"houdent","score":31.985025173032586},{"term":"meerverenigd","score":31.985025173032586},{"term":"iioverweg","score":31.985025173032586},{"term":"iprocesverlop","score":31.985025173032586},{"term":"woningbouwveren","score":31.91249962887513},{"term":"cvom","score":31.91249962887513},{"term":"pleidooi","score":31.899941194428184},{"term":"portugal","score":31.87494045284035},{"term":"compromis","score":31.870278447240146},{"term":"terugbetalingsverplicht","score":31.86562367516589},{"term":"pilot","score":31.86562367516589},{"term":"kapitaalverker","score":31.856335742050504},{"term":"beton","score":31.856335742050504},{"term":"meubel","score":31.856335742050504},{"term":"verteld","score":31.850582118754158},{"term":"geschrev","score":31.817724173506072},{"term":"allereerst","score":31.80396959300686},{"term":"detacher","score":31.796655120491792},{"term":"geslachtsnam","score":31.774013503585643},{"term":"plausibel","score":31.765004487806333},{"term":"motivatie","score":31.724795397424316},{"term":"onschuld","score":31.702687707104666},{"term":"protection","score":31.68511811076653},{"term":"brugg","score":31.676371748886154},{"term":"planvoorschrift","score":31.650284627983496},{"term":"seksuel","score":31.645387235163437},{"term":"heerhugowaard","score":31.590282627863488},{"term":"bit","score":31.564930364310314},{"term":"_NUMc","score":31.564551661150844},{"term":"proced","score":31.494241023125895},{"term":"stiefvader","score":31.494241023125895},{"term":"behandelverplicht","score":31.490134246303867},{"term":"enerzijd","score":31.484128847062163},{"term":"fin","score":31.48193751989325},{"term":"shares","score":31.47784753970966},{"term":"vervoermiddel","score":31.46154315175179},{"term":"wijl","score":31.44128649139993},{"term":"bh","score":31.43322197041453},{"term":"crvb","score":31.425928842741016},{"term":"_NUMverzoeker","score":31.409661028129022},{"term":"_NUMparketnummer","score":31.409661028129022},{"term":"gekennzeichnet","score":31.409661028129022},{"term":"enacqua","score":31.409661028129022},{"term":"meertijdstipp","score":31.409661028129022},{"term":"dsquared_NUM","score":31.409661028129022},{"term":"herindicatiebeschik","score":31.409661028129022},{"term":"risk","score":31.405165653703683},{"term":"bedekt","score":31.40117894215352},{"term":"beoordelingskader","score":31.39322137780613},{"term":"onderpand","score":31.338101717036274},{"term":"-/- €","score":31.338101717036274},{"term":"getroff","score":31.32569229468198},{"term":"way","score":31.30705357445026},{"term":"plaatsvervang","score":31.30319505301803},{"term":"director","score":31.295492866502293},{"term":"griffie","score":31.291194616038265},{"term":"inwerk","score":31.28781040391727},{"term":"ziektekostenverzeker","score":31.283976537580795},{"term":"schorst","score":31.264880355979564},{"term":"feitenvaststell","score":31.242124241811798},{"term":"zwembad","score":31.204576818016307},{"term":"staking","score":31.189688449989923},{"term":"bodemzak","score":31.189688449989923},{"term":"koning","score":31.178570463206967},{"term":"kvk","score":31.171181296909776},{"term":"veertig","score":31.152787615744966},{"term":"hieruit","score":31.148083893584225},{"term":"vaart","score":31.14546160477831},{"term":"coa","score":31.141805297195923},{"term":"nachtrust","score":31.141805297195923},{"term":"handhav","score":31.140443012559746},{"term":"doch","score":31.125204898893077},{"term":"altijd","score":31.08987343889904},{"term":"fonds","score":31.076742506151586},{"term":"ziet","score":31.07124089367121},{"term":"motorrijtuigenbelast","score":31.069599645634362},{"term":"whether","score":31.055364743481682},{"term":"security","score":31.051816546734536},{"term":"ligg","score":31.039226306621266},{"term":"verbind","score":31.00745489256552},{"term":"da","score":31.00606785960044},{"term":"begin","score":30.996569576626015},{"term":"thuiszorg","score":30.988656319141036},{"term":"getuige_NUM","score":30.985186104757567},{"term":"einddossier","score":30.9817198998659},{"term":"provision","score":30.978257695211582},{"term":"aangat","score":30.974799481572155},{"term":"winter","score":30.974799481572155},{"term":"hogerberoepschrift","score":30.974799481572155},{"term":"dalerpel","score":30.9633739255006},{"term":"wetartikel","score":30.9633739255006},{"term":"vluchtricht","score":30.9633739255006},{"term":"beoord","score":30.9633739255006},{"term":"newell","score":30.9633739255006},{"term":"nietopzet","score":30.9633739255006},{"term":"heintz","score":30.9633739255006},{"term":"), ([","score":30.9633739255006},{"term":"fleecedek","score":30.9633739255006},{"term":"grootmoeder","score":30.940434844647854},{"term":"relevantie","score":30.937019932497673},{"term":"pensioenrecht","score":30.876207039376816},{"term":",[","score":30.869525522138346},{"term":"transfer","score":30.85620696530225},{"term":"hill","score":30.83304082702052},{"term":"pr","score":30.83304082702052},{"term":"gered","score":30.820607738260243},{"term":"bruto","score":30.798521176665083},{"term":"apothek","score":30.78399341301593},{"term":"back","score":30.77751392885564},{"term":"begripp","score":30.76137618149643},{"term":"arbeidsongeschikt","score":30.74350332363517},{"term":"grootschal","score":30.735734999761057},{"term":"arbiter","score":30.73254520679707},{"term":"meld","score":30.717910586012735},{"term":"doorhal","score":30.716646918397572},{"term":"mobil","score":30.703988671769444},{"term":"referent","score":30.681964451194343},{"term":"bovenbedoeld","score":30.666331273556082},{"term":"alph","score":30.641486411060924},{"term":"nooit","score":30.63280443501029},{"term":"proximedia","score":30.62606221255966},{"term":"within","score":30.61991466942486},{"term":"hvjeu","score":30.610716910559354},{"term":"kgaa","score":30.598730811912695},{"term":"payrollkracht","score":30.598730811912695},{"term":"speeltuinveren","score":30.598730811912695},{"term":"sectorcompetentie","score":30.598730811912695},{"term":"northwest","score":30.598730811912695},{"term":"zorgverzekeraarsgroep","score":30.598730811912695},{"term":"rataregel","score":30.598730811912695},{"term":"aftrekpercentag","score":30.598730811912695},{"term":"serie","score":30.571182633417692},{"term":"result","score":30.559122315667693},{"term":"accord","score":30.547110287607545},{"term":"uitzendbureau","score":30.514323076010974},{"term":"vreemdelingenzak","score":30.496589034221802},{"term":"_NUMaa","score":30.478959208754844},{"term":"middelonderdel","score":30.473105548361062},{"term":"gerechtvaardigd","score":30.47275798453233},{"term":"lager","score":30.46568894431222},{"term":"due","score":30.455612787428677},{"term":"huis","score":30.43692725074529},{"term":"leeuw","score":30.432446649146947},{"term":"action","score":30.426682958613267},{"term":"should","score":30.41805813235647},{"term":"kwesties","score":30.398029445660242},{"term":"grondrecht","score":30.398029445660242},{"term":"schietincident","score":30.358368811375833},{"term":"what","score":30.34153060066096},{"term":"ons","score":30.301148283545746},{"term":"boord","score":30.294329256754324},{"term":"kavila","score":30.290429452258177},{"term":"belmij","score":30.290429452258177},{"term":"kombinatie","score":30.290429452258177},{"term":"aleida","score":30.290429452258177},{"term":"rubbermaid","score":30.290429452258177},{"term":"ofen","score":30.290429452258177},{"term":")€","score":30.290429452258177},{"term":"gehandicaptenparkeervergunn","score":30.290429452258177},{"term":"ecliptica","score":30.290429452258177},{"term":"slotbeschouw","score":30.290429452258177},{"term":"scheepshold","score":30.290429452258177},{"term":"amilo","score":30.290429452258177},{"term":"sigarettenkoker","score":30.290429452258177},{"term":"kraftwerk","score":30.290429452258177},{"term":"roemeni","score":30.288824668053994},{"term":"ivbpr","score":30.28607615663865},{"term":"rechtskracht","score":30.26417824919382},{"term":"ontbreekt","score":30.261851779812083},{"term":"inachtnem","score":30.25893462682398},{"term":"dvd","score":30.245147827427296},{"term":"maatgev","score":30.245147827427296},{"term":"resp","score":30.237028742634067},{"term":"gebruiksvergoed","score":30.231628201121623},{"term":"ruimt","score":30.231037673937767},{"term":"beroepsfout","score":30.202097761337875},{"term":"minst","score":30.198735241555667},{"term":"heijn","score":30.180802197204848},{"term":"cd","score":30.138659274165093},{"term":"desbetreff","score":30.120510526380198},{"term":"toedel","score":30.112617443972177},{"term":"weergev","score":30.094521740679056},{"term":"kop","score":30.084615725775418},{"term":"cie","score":30.053565473174075},{"term":"gezondheidszorg","score":30.048485031655076},{"term":"voldoet","score":30.04268108961192},{"term":"vertrouw","score":30.03081942097684},{"term":"maaskantj","score":30.02336666700913},{"term":"zweitbeg","score":30.02336666700913},{"term":"dadurch","score":30.02336666700913},{"term":"dopsleutel","score":30.02336666700913},{"term":"iet","score":30.007474921334364},{"term":"parkeerbelast","score":30.005645160470664},{"term":"effici","score":29.990670087778938},{"term":"(…),","score":29.946188218491685},{"term":"cs","score":29.938838276064736},{"term":"postnl","score":29.931506296855105},{"term":"vermogensrecht","score":29.916895878366972},{"term":"draagt","score":29.903411482824794},{"term":"terugwijz","score":29.89993983807293},{"term":"wijzigt","score":29.89270219885678},{"term":"overkom","score":29.875881970573072},{"term":"televisie","score":29.866312584468773},{"term":"hiermee","score":29.829917907886895},{"term":"betekenis","score":29.794190578410507},{"term":"racket","score":29.787800595696364},{"term":"lois","score":29.787800595696364},{"term":"applicables","score":29.787800595696364},{"term":"vervroegd","score":29.756100894178672},{"term":"uitdraai","score":29.72859879171761},{"term":"een","score":29.707132925759957},{"term":"after","score":29.69908663544407},{"term":"roll","score":29.681068563263434},{"term":"(…) [","score":29.678823894247376},{"term":"%-","score":29.665391035983493},{"term":"vogel","score":29.640919265136542},{"term":"bewust","score":29.63064298650496},{"term":"aangebod","score":29.597385985347284},{"term":"sterftegrondslag","score":29.577079564380714},{"term":"fulfillment","score":29.577079564380714},{"term":"warenmarkt","score":29.577079564380714},{"term":"einzureich","score":29.577079564380714},{"term":"risicopati","score":29.577079564380714},{"term":"speciality","score":29.577079564380714},{"term":"rwandaises","score":29.577079564380714},{"term":"platt","score":29.577079564380714},{"term":"bcol","score":29.577079564380714},{"term":"kennewegsteg","score":29.577079564380714},{"term":"pallandt","score":29.577079564380714},{"term":"inmiddel","score":29.566609974109937},{"term":"tw","score":29.56219754717899},{"term":"act","score":29.5535768515918},{"term":"gesprok","score":29.538260127326364},{"term":"proportionaliteit","score":29.512963477499625},{"term":"antwoordt","score":29.4960262412619},{"term":"achter","score":29.48888574838595},{"term":"uitslag","score":29.48548882263048},{"term":"]\"","score":29.4833857731227},{"term":"faillietverklar","score":29.481284196854617},{"term":"aangered","score":29.477085455791347},{"term":"rectificatie","score":29.46870556207451},{"term":"geteeld","score":29.447857667194892},{"term":"schadestaatprocedur","score":29.447857667194892},{"term":"oorsprong","score":29.443705417397073},{"term":"brussel","score":29.437487798591157},{"term":"tegenover","score":29.430145437774684},{"term":"octrooi","score":29.420970206967592},{"term":"aver","score":29.386459204772063},{"term":"brandcompartimenter","score":29.386459204772063},{"term":"baanstee","score":29.386459204772063},{"term":"tention","score":29.386459204772063},{"term":"gistel","score":29.386459204772063},{"term":"invorderingsbesliss","score":29.386459204772063},{"term":"ham","score":29.365885354371226},{"term":"ontdek","score":29.34975628326209},{"term":"ondernemingsvermog","score":29.343730168078842},{"term":"hengelo","score":29.343730168078842},{"term":"] (…)","score":29.33771613332171},{"term":"erbij","score":29.327719456294282},{"term":"lading","score":29.325724112023853},{"term":"ontruimd","score":29.321737400473687},{"term":"penitentiair","score":29.268428988937764},{"term":"wal","score":29.245037057557667},{"term":"schadeverzekeringmaatschappij","score":29.212436450792804},{"term":"substrates","score":29.212436450792804},{"term":"suez","score":29.212436450792804},{"term":"haarlemmerstrat","score":29.212436450792804},{"term":"oonk","score":29.212436450792804},{"term":"broer_NUM","score":29.212436450792804},{"term":"kinderpornografisch","score":29.20261989848514},{"term":"verwijt","score":29.20076123555042},{"term":"indicaties","score":29.193062706585753},{"term":"blijft","score":29.1929349759612},{"term":"kast","score":29.189248338621162},{"term":"beslaglegg","score":29.17973358278628},{"term":"_NUMi","score":29.15513552634022},{"term":"bruin","score":29.13822348656306},{"term":"instrument","score":29.113962079063523},{"term":"bemonster","score":29.091739755229938},{"term":"lot","score":29.08252881769016},{"term":"kas","score":29.073346074065128},{"term":"bijvoorbeeld","score":29.071053468510236},{"term":"utb","score":29.06784988932172},{"term":"sprl","score":29.05235103544573},{"term":"marktondernemer","score":29.05235103544573},{"term":"unterlag","score":29.05235103544573},{"term":"apothekersdijk","score":29.05235103544573},{"term":"fata","score":29.05235103544573},{"term":"telecommunicatiemiddel","score":29.05235103544573},{"term":"justitia","score":29.047782924869328},{"term":"beschuld","score":29.03508257327352},{"term":"cdw","score":29.033272618467485},{"term":"verplaatst","score":29.029655981501442},{"term":"zwitserland","score":29.02784929671332},{"term":"-[","score":29.024239187974423},{"term":"rijn","score":29.004460872404948},{"term":"afhandel","score":29.00087878242712},{"term":"procesbelang","score":28.997300964471748},{"term":"hek","score":28.990158103954528},{"term":"oudst","score":28.984812097193817},{"term":"overvall","score":28.984812097193817},{"term":"minimum","score":28.977698875129807},{"term":"formul","score":28.95469669815325},{"term":"petitum","score":28.938874803876626},{"term":"faciliteit","score":28.912689010623087},{"term":"hoofdentree","score":28.904135091138286},{"term":"vismarkt","score":28.904135091138286},{"term":"pl_NUMke","score":28.904135091138286},{"term":"opoefiet","score":28.904135091138286},{"term":"evaluatieprijs","score":28.904135091138286},{"term":"x_NUM","score":28.891903708077194},{"term":"[[","score":28.883285488993145},{"term":"st","score":28.821954562555703},{"term":"chiropractie","score":28.766149348164383},{"term":"heck","score":28.766149348164383},{"term":"obtenir","score":28.766149348164383},{"term":"lotos","score":28.766149348164383},{"term":"machinebouw","score":28.766149348164383},{"term":"identificatie","score":28.738800680380606},{"term":"eigendomsrecht","score":28.724074716806307},{"term":"vertoond","score":28.688379325431466},{"term":"woonlast","score":28.67871748133134},{"term":"roekelos","score":28.672293496007306},{"term":"smit","score":28.651510039581744},{"term":"langer","score":28.64729355102118},{"term":"korporal","score":28.63707230588924},{"term":"uitpad","score":28.63707230588924},{"term":"civielrecht","score":28.579103586836016},{"term":"identificatieplicht","score":28.57599477826766},{"term":"bewerk","score":28.569786809161563},{"term":"kilogram","score":28.546620670879832},{"term":"vertal","score":28.532806371719587},{"term":"uitzend","score":28.53127536887952},{"term":"dertig","score":28.529745146964167},{"term":"busscher","score":28.51582306225637},{"term":"kwartaalbedrag","score":28.51582306225637},{"term":"vandenberg","score":28.51582306225637},{"term":"pein","score":28.51582306225637},{"term":"cialit","score":28.51582306225637},{"term":"minimumsalaris","score":28.51582306225637},{"term":"verhinder","score":28.511443107485356},{"term":"slaagt","score":28.50522826998667},{"term":"respectiev","score":28.50221976140692},{"term":"voorzijd","score":28.493252046189333},{"term":"geconcludeerd","score":28.484590325517765},{"term":"procesrecht","score":28.48419771336062},{"term":"psychologisch","score":28.479680773987855},{"term":"zorgverzeker","score":28.47366874792771},{"term":"if","score":28.457197531108406},{"term":"biologisch","score":28.434881534331137},{"term":"teruggaf","score":28.427479606075764},{"term":"actiev","score":28.41125936503842},{"term":"such","score":28.40685084110034},{"term":"gardema","score":28.401506234576473},{"term":"controlebevind","score":28.401506234576473},{"term":"modelportefeuill","score":28.401506234576473},{"term":"martinus","score":28.401506234576473},{"term":"duru","score":28.401506234576473},{"term":"jacques","score":28.401506234576473},{"term":"bedrijfslev","score":28.396589409155844},{"term":"princip","score":28.38053488288183},{"term":"its","score":28.371813946507903},{"term":"global","score":28.300108812148608},{"term":"klantbehandel","score":28.293371792035924},{"term":"slopersbedrijf","score":28.293371792035924},{"term":"sharp","score":28.293371792035924},{"term":"_NUMg","score":28.263488639132472},{"term":"wez","score":28.24952213346012},{"term":"bill","score":28.24952213346012},{"term":"oogst","score":28.22731009307258},{"term":"new","score":28.208008555903373},{"term":"vweu","score":28.202516563522906},{"term":"marcel","score":28.190785203260823},{"term":"locatiekeuz","score":28.190785203260823},{"term":"kantonrechterkort","score":28.190785203260823},{"term":"bestuurderskaart","score":28.190785203260823},{"term":"kinderopvangcentrum","score":28.190785203260823},{"term":"rdinatieregel","score":28.190785203260823},{"term":"_NUMbij","score":28.190785203260823},{"term":"kaartlezer","score":28.190785203260823},{"term":"exploitant","score":28.177926261910365},{"term":"strafmat","score":28.169774082980865},{"term":"borg","score":28.15488571495448},{"term":"fond","score":28.132020775756942},{"term":"cz","score":28.09606030078935},{"term":"admissibility","score":28.09320487492196},{"term":"linq","score":28.09320487492196},{"term":"modelverorden","score":28.09320487492196},{"term":"herstal","score":28.09320487492196},{"term":"projectkwaliteitsplan","score":28.09320487492196},{"term":"bekeerl","score":28.09320487492196},{"term":"voorraadwaarder","score":28.09320487492196},{"term":"perlator","score":28.09320487492196},{"term":"madina","score":28.09320487492196},{"term":"debiteur","score":28.068386043484864},{"term":"reiz","score":28.052686051263663},{"term":"belastingaanslag","score":28.046168560105862},{"term":"bot","score":28.02540723157214},{"term":"beschad","score":28.024114406729897},{"term":"vingerafdruk","score":28.01250398587397},{"term":"duizend","score":28.011216708445936},{"term":"acknowledgement","score":28.000164843652172},{"term":"trivir","score":28.000164843652172},{"term":"woningcorporatiesector","score":28.000164843652172},{"term":"hanzevast","score":28.000164843652172},{"term":"appellant_NUM","score":28.000164843652172},{"term":"hiervan","score":27.94188915887705},{"term":"neemt","score":27.940582236259743},{"term":"slap","score":27.93121196719945},{"term":"bedien","score":27.922452836889445},{"term":"marokko","score":27.91496531220399},{"term":"renovatiebouw","score":27.911261318510505},{"term":"mutatiekader","score":27.911261318510505},{"term":"inreisverbod","score":27.908739952011977},{"term":"(‘","score":27.906253420580775},{"term":"belastingschuld","score":27.892614215438456},{"term":"boekenonderzoek","score":27.887669838546692},{"term":"you","score":27.879036739063842},{"term":"erfdienst","score":27.87165666154125},{"term":"tim","score":27.84596843119159},{"term":"lek","score":27.83138760054483},{"term":"marpro","score":27.826142089672913},{"term":"kenwod","score":27.826142089672913},{"term":"smallenbroek","score":27.826142089672913},{"term":"reparatiebedrijf","score":27.826142089672913},{"term":"ongewenst","score":27.820498296393097},{"term":"ggz","score":27.81687729414299},{"term":"stel","score":27.800036832920348},{"term":"ontkent","score":27.794045219185165},{"term":"agent","score":27.78806554808045},{"term":"zojuist","score":27.776141843989443},{"term":"nageleefd","score":27.769010303388626},{"term":"regiopolitie","score":27.767823359752963},{"term":"spoed","score":27.767823359752963},{"term":"correction","score":27.744498100632402},{"term":"vous","score":27.744498100632402},{"term":"vrijspraakoverweg","score":27.744498100632402},{"term":",-- (","score":27.727744178183166},{"term":"bijstand","score":27.717742222076005},{"term":") -","score":27.700932264725616},{"term":"identiteitsbewijs","score":27.6755083837527},{"term":"groeifinancier","score":27.66605667432584},{"term":"haagsch","score":27.66605667432584},{"term":"hoofdstraff","score":27.66605667432584},{"term":"stal","score":27.664022917962768},{"term":"overlever","score":27.664022917962768},{"term":"paard","score":27.65029815136356},{"term":"concurrentiebed","score":27.632095592676002},{"term":"jonger","score":27.59826064756288},{"term":"dreiging","score":27.591539210845326},{"term":"alteration","score":27.590576018360146},{"term":"eringa","score":27.590576018360146},{"term":"artiestenverlon","score":27.590576018360146},{"term":"verhes","score":27.590576018360146},{"term":"heinenoord","score":27.590576018360146},{"term":"beslissingskader","score":27.590576018360146},{"term":"strafged","score":27.58818413127849},{"term":"wob","score":27.58483279970673},{"term":"bladzijd","score":27.57035346957594},{"term":"uitnod","score":27.56813206999277},{"term":"courant","score":27.54711040920347},{"term":"order","score":27.54600807591897},{"term":"versie","score":27.533909067769002},{"term":"demonstration","score":27.517840730018396},{"term":"kapitalisatiedatum","score":27.517840730018396},{"term":"weerselo","score":27.517840730018396},{"term":"anspruch","score":27.517840730018396},{"term":"dean","score":27.517840730018396},{"term":"estro","score":27.517840730018396},{"term":"benadel","score":27.512035110125385},{"term":"veroorzak","score":27.461250537063318},{"term":"onderbouwd","score":27.453673695636592},{"term":"cvah","score":27.447658090395855},{"term":"eemshav","score":27.447658090395855},{"term":"aangedrag","score":27.428218770217505},{"term":"subsidie","score":27.380907469030397},{"term":"sjalon","score":27.379854987044492},{"term":"ƒ","score":27.37673790616323},{"term":"beweerd","score":27.36115345553192},{"term":"genworth","score":27.31427534139851},{"term":"aarl","score":27.31427534139851},{"term":"molenwiek","score":27.31427534139851},{"term":"opvolgster","score":27.31427534139851},{"term":"bernhov","score":27.31427534139851},{"term":"diagnos","score":27.312839922069056},{"term":"goud","score":27.306727865625522},{"term":"invorder","score":27.295554668352572},{"term":"cijfer","score":27.28745476468074},{"term":"betrekt","score":27.27333234451899},{"term":"ingebrekestell","score":27.259276093904557},{"term":"licens","score":27.25077794476935},{"term":"furnes","score":27.25077794476935},{"term":"bmax","score":27.25077794476935},{"term":"dezelfd","score":27.246404100923687},{"term":"geboortedag","score":27.227392698488046},{"term":"ontmoet","score":27.19486404071393},{"term":"onderwijsvrijstell","score":27.189234627435845},{"term":"aftrekgerechtigd","score":27.189234627435845},{"term":"mobility","score":27.189234627435845},{"term":"zorgkantor","score":27.175319997650348},{"term":"wmo","score":27.139495786251196},{"term":"nebo","score":27.129528701136483},{"term":"marinus","score":27.129528701136483},{"term":"lucent","score":27.129528701136483},{"term":"patron","score":27.099342223146948},{"term":"vishay","score":27.071553627389978},{"term":"regiecentrum","score":27.071553627389978},{"term":"korpschef","score":27.065347525118298},{"term":"out","score":27.06440870489471},{"term":"geschiedenis","score":27.05129597718102},{"term":"gerardus","score":27.015211873456582},{"term":"kalsbek","score":27.015211873456582},{"term":"hulpverlen","score":26.995738833879308},{"term":"dienstverlen","score":26.99298780204966},{"term":"rondom","score":26.981095729761567},{"term":"gas","score":26.97016011619457},{"term":"intocht","score":26.960413925080356},{"term":"hotelkamerveil","score":26.960413925080356},{"term":"begeleidingskost","score":26.960413925080356},{"term":"cod","score":26.958358012911468},{"term":"nadat","score":26.933401219371305},{"term":"cabana","score":26.907077430916033},{"term":"stadig","score":26.907077430916033},{"term":"vereinbarung","score":26.907077430916033},{"term":"onrechtstrek","score":26.907077430916033},{"term":"vakbeweg","score":26.907077430916033},{"term":"ambulant","score":26.906260895333546},{"term":"wederzijd","score":26.88054771885757},{"term":"gebeur","score":26.87878249374045},{"term":"achttien","score":26.877018306685784},{"term":"bekenn","score":26.863819869207326},{"term":"…","score":26.857680413768307},{"term":"cardea","score":26.85512645810951},{"term":"intratuin","score":26.85512645810951},{"term":"bijhorst","score":26.85512645810951},{"term":"moordrecht","score":26.85512645810951},{"term":"harman","score":26.85512645810951},{"term":"ass","score":26.81764500673049},{"term":"nek","score":26.809011907247648},{"term":"doorgan","score":26.804704655969335},{"term":"residential","score":26.804490842140932},{"term":"gts","score":26.804490842140932},{"term":"recherch","score":26.79267714845014},{"term":"betekent","score":26.787282189762422},{"term":"lod","score":26.77387366808805},{"term":"aanton","score":26.766215175771176},{"term":"enqu","score":26.759424001394017},{"term":"vertrag","score":26.756881269217494},{"term":"ergodirect","score":26.75510561696019},{"term":"cabinebelang","score":26.75510561696019},{"term":"andpol","score":26.75510561696019},{"term":"sosv","score":26.75510561696019},{"term":"grov","score":26.739142234004383},{"term":"context","score":26.729892051482974},{"term":"plus","score":26.726535407776154},{"term":"roermond","score":26.72318251555791},{"term":"opschort","score":26.722344877691235},{"term":"droeg","score":26.718996663057972},{"term":"vl","score":26.706910513802068},{"term":"delawar","score":26.706910513802068},{"term":"bolling","score":26.706910513802068},{"term":"nte","score":26.698985471572424},{"term":"=","score":26.698154561720834},{"term":"aanrijd","score":26.660179371105063},{"term":"poelenij","score":26.65984951898168},{"term":"uil","score":26.65984951898168},{"term":"croissanterie","score":26.65984951898168},{"term":"herstelbesliss","score":26.65984951898168},{"term":"ongeacht","score":26.65771934636697},{"term":"inwerkingtred","score":26.63241651646321},{"term":"talensshop","score":26.61387048253228},{"term":"weringh","score":26.61387048253228},{"term":"beloningscod","score":26.61387048253228},{"term":"vnc","score":26.61387048253228},{"term":"deelvrijsprak","score":26.61387048253228},{"term":"olifant","score":26.61387048253228},{"term":"krediet","score":26.593654200621863},{"term":"evenmin","score":26.577184428822413},{"term":"niemand","score":26.5760541501422},{"term":"len","score":26.572865204851226},{"term":"antwoordakt","score":26.572865204851226},{"term":"alternatiev","score":26.568088133778136},{"term":"alcohol","score":26.564113043757033},{"term":"geschat","score":26.553010731323788},{"term":"willekeur","score":26.529357520751347},{"term":"terborgseweg","score":26.524966957390614},{"term":"uiterst","score":26.5238651692761},{"term":"camerabeeld","score":26.519165429411537},{"term":"which","score":26.515254595850266},{"term":"perso","score":26.501217658361895},{"term":"kennisgev","score":26.494223742488636},{"term":"ivg","score":26.481954546948685},{"term":"drugssmokkel","score":26.481954546948685},{"term":"klinisch","score":26.467179129199994},{"term":"ingevoerd","score":26.46256729124333},{"term":"inspann","score":26.45796253208908},{"term":"-,","score":26.446481461819175},{"term":"jo","score":26.440376142998694},{"term":"afgelast","score":26.439847728553023},{"term":"toets","score":26.437328136640488},{"term":"gebruikmak","score":26.4191048459819},{"term":"tijdsverlop","score":26.405509656552255},{"term":"pasman","score":26.39860915414755},{"term":"knokkel","score":26.39860915414755},{"term":"bonbon","score":26.39860915414755},{"term":"petronella","score":26.39860915414755},{"term":"malledijk","score":26.39860915414755},{"term":"compulsory","score":26.39860915414755},{"term":"onzeker","score":26.376263080714008},{"term":"jullie","score":26.374025109703837},{"term":"kadastral","score":26.367321196299592},{"term":"geloofwaard","score":26.362860228934856},{"term":"bewijsminimumregel","score":26.35820373951251},{"term":"erica","score":26.35820373951251},{"term":"thermostat","score":26.35820373951251},{"term":"verfahr","score":26.35820373951251},{"term":"pva","score":26.35820373951251},{"term":"voeld","score":26.327409299420502},{"term":"overrijd","score":26.31859848492015},{"term":"uitgewerkt","score":26.296729704069012},{"term":"ban","score":26.294550263362915},{"term":"jou","score":26.292372404828065},{"term":"aagw_NUMnl","score":26.27976231320595},{"term":"luif","score":26.27976231320595},{"term":"oosterhuis","score":26.27976231320595},{"term":"dupliek","score":26.248428768849287},{"term":"meijburg","score":26.24166592326456},{"term":"truvo","score":26.24166592326456},{"term":"milieutechniek","score":26.24166592326456},{"term":"epd","score":26.24166592326456},{"term":"standaardregel","score":26.24166592326456},{"term":"prorogatie","score":26.204281657240255},{"term":"bako","score":26.204281657240255},{"term":"vco","score":26.204281657240255},{"term":"verwerk","score":26.18824975982568},{"term":"incitement","score":26.167583379903864},{"term":"porto","score":26.167583379903864},{"term":"jpb","score":26.167583379903864},{"term":"belangenafweg","score":26.139581253305018},{"term":"veiligheidsinstructies","score":26.131546368898505},{"term":"valouw","score":26.131546368898505},{"term":"westlandutrecht","score":26.131546368898505},{"term":"ponysportveren","score":26.131546368898505},{"term":"jasper","score":26.131546368898505},{"term":"disclos","score":26.131546368898505},{"term":"pati","score":26.10802367898267},{"term":"subsidair","score":26.096147214699702},{"term":"accijnsrichtlijn","score":26.096147214699702},{"term":"verkeersboet","score":26.096147214699702},{"term":"pharmaceutical","score":26.096147214699702},{"term":"aanvrager","score":26.091011044074474},{"term":"daderwetenschap","score":26.061363729275964},{"term":"leidinggev","score":26.050570081194774},{"term":"ondervond","score":26.031882590289953},{"term":"marcan","score":26.027174862557366},{"term":"onderzoekslocatie","score":26.027174862557366},{"term":"liniper","score":26.027174862557366},{"term":"famvo","score":26.027174862557366},{"term":"verrader","score":26.027174862557366},{"term":"mededelingstermijn","score":26.027174862557366},{"term":"[…])","score":26.027174862557366},{"term":"getuigenbeschermingstraject","score":25.9935606259246},{"term":"werkvoorzieningschap","score":25.9935606259246},{"term":"repla","score":25.9935606259246},{"term":"zeger","score":25.96050202202218},{"term":"hennepkwekerij","score":25.948553012174276},{"term":"-)","score":25.94467286505006},{"term":"terbeschikkingstellingsregel","score":25.92798098027862},{"term":"@-","score":25.895980297585737},{"term":"henri","score":25.86448358364946},{"term":"octrooibureau","score":25.86448358364946},{"term":"appelexplot","score":25.86448358364946},{"term":"beauty","score":25.86448358364946},{"term":"afloss","score":25.842386620558734},{"term":"sour","score":25.83347521057753},{"term":"genietingsmoment","score":25.83347521057753},{"term":"campfen","score":25.83347521057753},{"term":"paasberg","score":25.83347521057753},{"term":"shank","score":25.83347521057753},{"term":"meteor","score":25.83347521057753},{"term":"metalektro","score":25.83347521057753},{"term":"lopend","score":25.828676584233946},{"term":"verzekerar","score":25.812554180817557},{"term":"werknemersverzeker","score":25.810699466693563},{"term":"ferring","score":25.802940266315954},{"term":"harskamp","score":25.802940266315954},{"term":"ric","score":25.802940266315954},{"term":"rijkevoort","score":25.802940266315954},{"term":"vertrek","score":25.785465960659373},{"term":"clay","score":25.77286451158687},{"term":"gehandicaptenzorg","score":25.77286451158687},{"term":"creativ","score":25.77286451158687},{"term":"renteaftrekbeperk","score":25.77286451158687},{"term":"sltn","score":25.77286451158687},{"term":"ingrijp","score":25.74407759879088},{"term":"tekstra","score":25.743234340016592},{"term":"randmer","score":25.743234340016592},{"term":"ita","score":25.743234340016592},{"term":"ralph","score":25.743234340016592},{"term":"ebm","score":25.743234340016592},{"term":"ʺ","score":25.743234340016592},{"term":"deelnemer","score":25.737435752463156},{"term":"nsi","score":25.714036741174287},{"term":"bodemgesteld","score":25.714036741174287},{"term":"belastingobject","score":25.714036741174287},{"term":"toezegg","score":25.701464098467422},{"term":"hieronder","score":25.69194405874499},{"term":"daedalus","score":25.685259266270087},{"term":"fmc","score":25.685259266270087},{"term":"zorgdossier","score":25.685259266270087},{"term":"bamalit","score":25.65688999628617},{"term":"flevo","score":25.62891751233669},{"term":"exploitation","score":25.62891751233669},{"term":"autorijschol","score":25.62891751233669},{"term":"verhur","score":25.61975041554266},{"term":"introduction","score":25.601330868072022},{"term":"gezinsvoogdijinstell","score":25.601330868072022},{"term":"hersteluitsprak","score":25.601330868072022},{"term":"proceskostenbesliss","score":25.601330868072022},{"term":"bekracht","score":25.585727946142104},{"term":"appartementsgebouw","score":25.574119563960465},{"term":"wiener","score":25.574119563960465},{"term":"modification","score":25.574119563960465},{"term":"testing","score":25.574119563960465},{"term":"uitleveringsdetentie","score":25.574119563960465},{"term":"stuiver","score":25.547273523296184},{"term":"portion","score":25.547273523296184},{"term":"ene","score":25.544159472408953},{"term":"andries","score":25.520783069796142},{"term":"gedon","score":25.520783069796142},{"term":"buffel","score":25.520783069796142},{"term":"libertas","score":25.520783069796142},{"term":"drogisterij","score":25.494638906661436},{"term":"herroepingsprocedur","score":25.494638906661436},{"term":"whatsoever","score":25.494638906661436},{"term":"landelijk","score":25.47208225559433},{"term":"nicolas","score":25.46883209698962},{"term":"gobain","score":25.46883209698962},{"term":"reanimatie","score":25.46883209698962},{"term":"doorred","score":25.46883209698962},{"term":"rechtsbijstandsverzeker","score":25.44335404543476},{"term":"elisabeth","score":25.44335404543476},{"term":"toegeroep","score":25.44335404543476},{"term":"energiewacht","score":25.44335404543476},{"term":"rechtsvergelijk","score":25.44335404543476},{"term":"aanlegvergunn","score":25.44335404543476},{"term":"vergader","score":25.431504046007568},{"term":"colenbrander","score":25.41819648102104},{"term":"pon","score":25.41819648102104},{"term":"schuldenar","score":25.405473513930502},{"term":"discovery","score":25.393351441023928},{"term":"ideas","score":25.393351441023928},{"term":"heeswijk","score":25.393351441023928},{"term":"delicti","score":25.393351441023928},{"term":"achteraf","score":25.393080142016622},{"term":"gelijktijd","score":25.38717092710577},{"term":"verwerkt","score":25.380202282857354},{"term":"mdg","score":25.3688112558403},{"term":"triog","score":25.3688112558403},{"term":"teken","score":25.360456592076893},{"term":"vinship","score":25.344568534775608},{"term":"agrifirm","score":25.344568534775608},{"term":"planet","score":25.344568534775608},{"term":"confidential","score":25.344568534775608},{"term":"fal","score":25.323977157660426},{"term":"merck","score":25.320616152682177},{"term":"dierenartsenpraktijk","score":25.320616152682177},{"term":"gebond","score":25.310345536862794},{"term":"gram","score":25.305119054704633},{"term":"nta","score":25.29694723738817},{"term":"ps_NUM","score":25.29694723738817},{"term":"stang","score":25.29694723738817},{"term":"problematiek","score":25.29261252278254},{"term":"pga","score":25.27355515786179},{"term":"diavolezza","score":25.27355515786179},{"term":"mosquitno","score":25.27355515786179},{"term":"raptus","score":25.27355515786179},{"term":"consultatieregel","score":25.27355515786179},{"term":"feitenrechtsprak","score":25.27355515786179},{"term":"capgemini","score":25.250433513059637},{"term":"sprint","score":25.250433513059637},{"term":"waarborg","score":25.24873322379771},{"term":"betrokken","score":25.23594875034644},{"term":"nexus","score":25.22757612141239},{"term":"alcatel","score":25.22757612141239},{"term":"andes","score":25.22757612141239},{"term":"trinkenshof","score":25.22757612141239},{"term":"onweersprok","score":25.214592727957875},{"term":"onteigeningsvonnis","score":25.204977010904525},{"term":"terugvorder","score":25.196407792667145},{"term":"interflow","score":25.182630409708274},{"term":"racing","score":25.182630409708274},{"term":"opnem","score":25.165345390909895},{"term":"benodigd","score":25.161858466482467},{"term":"vijfenvijft","score":25.160530737335105},{"term":",-- (…)","score":25.160530737335105},{"term":"ips","score":25.160530737335105},{"term":"albus","score":25.160530737335105},{"term":"|","score":25.160530737335105},{"term":"verbeurt","score":25.150925871975097},{"term":"plaintiff","score":25.138672596270723},{"term":"geldkoerier","score":25.138672596270723},{"term":"staedion","score":25.138672596270723},{"term":"parnassia","score":25.138672596270723},{"term":"enterprises","score":25.11705076406229},{"term":"beleggingsvoorstel","score":25.11705076406229},{"term":"verbern","score":25.11705076406229},{"term":"politiebond","score":25.11705076406229},{"term":"nalatenschap","score":25.115422499034093},{"term":"slotconclusie","score":25.095660185828795},{"term":"distinction","score":25.095660185828795},{"term":"koffiezetapparat","score":25.095660185828795},{"term":"vermeerdert","score":25.095660185828795},{"term":"rustend","score":25.082757838960237},{"term":"pann","score":25.07449596716772},{"term":"dierenthuis","score":25.07449596716772},{"term":"gerekestreerd","score":25.07449596716772},{"term":"executies","score":25.07449596716772},{"term":"machinery","score":25.07449596716772},{"term":"boterslot","score":25.053553367433132},{"term":"suit","score":25.053553367433132},{"term":"percolaatwater","score":25.03282779336204},{"term":"bekenteniss","score":25.03282779336204},{"term":"taxatiekaart","score":25.03282779336204},{"term":"cob","score":25.03282779336204},{"term":"ngen","score":25.03282779336204},{"term":"with","score":25.03084178611786},{"term":"stena","score":25.01231479302766},{"term":"melkveebedrijf","score":25.01231479302766},{"term":"remark","score":24.992010050099623},{"term":"astrazeneca","score":24.992010050099623},{"term":"elbrina","score":24.992010050099623},{"term":"vw","score":24.98309040369167},{"term":"compartimenter","score":24.97190937839262},{"term":"patrimonium","score":24.97190937839262},{"term":"denk","score":24.970920001604824},{"term":"treft","score":24.969518896262947},{"term":"mobiel","score":24.967651773128146},{"term":"hoofdstraf","score":24.952008716686287},{"term":"woongoed","score":24.952008716686287},{"term":"aandacht","score":24.949971821213424},{"term":"kent","score":24.943021474448727},{"term":"justiti","score":24.942095976986508},{"term":"raamwerk","score":24.93230412380026},{"term":"eftel","score":24.93230412380026},{"term":"svb","score":24.924105610164915},{"term":"guarantor","score":24.89346795208606},{"term":"uvm","score":24.89346795208606},{"term":"concorp","score":24.89346795208606},{"term":"volksbank","score":24.89346795208606},{"term":"erfgoed","score":24.89346795208606},{"term":"dk","score":24.89346795208606},{"term":"infomedic","score":24.89346795208606},{"term":"process","score":24.88571970946355},{"term":"vertegenwoordigd","score":24.88299656252922},{"term":"ş","score":24.874329050053756},{"term":"hoogst","score":24.86355267805007},{"term":"vesteda","score":24.83659208144499},{"term":"restricties","score":24.817987296120364},{"term":"hoogh","score":24.817987296120364},{"term":"garantiefond","score":24.799553985910517},{"term":"ces","score":24.799553985910517},{"term":"hoofdletsel","score":24.799553985910517},{"term":"doorgeschov","score":24.799553985910517},{"term":"saver","score":24.799553985910517},{"term":"nieuwstrat","score":24.781289018783973},{"term":"karel","score":24.781289018783973},{"term":"sub_NUM","score":24.781289018783973},{"term":"booker","score":24.781289018783973},{"term":"hubert","score":24.781289018783973},{"term":"schadeformulier","score":24.781289018783973},{"term":"minimal","score":24.779991720516954},{"term":"woningborg","score":24.763189347744134},{"term":"achterwand","score":24.763189347744134},{"term":"getoond","score":24.760330978773617},{"term":"ubs","score":24.745252007778614},{"term":"reitsma","score":24.745252007778614},{"term":"cfv","score":24.745252007778614},{"term":"optred","score":24.745127876664903},{"term":"opsporingsambtenar","score":24.736042859987872},{"term":"ua","score":24.727474112944122},{"term":"ks","score":24.727474112944122},{"term":"landbouwbeleid","score":24.727474112944122},{"term":"me","score":24.725263114289497},{"term":"concernaansluit","score":24.709852853579815},{"term":"personeelsdienst","score":24.709852853579815},{"term":"centrada","score":24.692385493642302},{"term":"zolang","score":24.690600436396082},{"term":"altius","score":24.65790188077329},{"term":"teruggaafbeschik","score":24.65790188077329},{"term":"uitzendbedrijv","score":24.65790188077329},{"term":"uitleveringswet","score":24.65790188077329},{"term":"omlegg","score":24.640880501437476},{"term":"ps","score":24.624002764145747},{"term":"vijftienhonderd","score":24.624002764145747},{"term":"exclusief","score":24.591009701406566},{"term":"melkvee","score":24.59066865917532},{"term":"biesma","score":24.59066865917532},{"term":"vakkennis","score":24.59066865917532},{"term":"lansingerland","score":24.59066865917532},{"term":"qq","score":24.557881039623968},{"term":"arbeidsbelon","score":24.557881039623968},{"term":"“(…)","score":24.54121263969913},{"term":"crew","score":24.525622275764203},{"term":"onderwerp","score":24.51421265894203},{"term":"ervor","score":24.513009369935993},{"term":"cresco","score":24.509685936465846},{"term":"integriteitsbeoordel","score":24.509685936465846},{"term":"parkeerdruk","score":24.49387557745162},{"term":"jaarhur","score":24.47818922252957},{"term":"vlijm","score":24.47818922252957},{"term":"meegaa","score":24.46262494164546},{"term":"brema","score":24.46262494164546},{"term":"mosadex","score":24.447180849457638},{"term":"soci","score":24.447180849457638},{"term":"lufthansa","score":24.447180849457638},{"term":"vakveren","score":24.447180849457638},{"term":"ilt","score":24.447180849457638},{"term":"geadviseerd","score":24.441681218002742},{"term":"havensteder","score":24.4318551039665},{"term":"sigar","score":24.4318551039665},{"term":"lender","score":24.416645905196063},{"term":"opeenstapel","score":24.416645905196063},{"term":"vooropstell","score":24.416645905196063},{"term":"sneeuw","score":24.401551493925297},{"term":"lcd","score":24.401551493925297},{"term":"opgegev","score":24.39120112456063},{"term":"examination","score":24.38657015046698},{"term":"schotrestenonderzoek","score":24.38657015046698},{"term":"klachtenpatron","score":24.371700193491947},{"term":"dagelijk","score":24.368950383172766},{"term":"cofely","score":24.3569399788967},{"term":"belastingsamenwerk","score":24.3569399788967},{"term":"houthandel","score":24.3569399788967},{"term":"omgev","score":24.35103988221325},{"term":"gouw","score":24.342287898712552},{"term":"expo","score":24.342287898712552},{"term":"verloskund","score":24.327742380054396},{"term":"nieuwerkerk","score":24.31330188410742},{"term":"cuckoo","score":24.298964905150196},{"term":"waaran","score":24.29160220131901},{"term":"bovengenoemd","score":24.290857322358008},{"term":"mens","score":24.288995933702402},{"term":"warning","score":24.284729969612467},{"term":"staet","score":24.284729969612467},{"term":"blog","score":24.284729969612467},{"term":"emirat","score":24.27059563516628},{"term":"wp","score":24.27059563516628},{"term":"peiz","score":24.25656048984899},{"term":"advanced","score":24.2426231512168},{"term":"i_NUM","score":24.2426231512168},{"term":"mondzorg","score":24.2426231512168},{"term":"wasstrat","score":24.2426231512168},{"term":"undue","score":24.2426231512168},{"term":"alcoholverbod","score":24.228782265527656},{"term":"luid","score":24.22565738365793},{"term":"uitgebracht","score":24.224564421110166},{"term":"tso","score":24.21503650695213},{"term":"curandus","score":24.201384576811332},{"term":"nautic","score":24.201384576811332},{"term":"belastingtarief","score":24.201384576811332},{"term":"werkt","score":24.19268006301263},{"term":"watson","score":24.187825202840575},{"term":"stealth","score":24.174357138477884},{"term":"klussenbedrijf","score":24.174357138477884},{"term":"geestesvermogen","score":24.174357138477884},{"term":"net","score":24.168987227169453},{"term":"vnv","score":24.160979162176293},{"term":"waardevol","score":24.160979162176293},{"term":"bedrijfspensioenfond","score":24.160979162176293},{"term":"badhoevedorp","score":24.147690076738954},{"term":"henn","score":24.147690076738954},{"term":"vlm","score":24.147690076738954},{"term":"scher","score":24.147690076738954},{"term":"dienstverrichter","score":24.13448870867625},{"term":"hedg","score":24.13448870867625},{"term":"benzinestation","score":24.121373907583934},{"term":"farmerhoev","score":24.121373907583934},{"term":"lidstat","score":24.11828578994345},{"term":"voertuig","score":24.117231246608437},{"term":"agglomeratie","score":24.108344545541545},{"term":"description","score":24.108344545541545},{"term":"onred","score":24.108107365438535},{"term":"bested","score":24.101107828898797},{"term":"belangenbehart","score":24.09539951653031},{"term":"fr","score":24.06975813967219},{"term":"alarmnummer","score":24.06975813967219},{"term":"nettowinst","score":24.05705968431487},{"term":"ademanalys","score":24.05705968431487},{"term":"routes","score":24.04444134592834},{"term":"huizinga","score":24.04444134592834},{"term":"voorschot","score":24.03609836945452},{"term":"gefuseerd","score":24.03190211990115},{"term":"]@","score":24.03190211990115},{"term":"afvall","score":24.03190211990115},{"term":"forax","score":24.019441020399878},{"term":"papendrecht","score":24.019441020399878},{"term":"onthull","score":24.019441020399878},{"term":"netto","score":24.013266430999195},{"term":"invalid","score":24.007057079904037},{"term":"packard","score":24.007057079904037},{"term":"ttz","score":23.994749348755278},{"term":"inno","score":23.97035880256764},{"term":"fwg","score":23.97035880256764},{"term":"saint","score":23.958274173655717},{"term":"hondenbelast","score":23.958274173655717},{"term":"uitgeslot","score":23.95011327594996},{"term":"onherroep","score":23.9355227752105},{"term":"accon","score":23.934321791562287},{"term":"aanvang","score":23.932216625437935},{"term":"afstamm","score":23.922452320522655},{"term":"stelselwijz","score":23.922452320522655},{"term":"detention","score":23.91065287626828},{"term":"motorscooter","score":23.91065287626828},{"term":"van_NUM","score":23.91065287626828},{"term":"lgo","score":23.898922637363484},{"term":"begat","score":23.898922637363484},{"term":"ontstaansgeschiedenis","score":23.8872607967419},{"term":"_NUMen","score":23.875666561373247},{"term":"ef","score":23.875666561373247},{"term":"historie","score":23.875666561373247},{"term":"wiv","score":23.875666561373247},{"term":"geschillenregel","score":23.875666561373247},{"term":"siemen","score":23.8412817602925},{"term":"genot","score":23.840772917138295},{"term":"buitenmur","score":23.829950285221145},{"term":"educatiev","score":23.829950285221145},{"term":"klein","score":23.820011588604572},{"term":"gebr","score":23.818682649784634},{"term":"ka","score":23.818682649784634},{"term":"autohandel","score":23.818682649784634},{"term":"nadien","score":23.81016040661557},{"term":"fj","score":23.807478138687294},{"term":"remd","score":23.807478138687294},{"term":"brutowinst","score":23.796336048588383},{"term":"telefonisch","score":23.7883490255685},{"term":"zorgvuld","score":23.77295551569584},{"term":"verwikkel","score":23.752378235150832},{"term":"li","score":23.752378235150832},{"term":"filippijn","score":23.72003251665963},{"term":"vvaa","score":23.72003251665963},{"term":"artesia","score":23.72003251665963},{"term":"singapor","score":23.69875572024952},{"term":"mierenzur","score":23.69875572024952},{"term":"bedrijfsgegeven","score":23.69875572024952},{"term":"rooi","score":23.68820160604783},{"term":"rencken","score":23.68820160604783},{"term":"traffic","score":23.66725900631324},{"term":"verschil","score":23.66584524697022},{"term":"expenses","score":23.63625063324131},{"term":"apel","score":23.63625063324131},{"term":"aor","score":23.63625063324131},{"term":"row","score":23.62602043190777},{"term":"naamswijz","score":23.615842292892825},{"term":"verhuurbemiddelingsovereenkomst","score":23.605715688979732},{"term":"huwelijksakt","score":23.59564010091982},{"term":"diamant","score":23.59564010091982},{"term":"moker","score":23.575639934250653},{"term":"oprui","score":23.575639934250653},{"term":"weiger","score":23.557174286121004},{"term":"zorggroep","score":23.555837792285228},{"term":"avm","score":23.54600976268037},{"term":"hiertoe","score":23.540308873862827},{"term":"vaststat","score":23.528732552340415},{"term":"nidos","score":23.52649741278964},{"term":"parkstad","score":23.52649741278964},{"term":"autorit","score":23.52649741278964},{"term":"htm","score":23.52649741278964},{"term":"antiek","score":23.52649741278964},{"term":"automatiser","score":23.516812163838065},{"term":"dekbed","score":23.516812163838065},{"term":"bruter","score":23.47853348341667},{"term":"agency","score":23.469077201024778},{"term":"gezet","score":23.452865784751737},{"term":"griek","score":23.4502977203251},{"term":"overdekt","score":23.4502977203251},{"term":"sign","score":23.440973694113477},{"term":"belangrijk","score":23.434614991414215},{"term":"advocatur","score":23.431692935000473},{"term":"ontbrand","score":23.431692935000473},{"term":"immo","score":23.431692935000473},{"term":"eisverminder","score":23.422455043287883},{"term":"marktanalysebesluit","score":23.413259624790626},{"term":"dichtstbijzijnd","score":23.413259624790626},{"term":"spoorweg","score":23.404106290735804},{"term":"toegegan","score":23.376894986624244},{"term":"compensation","score":23.376894986624244},{"term":"tournement","score":23.376894986624244},{"term":"wrap","score":23.367906207448566},{"term":"kerkelijk","score":23.367906207448566},{"term":"maersk","score":23.358957646658723},{"term":"airway","score":23.358957646658723},{"term":"gezichtsbedek","score":23.358957646658723},{"term":"implementation","score":23.350048945959962},{"term":"aviation","score":23.350048945959962},{"term":"instan","score":23.350048945959962},{"term":"invloed","score":23.34580541749815},{"term":"nach","score":23.34117975182423},{"term":"noter","score":23.34117975182423},{"term":"nichtj","score":23.34117975182423},{"term":"pd","score":23.34117975182423},{"term":"psychisch","score":23.337121557974804},{"term":"eindconclusie","score":23.332349715406},{"term":"industries","score":23.323558492459924},{"term":"sigarettenpeuk","score":23.323558492459924},{"term":"beoordelingspunt","score":23.323558492459924},{"term":"four","score":23.314805743260326},{"term":"bosma","score":23.306091132522415},{"term":"toekenn","score":23.290338032372958},{"term":"eed","score":23.280172843237402},{"term":"kantonzak","score":23.280172843237402},{"term":"approach","score":23.2716075196534},{"term":"berekeningsjar","score":23.2716075196534},{"term":"aansluit","score":23.25611864195297},{"term":"brittanni","score":23.24612946809854},{"term":"liev","score":23.24612946809854},{"term":"tewerkgesteld","score":23.237708403025856},{"term":"hierdor","score":23.230903995182956},{"term":"g_NUMs","score":23.229322646505782},{"term":"werkafsprak","score":23.229322646505782},{"term":"boxmer","score":23.229322646505782},{"term":"gezond","score":23.224894325446932},{"term":"zedenzak","score":23.22097190368482},{"term":"leerstraf","score":23.212655883387495},{"term":"geclassificeerd","score":23.20437429805543},{"term":"koper","score":23.203825179151853},{"term":"paspoortwet","score":23.1879132997824},{"term":"brandschad","score":23.1879132997824},{"term":"melder","score":23.171586678504077},{"term":"gevolgencriterium","score":23.171586678504077},{"term":"portfolio","score":23.171586678504077},{"term":"hereinafter","score":23.16347307711285},{"term":"deg","score":23.149371629359628},{"term":"staalbankier","score":23.14734395743939},{"term":"somer","score":23.14734395743939},{"term":"affair","score":23.139327914644312},{"term":"beleggingsinstell","score":23.123391575345956},{"term":"asus","score":23.123391575345956},{"term":"dubbeltell","score":23.084097580578366},{"term":"fun","score":23.084097580578366},{"term":"gehanteerd","score":23.08341277543418},{"term":"ris","score":23.06859362696973},{"term":"voorvloei","score":23.060886488337747},{"term":"accept","score":23.060886488337747},{"term":"werkruimt","score":23.05320893572342},{"term":"rekenkund","score":23.05320893572342},{"term":"bouwgroep","score":23.05320893572342},{"term":"kenbar","score":23.0517083852488},{"term":"billijk","score":23.050969317291873},{"term":"dudok","score":23.02279009839636},{"term":"oldenzal","score":23.007752433568303},{"term":"indelingsregel","score":23.007752433568303},{"term":"bouwlag","score":23.007752433568303},{"term":"hoensbroek","score":22.992826991165124},{"term":"loondoorbetalingsverplicht","score":22.992826991165124},{"term":"unles","score":22.992826991165124},{"term":"w_NUM","score":22.985405832372056},{"term":"gereageerd","score":22.98422411538976},{"term":"indexdelict","score":22.978012108609402},{"term":"bijstandswet","score":22.97064561777681},{"term":"limiter","score":22.955993537592665},{"term":"smad","score":22.941448018934505},{"term":"network","score":22.92700752298753},{"term":"wangedrag","score":22.919826186726073},{"term":"luchtvaart","score":22.919826186726073},{"term":"objectiv","score":22.912670544030306},{"term":"viez","score":22.905540411701313},{"term":"erkend","score":22.894984088805185},{"term":"sea","score":22.884301274046393},{"term":"ssm","score":22.884301274046393},{"term":"aaw","score":22.877271389831503},{"term":"belangenbehartiger","score":22.877271389831503},{"term":"saneringskost","score":22.877271389831503},{"term":"abb","score":22.8702661287291},{"term":"luger","score":22.8702661287291},{"term":"schadebegrot","score":22.863285318849563},{"term":"ntie","score":22.863285318849563},{"term":"transavia","score":22.84939637414394},{"term":"obvion","score":22.842487904407765},{"term":"inkomensverklar","score":22.842487904407765},{"term":"baart","score":22.83560321602582},{"term":"garagebedrijf","score":22.83560321602582},{"term":"expediteur","score":22.82190453233467},{"term":"aanwijzingsbesluit","score":22.801530841720684},{"term":"chambaz","score":22.794785472763404},{"term":"putatief","score":22.774684801056402},{"term":",- = €","score":22.774684801056402},{"term":"ap_NUM","score":22.774684801056402},{"term":"grootvader","score":22.761395715619063},{"term":"veendam","score":22.761395715619063},{"term":"merten","score":22.74819434755636},{"term":"que","score":22.70910515541042},{"term":"dagteken","score":22.705375799531033},{"term":"inrit","score":22.702663926010334},{"term":"wijch","score":22.689843369288496},{"term":"consistentie","score":22.689843369288496},{"term":"protocoll","score":22.689843369288496},{"term":"ertoe","score":22.684375276265932},{"term":"gor","score":22.683463778552298},{"term":"voetbal","score":22.677104472717538},{"term":"versus","score":22.677104472717538},{"term":"basic","score":22.664446202614243},{"term":"lpw","score":22.65814698480845},{"term":"association","score":22.64560775878126},{"term":"alcoholslot","score":22.64560775878126},{"term":"trend","score":22.639367504108773},{"term":"noordwijk","score":22.620762718784146},{"term":"interveni","score":22.61459938570933},{"term":"fn","score":22.608454987635387},{"term":"ggn","score":22.608454987635387},{"term":"flatgebouw","score":22.602329408574295},{"term":"extent","score":22.596222533600518},{"term":"begeleid","score":22.591016764562482},{"term":"tsn","score":22.565964770407916},{"term":"klok","score":22.559967764415404},{"term":"pel","score":22.559967764415404},{"term":"afwijk","score":22.55990444030681},{"term":"corporation","score":22.55398868671867},{"term":"gard","score":22.55398868671867},{"term":"partnerbijdrag","score":22.54208388966408},{"term":"jurisdiction","score":22.536157959402768},{"term":"nederlandsch","score":22.52435851514839},{"term":"hogeschol","score":22.51848479580177},{"term":"ier","score":22.512628276243593},{"term":"incassobureau","score":22.489372200253356},{"term":"leeswijzer","score":22.477844790819855},{"term":"fo","score":22.477844790819855},{"term":"abonnee","score":22.47210590596395},{"term":"linkerarm","score":22.460677303437073},{"term":"wnt","score":22.45498739917261},{"term":"dekt","score":22.443655924101254},{"term":"krak","score":22.443655924101254},{"term":"rul","score":22.438014171417972},{"term":"mededel","score":22.437781452999953},{"term":"gekreg","score":22.425358245292166},{"term":"age","score":22.398961326717263},{"term":"pleegperiod","score":22.398961326717263},{"term":"verdeelsleutel","score":22.387942015095323},{"term":"brutolon","score":22.38245504320382},{"term":"beheermaatschappij","score":22.37698308356607},{"term":"purpos","score":22.371526054259665},{"term":"rechtskeuz","score":22.371526054259665},{"term":"living","score":22.36065646228775},{"term":"cnv","score":22.36065646228775},{"term":"verstand","score":22.358521840018884},{"term":"exceptie","score":22.355243739092263},{"term":"leaseauto","score":22.34446204182251},{"term":"leidschendam","score":22.339092911081174},{"term":"bik","score":22.339092911081174},{"term":"loi","score":22.32839769842798},{"term":"snijwond","score":22.323071463589013},{"term":"theorie","score":22.317759375472782},{"term":"stock","score":22.317759375472782},{"term":"informandum","score":22.317759375472782},{"term":"coevord","score":22.30717734020395},{"term":"stamrecht","score":22.30717734020395},{"term":"beschouwd","score":22.305028595089667},{"term":"klpd","score":22.30190724492794},{"term":"futur","score":22.30190724492794},{"term":"bezwaard","score":22.291408533155654},{"term":"terugtred","score":22.28617977200751},{"term":"merkinbreuk","score":22.275763081793237},{"term":"koster","score":22.275763081793237},{"term":"getuigenbewijs","score":22.270575011439142},{"term":"derdengeld","score":22.260239071122257},{"term":"mod","score":22.25509106313191},{"term":"amlin","score":22.24995627212142},{"term":"camper","score":22.24995627212142},{"term":"loods","score":22.239726070787878},{"term":"show","score":22.239726070787878},{"term":"vijfduiz","score":22.239726070787878},{"term":"knipp","score":22.23463052663028},{"term":"three","score":22.22447822056656},{"term":"harddisk","score":22.22447822056656},{"term":"raalt","score":22.214377188994423},{"term":"raadsonderzoek","score":22.20934573979993},{"term":"indigt","score":22.19932065615284},{"term":"oil","score":22.194326895755665},{"term":"notic","score":22.189345573130762},{"term":"claus","score":22.179419994446505},{"term":"genocid","score":22.169543431165337},{"term":"gea","score":22.15971540156048},{"term":"dertiend","score":22.149935430972096},{"term":"federatie","score":22.149935430972096},{"term":"afgeleid","score":22.135485631393546},{"term":"honderdtwint","score":22.13535456444674},{"term":"cb","score":22.120879229846278},{"term":"betoogd","score":22.114860776306685},{"term":"woordenwissel","score":22.11128688531929},{"term":"blue","score":22.11128688531929},{"term":"dusverr","score":22.101740327813975},{"term":"begrotingsakkoord","score":22.096984083004042},{"term":"spaargeld","score":22.09223912229678},{"term":"peil","score":22.082782839904887},{"term":"afneemt","score":22.082782839904887},{"term":"we","score":22.074209505149444},{"term":"tvm","score":22.06400335920521},{"term":"heffingsmaatstaf","score":22.06400335920521},{"term":"taxivervoer","score":22.05933591251277},{"term":"civil","score":22.054679332993587},{"term":"keizer","score":22.045398573880583},{"term":"winkelier","score":22.045398573880583},{"term":"_NUMw","score":22.036160682167996},{"term":"prostituees","score":22.031557688191434},{"term":"uitzonder","score":22.02603908337549},{"term":"recreatiepark","score":22.017811929615913},{"term":"notarisambt","score":22.00870029654419},{"term":"born","score":22.00870029654419},{"term":"settlement","score":22.00415999947511},{"term":"behartigt","score":22.00415999947511},{"term":"disput","score":22.00415999947511},{"term":"vught","score":22.00415999947511},{"term":"toepassingsbereik","score":22.00415999947511},{"term":"gehecht","score":22.000765134397735},{"term":"annuler","score":21.999629986213407},{"term":"betog","score":21.988812582281696},{"term":"shar","score":21.950465499402736},{"term":"zuidwest","score":21.937264131340033},{"term":"zoiet","score":21.928511382140435},{"term":"roepnam","score":21.919796771402524},{"term":"arabisch","score":21.919796771402524},{"term":"ops","score":21.906795642196336},{"term":"tussenconclusie","score":21.898174939194092},{"term":"comparitie","score":21.887171317539995},{"term":"prin","score":21.88531315853351},{"term":"kapitalisatiefactor","score":21.88531315853351},{"term":") = €","score":21.876784360960595},{"term":"ruimst","score":21.868291779197694},{"term":"crisisheff","score":21.868291779197694},{"term":"excel","score":21.85562014229952},{"term":"updat","score":21.847216768592123},{"term":"‘(…)","score":21.847216768592123},{"term":"tomtom","score":21.847216768592123},{"term":"heid","score":21.84302828538589},{"term":"vliegticket","score":21.838848555546974},{"term":"schilderwerk","score":21.83467754256493},{"term":"short","score":21.81807993693554},{"term":"bedrijf_NUM","score":21.809832502567815},{"term":"agis","score":21.80161893866251},{"term":"afwaarder","score":21.80161893866251},{"term":"corendon","score":21.79752477141906},{"term":"bet","score":21.79343896815946},{"term":"nor","score":21.78936149477976},{"term":"polisnummer","score":21.781231402283424},{"term":"vergezell","score":21.77717871599296},{"term":"return","score":21.77717871599296},{"term":"how","score":21.765069698175548},{"term":"loonadministratie","score":21.75303355352442},{"term":"heemskerk","score":21.75303355352442},{"term":"truck","score":21.749037548199073},{"term":"kredietbank","score":21.745049510985346},{"term":"bvo","score":21.745049510985346},{"term":"barneveld","score":21.745049510985346},{"term":"kasopstell","score":21.73709721422607},{"term":"super","score":21.729176411793873},{"term":"aannames","score":21.71735371726776},{"term":"daarover","score":21.71032514162947},{"term":"pachter","score":21.693915949041333},{"term":"lip","score":21.690036219405677},{"term":"helm","score":21.686164001351944},{"term":"grondwater","score":21.66308682223896},{"term":"wjz","score":21.651647324893386},{"term":"inverzekeringstell","score":21.632725707884926},{"term":"offer","score":21.632725707884926},{"term":"waakzam","score":21.632725707884926},{"term":"methodiek","score":21.632725707884926},{"term":"corporat","score":21.632725707884926},{"term":"bewindvoer","score":21.628962771685515},{"term":",,","score":21.625206902037114},{"term":"bewaarder","score":21.61771625657679},{"term":"z_NUM","score":21.61771625657679},{"term":"tul","score":21.6139814282272},{"term":"vakman","score":21.606532630045233},{"term":"afwerk","score":21.599111471252165},{"term":"dracht","score":21.59541119267584},{"term":"lin","score":21.580678161042314},{"term":"key","score":21.57335214552484},{"term":"world","score":21.569699176472774},{"term":"ondertek","score":21.56036305108109},{"term":"raadsrapport","score":21.558780132062974},{"term":"parfum","score":21.555153657814614},{"term":"gedragsregel","score":21.551533747324132},{"term":"inval","score":21.537119270514236},{"term":"winstbejag","score":21.529950804058604},{"term":"dusver","score":21.529950804058604},{"term":"flora","score":21.52280793932341},{"term":"bovenverdiep","score":21.512141247372686},{"term":"cbr","score":21.508598288075923},{"term":"informel","score":21.508598288075923},{"term":"woningoverval","score":21.505061593962438},{"term":"voer","score":21.500564350397237},{"term":"boedelbeschrijv","score":21.498006912926503},{"term":"know","score":21.48397176760921},{"term":"hdi","score":21.473509668774103},{"term":"logistic","score":21.473509668774103},{"term":"uitweg","score":21.470034428977023},{"term":"bijgestan","score":21.4675440004766},{"term":"poliss","score":21.459644795222815},{"term":"voorbijgan","score":21.456193543287874},{"term":"harderwijk","score":21.456193543287874},{"term":"luxembourg","score":21.43561017121478},{"term":"nvloeding","score":21.43220011071261},{"term":"beverwijk","score":21.42539738306563},{"term":"meegemaakt","score":21.415236480600793},{"term":"beschuldigd","score":21.408491111643514},{"term":"stemherkenn","score":21.408491111643514},{"term":"hous","score":21.405126939277544},{"term":"roc","score":21.38839043993651},{"term":"kpmg","score":21.38839043993651},{"term":")?","score":21.38839043993651},{"term":"royal","score":21.38505988129839},{"term":"tie","score":21.38505988129839},{"term":"biz","score":21.378415356914434},{"term":"sluitend","score":21.378415356914434},{"term":"amstel","score":21.368489778230174},{"term":"vorenstaand","score":21.353634234700262},{"term":"ingered","score":21.34225989407452},{"term":"tranch","score":21.32281079429053},{"term":"daardor","score":21.320938258892458},{"term":"dividenduitker","score":21.306746450896},{"term":"alimentatieverplicht","score":21.303549008168606},{"term":"opstart","score":21.287638025286377},{"term":"lekker","score":21.28447096207509},{"term":"ffw","score":21.28447096207509},{"term":"schietpartij","score":21.265573183679223},{"term":"judgment","score":21.262440841613735},{"term":"evidenc","score":21.249960301893566},{"term":"ontnemingsvorder","score":21.249960301893566},{"term":"zorgkost","score":21.246852298160096},{"term":"provided","score":21.246852298160096},{"term":"afzender","score":21.2406507428036},{"term":"levenslang","score":21.2406507428036},{"term":"diesel","score":21.237557161367004},{"term":"diagnostiek","score":21.231384316960625},{"term":"[…","score":21.231384316960625},{"term":"hoeveelh","score":21.216035047454408},{"term":"bewijslastverdel","score":21.216035047454408},{"term":"_NUMk","score":21.20080268136379},{"term":"registeraccountant","score":21.191718638494788},{"term":"value","score":21.18869976999708},{"term":"zekerheidstell","score":21.18869976999708},{"term":"ketel","score":21.185685451415935},{"term":"terminal","score":21.179670409288025},{"term":"voordracht","score":21.176669658537556},{"term":"request","score":21.16769432559878},{"term":"reclasseringswerker","score":21.16769432559878},{"term":"woningbouw","score":21.161733069322505},{"term":"langdurigheidstoeslag","score":21.152824368623744},{"term":"personalia","score":21.149863598282877},{"term":"terugkeerbesluit","score":21.135125138069778},{"term":"meeting","score":21.120494494917033},{"term":"present","score":21.117581165924104},{"term":"noodtoestand","score":21.103077839133466},{"term":"justitieel","score":21.100189751988996},{"term":"erfbelast","score":21.097305829355196},{"term":"geboorteakt","score":21.097305829355196},{"term":"voogd","score":21.09442605923922},{"term":"oordeelsvorm","score":21.09442605923922},{"term":"administratiekantor","score":21.088678928847756},{"term":"format","score":21.080089080281994},{"term":"reconstructie","score":21.068693038052718},{"term":"development","score":21.065854144744268},{"term":"purmer","score":21.065854144744268},{"term":"erfgren","score":21.065854144744268},{"term":"design","score":21.06018841857566},{"term":"uurlon","score":21.05171981029808},{"term":"bezet","score":21.048904890762323},{"term":"irak","score":21.04328690954021},{"term":"begrenz","score":21.040483825689634},{"term":"iran","score":21.037684664980787},{"term":"waaruit","score":21.019699643267913},{"term":"bc","score":21.012666965597372},{"term":"gehandicapt","score":21.007149720719212},{"term":"gereedschap","score":21.007149720719212},{"term":"schuldbekentenis","score":21.004396795301766},{"term":"randvoorwaard","score":20.998902286351488},{"term":"welstand","score":20.998902286351488},{"term":"voorgew","score":20.98250875194313},{"term":"dividendbelast","score":20.98250875194313},{"term":"du","score":20.97707396599013},{"term":"ra","score":20.97707396599013},{"term":"poeder","score":20.97436210116786},{"term":"logistiek","score":20.9716539084724},{"term":"e [","score":20.968949377972372},{"term":"essent","score":20.95816768070262},{"term":"twist","score":20.955481313609685},{"term":"doelgroep","score":20.955481313609685},{"term":"ymer","score":20.93146501435289},{"term":"honorarium","score":20.93146501435289},{"term":"cat","score":20.905114172035763},{"term":"pensioenaansprak","score":20.88428065031925},{"term":"wher","score":20.88428065031925},{"term":"binnenkort","score":20.88169165319396},{"term":"day","score":20.88169165319396},{"term":"verdragsbepal","score":20.87652369166211},{"term":"mevr","score":20.87652369166211},{"term":"actueel","score":20.86879670201202},{"term":"eindheff","score":20.86366191100153},{"term":"kentekenbewijs","score":20.86109945068943},{"term":"deelgenot","score":20.85854026927818},{"term":"pos","score":20.853431709667987},{"term":"entiteit","score":20.84833616551039},{"term":"luister","score":20.845793253533998},{"term":"se","score":20.820540392788374},{"term":"garagebox","score":20.80305121201087},{"term":"zwed","score":20.80305121201087},{"term":"tegel","score":20.80056519553168},{"term":"capell","score":20.783249070045446},{"term":"eigendomsoverdracht","score":20.77587352388708},{"term":"motorvoertu","score":20.770971560648796},{"term":"afgewikkeld","score":20.770971560648796},{"term":"cafetaria","score":20.766081582662665},{"term":"use","score":20.766081582662665},{"term":"invest","score":20.75390869054986},{"term":"dutchbat","score":20.751482977699443},{"term":"am","score":20.751482977699443},{"term":"mengsel","score":20.739398348787518},{"term":"deloit","score":20.734584868726387},{"term":"republiek","score":20.72978294565131},{"term":"subsidiariteit","score":20.7249925241994},{"term":"arbeidsongeschiktheidsuitker","score":20.7249925241994},{"term":"creditcard","score":20.72260160938985},{"term":"-/-","score":20.720213549404637},{"term":"schroevendraaier","score":20.720213549404637},{"term":"ned","score":20.696488478784996},{"term":"emolument","score":20.69413137774611},{"term":"chalet","score":20.682387362791665},{"term":"pseudo","score":20.67770899808532},{"term":"pensioengerechtigd","score":20.673041551392878},{"term":"reisdocument","score":20.647563499838018},{"term":"gunningsbesliss","score":20.6429657966638},{"term":"relatiev","score":20.640670902550845},{"term":"human","score":20.633801977653576},{"term":"contractspartij","score":20.633801977653576},{"term":"kruis","score":20.61786563835522},{"term":"oplet","score":20.611074460352945},{"term":"bovenlicham","score":20.611074460352945},{"term":"aanlegg","score":20.608815849158642},{"term":"herk","score":20.59980682491643},{"term":"jurist","score":20.593076588615805},{"term":"public","score":20.58860231381909},{"term":"architect","score":20.58636892441894},{"term":"participatie","score":20.58413802625121},{"term":"gouda","score":20.579683681425564},{"term":"big","score":20.57523923515213},{"term":"mooi","score":20.564171138282845},{"term":"motief","score":20.56196485013853},{"term":"oostenrijk","score":20.557559562013846},{"term":"mean","score":20.5269912813631},{"term":"elkar","score":20.52428082527227},{"term":"tant","score":20.522662275343954},{"term":"hamer","score":20.50758412099762},{"term":"brouwer","score":20.49688318589196},{"term":"invaliditeit","score":20.49688318589196},{"term":"feitenrechter","score":20.49688318589196},{"term":"jeugdrecht","score":20.477764612265048},{"term":"drijft","score":20.475651563996646},{"term":"meldkamer","score":20.47143215314895},{"term":"]/","score":20.458827069407345},{"term":"bespar","score":20.458827069407345},{"term":"motiveringsklacht","score":20.458827069407345},{"term":"heling","score":20.454642967462895},{"term":"regard","score":20.452554194427083},{"term":"vles","score":20.45046760060193},{"term":"ingezeten","score":20.44630093242822},{"term":"oranj","score":20.44422084903739},{"term":"london","score":20.43592208194377},{"term":"convention","score":20.433852759462216},{"term":"kinderporno","score":20.433852759462216},{"term":"conformiteit","score":20.433852759462216},{"term":"mentor","score":20.42765760737395},{"term":"contactverbod","score":20.417374808373108},{"term":"uitvoeringsbesluit","score":20.409186465307783},{"term":"adviesbureau","score":20.394937041163534},{"term":"ford","score":20.36673919240453},{"term":"dossieronderzoek","score":20.362743187079182},{"term":"betalingsachterstand","score":20.348819709661587},{"term":"tussenkom","score":20.344859312327863},{"term":"stop","score":20.340906741871436},{"term":"navorderingstermijn","score":20.33499249409195},{"term":"onderbouwt","score":20.33499249409195},{"term":"mulder","score":20.333024958271682},{"term":"), [","score":20.319306139169896},{"term":"beuker","score":20.317353967223788},{"term":"vest","score":20.313455330511687},{"term":"geweldpleg","score":20.303741858285786},{"term":"domeinnam","score":20.27488132862773},{"term":"fact","score":20.25776282183639},{"term":"]?","score":20.253978525532315},{"term":"work","score":20.25208905916599},{"term":"information","score":20.24831547314084},{"term":"presentatie","score":20.238912540917223},{"term":"onderzoeksrapport","score":20.229553609473307},{"term":"includ","score":20.218380396056027},{"term":"partieel","score":20.216524247430918},{"term":"bivakmut","score":20.188886774015728},{"term":"commercieel","score":20.188886774015728},{"term":"traumatisch","score":20.188886774015728},{"term":"voorzicht","score":20.183404815352883},{"term":"zaandam","score":20.179758506229852},{"term":"fusie","score":20.174301476923443},{"term":"charg","score":20.165239386204245},{"term":"optrad","score":20.150824909394345},{"term":"zijkant","score":20.14723746448629},{"term":"ijssel","score":20.11700122831279},{"term":"fotokopie","score":20.10819452099625},{"term":"rechtspositie","score":20.10819452099625},{"term":"funder","score":20.10292905064465},{"term":"groei","score":20.097677406489318},{"term":"°,","score":20.087215307654212},{"term":"][","score":20.073350434102924},{"term":"schadeplicht","score":20.073350434102924},{"term":"video","score":20.069899182167983},{"term":"pay","score":20.064733445331473},{"term":"belastingadviseur","score":20.061297018369203},{"term":"gelad","score":20.057866485795692},{"term":"aannam","score":20.057866485795692},{"term":"tenaamstell","score":20.04590574959272},{"term":"top","score":20.04590574959272},{"term":"let","score":20.044202897212283},{"term":"identiteitskaart","score":20.035710315449382},{"term":"uitwissel","score":20.03401611797408},{"term":"arbeidsverhoud","score":20.03401611797408},{"term":"urenbeperk","score":20.032323354436716},{"term":"p_NUM","score":20.02051395698724},{"term":"dodelijk","score":20.018832578157653},{"term":"gehinderd","score":20.007102339252857},{"term":"jongst","score":19.998765520178498},{"term":"haarlemmermer","score":19.997102318419447},{"term":"benoemt","score":19.99378005851929},{"term":"n_NUM","score":19.99046330817374},{"term":"verkoopt","score":19.98549847318723},{"term":"bouwbedrijf","score":19.97231885382912},{"term":"vastgebond","score":19.95596553295463},{"term":"geoordeeld","score":19.942312139028314},{"term":"echtscheidingsconvenant","score":19.938129987110518},{"term":"meenem","score":19.938129987110518},{"term":"litigieuz","score":19.933293225381956},{"term":"acties","score":19.93168356734738},{"term":"report","score":19.90769287584803},{"term":"component","score":19.904515750477756},{"term":"bijwon","score":19.893435389726527},{"term":"subject","score":19.876146480493844},{"term":"reageert","score":19.86211133517655},{"term":"schep","score":19.859005744490428},{"term":"usd","score":19.85745475565737},{"term":"puntig","score":19.854356381683708},{"term":"vermogensbelast","score":19.848173996544364},{"term":"doz","score":19.846631381735914},{"term":"dwangmiddel","score":19.83127032312036},{"term":"verzekeringsovereenkomst","score":19.823633811360736},{"term":"amerikan","score":19.823633811360736},{"term":"vecht","score":19.823633811360736},{"term":"einddatum","score":19.809961141324152},{"term":"pi","score":19.80240540887719},{"term":"volgd","score":19.797885632942315},{"term":"onderhoudsplicht","score":19.797885632942315},{"term":"part","score":19.788876608700104},{"term":"interest","score":19.772464729959456},{"term":"betwen","score":19.7694951674243},{"term":"gezondheidstoestand","score":19.76061284342436},{"term":"albert","score":19.748830776949887},{"term":"db","score":19.74589607356199},{"term":"gz","score":19.73274293883286},{"term":"vakantiegeld","score":19.713895390869105},{"term":"handvest","score":19.706693366521918},{"term":"belastingwet","score":19.703819802809193},{"term":"inroep","score":19.696653904781293},{"term":"trok","score":19.68951359016388},{"term":"familierecht","score":19.68808858119729},{"term":"bonus","score":19.679559783624377},{"term":"rom","score":19.679559783624377},{"term":"tussenbeschik","score":19.67530898499975},{"term":"geneesmiddel","score":19.658395564963303},{"term":"criminal","score":19.648595055327622},{"term":"bc_NUM","score":19.64719889471246},{"term":"stem","score":19.643016251152464},{"term":"spitst","score":19.633290632821062},{"term":"betalingsonmacht","score":19.63052054927574},{"term":"overdrachtsbelast","score":19.616727391157617},{"term":"bb","score":19.612607925231597},{"term":"saner","score":19.594854309165928},{"term":"doelbewust","score":19.52931852268816},{"term":"sit","score":19.52537504457848},{"term":"kran","score":19.51751133851068},{"term":"privacy","score":19.49669137170511},{"term":"vroeger","score":19.48250234089213},{"term":"training","score":19.47865001225325},{"term":"gelijkwaard","score":19.47352508952585},{"term":"doelmat","score":19.472245908158293},{"term":"voorval","score":19.46076994425225},{"term":"kinderbijslag","score":19.45062407594986},{"term":"liter","score":19.44557037774604},{"term":"perspectief","score":19.443048309315987},{"term":"vuurwapen","score":19.43801369369323},{"term":"cjib","score":19.429233497716364},{"term":"declaraties","score":19.413028984014854},{"term":"einduitsprak","score":19.406831272206723},{"term":"us","score":19.39326353593073},{"term":"berging","score":19.390806533164714},{"term":"lb","score":19.38467719952891},{"term":"branch","score":19.362765842206958},{"term":"anoniem","score":19.355515078978325},{"term":"cre","score":19.34348858453142},{"term":"kring","score":19.332726226722386},{"term":"meestal","score":19.329151605574193},{"term":"commissariss","score":19.32677206930302},{"term":"straftoemet","score":19.312553999944804},{"term":"waardebepal","score":19.3113737107397},{"term":"ovc","score":19.30665950702494},{"term":"eeg","score":19.289079552053355},{"term":"betalingsregel","score":19.285582028573852},{"term":"hevig","score":19.283253739603104},{"term":"asielaanvrag","score":19.255523659328762},{"term":"uiten","score":19.24180147856887},{"term":"realisatie","score":19.23952357399461},{"term":"surinam","score":19.219138346549773},{"term":"mailadres","score":19.21351246379654},{"term":"mvt","score":19.211266534307295},{"term":"ingeleid","score":19.210144514642522},{"term":"rechtszeker","score":19.209023124088894},{"term":", ee","score":19.209023124088894},{"term":"asbest","score":19.206782227495914},{"term":"oplop","score":19.195615252644515},{"term":"roerend","score":19.183403169184345},{"term":"expert","score":19.167967590913587},{"term":"may","score":19.16467540910025},{"term":"softdrug","score":19.151560608007934},{"term":"vereffenar","score":19.147208049162742},{"term":"onteigen","score":19.142864942135727},{"term":"vreemdelingenwet","score":19.134206919956554},{"term":"aanbevel","score":19.127737911621235},{"term":"verbaalnummer","score":19.12021708621297},{"term":"ob","score":19.11486233067154},{"term":"kredietovereenkomst","score":19.101006693054497},{"term":"uitzet","score":19.099944840096185},{"term":"kwekerij","score":19.095703056957912},{"term":"ondergebracht","score":19.08830151533575},{"term":"minderjarige_NUM","score":19.08513779202906},{"term":"schors","score":19.071485861888256},{"term":"toezichthouder","score":19.0579264879175},{"term":"bewijsopdracht","score":19.056887256925034},{"term":"gewet","score":19.053772800027822},{"term":"fair","score":19.052735724557344},{"term":"eex","score":19.04445842355481},{"term":"misslag","score":19.042394439883502},{"term":"gooi","score":19.035187224876903},{"term":"bedrijfsmat","score":19.034159739624403},{"term":"politiek","score":19.022892104187893},{"term":"rek","score":19.021870914479724},{"term":"vertell","score":19.01473714895458},{"term":"under","score":19.000545502991642},{"term":"onherstel","score":18.989465142240412},{"term":"vervaard","score":18.981445081368005},{"term":"opzegtermijn","score":18.972460788745565},{"term":"gefinancierd","score":18.96153451374313},{"term":"nou","score":18.95658768955409},{"term":"audi","score":18.95658768955409},{"term":"depot","score":18.954612380751545},{"term":"geboort","score":18.94673059715179},{"term":"echtscheidingsbeschik","score":18.933011778050005},{"term":"omgekeerd","score":18.930084234224044},{"term":"gebeurtenis","score":18.92132722680155},{"term":"hinder","score":18.8952839362144},{"term":"gescheid","score":18.89336822725567},{"term":"xx","score":18.88763208565633},{"term":"itali","score":18.881912348678803},{"term":"dhr","score":18.87715836498096},{"term":"reconventionel","score":18.872415654672164},{"term":"portemonnee","score":18.867684164412424},{"term":"bk","score":18.85919558765177},{"term":"%)","score":18.8516803134261},{"term":"gift","score":18.850742886645406},{"term":"vull","score":18.841392705987417},{"term":"donderdag","score":18.836733957083784},{"term":"andersluid","score":18.826522749012383},{"term":"woonadres","score":18.82559703735503},{"term":"geluid","score":18.809007080479564},{"term":"leges","score":18.802592412895837},{"term":"respect","score":18.79073376925928},{"term":"gewoond","score":18.78982447167599},{"term":"meijer","score":18.77442933113618},{"term":"aanschrijv","score":18.758256736273466},{"term":"lijk","score":18.75289480276695},{"term":"afschrijv","score":18.73600956583614},{"term":"geduid","score":18.735124805812863},{"term":"onvervalst","score":18.732472871722656},{"term":"tafel","score":18.731589674534536},{"term":"vervaardigd","score":18.714882608817003},{"term":"vrijheidsbenem","score":18.71313206165929},{"term":"spanning","score":18.71313206165929},{"term":"inbeslagnem","score":18.709635557272232},{"term":"rekeninghouder","score":18.683604821048093},{"term":"betred","score":18.6801595144252},{"term":"betracht","score":18.67757942376422},{"term":"mercedes","score":18.67757942376422},{"term":"energie","score":18.66643733366531},{"term":"ontnem","score":18.66131569194196},{"term":"overgangsrecht","score":18.657908536092393},{"term":"aantreff","score":18.62582680134382},{"term":"stuit","score":18.614968557926296},{"term":"loonheff","score":18.60416894705385},{"term":"mm","score":18.5934273389156},{"term":"buik","score":18.58766720327386},{"term":"verondersteld","score":18.57864894655086},{"term":"gefactureerd","score":18.57537963646447},{"term":"alwar","score":18.558302891124757},{"term":"leent","score":18.538161733997566},{"term":"netwerk","score":18.525377032053633},{"term":"pandrecht","score":18.516634717319075},{"term":"ruimtelijk","score":18.51188223983531},{"term":"onderzoeker","score":18.50241103034364},{"term":"bewon","score":18.49533694161657},{"term":"recidiv","score":18.488287786091227},{"term":"woonkamer","score":18.485943577969753},{"term":"succes","score":18.478149335837024},{"term":"film","score":18.474263575920315},{"term":"gemeentewet","score":18.472711383370537},{"term":"onnod","score":18.463423440981636},{"term":"district","score":18.4549472194715},{"term":"v_NUM","score":18.44650676957616},{"term":"bang","score":18.443446325214627},{"term":"ler","score":18.42518135808808},{"term":"arbeidskund","score":18.411591271822424},{"term":"installatie","score":18.401833324148882},{"term":"golf","score":18.378014657815907},{"term":"turkij","score":18.371366452248232},{"term":"bz_NUM","score":18.369155285632733},{"term":"inkop","score":18.357403507421395},{"term":"wabo","score":18.348634768493596},{"term":"opgehaald","score":18.33772763366231},{"term":"handelsrent","score":18.325437698007732},{"term":"tegelijkertijd","score":18.316807695404908},{"term":"igen","score":18.31465600073798},{"term":"bouwvergunn","score":18.31250661847845},{"term":"polis","score":18.306786199848588},{"term":"woensdag","score":18.30321922904399},{"term":"gegevensdrager","score":18.289014623879858},{"term":"toegank","score":18.27772313305471},{"term":"boer","score":18.27139957254346},{"term":"gepakt","score":18.269997047941803},{"term":"achterzijd","score":18.264396764268582},{"term":"betek","score":18.2393877548301},{"term":"dwang","score":18.226313564111706},{"term":"detentie","score":18.22015023103689},{"term":"opspor","score":18.216734346346577},{"term":"reclasseringstoezicht","score":18.205842556323653},{"term":"ondernom","score":18.202451000482924},{"term":"beginn","score":18.201773378928078},{"term":"jas","score":18.19636065573259},{"term":"institut","score":18.181550759719435},{"term":"profiel","score":18.17686142580093},{"term":"tegenbewijs","score":18.17485507218007},{"term":"material","score":18.172850729269705},{"term":"schur","score":18.163523597108714},{"term":"toet","score":18.162194693405336},{"term":"afsluit","score":18.1522559616722},{"term":"eerlijk","score":18.11882899972472},{"term":"verstat","score":18.109749866104572},{"term":"eran","score":18.104580234793307},{"term":"bell","score":18.096207979772238},{"term":"actie","score":18.071299466854256},{"term":"oogpunt","score":18.059907030986963},{"term":"beschadigd","score":18.052347966870517},{"term":"originel","score":18.045443833229406},{"term":"compenseert","score":18.043565016745532},{"term":"straatnam","score":18.040437572793167},{"term":"exact","score":18.033574362189775},{"term":"gevoelen","score":18.0329516002355},{"term":"les","score":18.022394208852255},{"term":"keuk","score":18.02115581871981},{"term":"bekom","score":18.013741537754168},{"term":"kwalificeert","score":18.003284801647297},{"term":"’ (","score":17.98618023801666},{"term":"alsdan","score":17.982533928893634},{"term":"coffeeshop","score":17.98131996831008},{"term":"tenslot","score":17.977076899587225},{"term":"eindigt","score":17.94523961519461},{"term":"achterstall","score":17.94523961519461},{"term":"afgenom","score":17.929804691451753},{"term":"rest","score":17.928031379676053},{"term":"verdov","score":17.918600180354414},{"term":"vloer","score":17.916836770982275},{"term":"bedrijfsnam","score":17.894052823691755},{"term":"gekek","score":17.89057052449512},{"term":"blijvend","score":17.876701644880654},{"term":"complex","score":17.87497477759824},{"term":"bod","score":17.857787619406444},{"term":"compenser","score":17.85322921287472},{"term":"geschopt","score":17.8486811722565},{"term":"vervuld","score":17.835098777100498},{"term":"vervalst","score":17.835098777100498},{"term":"tekortgeschot","score":17.83059173293961},{"term":"has","score":17.818808840112588},{"term":"belemmer","score":17.797662288073525},{"term":"geuit","score":17.78827389585101},{"term":"pv","score":17.782222454017937},{"term":",--,","score":17.77235942033039},{"term":"kunt","score":17.767446083616093},{"term":"ensched","score":17.76635586827157},{"term":"raakt","score":17.74845258854158},{"term":"relas","score":17.744136428247607},{"term":"daaromtrent","score":17.741981836065083},{"term":"ontstat","score":17.712589189484692},{"term":"blauw","score":17.70887910494389},{"term":"gepasseerd","score":17.687809644878346},{"term":"onverschuldigd","score":17.678921903157622},{"term":"onpartijd","score":17.665404825451052},{"term":"hoev","score":17.663333359703987},{"term":"bestuurd","score":17.647865378504513},{"term":"voorkomt","score":17.63761945445676},{"term":"groen","score":17.626409234024514},{"term":"nee","score":17.624885424426992},{"term":"spoedig","score":17.62285548261355},{"term":"bewijsstuk","score":17.622348318923752},{"term":"overstan","score":17.595150720248114},{"term":"bijnam","score":17.57227059008808},{"term":"illegal","score":17.558961853362845},{"term":"arbeidsdeskund","score":17.538920136098803},{"term":"consequenties","score":17.536003975193335},{"term":"gedetineerd","score":17.5094687891857},{"term":"]’","score":17.508510475798893},{"term":"bed","score":17.493715037843515},{"term":"uitwerk","score":17.483754149235224},{"term":"opgetred","score":17.471489959861536},{"term":"transactie","score":17.46632381867744},{"term":"ontleend","score":17.463511537923463},{"term":"band","score":17.46117098762708},{"term":"zwaarweg","score":17.425927784927442},{"term":"drug","score":17.376441660340586},{"term":"uitvoeringsinstitut","score":17.3724098525019},{"term":"offert","score":17.35547570498751},{"term":"herinner","score":17.34308902527033},{"term":"huisbezoek","score":17.342207191040877},{"term":"voortvloeit","score":17.33516647969924},{"term":"gebrekk","score":17.329902164310667},{"term":"nagekom","score":17.305949782217233},{"term":"gedacht","score":17.29817271473785},{"term":"rvs","score":17.287421156752163},{"term":"rod","score":17.286992294308146},{"term":"hersteld","score":17.28527776355591},{"term":"tas","score":17.28399282936459},{"term":"hanter","score":17.281425434917033},{"term":"professionel","score":17.261004070513117},{"term":"definitief","score":17.259311665081054},{"term":"beleidsregel","score":17.25846599910651},{"term":"bevrijd","score":17.255508980931342},{"term":"negatief","score":17.25044995631143},{"term":"kaart","score":17.226592814580883},{"term":"ga","score":17.22077644155756},{"term":"koopprijs","score":17.193581955828495},{"term":"fundamentel","score":17.149864368203495},{"term":"privaatrecht","score":17.142271192932327},{"term":"ingeschakeld","score":17.138684462868714},{"term":"lijdt","score":17.138286334072735},{"term":"opgelop","score":17.0953586252482},{"term":"verlangd","score":17.09419005962921},{"term":"beding","score":17.080993815363854},{"term":"rug","score":17.080220270585308},{"term":"onzorgvuld","score":17.04914050846088},{"term":"niveau","score":16.99542331571351},{"term":"kijk","score":16.995052705131517},{"term":"best","score":16.975507975054224},{"term":"bezoek","score":16.96562418080136},{"term":"appartement","score":16.95215856720132},{"term":"gewenst","score":16.947448846702276},{"term":"omvat","score":16.936262529763905},{"term":"verwijderd","score":16.932308183189466},{"term":"hierteg","score":16.927286659748084},{"term":"bijbehor","score":16.915856016023717},{"term":"bekrachtigd","score":16.886508720253097},{"term":"]”","score":16.87007936980309},{"term":"totdat","score":16.857239905216222},{"term":"invull","score":16.8527482388211},{"term":"gebruiker","score":16.846201588131162},{"term":"bijna","score":16.844482341055464},{"term":"vooral","score":16.835908204627625},{"term":"rekeningnummer","score":16.833855869917},{"term":"opeis","score":16.82839325129401},{"term":"dr","score":16.82328556144889},{"term":"nauw","score":16.817512571618362},{"term":"aanbreng","score":16.7996206470791},{"term":"wellicht","score":16.79626268792895},{"term":"toestand","score":16.7875582885181},{"term":"nalat","score":16.78455403192383},{"term":"volksverzeker","score":16.775568244281363},{"term":"zoek","score":16.75147355563584},{"term":"gezin","score":16.750489545775892},{"term":"duurzam","score":16.738393064274618},{"term":"ogen","score":16.732859656887822},{"term":"contractuel","score":16.708307579860513},{"term":"sociaal","score":16.69518648311487},{"term":"m_NUM","score":16.675191077335942},{"term":"verwijder","score":16.67424388338028},{"term":"krijgt","score":16.663540253236384},{"term":"postbus","score":16.663226306135826},{"term":"boek","score":16.658209843245626},{"term":"tijdvak","score":16.651020650539373},{"term":"bewog","score":16.648214507745248},{"term":"want","score":16.644479098099076},{"term":"bestemm","score":16.62190444791351},{"term":"nacht","score":16.610864861466315},{"term":"nabij","score":16.60079849384165},{"term":"geschied","score":16.59290294760196},{"term":"uitgebreid","score":16.591085316455953},{"term":"dienaangaand","score":16.582926356738543},{"term":"letter","score":16.580816492930484},{"term":"rolraadsher","score":16.57630283721196},{"term":"klaagt","score":16.552103572679876},{"term":"doorgenummerd","score":16.527900207643235},{"term":"par","score":16.496463088257727},{"term":"aangepast","score":16.493865497878865},{"term":"onaanvaard","score":16.47377636355732},{"term":"ton","score":16.457849089394227},{"term":"zichtbar","score":16.451344098861558},{"term":"eg","score":16.42637008091876},{"term":"zogenaamd","score":16.42135684893862},{"term":"snel","score":16.415523916535285},{"term":"twistged","score":16.397977694624455},{"term":"biourbanism","score":16.397977694624455},{"term":"delimar","score":16.397977694624455},{"term":"kaasspecialist","score":16.397977694624455},{"term":"p_NUMrocesverlop","score":16.397977694624455},{"term":"merwestrom","score":16.397977694624455},{"term":"qdh","score":16.397977694624455},{"term":",-€","score":16.397977694624455},{"term":"applicationplaza","score":16.397977694624455},{"term":"knowledgeplaza","score":16.397977694624455},{"term":"_NUMmaatstaf","score":16.397977694624455},{"term":"_NUMbelastingtariev","score":16.397977694624455},{"term":"_NUMheffingstijdvak","score":16.397977694624455},{"term":"_NUMwijz","score":16.397977694624455},{"term":"aansprakelijkheidd","score":16.397977694624455},{"term":"autopalac","score":16.397977694624455},{"term":"pocesverlop","score":16.397977694624455},{"term":"veroeker","score":16.397977694624455},{"term":"rechthdi","score":16.397977694624455},{"term":"devorderingtottenuitvoerlegg","score":16.397977694624455},{"term":"capeholland","score":16.397977694624455},{"term":"hetverloopvandeprocedur","score":16.397977694624455},{"term":"modecentrum","score":16.397977694624455},{"term":"geintimed","score":16.397977694624455},{"term":"besiss","score":16.397977694624455},{"term":"_NUMsek","score":16.397977694624455},{"term":"hendricus","score":16.397977694624455},{"term":"besllis","score":16.397977694624455},{"term":"vennootschaptosca","score":16.397977694624455},{"term":"_NUMbegripp","score":16.397977694624455},{"term":"aansprakelijkheidtennet","score":16.397977694624455},{"term":"procescomplicaties","score":16.397977694624455},{"term":"inbeginsel","score":16.397977694624455},{"term":"strategis","score":16.397977694624455},{"term":"cather","score":16.397977694624455},{"term":"podt","score":16.397977694624455},{"term":"atoem","score":16.397977694624455},{"term":"sectoroverschrijd","score":16.397977694624455},{"term":"tennant","score":16.397977694624455},{"term":"uitspraakvan","score":16.397977694624455},{"term":"vbverhog","score":16.397977694624455},{"term":"kusmierz","score":16.397977694624455},{"term":"mula","score":16.397977694624455},{"term":"_NUMhr_NUM","score":16.397977694624455},{"term":"mechanisatiecentrum","score":16.397977694624455},{"term":"valthermond","score":16.397977694624455},{"term":"fierljepp","score":16.397977694624455},{"term":"dingenis","score":16.397977694624455},{"term":"werknemersrol","score":16.397977694624455},{"term":"verwerengevoerd","score":16.397977694624455},{"term":"indezeaanslagenwordtafgewekenvandeaangegevenbelastbarewinstoverdedesbetreffendejar","score":16.397977694624455},{"term":"islaagt","score":16.397977694624455},{"term":"iislaagt","score":16.397977694624455},{"term":"_NUMverweerder","score":16.397977694624455},{"term":"_NUMaangetroff","score":16.397977694624455},{"term":"doelstellingsticht","score":16.397977694624455},{"term":"indelingsgebakkelei","score":16.397977694624455},{"term":"filippij","score":16.397977694624455},{"term":"vorderingtotherroepingvandevoorwaardelijkeinvrijheidstell","score":16.397977694624455},{"term":"middelkan","score":16.397977694624455},{"term":"bluemarin","score":16.397977694624455},{"term":"deelnemingsmaatschappij","score":16.397977694624455},{"term":"beleggingsverzekeringenpoliss","score":16.397977694624455},{"term":"hedzer","score":16.397977694624455},{"term":"grootlandbouwbedrijf","score":16.397977694624455},{"term":"outonomo","score":16.397977694624455},{"term":"andro","score":16.397977694624455},{"term":"auhag","score":16.397977694624455},{"term":"iverklaart","score":16.397977694624455},{"term":"deopleggingvanstrafenmaatregel","score":16.397977694624455},{"term":"aitlines","score":16.397977694624455},{"term":"dewettelijkevoorschrift","score":16.397977694624455},{"term":"naklyat","score":16.397977694624455},{"term":"nakliyat","score":16.397977694624455},{"term":"besslisng","score":16.397977694624455},{"term":"rechtsbevoegdheidabvakabo","score":16.397977694624455},{"term":"bonusvoorwaard","score":16.397977694624455},{"term":"vennootschaplondon","score":16.397977694624455},{"term":"zijkamertj","score":16.397977694624455},{"term":"aontvank","score":16.397977694624455},{"term":"aanzein","score":16.397977694624455},{"term":"_NUMexclusiviteit","score":16.397977694624455},{"term":"rechstmiddel","score":16.397977694624455},{"term":"leocadia","score":16.397977694624455},{"term":"hendrina","score":16.397977694624455},{"term":"marlijn","score":16.397977694624455},{"term":"eilandsontvanger","score":16.397977694624455},{"term":"oerweg","score":16.397977694624455},{"term":"peratieco","score":16.397977694624455},{"term":"certificatenplan","score":16.397977694624455},{"term":"scheepsexpertis","score":16.397977694624455},{"term":"bunkerlever","score":16.397977694624455},{"term":"fleurijn","score":16.397977694624455},{"term":"gascoign","score":16.397977694624455},{"term":"melot","score":16.397977694624455},{"term":"firmalanouvell","score":16.397977694624455},{"term":"debenadeeldepartij","score":16.397977694624455},{"term":"contactplicht","score":16.397977694624455},{"term":"pauzesoftwar","score":16.397977694624455},{"term":"veredacht","score":16.397977694624455},{"term":"_NUMperoogst","score":16.397977694624455},{"term":"peroogst","score":16.397977694624455},{"term":"middeldeelt","score":16.397977694624455},{"term":"renteconversie","score":16.397977694624455},{"term":"domeinstrijd","score":16.397977694624455},{"term":"boyd","score":16.397977694624455},{"term":"projectomstand","score":16.397977694624455},{"term":"swartemer","score":16.397977694624455},{"term":"disquisition","score":16.397977694624455},{"term":"beleggingseffect","score":16.397977694624455},{"term":"besprekingvanmiddel_NUM","score":16.397977694624455},{"term":"redelijkeschat","score":16.397977694624455},{"term":"vennootschappostbank","score":16.397977694624455},{"term":"-\\","score":16.397977694624455},{"term":"eensysteemplafond","score":16.397977694624455},{"term":"eenwap","score":16.397977694624455},{"term":"vennootschaprefineria","score":16.397977694624455},{"term":"firmav","score":16.397977694624455},{"term":"pilastri","score":16.397977694624455},{"term":"kilometerperuurgold","score":16.397977694624455},{"term":"appellantenwoonplaatsprocedurenummer","score":16.397977694624455},{"term":"nduizendnegenhonderddrie","score":16.397977694624455},{"term":"] (†),","score":16.397977694624455},{"term":"blijheid","score":16.397977694624455},{"term":"spiegelgevecht","score":16.397977694624455},{"term":"geijzeld","score":16.397977694624455},{"term":"zknr","score":16.397977694624455},{"term":"rlnr","score":16.397977694624455},{"term":")* = ((","score":16.397977694624455},{"term":"gasoil","score":16.397977694624455},{"term":"vennootschapvgz","score":16.397977694624455},{"term":"tankstorag","score":16.397977694624455},{"term":"_NUMbox","score":16.397977694624455},{"term":"devorderingendestandpuntenvanpartij","score":16.397977694624455},{"term":"relevantewet","score":16.397977694624455},{"term":"enregelgev","score":16.397977694624455},{"term":"jurisprudentieenliteratur","score":16.397977694624455},{"term":"wetgevingsbeginsel","score":16.397977694624455},{"term":"volkert","score":16.397977694624455},{"term":"rorij","score":16.397977694624455},{"term":"aazien","score":16.397977694624455},{"term":"marvin","score":16.397977694624455},{"term":"klaxina","score":16.397977694624455},{"term":"otterstation","score":16.397977694624455},{"term":"tweewielerspecialist","score":16.397977694624455},{"term":"invorderingsrenterent","score":16.397977694624455},{"term":"_NUMbedraagt","score":16.397977694624455},{"term":"overwegng","score":16.397977694624455},{"term":"firmak","score":16.397977694624455},{"term":"falkplan","score":16.397977694624455},{"term":"vennootschapvan","score":16.397977694624455},{"term":"testerink","score":16.397977694624455},{"term":"dichtung","score":16.397977694624455},{"term":"procesgoedkeur","score":16.397977694624455},{"term":"rotger","score":16.397977694624455},{"term":"geuig","score":16.397977694624455},{"term":"nzk","score":16.397977694624455},{"term":"specjfiek","score":16.397977694624455},{"term":"teinveld","score":16.397977694624455},{"term":"_NUMhij","score":16.397977694624455},{"term":"patrimoin","score":16.397977694624455},{"term":"oleagineux","score":16.397977694624455},{"term":"doormiddelvanhetopenlijktentoonstellen","score":16.397977694624455},{"term":"aanslaanvan","score":16.397977694624455},{"term":"ofdegoedenaamvan","score":16.397977694624455},{"term":"heeftaangeranddoortelasteleggingvanen","score":16.397977694624455},{"term":"meerbepaaldefeit","score":16.397977694624455},{"term":"methetkennelijkedoelomdaaraanruchtbaarheidtegev","score":16.397977694624455},{"term":"immersheeftverdachtemetvoormelddoel","score":16.397977694624455},{"term":"ophaarfacebok","score":16.397977694624455},{"term":"omgevinggeplaatst","score":16.397977694624455},{"term":"maarjahetfeitblijftheballeskunnenvertellenookzijnstrafbarefeitvanhetzelfdewathijalvoorzijn_NUMeheeftgeflikt","score":16.397977694624455},{"term":"zekernietnuhijweetwatikweetdathijvoorzijn_NUMeookaleenzedendelictheeftgepleegd","score":16.397977694624455},{"term":"appellantis","score":16.397977694624455},{"term":"hanseatic","score":16.397977694624455},{"term":"verhuurkantor","score":16.397977694624455},{"term":"datumomschrijvingbedrag","score":16.397977694624455},{"term":"percy","score":16.397977694624455},{"term":"detoepasselijkewetsartikel","score":16.397977694624455},{"term":"brinkel","score":16.397977694624455},{"term":"isenmedepleg","score":16.397977694624455},{"term":", (…), [","score":16.397977694624455},{"term":"toevertrouwdeminderjar","score":16.397977694624455},{"term":"eencommunicatiedienst","score":16.397977694624455},{"term":"borgstellingsvergoedingtarief","score":16.397977694624455},{"term":"middelvalt","score":16.397977694624455},{"term":"informationshall","score":16.397977694624455},{"term":"servicesshall","score":16.397977694624455},{"term":"madja","score":16.397977694624455},{"term":"vermoedendat","score":16.397977694624455},{"term":"subsidiairpog","score":16.397977694624455},{"term":"iiienhandel","score":16.397977694624455},{"term":"cassatierond","score":16.397977694624455},{"term":"dealergroep","score":16.397977694624455},{"term":"enopzet","score":16.397977694624455},{"term":"vanessa","score":16.397977694624455},{"term":"aansprakelijkheidbb","score":16.397977694624455},{"term":"bewijsoverweqinq","score":16.397977694624455},{"term":"ofgennep","score":16.397977694624455},{"term":"rechtmatigheidvandeaanhoud","score":16.397977694624455},{"term":"afrikahav","score":16.397977694624455},{"term":"fraudebepal","score":16.397977694624455},{"term":"eenheidsverliez","score":16.397977694624455},{"term":"hotelaccomodatie","score":16.397977694624455},{"term":"sslu","score":16.397977694624455},{"term":"cingular","score":16.397977694624455},{"term":"wayport","score":16.397977694624455},{"term":"nextel","score":16.397977694624455},{"term":"andertoebehoort","score":16.397977694624455},{"term":"metingang","score":16.397977694624455},{"term":"zaakmanagement","score":16.397977694624455},{"term":"waterfiltration","score":16.397977694624455},{"term":"vennootschapbruscom","score":16.397977694624455},{"term":"waarderingsgegeven","score":16.397977694624455},{"term":"zaaknummereiserobject","score":16.397977694624455},{"term":"→ → → €","score":16.397977694624455},{"term":"workx","score":16.397977694624455},{"term":"primairvals","score":16.397977694624455},{"term":"bekendmakingverorden","score":16.397977694624455},{"term":"vestigingsverker","score":16.397977694624455},{"term":"kinderverblijf","score":16.397977694624455},{"term":"hereinafterrefertoas","score":16.397977694624455},{"term":"alexandria","score":16.397977694624455},{"term":"echtgenooteiseres","score":16.397977694624455},{"term":"doodslagt","score":16.397977694624455},{"term":"stoepplat","score":16.397977694624455},{"term":"lopsol","score":16.397977694624455},{"term":"debenadeeldepartijenendeschadevergoedingsmaatregel","score":16.397977694624455},{"term":"lultin","score":16.397977694624455},{"term":"hiernatenoem","score":16.397977694624455},{"term":"daytoday","score":16.397977694624455},{"term":"donieinstrijd","score":16.397977694624455},{"term":"salsalerar","score":16.397977694624455},{"term":"neonfabriek","score":16.397977694624455},{"term":"hijwet","score":16.397977694624455},{"term":"hetpleg","score":16.397977694624455},{"term":"feitt","score":16.397977694624455},{"term":"ofiii","score":16.397977694624455},{"term":"ofbijbehor","score":16.397977694624455},{"term":"verklaringvan","score":16.397977694624455},{"term":"ofmeerder","score":16.397977694624455},{"term":"ofotterlo","score":16.397977694624455},{"term":"ofelder","score":16.397977694624455},{"term":"tweedemiddel","score":16.397977694624455},{"term":"eerstemiddel","score":16.397977694624455},{"term":"rolonr","score":16.397977694624455},{"term":"verkenningmogelijkhedentotuitvoerenonderzoek","score":16.397977694624455},{"term":"engroupest","score":16.397977694624455},{"term":"serguei","score":16.397977694624455},{"term":"ivanovitsj","score":16.397977694624455},{"term":"caldaza","score":16.397977694624455},{"term":"maderas","score":16.397977694624455},{"term":"onderzoekopdrachtuitvoerder","score":16.397977694624455},{"term":"deimmateri","score":16.397977694624455},{"term":"dagder","score":16.397977694624455},{"term":"gerichtt","score":16.397977694624455},{"term":"_NUMopschort","score":16.397977694624455},{"term":"projectontwikkelingsadvies","score":16.397977694624455},{"term":"taxatieonderbouw","score":16.397977694624455},{"term":"vedracht","score":16.397977694624455},{"term":"hoofdondernemer","score":16.397977694624455},{"term":"sleutelstad","score":16.397977694624455},{"term":"subsidiairovertred","score":16.397977694624455},{"term":"_NUMdel","score":16.397977694624455},{"term":"marktwaardemaatstaf","score":16.397977694624455},{"term":"vorderingingedi","score":16.397977694624455},{"term":"agh","score":16.397977694624455},{"term":"statusbeoordel","score":16.397977694624455},{"term":"getuiget","score":16.397977694624455},{"term":"cadrio","score":16.397977694624455},{"term":"_NUMbesliss","score":16.397977694624455},{"term":"middelniet","score":16.397977694624455},{"term":"boetebeoordel","score":16.397977694624455},{"term":"debeoordelingvanhetbewijs","score":16.397977694624455},{"term":"derdenamen","score":16.397977694624455},{"term":"vierdenamen","score":16.397977694624455},{"term":"hoofdstaf","score":16.397977694624455},{"term":"iibeoordel","score":16.397977694624455},{"term":"middelenklag","score":16.397977694624455},{"term":"nisstraf","score":16.397977694624455},{"term":"beoordelingsliss","score":16.397977694624455},{"term":"bewijsmiddelen","score":16.397977694624455},{"term":"kredietaanpass","score":16.397977694624455},{"term":"destukkenendeprocesgang","score":16.397977694624455},{"term":"deprocesgang","score":16.397977694624455},{"term":"sprek","score":16.392635007631554},{"term":"overschred","score":16.38551853645809},{"term":"vermeldt","score":16.38415288259827},{"term":"beter","score":16.38142436921464},{"term":"achterweg","score":16.35622590542361},{"term":"ingenom","score":16.353535201398774},{"term":"toekom","score":16.35272869557428},{"term":"desgevraagd","score":16.344949194115078},{"term":"benoemd","score":16.338000102532288},{"term":"websit","score":16.300729480592036},{"term":"gebeld","score":16.284818082681422},{"term":"getrok","score":16.28196223745028},{"term":"bestemmingsplan","score":16.281443430702883},{"term":"terzak","score":16.24851664199136},{"term":"bewijz","score":16.233773311373643},{"term":"uitgan","score":16.21511928151296},{"term":"geformuleerd","score":16.213865127085455},{"term":"akkoord","score":16.206356671263766},{"term":"wederom","score":16.155059161335796},{"term":"schuldeiser","score":16.15238259910166},{"term":"nakost","score":16.138811724301405},{"term":"sterk","score":16.138328749172274},{"term":"factor","score":16.12893397128303},{"term":"bestuursorgan","score":16.110752027880526},{"term":"aangegan","score":16.086371585134085},{"term":"geraakt","score":16.08190651072208},{"term":"formulier","score":16.07768561573299},{"term":"deugdelijk","score":16.075812517960863},{"term":"volstan","score":16.06927045605296},{"term":"meent","score":16.041012892766712},{"term":"ontvangt","score":16.040323039353723},{"term":"ner","score":16.026804055646117},{"term":"pleitnota","score":16.026119085036076},{"term":"_NUMf","score":16.017917683863455},{"term":"pvv","score":16.008617978987434},{"term":"drs","score":16.00206615160879},{"term":"fosloconfrontatie","score":15.992512586516293},{"term":"vaillantlan","score":15.992512586516293},{"term":"faunawerkgroep","score":15.992512586516293},{"term":"tourdirectie","score":15.992512586516293},{"term":"societas","score":15.992512586516293},{"term":"regendi","score":15.992512586516293},{"term":"administrandi","score":15.992512586516293},{"term":"ntimer","score":15.992512586516293},{"term":"______________________","score":15.992512586516293},{"term":"outdoord","score":15.992512586516293},{"term":"aywaill","score":15.992512586516293},{"term":"marianus","score":15.992512586516293},{"term":"ontvankelijkheidsperikel","score":15.992512586516293},{"term":"reumken","score":15.992512586516293},{"term":"kraggenburg","score":15.992512586516293},{"term":"circulus","score":15.992512586516293},{"term":"eehzerwold","score":15.992512586516293},{"term":"verbindingskost","score":15.992512586516293},{"term":"mobilofonie","score":15.992512586516293},{"term":"punctually","score":15.992512586516293},{"term":"whishes","score":15.992512586516293},{"term":"safecom","score":15.992512586516293},{"term":"indraait","score":15.992512586516293},{"term":"zorgkwekerij","score":15.992512586516293},{"term":"lauwersland","score":15.992512586516293},{"term":"ooft","score":15.992512586516293},{"term":"wyckerveldflat","score":15.992512586516293},{"term":"resultaatcompartimenter","score":15.992512586516293},{"term":"beerthuiz","score":15.992512586516293},{"term":"middelklaagt","score":15.992512586516293},{"term":"beslisss","score":15.992512586516293},{"term":"bernardina","score":15.992512586516293},{"term":"evaluatiepunt","score":15.992512586516293},{"term":"pompdrukfluctuatie","score":15.992512586516293},{"term":"fuert","score":15.992512586516293},{"term":"suppleyor","score":15.992512586516293},{"term":"fietsveerpont","score":15.992512586516293},{"term":"rockband","score":15.992512586516293},{"term":"gebodsvorder","score":15.992512586516293},{"term":"salarisgedeelt","score":15.992512586516293},{"term":"proceswaarnemer","score":15.992512586516293},{"term":"kostenkwesties","score":15.992512586516293},{"term":"greta","score":15.992512586516293},{"term":"kitchenwar","score":15.992512586516293},{"term":"vacancy","score":15.992512586516293},{"term":"encumbranc","score":15.992512586516293},{"term":"ofin","score":15.992512586516293},{"term":"zingevingssystem","score":15.992512586516293},{"term":"potharst","score":15.992512586516293},{"term":"alphonsus","score":15.992512586516293},{"term":"kzk","score":15.992512586516293},{"term":"ovrk","score":15.992512586516293},{"term":"audiovisual","score":15.992512586516293},{"term":"eeren","score":15.992512586516293},{"term":"chemmat","score":15.992512586516293},{"term":"temporair","score":15.992512586516293},{"term":"auxquel","score":15.992512586516293},{"term":"rales","score":15.992512586516293},{"term":"n_NUMs","score":15.992512586516293},{"term":"postduivenhoudersveren","score":15.992512586516293},{"term":"grauwig","score":15.992512586516293},{"term":"wegveg","score":15.992512586516293},{"term":"optiesverlies","score":15.992512586516293},{"term":"_NUMontvank","score":15.992512586516293},{"term":"motorhur","score":15.992512586516293},{"term":"onvervuild","score":15.992512586516293},{"term":"pennautier","score":15.992512586516293},{"term":"bezoekersregel","score":15.992512586516293},{"term":"keukenkran","score":15.992512586516293},{"term":"birgit","score":15.992512586516293},{"term":"firmad","score":15.992512586516293},{"term":"neurobiologisch","score":15.992512586516293},{"term":"deofficier","score":15.992512586516293},{"term":"saller","score":15.992512586516293},{"term":"jennek","score":15.992512586516293},{"term":") - (€","score":15.992512586516293},{"term":"delftplus","score":15.992512586516293},{"term":"classificatieschema","score":15.992512586516293},{"term":"exploitatieflat","score":15.992512586516293},{"term":"/]","score":15.992512586516293},{"term":"kennisbenut","score":15.992512586516293},{"term":"cecilia","score":15.992512586516293},{"term":"queenstaet","score":15.992512586516293},{"term":"syndicer","score":15.992512586516293},{"term":"obligotarief","score":15.992512586516293},{"term":"financieringsteun","score":15.992512586516293},{"term":"keyserleiconstructie","score":15.992512586516293},{"term":"mijnenveld","score":15.992512586516293},{"term":"uitzendteam","score":15.992512586516293},{"term":"galway","score":15.992512586516293},{"term":"modehandel","score":15.992512586516293},{"term":"luchtvaartdienst","score":15.992512586516293},{"term":"vervangingsonderdel","score":15.992512586516293},{"term":"gesteldevoorwaard","score":15.992512586516293},{"term":"verrekenschuld","score":15.992512586516293},{"term":"termijnloz","score":15.992512586516293},{"term":"gepleegdverklaart","score":15.992512586516293},{"term":"sensorial","score":15.992512586516293},{"term":"claxoner","score":15.992512586516293},{"term":"openlegt","score":15.992512586516293},{"term":"marjolein","score":15.992512586516293},{"term":"opiumwetgegev","score":15.992512586516293},{"term":"callfactory","score":15.992512586516293},{"term":"verdachted","score":15.992512586516293},{"term":"mobilfunk","score":15.992512586516293},{"term":"kappacher","score":15.992512586516293},{"term":"tonnageregimeverzoek","score":15.992512586516293},{"term":"lummus","score":15.992512586516293},{"term":"lutech","score":15.992512586516293},{"term":"betaalbesliss","score":15.992512586516293},{"term":"cutif","score":15.992512586516293},{"term":"joacim","score":15.992512586516293},{"term":", - )","score":15.992512586516293},{"term":"alsrechthebb","score":15.992512586516293},{"term":"verschotten","score":15.992512586516293},{"term":"_NUMeiser","score":15.992512586516293},{"term":"eendagsmeld","score":15.992512586516293},{"term":"kanword","score":15.992512586516293},{"term":"gewerkt","score":15.970735086801318},{"term":"overgedrag","score":15.95481108362529},{"term":"geweigerd","score":15.92377220929052},{"term":"ded","score":15.922688022133272},{"term":"vastgelegd","score":15.917924568544485},{"term":"getred","score":15.893207688491026},{"term":"kreg","score":15.838219235059775},{"term":"verwerpt","score":15.795454447877063},{"term":"kunststoftoepass","score":15.704830514064511},{"term":"thoracolumbal","score":15.704830514064511},{"term":"vastgoedcombinatie","score":15.704830514064511},{"term":"jubilea","score":15.704830514064511},{"term":"uzic","score":15.704830514064511},{"term":"vlakland","score":15.704830514064511},{"term":"speijer","score":15.704830514064511},{"term":"groenaannemer","score":15.704830514064511},{"term":"wytz","score":15.704830514064511},{"term":"afschriftverstrek","score":15.704830514064511},{"term":"vluchtuitgang","score":15.704830514064511},{"term":"borgmaatschappij","score":15.704830514064511},{"term":"amerstrek","score":15.704830514064511},{"term":"pomptechniek","score":15.704830514064511},{"term":"standplaatsvoorschrift","score":15.704830514064511},{"term":"zaterdagsmarkt","score":15.704830514064511},{"term":"concusie","score":15.704830514064511},{"term":"stappaert","score":15.704830514064511},{"term":"minderjarigensanctierecht","score":15.704830514064511},{"term":"aveon","score":15.704830514064511},{"term":"“[…]”,","score":15.704830514064511},{"term":"vakbondsunie","score":15.704830514064511},{"term":"hybrideleningwetgev","score":15.704830514064511},{"term":"kotteman","score":15.704830514064511},{"term":"monica","score":15.704830514064511},{"term":"damianus","score":15.704830514064511},{"term":"ruimtevaartorganisatie","score":15.704830514064511},{"term":"wernhout","score":15.704830514064511},{"term":"handelscompagnie","score":15.704830514064511},{"term":"ppmo","score":15.704830514064511},{"term":"soln","score":15.704830514064511},{"term":"diamin","score":15.704830514064511},{"term":"’ (…),","score":15.704830514064511},{"term":"sugg","score":15.704830514064511},{"term":"conseill","score":15.704830514064511},{"term":"k_NUMf","score":15.704830514064511},{"term":"lenevo","score":15.704830514064511},{"term":"criminaliteitspreventie","score":15.704830514064511},{"term":"sregel","score":15.704830514064511},{"term":"valkhoff","score":15.704830514064511},{"term":"orthobanda","score":15.704830514064511},{"term":"voorschrif","score":15.704830514064511},{"term":"transfair","score":15.704830514064511},{"term":"eug","score":15.704830514064511},{"term":"voegingsvorder","score":15.704830514064511},{"term":"ryan","score":15.704830514064511},{"term":"wahrheit","score":15.704830514064511},{"term":"bevoegdheidsgebied","score":15.704830514064511},{"term":"verkoopondersteun","score":15.704830514064511},{"term":"wooninvesteringsfond","score":15.704830514064511},{"term":"industriell","score":15.704830514064511},{"term":"metercontrol","score":15.704830514064511},{"term":"probiotisch","score":15.704830514064511},{"term":"wrakingsverbod","score":15.704830514064511},{"term":"bayraktar","score":15.704830514064511},{"term":"risicovergoed","score":15.704830514064511},{"term":"……’","score":15.704830514064511},{"term":"daalsesingel","score":15.704830514064511},{"term":"urirama","score":15.704830514064511},{"term":"computation","score":15.704830514064511},{"term":"hmt","score":15.704830514064511},{"term":"opvangtijd","score":15.704830514064511},{"term":"verblijfovereenkomst","score":15.704830514064511},{"term":"alimentatiewijz","score":15.704830514064511},{"term":"gevelconstructies","score":15.704830514064511},{"term":"directiepensioen","score":15.704830514064511},{"term":"decis","score":15.704830514064511},{"term":"tweewielercentrum","score":15.704830514064511},{"term":"rexroth","score":15.704830514064511},{"term":"directpay","score":15.704830514064511},{"term":"zeescheepvaartwinst","score":15.704830514064511},{"term":"nederand","score":15.704830514064511},{"term":"rechtvaard","score":15.643899434974653},{"term":"weinig","score":15.640696268808776},{"term":"slag","score":15.612467543887846},{"term":"opgemerkt","score":15.59877773000845},{"term":"wenst","score":15.587746262437845},{"term":"onderscheid","score":15.576775307884503},{"term":"nadel","score":15.563689110706632},{"term":"wetgever","score":15.56169734919124},{"term":"ingeschrev","score":15.552128384875632},{"term":"geschikt","score":15.54045502528715},{"term":"steun","score":15.53205653182006},{"term":"bedoel","score":15.515895186498314},{"term":"gelat","score":15.510419360600306},{"term":"tweeenveert","score":15.4816869627503},{"term":"schortenindustrie","score":15.4816869627503},{"term":"meritorious","score":15.4816869627503},{"term":"basisrioleringsplan","score":15.4816869627503},{"term":"willibrordus","score":15.4816869627503},{"term":"commissionairsdienst","score":15.4816869627503},{"term":"inkomensverlop","score":15.4816869627503},{"term":"partijwijz","score":15.4816869627503},{"term":"implementatietest","score":15.4816869627503},{"term":"nspyr","score":15.4816869627503},{"term":"volver","score":15.4816869627503},{"term":"assessabl","score":15.4816869627503},{"term":"inrichtingproblematiek","score":15.4816869627503},{"term":"hennepproductie","score":15.4816869627503},{"term":"veiligheidsadvies","score":15.4816869627503},{"term":"_NUMnovember","score":15.4816869627503},{"term":"sapouni","score":15.4816869627503},{"term":"franciscaness","score":15.4816869627503},{"term":"bouwexploitatie","score":15.4816869627503},{"term":"drukfluctuatie","score":15.4816869627503},{"term":"[ ] [","score":15.4816869627503},{"term":"resulat","score":15.4816869627503},{"term":"wf","score":15.4816869627503},{"term":"leningverzoek","score":15.4816869627503},{"term":"bentfort","score":15.4816869627503},{"term":"psct","score":15.4816869627503},{"term":"terugbestemm","score":15.4816869627503},{"term":"karakiet","score":15.4816869627503},{"term":"back_NUMbasic","score":15.4816869627503},{"term":"botletsel","score":15.4816869627503},{"term":"guldensteekproef","score":15.4816869627503},{"term":"voorbeschouw","score":15.4816869627503},{"term":"vogelspeciaalzak","score":15.4816869627503},{"term":"scheersander","score":15.4816869627503},{"term":"umfang","score":15.4816869627503},{"term":"produites","score":15.4816869627503},{"term":"contenu","score":15.4816869627503},{"term":"systeemplafondplat","score":15.4816869627503},{"term":"planbeschrijv","score":15.4816869627503},{"term":"burggraf","score":15.4816869627503},{"term":"mlu","score":15.4816869627503},{"term":"appelland","score":15.4816869627503},{"term":"bestuursniveaus","score":15.4816869627503},{"term":"tgi","score":15.4816869627503},{"term":"vtt","score":15.4816869627503},{"term":"gekwestreerd","score":15.4816869627503},{"term":"boilerplat","score":15.4816869627503},{"term":"drumhor","score":15.4816869627503},{"term":"henrica","score":15.4816869627503},{"term":"levensverzekeringmaatschappij","score":15.4816869627503},{"term":"mobi","score":15.4816869627503},{"term":"smiss","score":15.4816869627503},{"term":"infuuspomp","score":15.4816869627503},{"term":"generality","score":15.4816869627503},{"term":"bailment","score":15.4816869627503},{"term":"duinwaterbedrijf","score":15.4816869627503},{"term":"emmausbeweg","score":15.4816869627503},{"term":"karar","score":15.4816869627503},{"term":"houtberoep","score":15.4816869627503},{"term":"testterminal","score":15.4816869627503},{"term":"hypothekengroep","score":15.4816869627503},{"term":"beliss","score":15.4816869627503},{"term":"poederrest","score":15.4816869627503},{"term":"vkn","score":15.4816869627503},{"term":"fietsersbond","score":15.4816869627503},{"term":"netjurkj","score":15.4816869627503},{"term":"loonwaardespecialist","score":15.4816869627503},{"term":"alkof","score":15.4816869627503},{"term":"maasveld","score":15.4816869627503},{"term":"slotconclusies","score":15.4816869627503},{"term":"beslagbesliss","score":15.4816869627503},{"term":"opd","score":15.4816869627503},{"term":"februari_NUM","score":15.4816869627503},{"term":"rentestructur","score":15.4816869627503},{"term":"inbreuk","score":15.452182227063478},{"term":"weersprok","score":15.433423562942123},{"term":"telefoonnummer","score":15.4092974110027},{"term":"mal","score":15.35389220590403},{"term":"toekomst","score":15.32764000594145},{"term":"rocco","score":15.299365405956348},{"term":"flexbureau","score":15.299365405956348},{"term":"biog","score":15.299365405956348},{"term":"verwervingsdeelnem","score":15.299365405956348},{"term":"schermerhoek","score":15.299365405956348},{"term":"budgetcomponent","score":15.299365405956348},{"term":"wexler","score":15.299365405956348},{"term":"europaboulevard","score":15.299365405956348},{"term":"vaersenbaai","score":15.299365405956348},{"term":"rijksrecherchezak","score":15.299365405956348},{"term":"qmagic","score":15.299365405956348},{"term":"caiw","score":15.299365405956348},{"term":"margaretha","score":15.299365405956348},{"term":"peerebom","score":15.299365405956348},{"term":"lennart","score":15.299365405956348},{"term":"talp","score":15.299365405956348},{"term":"afstammingsvoorlicht","score":15.299365405956348},{"term":"serpula","score":15.299365405956348},{"term":"beleggingsveren","score":15.299365405956348},{"term":"cactuskwekerij","score":15.299365405956348},{"term":"hydrobromic","score":15.299365405956348},{"term":"delf","score":15.299365405956348},{"term":"quoi","score":15.299365405956348},{"term":"richtlijnvrijstell","score":15.299365405956348},{"term":"extrusion","score":15.299365405956348},{"term":"poept","score":15.299365405956348},{"term":"bitumensoort","score":15.299365405956348},{"term":"fransisco","score":15.299365405956348},{"term":"verpf","score":15.299365405956348},{"term":"richtlijnsystem","score":15.299365405956348},{"term":"paulina","score":15.299365405956348},{"term":"bataviastad","score":15.299365405956348},{"term":"mooring","score":15.299365405956348},{"term":"waterbehandel","score":15.299365405956348},{"term":"sandra","score":15.299365405956348},{"term":"sophie","score":15.299365405956348},{"term":"uitvoerverklar","score":15.299365405956348},{"term":"katoenfabriek","score":15.299365405956348},{"term":"tdb","score":15.299365405956348},{"term":"borgstellingverplicht","score":15.299365405956348},{"term":"zeeslag","score":15.299365405956348},{"term":"behoordel","score":15.299365405956348},{"term":"lcvr","score":15.299365405956348},{"term":"creil","score":15.299365405956348},{"term":"sigberta","score":15.299365405956348},{"term":"decisi","score":15.299365405956348},{"term":"noordmur","score":15.299365405956348},{"term":"stelleplas","score":15.299365405956348},{"term":"interieurwerk","score":15.299365405956348},{"term":"gorecht","score":15.299365405956348},{"term":"newbuild","score":15.299365405956348},{"term":"afwerklijst","score":15.299365405956348},{"term":"navir","score":15.299365405956348},{"term":"ontvankelijkheidsincident","score":15.299365405956348},{"term":"aanhoudingsincident","score":15.299365405956348},{"term":"onbevoegdheidsverklar","score":15.299365405956348},{"term":"plaatsvind","score":15.251199592698429},{"term":"gehandhaafd","score":15.219530983305955},{"term":"verworp","score":15.1952753991164},{"term":"pallottichurch","score":15.145214726129089},{"term":"renting","score":15.145214726129089},{"term":"olufs","score":15.145214726129089},{"term":"alphon","score":15.145214726129089},{"term":"terbeschikkingsgesteld","score":15.145214726129089},{"term":"vogelpark","score":15.145214726129089},{"term":"honderdzevent","score":15.145214726129089},{"term":"tweetj","score":15.145214726129089},{"term":"oudervergader","score":15.145214726129089},{"term":"diligenceonderzoek","score":15.145214726129089},{"term":"niem","score":15.145214726129089},{"term":"beamix","score":15.145214726129089},{"term":"gabriel","score":15.145214726129089},{"term":"deenik","score":15.145214726129089},{"term":"sieg","score":15.145214726129089},{"term":"butin","score":15.145214726129089},{"term":"](…),","score":15.145214726129089},{"term":"msnp","score":15.145214726129089},{"term":"signaalpatron","score":15.145214726129089},{"term":"reststraf","score":15.145214726129089},{"term":"lacryman","score":15.145214726129089},{"term":"inspir","score":15.145214726129089},{"term":"admissibilit","score":15.145214726129089},{"term":"hoofdverzoek","score":15.145214726129089},{"term":"dakteam","score":15.145214726129089},{"term":"misinformatie","score":15.145214726129089},{"term":"mestraffinag","score":15.145214726129089},{"term":"partijenhandel","score":15.145214726129089},{"term":"drukspuit","score":15.145214726129089},{"term":"kalanderjar","score":15.145214726129089},{"term":"honderdvierentacht","score":15.145214726129089},{"term":"donerix","score":15.145214726129089},{"term":"dispensatiebevoegd","score":15.145214726129089},{"term":"abonnementstarief","score":15.145214726129089},{"term":"topscor","score":15.145214726129089},{"term":"discrepantiejurisprudentie","score":15.145214726129089},{"term":"kostenplaatsenmethod","score":15.145214726129089},{"term":"vooruitstrev","score":15.145214726129089},{"term":"hindoe","score":15.145214726129089},{"term":"comedy","score":15.145214726129089},{"term":") […],","score":15.145214726129089},{"term":"_NUMdoega_NUM","score":15.145214726129089},{"term":"recreatieondernemer","score":15.145214726129089},{"term":"zwembadbranch","score":15.145214726129089},{"term":"zuydgeest","score":15.145214726129089},{"term":"shan","score":15.145214726129089},{"term":"yachtleas","score":15.145214726129089},{"term":"unseaworthines","score":15.145214726129089},{"term":"decorativ","score":15.145214726129089},{"term":"uitsluitingscriteria","score":15.145214726129089},{"term":"pensioenafstort","score":15.145214726129089},{"term":"erechtshof","score":15.145214726129089},{"term":"spol","score":15.145214726129089},{"term":"voegingsmog","score":15.145214726129089},{"term":"almacenes","score":15.145214726129089},{"term":"houtwerf","score":15.145214726129089},{"term":"inwerpmechanism","score":15.145214726129089},{"term":"plexiglasplat","score":15.145214726129089},{"term":"ē","score":15.145214726129089},{"term":"saisie","score":15.145214726129089},{"term":"voortvloei","score":15.138234771011955},{"term":"ruim","score":15.125388949170112},{"term":"bepleit","score":15.08021367220899},{"term":"geconstateerd","score":15.058284403595122},{"term":"aktivabedrijf","score":15.011683333504566},{"term":"lucien","score":15.011683333504566},{"term":"berichtencentrum","score":15.011683333504566},{"term":"immovabl","score":15.011683333504566},{"term":"winstontwikkel","score":15.011683333504566},{"term":"parkerhous","score":15.011683333504566},{"term":"geldhulp","score":15.011683333504566},{"term":"ingedikt","score":15.011683333504566},{"term":"_NUMwordt","score":15.011683333504566},{"term":"maertzdorff","score":15.011683333504566},{"term":"reggeland","score":15.011683333504566},{"term":"zevenenveertigduiz","score":15.011683333504566},{"term":"baggerbedrijf","score":15.011683333504566},{"term":"bouwmij","score":15.011683333504566},{"term":"inbraakwer","score":15.011683333504566},{"term":"cargoharc","score":15.011683333504566},{"term":"gur","score":15.011683333504566},{"term":"dawn","score":15.011683333504566},{"term":"serait","score":15.011683333504566},{"term":"emprisonnement","score":15.011683333504566},{"term":"afvalservic","score":15.011683333504566},{"term":"stallingsgarag","score":15.011683333504566},{"term":"raveleijn","score":15.011683333504566},{"term":"termijnschema","score":15.011683333504566},{"term":"swellfix","score":15.011683333504566},{"term":"uvp","score":15.011683333504566},{"term":"hefboomproduct","score":15.011683333504566},{"term":"sbu","score":15.011683333504566},{"term":"wilfred","score":15.011683333504566},{"term":"meerjarenonderhoudsbegrot","score":15.011683333504566},{"term":"portfolios","score":15.011683333504566},{"term":"zeekadetkorp","score":15.011683333504566},{"term":"premiesector","score":15.011683333504566},{"term":"drawdown","score":15.011683333504566},{"term":"howsoever","score":15.011683333504566},{"term":"dagvaart","score":15.011683333504566},{"term":"escortdienst","score":15.011683333504566},{"term":"tendant","score":15.011683333504566},{"term":"bronnenbad","score":15.011683333504566},{"term":"parketnummer_NUM","score":15.011683333504566},{"term":"jurisdictiegeschil","score":15.011683333504566},{"term":"cycles","score":15.011683333504566},{"term":"ongelijk","score":14.968585739798327},{"term":"kenn","score":14.953574276750178},{"term":"toegelicht","score":14.919679648503337},{"term":"punishability","score":14.893900297848182},{"term":"calp","score":14.893900297848182},{"term":"karsken","score":14.893900297848182},{"term":"alpenetappes","score":14.893900297848182},{"term":"millenergy","score":14.893900297848182},{"term":"randvuurpatron","score":14.893900297848182},{"term":"accijnswetgev","score":14.893900297848182},{"term":"paardensportcentrum","score":14.893900297848182},{"term":"\"ee","score":14.893900297848182},{"term":"“([","score":14.893900297848182},{"term":"toncken","score":14.893900297848182},{"term":"verbeteracties","score":14.893900297848182},{"term":"achthonderdtacht","score":14.893900297848182},{"term":"busticket","score":14.893900297848182},{"term":"autoleaseteam","score":14.893900297848182},{"term":"bewindvoeder","score":14.893900297848182},{"term":"condamn","score":14.893900297848182},{"term":"drielandenpuntarrest","score":14.893900297848182},{"term":"asv","score":14.893900297848182},{"term":"unlawfulnes","score":14.893900297848182},{"term":"jonkher","score":14.893900297848182},{"term":"nwe","score":14.893900297848182},{"term":"schee","score":14.893900297848182},{"term":"vooruitblik","score":14.893900297848182},{"term":"valdink","score":14.893900297848182},{"term":"geldbeweg","score":14.893900297848182},{"term":"vroegindeweij","score":14.893900297848182},{"term":"herik","score":14.893900297848182},{"term":"overtredingsgegeven","score":14.893900297848182},{"term":"uitgeversverbond","score":14.893900297848182},{"term":"dossieraanleg","score":14.893900297848182},{"term":"archer","score":14.893900297848182},{"term":"baba","score":14.893900297848182},{"term":"beeldherkenn","score":14.893900297848182},{"term":"homij","score":14.893900297848182},{"term":"gameworld","score":14.893900297848182},{"term":"princelandt","score":14.893900297848182},{"term":"kultuurkamer","score":14.893900297848182},{"term":"tasc","score":14.893900297848182},{"term":"sww","score":14.893900297848182},{"term":"pnl","score":14.893900297848182},{"term":"tussenbalan","score":14.893900297848182},{"term":"genotsderv","score":14.893900297848182},{"term":"testcertificat","score":14.893900297848182},{"term":"braakt","score":14.893900297848182},{"term":"zwartgeldcircuit","score":14.893900297848182},{"term":"hoorder","score":14.893900297848182},{"term":"materializ","score":14.893900297848182},{"term":"munitiedoosj","score":14.893900297848182},{"term":"snoepautomat","score":14.893900297848182},{"term":"webcasso","score":14.893900297848182},{"term":"kwijtscheldingsovereenkomst","score":14.893900297848182},{"term":"judic","score":14.893900297848182},{"term":"marktdag","score":14.893900297848182},{"term":"beginseluitsprak","score":14.893900297848182},{"term":"geleverd","score":14.818688812353043},{"term":"tussenoordel","score":14.788539782190357},{"term":"perpetration","score":14.788539782190357},{"term":"rioleringsinvester","score":14.788539782190357},{"term":"claw","score":14.788539782190357},{"term":"cvexpl","score":14.788539782190357},{"term":"boks","score":14.788539782190357},{"term":"oppervlaktestructur","score":14.788539782190357},{"term":"distres","score":14.788539782190357},{"term":"((…)","score":14.788539782190357},{"term":"waarnemingsregel","score":14.788539782190357},{"term":"wachtgeldgarantie","score":14.788539782190357},{"term":"kantonrecht","score":14.788539782190357},{"term":"sijtwend","score":14.788539782190357},{"term":"dbk","score":14.788539782190357},{"term":"stipuler","score":14.788539782190357},{"term":"claration","score":14.788539782190357},{"term":"supercenter","score":14.788539782190357},{"term":"houwert","score":14.788539782190357},{"term":"grondslaguitholl","score":14.788539782190357},{"term":"douchekran","score":14.788539782190357},{"term":"solicitation","score":14.788539782190357},{"term":"liesveld","score":14.788539782190357},{"term":"westkapell","score":14.788539782190357},{"term":"dossierverwerk","score":14.788539782190357},{"term":"lakdikt","score":14.788539782190357},{"term":"alberta","score":14.788539782190357},{"term":"bovennormatiev","score":14.788539782190357},{"term":"kroneman","score":14.788539782190357},{"term":"strafexecutie","score":14.788539782190357},{"term":"chrysant","score":14.788539782190357},{"term":"bezield","score":14.788539782190357},{"term":"neurotrauma","score":14.788539782190357},{"term":"ictu","score":14.788539782190357},{"term":"jetairfly","score":14.788539782190357},{"term":"integrat","score":14.788539782190357},{"term":"vlietstrek","score":14.788539782190357},{"term":"treindienst","score":14.788539782190357},{"term":"executeurslon","score":14.788539782190357},{"term":"aanslagbedrag","score":14.788539782190357},{"term":"neusrug","score":14.788539782190357},{"term":"zorgproces","score":14.788539782190357},{"term":"tarievenhuis","score":14.788539782190357},{"term":"ntieparameter","score":14.788539782190357},{"term":"meregaard","score":14.788539782190357},{"term":"opgesteld","score":14.77498336946907},{"term":"beoordeeld","score":14.729345750025981},{"term":"volg","score":14.70337366930956},{"term":"box_NUM","score":14.693229602386031},{"term":"specialiteitenrestaurant","score":14.693229602386031},{"term":"heidema","score":14.693229602386031},{"term":"frouw","score":14.693229602386031},{"term":"korsou","score":14.693229602386031},{"term":"zwammerdam","score":14.693229602386031},{"term":"luchtvaartschol","score":14.693229602386031},{"term":"kappersbedrijf","score":14.693229602386031},{"term":"meerverbruik","score":14.693229602386031},{"term":"henricus","score":14.693229602386031},{"term":"huiszwam","score":14.693229602386031},{"term":"inkomstenverhoud","score":14.693229602386031},{"term":"lijkdel","score":14.693229602386031},{"term":"dav","score":14.693229602386031},{"term":"majesteit","score":14.693229602386031},{"term":"meldcod","score":14.693229602386031},{"term":"scheidingsverzoek","score":14.693229602386031},{"term":"sulphur","score":14.693229602386031},{"term":"‟","score":14.693229602386031},{"term":"belgrad","score":14.693229602386031},{"term":"meederjar","score":14.693229602386031},{"term":"vijg","score":14.693229602386031},{"term":"dmj","score":14.693229602386031},{"term":"bernardus","score":14.693229602386031},{"term":"vernooij","score":14.693229602386031},{"term":"vestigingsplaats_NUM","score":14.693229602386031},{"term":"pinas","score":14.693229602386031},{"term":"gypsy","score":14.693229602386031},{"term":"boomar","score":14.693229602386031},{"term":"rechtmatigheidsverwer","score":14.693229602386031},{"term":"wilderink","score":14.693229602386031},{"term":"woonkracht","score":14.693229602386031},{"term":"micronit","score":14.693229602386031},{"term":"microfluidic","score":14.693229602386031},{"term":"toepassingsvo","score":14.693229602386031},{"term":"→ → → [","score":14.693229602386031},{"term":"kostentekort","score":14.693229602386031},{"term":"haarmod","score":14.693229602386031},{"term":"counting","score":14.693229602386031},{"term":"badcuyp","score":14.693229602386031},{"term":"bovenhoek","score":14.693229602386031},{"term":"dommages","score":14.693229602386031},{"term":"fondement","score":14.693229602386031},{"term":"rouen","score":14.693229602386031},{"term":"salarisontwikkel","score":14.693229602386031},{"term":"lastegelegd","score":14.693229602386031},{"term":"mailincident","score":14.693229602386031},{"term":"massacr","score":14.606218225396402},{"term":"kincaidfisher","score":14.606218225396402},{"term":"beekenkamp","score":14.606218225396402},{"term":"ouderenhuisvest","score":14.606218225396402},{"term":"uitzendreglement","score":14.606218225396402},{"term":"caecilia","score":14.606218225396402},{"term":"alimentatiebehoeft","score":14.606218225396402},{"term":"automatiek","score":14.606218225396402},{"term":"schamel","score":14.606218225396402},{"term":"pursuanc","score":14.606218225396402},{"term":"toerust","score":14.606218225396402},{"term":"spelersrad","score":14.606218225396402},{"term":"franj","score":14.606218225396402},{"term":"arbeidsbehoeft","score":14.606218225396402},{"term":"̊)","score":14.606218225396402},{"term":"workorder","score":14.606218225396402},{"term":"luchtkwaliteitseis","score":14.606218225396402},{"term":"antonie","score":14.606218225396402},{"term":"aanvullingsfond","score":14.606218225396402},{"term":"rackspac","score":14.606218225396402},{"term":"soerendonk","score":14.606218225396402},{"term":"afhandelingskost","score":14.606218225396402},{"term":"koningsket","score":14.606218225396402},{"term":"canister","score":14.606218225396402},{"term":"driftarm","score":14.606218225396402},{"term":"constantijn","score":14.606218225396402},{"term":"oudedagvoorzien","score":14.606218225396402},{"term":"proposities","score":14.606218225396402},{"term":"bowmer","score":14.606218225396402},{"term":"lvdz","score":14.606218225396402},{"term":"dierenpolitie","score":14.606218225396402},{"term":"modeltekst","score":14.606218225396402},{"term":"ambilight","score":14.606218225396402},{"term":"klembord","score":14.606218225396402},{"term":"schaderegelingsproces","score":14.606218225396402},{"term":"derdenbelang","score":14.526175517722866},{"term":"koepelvrijstell","score":14.526175517722866},{"term":"sportland","score":14.526175517722866},{"term":"subsidiefraud","score":14.526175517722866},{"term":"guardianship","score":14.526175517722866},{"term":"mayhill","score":14.526175517722866},{"term":"beroepspensioenfond","score":14.526175517722866},{"term":"zijlman","score":14.526175517722866},{"term":"interpretatieregel","score":14.526175517722866},{"term":"gadjo","score":14.526175517722866},{"term":"strawberryfrog","score":14.526175517722866},{"term":"eijkelestam","score":14.526175517722866},{"term":"verlofstatus","score":14.526175517722866},{"term":"boterman","score":14.526175517722866},{"term":"kwalificatieverwer","score":14.526175517722866},{"term":"cocon","score":14.526175517722866},{"term":"demarcatie","score":14.526175517722866},{"term":"valkenier","score":14.526175517722866},{"term":"markar","score":14.526175517722866},{"term":"lambertus","score":14.526175517722866},{"term":"sgeschiedenis","score":14.526175517722866},{"term":"ondernemerscapaciteit","score":14.526175517722866},{"term":"foralway","score":14.526175517722866},{"term":"leerwerktraject","score":14.526175517722866},{"term":"participatietraject","score":14.526175517722866},{"term":"taalverded","score":14.526175517722866},{"term":"rechercherapport","score":14.526175517722866},{"term":"saneringsperiod","score":14.526175517722866},{"term":"hendrika","score":14.526175517722866},{"term":"monumentenbezitter","score":14.526175517722866},{"term":"erbestaet","score":14.526175517722866},{"term":"voorvoegingswinst","score":14.526175517722866},{"term":"defences","score":14.526175517722866},{"term":"momentan","score":14.526175517722866},{"term":"nachtzorg","score":14.526175517722866},{"term":"bewijsverdrag","score":14.526175517722866},{"term":"asielberoep","score":14.526175517722866},{"term":"ernst","score":14.491380508193782},{"term":"tweehonderdvijfentacht","score":14.452067545569143},{"term":"houttil","score":14.452067545569143},{"term":"avifauna","score":14.452067545569143},{"term":"levied","score":14.452067545569143},{"term":"licences","score":14.452067545569143},{"term":"chatenier","score":14.452067545569143},{"term":"hobbemakad","score":14.452067545569143},{"term":"champino","score":14.452067545569143},{"term":"personeelsbeher","score":14.452067545569143},{"term":"kantoormeubel","score":14.452067545569143},{"term":"ntenbelang","score":14.452067545569143},{"term":"lmi_NUM","score":14.452067545569143},{"term":"fixet","score":14.452067545569143},{"term":"peperstoep","score":14.452067545569143},{"term":"yami","score":14.452067545569143},{"term":"uitwerkingsplann","score":14.452067545569143},{"term":"bouwdienst","score":14.452067545569143},{"term":"snas","score":14.452067545569143},{"term":"ludiek","score":14.452067545569143},{"term":"nrg","score":14.452067545569143},{"term":") à","score":14.452067545569143},{"term":"faebb","score":14.452067545569143},{"term":"xxxvi","score":14.452067545569143},{"term":"carex","score":14.452067545569143},{"term":"salarisverzoek","score":14.452067545569143},{"term":"wondend","score":14.383074674082192},{"term":"laesies","score":14.383074674082192},{"term":"riad","score":14.383074674082192},{"term":"ordogh","score":14.383074674082192},{"term":"centraalvuurpatron","score":14.383074674082192},{"term":"fotoapparatur","score":14.383074674082192},{"term":"conservation","score":14.383074674082192},{"term":"outward","score":14.383074674082192},{"term":"sectorbred","score":14.383074674082192},{"term":"galoupp","score":14.383074674082192},{"term":"instandhoudingsovereenkomst","score":14.383074674082192},{"term":"sanayi","score":14.383074674082192},{"term":"tegenwind","score":14.383074674082192},{"term":"parij","score":14.383074674082192},{"term":"plusieur","score":14.383074674082192},{"term":"inbart","score":14.383074674082192},{"term":"comfortparc","score":14.383074674082192},{"term":"dwangsomvorder","score":14.383074674082192},{"term":"hendrikus","score":14.383074674082192},{"term":"jrp","score":14.383074674082192},{"term":"outheusd","score":14.383074674082192},{"term":"stables","score":14.383074674082192},{"term":"arkoel","score":14.383074674082192},{"term":"brockstar","score":14.383074674082192},{"term":"bangert","score":14.383074674082192},{"term":"pacogi","score":14.383074674082192},{"term":"rcr","score":14.383074674082192},{"term":"afin","score":14.383074674082192},{"term":"_NUMwc","score":14.383074674082192},{"term":"nesia","score":14.383074674082192},{"term":"looncorrecties","score":14.31853615294462},{"term":"wooninvest","score":14.31853615294462},{"term":"ahmtec","score":14.31853615294462},{"term":"mixing","score":14.31853615294462},{"term":"turien","score":14.31853615294462},{"term":"lesmeister","score":14.31853615294462},{"term":"geintmeerd","score":14.31853615294462},{"term":"nihilbed","score":14.31853615294462},{"term":"maagdenvlies","score":14.31853615294462},{"term":"harechair","score":14.31853615294462},{"term":"verwijsbord","score":14.31853615294462},{"term":"esconado","score":14.31853615294462},{"term":"resonantie","score":14.31853615294462},{"term":"verzetsdagvaard","score":14.31853615294462},{"term":"filling","score":14.31853615294462},{"term":"koppenol","score":14.31853615294462},{"term":"interieurarchitect","score":14.31853615294462},{"term":"arie","score":14.31853615294462},{"term":"nhv","score":14.31853615294462},{"term":"kgza","score":14.31853615294462},{"term":"huwelijksvermogensgemeenschap","score":14.31853615294462},{"term":"nstigt","score":14.31853615294462},{"term":"boetebewijs","score":14.31853615294462},{"term":"luchtled","score":14.31853615294462},{"term":"%\"","score":14.31853615294462},{"term":"hoogkarspel","score":14.31853615294462},{"term":"provisievergoed","score":14.31853615294462},{"term":"vormverzuimverwer","score":14.31853615294462},{"term":"lieven","score":14.31853615294462},{"term":"buyout","score":14.31853615294462},{"term":"montessori","score":14.31853615294462},{"term":"ebel","score":14.31853615294462},{"term":"rijnstrek","score":14.31853615294462},{"term":"_NUMdag","score":14.31853615294462},{"term":"slimmer","score":14.31853615294462},{"term":"erdbrinkplein","score":14.31853615294462},{"term":"twaalfhonderdvijft","score":14.31853615294462},{"term":"holgro","score":14.31853615294462},{"term":"cna","score":14.31853615294462},{"term":"houtgestookt","score":14.31853615294462},{"term":"arbeidsduurbeperk","score":14.31853615294462},{"term":"deelgeschilrechter","score":14.31853615294462},{"term":"kempenban","score":14.31853615294462},{"term":"gva","score":14.31853615294462},{"term":"personeelsfeest","score":14.257911531128185},{"term":"b_NUMkid","score":14.257911531128185},{"term":"lauwer","score":14.257911531128185},{"term":"dekkingsvrag","score":14.257911531128185},{"term":"brandcompartiment","score":14.257911531128185},{"term":"haghested","score":14.257911531128185},{"term":"preparatory","score":14.257911531128185},{"term":"detournement","score":14.257911531128185},{"term":"fenna","score":14.257911531128185},{"term":"rava","score":14.257911531128185},{"term":"goldcard","score":14.257911531128185},{"term":"innoweb","score":14.257911531128185},{"term":"hrsolar","score":14.257911531128185},{"term":"culion","score":14.257911531128185},{"term":"sujet","score":14.257911531128185},{"term":"dakbeher","score":14.257911531128185},{"term":"ijzel","score":14.257911531128185},{"term":"nattig","score":14.257911531128185},{"term":"teeltsystem","score":14.257911531128185},{"term":"ndung","score":14.257911531128185},{"term":"katlijk","score":14.257911531128185},{"term":"breesap","score":14.257911531128185},{"term":"woningbouwsticht","score":14.257911531128185},{"term":"sael","score":14.257911531128185},{"term":"wizz","score":14.257911531128185},{"term":"drecht","score":14.257911531128185},{"term":"faciliteringsvolum","score":14.257911531128185},{"term":"schuldplicht","score":14.257911531128185},{"term":"werkschur","score":14.257911531128185},{"term":"kalverstrat","score":14.257911531128185},{"term":"capsa","score":14.257911531128185},{"term":"tooling","score":14.257911531128185},{"term":"verkeersschol","score":14.257911531128185},{"term":"drinkwaterinstallatie","score":14.257911531128185},{"term":"crisistijd","score":14.257911531128185},{"term":"fido","score":14.257911531128185},{"term":"cll","score":14.257911531128185},{"term":"palth","score":14.257911531128185},{"term":"meemoeder","score":14.257911531128185},{"term":"powercem","score":14.257911531128185},{"term":"], […],","score":14.257911531128185},{"term":"lichaamsfuncties","score":14.257911531128185},{"term":"kaaimaneiland","score":14.200753117288237},{"term":"wallast","score":14.200753117288237},{"term":"kwaliteitszorg","score":14.200753117288237},{"term":"kza","score":14.200753117288237},{"term":"intertrust","score":14.200753117288237},{"term":"kladovo","score":14.200753117288237},{"term":"azijn","score":14.200753117288237},{"term":"vluchtroutes","score":14.200753117288237},{"term":"duingebied","score":14.200753117288237},{"term":"opbar","score":14.200753117288237},{"term":"verpachtster","score":14.200753117288237},{"term":"edgar","score":14.200753117288237},{"term":"aviationvalley","score":14.200753117288237},{"term":"compascum","score":14.200753117288237},{"term":"roey","score":14.200753117288237},{"term":"spliet","score":14.200753117288237},{"term":"awac","score":14.200753117288237},{"term":"ondernemingswaard","score":14.200753117288237},{"term":"moneyhous","score":14.200753117288237},{"term":"ember","score":14.200753117288237},{"term":"lyneth","score":14.200753117288237},{"term":"groenhuijz","score":14.200753117288237},{"term":"reclasseringsmedewerkster","score":14.200753117288237},{"term":"dominicus","score":14.200753117288237},{"term":"xpressfish","score":14.200753117288237},{"term":"borgstellingsvergoed","score":14.200753117288237},{"term":"schaamben","score":14.200753117288237},{"term":"depositary","score":14.200753117288237},{"term":"overlon","score":14.200753117288237},{"term":"oddo","score":14.200753117288237},{"term":"verweerder_NUM","score":14.200753117288237},{"term":"harpsluit","score":14.200753117288237},{"term":"grondstrom","score":14.200753117288237},{"term":"mercer","score":14.200753117288237},{"term":"saldal","score":14.146685896017962},{"term":"uitzendburo","score":14.146685896017962},{"term":"keukenmanager","score":14.146685896017962},{"term":"binckhorst","score":14.146685896017962},{"term":"cassatietechnisch","score":14.146685896017962},{"term":"spaarregel","score":14.146685896017962},{"term":"metastyl","score":14.146685896017962},{"term":"dupliceert","score":14.146685896017962},{"term":"kerngegeven","score":14.146685896017962},{"term":"+/+","score":14.146685896017962},{"term":"zorgpartner","score":14.146685896017962},{"term":"hoornvlies","score":14.146685896017962},{"term":"assicurazioni","score":14.146685896017962},{"term":"remmerswal","score":14.146685896017962},{"term":"mattner","score":14.146685896017962},{"term":"ziektegeval","score":14.146685896017962},{"term":"toegesneld","score":14.146685896017962},{"term":"bouwprogramma","score":14.146685896017962},{"term":"scheidegger","score":14.146685896017962},{"term":"vollenbroek","score":14.146685896017962},{"term":"bovanas","score":14.146685896017962},{"term":"voordeelbereken","score":14.146685896017962},{"term":"comput","score":14.146685896017962},{"term":"stootvoeg","score":14.146685896017962},{"term":"windnet","score":14.146685896017962},{"term":"managementdienst","score":14.146685896017962},{"term":"testmethod","score":14.146685896017962},{"term":"seizur","score":14.146685896017962},{"term":"seized","score":14.146685896017962},{"term":"nasrullah","score":14.146685896017962},{"term":"rue","score":14.146685896017962},{"term":"lyca","score":14.146685896017962},{"term":"beverag","score":14.146685896017962},{"term":"beconet","score":14.146685896017962},{"term":"swienink","score":14.146685896017962},{"term":"indicatiebeschik","score":14.146685896017962},{"term":"debiteurenstand","score":14.146685896017962},{"term":"asbestsoort","score":14.095392601630412},{"term":"vermogensobject","score":14.095392601630412},{"term":"petrol","score":14.095392601630412},{"term":"crediteringsmaximum","score":14.095392601630412},{"term":"verrekenprijz","score":14.095392601630412},{"term":"identificatiefas","score":14.095392601630412},{"term":"exiton","score":14.095392601630412},{"term":"oploo","score":14.095392601630412},{"term":"staminek","score":14.095392601630412},{"term":"rambo","score":14.095392601630412},{"term":"rentewijz","score":14.095392601630412},{"term":"polocosmetic","score":14.095392601630412},{"term":"leverdata","score":14.095392601630412},{"term":"ruma","score":14.095392601630412},{"term":"overlegstructur","score":14.095392601630412},{"term":"hannek","score":14.095392601630412},{"term":"marius","score":14.095392601630412},{"term":"limbra","score":14.095392601630412},{"term":"ovk","score":14.095392601630412},{"term":"aurea","score":14.095392601630412},{"term":"chaletpark","score":14.095392601630412},{"term":"rmo","score":14.095392601630412},{"term":"scarabee","score":14.095392601630412},{"term":"geboortemoeder","score":14.095392601630412},{"term":"heibedrijf","score":14.095392601630412},{"term":"entzinger","score":14.095392601630412},{"term":"alres","score":14.04660243746098},{"term":"mandema","score":14.04660243746098},{"term":"goeseij","score":14.04660243746098},{"term":"meulenberg","score":14.04660243746098},{"term":"miljeu","score":14.04660243746098},{"term":"furnitur","score":14.04660243746098},{"term":"ongevalsbeschrijv","score":14.04660243746098},{"term":"nedglas","score":14.04660243746098},{"term":"reylof","score":14.04660243746098},{"term":"aanbodscop","score":14.04660243746098},{"term":"pompekliniek","score":14.04660243746098},{"term":"kurt","score":14.04660243746098},{"term":"looijestijn","score":14.04660243746098},{"term":"lekkerkerker","score":14.04660243746098},{"term":"nderung","score":14.04660243746098},{"term":"thinkpad","score":14.04660243746098},{"term":"xsundo","score":14.04660243746098},{"term":"artventur","score":14.04660243746098},{"term":"laklag","score":14.04660243746098},{"term":"minded","score":14.04660243746098},{"term":"interconfessionel","score":14.04660243746098},{"term":"huisvestingslast","score":14.04660243746098},{"term":"vermogensniveau","score":14.04660243746098},{"term":"wetsovertred","score":14.04660243746098},{"term":"raadkamerdossier","score":14.04660243746098},{"term":"erlater","score":14.04660243746098},{"term":"sportfederatie","score":14.04660243746098},{"term":"helmig","score":14.04660243746098},{"term":"trambestuurder","score":14.04660243746098},{"term":"weerdt","score":14.04660243746098},{"term":"markthuurwaard","score":14.04660243746098},{"term":"gerechthof","score":14.04660243746098},{"term":"_NUMis","score":14.04660243746098},{"term":"personeelsmiddel","score":14.04660243746098},{"term":"premievaststell","score":14.04660243746098},{"term":"angelo","score":14.000082421826086},{"term":"multiwal","score":14.000082421826086},{"term":"bastiaan","score":14.000082421826086},{"term":"swu","score":14.000082421826086},{"term":"orapi","score":14.000082421826086},{"term":"touringcarbedrijf","score":14.000082421826086},{"term":"rondvrag","score":14.000082421826086},{"term":"steketee","score":14.000082421826086},{"term":"kraamvogel","score":14.000082421826086},{"term":"gezeteld","score":14.000082421826086},{"term":"zaaigoed","score":14.000082421826086},{"term":"optieplan","score":14.000082421826086},{"term":"kaandorp","score":14.000082421826086},{"term":"aupr","score":14.000082421826086},{"term":"slotpleidooi","score":14.000082421826086},{"term":"waaykamp","score":14.000082421826086},{"term":"gc_NUM","score":14.000082421826086},{"term":"kien","score":14.000082421826086},{"term":"kopenhag","score":14.000082421826086},{"term":"bitraco","score":14.000082421826086},{"term":"testgroep","score":14.000082421826086},{"term":"seeger","score":14.000082421826086},{"term":"eric","score":14.000082421826086},{"term":"cooperatiev","score":14.000082421826086},{"term":"maatschapsarrest","score":14.000082421826086},{"term":"vand","score":14.000082421826086},{"term":"adsr","score":14.000082421826086},{"term":"stuijt","score":14.000082421826086},{"term":"identificatiebewijs","score":14.000082421826086},{"term":"continuvaart","score":14.000082421826086},{"term":"dakterrass","score":14.000082421826086},{"term":"za_NUM","score":14.000082421826086},{"term":"geldmarkt","score":14.000082421826086},{"term":"rogier","score":13.955630659255252},{"term":"malus","score":13.955630659255252},{"term":"indraai","score":13.955630659255252},{"term":"opleidingsfond","score":13.955630659255252},{"term":"goltstein","score":13.955630659255252},{"term":"activit","score":13.955630659255252},{"term":"ererad","score":13.955630659255252},{"term":"verzuimrent","score":13.955630659255252},{"term":"haagwon","score":13.955630659255252},{"term":"sportlandgoed","score":13.955630659255252},{"term":"trait","score":13.955630659255252},{"term":"alternatives","score":13.955630659255252},{"term":"patisserie","score":13.955630659255252},{"term":"lingewal","score":13.955630659255252},{"term":"adfis","score":13.955630659255252},{"term":"crop","score":13.955630659255252},{"term":"nilwik","score":13.955630659255252},{"term":"stuurop","score":13.955630659255252},{"term":"barron","score":13.955630659255252},{"term":"geervliet","score":13.955630659255252},{"term":"attijariwafa","score":13.955630659255252},{"term":"levensloopreken","score":13.955630659255252},{"term":"nieuwdorp","score":13.955630659255252},{"term":"ã","score":13.955630659255252},{"term":"spaarnevest","score":13.955630659255252},{"term":"rotown","score":13.955630659255252},{"term":"euclaim","score":13.955630659255252},{"term":"getek","score":13.953294655013268},{"term":"beslis","score":13.913071044836457},{"term":"hiding","score":13.913071044836457},{"term":"pkf","score":13.913071044836457},{"term":"geologistiek","score":13.913071044836457},{"term":"dissolution","score":13.913071044836457},{"term":"skat","score":13.913071044836457},{"term":"entscheidung","score":13.913071044836457},{"term":"salet","score":13.913071044836457},{"term":"herwaarderingswinst","score":13.913071044836457},{"term":"vakantiexpert","score":13.913071044836457},{"term":"agiris","score":13.913071044836457},{"term":"nebem","score":13.913071044836457},{"term":"apco","score":13.913071044836457},{"term":"nederwet","score":13.913071044836457},{"term":"onuitwis","score":13.913071044836457},{"term":"saladini","score":13.913071044836457},{"term":"paratoires","score":13.913071044836457},{"term":"nieuwsmedia","score":13.913071044836457},{"term":"breeveld","score":13.913071044836457},{"term":"huisvestingsbijdrag","score":13.913071044836457},{"term":"substitueer","score":13.913071044836457},{"term":"ndea","score":13.913071044836457},{"term":"soil","score":13.913071044836457},{"term":"vervoersverklar","score":13.913071044836457},{"term":"pearl","score":13.913071044836457},{"term":"verwijzingsplicht","score":13.913071044836457},{"term":"hoogmoed","score":13.913071044836457},{"term":"refinery","score":13.913071044836457},{"term":"exploitatiewijz","score":13.913071044836457},{"term":"mistig","score":13.913071044836457},{"term":"leendert","score":13.913071044836457},{"term":"oo","score":13.90837413669085},{"term":"zorgaccent","score":13.872249050316201},{"term":"onderbedelingsvorder","score":13.872249050316201},{"term":"echoput","score":13.872249050316201},{"term":"ubvr","score":13.872249050316201},{"term":"tendercoach","score":13.872249050316201},{"term":"vmw","score":13.872249050316201},{"term":"etam","score":13.872249050316201},{"term":"refinanc","score":13.872249050316201},{"term":"vem","score":13.872249050316201},{"term":"douverhav","score":13.872249050316201},{"term":"aebi","score":13.872249050316201},{"term":"travaux","score":13.872249050316201},{"term":"esmilo","score":13.872249050316201},{"term":"gedingfas","score":13.872249050316201},{"term":"adrianus","score":13.872249050316201},{"term":"viecuri","score":13.872249050316201},{"term":"reuser","score":13.872249050316201},{"term":"allco","score":13.872249050316201},{"term":"schouderband","score":13.872249050316201},{"term":"projectplann","score":13.872249050316201},{"term":"climate_NUMu","score":13.872249050316201},{"term":"netwerkkost","score":13.872249050316201},{"term":"tolkdienst","score":13.872249050316201},{"term":"erecod","score":13.872249050316201},{"term":"lijfrenteclausul","score":13.872249050316201},{"term":"geerath","score":13.872249050316201},{"term":"marked","score":13.872249050316201},{"term":"promotieactiviteit","score":13.872249050316201},{"term":"bewijsverkrijg","score":13.872249050316201},{"term":"occurr","score":13.872249050316201},{"term":"kalsbeeknorm","score":13.872249050316201},{"term":"eligibility","score":13.872249050316201},{"term":"efecto","score":13.872249050316201},{"term":"bamboesauna","score":13.872249050316201},{"term":"bokhov","score":13.872249050316201},{"term":"tourism","score":13.83302833716292},{"term":"intermediary","score":13.83302833716292},{"term":"dfa","score":13.83302833716292},{"term":"leuver","score":13.83302833716292},{"term":"abcvo","score":13.83302833716292},{"term":"ruger","score":13.83302833716292},{"term":"toestemt","score":13.83302833716292},{"term":"werkingsgebied","score":13.83302833716292},{"term":"indemnification","score":13.83302833716292},{"term":"societ","score":13.83302833716292},{"term":"hoofdbedrijfschap","score":13.83302833716292},{"term":"bewaringstell","score":13.83302833716292},{"term":"bruidsgav","score":13.83302833716292},{"term":"trends_NUMcom","score":13.83302833716292},{"term":"opeisingsgrond","score":13.83302833716292},{"term":"nstigung","score":13.83302833716292},{"term":"sama","score":13.83302833716292},{"term":"rgschaft","score":13.83302833716292},{"term":"zeijlemaker","score":13.83302833716292},{"term":"giorno","score":13.83302833716292},{"term":"acceptatiecriteria","score":13.83302833716292},{"term":"keukenconcurrent","score":13.83302833716292},{"term":"_NUMhet","score":13.83302833716292},{"term":"meru","score":13.83302833716292},{"term":"eimskip","score":13.83302833716292},{"term":"verifier","score":13.83302833716292},{"term":"kwaliteitsborg","score":13.83302833716292},{"term":"avd","score":13.83302833716292},{"term":"scheldepoort","score":13.83302833716292},{"term":"rdinatiewet","score":13.83302833716292},{"term":"annexis","score":13.795288009180073},{"term":"projectvennootschap","score":13.795288009180073},{"term":"bodemgoed","score":13.795288009180073},{"term":"wiek","score":13.795288009180073},{"term":"atuserv","score":13.795288009180073},{"term":"aster","score":13.795288009180073},{"term":"samenstellingsopdracht","score":13.795288009180073},{"term":"growepa","score":13.795288009180073},{"term":"bamboeplat","score":13.795288009180073},{"term":"spuitbusj","score":13.795288009180073},{"term":"wallaard","score":13.795288009180073},{"term":"attractiviteit","score":13.795288009180073},{"term":"disck","score":13.795288009180073},{"term":"fides","score":13.795288009180073},{"term":"buon","score":13.795288009180073},{"term":"damicom","score":13.795288009180073},{"term":"scott","score":13.795288009180073},{"term":"kabalt","score":13.795288009180073},{"term":"in_NUMbi","score":13.795288009180073},{"term":"edelsmed","score":13.795288009180073},{"term":"biggym","score":13.795288009180073},{"term":"transcom","score":13.795288009180073},{"term":",(…)","score":13.795288009180073},{"term":"\"\"","score":13.795288009180073},{"term":"declin","score":13.795288009180073},{"term":"enhanc","score":13.795288009180073},{"term":"osdorpplein","score":13.795288009180073},{"term":"akteverzoek","score":13.795288009180073},{"term":"financieringswijz","score":13.795288009180073},{"term":"centres","score":13.795288009180073},{"term":"lamas","score":13.758920365009198},{"term":"vennix","score":13.758920365009198},{"term":"complicity","score":13.758920365009198},{"term":"rioleringsplan","score":13.758920365009198},{"term":"casemanagement","score":13.758920365009198},{"term":"lenter","score":13.758920365009198},{"term":"movabl","score":13.758920365009198},{"term":"luwt","score":13.758920365009198},{"term":"distribrand","score":13.758920365009198},{"term":"verzoekprocedur","score":13.758920365009198},{"term":"december_NUM","score":13.758920365009198},{"term":"terugblik","score":13.758920365009198},{"term":"aftrekregel","score":13.758920365009198},{"term":"beoordelingsresultat","score":13.758920365009198},{"term":"projectregisseur","score":13.758920365009198},{"term":"verhulst","score":13.758920365009198},{"term":"arthur","score":13.758920365009198},{"term":"cassatieverzoek","score":13.758920365009198},{"term":"eef","score":13.758920365009198},{"term":",--(","score":13.758920365009198},{"term":"waardevast","score":13.758920365009198},{"term":"forever","score":13.758920365009198},{"term":"waarschuwingssignal","score":13.758920365009198},{"term":"pluimer","score":13.758920365009198},{"term":"(à €","score":13.758920365009198},{"term":"globalisatieregel","score":13.723829045197927},{"term":"plaquenil","score":13.723829045197927},{"term":"alifa","score":13.723829045197927},{"term":"eduard","score":13.723829045197927},{"term":"huidekoper","score":13.723829045197927},{"term":"havenschap","score":13.723829045197927},{"term":"terugverhuiz","score":13.723829045197927},{"term":"prae","score":13.723829045197927},{"term":"eisers_NUM","score":13.723829045197927},{"term":"strafprocedures","score":13.723829045197927},{"term":"doeloverschrijd","score":13.723829045197927},{"term":"deskundigenprocedur","score":13.723829045197927},{"term":"ingroeiregel","score":13.723829045197927},{"term":"vrijwaringsvorder","score":13.723829045197927},{"term":"bemiddelingsdienst","score":13.723829045197927},{"term":"bovenkarspel","score":13.723829045197927},{"term":"markus","score":13.723829045197927},{"term":"kogra","score":13.723829045197927},{"term":"precario","score":13.723829045197927},{"term":"teikyo","score":13.723829045197927},{"term":"intralot","score":13.723829045197927},{"term":"tjongerschan","score":13.723829045197927},{"term":"reverlop","score":13.723829045197927},{"term":"injured","score":13.689927493522246},{"term":"zwartepoort","score":13.689927493522246},{"term":"diefhoek","score":13.689927493522246},{"term":"occurrenc","score":13.689927493522246},{"term":"fryslan","score":13.689927493522246},{"term":"veob","score":13.689927493522246},{"term":"oldenkot","score":13.689927493522246},{"term":"shapshap","score":13.689927493522246},{"term":"saldomethod","score":13.689927493522246},{"term":"bereikbaarheidsdienst","score":13.689927493522246},{"term":"tweehonderdtien","score":13.689927493522246},{"term":"strijthag","score":13.689927493522246},{"term":"capabilities","score":13.689927493522246},{"term":"machinefabriek","score":13.689927493522246},{"term":"oudijk","score":13.689927493522246},{"term":"pdx","score":13.689927493522246},{"term":"tempus","score":13.689927493522246},{"term":"zekerheidsuitker","score":13.689927493522246},{"term":"coureur","score":13.689927493522246},{"term":"devil","score":13.689927493522246},{"term":"melkweg","score":13.689927493522246},{"term":"vhb","score":13.689927493522246},{"term":"genezingsproces","score":13.657137670699255},{"term":"muziekveren","score":13.657137670699255},{"term":"integrator","score":13.657137670699255},{"term":"woondienst","score":13.657137670699255},{"term":"schedules","score":13.657137670699255},{"term":"heijkop","score":13.657137670699255},{"term":"vit","score":13.657137670699255},{"term":"assurances","score":13.657137670699255},{"term":"nvos","score":13.657137670699255},{"term":"rodersana","score":13.657137670699255},{"term":"andr","score":13.657137670699255},{"term":"graphic","score":13.657137670699255},{"term":"kernelement","score":13.657137670699255},{"term":"grondbedrijf","score":13.657137670699255},{"term":"fut","score":13.657137670699255},{"term":"o_NUMb","score":13.657137670699255},{"term":"vrebamelkvee","score":13.657137670699255},{"term":"discharged","score":13.657137670699255},{"term":"spendlov","score":13.657137670699255},{"term":"nenefa","score":13.657137670699255},{"term":"pmp","score":13.657137670699255},{"term":"korvemaker","score":13.657137670699255},{"term":"rechtercommissaris","score":13.657137670699255},{"term":"loonregel","score":13.625388972384675},{"term":"reisburo","score":13.625388972384675},{"term":"ruinerwold","score":13.625388972384675},{"term":"job_NUMlif","score":13.625388972384675},{"term":"cruquius","score":13.625388972384675},{"term":"wederprestatie","score":13.625388972384675},{"term":"derlon","score":13.625388972384675},{"term":"gebruiksregel","score":13.625388972384675},{"term":"redwod","score":13.625388972384675},{"term":"natuurgrond","score":13.625388972384675},{"term":"straatkant","score":13.625388972384675},{"term":"dombo","score":13.625388972384675},{"term":"verhuuractiviteit","score":13.625388972384675},{"term":"acqua","score":13.625388972384675},{"term":"thiss","score":13.625388972384675},{"term":"keyser","score":13.625388972384675},{"term":"wander","score":13.625388972384675},{"term":"eurowon","score":13.625388972384675},{"term":"fun_NUMbfit","score":13.625388972384675},{"term":"ardea","score":13.625388972384675},{"term":"vrijspraakverwer","score":13.625388972384675},{"term":"tort","score":13.625388972384675},{"term":"dienvolgen","score":13.625388972384675},{"term":"veelgesteld","score":13.625388972384675},{"term":"filing","score":13.625388972384675},{"term":"ilitran","score":13.625388972384675},{"term":"splet","score":13.625388972384675},{"term":"strafrechtadvocat","score":13.625388972384675},{"term":"recreatievilla","score":13.625388972384675},{"term":"factorbesluit","score":13.625388972384675},{"term":"evenen","score":13.620731897821782},{"term":"correctievoorstel","score":13.594617313717922},{"term":"pensioenverwer","score":13.594617313717922},{"term":"bedrijvenpark","score":13.594617313717922},{"term":"(?)","score":13.594617313717922},{"term":"eindconclusies","score":13.594617313717922},{"term":"tijss","score":13.594617313717922},{"term":"waarderingsmethodiek","score":13.594617313717922},{"term":"bestekswijz","score":13.594617313717922},{"term":"vervolgingsrichtlijn","score":13.594617313717922},{"term":"synex","score":13.594617313717922},{"term":"sportservic","score":13.594617313717922},{"term":"winstrealisatie","score":13.594617313717922},{"term":"katoomba","score":13.594617313717922},{"term":"bakkersbedrijf","score":13.594617313717922},{"term":"politica","score":13.594617313717922},{"term":"terugreken","score":13.594617313717922},{"term":"twenc","score":13.594617313717922},{"term":"vastgoedfond","score":13.594617313717922},{"term":"boxum","score":13.594617313717922},{"term":"raadsrapportag","score":13.594617313717922},{"term":"dierenartspraktijk","score":13.594617313717922},{"term":"sted","score":13.594504023950407},{"term":"rempla","score":13.564764350568241},{"term":"impex","score":13.564764350568241},{"term":"_NUMna","score":13.564764350568241},{"term":"maaldrink","score":13.564764350568241},{"term":"liquiditeitsbehoeft","score":13.564764350568241},{"term":"nautical","score":13.564764350568241},{"term":"audienc","score":13.564764350568241},{"term":"xbox","score":13.564764350568241},{"term":") *","score":13.564764350568241},{"term":"pernis","score":13.564764350568241},{"term":"negentienduiz","score":13.564764350568241},{"term":"afkalv","score":13.564764350568241},{"term":"compaxo","score":13.564764350568241},{"term":"recipient","score":13.564764350568241},{"term":"dertronic","score":13.564764350568241},{"term":"madras","score":13.564764350568241},{"term":"rekenblad","score":13.564764350568241},{"term":"marshall","score":13.564764350568241},{"term":"tigera","score":13.564764350568241},{"term":"raffinaderij","score":13.564764350568241},{"term":"prodema","score":13.564764350568241},{"term":"installment","score":13.535776813694989},{"term":"mapl","score":13.535776813694989},{"term":"topzorg","score":13.535776813694989},{"term":"ferdinand","score":13.535776813694989},{"term":"discriminatiebepal","score":13.535776813694989},{"term":"fundashon","score":13.535776813694989},{"term":"verdegro","score":13.535776813694989},{"term":"vervolgprofijt","score":13.535776813694989},{"term":"valeur","score":13.535776813694989},{"term":"markland","score":13.535776813694989},{"term":"bommelerwaard","score":13.535776813694989},{"term":"prijsverminder","score":13.535776813694989},{"term":"raison","score":13.535776813694989},{"term":"ijzendoorn","score":13.535776813694989},{"term":"dierenkliniek","score":13.535776813694989},{"term":"hulpfond","score":13.535776813694989},{"term":"grondverzetbedrijf","score":13.535776813694989},{"term":"chleb","score":13.535776813694989},{"term":"batting","score":13.535776813694989},{"term":"machtigingsverzoek","score":13.535776813694989},{"term":"ijsselmond","score":13.535776813694989},{"term":"berlag","score":13.535776813694989},{"term":"partoer","score":13.535776813694989},{"term":"altra","score":13.535776813694989},{"term":"schadebehandelar","score":13.535776813694989},{"term":"toeter","score":13.535776813694989},{"term":"archipel","score":13.535776813694989},{"term":"gassel","score":13.535776813694989},{"term":"eerstdien","score":13.535776813694989},{"term":"bataljon","score":13.535776813694989},{"term":"indienster","score":13.507605936728291},{"term":"vliegopleid","score":13.507605936728291},{"term":"handwap","score":13.507605936728291},{"term":"ipso","score":13.507605936728291},{"term":"rdm","score":13.507605936728291},{"term":"kansendossier","score":13.507605936728291},{"term":"bloemenveil","score":13.507605936728291},{"term":"schietbeker","score":13.507605936728291},{"term":"_NUMartikel","score":13.507605936728291},{"term":"thermo","score":13.507605936728291},{"term":"_NUMha","score":13.507605936728291},{"term":"soit","score":13.507605936728291},{"term":"shopping","score":13.507605936728291},{"term":"korterink","score":13.507605936728291},{"term":"hotradio","score":13.507605936728291},{"term":"fastned","score":13.507605936728291},{"term":"bouwontwikkel","score":13.507605936728291},{"term":"woudfenn","score":13.507605936728291},{"term":"helena","score":13.507605936728291},{"term":"noad","score":13.507605936728291},{"term":"plunder","score":13.507605936728291},{"term":"leverag","score":13.507605936728291},{"term":"huuroveren","score":13.507605936728291},{"term":") —","score":13.507605936728291},{"term":"eurofunk","score":13.507605936728291},{"term":"kostum","score":13.507605936728291},{"term":"] /[","score":13.480206962540178},{"term":"novell","score":13.480206962540178},{"term":"woonvest","score":13.480206962540178},{"term":"shoebaloo","score":13.480206962540178},{"term":"frontteam","score":13.480206962540178},{"term":"greenhold","score":13.480206962540178},{"term":"klachtvereist","score":13.480206962540178},{"term":"bankrupt","score":13.480206962540178},{"term":"interactief","score":13.480206962540178},{"term":"stev","score":13.480206962540178},{"term":"jirnsum","score":13.480206962540178},{"term":"jvr","score":13.480206962540178},{"term":"afwegingskader","score":13.480206962540178},{"term":"geldigheidstermijn","score":13.480206962540178},{"term":"ccp","score":13.480206962540178},{"term":"gedaage_NUM","score":13.480206962540178},{"term":"verhuuropbrengst","score":13.480206962540178},{"term":"bedrijfsfusie","score":13.480206962540178},{"term":"westfries","score":13.480206962540178},{"term":"beleidsuitgangspunt","score":13.480206962540178},{"term":"royalton","score":13.480206962540178},{"term":"escortdam","score":13.480206962540178},{"term":"hippo","score":13.480206962540178},{"term":"wieringermer","score":13.480206962540178},{"term":"gebruiksvriend","score":13.480206962540178},{"term":"organogram","score":13.480206962540178},{"term":"handelsnaaminbreuk","score":13.453538715458016},{"term":"terbeschik","score":13.453538715458016},{"term":"opleidingscentrum","score":13.453538715458016},{"term":"melamin","score":13.453538715458016},{"term":"boumatic","score":13.453538715458016},{"term":"strabag","score":13.453538715458016},{"term":"castelijn","score":13.453538715458016},{"term":"shor","score":13.453538715458016},{"term":"arkin","score":13.453538715458016},{"term":"duyvis","score":13.453538715458016},{"term":"vrijstellingsgrond","score":13.453538715458016},{"term":"biomet","score":13.453538715458016},{"term":"suntrap","score":13.453538715458016},{"term":"onderhoudswerk","score":13.453538715458016},{"term":"technicus","score":13.453538715458016},{"term":"meijros","score":13.453538715458016},{"term":"mosae","score":13.427563229054755},{"term":"dienstenbond","score":13.427563229054755},{"term":"casser","score":13.427563229054755},{"term":"partijaanduid","score":13.427563229054755},{"term":"slotvast","score":13.427563229054755},{"term":"partnervrijstell","score":13.427563229054755},{"term":"minur","score":13.427563229054755},{"term":"hdt","score":13.427563229054755},{"term":"passiver","score":13.427563229054755},{"term":"personeelsvertegenwoord","score":13.427563229054755},{"term":"damast","score":13.427563229054755},{"term":"rijksgebouwendienst","score":13.427563229054755},{"term":"provisiebedrag","score":13.427563229054755},{"term":"uitstapj","score":13.427563229054755},{"term":"football","score":13.427563229054755},{"term":"vennootschapscontract","score":13.427563229054755},{"term":"nitsba","score":13.427563229054755},{"term":"bolidt","score":13.402245421070466},{"term":"conclusiv","score":13.402245421070466},{"term":"pijzelar","score":13.402245421070466},{"term":"motorsport","score":13.402245421070466},{"term":"spermacell","score":13.402245421070466},{"term":"vrijgehoud","score":13.402245421070466},{"term":"selikor","score":13.402245421070466},{"term":"appell","score":13.402245421070466},{"term":"gerw","score":13.402245421070466},{"term":"hypotheker","score":13.402245421070466},{"term":"spaarloonregel","score":13.402245421070466},{"term":"estoppel","score":13.402245421070466},{"term":"irish","score":13.402245421070466},{"term":"nijestee","score":13.402245421070466},{"term":"jozef","score":13.402245421070466},{"term":"consolidated","score":13.402245421070466},{"term":"rever","score":13.402245421070466},{"term":"smeulder","score":13.402245421070466},{"term":"christiaan","score":13.402245421070466},{"term":"holsboer","score":13.402245421070466},{"term":"tegenargument","score":13.402245421070466},{"term":"rolvoeg","score":13.402245421070466},{"term":"versicherung","score":13.377552808480095},{"term":"welbion","score":13.377552808480095},{"term":"dissolved","score":13.377552808480095},{"term":"rietberg","score":13.377552808480095},{"term":"meubelhal","score":13.377552808480095},{"term":"winningsbedrijf","score":13.377552808480095},{"term":"quispel","score":13.377552808480095},{"term":"neptunus","score":13.377552808480095},{"term":"weijn","score":13.377552808480095},{"term":"hennepgruis","score":13.377552808480095},{"term":"gemalto","score":13.377552808480095},{"term":"drema","score":13.377552808480095},{"term":"cvve","score":13.377552808480095},{"term":"hendrick","score":13.377552808480095},{"term":"inkomensdel","score":13.377552808480095},{"term":"mandaatverlen","score":13.377552808480095},{"term":"westrad","score":13.377552808480095},{"term":"finidaf","score":13.377552808480095},{"term":"voornaamswijz","score":13.377552808480095},{"term":"bleeker","score":13.377552808480095},{"term":"butterman","score":13.377552808480095},{"term":"westbroek","score":13.377552808480095},{"term":"vema","score":13.377552808480095},{"term":"redemption","score":13.377552808480095},{"term":"kooprijs","score":13.377552808480095},{"term":"georg","score":13.353455256901034},{"term":"hydepark","score":13.353455256901034},{"term":"obin","score":13.353455256901034},{"term":"nim","score":13.353455256901034},{"term":"fina","score":13.353455256901034},{"term":"venootschap","score":13.353455256901034},{"term":"gastvrij","score":13.353455256901034},{"term":"domino","score":13.353455256901034},{"term":"sparck","score":13.353455256901034},{"term":"teckru","score":13.353455256901034},{"term":"wifra","score":13.353455256901034},{"term":"eminent","score":13.353455256901034},{"term":"andreas","score":13.353455256901034},{"term":"eurovit","score":13.353455256901034},{"term":"locus","score":13.353455256901034},{"term":"maxwell","score":13.353455256901034},{"term":"exemption","score":13.353455256901034},{"term":"michiel","score":13.353455256901034},{"term":"onderhoudsbedrijf","score":13.353455256901034},{"term":"belastingkund","score":13.353455256901034},{"term":"zorgzak","score":13.353455256901034},{"term":"filosofie","score":13.353455256901034},{"term":"vrijhoud","score":13.353455256901034},{"term":"invested","score":13.353455256901034},{"term":"vitalis","score":13.353455256901034},{"term":"hytera","score":13.353455256901034},{"term":"schakenbosch","score":13.353455256901034},{"term":"speksnijder","score":13.353455256901034},{"term":"goederencodes","score":13.32992475949084},{"term":"operativ","score":13.32992475949084},{"term":"pressieverbod","score":13.32992475949084},{"term":"anbos","score":13.32992475949084},{"term":"kernproblematiek","score":13.32992475949084},{"term":"hofkwartier","score":13.32992475949084},{"term":"lmi","score":13.32992475949084},{"term":"verzetzak","score":13.32992475949084},{"term":"bikkel","score":13.32992475949084},{"term":"orthodontisch","score":13.32992475949084},{"term":"referr","score":13.32992475949084},{"term":"ontvankelijkheidsvrag","score":13.32992475949084},{"term":"attribution","score":13.32992475949084},{"term":"prealabel","score":13.32992475949084},{"term":"slotentrekker","score":13.32992475949084},{"term":"garantsteller","score":13.32992475949084},{"term":"inbreukvrag","score":13.32992475949084},{"term":"season","score":13.32992475949084},{"term":"compagnie","score":13.32992475949084},{"term":"iemant","score":13.32992475949084},{"term":"dkcl","score":13.32992475949084},{"term":"hoofzak","score":13.32992475949084},{"term":"ploeger","score":13.30693524126614},{"term":"carrouselfraud","score":13.30693524126614},{"term":"sympathie","score":13.30693524126614},{"term":"wervershof","score":13.30693524126614},{"term":"afc","score":13.30693524126614},{"term":"aanstrom","score":13.30693524126614},{"term":"verdoold","score":13.30693524126614},{"term":"vaststellingsregel","score":13.30693524126614},{"term":"isla","score":13.30693524126614},{"term":"sekeris","score":13.30693524126614},{"term":"trientalis","score":13.30693524126614},{"term":"mcp","score":13.30693524126614},{"term":"hci","score":13.30693524126614},{"term":"waterhuiz","score":13.30693524126614},{"term":"udenhout","score":13.30693524126614},{"term":"koersverliez","score":13.30693524126614},{"term":"sustainabl","score":13.30693524126614},{"term":"spanish","score":13.30693524126614},{"term":"soram","score":13.30693524126614},{"term":"apparent","score":13.30693524126614},{"term":"facie","score":13.30693524126614},{"term":"jaeger","score":13.30693524126614},{"term":"biedingsvennootschap","score":13.30693524126614},{"term":"okean","score":13.30693524126614},{"term":"onderzoekswen","score":13.30693524126614},{"term":"bestemmingsreserv","score":13.284462385414082},{"term":"hinderpaalcriterium","score":13.284462385414082},{"term":"itl","score":13.284462385414082},{"term":"meepo","score":13.284462385414082},{"term":"verteken","score":13.284462385414082},{"term":"hoofdprestatie","score":13.284462385414082},{"term":"], (…),","score":13.284462385414082},{"term":"withhold","score":13.284462385414082},{"term":"jett","score":13.284462385414082},{"term":"cromstrij","score":13.284462385414082},{"term":"cccp","score":13.284462385414082},{"term":"dopp","score":13.284462385414082},{"term":"onderworpenheidstoet","score":13.284462385414082},{"term":"integis","score":13.262483478695307},{"term":"febo","score":13.262483478695307},{"term":"petrikov","score":13.262483478695307},{"term":"schiller","score":13.262483478695307},{"term":"novitaris","score":13.262483478695307},{"term":"arcadia","score":13.262483478695307},{"term":"nerix","score":13.262483478695307},{"term":"dummy","score":13.262483478695307},{"term":"marien","score":13.262483478695307},{"term":"bios","score":13.262483478695307},{"term":"opwaardeerkaart","score":13.262483478695307},{"term":"montad","score":13.262483478695307},{"term":"foodguard","score":13.262483478695307},{"term":"uurwerk","score":13.262483478695307},{"term":"marinier","score":13.262483478695307},{"term":"gbv","score":13.262483478695307},{"term":"achthonderdvijft","score":13.262483478695307},{"term":"powertol","score":13.262483478695307},{"term":"fraanj","score":13.262483478695307},{"term":"subsidiaries","score":13.262483478695307},{"term":"oij","score":13.262483478695307},{"term":"rehabilitatie","score":13.262483478695307},{"term":"cream","score":13.262483478695307},{"term":"lebara","score":13.262483478695307},{"term":"curit","score":13.262483478695307},{"term":"parer","score":13.262483478695307},{"term":"adoptieverdrag","score":13.262483478695307},{"term":"krimat","score":13.240977273474343},{"term":"munitiedel","score":13.240977273474343},{"term":"nadaud","score":13.240977273474343},{"term":"bottenberg","score":13.240977273474343},{"term":"asperges","score":13.240977273474343},{"term":"onverwerkt","score":13.240977273474343},{"term":"aveleijn","score":13.240977273474343},{"term":"croiset","score":13.240977273474343},{"term":"uchel","score":13.240977273474343},{"term":"kogelinslag","score":13.240977273474343},{"term":"precis","score":13.240977273474343},{"term":"consumentenautoriteit","score":13.240977273474343},{"term":"telastgelegd","score":13.240977273474343},{"term":"tegeneis","score":13.240977273474343},{"term":"opportunities","score":13.240977273474343},{"term":"fractievoorzitter","score":13.240977273474343},{"term":"kernklacht","score":13.240977273474343},{"term":"restrict","score":13.240977273474343},{"term":"osc","score":13.240977273474343},{"term":"sagem","score":13.240977273474343},{"term":"monumentenvergunn","score":13.219923864276511},{"term":"schotland","score":13.219923864276511},{"term":"posthoorn","score":13.219923864276511},{"term":"schoterland","score":13.219923864276511},{"term":"probleemstell","score":13.219923864276511},{"term":"sigma","score":13.219923864276511},{"term":"foarunder","score":13.219923864276511},{"term":"pootgoed","score":13.219923864276511},{"term":"affectief","score":13.219923864276511},{"term":"sugar","score":13.219923864276511},{"term":"meess","score":13.219923864276511},{"term":"secondary","score":13.219923864276511},{"term":"spuugd","score":13.219923864276511},{"term":"bruikleengever","score":13.219923864276511},{"term":"beup","score":13.219923864276511},{"term":"natuurwetenschapp","score":13.219923864276511},{"term":"zorginkop","score":13.219923864276511},{"term":"vv_NUM","score":13.219923864276511},{"term":"cruis","score":13.219923864276511},{"term":"sciences","score":13.219923864276511},{"term":"praktijkpand","score":13.219923864276511},{"term":"aros","score":13.219923864276511},{"term":"fiam","score":13.219923864276511},{"term":"tijdsplann","score":13.219923864276511},{"term":"landmacht","score":13.219923864276511},{"term":"ontwikkelingsproject","score":13.199304577073775},{"term":"netwerkplaats","score":13.199304577073775},{"term":"sanofi","score":13.199304577073775},{"term":"cowhous","score":13.199304577073775},{"term":"ndm","score":13.199304577073775},{"term":"acceptatietest","score":13.199304577073775},{"term":"](…)","score":13.199304577073775},{"term":"notified","score":13.199304577073775},{"term":"visacard","score":13.199304577073775},{"term":"fosfor","score":13.199304577073775},{"term":"autres","score":13.199304577073775},{"term":"metaalwar","score":13.199304577073775},{"term":"wessem","score":13.199304577073775},{"term":"velocitas","score":13.199304577073775},{"term":"linn","score":13.199304577073775},{"term":"schoolexam","score":13.199304577073775},{"term":"anton","score":13.199304577073775},{"term":"named","score":13.199304577073775},{"term":"interfod","score":13.199304577073775},{"term":"wilvicto","score":13.199304577073775},{"term":"managementinformatie","score":13.199304577073775},{"term":"in_NUMwater","score":13.199304577073775},{"term":"cum","score":13.179101869756256},{"term":"dely","score":13.179101869756256},{"term":"lunsingh","score":13.179101869756256},{"term":"ğ","score":13.179101869756256},{"term":"retailgroep","score":13.179101869756256},{"term":"inkomensbegrip","score":13.179101869756256},{"term":"metaalbewerk","score":13.179101869756256},{"term":"onderhoudstermijn","score":13.179101869756256},{"term":"consolid","score":13.179101869756256},{"term":"avan","score":13.179101869756256},{"term":"eylaend","score":13.179101869756256},{"term":"trucking","score":13.179101869756256},{"term":"_NUMdavit","score":13.179101869756256},{"term":"praatj","score":13.179101869756256},{"term":"ebn","score":13.179101869756256},{"term":"wireles","score":13.179101869756256},{"term":"surrender","score":13.179101869756256},{"term":"hippisch","score":13.179101869756256},{"term":"bavo","score":13.179101869756256},{"term":"krimpenerwaard","score":13.179101869756256},{"term":"denied","score":13.179101869756256},{"term":"increases","score":13.179101869756256},{"term":"distantie","score":13.159299242460076},{"term":"grondverzet","score":13.159299242460076},{"term":"wlp","score":13.159299242460076},{"term":"qatar","score":13.159299242460076},{"term":"waaier","score":13.159299242460076},{"term":"sovf","score":13.159299242460076},{"term":"kledingbedrijf","score":13.159299242460076},{"term":"ncvb","score":13.159299242460076},{"term":"herbeleggingsreserv","score":13.159299242460076},{"term":"afgetimmerd","score":13.159299242460076},{"term":"consid","score":13.159299242460076},{"term":"simpelveld","score":13.159299242460076},{"term":"bouwondernem","score":13.159299242460076},{"term":"stout","score":13.159299242460076},{"term":"biemond","score":13.159299242460076},{"term":"urban","score":13.159299242460076},{"term":"leftover","score":13.159299242460076},{"term":"milestones","score":13.159299242460076},{"term":"jekervest","score":13.159299242460076},{"term":"subsidiary","score":13.159299242460076},{"term":"adco","score":13.159299242460076},{"term":"nietigheidsverwer","score":13.159299242460076},{"term":"hoka","score":13.159299242460076},{"term":"overcompensatie","score":13.159299242460076},{"term":"woonservic","score":13.159299242460076},{"term":"prangend","score":13.159299242460076},{"term":"ongevalsgerelateerd","score":13.159299242460076},{"term":"iertrex","score":13.139881156602975},{"term":"florius","score":13.139881156602975},{"term":"zyg","score":13.139881156602975},{"term":"/)","score":13.139881156602975},{"term":"eetwar","score":13.139881156602975},{"term":"belastingtariev","score":13.139881156602975},{"term":"sera","score":13.139881156602975},{"term":"kinderboerderij","score":13.139881156602975},{"term":"cordares","score":13.139881156602975},{"term":"sfb","score":13.139881156602975},{"term":"asml","score":13.139881156602975},{"term":"_NUMjuni","score":13.139881156602975},{"term":"deelkwalificatie","score":13.139881156602975},{"term":"bouwbasic","score":13.12083296163228},{"term":"carint","score":13.12083296163228},{"term":"taxand","score":13.12083296163228},{"term":"schieveld","score":13.12083296163228},{"term":"noordelos","score":13.12083296163228},{"term":"kegra","score":13.12083296163228},{"term":"assurantiezorg","score":13.12083296163228},{"term":"uitwerpsel","score":13.12083296163228},{"term":"unipharma","score":13.12083296163228},{"term":"kenia","score":13.12083296163228},{"term":"obligo","score":13.12083296163228},{"term":"nca","score":13.12083296163228},{"term":"bestuursfunctie","score":13.12083296163228},{"term":"ista","score":13.12083296163228},{"term":"founded","score":13.12083296163228},{"term":"cfr","score":13.12083296163228},{"term":"uba","score":13.12083296163228},{"term":"titulair","score":13.12083296163228},{"term":"beoordelingsaspect","score":13.12083296163228},{"term":"involvement","score":13.102140828620128},{"term":"conspiracy","score":13.102140828620128},{"term":"tariefswijz","score":13.102140828620128},{"term":"professionaliser","score":13.102140828620128},{"term":"ontredderd","score":13.102140828620128},{"term":"paddo","score":13.102140828620128},{"term":"opruimkost","score":13.102140828620128},{"term":"versatel","score":13.102140828620128},{"term":"adolescentie","score":13.102140828620128},{"term":"handschriftonderzoek","score":13.102140828620128},{"term":"mestbassin","score":13.102140828620128},{"term":"stellendam","score":13.102140828620128},{"term":"gradatie","score":13.102140828620128},{"term":"snorfietser","score":13.102140828620128},{"term":"interventiewet","score":13.102140828620128},{"term":"eprom","score":13.102140828620128},{"term":"values","score":13.102140828620128},{"term":"zestienduiz","score":13.102140828620128},{"term":"stomamaterial","score":13.102140828620128},{"term":"luchtverontrein","score":13.102140828620128},{"term":"blackston","score":13.102140828620128},{"term":"amitech","score":13.102140828620128},{"term":"custodian","score":13.102140828620128},{"term":"zorgbelang","score":13.102140828620128},{"term":"giethoorn","score":13.102140828620128},{"term":"competenc","score":13.083791689951932},{"term":"houses","score":13.083791689951932},{"term":"veraart","score":13.083791689951932},{"term":"veerdrukpistol","score":13.083791689951932},{"term":"penalties","score":13.083791689951932},{"term":"leerorkest","score":13.083791689951932},{"term":"laarbek","score":13.083791689951932},{"term":"prisma","score":13.083791689951932},{"term":"berten","score":13.083791689951932},{"term":"navteq","score":13.083791689951932},{"term":"leveringstermijn","score":13.083791689951932},{"term":"opposer","score":13.083791689951932},{"term":"unicum","score":13.083791689951932},{"term":"franciscus","score":13.083791689951932},{"term":"affiliates","score":13.083791689951932},{"term":"rolverwijz","score":13.083791689951932},{"term":"zederik","score":13.083791689951932},{"term":"broodmes","score":13.083791689951932},{"term":"bikini","score":13.083791689951932},{"term":"heat","score":13.083791689951932},{"term":"rechtmatigheidstoet","score":13.065773184449252},{"term":"vijfhonderdvijft","score":13.065773184449252},{"term":"aramis","score":13.065773184449252},{"term":"onoordeelkund","score":13.065773184449252},{"term":"uitzendbepal","score":13.065773184449252},{"term":"schouwing","score":13.065773184449252},{"term":"bvi","score":13.065773184449252},{"term":"mortgag","score":13.065773184449252},{"term":"intertast","score":13.065773184449252},{"term":"handtast","score":13.065773184449252},{"term":"hoffland","score":13.065773184449252},{"term":"dunea","score":13.065773184449252},{"term":"autobestuurder","score":13.065773184449252},{"term":"hemar","score":13.065773184449252},{"term":"tucht","score":13.065773184449252},{"term":"ingwers","score":13.048073607349851},{"term":"rembrandt","score":13.048073607349851},{"term":"basisoctrooi","score":13.048073607349851},{"term":"bronovo","score":13.048073607349851},{"term":"cristal","score":13.048073607349851},{"term":"negligenc","score":13.048073607349851},{"term":"verzekeringsbewijs","score":13.048073607349851},{"term":"soed","score":13.048073607349851},{"term":"whereabout","score":13.030681864637982},{"term":"halford","score":13.030681864637982},{"term":"bedrijfsleid","score":13.030681864637982},{"term":"drabo","score":13.030681864637982},{"term":"pl_NUMcd","score":13.030681864637982},{"term":"mgx","score":13.030681864637982},{"term":"schaink","score":13.030681864637982},{"term":"hereof","score":13.030681864637982},{"term":"vakantiehuis","score":13.030681864637982},{"term":"powned","score":13.030681864637982},{"term":"styl","score":13.030681864637982},{"term":"snl","score":13.030681864637982},{"term":"risicobeoordel","score":13.030681864637982},{"term":"omgevingsfactor","score":13.030681864637982},{"term":"_NUMsexies","score":13.013587431278683},{"term":"passport","score":13.013587431278683},{"term":"bef","score":13.013587431278683},{"term":"bnm","score":13.013587431278683},{"term":"terlouw","score":13.013587431278683},{"term":"wii","score":13.013587431278683},{"term":"scheepvaartondernem","score":13.013587431278683},{"term":"bouwmanagement","score":13.013587431278683},{"term":"rotary","score":13.013587431278683},{"term":"marvesa","score":13.013587431278683},{"term":"integration","score":13.013587431278683},{"term":"parkmobil","score":13.013587431278683},{"term":"plaswijck","score":13.013587431278683},{"term":"tonnageregel","score":13.013587431278683},{"term":"leerfashion","score":12.9967803129623},{"term":"oogheelkund","score":12.9967803129623},{"term":"standplaatsvergunn","score":12.9967803129623},{"term":"ncd","score":12.9967803129623},{"term":"broed","score":12.9967803129623},{"term":"schadeherstel","score":12.9967803129623},{"term":"muzerie","score":12.9967803129623},{"term":"onteigeningsged","score":12.9967803129623},{"term":"ami","score":12.9967803129623},{"term":"anatom","score":12.9967803129623},{"term":"otterlo","score":12.9967803129623},{"term":"kredietverlener","score":12.9967803129623},{"term":"opsporingsteam","score":12.98025101101109},{"term":"jurilex","score":12.98025101101109},{"term":"sprongcassatie","score":12.98025101101109},{"term":"tennisveren","score":12.98025101101109},{"term":"matchend","score":12.98025101101109},{"term":"handpalm","score":12.98025101101109},{"term":"herzieningsaanvrag","score":12.98025101101109},{"term":"thereafter","score":12.98025101101109},{"term":"halfzus","score":12.98025101101109},{"term":"keuzerecht","score":12.98025101101109},{"term":"shipyard","score":12.98025101101109},{"term":"relatieverklar","score":12.98025101101109},{"term":"handelsmaatschappij","score":12.96399049013931},{"term":"frederik","score":12.96399049013931},{"term":"ruilplan","score":12.96399049013931},{"term":"artikel_NUM","score":12.96399049013931},{"term":"gansewinkel","score":12.96399049013931},{"term":"totalplast","score":12.96399049013931},{"term":"plastiflex","score":12.96399049013931},{"term":"dagrecreatie","score":12.947990148792869},{"term":"fulfilled","score":12.947990148792869},{"term":"euroclaim","score":12.947990148792869},{"term":"beeuwkes","score":12.947990148792869},{"term":"grupa","score":12.947990148792869},{"term":"ambtsweg","score":12.947990148792869},{"term":"hoogslag","score":12.947990148792869},{"term":"hauck","score":12.947990148792869},{"term":"cw","score":12.947990148792869},{"term":"duinkerk","score":12.947990148792869},{"term":"spijz","score":12.947990148792869},{"term":"terugvorderingsbeschik","score":12.947990148792869},{"term":"associates","score":12.947990148792869},{"term":"lmb","score":12.947990148792869},{"term":"beleggingsdeelnem","score":12.947990148792869},{"term":"ada","score":12.947990148792869},{"term":"herstelarrest","score":12.93224179182473},{"term":"toetsingsverbod","score":12.93224179182473},{"term":"lijnd","score":12.93224179182473},{"term":"tripp","score":12.93224179182473},{"term":"coli","score":12.93224179182473},{"term":"thales","score":12.93224179182473},{"term":"luttermolenveld","score":12.93224179182473},{"term":"multidag","score":12.93224179182473},{"term":"toko","score":12.93224179182473},{"term":"voorhuw","score":12.93224179182473},{"term":"ontaard","score":12.93224179182473},{"term":"gereformeerd","score":12.916737605288764},{"term":"wolber","score":12.916737605288764},{"term":"energiemaatschappij","score":12.916737605288764},{"term":"beroepsveren","score":12.916737605288764},{"term":"notarispraktijk","score":12.916737605288764},{"term":"fotoshop","score":12.916737605288764},{"term":"draairam","score":12.916737605288764},{"term":"ontruimingsdatum","score":12.916737605288764},{"term":"infratechniek","score":12.916737605288764},{"term":"attributabl","score":12.916737605288764},{"term":"biesebek","score":12.916737605288764},{"term":"vaktechnisch","score":12.916737605288764},{"term":"gunningsprocedur","score":12.916737605288764},{"term":"ridderveld","score":12.916737605288764},{"term":"fairmount","score":12.916737605288764},{"term":"waterbouw","score":12.916737605288764},{"term":"_NUMag","score":12.916737605288764},{"term":"klachtenregel","score":12.916737605288764},{"term":"talen","score":12.901470133157977},{"term":"deltawon","score":12.901470133157977},{"term":"reclasseringsbegeleid","score":12.901470133157977},{"term":"agema","score":12.901470133157977},{"term":"eska","score":12.901470133157977},{"term":"authorized","score":12.901470133157977},{"term":"broertjes","score":12.901470133157977},{"term":"oceanteam","score":12.901470133157977},{"term":"modal","score":12.901470133157977},{"term":"herindicatie","score":12.901470133157977},{"term":"apc","score":12.886432255793435},{"term":"twistpunt","score":12.886432255793435},{"term":"querido","score":12.886432255793435},{"term":"infringement","score":12.886432255793435},{"term":"hochwald","score":12.886432255793435},{"term":"omrin","score":12.886432255793435},{"term":"velerlei","score":12.886432255793435},{"term":"atlantis","score":12.886432255793435},{"term":"herverkavel","score":12.886432255793435},{"term":"] (…)”","score":12.886432255793435},{"term":"wkk","score":12.886432255793435},{"term":"fraudeur","score":12.886432255793435},{"term":"avas","score":12.886432255793435},{"term":"kolenbelast","score":12.886432255793435},{"term":"govern","score":12.886432255793435},{"term":"mitsui","score":12.886432255793435},{"term":"zg","score":12.886432255793435},{"term":"iibis","score":12.886432255793435},{"term":"georgi","score":12.886432255793435},{"term":"chronologie","score":12.871617170008296},{"term":"draagmoeder","score":12.871617170008296},{"term":"promen","score":12.871617170008296},{"term":"eikelenbom","score":12.871617170008296},{"term":"apellant","score":12.871617170008296},{"term":"french","score":12.871617170008296},{"term":"paulus","score":12.871617170008296},{"term":"ief","score":12.871617170008296},{"term":"kijveland","score":12.871617170008296},{"term":"bankrelatie","score":12.871617170008296},{"term":"weldeg","score":12.857018370587143},{"term":"cmc","score":12.857018370587143},{"term":"euretco","score":12.857018370587143},{"term":"advocatenmaatschap","score":12.857018370587143},{"term":"polski","score":12.857018370587143},{"term":"bouwtermijn","score":12.857018370587143},{"term":"staalbouw","score":12.857018370587143},{"term":"meta","score":12.857018370587143},{"term":"staffing","score":12.857018370587143},{"term":"wijzigingsgrond","score":12.857018370587143},{"term":"ecli","score":12.85226160914997},{"term":"avenue_NUM","score":12.842629633135044},{"term":"uitdrag","score":12.842629633135044},{"term":"advertis","score":12.842629633135044},{"term":"draagkrachtverwer","score":12.842629633135044},{"term":"friso","score":12.842629633135044},{"term":"tabijn","score":12.842629633135044},{"term":"oostrom","score":12.842629633135044},{"term":"clemen","score":12.842629633135044},{"term":"aloysius","score":12.842629633135044},{"term":"houseboat","score":12.842629633135044},{"term":"didam","score":12.842629633135044},{"term":"wapenverlof","score":12.842629633135044},{"term":"stages","score":12.842629633135044},{"term":"brauw","score":12.842629633135044},{"term":"issos","score":12.842629633135044},{"term":"connex","score":12.842629633135044},{"term":"ehs","score":12.842629633135044},{"term":"winl","score":12.828444998143086},{"term":"genemuid","score":12.828444998143086},{"term":"adam","score":12.828444998143086},{"term":"weber","score":12.828444998143086},{"term":"vormvast","score":12.828444998143086},{"term":"tijdschema","score":12.828444998143086},{"term":"fokken","score":12.828444998143086},{"term":"ammoniakemissie","score":12.828444998143086},{"term":"fob","score":12.828444998143086},{"term":"hpg","score":12.828444998143086},{"term":"league","score":12.828444998143086},{"term":"honderdzest","score":12.828444998143086},{"term":"carwash","score":12.828444998143086},{"term":"afzegg","score":12.828444998143086},{"term":"zumba","score":12.814458756168346},{"term":"verkeersregelinstallatie","score":12.814458756168346},{"term":"leegwaarderatio","score":12.814458756168346},{"term":"contractur","score":12.814458756168346},{"term":"turnkey","score":12.814458756168346},{"term":"scheldestrom","score":12.814458756168346},{"term":"compet","score":12.814458756168346},{"term":"benoit","score":12.814458756168346},{"term":"volkerrail","score":12.814458756168346},{"term":"bgo","score":12.814458756168346},{"term":"cbre","score":12.814458756168346},{"term":"tenniscentrum","score":12.814458756168346},{"term":"duinzigt","score":12.814458756168346},{"term":"taliban","score":12.814458756168346},{"term":"uitlooptermijn","score":12.800665434036011},{"term":"depositogarantiestelsel","score":12.800665434036011},{"term":"triest","score":12.800665434036011},{"term":"compaq","score":12.800665434036011},{"term":"bisdom","score":12.800665434036011},{"term":"integrated","score":12.800665434036011},{"term":"error","score":12.800665434036011},{"term":"leegstandsrisico","score":12.800665434036011},{"term":"kredietopzegg","score":12.800665434036011},{"term":"bierglas","score":12.787059781980233},{"term":"litigation","score":12.787059781980233},{"term":"isala","score":12.787059781980233},{"term":"grondtransacties","score":12.787059781980233},{"term":"belastingtijdvak","score":12.787059781980233},{"term":"bencis","score":12.787059781980233},{"term":"scholengemeenschap","score":12.787059781980233},{"term":"lines","score":12.787059781980233},{"term":"burgum","score":12.773636761648092},{"term":"ouderschapsonderzoek","score":12.773636761648092},{"term":"geertruidenberg","score":12.773636761648092},{"term":"weski","score":12.773636761648092},{"term":"afsnijd","score":12.773636761648092},{"term":"boarnsterhim","score":12.773636761648092},{"term":"]”),","score":12.773636761648092},{"term":"relativiteit","score":12.773636761648092},{"term":"trias","score":12.773636761648092},{"term":"hrc","score":12.773636761648092},{"term":"managementfes","score":12.773636761648092},{"term":"berend","score":12.773636761648092},{"term":"compressie","score":12.760391534898071},{"term":"cement","score":12.760391534898071},{"term":"bvdb","score":12.760391534898071},{"term":"henisol","score":12.760391534898071},{"term":"xxi","score":12.760391534898071},{"term":"wiertz","score":12.760391534898071},{"term":"cardio","score":12.760391534898071},{"term":"marie","score":12.747319453330718},{"term":"nxp","score":12.747319453330718},{"term":"promocean","score":12.747319453330718},{"term":"avrotros","score":12.747319453330718},{"term":"parklan","score":12.747319453330718},{"term":"………","score":12.747319453330718},{"term":"archeomedia","score":12.747319453330718},{"term":"gikondo","score":12.73441604849481},{"term":"inbreukvorder","score":12.73441604849481},{"term":"meegeholp","score":12.73441604849481},{"term":"royalties","score":12.73441604849481},{"term":"tuinwijk","score":12.73441604849481},{"term":"blokkeringsrecht","score":12.73441604849481},{"term":"acid","score":12.73441604849481},{"term":"ergon","score":12.73441604849481},{"term":"outlet","score":12.73441604849481},{"term":"prepar","score":12.73441604849481},{"term":"bankruptcy","score":12.73441604849481},{"term":"murder","score":12.72167702271738},{"term":"putevi","score":12.72167702271738},{"term":"jongmeerderjarige_NUM","score":12.72167702271738},{"term":"aabo","score":12.72167702271738},{"term":"clam","score":12.72167702271738},{"term":"motorola","score":12.72167702271738},{"term":"daniel","score":12.72167702271738},{"term":"meerderjarigenbewind","score":12.72167702271738},{"term":"promisvonnis","score":12.72167702271738},{"term":"beloningsvoordel","score":12.72167702271738},{"term":"binnenterrein","score":12.72167702271738},{"term":"multimediafiles","score":12.72167702271738},{"term":"jurisprudenti","score":12.70909824051052},{"term":"meldingsformulier","score":12.70909824051052},{"term":"rtsil","score":12.70909824051052},{"term":"ln","score":12.70909824051052},{"term":"collegial","score":12.70909824051052},{"term":"appointment","score":12.70909824051052},{"term":"onderhandelingstraject","score":12.70909824051052},{"term":"freight","score":12.696675720511964},{"term":"nuth","score":12.696675720511964},{"term":"brocacef","score":12.696675720511964},{"term":"fransson","score":12.696675720511964},{"term":"gm","score":12.696675720511964},{"term":"benieuwd","score":12.696675720511964},{"term":"relatiegeschenk","score":12.696675720511964},{"term":"fiscaalnummer","score":12.696675720511964},{"term":"verzoeker_NUM","score":12.696675720511964},{"term":"ria","score":12.696675720511964},{"term":"parkeerkelder","score":12.696675720511964},{"term":"permx","score":12.68440562792015},{"term":"hommerson","score":12.68440562792015},{"term":"tonger","score":12.68440562792015},{"term":"idm","score":12.68440562792015},{"term":"served","score":12.68440562792015},{"term":"subscription","score":12.68440562792015},{"term":"ish","score":12.68440562792015},{"term":"nautisch","score":12.68440562792015},{"term":"aimg","score":12.68440562792015},{"term":"verstekarrest","score":12.68440562792015},{"term":"treb","score":12.68440562792015},{"term":"taxatiebureau","score":12.68440562792015},{"term":"vervaardigt","score":12.68440562792015},{"term":"”?","score":12.68440562792015},{"term":"knoflok","score":12.68440562792015},{"term":"vlonder","score":12.68440562792015},{"term":"hologic","score":12.68440562792015},{"term":"qiag","score":12.68440562792015},{"term":"genotsrecht","score":12.68440562792015},{"term":"waterbedrijf","score":12.68440562792015},{"term":"grondbeginsel","score":12.672284267387804},{"term":"desk","score":12.672284267387804},{"term":"mijnbouwwet","score":12.672284267387804},{"term":"verkeersvlieger","score":12.672284267387804},{"term":"mijnss","score":12.672284267387804},{"term":"roland","score":12.672284267387804},{"term":"gunningsfas","score":12.672284267387804},{"term":"strooi","score":12.672284267387804},{"term":"cosmo","score":12.672284267387804},{"term":"pensioenlast","score":12.672284267387804},{"term":"domus","score":12.672284267387804},{"term":"brancheorganisatie","score":12.672284267387804},{"term":"zakelijkheidstoet","score":12.672284267387804},{"term":"advisor","score":12.660308076341089},{"term":"goederenbeweg","score":12.660308076341089},{"term":"gonggrijp","score":12.660308076341089},{"term":"verpakkingenbelast","score":12.660308076341089},{"term":"bcf","score":12.660308076341089},{"term":"drom","score":12.660308076341089},{"term":"schadeperiod","score":12.660308076341089},{"term":"hypotheekstell","score":12.660308076341089},{"term":"pac","score":12.660308076341089},{"term":"gevaarscriterium","score":12.660308076341089},{"term":"bandopnam","score":12.660308076341089},{"term":"hec","score":12.648473618694085},{"term":"monumentenzorg","score":12.648473618694085},{"term":"contractperiod","score":12.648473618694085},{"term":"nevenverzoek","score":12.648473618694085},{"term":"meningsverschill","score":12.648473618694085},{"term":"scheper","score":12.636777578930895},{"term":"mierd","score":12.636777578930895},{"term":"bedrijfsmaatschapp","score":12.636777578930895},{"term":"veeggrief","score":12.636777578930895},{"term":"bouwstoff","score":12.636777578930895},{"term":"welsaen","score":12.636777578930895},{"term":"peraties","score":12.636777578930895},{"term":"huurkort","score":12.636777578930895},{"term":"romein","score":12.636777578930895},{"term":"playgo","score":12.636777578930895},{"term":"spac","score":12.636777578930895},{"term":"breuil","score":12.636777578930895},{"term":"hub","score":12.636777578930895},{"term":"ziut","score":12.636777578930895},{"term":"directievoer","score":12.636777578930895},{"term":"incorporated","score":12.636777578930895},{"term":"anatomisch","score":12.636777578930895},{"term":"chiropractor","score":12.625216756529818},{"term":"wamil","score":12.625216756529818},{"term":"cijfermateriaal","score":12.625216756529818},{"term":"weapon","score":12.625216756529818},{"term":"borsbom","score":12.625216756529818},{"term":"handhavingstraject","score":12.625216756529818},{"term":"curatric","score":12.613788060706195},{"term":"uts","score":12.613788060706195},{"term":"ferm","score":12.613788060706195},{"term":"schouwenburg","score":12.613788060706195},{"term":"dream","score":12.613788060706195},{"term":"xxii","score":12.613788060706195},{"term":"printing","score":12.613788060706195},{"term":"robeco","score":12.613788060706195},{"term":"giving","score":12.613788060706195},{"term":"vitae","score":12.613788060706195},{"term":"geldkist","score":12.613788060706195},{"term":"dps","score":12.613788060706195},{"term":"] ) (","score":12.613788060706195},{"term":"wapenhandel","score":12.602488505452262},{"term":"ekro","score":12.602488505452262},{"term":"hvo","score":12.602488505452262},{"term":"arkefly","score":12.602488505452262},{"term":"rossinant","score":12.602488505452262},{"term":"denial","score":12.602488505452262},{"term":"muts","score":12.602488505452262},{"term":"laco","score":12.602488505452262},{"term":"technologies","score":12.602488505452262},{"term":"rabbie","score":12.591315204854137},{"term":"produkties","score":12.591315204854137},{"term":"uniformer","score":12.591315204854137},{"term":"mensink","score":12.591315204854137},{"term":"settlor","score":12.591315204854137},{"term":"indelingsverorden","score":12.591315204854137},{"term":"toy","score":12.591315204854137},{"term":"dommel","score":12.591315204854137},{"term":"aanraakt","score":12.591315204854137},{"term":"merchant","score":12.591315204854137},{"term":"whatever","score":12.591315204854137},{"term":"vijfentwintigduiz","score":12.591315204854137},{"term":"parkking","score":12.591315204854137},{"term":"aqua","score":12.580265368667552},{"term":"booking","score":12.580265368667552},{"term":"inten","score":12.580265368667552},{"term":"mahr","score":12.580265368667552},{"term":"harsco","score":12.580265368667552},{"term":"biezenrijt","score":12.580265368667552},{"term":"ambities","score":12.580265368667552},{"term":"gegevensverstrek","score":12.580265368667552},{"term":"zh","score":12.569336298135362},{"term":"hoekman","score":12.569336298135362},{"term":"trmi","score":12.569336298135362},{"term":"album","score":12.569336298135362},{"term":"thermphos","score":12.569336298135362},{"term":"aanstek","score":12.569336298135362},{"term":"antibiotica","score":12.569336298135362},{"term":"waardematrix","score":12.569336298135362},{"term":"baard","score":12.569336298135362},{"term":"belangenveren","score":12.569336298135362},{"term":"kadasternummer","score":12.569336298135362},{"term":"transvision","score":12.558525382031146},{"term":"rotij","score":12.558525382031146},{"term":"astellas","score":12.558525382031146},{"term":"elfduiz","score":12.558525382031146},{"term":"einddoel","score":12.558525382031146},{"term":"ontslagred","score":12.558525382031146},{"term":"adviesprocedur","score":12.547830092914397},{"term":"afreiz","score":12.547830092914397},{"term":"mme","score":12.547830092914397},{"term":"slim","score":12.547830092914397},{"term":"opportuniteit","score":12.547830092914397},{"term":"zijwand","score":12.547830092914397},{"term":"geannoteerd","score":12.53724798358386},{"term":"weisz","score":12.53724798358386},{"term":"bouwsten","score":12.53724798358386},{"term":"rayon","score":12.53724798358386},{"term":"diarree","score":12.53724798358386},{"term":"hoen","score":12.53724798358386},{"term":"kostentoereken","score":12.53724798358386},{"term":"familiebezoek","score":12.53724798358386},{"term":"spv","score":12.53724798358386},{"term":"xs_NUMall","score":12.53724798358386},{"term":"prejudic","score":12.526776683716566},{"term":"douanetarief","score":12.526776683716566},{"term":"procesvertegenwoord","score":12.526776683716566},{"term":"schumacker","score":12.526776683716566},{"term":"phanos","score":12.526776683716566},{"term":"mediaspeler","score":12.526776683716566},{"term":"duwo","score":12.526776683716566},{"term":"mcd","score":12.526776683716566},{"term":"forego","score":12.526776683716566},{"term":"[…] ,","score":12.526776683716566},{"term":"laagbelast","score":12.526776683716566},{"term":"contractual","score":12.51641389668102},{"term":"zoeterwoud","score":12.51641389668102},{"term":"winstrecht","score":12.51641389668102},{"term":"kerberg","score":12.51641389668102},{"term":"voortman","score":12.51641389668102},{"term":"soffimat","score":12.51641389668102},{"term":"→","score":12.51641389668102},{"term":"beleidsbesluit","score":12.51641389668102},{"term":"kostenbegrot","score":12.51641389668102},{"term":"fourner","score":12.51641389668102},{"term":"gruis","score":12.51641389668102},{"term":"lijkschouw","score":12.50615739651383},{"term":"cadeaus","score":12.50615739651383},{"term":"strafrechtspleg","score":12.50615739651383},{"term":"helpend","score":12.50615739651383},{"term":"congregatie","score":12.50615739651383},{"term":"overleveringsdetentie","score":12.50615739651383},{"term":"ministry","score":12.50615739651383},{"term":"mkz","score":12.50615739651383},{"term":"wisselkoer","score":12.50615739651383},{"term":"voorrangsregel","score":12.50615739651383},{"term":"nachthemd","score":12.50615739651383},{"term":"vleeswar","score":12.50615739651383},{"term":"uitvoeringsorgan","score":12.50615739651383},{"term":"premier","score":12.50615739651383},{"term":"] ”,","score":12.50615739651383},{"term":"frovia","score":12.496005025049811},{"term":"rechtsgrondslag","score":12.496005025049811},{"term":"ambulancezorg","score":12.496005025049811},{"term":"huisbankier","score":12.496005025049811},{"term":"hoeslak","score":12.496005025049811},{"term":"tebodin","score":12.496005025049811},{"term":"berekeningsmethodiek","score":12.496005025049811},{"term":"krom","score":12.496005025049811},{"term":"beagl","score":12.496005025049811},{"term":"kasopnam","score":12.48595468919631},{"term":"reisur","score":12.48595468919631},{"term":"ration","score":12.48595468919631},{"term":"exclusively","score":12.48595468919631},{"term":"toelegt","score":12.48595468919631},{"term":"michel","score":12.48595468919631},{"term":"medifirst","score":12.48595468919631},{"term":"farm","score":12.48595468919631},{"term":"lijkschouwer","score":12.48595468919631},{"term":"dpd","score":12.476004358343143},{"term":"bruno","score":12.476004358343143},{"term":"beursgang","score":12.476004358343143},{"term":"oy","score":12.476004358343143},{"term":"vertragingsvergoed","score":12.476004358343143},{"term":"tub","score":12.476004358343143},{"term":"mdca","score":12.476004358343143},{"term":"automobiel","score":12.476004358343143},{"term":"geassocieerd","score":12.476004358343143},{"term":"zusjes","score":12.476004358343143},{"term":"nieuwkop","score":12.476004358343143},{"term":"schoolkeuz","score":12.476004358343143},{"term":"mantgem","score":12.476004358343143},{"term":"hyatt","score":12.46615206190013},{"term":"norma","score":12.46615206190013},{"term":"gastel","score":12.46615206190013},{"term":"soesterberg","score":12.46615206190013},{"term":"huurgarantie","score":12.46615206190013},{"term":"maritim","score":12.46615206190013},{"term":"assurantiekantor","score":12.46615206190013},{"term":"assurantie","score":12.46615206190013},{"term":"_NUMae","score":12.46615206190013},{"term":"town","score":12.456395886954766},{"term":"background","score":12.456395886954766},{"term":"verrekenpost","score":12.456395886954766},{"term":"fait","score":12.456395886954766},{"term":"vereffeningskost","score":12.456395886954766},{"term":"vvt","score":12.456395886954766},{"term":"tunesi","score":12.44673397604303},{"term":"uci","score":12.44673397604303},{"term":"fresh","score":12.44673397604303},{"term":"ongevallenverzeker","score":12.44673397604303},{"term":"stec","score":12.44673397604303},{"term":"richard","score":12.44673397604303},{"term":"eia","score":12.44673397604303},{"term":"geperforeerd","score":12.44673397604303},{"term":"beklagzak","score":12.44673397604303},{"term":"zakgeld","score":12.44673397604303},{"term":"defensiev","score":12.437164525026878},{"term":"attachment","score":12.437164525026878},{"term":"oudedagsvoorzien","score":12.437164525026878},{"term":"dg","score":12.437164525026878},{"term":"kapperszak","score":12.427685781072334},{"term":"mega","score":12.427685781072334},{"term":"fail","score":12.427685781072334},{"term":"verhol","score":12.427685781072334},{"term":"herzieningsprocedur","score":12.427685781072334},{"term":"dvg","score":12.427685781072334},{"term":"itom","score":12.427685781072334},{"term":"schepel","score":12.427685781072334},{"term":"planvorm","score":12.427685781072334},{"term":"schuijlenburg","score":12.427685781072334},{"term":"sensetek","score":12.427685781072334},{"term":"] – [","score":12.427685781072334},{"term":"prestatiebeur","score":12.427685781072334},{"term":"inforsa","score":12.418296040722495},{"term":"sappi","score":12.418296040722495},{"term":"verbrijzeld","score":12.418296040722495},{"term":"procedurevoorschrift","score":12.418296040722495},{"term":"meldster","score":12.418296040722495},{"term":"ameland","score":12.418296040722495},{"term":"ondersplits","score":12.418296040722495},{"term":"reusel","score":12.408993648060182},{"term":"schedelbreuk","score":12.408993648060182},{"term":"dolc","score":12.408993648060182},{"term":"sui","score":12.408993648060182},{"term":"generis","score":12.408993648060182},{"term":"pech","score":12.408993648060182},{"term":"onbehag","score":12.408993648060182},{"term":"aanbrengst","score":12.408993648060182},{"term":"characterized","score":12.408993648060182},{"term":"zwaard","score":12.408993648060182},{"term":"rddf","score":12.408993648060182},{"term":"schoonhoud","score":12.399776992955259},{"term":"mandater","score":12.399776992955259},{"term":"duration","score":12.399776992955259},{"term":"gaskran","score":12.399776992955259},{"term":"gladheid","score":12.399776992955259},{"term":"telastegelegd","score":12.399776992955259},{"term":"terpstra","score":12.399776992955259},{"term":"gedachtenstreepj","score":12.399776992955259},{"term":"lv","score":12.399776992955259},{"term":"licenc","score":12.399776992955259},{"term":"fokker","score":12.399776992955259},{"term":"opstalhouder","score":12.399776992955259},{"term":"pensioenleeftijd","score":12.390644509391986},{"term":"outdor","score":12.390644509391986},{"term":"inkoopkost","score":12.390644509391986},{"term":"balanspost","score":12.390644509391986},{"term":"intellectual","score":12.390644509391986},{"term":"rodenburg","score":12.390644509391986},{"term":"beroepsaansprakelijkheidsverzeker","score":12.390644509391986},{"term":"marktsituatie","score":12.390644509391986},{"term":"adriaan","score":12.390644509391986},{"term":"€ €","score":12.390644509391986},{"term":"delictgevar","score":12.390644509391986},{"term":"wacc","score":12.390644509391986},{"term":"confrontaties","score":12.381594673872067},{"term":"alwin","score":12.381594673872067},{"term":"eva","score":12.381594673872067},{"term":"vita","score":12.381594673872067},{"term":"portabl","score":12.381594673872067},{"term":"afleveradres","score":12.381594673872067},{"term":", ,","score":12.381594673872067},{"term":"anderzorg","score":12.381594673872067},{"term":"hoofdpunt","score":12.381594673872067},{"term":"loonbegrip","score":12.381594673872067},{"term":"servisch","score":12.372626003889307},{"term":"sql","score":12.372626003889307},{"term":"loopafstand","score":12.372626003889307},{"term":"corporaties","score":12.372626003889307},{"term":"closed","score":12.372626003889307},{"term":"welt","score":12.372626003889307},{"term":"idr","score":12.372626003889307},{"term":"hennepplantages","score":12.363737056472061},{"term":"leeuwarder","score":12.363737056472061},{"term":"endstra","score":12.363737056472061},{"term":"proefprocedures","score":12.363737056472061},{"term":"loodgieter","score":12.363737056472061},{"term":"dinkelland","score":12.363737056472061},{"term":"worldwid","score":12.363737056472061},{"term":"vliegwiel","score":12.363737056472061},{"term":"sloopwerk","score":12.363737056472061},{"term":"stembiljet","score":12.363737056472061},{"term":"navorderingaanslag","score":12.363737056472061},{"term":"verwijzingsregister","score":12.354926426789907},{"term":"achtenveert","score":12.354926426789907},{"term":"runner","score":12.354926426789907},{"term":"imagoschad","score":12.354926426789907},{"term":"slotoverweg","score":12.354926426789907},{"term":"vijfentacht","score":12.346192746821151},{"term":"walcher","score":12.346192746821151},{"term":"- &","score":12.346192746821151},{"term":"stroomstor","score":12.346192746821151},{"term":"sound","score":12.346192746821151},{"term":"ontbindingsbesluit","score":12.346192746821151},{"term":"seaport","score":12.346192746821151},{"term":"nav","score":12.346192746821151},{"term":"paramaribo","score":12.346192746821151},{"term":"ss","score":12.337534684078037},{"term":"guaranted","score":12.337534684078037},{"term":"traver","score":12.337534684078037},{"term":"zero","score":12.337534684078037},{"term":"schoondochter","score":12.337534684078037},{"term":"oosterveld","score":12.328950940386646},{"term":"scheidingswand","score":12.328950940386646},{"term":"taiwan","score":12.328950940386646},{"term":"accountantscontrol","score":12.328950940386646},{"term":"faith","score":12.328950940386646},{"term":"nle","score":12.328950940386646},{"term":"middelharnis","score":12.328950940386646},{"term":"arg","score":12.328950940386646},{"term":"uitvaartkost","score":12.328950940386646},{"term":"donald","score":12.328950940386646},{"term":"noordoostpolder","score":12.328950940386646},{"term":"imsi","score":12.320440250718738},{"term":"cranendonck","score":12.320440250718738},{"term":"begripsbepal","score":12.320440250718738},{"term":"bovag","score":12.320440250718738},{"term":"methodebesluit","score":12.320440250718738},{"term":"oad","score":12.312001382072873},{"term":"compartiment","score":12.312001382072873},{"term":"begrijpelijkerwijs","score":12.312001382072873},{"term":"flanker","score":12.312001382072873},{"term":"wvv","score":12.312001382072873},{"term":"ù","score":12.312001382072873},{"term":"taakomschrijv","score":12.312001382072873},{"term":"kooij","score":12.312001382072873},{"term":"eindsaldo","score":12.312001382072873},{"term":"stanley","score":12.312001382072873},{"term":"handling","score":12.312001382072873},{"term":"timing","score":12.312001382072873},{"term":"omzetbereken","score":12.312001382072873},{"term":"ownership","score":12.312001382072873},{"term":"bouwinvest","score":12.312001382072873},{"term":"barst","score":12.312001382072873},{"term":"teva","score":12.303633132402355},{"term":"mand","score":12.303633132402355},{"term":"applicaties","score":12.303633132402355},{"term":"unlawful","score":12.303633132402355},{"term":"merkinschrijv","score":12.303633132402355},{"term":"lab","score":12.303633132402355},{"term":"witlox","score":12.303633132402355},{"term":"infiltratie","score":12.303633132402355},{"term":"employer","score":12.303633132402355},{"term":"subgunningscriteria","score":12.303633132402355},{"term":"herformuler","score":12.303633132402355},{"term":"doorbrekingsgrond","score":12.303633132402355},{"term":"tutsis","score":12.29533432958766},{"term":"exploitatieresultat","score":12.29533432958766},{"term":"cet","score":12.29533432958766},{"term":"governed","score":12.29533432958766},{"term":"dienstenverker","score":12.29533432958766},{"term":"iraq","score":12.29533432958766},{"term":"autoverhur","score":12.29533432958766},{"term":"wwz","score":12.29533432958766},{"term":"twintigduiz","score":12.287103830451146},{"term":"mededingingsautoriteit","score":12.287103830451146},{"term":"å","score":12.287103830451146},{"term":"oft","score":12.287103830451146},{"term":"kd","score":12.278940519811984},{"term":"lumc","score":12.278940519811984},{"term":"braun","score":12.278940519811984},{"term":"alexander","score":12.278940519811984},{"term":"relief","score":12.278940519811984},{"term":"banque","score":12.278940519811984},{"term":"ali","score":12.278940519811984},{"term":"kuijer","score":12.278940519811984},{"term":"representation","score":12.278940519811984},{"term":"blg","score":12.270843309579366},{"term":"newconomy","score":12.270843309579366},{"term":"ur","score":12.270843309579366},{"term":"personn","score":12.270843309579366},{"term":"geldermals","score":12.270843309579366},{"term":"sassenheim","score":12.270843309579366},{"term":"oosterhof","score":12.270843309579366},{"term":"meetbar","score":12.270843309579366},{"term":"verev","score":12.270843309579366},{"term":"creditsaldo","score":12.270843309579366},{"term":"[]","score":12.270843309579366},{"term":"renswoud","score":12.262811137882101},{"term":"meetresultat","score":12.262811137882101},{"term":"okt","score":12.262811137882101},{"term":"hague","score":12.262811137882101},{"term":"procesdeelnemer","score":12.262811137882101},{"term":"belastinginspecteur","score":12.262811137882101},{"term":"islam","score":12.262811137882101},{"term":"willing","score":12.262811137882101},{"term":"weerwoord","score":12.262811137882101},{"term":"bnt","score":12.254842968232923},{"term":"voorzitterschap","score":12.254842968232923},{"term":"pris","score":12.254842968232923},{"term":"betalingsvoorwaard","score":12.254842968232923},{"term":"liberec","score":12.254842968232923},{"term":"duplicat","score":12.254842968232923},{"term":"refugee","score":12.254842968232923},{"term":"verbalisant_NUM","score":12.24693778872581},{"term":"tonnageregim","score":12.24693778872581},{"term":"ambulatorium","score":12.24693778872581},{"term":"hoofdstrat","score":12.24693778872581},{"term":"uitwijz","score":12.24693778872581},{"term":"task","score":12.24693778872581},{"term":"schuldbekenteniss","score":12.24693778872581},{"term":"puntsgewijs","score":12.239094611264784},{"term":"erkenner","score":12.239094611264784},{"term":"rental","score":12.239094611264784},{"term":"inkomensgegev","score":12.239094611264784},{"term":"divers","score":12.239094611264784},{"term":"adviesdienst","score":12.239094611264784},{"term":"ictr","score":12.239094611264784},{"term":"danc","score":12.239094611264784},{"term":"_NUMq","score":12.239094611264784},{"term":"though","score":12.239094611264784},{"term":"brutosalaris","score":12.239094611264784},{"term":"bae","score":12.23131247082273},{"term":"certificates","score":12.23131247082273},{"term":"slagerij","score":12.23131247082273},{"term":"volkerwessel","score":12.23131247082273},{"term":"marktontwikkel","score":12.23131247082273},{"term":"weller","score":12.23131247082273},{"term":"gereedkom","score":12.223590424728819},{"term":"serviceflat","score":12.223590424728819},{"term":"assuranc","score":12.223590424728819},{"term":"cition","score":12.223590424728819},{"term":"waarderingsvoorschrift","score":12.223590424728819},{"term":"jurisdictie","score":12.21592755198325},{"term":"representativ","score":12.21592755198325},{"term":"grap","score":12.21592755198325},{"term":"geurbelast","score":12.21592755198325},{"term":"familial","score":12.21592755198325},{"term":"rekenregel","score":12.21592755198325},{"term":"oogart","score":12.21592755198325},{"term":"inactiviteit","score":12.21592755198325},{"term":"bladel","score":12.208322952598031},{"term":"ged","score":12.208322952598031},{"term":"gulf","score":12.208322952598031},{"term":"nder","score":12.208322952598031},{"term":"forecast","score":12.208322952598031},{"term":"volker","score":12.208322952598031},{"term":"zomed","score":12.208322952598031},{"term":"sloopvergunn","score":12.200775746962648},{"term":"campus","score":12.200775746962648},{"term":"terugkeergarantie","score":12.200775746962648},{"term":"kroati","score":12.200775746962648},{"term":"aug","score":12.200775746962648},{"term":"verwervingskost","score":12.200775746962648},{"term":"nidera","score":12.200775746962648},{"term":"bemiddelt","score":12.200775746962648},{"term":"sdc","score":12.200775746962648},{"term":"ambtenarenreglement","score":12.200775746962648},{"term":"kleijker","score":12.19328507523349},{"term":"brk","score":12.19328507523349},{"term":"maritiem","score":12.19328507523349},{"term":"hne","score":12.19328507523349},{"term":"aanving","score":12.19328507523349},{"term":"aib","score":12.185850096745973},{"term":"ei","score":12.185850096745973},{"term":"grafimedia","score":12.185850096745973},{"term":"[-]","score":12.185850096745973},{"term":"retailmarkt","score":12.185850096745973},{"term":"gordel","score":12.185850096745973},{"term":"cdr","score":12.17846998944835},{"term":"geert","score":12.17846998944835},{"term":"azi","score":12.17846998944835},{"term":"custody","score":12.17846998944835},{"term":"dakopbouw","score":12.17846998944835},{"term":"architectenbureau","score":12.17846998944835},{"term":"bergman","score":12.171143949356276},{"term":"panning","score":12.171143949356276},{"term":"borging","score":12.171143949356276},{"term":"workrelax","score":12.171143949356276},{"term":"paprika","score":12.171143949356276},{"term":"holman","score":12.171143949356276},{"term":"martinistad","score":12.163871190027198},{"term":"fe","score":12.163871190027198},{"term":"verzekeringstechnisch","score":12.163871190027198},{"term":"elektrotechniek","score":12.15665094205371},{"term":"images","score":12.15665094205371},{"term":"beginsaldo","score":12.15665094205371},{"term":"discount","score":12.149482452575098},{"term":"publication","score":12.149482452575098},{"term":"inhaald","score":12.149482452575098},{"term":"conclud","score":12.149482452575098},{"term":"janssen","score":12.149482452575098},{"term":"kogelgewer","score":12.149482452575098},{"term":"oorlogsmisdrijv","score":12.149482452575098},{"term":"kleinbedrijf","score":12.149482452575098},{"term":"environment","score":12.142364984806234},{"term":"persconferentie","score":12.142364984806234},{"term":"knijff","score":12.142364984806234},{"term":"brinkman","score":12.142364984806234},{"term":"declaratieformulier","score":12.142364984806234},{"term":"handelsnaamwet","score":12.13529781758314},{"term":"eemnes","score":12.13529781758314},{"term":"hos","score":12.13529781758314},{"term":"acceptanc","score":12.13529781758314},{"term":"exploitatiebijdrag","score":12.13529781758314},{"term":"[…] [","score":12.13529781758314},{"term":"rutger","score":12.13529781758314},{"term":"allee","score":12.13529781758314},{"term":"attribut","score":12.13529781758314},{"term":"fora","score":12.13529781758314},{"term":"weder","score":12.13529781758314},{"term":"maasdriel","score":12.13529781758314},{"term":"greenery","score":12.13529781758314},{"term":"cooper","score":12.128280244924495},{"term":"sandton","score":12.128280244924495},{"term":"venezuela","score":12.1213115756084},{"term":"merwed","score":12.1213115756084},{"term":"hangelbroek","score":12.1213115756084},{"term":"metterwon","score":12.1213115756084},{"term":"assuradeur","score":12.114391132763828},{"term":"undertak","score":12.114391132763828},{"term":"zorgboerderij","score":12.114391132763828},{"term":"apg","score":12.114391132763828},{"term":"ido","score":12.114391132763828},{"term":"gasthuis","score":12.114391132763828},{"term":"interbank","score":12.114391132763828},{"term":"pornografisch","score":12.114391132763828},{"term":"zeecontainer","score":12.107518253476066},{"term":"peildata","score":12.107518253476066},{"term":"reeuwijk","score":12.107518253476066},{"term":"], (…)","score":12.107518253476066},{"term":"incriminat","score":12.100692288405666},{"term":"automobielbedrijf","score":12.100692288405666},{"term":"handelsactiviteit","score":12.100692288405666},{"term":"brought","score":12.100692288405666},{"term":"mtm","score":12.100692288405666},{"term":"huisvredebreuk","score":12.100692288405666},{"term":"massagesalon","score":12.093912601420287},{"term":"dsv","score":12.087178569238942},{"term":"paribas","score":12.087178569238942},{"term":"logidex","score":12.087178569238942},{"term":"douanegebied","score":12.087178569238942},{"term":"executieopbrengst","score":12.087178569238942},{"term":"uitzendbed","score":12.087178569238942},{"term":"wif","score":12.087178569238942},{"term":"toeristisch","score":12.087178569238942},{"term":"*,","score":12.087178569238942},{"term":"cassatierechter","score":12.087178569238942},{"term":"residenc","score":12.080489581088147},{"term":"illustreert","score":12.080489581088147},{"term":"omzetdal","score":12.080489581088147},{"term":"rechtsfigur","score":12.080489581088147},{"term":"category","score":12.080489581088147},{"term":"expeditie","score":12.073845038369477},{"term":"vergelijkt","score":12.073845038369477},{"term":"mediq","score":12.073845038369477},{"term":"monarch","score":12.073845038369477},{"term":"_NUM__NUM","score":12.073845038369477},{"term":"gescand","score":12.067244354338126},{"term":"alg","score":12.067244354338126},{"term":"huvas","score":12.067244354338126},{"term":"duty","score":12.067244354338126},{"term":"autoverzeker","score":12.067244354338126},{"term":"herstelwet","score":12.067244354338126},{"term":"dagloonbesluit","score":12.067244354338126},{"term":"woordvoerder","score":12.067244354338126},{"term":"afvalcontainer","score":12.067244354338126},{"term":"achiev","score":12.067244354338126},{"term":"halfbroer","score":12.060686953791967},{"term":"rentekost","score":12.060686953791967},{"term":"ecn","score":12.060686953791967},{"term":"rendo","score":12.060686953791967},{"term":"ruling","score":12.060686953791967},{"term":"misselijk","score":12.054172272770773},{"term":"schmidt","score":12.054172272770773},{"term":"hoc","score":12.054172272770773},{"term":"bierbrouwerij","score":12.054172272770773},{"term":"morgan","score":12.054172272770773},{"term":"acceptabl","score":12.054172272770773},{"term":"gerechtsdeurwaarderskantor","score":12.054172272770773},{"term":"dep","score":12.047699758265155},{"term":"delivered","score":12.047699758265155},{"term":"possibility","score":12.047699758265155},{"term":"aed","score":12.047699758265155},{"term":"verblijfplaats","score":12.041268867934864},{"term":"koerselman","score":12.041268867934864},{"term":"stov","score":12.041268867934864},{"term":"_NUMr","score":12.041268867934864},{"term":"toetsingsmaatstaf","score":12.041268867934864},{"term":"schreur","score":12.034879069836094},{"term":"ark","score":12.034879069836094},{"term":"gezwaaid","score":12.034879069836094},{"term":"produktie","score":12.034879069836094},{"term":"atelier","score":12.034879069836094},{"term":"secretariaat","score":12.034879069836094},{"term":"vrijstellingsregel","score":12.028529842157434},{"term":"nootdorp","score":12.028529842157434},{"term":"weerbar","score":12.028529842157434},{"term":"oeganda","score":12.028529842157434},{"term":"steekpartij","score":12.028529842157434},{"term":"vijfjar","score":12.028529842157434},{"term":"regresrecht","score":12.02222067296417},{"term":"individualiser","score":12.02222067296417},{"term":"afslan","score":12.02222067296417},{"term":"winstbepal","score":12.02222067296417},{"term":"procesregel","score":12.02222067296417},{"term":"xvii","score":12.02222067296417},{"term":"koningin","score":12.015951059950575},{"term":"terugnam","score":12.015951059950575},{"term":") =","score":12.015951059950575},{"term":"aquaserva","score":12.015951059950575},{"term":"bekent","score":12.015951059950575},{"term":"clean","score":12.015951059950575},{"term":"penetratie","score":12.009720510199939},{"term":"schuldeiseres","score":12.009720510199939},{"term":"arnoldus","score":12.009720510199939},{"term":"amex","score":12.009720510199939},{"term":"entertainment","score":12.009720510199939},{"term":"naaldwijk","score":12.003528539952018},{"term":"broker","score":12.003528539952018},{"term":"afnameverplicht","score":12.003528539952018},{"term":"arena","score":12.003528539952018},{"term":"boeteclausul","score":12.003528539952018},{"term":"internal","score":12.003528539952018},{"term":"mecra","score":12.003528539952018},{"term":"] […]","score":12.003528539952018},{"term":"dockwis","score":12.003528539952018},{"term":"keuning","score":11.997374674377639},{"term":"europeesch","score":11.997374674377639},{"term":"bewijsgar","score":11.997374674377639},{"term":"verloond","score":11.997374674377639},{"term":"gam","score":11.997374674377639},{"term":"gun","score":11.997374674377639},{"term":"cleaning","score":11.997374674377639},{"term":"woonvisie","score":11.997374674377639},{"term":"huurauto","score":11.997374674377639},{"term":"huisdier","score":11.997374674377639},{"term":"schik","score":11.991258447360204},{"term":"supervisor","score":11.991258447360204},{"term":"sportcomplex","score":11.991258447360204},{"term":"geschillenbeslecht","score":11.991258447360204},{"term":"littl","score":11.98517940128382},{"term":"lingewaard","score":11.98517940128382},{"term":"writing","score":11.98517940128382},{"term":"schadeoorzak","score":11.979137086827858},{"term":"_NUMab","score":11.979137086827858},{"term":"onderzoeksmethod","score":11.979137086827858},{"term":"aanrecht","score":11.979137086827858},{"term":"albanes","score":11.979137086827858},{"term":"plint","score":11.973131062767647},{"term":"placed","score":11.973131062767647},{"term":"informatieregel","score":11.973131062767647},{"term":"wrakingsprocedur","score":11.973131062767647},{"term":"ingeschaald","score":11.973131062767647},{"term":"moslim","score":11.973131062767647},{"term":"louis","score":11.961226160261328},{"term":"bouwcombinatie","score":11.961226160261328},{"term":"neergestok","score":11.961226160261328},{"term":"neutraliteit","score":11.961226160261328},{"term":"plaatj","score":11.961226160261328},{"term":"moerdijk","score":11.95532643813414},{"term":"submitted","score":11.95532643813414},{"term":"prim","score":11.95532643813414},{"term":"dienstverleningsovereenkomst","score":11.95532643813414},{"term":"verdragsstat","score":11.95532643813414},{"term":"ô","score":11.949461318681742},{"term":"immediat","score":11.949461318681742},{"term":"maintenanc","score":11.949461318681742},{"term":"ondertekenar","score":11.949461318681742},{"term":"veg","score":11.949461318681742},{"term":"zittinghoud","score":11.949461318681742},{"term":"determination","score":11.94363039837095},{"term":"gauw","score":11.94363039837095},{"term":"vijfendert","score":11.94363039837095},{"term":"draagvlakmet","score":11.94363039837095},{"term":"afsplits","score":11.937833280686624},{"term":"wierd","score":11.937833280686624},{"term":"whiplash","score":11.937833280686624},{"term":"rijnvarendenverdrag","score":11.937833280686624},{"term":"crimes","score":11.932069575969873},{"term":"dela","score":11.932069575969873},{"term":"psycho","score":11.932069575969873},{"term":"jonkman","score":11.932069575969873},{"term":"bezwaarschriftprocedur","score":11.932069575969873},{"term":"aanslagtermijn","score":11.932069575969873},{"term":"arnold","score":11.932069575969873},{"term":"sloep","score":11.932069575969873},{"term":"beschikkingstell","score":11.932069575969873},{"term":"anc","score":11.926338901260888},{"term":"ommer","score":11.926338901260888},{"term":"rova","score":11.926338901260888},{"term":"switch","score":11.926338901260888},{"term":"veldhuis","score":11.926338901260888},{"term":"vidg","score":11.926338901260888},{"term":"wegbreng","score":11.92064088014625},{"term":"jorritsma","score":11.92064088014625},{"term":"weggevoerd","score":11.92064088014625},{"term":"verhel","score":11.92064088014625},{"term":"modelreglement","score":11.92064088014625},{"term":"beiderzijd","score":11.92064088014625},{"term":"huwelijkssluit","score":11.92064088014625},{"term":"tido","score":11.914975142610572},{"term":"bavaria","score":11.914975142610572},{"term":"geruisloz","score":11.914975142610572},{"term":"south","score":11.914975142610572},{"term":"heerd","score":11.914975142610572},{"term":"floraholland","score":11.914975142610572},{"term":"overkomt","score":11.914975142610572},{"term":"happy","score":11.914975142610572},{"term":"natuurmonument","score":11.914975142610572},{"term":"aanmaningskost","score":11.914975142610572},{"term":"telefoongid","score":11.914975142610572},{"term":"fundamental","score":11.914975142610572},{"term":"beslan","score":11.914975142610572},{"term":"beeldvorm","score":11.909341324892317},{"term":"chevron","score":11.909341324892317},{"term":"taxes","score":11.903739069343647},{"term":"dubieuz","score":11.903739069343647},{"term":"lemmer","score":11.903739069343647},{"term":"stuurgroep","score":11.903739069343647},{"term":"higher","score":11.903739069343647},{"term":"taxibedrijf","score":11.898168024294192},{"term":"rechtsingang","score":11.898168024294192},{"term":"read","score":11.892627843918577},{"term":"prijsverhog","score":11.887118188107607},{"term":"projectmanagement","score":11.887118188107607},{"term":"cop","score":11.887118188107607},{"term":"accurat","score":11.887118188107607},{"term":"echtscheidingsverzoek","score":11.887118188107607},{"term":"gemal","score":11.887118188107607},{"term":"witnesses","score":11.88163872234298},{"term":"rob","score":11.88163872234298},{"term":"progres","score":11.88163872234298},{"term":"interpretaties","score":11.88163872234298},{"term":"consequences","score":11.88163872234298},{"term":"hungary","score":11.876189117575416},{"term":"tbb","score":11.876189117575416},{"term":"stringent","score":11.876189117575416},{"term":"loyaliteit","score":11.870769050106077},{"term":"ller","score":11.870769050106077},{"term":"defini","score":11.870769050106077},{"term":"pf","score":11.870769050106077},{"term":"\"),","score":11.8653782014712},{"term":"waddinxven","score":11.8653782014712},{"term":"judicial","score":11.8653782014712},{"term":"ntachtig","score":11.8653782014712},{"term":"turn","score":11.8653782014712},{"term":"gebruteerd","score":11.8653782014712},{"term":"huwelijksvermogensregim","score":11.8653782014712},{"term":"middelst","score":11.860016258329814},{"term":"orionis","score":11.854682912354452},{"term":"overhen","score":11.854682912354452},{"term":"renteaftrek","score":11.854682912354452},{"term":"votech","score":11.854682912354452},{"term":"ais","score":11.84937786012476},{"term":"victim","score":11.84937786012476},{"term":"novec","score":11.84937786012476},{"term":"brigad","score":11.84937786012476},{"term":"duijn","score":11.84937786012476},{"term":"nagezond","score":11.84937786012476},{"term":"beoordelingscriteria","score":11.84937786012476},{"term":"nijhuis","score":11.844100803023915},{"term":"wcs","score":11.844100803023915},{"term":"zesendert","score":11.838851447137772},{"term":"protect","score":11.838851447137772},{"term":"recreatief","score":11.838851447137772},{"term":"continental","score":11.838851447137772},{"term":"kid","score":11.838851447137772},{"term":"grootboek","score":11.838851447137772},{"term":"vu","score":11.838851447137772},{"term":"tariefstell","score":11.838851447137772},{"term":"zorgplan","score":11.838851447137772},{"term":"schakelbewijs","score":11.83362950315662},{"term":"thuiskopie","score":11.83362950315662},{"term":"halt","score":11.83362950315662},{"term":"weggemaakt","score":11.828434686279516},{"term":"discrepanties","score":11.828434686279516},{"term":"scheepswerf","score":11.823266716121074},{"term":"pleegvader","score":11.823266716121074},{"term":"- ,","score":11.823266716121074},{"term":"ryanair","score":11.818125316620655},{"term":"beoefen","score":11.818125316620655},{"term":"kantooradres","score":11.818125316620655},{"term":"receipt","score":11.818125316620655},{"term":"gefourneerd","score":11.818125316620655},{"term":"schadevaststell","score":11.813010215953884},{"term":"agro","score":11.813010215953884},{"term":"levenspartner","score":11.813010215953884},{"term":"bao","score":11.807921146446413},{"term":"fuel","score":11.807921146446413},{"term":"programmatur","score":11.807921146446413},{"term":"overstapp","score":11.807921146446413},{"term":"nassau","score":11.802857844489866},{"term":"getankt","score":11.802857844489866},{"term":"eurosaf","score":11.802857844489866},{"term":"lefier","score":11.802857844489866},{"term":"sanoma","score":11.79782005045991},{"term":"dijker","score":11.792807508636365},{"term":"fgh","score":11.792807508636365},{"term":"oppak","score":11.787819967125326},{"term":"corneliss","score":11.787819967125326},{"term":"consumer","score":11.787819967125326},{"term":"abs","score":11.787819967125326},{"term":"psa","score":11.787819967125326},{"term":"laurentius","score":11.787819967125326},{"term":"pensioenoverzicht","score":11.787819967125326},{"term":"zoeking","score":11.787819967125326},{"term":"prosecution","score":11.782857177783198},{"term":"geheimhouder","score":11.782857177783198},{"term":"artikelsgewijz","score":11.782857177783198},{"term":"hall","score":11.782857177783198},{"term":"genetisch","score":11.782857177783198},{"term":"wev","score":11.782857177783198},{"term":"meerderheidsregel","score":11.782857177783198},{"term":"vertrouwen","score":11.782857177783198},{"term":"zlm","score":11.782857177783198},{"term":"lichtmast","score":11.777918896142614},{"term":"aanhangwagen","score":11.777918896142614},{"term":"laatstgenot","score":11.777918896142614},{"term":"uitleveringsverzoek","score":11.777918896142614},{"term":"veolia","score":11.777918896142614},{"term":"boekel","score":11.777918896142614},{"term":"involved","score":11.773004881340185},{"term":"deurwaarderskost","score":11.773004881340185},{"term":"pensioentoezegg","score":11.773004881340185},{"term":"referentieperiod","score":11.773004881340185},{"term":"esso","score":11.773004881340185},{"term":"attack","score":11.768114896045994},{"term":"fully","score":11.768114896045994},{"term":"kentekenhouder","score":11.768114896045994},{"term":"loonvorm","score":11.768114896045994},{"term":"doorverwijz","score":11.768114896045994},{"term":"meegeteld","score":11.768114896045994},{"term":"medtronic","score":11.76324870639482},{"term":"chronologisch","score":11.76324870639482},{"term":"werkvoorzien","score":11.758406081919032},{"term":"geluidhinder","score":11.753586795483084},{"term":"overlijdensrisicoverzeker","score":11.753586795483084},{"term":"wbm","score":11.753586795483084},{"term":"tussengekom","score":11.753586795483084},{"term":"lasthebber","score":11.753586795483084},{"term":"mts","score":11.748790623219591},{"term":"natuurterrein","score":11.748790623219591},{"term":"heusd","score":11.748790623219591},{"term":"pres","score":11.748790623219591},{"term":"situatieteken","score":11.748790623219591},{"term":"belfabriek","score":11.744017344466933},{"term":"uitkeringsfraud","score":11.744017344466933},{"term":"berekeningsmethod","score":11.744017344466933},{"term":"knock","score":11.744017344466933},{"term":"staphorst","score":11.739266741708335},{"term":"much","score":11.739266741708335},{"term":"waterker","score":11.739266741708335},{"term":"nobel","score":11.739266741708335},{"term":"bedrijfswaard","score":11.734538600512389},{"term":"funderingsherstel","score":11.734538600512389},{"term":"grant","score":11.734538600512389},{"term":"dalfs","score":11.729832709474977},{"term":"verstopp","score":11.729832709474977},{"term":"afgesplitst","score":11.729832709474977},{"term":"achttiend","score":11.729832709474977},{"term":"usa","score":11.729832709474977},{"term":"rbs","score":11.72514886016255},{"term":"stroomstot","score":11.72514886016255},{"term":"thereof","score":11.72514886016255},{"term":"huurprijsverminder","score":11.72514886016255},{"term":"spell","score":11.72514886016255},{"term":"resources","score":11.72514886016255},{"term":"steegh","score":11.720486847056739},{"term":"santander","score":11.720486847056739},{"term":"spyker","score":11.720486847056739},{"term":"actium","score":11.720486847056739},{"term":"interpretation","score":11.720486847056739},{"term":"aanvaardbaarheidstoet","score":11.720486847056739},{"term":"kortlop","score":11.720486847056739},{"term":"columbus","score":11.715846467500237},{"term":"gefietst","score":11.715846467500237},{"term":"guarantes","score":11.715846467500237},{"term":"nieuwsbrief","score":11.715846467500237},{"term":"swinkel","score":11.715846467500237},{"term":"kompas","score":11.711227521643941},{"term":"sloopkost","score":11.711227521643941},{"term":"beroepsonderwijs","score":11.711227521643941},{"term":"rivierenland","score":11.706629812395313},{"term":"eurolight","score":11.706629812395313},{"term":"transitievergoed","score":11.706629812395313},{"term":"snij","score":11.702053145367902},{"term":"sentenc","score":11.702053145367902},{"term":"vermogensvergelijk","score":11.702053145367902},{"term":"slagbom","score":11.702053145367902},{"term":"pijnacker","score":11.702053145367902},{"term":"? (…)","score":11.702053145367902},{"term":"fbi","score":11.702053145367902},{"term":"expedition","score":11.702053145367902},{"term":"pizza","score":11.697497328832041},{"term":"functiewijz","score":11.697497328832041},{"term":"sealbag","score":11.697497328832041},{"term":"meldingsgebod","score":11.692962173666649},{"term":"allianc","score":11.688447493312122},{"term":"grovelijk","score":11.688447493312122},{"term":"oisterwijk","score":11.688447493312122},{"term":"ni","score":11.688447493312122},{"term":"doorgeleid","score":11.688447493312122},{"term":"aktewissel","score":11.688447493312122},{"term":"anticipatie","score":11.688447493312122},{"term":"ant","score":11.683953103724283},{"term":"verkeersvoorschrift","score":11.683953103724283},{"term":"laser","score":11.683953103724283},{"term":"instruer","score":11.683953103724283},{"term":"vesta","score":11.679478823329362},{"term":"wentholt","score":11.679478823329362},{"term":"parkeervergunn","score":11.679478823329362},{"term":"beoordelingssystematiek","score":11.679478823329362},{"term":"search","score":11.675024472979981},{"term":"”(…)","score":11.670589875912116},{"term":"cater","score":11.670589875912116},{"term":"onderneemt","score":11.670589875912116},{"term":"adjunct","score":11.666174857703},{"term":"tuchtcolleg","score":11.666174857703},{"term":"hulpeloz","score":11.666174857703},{"term":"beroepsorganisatie","score":11.666174857703},{"term":"keuzemog","score":11.661779246229962},{"term":"generali","score":11.661779246229962},{"term":"bedrijfskost","score":11.661779246229962},{"term":"herwaarder","score":11.657402871630163},{"term":"becom","score":11.657402871630163},{"term":"vng","score":11.657402871630163},{"term":"dieetpreparat","score":11.657402871630163},{"term":"voorheff","score":11.657402871630163},{"term":"une","score":11.653045566261207},{"term":"] - [","score":11.653045566261207},{"term":"fishing","score":11.653045566261207},{"term":"ondergoed","score":11.648707164662609},{"term":"mst","score":11.648707164662609},{"term":"plaatsvervanger","score":11.648707164662609},{"term":"kapitaalstort","score":11.648707164662609},{"term":"associatie","score":11.648707164662609},{"term":"rusttijd","score":11.648707164662609},{"term":"kortingsregel","score":11.648707164662609},{"term":"afgesned","score":11.644387503518091},{"term":"honderdvijft","score":11.644387503518091},{"term":"welstandscommissie","score":11.644387503518091},{"term":"uitlop","score":11.644387503518091},{"term":"palm","score":11.640086421618701},{"term":"mazda","score":11.640086421618701},{"term":"sl","score":11.640086421618701},{"term":"voorwetenschap","score":11.640086421618701},{"term":"beuning","score":11.640086421618701},{"term":"scheven","score":11.640086421618701},{"term":"parol","score":11.640086421618701},{"term":"attero","score":11.640086421618701},{"term":"opzegt","score":11.640086421618701},{"term":"con","score":11.640086421618701},{"term":"gaspistol","score":11.6358037598267},{"term":"swis","score":11.6358037598267},{"term":"waterwet","score":11.6358037598267},{"term":"werkloz","score":11.6358037598267},{"term":"kredietvergoed","score":11.6358037598267},{"term":"wilhelminaplein","score":11.6358037598267},{"term":"nalatenschapp","score":11.631539361040243},{"term":"arag","score":11.631539361040243},{"term":"brok","score":11.627293070158792},{"term":"boedelschuld","score":11.627293070158792},{"term":"sorry","score":11.627293070158792},{"term":"mantelovereenkomst","score":11.62306473404927},{"term":"flex","score":11.62306473404927},{"term":"spelcomputer","score":11.62306473404927},{"term":"vites","score":11.62306473404927},{"term":"voorthuiz","score":11.62306473404927},{"term":"maaiveld","score":11.618854201512928},{"term":"wa","score":11.618854201512928},{"term":"beschermt","score":11.614661323252891},{"term":"betaalmiddel","score":11.614661323252891},{"term":"tvi","score":11.61048595184241},{"term":"lidl","score":11.606327941693747},{"term":"noort","score":11.606327941693747},{"term":"knottenbelt","score":11.606327941693747},{"term":"lineair","score":11.606327941693747},{"term":"ts","score":11.606327941693747},{"term":"beroepsuitoefen","score":11.602187149027715},{"term":"ventures","score":11.602187149027715},{"term":"afgew","score":11.602187149027715},{"term":"liability","score":11.602187149027715},{"term":"motorrijder","score":11.602187149027715},{"term":"groefsema","score":11.602187149027715},{"term":"bejaard","score":11.598063431843853},{"term":"contribution","score":11.598063431843853},{"term":"situer","score":11.5939566498912},{"term":"limit","score":11.5939566498912},{"term":"csu","score":11.5939566498912},{"term":"vlieger","score":11.589866664639676},{"term":"herstelvonnis","score":11.589866664639676},{"term":"beroepsopleid","score":11.589866664639676},{"term":"onderzoeksvrag","score":11.589866664639676},{"term":"aantrof","score":11.585793339252039},{"term":"samendruk","score":11.581736538556425},{"term":"gestreefd","score":11.581736538556425},{"term":"cursief","score":11.581736538556425},{"term":"lieshout","score":11.57769612901942},{"term":"mammoet","score":11.57769612901942},{"term":"ouderkop","score":11.57769612901942},{"term":"veiligheidsdienst","score":11.57769612901942},{"term":"dcc","score":11.573671978719695},{"term":"hut","score":11.569663957322156},{"term":"installatiebedrijf","score":11.565671936052619},{"term":"bt","score":11.565671936052619},{"term":"douaneregel","score":11.565671936052619},{"term":"compensatieregel","score":11.565671936052619},{"term":"schuldigverklar","score":11.565671936052619},{"term":"belastingvrij","score":11.561695787672978},{"term":"bnp","score":11.561695787672978},{"term":"leiderdorp","score":11.561695787672978},{"term":"mttm","score":11.557735386456882},{"term":"huisverbod","score":11.557735386456882},{"term":"losser","score":11.557735386456882},{"term":"edco","score":11.557735386456882},{"term":"liechtenstein","score":11.553790608165865},{"term":"bunnik","score":11.553790608165865},{"term":"insluit","score":11.553790608165865},{"term":"bob","score":11.553790608165865},{"term":"accused","score":11.553790608165865},{"term":"employee","score":11.553790608165865},{"term":"schedul","score":11.549861330025976},{"term":"/ [","score":11.549861330025976},{"term":"tog","score":11.549861330025976},{"term":"defam","score":11.549861330025976},{"term":"christelijk","score":11.549861330025976},{"term":"unilever","score":11.545947430704839},{"term":"steekproef","score":11.545947430704839},{"term":"voorgezet","score":11.545947430704839},{"term":"turbo","score":11.545947430704839},{"term":"tgo","score":11.545947430704839},{"term":"moederszijd","score":11.545947430704839},{"term":"offshor","score":11.542048790289183},{"term":"afvalstof","score":11.542048790289183},{"term":"prijspeil","score":11.542048790289183},{"term":"erasmus","score":11.542048790289183},{"term":"refer","score":11.538165290262784},{"term":"groepsmaatschappij","score":11.534296813484865},{"term":"zinn","score":11.534296813484865},{"term":"akzo","score":11.534296813484865},{"term":"mio","score":11.530443244168874},{"term":"ew","score":11.530443244168874},{"term":"eb","score":11.530443244168874},{"term":"middelpunt","score":11.530443244168874},{"term":"financieringskost","score":11.52660446786171},{"term":"heter","score":11.52660446786171},{"term":"pakjes","score":11.52660446786171},{"term":"tr","score":11.52660446786171},{"term":"klerk","score":11.52660446786171},{"term":"draaiend","score":11.522780371423305},{"term":"hypotheekaanvrag","score":11.518970843006638},{"term":"onderhoudstoestand","score":11.518970843006638},{"term":"gevang","score":11.518970843006638},{"term":"ntenrad","score":11.515175772038086},{"term":"ethyl","score":11.515175772038086},{"term":"boulevard","score":11.515175772038086},{"term":"omring","score":11.515175772038086},{"term":"jbra","score":11.515175772038086},{"term":"motiveringseis","score":11.515175772038086},{"term":"mourik","score":11.51139504919818},{"term":"typisch","score":11.51139504919818},{"term":"behalf","score":11.51139504919818},{"term":"geannuleerd","score":11.51139504919818},{"term":"weid","score":11.51139504919818},{"term":"mailverker","score":11.507628566402703},{"term":"committed","score":11.503876216784152},{"term":"zorgautoriteit","score":11.503876216784152},{"term":"requested","score":11.503876216784152},{"term":"waardeontwikkel","score":11.503876216784152},{"term":"effectenreken","score":11.500137894673545},{"term":"cif","score":11.500137894673545},{"term":"breach","score":11.500137894673545},{"term":"linkerog","score":11.496413495582562},{"term":"requir","score":11.496413495582562},{"term":"kier","score":11.496413495582562},{"term":"arbeidstijd","score":11.496413495582562},{"term":"kruidvat","score":11.489006054304701},{"term":"groenvoorzien","score":11.489006054304701},{"term":"belastingverorden","score":11.489006054304701},{"term":"steenwijkerland","score":11.489006054304701},{"term":")[","score":11.481653079999441},{"term":"applied","score":11.481653079999441},{"term":"veluw","score":11.477996768796332},{"term":"arbeidsomstand","score":11.477996768796332},{"term":"weegschal","score":11.477996768796332},{"term":"boetegrondslag","score":11.47435377751783},{"term":"rubber","score":11.47435377751783},{"term":"uitstelverzoek","score":11.47435377751783},{"term":"bont","score":11.47435377751783},{"term":"metselwerk","score":11.47435377751783},{"term":"beloond","score":11.470724009467252},{"term":"hh","score":11.470724009467252},{"term":"bouwplat","score":11.470724009467252},{"term":"schet","score":11.470724009467252},{"term":"afgeperst","score":11.470724009467252},{"term":"ez","score":11.470724009467252},{"term":"slip","score":11.470724009467252},{"term":"confronter","score":11.467107368997063},{"term":"immediately","score":11.467107368997063},{"term":"koersgevoel","score":11.467107368997063},{"term":"vacu","score":11.467107368997063},{"term":"orang","score":11.467107368997063},{"term":"tweehonderdveert","score":11.467107368997063},{"term":"olthof","score":11.467107368997063},{"term":"voetbalclub","score":11.463503761493765},{"term":"tijdsbested","score":11.463503761493765},{"term":"rijksrecherch","score":11.459913093363037},{"term":"betalingsproblem","score":11.459913093363037},{"term":"hyundai","score":11.459913093363037},{"term":"bouwvlak","score":11.459913093363037},{"term":"copd","score":11.459913093363037},{"term":"banksaldi","score":11.456335272015153},{"term":"eerd","score":11.456335272015153},{"term":"supplier","score":11.456335272015153},{"term":"described","score":11.456335272015153},{"term":"gal","score":11.456335272015153},{"term":"opt","score":11.452770205850657},{"term":"treaty","score":11.449217804246288},{"term":"mo","score":11.445677977541164},{"term":"trainer","score":11.445677977541164},{"term":"iriszorg","score":11.442150637023197},{"term":"karakteristiek","score":11.442150637023197},{"term":"havenbedrijf","score":11.442150637023197},{"term":"wevab","score":11.438635694915751},{"term":"goudkleur","score":11.431642659424782},{"term":"bezighoud","score":11.428164395048455},{"term":"binckbank","score":11.428164395048455},{"term":"ioaw","score":11.428164395048455},{"term":"certificaathouder","score":11.42469818707197},{"term":"nijverdal","score":11.42469818707197},{"term":"bm","score":11.42469818707197},{"term":"balansdatum","score":11.42469818707197},{"term":"woonpunt","score":11.421243952203882},{"term":"fpc","score":11.421243952203882},{"term":"boeterent","score":11.421243952203882},{"term":"emvi","score":11.421243952203882},{"term":"port","score":11.421243952203882},{"term":"denkbeeld","score":11.41780160801291},{"term":"gevangenhoud","score":11.410952266167335},{"term":"naties","score":11.410952266167335},{"term":"ontspann","score":11.410952266167335},{"term":"bijleveld","score":11.40754510784572},{"term":"pleegdatum","score":11.40754510784572},{"term":"maneg","score":11.404149518844582},{"term":"aben","score":11.404149518844582},{"term":"winschot","score":11.404149518844582},{"term":"opgeslot","score":11.404149518844582},{"term":"opgan","score":11.404149518844582},{"term":"plc","score":11.400765420860342},{"term":"yard","score":11.400765420860342},{"term":"ou","score":11.400765420860342},{"term":"colombia","score":11.400765420860342},{"term":"directly","score":11.400765420860342},{"term":"medium","score":11.397392736381702},{"term":"voorzorg","score":11.394031388678997},{"term":"rva","score":11.394031388678997},{"term":"uitvoeringsverorden","score":11.390681301793716},{"term":"reiskostenvergoed","score":11.390681301793716},{"term":"woonzorg","score":11.390681301793716},{"term":"fraus","score":11.390681301793716},{"term":"anwb","score":11.387342400528201},{"term":"spectrum","score":11.387342400528201},{"term":"innovatie","score":11.387342400528201},{"term":"relationship","score":11.384014610435527},{"term":"schoonmaakbedrijf","score":11.384014610435527},{"term":"doodgeschot","score":11.380697857809531},{"term":"bil","score":11.380697857809531},{"term":"uitgeleverd","score":11.380697857809531},{"term":"gerrit","score":11.377392069675032},{"term":"inboedelgoeder","score":11.377392069675032},{"term":"minimumeis","score":11.377392069675032},{"term":"geweldsincident","score":11.37409717377818},{"term":"produceert","score":11.37081309857699},{"term":"discriminer","score":11.37081309857699},{"term":"waterstat","score":11.37081309857699},{"term":"_NUMv","score":11.37081309857699},{"term":"koerslijst","score":11.367539773232021},{"term":"litouw","score":11.367539773232021},{"term":"doorzend","score":11.367539773232021},{"term":"schijndel","score":11.367539773232021},{"term":"ng","score":11.364277127597205},{"term":"overgespaard","score":11.364277127597205},{"term":"bodemged","score":11.364277127597205},{"term":"kredietverlen","score":11.361025092210827},{"term":"arrangement","score":11.357783598286657},{"term":"wetstoepass","score":11.357783598286657},{"term":"ambacht","score":11.357783598286657},{"term":"gem","score":11.357783598286657},{"term":"veiligheidsrisico","score":11.35455257770521},{"term":"ricoh","score":11.35455257770521},{"term":"gedelegeerd","score":11.35455257770521},{"term":"user","score":11.35455257770521},{"term":"vr","score":11.35455257770521},{"term":"vereff","score":11.35455257770521},{"term":"verbaasd","score":11.351331963005167},{"term":"expres","score":11.348121687374919},{"term":"egypt","score":11.348121687374919},{"term":"kong","score":11.348121687374919},{"term":"bootsma","score":11.348121687374919},{"term":"wsj","score":11.344921684644248},{"term":"defenc","score":11.341731889276149},{"term":"hoed","score":11.341731889276149},{"term":"determin","score":11.341731889276149},{"term":"verplichtstell","score":11.341731889276149},{"term":"writ","score":11.338552236358769},{"term":"getrouw","score":11.338552236358769},{"term":"daglicht","score":11.338552236358769},{"term":"gedrang","score":11.33538266159749},{"term":"ethanol","score":11.33538266159749},{"term":"oppervlaktewater","score":11.33538266159749},{"term":"ell","score":11.33538266159749},{"term":"weerlos","score":11.332223101307122},{"term":"ndertig","score":11.332223101307122},{"term":"employment","score":11.332223101307122},{"term":"koopoptie","score":11.332223101307122},{"term":"……","score":11.329073492404225},{"term":"daderschap","score":11.329073492404225},{"term":"xv","score":11.329073492404225},{"term":"frauduleus","score":11.329073492404225},{"term":"beroepszak","score":11.329073492404225},{"term":"mix","score":11.325933772399557},{"term":"gerrits","score":11.325933772399557},{"term":"warenautoriteit","score":11.325933772399557},{"term":"concentrer","score":11.325933772399557},{"term":"forfait","score":11.325933772399557},{"term":"litteken","score":11.32280387939063},{"term":"steekt","score":11.32280387939063},{"term":"vrijgav","score":11.32280387939063},{"term":"bedrijfshal","score":11.32280387939063},{"term":"schuldhulpverlen","score":11.319683752054386},{"term":"consortium","score":11.319683752054386},{"term":"onderbreng","score":11.319683752054386},{"term":"industrieterrein","score":11.316573329639994},{"term":"bespaard","score":11.316573329639994},{"term":"fenomen","score":11.313472551961745},{"term":"legis","score":11.313472551961745},{"term":"ontbindingsprocedur","score":11.310381359392073},{"term":"nevenvoorzien","score":11.310381359392073},{"term":"energetisch","score":11.310381359392073},{"term":"agri","score":11.307299692854665},{"term":"kinderpornografie","score":11.301164704287148},{"term":"_NUMbd","score":11.301164704287148},{"term":"aalsmer","score":11.301164704287148},{"term":"waterland","score":11.298111266800259},{"term":"hb","score":11.298111266800259},{"term":"administratiefrecht","score":11.298111266800259},{"term":"onderaannem","score":11.298111266800259},{"term":"bouwman","score":11.298111266800259},{"term":"duim","score":11.298111266800259},{"term":"vooruitgang","score":11.29506712441903},{"term":"dental","score":11.29506712441903},{"term":"contactgegeven","score":11.29506712441903},{"term":"academisch","score":11.292032220723875},{"term":"sg","score":11.292032220723875},{"term":"sd","score":11.292032220723875},{"term":"diensttijd","score":11.28900649980734},{"term":"pachtkamer","score":11.28900649980734},{"term":"centr","score":11.285989906267913},{"term":"cassatieprocedur","score":11.285989906267913},{"term":"otherwis","score":11.285989906267913},{"term":"geschoond","score":11.282982385203958},{"term":"bog","score":11.282982385203958},{"term":"equality","score":11.282982385203958},{"term":"aivd","score":11.279983882207702},{"term":"vereenvoud","score":11.279983882207702},{"term":"zevenar","score":11.276994343359336},{"term":"construction","score":11.276994343359336},{"term":"openstell","score":11.276994343359336},{"term":"schadebeperkingsplicht","score":11.276994343359336},{"term":"vergoedingsrecht","score":11.274013715221198},{"term":"bedrijfstak","score":11.27104194483204},{"term":"nabur","score":11.268078979701384},{"term":"sporttas","score":11.268078979701384},{"term":"bebloed","score":11.268078979701384},{"term":"oppas","score":11.268078979701384},{"term":"stadgenot","score":11.265124767803952},{"term":"forward","score":11.265124767803952},{"term":"february","score":11.259242397900884},{"term":"research","score":11.259242397900884},{"term":"investigation","score":11.256314138121796},{"term":"bouwverorden","score":11.256314138121796},{"term":"leveranties","score":11.253394428018462},{"term":"rap","score":11.250483217811004},{"term":"rata","score":11.250483217811004},{"term":"geseponeerd","score":11.250483217811004},{"term":"fictie","score":11.247580458153042},{"term":"lief","score":11.247580458153042},{"term":"(-","score":11.244686100126678},{"term":"ahold","score":11.241800095237544},{"term":"pleitbar","score":11.241800095237544},{"term":"“-","score":11.241800095237544},{"term":"wilsgebrek","score":11.241800095237544},{"term":"courantverhoud","score":11.241800095237544},{"term":"statistisch","score":11.238922395409928},{"term":"uitlok","score":11.236052952981975},{"term":"bo","score":11.236052952981975},{"term":"isolatie","score":11.236052952981975},{"term":"korpsbeheerder","score":11.233191720700942},{"term":"nissan","score":11.230338651718537},{"term":"rwanda","score":11.224656818251106},{"term":"_NUMm_NUM","score":11.219007085708986},{"term":"afgekocht","score":11.219007085708986},{"term":"geknipt","score":11.216194144332372},{"term":"randstad","score":11.216194144332372},{"term":"sofinummer","score":11.213389093404762},{"term":"botsend","score":11.213389093404762},{"term":"teweegbreng","score":11.213389093404762},{"term":"justic","score":11.213389093404762},{"term":"zestal","score":11.207802486696123},{"term":"verkeerssituatie","score":11.205020843734246},{"term":"lock","score":11.205020843734246},{"term":"cursuss","score":11.205020843734246},{"term":"verbek","score":11.202246916851522},{"term":"ecuador","score":11.202246916851522},{"term":"houdstermaatschappij","score":11.202246916851522},{"term":"fall","score":11.199480663358631},{"term":"vacatures","score":11.199480663358631},{"term":"technologie","score":11.199480663358631},{"term":"latent","score":11.199480663358631},{"term":"verpleeghuis","score":11.199480663358631},{"term":"elfd","score":11.199480663358631},{"term":"supply","score":11.196722040919552},{"term":"tienduiz","score":11.193971007547662},{"term":"leerplichtwet","score":11.193971007547662},{"term":"adstructie","score":11.193971007547662},{"term":"gestoord","score":11.193971007547662},{"term":"]”)","score":11.193971007547662},{"term":"duiding","score":11.193971007547662},{"term":"herman","score":11.19122752160191},{"term":"mark","score":11.19122752160191},{"term":"doorverwez","score":11.19122752160191},{"term":"financieringsvoorbehoud","score":11.188491541783035},{"term":"dijkstra","score":11.18304193701547},{"term":"omgangshuis","score":11.18304193701547},{"term":"interieur","score":11.18304193701547},{"term":"verificatievergader","score":11.177621869546131},{"term":"afvalstoffenheff","score":11.177621869546131},{"term":"begrav","score":11.174922812576966},{"term":"informatief","score":11.174922812576966},{"term":"onbeantwoord","score":11.174922812576966},{"term":"jur","score":11.172231020911255},{"term":"vervaldatum","score":11.172231020911255},{"term":"trailer","score":11.169546455540587},{"term":"zout","score":11.169546455540587},{"term":"samenlevingsovereenkomst","score":11.169546455540587},{"term":"common","score":11.16686907776987},{"term":"seller","score":11.16686907776987},{"term":"naam_NUM","score":11.16419884921399},{"term":"koevoet","score":11.16419884921399},{"term":"agentschap","score":11.16419884921399},{"term":"srk","score":11.158879687736391},{"term":"=)","score":11.158879687736391},{"term":"dominican","score":11.153588670101975},{"term":"veiligstell","score":11.153588670101975},{"term":"activities","score":11.15095362246397},{"term":"regulation","score":11.15095362246397},{"term":"beschermingsbewind","score":11.15095362246397},{"term":"schuldeisersverzuim","score":11.148325500057702},{"term":"bovenwet","score":11.148325500057702},{"term":"verrekenbed","score":11.145704266577827},{"term":"eijk","score":11.145704266577827},{"term":"dato","score":11.137881540896618},{"term":"geschaard","score":11.137881540896618},{"term":"apply","score":11.135287505719571},{"term":"tekortgedan","score":11.13270018215462},{"term":"groet","score":11.13270018215462},{"term":"sleutelbos","score":11.13270018215462},{"term":"ajax","score":11.130119535561128},{"term":"dem","score":11.130119535561128},{"term":"boodschap","score":11.130119535561128},{"term":"dublin","score":11.127545531565955},{"term":"stilstaand","score":11.12497813606071},{"term":"achterstell","score":11.12497813606071},{"term":"therapeutisch","score":11.12497813606071},{"term":"beleggingsreken","score":11.122417315199035},{"term":"overbrugg","score":11.122417315199035},{"term":"inwend","score":11.122417315199035},{"term":"kraker","score":11.114773965886467},{"term":"alcoholslotprogramma","score":11.114773965886467},{"term":"profit","score":11.114773965886467},{"term":"douanewetboek","score":11.114773965886467},{"term":"coulanc","score":11.114773965886467},{"term":"kist","score":11.11223911028328},{"term":"integrer","score":11.107188594497211},{"term":"drukkend","score":11.102163458294537},{"term":"omega","score":11.097163447877833},{"term":"bouwkavel","score":11.094672786565381},{"term":"doodsoorzak","score":11.089709997223252},{"term":"geschokt","score":11.087237808077862},{"term":"slang","score":11.087237808077862},{"term":"schadeclaim","score":11.087237808077862},{"term":"citro","score":11.084771715582669},{"term":"lbio","score":11.07985770078024},{"term":"belastingplan","score":11.07985770078024},{"term":"barp","score":11.0774097191416},{"term":"diligenc","score":11.074967715486048},{"term":"chatgesprek","score":11.072531660688167},{"term":"perfect","score":11.072531660688167},{"term":"onkostenvergoed","score":11.072531660688167},{"term":"klemmend","score":11.070101525834875},{"term":"bemann","score":11.070101525834875},{"term":"warmt","score":11.06767728222337},{"term":"wko","score":11.065258901359087},{"term":"commandant","score":11.065258901359087},{"term":"kwijtgeraakt","score":11.062846354953704},{"term":"im","score":11.060439614923139},{"term":"elektriciteitsmeter","score":11.060439614923139},{"term":"achterst","score":11.0580386533856},{"term":"conceptrapport","score":11.0580386533856},{"term":"bre","score":11.0580386533856},{"term":"appelschriftur","score":11.055643442659646},{"term":"sky","score":11.053253955262264},{"term":"dong","score":11.053253955262264},{"term":"portal","score":11.053253955262264},{"term":"bart","score":11.050870163906987},{"term":"polisblad","score":11.04611956114839},{"term":"servatius","score":11.043752696138124},{"term":"vermiss","score":11.043752696138124},{"term":"verouderd","score":11.043752696138124},{"term":"emmer","score":11.041391419952443},{"term":"deelnemingsvrijstell","score":11.039035706259986},{"term":"handelspraktijk","score":11.039035706259986},{"term":"draagwijdt","score":11.039035706259986},{"term":"alfa","score":11.036685528915031},{"term":"hoekstra","score":11.034340861955778},{"term":"factuurdatum","score":11.034340861955778},{"term":"hollander","score":11.032001679602605},{"term":"gehet","score":11.032001679602605},{"term":"compound","score":11.032001679602605},{"term":"valkenswaard","score":11.029667956256384},{"term":"descent","score":11.029667956256384},{"term":"wanbeleid","score":11.029667956256384},{"term":"gunningscriteria","score":11.027339666496793},{"term":"kinderdagverblijf","score":11.025016785080654},{"term":"conversie","score":11.022699286940291},{"term":"schenk","score":11.022699286940291},{"term":"overleveringswet","score":11.022699286940291},{"term":"vee","score":11.020387147181912},{"term":"vijfentwint","score":11.020387147181912},{"term":") (…)","score":11.015778844095717},{"term":"rusland","score":11.015778844095717},{"term":"skyp","score":11.013482631835368},{"term":"drieduiz","score":11.013482631835368},{"term":"zijdel","score":11.008905964807957},{"term":"euribor","score":11.008905964807957},{"term":"ce","score":11.008905964807957},{"term":"optierecht","score":11.008905964807957},{"term":"wsg","score":11.008905964807957},{"term":"authenticiteit","score":11.00662546210923},{"term":"toepassingsgebied","score":11.004350148272096},{"term":"vrijval","score":11.004350148272096},{"term":"performanc","score":11.002079999737555},{"term":"horend","score":11.002079999737555},{"term":"lak","score":11.002079999737555},{"term":"pleegzorg","score":11.002079999737555},{"term":"stijg","score":11.002079999737555},{"term":"gelder","score":10.999814993106703},{"term":"vakantiewon","score":10.999814993106703},{"term":"beslat","score":10.997555105139266},{"term":"kroongetu","score":10.997555105139266},{"term":"hofuitsprak","score":10.995300312752176},{"term":"jaarverslag","score":10.995300312752176},{"term":"blind","score":10.984101987442148},{"term":"straffelos","score":10.984101987442148},{"term":"somalisch","score":10.984101987442148},{"term":"fruit","score":10.984101987442148},{"term":"verzekeringsplicht","score":10.981877292420036},{"term":"tegengan","score":10.981877292420036},{"term":"ontzegt","score":10.981877292420036},{"term":"invorderingsrent","score":10.981877292420036},{"term":"dagvaardingskost","score":10.981877292420036},{"term":"japan","score":10.97744269535217},{"term":"driehonderd","score":10.97744269535217},{"term":"oppervlak","score":10.975232749701368},{"term":"bestrat","score":10.975232749701368},{"term":"ziggo","score":10.975232749701368},{"term":"weggedacht","score":10.975232749701368},{"term":"bodemverontrein","score":10.973027677143055},{"term":"speelautomat","score":10.973027677143055},{"term":"zur","score":10.970827456233451},{"term":"berok","score":10.970827456233451},{"term":"principl","score":10.968632065670016},{"term":"touw","score":10.968632065670016},{"term":"jeugdstrafrecht","score":10.966441484290197},{"term":"subsidieverlen","score":10.964255691070218},{"term":"herstelexplot","score":10.964255691070218},{"term":"verzetprocedur","score":10.964255691070218},{"term":"vlaarding","score":10.962074665123856},{"term":"distributeur","score":10.962074665123856},{"term":"get","score":10.962074665123856},{"term":"bbl","score":10.962074665123856},{"term":"toeslagrecht","score":10.962074665123856},{"term":"valkenburg","score":10.959898385701262},{"term":"gedachtegang","score":10.959898385701262},{"term":"[(","score":10.959898385701262},{"term":"fairstar","score":10.957726832187753},{"term":"shipping","score":10.955559984102663},{"term":"opbrengstlimiet","score":10.953397821098168},{"term":"int","score":10.953397821098168},{"term":"sedertdien","score":10.951240322958146},{"term":"fikker","score":10.951240322958146},{"term":"landbouwbedrijf","score":10.949087469597046},{"term":"onkost","score":10.949087469597046},{"term":"porsch","score":10.949087469597046},{"term":"geopenbaard","score":10.944795617515505},{"term":"wuv","score":10.944795617515505},{"term":"arbeidsinspectie","score":10.942656579266755},{"term":"zilverkleur","score":10.942656579266755},{"term":"@","score":10.940522106738122},{"term":"principi","score":10.938392180480298},{"term":"dhl","score":10.936266781167983},{"term":"mutatis","score":10.936266781167983},{"term":"zeventien","score":10.934145889598847},{"term":"onderhuurder","score":10.934145889598847},{"term":"mutandis","score":10.93202948669247},{"term":"premieheff","score":10.93202948669247},{"term":"burgerrecht","score":10.93202948669247},{"term":"overlegt","score":10.92781007114976},{"term":"noorweg","score":10.92781007114976},{"term":"cr","score":10.92781007114976},{"term":"overlop","score":10.923608384296061},{"term":"beknopt","score":10.91525760507864},{"term":"werf","score":10.91525760507864},{"term":"mierlo","score":10.91525760507864},{"term":"telefoontoestel","score":10.91525760507864},{"term":"companies","score":10.91525760507864},{"term":"voorkant","score":10.91110822153183},{"term":"oppositie","score":10.90903996846777},{"term":"successie","score":10.904916251283908},{"term":"section","score":10.902860752101812},{"term":"filmfragment","score":10.902860752101812},{"term":"hechting","score":10.902860752101812},{"term":"acer","score":10.900809469331255},{"term":"rooij","score":10.900809469331255},{"term":"debet","score":10.89876238570953},{"term":"fte","score":10.89876238570953},{"term":"gedraagt","score":10.89671948407973},{"term":"vink","score":10.892646158692093},{"term":"bandbreedt","score":10.892646158692093},{"term":"kantin","score":10.892646158692093},{"term":"rechtsvorm","score":10.892646158692093},{"term":"goederenrecht","score":10.890615701141712},{"term":"verzak","score":10.88858935799648},{"term":"mc","score":10.886567112615712},{"term":"msn","score":10.884548948459475},{"term":"vervolgt","score":10.884548948459475},{"term":"vuilniszak","score":10.884548948459475},{"term":"arbeidsur","score":10.878518779432882},{"term":"don","score":10.87651677676221},{"term":"fit","score":10.87651677676221},{"term":"circumstances","score":10.874518774099537},{"term":"wed","score":10.874518774099537},{"term":"tros","score":10.874518774099537},{"term":"martinair","score":10.870534705084662},{"term":"gemerkt","score":10.868548607113034},{"term":"leugenacht","score":10.868548607113034},{"term":"mentioned","score":10.866566445909042},{"term":"rechterarm","score":10.866566445909042},{"term":"referred","score":10.862613871593219},{"term":"jbz","score":10.862613871593219},{"term":",-),","score":10.86064342760592},{"term":"bovenwon","score":10.86064342760592},{"term":"bol","score":10.86064342760592},{"term":"tennet","score":10.86064342760592},{"term":"voegingsformulier","score":10.86064342760592},{"term":"dsw","score":10.86064342760592},{"term":"heup","score":10.85867685863388},{"term":"onderbrok","score":10.85671414946603},{"term":"ever","score":10.85671414946603},{"term":"ongedaanmak","score":10.85671414946603},{"term":"ground","score":10.850849030013633},{"term":"oprichter","score":10.850849030013633},{"term":"teruggeleid","score":10.846957974520667},{"term":"ingehaald","score":10.846957974520667},{"term":"glencor","score":10.845018109702838},{"term":"verwant","score":10.845018109702838},{"term":"bedrijfsterrein","score":10.845018109702838},{"term":"maaltijd","score":10.845018109702838},{"term":"geest","score":10.845018109702838},{"term":"beland","score":10.843082000675972},{"term":"incomplet","score":10.843082000675972},{"term":"(\"","score":10.839220992018513},{"term":"steiger","score":10.839220992018513},{"term":"bestuurslid","score":10.839220992018513},{"term":"voorburg","score":10.837296063608928},{"term":"geneesher","score":10.837296063608928},{"term":"loonkost","score":10.837296063608928},{"term":"veertiend","score":10.837296063608928},{"term":"verdedigt","score":10.835374833431034},{"term":"]’,","score":10.835374833431034},{"term":"exam","score":10.833457287301764},{"term":"tom","score":10.833457287301764},{"term":"volgnummer","score":10.83154341111948},{"term":"onderzoeksverslag","score":10.83154341111948},{"term":"ongemotiveerd","score":10.82963319086336},{"term":"lj","score":10.82963319086336},{"term":"vruchtgebruik","score":10.82963319086336},{"term":"neurologisch","score":10.82963319086336},{"term":"verwezen","score":10.82963319086336},{"term":"vorming","score":10.82963319086336},{"term":"upc","score":10.827726612592778},{"term":"echtelied","score":10.827726612592778},{"term":"domicilie","score":10.825823662446693},{"term":"india","score":10.82392432664304},{"term":"indexatie","score":10.82202859147814},{"term":"fijn","score":10.82202859147814},{"term":"paid","score":10.820136443326103},{"term":"herbereken","score":10.818247868638235},{"term":"executiegeschil","score":10.818247868638235},{"term":"uitstaand","score":10.816362853942463},{"term":"strafbeschik","score":10.812603451018557},{"term":"huid","score":10.810729036224206},{"term":"panel","score":10.810729036224206},{"term":"klachtplicht","score":10.805126780675538},{"term":"liv","score":10.805126780675538},{"term":"pathologie","score":10.803266315022617},{"term":"small","score":10.803266315022617},{"term":"galaxy","score":10.803266315022617},{"term":"kiezend","score":10.799555735626083},{"term":"lijfrent","score":10.799555735626083},{"term":"voorganger","score":10.799555735626083},{"term":"through","score":10.79770559633792},{"term":"accijn","score":10.79770559633792},{"term":"strafmaatverwer","score":10.795858873744756},{"term":"aldi","score":10.794015555250466},{"term":"boekwinst","score":10.79217562832846},{"term":"requirement","score":10.79217562832846},{"term":"aangestok","score":10.790339080521157},{"term":"steekwond","score":10.78667607276242},{"term":"gegrep","score":10.784849588236387},{"term":"cases","score":10.783026433674872},{"term":"”-","score":10.783026433674872},{"term":"imtech","score":10.783026433674872},{"term":"journal","score":10.783026433674872},{"term":"noodweersituatie","score":10.781206596957885},{"term":"citer","score":10.779390066031487},{"term":"deponer","score":10.779390066031487},{"term":"semi","score":10.779390066031487},{"term":"keur","score":10.779390066031487},{"term":"_NUMz","score":10.777576828907307},{"term":"rioolheff","score":10.773960188437117},{"term":"afrika","score":10.772156761437968},{"term":"automat","score":10.763188091455207},{"term":"rdinatie","score":10.761403969661705},{"term":"bouwbesluit","score":10.759623025290711},{"term":"berisp","score":10.759623025290711},{"term":"regiezit","score":10.759623025290711},{"term":"jager","score":10.754299144037962},{"term":"sbc","score":10.75076557145665},{"term":"opslan","score":10.749003456463251},{"term":"eiland","score":10.74724444105807},{"term":"continue","score":10.741985883804604},{"term":"boel","score":10.740239158469663},{"term":"zorgdrag","score":10.738495478864836},{"term":"mrb","score":10.735017214488511},{"term":"mess","score":10.735017214488511},{"term":",= (","score":10.729822397611407},{"term":"curatel","score":10.72637411833249},{"term":"vob","score":10.72637411833249},{"term":"familielid","score":10.72637411833249},{"term":"nachtelijk","score":10.722937688833909},{"term":"ntroduceerd","score":10.722937688833909},{"term":"meedel","score":10.721223892356175},{"term":"wfsv","score":10.721223892356175},{"term":"koud","score":10.719513027952546},{"term":"secundair","score":10.71780508560739},{"term":"leider","score":10.716100055356305},{"term":"successierecht","score":10.712698691532815},{"term":"schema","score":10.712698691532815},{"term":"benz","score":10.709308857778304},{"term":"bankier","score":10.709308857778304},{"term":"declarer","score":10.707618240300397},{"term":"consider","score":10.705930476186676},{"term":"ontdoen","score":10.705930476186676},{"term":"schend","score":10.705930476186676},{"term":"scores","score":10.705930476186676},{"term":"rabo","score":10.704245555821757},{"term":"wegverker","score":10.704245555821757},{"term":")-","score":10.704245555821757},{"term":"chr","score":10.702563469638772},{"term":"]) (","score":10.702563469638772},{"term":"jacob","score":10.6992077617918},{"term":"hasj","score":10.69753412123377},{"term":"kat","score":10.69753412123377},{"term":"monteur","score":10.695863277068952},{"term":"boeterapport","score":10.695863277068952},{"term":"gestand","score":10.695863277068952},{"term":"roemen","score":10.695863277068952},{"term":"arbeidskort","score":10.692529940649194},{"term":"conditie","score":10.689207678457217},{"term":"opend","score":10.689207678457217},{"term":"muziek","score":10.689207678457217},{"term":"bedrijfsleider","score":10.689207678457217},{"term":"dsb","score":10.687550677249586},{"term":"ouderschap","score":10.685896417153561},{"term":"rochdal","score":10.684244889115087},{"term":"slotervaartziekenhuis","score":10.682596084124903},{"term":"hp","score":10.680949993218235},{"term":"ongehuwd","score":10.679306607474505},{"term":"zorgverlen","score":10.679306607474505},{"term":"grav","score":10.679306607474505},{"term":"hersteltermijn","score":10.677665918017045},{"term":"call","score":10.676027916012806},{"term":"ondernemerschap","score":10.674392592672076},{"term":"onzak","score":10.674392592672076},{"term":"vloei","score":10.674392592672076},{"term":"gelukk","score":10.674392592672076},{"term":"stroomstootwap","score":10.672759939248191},{"term":"successiewet","score":10.672759939248191},{"term":"legat","score":10.672759939248191},{"term":"rooster","score":10.67112994703726},{"term":"achtertuin","score":10.669502607377884},{"term":"tempo","score":10.667877911650882},{"term":"betonn","score":10.667877911650882},{"term":"hom","score":10.664636417726712},{"term":"generiek","score":10.663019602499805},{"term":"punitiev","score":10.663019602499805},{"term":"verkapt","score":10.663019602499805},{"term":"erfpachter","score":10.661405397145264},{"term":"consultancy","score":10.653373225448},{"term":"br","score":10.651774504084303},{"term":"nvm","score":10.651774504084303},{"term":"aankwam","score":10.648584708716204},{"term":"asielprocedur","score":10.645405055798824},{"term":"kantoorpand","score":10.643819012643188},{"term":"aanbetal","score":10.642235481037545},{"term":"studio","score":10.642235481037545},{"term":"meester","score":10.639075920747176},{"term":"– (","score":10.639075920747176},{"term":"misgelop","score":10.639075920747176},{"term":"pric","score":10.63749987629171},{"term":"ondervrag","score":10.63749987629171},{"term":"matching","score":10.63749987629171},{"term":"middelengebruik","score":10.63592631184428},{"term":"anbi","score":10.631220420806867},{"term":"materie","score":10.631220420806867},{"term":"spijkerbroek","score":10.629656698830685},{"term":"kostbar","score":10.629656698830685},{"term":"sprekend","score":10.628095418263731},{"term":"hes","score":10.628095418263731},{"term":") “","score":10.62653657149444},{"term":"folmer","score":10.624980150946783},{"term":"application","score":10.623426149080048},{"term":"ers","score":10.623426149080048},{"term":"uitgev","score":10.623426149080048},{"term":"ongevall","score":10.62187455838863},{"term":"importeur","score":10.61415251229472},{"term":"aangeraakt","score":10.61415251229472},{"term":"devolutiev","score":10.612615232975832},{"term":"erfenis","score":10.611080313257748},{"term":"kasstort","score":10.608017523727204},{"term":"vlissing","score":10.60648963954915},{"term":"ongenummerd","score":10.603440856699791},{"term":"huuropbrengst","score":10.601919943859084},{"term":"toelag","score":10.600401340681895},{"term":"opgelicht","score":10.600401340681895},{"term":"parket","score":10.600401340681895},{"term":"wijn","score":10.597371035332715},{"term":"valk","score":10.595859319247394},{"term":"spirit","score":10.595859319247394},{"term":"signaler","score":10.59434988499854},{"term":"nieuwenhuiz","score":10.592842725707968},{"term":"veb","score":10.592842725707968},{"term":"aanverwant","score":10.591337834528547},{"term":"gevangenis","score":10.588334829268778},{"term":"abuis","score":10.585340815056174},{"term":"lus","score":10.58384716279939},{"term":"werkbedrijf","score":10.58384716279939},{"term":"(…)’","score":10.58384716279939},{"term":"] ”","score":10.580866534661252},{"term":"trauma","score":10.577894764272095},{"term":"huurwaard","score":10.576412184311872},{"term":"geactiveerd","score":10.573453602272126},{"term":"wetend","score":10.573453602272126},{"term":"bal","score":10.573453602272126},{"term":"autobedrijf","score":10.571977587244007},{"term":"mini","score":10.570503747625706},{"term":"consequent","score":10.570503747625706},{"term":"teamleider","score":10.56903207701425},{"term":"onderscheidt","score":10.56903207701425},{"term":"margin","score":10.56609521734094},{"term":"szw","score":10.563166957561851},{"term":"lichtvaard","score":10.558790582962052},{"term":"client","score":10.558790582962052},{"term":"verandert","score":10.557336037251059},{"term":"referentie","score":10.557336037251059},{"term":"minimumlon","score":10.552985051416732},{"term":"onbelast","score":10.551538919566733},{"term":"dragend","score":10.551538919566733},{"term":"jumbo","score":10.551538919566733},{"term":"patholog","score":10.551538919566733},{"term":"gestoeld","score":10.550094875994498},{"term":"chemisch","score":10.548652914677598},{"term":"gesuggereerd","score":10.548652914677598},{"term":"nuon","score":10.548652914677598},{"term":"twaalfd","score":10.54721302961961},{"term":"snijd","score":10.544339464423878},{"term":"mol","score":10.544339464423878},{"term":"geforceerd","score":10.54290577242203},{"term":"verruim","score":10.541474132950592},{"term":"univ","score":10.541474132950592},{"term":"miskenn","score":10.540044540140997},{"term":"landgoed","score":10.535767983373736},{"term":"vernieuw","score":10.534346519026359},{"term":"omwill","score":10.534346519026359},{"term":"herstelkost","score":10.53150963769116},{"term":"figur","score":10.53009420928783},{"term":"ongewenstverklar","score":10.528680781490682},{"term":"gedraaid","score":10.528680781490682},{"term":"levensmiddel","score":10.52585990514904},{"term":"ontbindt","score":10.524452445381161},{"term":"rc","score":10.518842332490394},{"term":"pomp","score":10.517444708223756},{"term":"reinder","score":10.516049034584782},{"term":"aandelenoverdracht","score":10.514655306136177},{"term":"geobjectiveerd","score":10.513263517463356},{"term":"arbeidsgeschikt","score":10.5118736631743},{"term":"export","score":10.510485737899453},{"term":"subsidiabel","score":10.510485737899453},{"term":"york","score":10.509099736291576},{"term":"dwi","score":10.506333482798686},{"term":"geldleningsovereenkomst","score":10.503574860359606},{"term":"bankbiljet","score":10.503574860359606},{"term":"serienummer","score":10.503574860359606},{"term":"dezer","score":10.500823826987716},{"term":"prima","score":10.500823826987716},{"term":"gezichtspunt","score":10.500823826987716},{"term":"leuk","score":10.499451143175744},{"term":"politieregio","score":10.496711415434623},{"term":"stuiting","score":10.496711415434623},{"term":"waarheidsvind","score":10.49534436122309},{"term":"vari","score":10.49397917329775},{"term":"timmerman","score":10.491254375971565},{"term":"contactregel","score":10.491254375971565},{"term":"beeldmerk","score":10.488536982995065},{"term":"two","score":10.488536982995065},{"term":"personeelsled","score":10.48718105058393},{"term":"aankom","score":10.48718105058393},{"term":"drempel","score":10.484474688986186},{"term":"aanvangt","score":10.484474688986186},{"term":"terroristisch","score":10.484474688986186},{"term":"kaderbesluit","score":10.47908384035131},{"term":"motiveringsgebrek","score":10.47908384035131},{"term":"notariskantor","score":10.477740656804842},{"term":"winkelruimt","score":10.477740656804842},{"term":"mailwissel","score":10.476399274980642},{"term":"monument","score":10.476399274980642},{"term":"doorwerk","score":10.476399274980642},{"term":"herken","score":10.473721897209925},{"term":"psychiatrie","score":10.473721897209925},{"term":"opgepakt","score":10.472385891667182},{"term":"buitenzijd","score":10.472385891667182},{"term":"saf","score":10.471051668654045},{"term":"un","score":10.471051668654045},{"term":"ingewilligd","score":10.471051668654045},{"term":"vis","score":10.469719223420267},{"term":"ongunst","score":10.468388551234561},{"term":"alimentatienorm","score":10.467059647384508},{"term":"overblijv","score":10.467059647384508},{"term":"complexiteit","score":10.467059647384508},{"term":"ingevoegd","score":10.463083499004869},{"term":"achterdeur","score":10.461761621746954},{"term":"volhard","score":10.461761621746954},{"term":"waz","score":10.46044148954203},{"term":"betaamt","score":10.456491517322716},{"term":"states","score":10.455178319497756},{"term":"wahv","score":10.453866843899945},{"term":"pandhouder","score":10.453866843899945},{"term":"scan","score":10.452557086017881},{"term":"herhaalt","score":10.452557086017881},{"term":"aktes","score":10.44994270544381},{"term":"’-","score":10.44994270544381},{"term":"indicatiestell","score":10.44733514203673},{"term":"bekled","score":10.446033905678759},{"term":"industrie","score":10.439553001594675},{"term":"bindt","score":10.438261845831056},{"term":"],[","score":10.435684524816754},{"term":"instemt","score":10.433113829313653},{"term":"loonbetal","score":10.433113829313653},{"term":"vliet","score":10.431830955500764},{"term":"nifp","score":10.431830955500764},{"term":"wubo","score":10.430549725344715},{"term":"wegdek","score":10.430549725344715},{"term":"uitdel","score":10.427992179193598},{"term":"vermogensdelict","score":10.426715854833994},{"term":"vliegtuig","score":10.424168082755195},{"term":"cost","score":10.424168082755195},{"term":"s_NUM","score":10.424168082755195},{"term":"goes","score":10.420358554338527},{"term":"united","score":10.420358554338527},{"term":"daarenbov","score":10.420358554338527},{"term":"long","score":10.417826907416748},{"term":"area","score":10.417826907416748},{"term":"gecommuniceerd","score":10.415301653549554},{"term":"radio","score":10.414041413937266},{"term":"additionel","score":10.410270196394187},{"term":"trof","score":10.407763929418127},{"term":"voedsel","score":10.407763929418127},{"term":"geschov","score":10.406513147516474},{"term":"werkgroep","score":10.406513147516474},{"term":"stemt","score":10.404016267317887},{"term":"detailhandel","score":10.40277016123764},{"term":"zed","score":10.40277016123764},{"term":"paleis","score":10.40277016123764},{"term":"dga","score":10.40028259776584},{"term":"vc","score":10.40028259776584},{"term":"loan","score":10.40028259776584},{"term":"wessel","score":10.40028259776584},{"term":"koopt","score":10.40028259776584},{"term":"sales","score":10.399041132677773},{"term":"russisch","score":10.397801206914453},{"term":"being","score":10.396562816663307},{"term":"sncu","score":10.396562816663307},{"term":"situation","score":10.39532595812591},{"term":"toegangsdeur","score":10.39532595812591},{"term":"bvie","score":10.392856821068989},{"term":"haard","score":10.391624535022723},{"term":"event","score":10.391624535022723},{"term":"beur","score":10.389164509181862},{"term":"inwon","score":10.389164509181862},{"term":"verliet","score":10.38793676194354},{"term":"taakvervull","score":10.38793676194354},{"term":"griend","score":10.384262538581655},{"term":"spaarreken","score":10.384262538581655},{"term":"laagst","score":10.384262538581655},{"term":"opdrag","score":10.384262538581655},{"term":"graf","score":10.381820534926103},{"term":"liquiditeit","score":10.375741488849721},{"term":"ontzet","score":10.374530101663424},{"term":"wettekst","score":10.373320180160711},{"term":"steg","score":10.370904720049106},{"term":"pet","score":10.369699174393759},{"term":"weert","score":10.369699174393759},{"term":"kinderbijdrag","score":10.36609123301756},{"term":"f_NUM","score":10.363693150333546},{"term":"verkoopprijz","score":10.363693150333546},{"term":"googl","score":10.363693150333546},{"term":"hoogeven","score":10.361300804694926},{"term":"mechanisch","score":10.358914168717195},{"term":"informatieverplicht","score":10.355344860942076},{"term":"t_NUM","score":10.355344860942076},{"term":"asbesthoud","score":10.352972380588445},{"term":"achtergesteld","score":10.352972380588445},{"term":"inbegrep","score":10.352972380588445},{"term":"”),","score":10.352972380588445},{"term":"ontslagvergoed","score":10.352972380588445},{"term":"afkop","score":10.350605515578179},{"term":"rechtsvoorgangster","score":10.349424180532347},{"term":"\"(","score":10.349424180532347},{"term":"engeland","score":10.348244239392498},{"term":"schiedam","score":10.347065688873055},{"term":"und","score":10.344712746611028},{"term":"consult","score":10.342365327692724},{"term":"ambt","score":10.342365327692724},{"term":"ph","score":10.32608589840386},{"term":"stukj","score":10.32608589840386},{"term":"achterligg","score":10.323781749919009},{"term":"beslistermijn","score":10.323781749919009},{"term":"cash","score":10.322631663535772},{"term":"abp","score":10.316900988826788},{"term":"uitwend","score":10.315758784248011},{"term":"oma","score":10.315758784248011},{"term":"item","score":10.313478281549285},{"term":"interim","score":10.312339977499224},{"term":"]”,","score":10.312339977499224},{"term":"polikliniek","score":10.312339977499224},{"term":"board","score":10.31120296771215},{"term":"vital","score":10.31120296771215},{"term":"uitreik","score":10.306667812546758},{"term":"herinvesteringsreserv","score":10.306667812546758},{"term":"landbouw","score":10.306667812546758},{"term":"verschrijv","score":10.305537230176473},{"term":"aangehecht","score":10.304407924579321},{"term":"keuring","score":10.302153132192231},{"term":"evaluatie","score":10.301027639670497},{"term":"credit","score":10.301027639670497},{"term":"toetst","score":10.301027639670497},{"term":"bind","score":10.298780447713593},{"term":"gefixeerd","score":10.298780447713593},{"term":"geheimhoudingsplicht","score":10.296538294307902},{"term":"rechtszekerheidsbeginsel","score":10.294301156909546},{"term":"varken","score":10.294301156909546},{"term":"alert","score":10.290954806882203},{"term":"overgelat","score":10.289841840712782},{"term":"befor","score":10.289841840712782},{"term":"verscheid","score":10.289841840712782},{"term":"gelijksoort","score":10.287619617576064},{"term":"veenendal","score":10.287619617576064},{"term":"herstelwerkzam","score":10.286510355121779},{"term":"belastingrechter","score":10.27988049658311},{"term":"oral","score":10.275484885110071},{"term":"doeltreff","score":10.275484885110071},{"term":"geoorloofd","score":10.275484885110071},{"term":"objectiver","score":10.275484885110071},{"term":"aangeschrev","score":10.274388994589433},{"term":"fabriek","score":10.273294303730252},{"term":"netbeheerder","score":10.272200809908881},{"term":"uniek","score":10.268927484563912},{"term":"gelijkenis","score":10.267838752828714},{"term":"vermeul","score":10.267838752828714},{"term":"gestraft","score":10.267838752828714},{"term":"verenig","score":10.267838752828714},{"term":"blokker","score":10.267838752828714},{"term":"ptss","score":10.266751205141317},{"term":"participant","score":10.264579651627807},{"term":"bbz","score":10.26349564068155},{"term":"alcoholhoud","score":10.260250640538223},{"term":"terras","score":10.257016136370563},{"term":"ingelicht","score":10.25379206049881},{"term":"kabinet","score":10.25379206049881},{"term":"kok","score":10.252719674337058},{"term":"bijstandverlen","score":10.25164843695556},{"term":"lif","score":10.24950939870681},{"term":"geldelijk","score":10.246309395976139},{"term":"uitgift","score":10.240998709038902},{"term":"advocatenkantor","score":10.240998709038902},{"term":"dwangbevel","score":10.237825781998323},{"term":"anw","score":10.23677037292938},{"term":"ervaringsregel","score":10.235716076574475},{"term":"support","score":10.235716076574475},{"term":"vertelt","score":10.234662890589815},{"term":"diefstall","score":10.23361081263901},{"term":"voortijd","score":10.232559840393037},{"term":"aanmeld","score":10.232559840393037},{"term":"stoorniss","score":10.232559840393037},{"term":"ouderschapsplan","score":10.2315099715302},{"term":"verzekeringnemer","score":10.229413534703673},{"term":"vangt","score":10.226277097213542},{"term":"arbeidsrelatie","score":10.22315046621411},{"term":"reserves","score":10.222110424518695},{"term":"gratis","score":10.222110424518695},{"term":"statement","score":10.221071463386277},{"term":"verzwaard","score":10.218996773845417},{"term":"interpolis","score":10.216926379731108},{"term":"nabetal","score":10.209713571541867},{"term":"lar","score":10.208687404186554},{"term":"immigratie","score":10.206638223141619},{"term":"doorstan","score":10.204593232653892},{"term":"simon","score":10.204593232653892},{"term":"nokia","score":10.204593232653892},{"term":"snell","score":10.203572303519785},{"term":"ns","score":10.201533566829935},{"term":"motiev","score":10.195442177436535},{"term":"isd","score":10.192410340854414},{"term":"grijs","score":10.19140176789953},{"term":"liquid","score":10.19039421113923},{"term":"grootouder","score":10.189387668527829},{"term":"noodzakelijkerwijs","score":10.185371598872939},{"term":"gedupeerd","score":10.184370096535849},{"term":"huwelijksgoederengemeenschap","score":10.184370096535849},{"term":"tno","score":10.184370096535849},{"term":"menselijk","score":10.183369596202265},{"term":"neutral","score":10.181371593539591},{"term":"breuk","score":10.179377574932728},{"term":"onbeperkt","score":10.179377574932728},{"term":"onderdak","score":10.178382054691225},{"term":"onachtzam","score":10.17639398246584},{"term":"autosleutel","score":10.175401426553089},{"term":"lamp","score":10.174409854830794},{"term":"giv","score":10.174409854830794},{"term":"dagbested","score":10.173419265349096},{"term":"huurwon","score":10.172429656163931},{"term":"bijgevolg","score":10.171441025336991},{"term":"aanzegg","score":10.167496247045975},{"term":"borgstell","score":10.165529678073934},{"term":"gewog","score":10.162587057019737},{"term":"alhoewel","score":10.161608104420752},{"term":"leidrad","score":10.161608104420752},{"term":"dertien","score":10.160630109232775},{"term":"abonnement","score":10.156727665255843},{"term":"rijksweg","score":10.152840391167443},{"term":"hing","score":10.151870929142893},{"term":"aantrek","score":10.148968169484933},{"term":"zwijgrecht","score":10.148968169484933},{"term":"lanschot","score":10.146073811458567},{"term":"komst","score":10.137440664313864},{"term":"onderzoeksplicht","score":10.136486010303415},{"term":"bijgeschrev","score":10.135532266788104},{"term":"zuster","score":10.131726363050426},{"term":"(…)\"","score":10.130777146083094},{"term":"janss","score":10.127934889314163},{"term":"ir","score":10.127934889314163},{"term":"against","score":10.126989262766157},{"term":"inn","score":10.126989262766157},{"term":"drink","score":10.126989262766157},{"term":"ecd","score":10.126044529582995},{"term":"gedog","score":10.12510068807829},{"term":"lekkag","score":10.121334205282812},{"term":"tevred","score":10.116645964159357},{"term":"about","score":10.11571094772845},{"term":"terugkom","score":10.109190198028013},{"term":"fee","score":10.107334909926267},{"term":"universiteit","score":10.10271169318481},{"term":"), (","score":10.101789609209842},{"term":"oom","score":10.10086837469052},{"term":"rechtsbetrek","score":10.099028447768514},{"term":"schijf","score":10.09535871887955},{"term":"tax","score":10.09535871887955},{"term":"ip","score":10.094443387007864},{"term":"fod","score":10.094443387007864},{"term":"veronachtzam","score":10.091702407676442},{"term":"assistent","score":10.08805941639794},{"term":"gegenereerd","score":10.086242885471542},{"term":"boodschapp","score":10.084429648347362},{"term":"appl","score":10.083524261232549},{"term":"aankomst","score":10.083524261232549},{"term":"pur","score":10.082619693102123},{"term":"snijder","score":10.079910887834036},{"term":"lidmaatschap","score":10.075412454697172},{"term":"brandwer","score":10.072723076115201},{"term":"plafond","score":10.071828221469357},{"term":"vgz","score":10.070040910895262},{"term":"producer","score":10.070040910895262},{"term":"aangedan","score":10.066475844730766},{"term":"eindbesliss","score":10.066475844730766},{"term":",--)","score":10.064698066484766},{"term":"bpf","score":10.063810361144625},{"term":"vluchtelingenverdrag","score":10.06203731103474},{"term":"omgevingsrecht","score":10.060267399068282},{"term":"concurrer","score":10.057618390896705},{"term":"onderaannemer","score":10.057618390896705},{"term":"verificatie","score":10.056736945268899},{"term":"feitencomplex","score":10.054976381433866},{"term":"installaties","score":10.054976381433866},{"term":"koopman","score":10.05321891173722},{"term":"voorst","score":10.05234133379586},{"term":"parties","score":10.04971321138959},{"term":"dal","score":10.047091977909718},{"term":"borgtocht","score":10.046219758055031},{"term":"retour","score":10.04534829830489},{"term":"uitsprek","score":10.043607653827106},{"term":"oner","score":10.039269252228507},{"term":"overwaard","score":10.038403825952079},{"term":"ministeri","score":10.038403825952079},{"term":"gav","score":10.038403825952079},{"term":"geresulteerd","score":10.036675217051462},{"term":"ingewikkeld","score":10.036675217051462},{"term":"italiaan","score":10.032366722665737},{"term":"registergoed","score":10.029790508273964},{"term":"onderan","score":10.02807671179623},{"term":"gemist","score":10.024657905047444},{"term":"specificaties","score":10.023805026530926},{"term":"strafbepal","score":10.023805026530926},{"term":"waardeverminder","score":10.017855157724691},{"term":"overzien","score":10.017855157724691},{"term":"verschilt","score":10.015316011206014},{"term":"voortbestan","score":10.01278329562673},{"term":"onwens","score":10.011098375261811},{"term":"hennepteelt","score":10.009416289078827},{"term":"zijzelf","score":10.005223410818791},{"term":"rechtsord","score":10.003551169626428},{"term":"zolder","score":10.003551169626428},{"term":"kentekenplat","score":10.003551169626428},{"term":"fiets","score":10.002716096509007},{"term":"boetebesluit","score":9.999382760089249},{"term":"bk_NUM","score":9.998551159209724},{"term":"crisis","score":9.996060497897272},{"term":"\" (","score":9.996060497897272},{"term":"mis","score":9.99357602456961},{"term":"aangiftes","score":9.991097708555142},{"term":"duurt","score":9.990272966240353},{"term":"offici","score":9.989448903564957},{"term":"agenda","score":9.988625519409753},{"term":"blijkend","score":9.98615942691456},{"term":"koer","score":9.9845187374571},{"term":"zorgverlener","score":9.982880735452861},{"term":"speciaal","score":9.982062739497229},{"term":"medicijn","score":9.982062739497229},{"term":"reserver","score":9.97879743047349},{"term":"casus","score":9.974730731090936},{"term":"inhield","score":9.97310867071907},{"term":"ongeluk","score":9.968258216585319},{"term":"communautair","score":9.966646612690978},{"term":"vakantiedag","score":9.964234066285593},{"term":"verzekeringsbank","score":9.96182732625503},{"term":"grondwet","score":9.95942636471749},{"term":"hernieuwd","score":9.95942636471749},{"term":"natura","score":9.957031153991537},{"term":"benzin","score":9.955437528156258},{"term":"pensioenregel","score":9.954641666594155},{"term":"bocht","score":9.950671832083243},{"term":"geprocedeerd","score":9.950671832083243},{"term":"bekijk","score":9.950671832083243},{"term":"overgangsregel","score":9.948297474022782},{"term":"verz","score":9.948297474022782},{"term":"kozijn","score":9.946717694863263},{"term":"onvoorzien","score":9.946717694863263},{"term":"bq_NUM","score":9.943565603994948},{"term":"tuchtrecht","score":9.942779131284334},{"term":"vooruitzicht","score":9.935728573287667},{"term":"afmet","score":9.933389390934495},{"term":"voortduurt","score":9.931832970386838},{"term":"verontrein","score":9.931832970386838},{"term":"bezwaarprocedur","score":9.929502872245214},{"term":"wier","score":9.926404496412545},{"term":"abov","score":9.926404496412545},{"term":"they","score":9.921005331734774},{"term":"woninginbrak","score":9.920236396671896},{"term":"neurolog","score":9.919468052415887},{"term":"afgewog","score":9.914106109274503},{"term":"declaratie","score":9.914106109274503},{"term":"bopz","score":9.911055000881605},{"term":"into","score":9.910293676139846},{"term":"exploitatievergunn","score":9.908013173441121},{"term":"producent","score":9.908013173441121},{"term":"afstemm","score":9.90422385477277},{"term":"pleeggezin","score":9.901202704438594},{"term":"nvesteerd","score":9.900448840852194},{"term":"rechtsopvolger","score":9.898190653968602},{"term":"mad","score":9.895937555005693},{"term":"onverenig","score":9.895187648708832},{"term":"stadsdel","score":9.890699982239445},{"term":"his","score":9.890699982239445},{"term":"opgehoud","score":9.889953991902724},{"term":"your","score":9.889208557652776},{"term":"thes","score":9.887719354101307},{"term":"advocaatkost","score":9.883265003751927},{"term":"bewerkstelligd","score":9.882524537224203},{"term":"?”","score":9.878830406684061},{"term":"bas","score":9.874415388474944},{"term":"hoorn","score":9.874415388474944},{"term":"renovatie","score":9.874415388474944},{"term":"wetenschapp","score":9.872948036780995},{"term":"rechterhand","score":9.869289049895952},{"term":"renault","score":9.867829195622088},{"term":"vakanties","score":9.86710006689857},{"term":"kelder","score":9.865643402402107},{"term":"werkplek","score":9.865643402402107},{"term":"eer","score":9.86491586508373},{"term":"gewond","score":9.862010997391101},{"term":"gezorgd","score":9.859837870856786},{"term":"intellectuel","score":9.857669456562963},{"term":"verrijk","score":9.851909901404596},{"term":"kwijtscheld","score":9.851192283863933},{"term":"kadaster","score":9.851192283863933},{"term":"reclasseringsadvies","score":9.84904251592744},{"term":"tegenprestatie","score":9.846183328847395},{"term":"stilstand","score":9.84475679508194},{"term":"bijgehoud","score":9.84475679508194},{"term":"huurcontract","score":9.837654492499874},{"term":"verkoopopbrengst","score":9.832712724589095},{"term":"wetswijz","score":9.831305264821216},{"term":"elf","score":9.831305264821216},{"term":"medepleger","score":9.82849627421016},{"term":"diep","score":9.827094732284872},{"term":"uitvoeringsregel","score":9.82499609562795},{"term":"politiebureau","score":9.82499609562795},{"term":"voelt","score":9.82220474699068},{"term":"schadebedrag","score":9.82220474699068},{"term":"wes","score":9.821508125576232},{"term":"abrv","score":9.819421168297033},{"term":"hero","score":9.81595255573163},{"term":"vergelijkingsobject","score":9.81595255573163},{"term":"she","score":9.814568472465691},{"term":"bronn","score":9.811806039769781},{"term":"gecertificeerd","score":9.809051217090937},{"term":"opgev","score":9.80767664642777},{"term":"bar","score":9.806990068989197},{"term":"neus","score":9.805618326661616},{"term":"operationel","score":9.80493316048202},{"term":"reorganisatie","score":9.803564234874678},{"term":"gesplitst","score":9.800831992737805},{"term":"meting","score":9.80015009704142},{"term":"toebehoort","score":9.79538980243512},{"term":"wav","score":9.792679773676255},{"term":"europa","score":9.78997706932837},{"term":"notul","score":9.78930253304647},{"term":"navrag","score":9.785264842500393},{"term":"beroepsfas","score":9.784593476244897},{"term":"aangekom","score":9.78191250949164},{"term":"keert","score":9.7779044880941},{"term":"nakomt","score":9.77723804355094},{"term":"indexer","score":9.773912466824562},{"term":"onderdan","score":9.769275161396045},{"term":"hulpmiddel","score":9.765975917228827},{"term":"bewoond","score":9.765975917228827},{"term":"gedekt","score":9.76334433676277},{"term":"kassa","score":9.75810186079792},{"term":"portefeuill","score":9.756795524883865},{"term":"huurpenn","score":9.756142996312589},{"term":"trekt","score":9.756142996312589},{"term":"badkamer","score":9.752236741345524},{"term":"inherent","score":9.752236741345524},{"term":"bedrijfsactiviteit","score":9.74899314459968},{"term":"rok","score":9.745760034767533},{"term":"their","score":9.743825174441238},{"term":"verhinderd","score":9.743181052035714},{"term":"wilt","score":9.740608703350404},{"term":"toevertrouwd","score":9.736122954079146},{"term":"han","score":9.735483772210149},{"term":"can","score":9.733568674274048},{"term":"afnam","score":9.730384972126124},{"term":"uitgevaardigd","score":9.727211373778582},{"term":"venlo","score":9.72657786092876},{"term":"pot","score":9.722154472989608},{"term":"rechtsregel","score":9.712741162187687},{"term":"frequentie","score":9.712741162187687},{"term":"geraadpleegd","score":9.710245839620285},{"term":"dwangverpleg","score":9.70837842544549},{"term":"uurtarief","score":9.707756728192019},{"term":"schikking","score":9.705893952117828},{"term":"citat","score":9.703415636103362},{"term":"geautomatiseerd","score":9.700326353448725},{"term":"gelukt","score":9.697861780420563},{"term":"meegewerkt","score":9.695403266582357},{"term":"handschoen","score":9.688673354366157},{"term":"aangeleg","score":9.687454585172029},{"term":") –","score":9.686237299568276},{"term":"gerez","score":9.686237299568276},{"term":"gedeponeerd","score":9.683807164714985},{"term":"ongebruik","score":9.682594308289776},{"term":"first","score":9.679568587373238},{"term":"trapp","score":9.679568587373238},{"term":"intred","score":9.678964540239196},{"term":"hangend","score":9.67836085775784},{"term":"onterecht","score":9.669349081539755},{"term":"chronisch","score":9.667556430925234},{"term":"vermijd","score":9.666363113361193},{"term":"discriminatie","score":9.664575802787096},{"term":"ziektekost","score":9.664575802787096},{"term":"huurachterstand","score":9.661604032397939},{"term":"tek","score":9.659232958376602},{"term":"still","score":9.655097058832553},{"term":"confrontatie","score":9.655097058832553},{"term":"ongeld","score":9.654507609950176},{"term":"gewend","score":9.651565566051083},{"term":"schadelijk","score":9.65039116779514},{"term":"blot","score":9.649804485466785},{"term":"honderd","score":9.64746119206881},{"term":"gelijkluid","score":9.646876225687697},{"term":"doorgang","score":9.643373595136493},{"term":"douan","score":9.640464078972862},{"term":"datzelfd","score":9.639883190196725},{"term":"beboet","score":9.635826389501112},{"term":"sten","score":9.630634569359065},{"term":"incasso","score":9.629484482975828},{"term":"toebreng","score":9.628335717771954},{"term":"vijftien","score":9.627188270715477},{"term":"jaarstuk","score":9.626615040547874},{"term":"belegger","score":9.626042138784854},{"term":"ouderdomspensioen","score":9.6243254001658},{"term":"vakantietoeslag","score":9.622040990263168},{"term":"momentel","score":9.619761786997305},{"term":"ede","score":9.619192796939279},{"term":"respecter","score":9.616352692786531},{"term":"haalbar","score":9.611825237401568},{"term":"beslaglijst","score":9.610696569110194},{"term":"visser","score":9.609005951632286},{"term":"studie","score":9.605072257321261},{"term":"bewaard","score":9.603951180970663},{"term":"gunn","score":9.593363174561834},{"term":"eigenschapp","score":9.589490417300988},{"term":"ontdekt","score":9.586733316023164},{"term":"actuel","score":9.586733316023164},{"term":"vergunninghouder","score":9.58618280678823},{"term":"elektrisch","score":9.583983795456794},{"term":"arbeidsvoorwaard","score":9.583983795456794},{"term":"vernietig","score":9.582337704550126},{"term":"inventaris","score":9.580694318806396},{"term":"percent","score":9.578507330509892},{"term":"detail","score":9.576325114695731},{"term":"rij","score":9.575780304003967},{"term":"strafrechter","score":9.573604024581371},{"term":"heroverweg","score":9.573060693951325},{"term":"ergen","score":9.567103459978277},{"term":"gederfd","score":9.562793108477155},{"term":"verpak","score":9.560644879938865},{"term":"familieled","score":9.558501256395614},{"term":"jans","score":9.55743116533577},{"term":"kek","score":9.553162215416194},{"term":"rechtsgrond","score":9.552629875770936},{"term":"gemeenterad","score":9.552629875770936},{"term":"dnb","score":9.546263806525944},{"term":"inschakel","score":9.543095905249388},{"term":"regim","score":9.539412659833092},{"term":"schakel","score":9.536790051458654},{"term":"aanbieder","score":9.533129916653596},{"term":"kinderopvang","score":9.532608132138757},{"term":"belangrijkst","score":9.532086619741019},{"term":"up","score":9.527924282826332},{"term":"complet","score":9.526367862278672},{"term":"gunstig","score":9.521197219941751},{"term":"boekjar","score":9.51554022362661},{"term":"macht","score":9.512979402764936},{"term":"instructies","score":9.50838638626999},{"term":"verschon","score":9.50787735088104},{"term":"frequent","score":9.50736857447729},{"term":"executorial","score":9.498758746664606},{"term":"glas","score":9.496240487967883},{"term":"zozer","score":9.490722540684002},{"term":"monster","score":9.49022241564232},{"term":"bewind","score":9.49022241564232},{"term":"teit","score":9.489222915309236},{"term":"bestuursdwang","score":9.488723539518583},{"term":"bred","score":9.487725535443733},{"term":"ophal","score":9.487725535443733},{"term":"vergiss","score":9.48722690666252},{"term":"ondergrond","score":9.485732510369468},{"term":"aanvoer","score":9.482750400803619},{"term":"parkeerplat","score":9.478788038016656},{"term":"bankgarantie","score":9.477306190375772},{"term":"herkenn","score":9.475333803148569},{"term":"woonhuis","score":9.470910104609947},{"term":"vernield","score":9.466017726945502},{"term":"bjz","score":9.465042117112022},{"term":"bus","score":9.461149168156613},{"term":"collectiev","score":9.454855271805028},{"term":"verpand","score":9.454855271805028},{"term":"gerapporteerd","score":9.454855271805028},{"term":"raadpleg","score":9.448121239623685},{"term":"signal","score":9.446205530225546},{"term":"tegoed","score":9.445727175755769},{"term":"iederen","score":9.444293483753919},{"term":"gepresenteerd","score":9.44333882974347},{"term":"verzekeringsgeneeskund","score":9.441908555363769},{"term":"individueel","score":9.436207827336082},{"term":"weten","score":9.434314783704023},{"term":"overboek","score":9.431953507518344},{"term":"willen","score":9.430068492822572},{"term":"turk","score":9.429127316282509},{"term":"idee","score":9.427717210290922},{"term":"onverkort","score":9.42584015671305},{"term":"onbehor","score":9.419763951993758},{"term":"pakket","score":9.418366978541892},{"term":"dientengevolg","score":9.418366978541892},{"term":"cm","score":9.417901754062694},{"term":"hoek","score":9.415114943155514},{"term":"geboekt","score":9.410487447623465},{"term":"ruit","score":9.40818170530611},{"term":"adviseert","score":9.404044719401266},{"term":"gef","score":9.403586108982772},{"term":"beheerder","score":9.402669518634458},{"term":"overnem","score":9.398555227116496},{"term":"account","score":9.396276802799791},{"term":"tracht","score":9.389472512542177},{"term":"ongerechtvaardigd","score":9.386312952251808},{"term":"bouwwerk","score":9.378233913694272},{"term":"getaxeerd","score":9.371106934752158},{"term":"toezend","score":9.36800478291807},{"term":"trap","score":9.367562402906431},{"term":"twintig","score":9.36270909534336},{"term":"vaker","score":9.360071731177275},{"term":"toegekom","score":9.357879228654607},{"term":"rechtsvoorganger","score":9.356566030829645},{"term":"klap","score":9.351330416775701},{"term":"unit","score":9.351330416775701},{"term":"verstek","score":9.35046047326716},{"term":"convenant","score":9.350025785149036},{"term":"vastgepakt","score":9.346122071668562},{"term":"kavel","score":9.343528036491517},{"term":"inschat","score":9.341802410524046},{"term":"opgesomd","score":9.341371468909966},{"term":"afleid","score":9.339649557162945},{"term":"standaard","score":9.336214603162972},{"term":"huisvest","score":9.331510724487499},{"term":"essenti","score":9.331084194304022},{"term":"opgekom","score":9.328528826644332},{"term":"aanwend","score":9.316269108518881},{"term":"verstoord","score":9.308734539596943},{"term":"sindsdien","score":9.307484278963148},{"term":"aannemer","score":9.3037428486997},{"term":"schon","score":9.297125785680407},{"term":"bekek","score":9.2967136694578},{"term":"leest","score":9.294655632098344},{"term":"gematigd","score":9.291781476968481},{"term":"onderstaand","score":9.288915558937285},{"term":"kogel","score":9.282801824793346},{"term":"verjaringstermijn","score":9.281583550530991},{"term":"werkdag","score":9.27834205660682},{"term":"jarig","score":9.275111036025374},{"term":"vermogensbestanddel","score":9.273499432131032},{"term":"consistent","score":9.272694603113745},{"term":"lokal","score":9.271890421325331},{"term":"hoofdregel","score":9.270283995277058},{"term":"zuiver","score":9.269080866998783},{"term":"uitgelat","score":9.266279184157545},{"term":"gewijsd","score":9.265480142964412},{"term":"tolk","score":9.264681739728388},{"term":"onverminderd","score":9.264282777026109},{"term":"directie","score":9.263086843058572},{"term":"onderhand","score":9.262290347596313},{"term":"bmw","score":9.257128533474942},{"term":"aanman","score":9.257128533474942},{"term":"specificatie","score":9.255545628514437},{"term":"frankrijk","score":9.254360091920335},{"term":"testament","score":9.252781559627286},{"term":"chauffeur","score":9.250418423435002},{"term":"premies","score":9.250025109762282},{"term":"duw","score":9.249631950724389},{"term":"inbreng","score":9.248060858492348},{"term":"terugbetaald","score":9.248060858492348},{"term":"schat","score":9.247668470808286},{"term":"wanprestatie","score":9.247668470808286},{"term":"absolut","score":9.243753039024613},{"term":"bpm","score":9.24219114882033},{"term":"zend","score":9.241411117579531},{"term":"overeenkom","score":9.24102133000882},{"term":"alias","score":9.23790848702833},{"term":"continu","score":9.237520062086565},{"term":"vervalt","score":9.229012663521653},{"term":"emotionel","score":9.228627677953858},{"term":"wetsvoorstel","score":9.228627677953858},{"term":"hangt","score":9.228627677953858},{"term":"opvolg","score":9.22747360973575},{"term":"rekestnummer","score":9.226320871855942},{"term":"gewelddad","score":9.215625582739195},{"term":"°","score":9.211454960580113},{"term":"deurwaarder","score":9.211076674212824},{"term":"ondeugd","score":9.206924941508362},{"term":"help","score":9.206548364588077},{"term":"gepaard","score":9.202790374445748},{"term":"verjaard","score":9.199046453936283},{"term":",-)","score":9.1975528016795},{"term":"stelplicht","score":9.193828402588517},{"term":"sek","score":9.189747521391785},{"term":"²","score":9.184577642816366},{"term":"gaand","score":9.181635382510642},{"term":"veelvuld","score":9.178335654493722},{"term":"gestopt","score":9.171405114259827},{"term":"geduurd","score":9.168138916473206},{"term":"geklaagd","score":9.160559020950437},{"term":"rechtsverhoud","score":9.159480853730091},{"term":"stelsel","score":9.154107385668949},{"term":"vereffen","score":9.1508971100387},{"term":"cent","score":9.148407334610612},{"term":"geselecteerd","score":9.145569446930912},{"term":"pakt","score":9.14309288454712},{"term":"kwalificer","score":9.13991771871143},{"term":"dwingend","score":9.13956554402915},{"term":"loonbelast","score":9.13464807714762},{"term":"conflict","score":9.129406059203037},{"term":"mishandeld","score":9.126621511632854},{"term":"linker","score":9.122459174718166},{"term":"toerekeningsvat","score":9.111443572796661},{"term":"praktisch","score":9.110759110073156},{"term":"aangev","score":9.108708528356669},{"term":"gediend","score":9.10768481217786},{"term":"belastingheff","score":9.107002916481475},{"term":"belt","score":9.105300208765485},{"term":"stopp","score":9.105300208765485},{"term":"voortvar","score":9.09953259311631},{"term":"adequat","score":9.096155352486525},{"term":"volded","score":9.096155352486525},{"term":"bom","score":9.093798052186305},{"term":"constructie","score":9.093461748164302},{"term":"omgezet","score":9.092117661940447},{"term":"meegewog","score":9.09010491386075},{"term":"bestanddel","score":9.084424029744035},{"term":"gespeeld","score":9.07910663602534},{"term":"failliet","score":9.077450732351718},{"term":"bevol","score":9.077119880404943},{"term":"tweetal","score":9.07381736749582},{"term":"kel","score":9.072170192028684},{"term":"oefen","score":9.069540341729295},{"term":"engel","score":9.068883958377866},{"term":"kleiner","score":9.065608488695394},{"term":"waarbinn","score":9.061692034603158},{"term":"ontoereik","score":9.058115376587734},{"term":"schal","score":9.055845964039735},{"term":"fraud","score":9.049390163696863},{"term":"meebreng","score":9.046498595417065},{"term":"aanvoert","score":9.044255364224826},{"term":"doeleind","score":9.04329551772656},{"term":"vries","score":9.042975773519201},{"term":"geantwoord","score":9.040102666993723},{"term":"bestrijd","score":9.03946531834245},{"term":"buurt","score":9.03946531834245},{"term":"ontwerp","score":9.038510056368835},{"term":"leg","score":9.038191838386835},{"term":"(“","score":9.034698107661418},{"term":"geschiedt","score":9.031216540606126},{"term":"ingeroep","score":9.025231328220128},{"term":"apart","score":9.024917307178137},{"term":"prijz","score":9.022408682469619},{"term":"gebouwd","score":9.021469568298237},{"term":"erover","score":9.021469568298237},{"term":"meewerk","score":9.02115672612456},{"term":"verblev","score":9.016787181094887},{"term":"opslag","score":9.01647580011775},{"term":"gba","score":9.012746771557882},{"term":"afgeleverd","score":9.010577909359904},{"term":"gevoel","score":9.005022339515302},{"term":"verplaats","score":9.004714599860618},{"term":"kwartal","score":9.004714599860618},{"term":"korting","score":9.003484587405419},{"term":"erkent","score":9.001335699395083},{"term":"klopt","score":9.000416159100403},{"term":"berecht","score":8.99735711725332},{"term":"stapp","score":8.996746430211441},{"term":"amersfoort","score":8.996746430211441},{"term":"bedrijfsvoer","score":8.991570592808037},{"term":"aangevang","score":8.991266964446817},{"term":"geroep","score":8.988538449793612},{"term":"stof","score":8.986119337637156},{"term":"persoonsgegeven","score":8.985515472884506},{"term":"garag","score":8.983103653807655},{"term":"louter","score":8.980697637609376},{"term":"volstrekt","score":8.979796871897669},{"term":"maandag","score":8.975603993637632},{"term":"‘[","score":8.972321878204367},{"term":"wien","score":8.967567040122752},{"term":"definitie","score":8.966381854798835},{"term":"vb","score":8.966085777816657},{"term":"prostitutie","score":8.960771327753164},{"term":"nvloed","score":8.958712200683497},{"term":"koophandel","score":8.957537457523435},{"term":"oppervlakt","score":8.952852236654216},{"term":"lezing","score":8.949935081726709},{"term":"gestaakt","score":8.94964383372698},{"term":"ding","score":8.948479689241607},{"term":"nop","score":8.942101007132631},{"term":"toegezegd","score":8.923772888128331},{"term":"beoogt","score":8.91839577031893},{"term":"voorstell","score":8.91811356345943},{"term":"registratie","score":8.916703722537905},{"term":"dicht","score":8.91501453294706},{"term":"map","score":8.912766709578566},{"term":"afdoend","score":8.911925076761316},{"term":"spull","score":8.910523927589953},{"term":"woonacht","score":8.909404420056294},{"term":"verspreid","score":8.90689010108958},{"term":"hout","score":8.90661112215068},{"term":"ordner","score":8.906332221019325},{"term":"passag","score":8.901602764436308},{"term":"el","score":8.898554404032229},{"term":"doorlop","score":8.895239483869606},{"term":"rechtshandel","score":8.892485419887032},{"term":"afgewek","score":8.888642428607865},{"term":"vlak","score":8.88754713824645},{"term":"handteken","score":8.88645304623359},{"term":"hoorzit","score":8.885360149949946},{"term":"rijbewijs","score":8.879641958739336},{"term":"opgehev","score":8.87584791624777},{"term":"’)","score":8.874766542798247},{"term":"bloed","score":8.865086445698918},{"term":"wenselijk","score":8.856559881917267},{"term":"gespecificeerd","score":8.848368529469925},{"term":"berg","score":8.847316451519122},{"term":"particulier","score":8.846528118801904},{"term":"wissel","score":8.844691089024037},{"term":"geestelijk","score":8.843380987958886},{"term":"gemoeid","score":8.842334141487989},{"term":"uitgekeerd","score":8.839982736093651},{"term":"strek","score":8.83867878493745},{"term":"sectie","score":8.836075972432107},{"term":"informer","score":8.832961513646783},{"term":"speelt","score":8.832184412195943},{"term":"gekoppeld","score":8.829856724606755},{"term":"benader","score":8.827792207744436},{"term":"aangesprok","score":8.824446431878506},{"term":"behalv","score":8.819065252973989},{"term":"beantwoordt","score":8.81753307698477},{"term":"voorbeeld","score":8.816512928974849},{"term":"pleitnotities","score":8.815493820606392},{"term":"inschrijver","score":8.814984655625942},{"term":"allemal","score":8.807125570935876},{"term":"blef","score":8.805359454670072},{"term":"gewekt","score":8.802841829976712},{"term":"vervull","score":8.802590415770485},{"term":"plicht","score":8.801836552184085},{"term":"executie","score":8.795826015681943},{"term":"gevergd","score":8.792336480735202},{"term":"boedel","score":8.78564085745671},{"term":"geschetst","score":8.784899659152241},{"term":"bestrijdt","score":8.78341890863489},{"term":"viel","score":8.775069154819759},{"term":"indicatie","score":8.774824626147554},{"term":"stadium","score":8.769460068049401},{"term":"voornemen","score":8.768244853414497},{"term":"dwangsomm","score":8.767759180748321},{"term":"verlof","score":8.766546030047552},{"term":"ontlen","score":8.766546030047552},{"term":"huurprijs","score":8.758335406766443},{"term":"bent","score":8.756653320583458},{"term":"verrek","score":8.754494787547255},{"term":"gepubliceerd","score":8.754015745621928},{"term":"makelar","score":8.752340903742203},{"term":"uitbetaald","score":8.751385109165259},{"term":"mitsdien","score":8.745432001930535},{"term":"temer","score":8.743534468154344},{"term":"vrijsprek","score":8.739986268098248},{"term":"gerelateerd","score":8.738570505709811},{"term":"huisart","score":8.738334740059774},{"term":"opgeroep","score":8.736921312262627},{"term":"overlast","score":8.73386572168449},{"term":"ontwikkeld","score":8.73363106252584},{"term":"relatief","score":8.732224262762758},{"term":"grenz","score":8.731521604342936},{"term":"strafproces","score":8.725685238995702},{"term":"uiteengezet","score":8.722895836908123},{"term":"geduwd","score":8.715034524746164},{"term":"geconfronteerd","score":8.70792032377148},{"term":"vrijwel","score":8.706777597101594},{"term":"wegingsfactor","score":8.704040369115186},{"term":"wen","score":8.701765055278049},{"term":"schiet","score":8.695421581355875},{"term":"waarschuw","score":8.694518646757283},{"term":"nfi","score":8.687772500191924},{"term":"alleenstaand","score":8.682408160104249},{"term":"verhuurd","score":8.681962427981869},{"term":"handelwijz","score":8.680848966114242},{"term":"voorzover","score":8.678403705364875},{"term":"verlor","score":8.67022234408645},{"term":"] –","score":8.66890192135939},{"term":"hypothecair","score":8.668462167614399},{"term":"correspondentie","score":8.66802260716776},{"term":"aangew","score":8.65644410534263},{"term":"bijzijn","score":8.653407885269962},{"term":"adviser","score":8.649733397184447},{"term":", €","score":8.646717453493817},{"term":"opgezegd","score":8.646287345960305},{"term":"situaties","score":8.634743823164921},{"term":"indruk","score":8.6315607966048},{"term":"betaalt","score":8.630078844391166},{"term":"constater","score":8.62733246049528},{"term":"nagenoeg","score":8.62375237875212},{"term":"vervoerd","score":8.620813669668294},{"term":"th","score":8.61663043830128},{"term":"tenminst","score":8.613920691984529},{"term":"constateert","score":8.613920691984529},{"term":"ziek","score":8.613504458887988},{"term":"voortdur","score":8.583982019621667},{"term":"aangevuld","score":8.580352251571087},{"term":"lijd","score":8.5795474225538},{"term":"controler","score":8.576535069638279},{"term":"helemal","score":8.572532662854456},{"term":"actief","score":8.570138885529468},{"term":"ingevuld","score":8.565765121101771},{"term":"mit","score":8.553345050159775},{"term":"mann","score":8.550215157150848},{"term":"vriendin","score":8.543984607400212},{"term":"rechtvaardigt","score":8.543790526856178},{"term":"treedt","score":8.522098535128148},{"term":"moeilijk","score":8.515474114196596},{"term":"verleent","score":8.511896292848713},{"term":"vooralsnog","score":8.505525651104104},{"term":"objectief","score":8.50198318445558},{"term":"wezen","score":8.501797086008965},{"term":"uitmaakt","score":8.490878128632879},{"term":"instemm","score":8.490878128632879},{"term":"] -","score":8.489958249991986},{"term":"behoord","score":8.489590535334413},{"term":"onderhandel","score":8.489039216750202},{"term":"ingezet","score":8.485554573150752},{"term":"miljoen","score":8.478803338676586},{"term":"regulier","score":8.476442062490907},{"term":"herkomst","score":8.473181780668021},{"term":"ver","score":8.472820182399753},{"term":"zicht","score":8.472458714837531},{"term":"verworv","score":8.46993209374968},{"term":"mond","score":8.469571668443923},{"term":"bewoord","score":8.466154091594733},{"term":"grotendel","score":8.465974542263073},{"term":"degelijk","score":8.462569215914703},{"term":"overgenom","score":8.461675001422497},{"term":"negatiev","score":8.460960205109004},{"term":"recent","score":8.441851343410956},{"term":"verwerv","score":8.435562014503393},{"term":"nimmer","score":8.433474331072908},{"term":"toereik","score":8.432258525452019},{"term":"voorshand","score":8.431217581568989},{"term":"herstell","score":8.42965819461174},{"term":"onmog","score":8.429138938866624},{"term":"komend","score":8.422928810231637},{"term":"jaarlijk","score":8.406554574360033},{"term":"arm","score":8.40149212682866},{"term":"voorgeschrev","score":8.392610627307793},{"term":"diezelfd","score":8.387452116825546},{"term":"voorgedan","score":8.380999947862195},{"term":"kanton","score":8.377707221805219},{"term":"woont","score":8.368219174183634},{"term":"groter","score":8.36269878347979},{"term":"vergoedt","score":8.35882030415122},{"term":"afgerond","score":8.352389413820928},{"term":"uitgelegd","score":8.342343885671292},{"term":"casu","score":8.338385365736912},{"term":"toegelat","score":8.326290660853568},{"term":"afgeslot","score":8.315729916638892},{"term":"functioner","score":8.314340491482909},{"term":"inderdad","score":8.30176942297442},{"term":"tred","score":8.293878638480875},{"term":"objectiev","score":8.28815141860598},{"term":"hoeft","score":8.287550457049432},{"term":"kant","score":8.285899672552096},{"term":"gerechtigd","score":8.281262069805344},{"term":"toegezond","score":8.271163973898343},{"term":"hoeverr","score":8.268655302915432},{"term":"meegenom","score":8.255041684192191},{"term":"aangevraagd","score":8.251848184599051},{"term":"aangan","score":8.251268642421138},{"term":"aangetoond","score":8.247942777161796},{"term":",=","score":8.236459946813868},{"term":"telefoongesprek","score":8.229916537976223},{"term":"tekort","score":8.226519507108618},{"term":"gerealiseerd","score":8.22200800925492},{"term":"regelmat","score":8.213742920529635},{"term":"aanhang","score":8.21234880347685},{"term":"reager","score":8.212209498607919},{"term":"liep","score":8.205407223472283},{"term":"commissie","score":8.196866050181699},{"term":"ontving","score":8.18866928297752},{"term":"saldo","score":8.183918680218921},{"term":"ondernemingskamer","score":8.180404213233043},{"term":"thuis","score":8.169400344820092},{"term":"gold","score":8.163943486932416},{"term":"discussie","score":8.163943486932416},{"term":"zogenoemd","score":8.145792258621128},{"term":"nagelat","score":8.14383900583527},{"term":"tegensprak","score":8.137226339923943},{"term":"effect","score":8.128476927443842},{"term":"hoord","score":8.122856064407948},{"term":"functies","score":8.122856064407948},{"term":"onbegrijp","score":8.119803403680718},{"term":"toegerek","score":8.112968799174578},{"term":"pass","score":8.112086242223892},{"term":"gaf","score":8.104428179564112},{"term":"’,","score":8.097076579573327},{"term":"verschijn","score":8.091135232723676},{"term":"financieel","score":8.087562700436164},{"term":"anderszin","score":8.084370555306899},{"term":"gewicht","score":8.08412542722625},{"term":"verzekeringsart","score":8.078869655181652},{"term":"manier","score":8.077894877475613},{"term":"medegedeeld","score":8.042597799370823},{"term":"verled","score":8.040249277859246},{"term":"gekocht","score":8.03802327963471},{"term":"toekomt","score":8.029400527537003},{"term":"tekortkom","score":8.014658811764546},{"term":"verlat","score":8.005327838282819},{"term":"uitgaand","score":7.974765566679113},{"term":"betrof","score":7.957989574117191},{"term":"meter","score":7.953355196043055},{"term":"afgesprok","score":7.938625502898069},{"term":"staand","score":7.9364029269712315},{"term":"tal","score":7.935029518060615},{"term":"herhaald","score":7.919629287031786},{"term":"voorbij","score":7.9095955850623385},{"term":"begrijp","score":7.909081102363333},{"term":"briev","score":7.892553486808218},{"term":"diend","score":7.885093774796317},{"term":"voordat","score":7.881284628249902},{"term":"trek","score":7.862748140722121},{"term":"vall","score":7.855799250514311},{"term":"hieran","score":7.843777777579123},{"term":"norm","score":7.840794854992061},{"term":"- [","score":7.821101501047045},{"term":"gekoz","score":7.808184810793875},{"term":"blek","score":7.798191204904366},{"term":"geschad","score":7.771930098296137},{"term":"ingegan","score":7.767009488949374},{"term":"concludeert","score":7.7547687826253595},{"term":"zegt","score":7.7450305822306},{"term":"richt","score":7.697380199463026},{"term":"ondank","score":7.68876017095288},{"term":"vormt","score":7.658281156781433},{"term":"toch","score":7.65444606099749},{"term":"vgl","score":7.6431859309244246},{"term":"waarna","score":7.620113980003288},{"term":"bied","score":7.609536737219863},{"term":"besprok","score":7.589010812853185},{"term":"evenwel","score":7.5780167933383975},{"term":"rust","score":7.546099577161102},{"term":"inzicht","score":7.52956440995245},{"term":"nj","score":7.510463097235636},{"term":"twijfel","score":7.477588634544098},{"term":"neergelegd","score":7.4506918252753245},{"term":"passend","score":7.402812704313307},{"term":"spreekt","score":7.395276687426518},{"term":"begrijpt","score":7.378797828109433},{"term":"vindt","score":7.345754190745411},{"term":"vertegenwoord","score":7.3193418136457025},{"term":"daarna","score":7.177192064786735},{"term":"mail","score":7.1512571785555235},{"term":"weliswar","score":7.033072442899362}];

/***/ },
/* 303 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _getTermFrequencyData = __webpack_require__(304);

	var _getTermFrequencyData2 = _interopRequireDefault(_getTermFrequencyData);

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _underscore = __webpack_require__(210);

	var _underscore2 = _interopRequireDefault(_underscore);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _PercentageBar = __webpack_require__(294);

	var _PercentageBar2 = _interopRequireDefault(_PercentageBar);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	function getTablesForTfIdfScores(tfIdfScores) {
	    var tables = [];
	    for (var sectionRole in tfIdfScores) {
	        //noinspection JSUnfilteredForInLoop
	        var tfIdfScoresForRole = tfIdfScores[sectionRole];
	        //noinspection JSUnfilteredForInLoop
	        tables.push(_react2.default.createElement(
	            'table',
	            { key: sectionRole, className: 'table inline-table' },
	            _react2.default.createElement(
	                'caption',
	                null,
	                sectionRole
	            ),
	            _react2.default.createElement(
	                'thead',
	                null,
	                _react2.default.createElement(
	                    'tr',
	                    null,
	                    _react2.default.createElement('th', null),
	                    _react2.default.createElement(
	                        'th',
	                        null,
	                        'term'
	                    ),
	                    _react2.default.createElement(
	                        'th',
	                        null,
	                        'tf-idf score'
	                    )
	                )
	            ),
	            _react2.default.createElement(
	                'tbody',
	                null,
	                _underscore2.default.map(tfIdfScoresForRole, function (term, o) {
	                    var tfIdfScoreStr = term[1].toFixed(2);
	                    return _react2.default.createElement(
	                        'tr',
	                        { key: term[0] },
	                        _react2.default.createElement(
	                            'th',
	                            { className: 'nr' },
	                            o + 1
	                        ),
	                        _react2.default.createElement(
	                            'td',
	                            null,
	                            term[0]
	                        ),
	                        _react2.default.createElement(
	                            'td',
	                            { className: 'tf-idf-score' },
	                            _react2.default.createElement(_PercentageBar2.default, { percentage: term[2], text: tfIdfScoreStr + '' }),
	                            _react2.default.createElement(
	                                'span',
	                                { className: 'perc-text' },
	                                tfIdfScoreStr
	                            )
	                        )
	                    );
	                })
	            )
	        ));
	    }
	    return tables;
	}

	var FigureSectionTitleTfIdf = function (_React$Component) {
	    _inherits(FigureSectionTitleTfIdf, _React$Component);

	    function FigureSectionTitleTfIdf() {
	        _classCallCheck(this, FigureSectionTitleTfIdf);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(FigureSectionTitleTfIdf).apply(this, arguments));
	    }

	    _createClass(FigureSectionTitleTfIdf, [{
	        key: 'render',
	        value: function render() {
	            var nTerms = 10;

	            var tfIdfScores = (0, _getTermFrequencyData2.default)(nTerms);

	            var _loop = function _loop() {
	                //noinspection JSUnfilteredForInLoop
	                var tfIdfScoresForRole = tfIdfScores[sectionRole];

	                var maxVal = tfIdfScoresForRole[0][1];
	                _underscore2.default.map(tfIdfScoresForRole, function (arr) {
	                    arr[2] = (100 * arr[1] / maxVal).toFixed(2) + "%";
	                });
	            };

	            for (var sectionRole in tfIdfScores) {
	                _loop();
	            }

	            return _react2.default.createElement(
	                'figure',
	                { className: 'chart', id: _figs2.default.tfidf.id },
	                _react2.default.createElement(
	                    'div',
	                    null,
	                    getTablesForTfIdfScores(tfIdfScores)
	                ),
	                _react2.default.createElement(
	                    'figcaption',
	                    null,
	                    _react2.default.createElement(
	                        'span',
	                        { className: 'figure-number' },
	                        'Fig ',
	                        _figs2.default.tfidf.num,
	                        '.'
	                    ),
	                    ' Top ',
	                    nTerms,
	                    ' tf-idf scores for stemmed words in section titles, per section role. We take as document here any string block (such as a paragraph, or title). Stemming is performed using the ',
	                    _react2.default.createElement(
	                        'a',
	                        { href: 'http://snowball.tartarus.org/algorithms/dutch/stemmer.html' },
	                        'Snowball algorithm for Dutch'
	                    ),
	                    '.'
	                )
	            );
	        }
	    }]);

	    return FigureSectionTitleTfIdf;
	}(_react2.default.Component);

	exports.default = FigureSectionTitleTfIdf;

/***/ },
/* 304 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _underscore = __webpack_require__(210);

	var _underscore2 = _interopRequireDefault(_underscore);

	var _beslissing = __webpack_require__(305);

	var _beslissing2 = _interopRequireDefault(_beslissing);

	var _overwegingen = __webpack_require__(306);

	var _overwegingen2 = _interopRequireDefault(_overwegingen);

	var _procesverloop = __webpack_require__(307);

	var _procesverloop2 = _interopRequireDefault(_procesverloop);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	var rawData = {
	    beslissing: _beslissing2.default,
	    overwegingen: _overwegingen2.default,
	    procesverloop: _procesverloop2.default
	};

	function tfidfForAllTerms(topN) {
	    var returnObj = Object.create(null);
	    for (var sectionRole in rawData) {
	        if (rawData.hasOwnProperty(sectionRole)) {
	            var sorted = _underscore2.default.sortBy(rawData[sectionRole], function (e) {
	                return -e[1];
	            });
	            if (topN) {
	                sorted.splice(topN);
	            }
	            returnObj[sectionRole] = sorted;
	        }
	    }
	    return returnObj;
	}

	exports.default = tfidfForAllTerms;

/***/ },
/* 305 */
/***/ function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.default = [["besliss", 7.956591778188496], ["_NUM", 4.605893573909974], ["_ART", 3.08221514837768], ["van", 0.4703645139566689], ["slotsom", 0.3748379460007891], ["motiver", 0.2678897231806194], ["in", 0.20781478525805525], ["_PUNCT", 0.16850649254329103], ["beroep", 0.1639081639615639], ["hoger", 0.16005150128011533], ["grond", 0.10902488734094974], ["en", 0.10679988964011404], ["op", 0.0691232619059627], ["eerst", 0.04909828259844132], ["aanleg", 0.048949949418385603], ["geschil", 0.046724951717549895], ["rechtbank", 0.04079162451532133], ["vorder", 0.034413297772925636], ["verleng", 0.028479970570697077], ["terbeschikkingstell", 0.028331637390641363], [",", 0.026996638770139938], ["partij", 0.019431646587298526], ["beoordel", 0.015871650265961393], ["benadeeld", 0.01572331708590568], ["bewijs", 0.014981651185627108], ["feit", 0.014981651185627108], ["verder", 0.01468498482551568], ["inzak", 0.014239985285348538], ["voorwaard", 0.013349986205014255], ["tot", 0.013349986205014255], ["vor", 0.012163320764568544], ["geding", 0.01201498758451283], ["aan", 0.011421654864289973], ["[", 0.011124988504178545], ["verzoek", 0.010976655324122831], ["]", 0.010828322144067117], ["na", 0.010679988964011403], ["veroordel", 0.010679988964011403], ["met", 0.009493323523565692], ["-", 0.009493323523565692], [")", 0.009344990343509978], ["kort", 0.009196657163454264], ["gerechtshof", 0.00904832398339855], ["(", 0.008751657623287123], ["incident", 0.008603324443231409], ["te", 0.008009991723008553], ["bewijsbesliss", 0.008009991723008553], ["proceskost", 0.007268325822729983], ["kantonrechter", 0.007268325822729983], ["principal", 0.006971659462618555], ["dez", 0.006971659462618555], ["kost", 0.0066749931025071275], ["zak", 0.0066749931025071275], ["incidentel", 0.0066749931025071275], ["dat", 0.0063783267423957], ["is", 0.006229993562339986], ["/", 0.006229993562339986], ["vonnis", 0.005784994022172843], ["€", 0.0056366608421171294], ["appellant", 0.005339994482005702], ["over", 0.005339994482005702], ["verpleg", 0.004598328581727132], ["overheidsweg", 0.004598328581727132], ["heeft", 0.004449995401671418], ["veroordeelt", 0.004449995401671418], ["dor", 0.0043016622216157045], ["hof", 0.004153329041559991], ["daarop", 0.004153329041559991], ["bij", 0.004004995861504277], ["niet", 0.004004995861504277], ["ten", 0.004004995861504277], ["ge", 0.0038566626814485624], ["griev", 0.0037083295013928485], [",-", 0.0037083295013928485], ["zover", 0.0035599963213371346], ["voorzieningenrechter", 0.0034116631412814207], ["i", 0.0034116631412814207], ["aanzien", 0.0034116631412814207], ["waarvan", 0.003263329961225707], ["beid", 0.003263329961225707], ["af", 0.003114996781169993], ["conventie", 0.003114996781169993], ["reconventie", 0.003114996781169993], ["zijn", 0.003114996781169993], ["ntimeerd", 0.003114996781169993], ["begrot", 0.003114996781169993], ["beslag", 0.003114996781169993], ["wijst", 0.002966663601114279], ["om", 0.002966663601114279], ["verklaart", 0.002966663601114279], ["teg", 0.002966663601114279], ["eind", 0.002966663601114279], ["artikel", 0.002966663601114279], ["advocat", 0.002966663601114279], ["tuss", 0.0028183304210585647], ["word", 0.0028183304210585647], ["bedrag", 0.0028183304210585647], ["bekrachtigt", 0.0028183304210585647], ["die", 0.002669997241002851], ["dit", 0.002669997241002851], ["betrek", 0.002669997241002851], ["inleid", 0.002669997241002851], ["bestred", 0.002669997241002851], ["salaris", 0.002669997241002851], ["gen", 0.002521664060947137], ["vernietigt", 0.002521664060947137], ["rechtdoend", 0.002521664060947137], ["heden", 0.002521664060947137], ["zijd", 0.002521664060947137], ["januari", 0.002373330880891423], ["of", 0.002373330880891423], ["uitgesprok", 0.002373330880891423], ["als", 0.002373330880891423], ["procedur", 0.002373330880891423], ["verschot", 0.002373330880891423], ["geliquideerd", 0.002373330880891423], ["ander", 0.002224997700835709], ["wettelijk", 0.002224997700835709], ["zal", 0.002224997700835709], ["d", 0.002224997700835709], ["ieder", 0.0020766645207799953], ["nader", 0.0020766645207799953], ["voorlop", 0.0020766645207799953], ["voortgezet", 0.0020766645207799953], ["betal", 0.0020766645207799953], ["opnieuw", 0.0020766645207799953], ["gevall", 0.0020766645207799953], ["april", 0.0019283313407242812], ["mer", 0.0019283313407242812], ["oktober", 0.0019283313407242812], ["gewez", 0.0019283313407242812], ["voorzitter", 0.0017799981606685673], ["omtrent", 0.0017799981606685673], ["nummer", 0.0017799981606685673], ["juli", 0.0017799981606685673], ["“", 0.0017799981606685673], ["aanduid", 0.0017799981606685673], ["rent", 0.0017799981606685673], ["voldoen", 0.0017799981606685673], ["vermeerder", 0.0017799981606685673], ["onder", 0.0017799981606685673], ["genom", 0.0017799981606685673], ["vanaf", 0.0017799981606685673], ["uitvoer", 0.0017799981606685673], ["voorrad", 0.0017799981606685673], ["februari", 0.0017799981606685673], ["houdt", 0.0016316649806128534], ["open", 0.0016316649806128534], ["c", 0.0016316649806128534], ["appel", 0.0016316649806128534], ["voorwerp", 0.0016316649806128534], ["overweg", 0.0016316649806128534], ["stat", 0.0014833318005571395], ["recht", 0.0014833318005571395], ["oordel", 0.0014833318005571395], ["dag", 0.0014833318005571395], ["hechtenis", 0.0014833318005571395], ["nar", 0.0014833318005571395], ["e", 0.0014833318005571395], ["bezwar", 0.0014833318005571395], ["betreft", 0.0013349986205014254], ["ter", 0.0013349986205014254], ["rechter", 0.0013349986205014254], ["slachtoffer", 0.0013349986205014254], ["december", 0.0013349986205014254], ["zoverr", 0.0013349986205014254], ["tijdstip", 0.0013349986205014254], ["conclusie", 0.0013349986205014254], ["verzoeker", 0.0013349986205014254], ["uitsprak", 0.0011866654404457115], ["zoal", 0.0011866654404457115], ["ongegrond", 0.0011866654404457115], ["bijkom", 0.0011866654404457115], ["er", 0.0011866654404457115], ["onderzoek", 0.0011866654404457115], ["proceskostenveroordel", 0.0011866654404457115], ["gevorderd", 0.0011866654404457115], ["tussenbesliss", 0.0010383322603899976], ["verdacht", 0.0010383322603899976], ["november", 0.0010383322603899976], ["zij", 0.0010383322603899976], ["har", 0.0010383322603899976], ["verwijz", 0.0010383322603899976], ["kernpunt", 0.0010383322603899976], ["verwijst", 0.0010383322603899976], ["le", 0.0010383322603899976], ["mr", 0.0010383322603899976], ["afwijz", 0.0010383322603899976], ["aangevoerd", 0.0008899990803342836], ["uit", 0.0008899990803342836], ["nog", 0.0008899990803342836], ["moet", 0.0008899990803342836], ["bijzonder", 0.0008899990803342836], ["september", 0.0008899990803342836], ["augustus", 0.0008899990803342836], ["mei", 0.0008899990803342836], ["ook", 0.0008899990803342836], ["zich", 0.0008899990803342836], ["verwer", 0.0008899990803342836], ["commissaris", 0.0008899990803342836], ["s", 0.0008899990803342836], ["akt", 0.0008899990803342836], ["rol", 0.0008899990803342836], ["wat", 0.0008899990803342836], ["veertien", 0.0008899990803342836], ["teruggegev", 0.0008899990803342836], ["dient", 0.0008899990803342836], ["behor", 0.0008899990803342836], ["subsidiair", 0.0008899990803342836], ["getuig", 0.0007416659002785698], ["ontvank", 0.0007416659002785698], ["dan", 0.0007416659002785698], ["voorzien", 0.0007416659002785698], ["hoofdzak", 0.0007416659002785698], ["der", 0.0007416659002785698], [",-,", 0.0007416659002785698], ["all", 0.0007416659002785698], ["plaatsing", 0.0007416659002785698], ["inricht", 0.0007416659002785698], ["jeugdig", 0.0007416659002785698], ["toepass", 0.0007416659002785698], ["bepal", 0.0007416659002785698], ["vaststell", 0.0007416659002785698], ["hebb", 0.0007416659002785698], ["terechtzit", 0.0007416659002785698], ["vermeld", 0.0007416659002785698], ["’", 0.0007416659002785698], ["betreff", 0.0007416659002785698], ["bestat", 0.0007416659002785698], ["wijzig", 0.0007416659002785698], ["alsnog", 0.0007416659002785698], ["griffier", 0.0007416659002785698], ["dinsdag", 0.0007416659002785698], ["uitlat", 0.0007416659002785698], ["justitie", 0.0007416659002785698], ["gegev", 0.0007416659002785698], ["- €", 0.0007416659002785698], ["ondernem", 0.0007416659002785698], ["leid", 0.0007416659002785698], ["kan", 0.0007416659002785698], ["alvoren", 0.0007416659002785698], ["eventueel", 0.0007416659002785698], ["daarvor", 0.0007416659002785698], ["zwoll", 0.0007416659002785698], ["lelystad", 0.0007416659002785698], ["aangaand", 0.0005933327202228558], ["besprek", 0.0005933327202228558], ["lid", 0.0005933327202228558], ["wet", 0.0005933327202228558], ["verzet", 0.0005933327202228558], ["rechtsmiddel", 0.0005933327202228558], ["hij", 0.0005933327202228558], ["verklaard", 0.0005933327202228558], ["doend", 0.0005933327202228558], ["a", 0.0005933327202228558], ["omstand", 0.0005933327202228558], ["algemen", 0.0005933327202228558], ["colleg", 0.0005933327202228558], ["verded", 0.0005933327202228558], ["b", 0.0005933327202228558], ["hervat", 0.0005933327202228558], ["verstekvonnis", 0.0005933327202228558], ["toegewez", 0.0005933327202228558], ["per", 0.0005933327202228558], ["vervolgen", 0.0005933327202228558], ["punt", 0.0005933327202228558], ["verzocht", 0.0005933327202228558], ["aangezien", 0.0005933327202228558], ["gegeven", 0.0005933327202228558], ["aanwez", 0.0005933327202228558], ["”", 0.0005933327202228558], ["havr", 0.0005933327202228558], ["schadevergoed", 0.0005933327202228558], ["schad", 0.0005933327202228558], ["schip", 0.0005933327202228558], ["grief", 0.0005933327202228558], ["wordt", 0.0005933327202228558], ["hun", 0.0005933327202228558], ["j", 0.0005933327202228558], ["n", 0.0005933327202228558], ["ex", 0.0005933327202228558], ["geval", 0.0005933327202228558], ["nasalaris", 0.0005933327202228558], ["binn", 0.0005933327202228558], ["vonniss", 0.0005933327202228558], ["overeenkomst", 0.0005933327202228558], ["wetboek", 0.0005933327202228558], ["omdat", 0.0005933327202228558], ["onvoldo", 0.0005933327202228558], ["rapport", 0.0005933327202228558], ["gelast", 0.0005933327202228558], ["wanner", 0.0005933327202228558], ["eig", 0.0005933327202228558], ["herzien", 0.0005933327202228558], ["arrest", 0.0005933327202228558], ["h", 0.0005933327202228558], ["verricht", 0.0005933327202228558], ["bedoeld", 0.0005933327202228558], ["geldt", 0.0005933327202228558], ["lc", 0.0005933327202228558], ["leeuward", 0.0005933327202228558], ["wetterskip", 0.0005933327202228558], ["instructie", 0.0004449995401671418], ["iiibesliss", 0.0004449995401671418], ["iibesliss", 0.0004449995401671418], ["straf", 0.0004449995401671418], ["indien", 0.0004449995401671418], ["zitting", 0.0004449995401671418], ["redelijkerwijs", 0.0004449995401671418], ["ad", 0.0004449995401671418], ["namen", 0.0004449995401671418], ["genoemd", 0.0004449995401671418], ["had", 0.0004449995401671418], ["was", 0.0004449995401671418], ["opmerk", 0.0004449995401671418], ["ingang", 0.0004449995401671418], ["algehel", 0.0004449995401671418], ["herroep", 0.0004449995401671418], ["verschuldigd", 0.0004449995401671418], ["aansprak", 0.0004449995401671418], ["gemaakt", 0.0004449995401671418], ["rechtsoverweg", 0.0004449995401671418], ["bw", 0.0004449995401671418], ["afgewez", 0.0004449995401671418], ["nieuw", 0.0004449995401671418], ["gesteld", 0.0004449995401671418], ["nu", 0.0004449995401671418], ["slecht", 0.0004449995401671418], ["aangehoud", 0.0004449995401671418], ["tpi", 0.0004449995401671418], ["des", 0.0004449995401671418], ["l", 0.0004449995401671418], ["ingesteld", 0.0004449995401671418], ["nem", 0.0004449995401671418], ["vast", 0.0004449995401671418], ["primair", 0.0004449995401671418], ["fal", 0.0004449995401671418], ["aanvull", 0.0004449995401671418], ["verweerder", 0.0004449995401671418], ["_NUMvor", 0.0004449995401671418], ["deskundigenbericht", 0.0004449995401671418], ["bewijsvoer", 0.0004449995401671418], ["gevoerd", 0.0004449995401671418], ["goeder", 0.0004449995401671418], ["voldan", 0.0004449995401671418], ["beteken", 0.0004449995401671418], ["plaatsgevond", 0.0004449995401671418], ["*", 0.0004449995401671418], ["omvang", 0.0004449995401671418], ["beschik", 0.0004449995401671418], ["burger", 0.0004449995401671418], ["hor", 0.0004449995401671418], ["–", 0.0004449995401671418], ["jar", 0.0004449995401671418], ["hem", 0.0004449995401671418], ["goed", 0.0004449995401671418], ["aegon", 0.0004449995401671418], ["beperk", 0.0004449995401671418], ["aangenom", 0.0004449995401671418], ["kruithof", 0.0004449995401671418], ["bindend", 0.0004449995401671418], ["houd", 0.0004449995401671418], ["gehandeld", 0.0004449995401671418], ["benoem", 0.0004449995401671418], ["nihil", 0.0004449995401671418], ["hek", 0.0004449995401671418], ["tegenwoord", 0.0004449995401671418], ["bekracht", 0.0004449995401671418], ["_NUMa", 0.0004449995401671418], ["curator", 0.0004449995401671418], ["afdoen", 0.0004449995401671418], ["hoofdelijk", 0.0004449995401671418], ["inbeslaggenom", 0.0004449995401671418], ["definities", 0.0004449995401671418], ["begrip", 0.0004449995401671418], ["‘", 0.0004449995401671418], ["rijnvar", 0.0004449995401671418], ["werknemer", 0.0004449995401671418], ["zelfstand", 0.0004449995401671418], ["verstan", 0.0004449995401671418], [", (…),", 0.0004449995401671418], ["varend", 0.0004449995401671418], ["personel", 0.0004449995401671418], ["beroepsarbeid", 0.0004449995401671418], ["boord", 0.0004449995401671418], ["winstoogmerk", 0.0004449995401671418], ["rijnvaart", 0.0004449995401671418], ["gebruikt", 0.0004449995401671418], ["certificat", 0.0004449995401671418], ["rijnvaartakt", 0.0004449995401671418], [", (…)", 0.0004449995401671418], ["uitdruk", 0.0004449995401671418], ["waartoe", 0.0004449995401671418], ["behoort", 0.0004449995401671418], ["vennootschap", 0.0004449995401671418], ["betrok", 0.0004449995401671418], ["exploiteert", 0.0004449995401671418], ["ongeacht", 0.0004449995401671418], ["eigenar", 0.0004449995401671418], ["meerder", 0.0004449995401671418], ["vennootschapp", 0.0004449995401671418], ["xploiteerd", 0.0004449995401671418], ["exploitant", 0.0004449995401671418], ["daadwerk", 0.0004449995401671418], ["beslissingsbevoegd", 0.0004449995401671418], ["economisch", 0.0004449995401671418], ["commerci", 0.0004449995401671418], ["management", 0.0004449995401671418], ["rijnvaartverklar", 0.0004449995401671418], ["maatgev", 0.0004449995401671418], ["afrond", 0.0004449995401671418], ["aangemerkt", 0.0004449995401671418], ["juni", 0.0004449995401671418], ["jegen", 0.0004449995401671418], ["bepaalt", 0.0004449995401671418], ["onrechtmat", 0.0004449995401671418], ["werkzam", 0.0004449995401671418], ["bevestigt", 0.0002966663601114279], ["wraking", 0.0002966663601114279], ["dictum", 0.0002966663601114279], ["omschrev", 0.0002966663601114279], ["oplegg", 0.0002966663601114279], ["kamer", 0.0002966663601114279], ["gelet", 0.0002966663601114279], ["geslot", 0.0002966663601114279], ["klacht", 0.0002966663601114279], ["zou", 0.0002966663601114279], ["voorgaand", 0.0002966663601114279], ["weg", 0.0002966663601114279], ["ilt", 0.0002966663601114279], ["gegrond", 0.0002966663601114279], ["del", 0.0002966663601114279], ["overwog", 0.0002966663601114279], ["sprak", 0.0002966663601114279], ["zin", 0.0002966663601114279], ["volgen", 0.0002966663601114279], ["pro", 0.0002966663601114279], ["forma", 0.0002966663601114279], ["informatie", 0.0002966663601114279], ["politierechter", 0.0002966663601114279], ["vermeerderd", 0.0002966663601114279], ["vernietigd", 0.0002966663601114279], ["behouden", 0.0002966663601114279], ["daarin", 0.0002966663601114279], ["beslissingskader", 0.0002966663601114279], ["inmiddel", 0.0002966663601114279], ["sol", 0.0002966663601114279], ["mallorca", 0.0002966663601114279], ["voorafgaand", 0.0002966663601114279], ["aanpass", 0.0002966663601114279], ["deskund", 0.0002966663601114279], ["zie", 0.0002966663601114279], ["conform", 0.0002966663601114279], ["] (", 0.0002966663601114279], ["uitdraai", 0.0002966663601114279], ["brp", 0.0002966663601114279], ["dossier", 0.0002966663601114279], ["toe", 0.0002966663601114279], ["voeg", 0.0002966663601114279], ["zoud", 0.0002966663601114279], ["noodzak", 0.0002966663601114279], ["war", 0.0002966663601114279], ["system", 0.0002966663601114279], ["mar", 0.0002966663601114279], ["hetgen", 0.0002966663601114279], ["geblek", 0.0002966663601114279], ["geword", 0.0002966663601114279], ["overigen", 0.0002966663601114279], ["bevoegd", 0.0002966663601114279], ["onderhav", 0.0002966663601114279], ["hoofdprocedur", 0.0002966663601114279], ["onbevoegd", 0.0002966663601114279], ["t", 0.0002966663601114279], ["vergoed", 0.0002966663601114279], ["gevolg", 0.0002966663601114279], ["wijz", 0.0002966663601114279], ["kennis", 0.0002966663601114279], ["than", 0.0002966663601114279], ["mag", 0.0002966663601114279], ["gedan", 0.0002966663601114279], ["m", 0.0002966663601114279], ["scher", 0.0002966663601114279], ["middel", 0.0002966663601114279], ["officier", 0.0002966663601114279], ["beperkt", 0.0002966663601114279], ["vaststaand", 0.0002966663601114279], ["gehuurd", 0.0002966663601114279], ["ontruimd", 0.0002966663601114279], ["woning", 0.0002966663601114279], ["dur", 0.0002966663601114279], ["aanleid", 0.0002966663601114279], ["verbalisant", 0.0002966663601114279], ["maart", 0.0002966663601114279], ["lever", 0.0002966663601114279], ["ontbond", 0.0002966663601114279], ["saver", 0.0002966663601114279], ["vco", 0.0002966663601114279], ["arnhem", 0.0002966663601114279], ["locatie", 0.0002966663601114279], ["arbeidsovereenkomst", 0.0002966663601114279], ["hierna", 0.0002966663601114279], ["gericht", 0.0002966663601114279], ["verworp", 0.0002966663601114279], ["geschond", 0.0002966663601114279], ["gunningsbesliss", 0.0002966663601114279], ["verlies", 0.0002966663601114279], ["verdienvermog", 0.0002966663601114279], ["berek", 0.0002966663601114279], ["althan", 0.0002966663601114279], ["vermeent", 0.0002966663601114279], ["gehoud", 0.0002966663601114279], ["uur", 0.0002966663601114279], ["griffierecht", 0.0002966663601114279], ["behandel", 0.0002966663601114279], ["veroordeeld", 0.0002966663601114279], ["gedur", 0.0002966663601114279], ["leent", 0.0002966663601114279], ["deelgeschil", 0.0002966663601114279], ["bovendien", 0.0002966663601114279], ["procesrecht", 0.0002966663601114279], ["instrument", 0.0002966663601114279], ["hand", 0.0002966663601114279], ["art", 0.0002966663601114279], ["stell", 0.0002966663601114279], ["stand", 0.0002966663601114279], ["betwist", 0.0002966663601114279], ["artos", 0.0002966663601114279], ["urenbeperk", 0.0002966663601114279], ["licht", 0.0002966663601114279], ["zo", 0.0002966663601114279], ["gebaseerd", 0.0002966663601114279], ["vastgesteld", 0.0002966663601114279], ["al", 0.0002966663601114279], ["gemachtigd", 0.0002966663601114279], ["rolraadsher", 0.0002966663601114279], ["herstelbesliss", 0.0002966663601114279], ["onderzoekswens", 0.0002966663601114279], ["strafvorder", 0.0002966663601114279], ["hersteld", 0.0002966663601114279], ["voortzet", 0.0002966663601114279], ["invrijheidstell", 0.0002966663601114279], ["bekrachtigd", 0.0002966663601114279], ["tarief", 0.0002966663601114279], ["moment", 0.0002966663601114279], ["invorderingsbesliss", 0.0002966663601114279], ["last", 0.0002966663601114279], ["gelegd", 0.0002966663601114279], ["tussenvonnis", 0.0002966663601114279], ["eindvonnis", 0.0002966663601114279], ["compenseert", 0.0002966663601114279], ["drag", 0.0002966663601114279], ["bot", 0.0002966663601114279], ["instanties", 0.0002966663601114279], ["verhoogd", 0.0002966663601114279], ["uw", 0.0002966663601114279], ["ingevolg", 0.0002966663601114279], ["teruggav", 0.0002966663601114279], ["deg", 0.0002966663601114279], ["gehecht", 0.0002966663601114279], ["lijst", 0.0002966663601114279], ["voort", 0.0002966663601114279], ["hoofdsom", 0.0002966663601114279], ["beroepschrift", 0.0002966663601114279], ["administratief", 0.0002966663601114279], ["noord", 0.0002966663601114279], ["nederland", 0.0002966663601114279], ["groning", 0.0002966663601114279], ["den", 0.0002966663601114279], ["hag", 0.0002966663601114279], ["doordat", 0.0002966663601114279], ["bevrijd", 0.0002966663601114279], ["bevestigd", 0.0002966663601114279], ["rabobank", 0.0002966663601114279], ["verdel", 0.0002966663601114279], ["gea", 0.0002966663601114279], ["tussenkomst", 0.0002966663601114279], ["proceskostenbesliss", 0.0002966663601114279], ["hen", 0.0002966663601114279], ["grondwater", 0.0002966663601114279], ["percel", 0.0002966663601114279], ["dar", 0.0002966663601114279], ["aldus", 0.0002966663601114279], ["inspecteur", 0.00014833318005571394], ["luidt", 0.00014833318005571394], ["krijgt", 0.00014833318005571394], ["geleg", 0.00014833318005571394], ["breng", 0.00014833318005571394], ["hieronder", 0.00014833318005571394], ["akteverzoek", 0.00014833318005571394], ["notarisambt", 0.00014833318005571394], ["geldend", 0.00014833318005571394], ["kon", 0.00014833318005571394], ["klager", 0.00014833318005571394], ["plaatsvervang", 0.00014833318005571394], ["doen", 0.00014833318005571394], ["stelsel", 0.00014833318005571394], ["brief", 0.00014833318005571394], ["secretariaat", 0.00014833318005571394], ["afgeleid", 0.00014833318005571394], ["behandeld", 0.00014833318005571394], ["doet", 0.00014833318005571394], ["los", 0.00014833318005571394], ["vrag", 0.00014833318005571394], ["mocht", 0.00014833318005571394], ["afleid", 0.00014833318005571394], ["leidt", 0.00014833318005571394], ["ertoe", 0.00014833318005571394], ["rechtskracht", 0.00014833318005571394], ["euclaim", 0.00014833318005571394], ["passagier", 0.00014833318005571394], ["_NUMen", 0.00014833318005571394], ["handhav", 0.00014833318005571394], ["verorden", 0.00014833318005571394], ["ingedi", 0.00014833318005571394], ["voorganger", 0.00014833318005571394], ["inspectie", 0.00014833318005571394], ["verker", 0.00014833318005571394], ["waterstat", 0.00014833318005571394], ["ministerie", 0.00014833318005571394], ["- ,", 0.00014833318005571394], ["hebbend", 0.00014833318005571394], ["vertrag", 0.00014833318005571394], ["hier", 0.00014833318005571394], ["zijnd", 0.00014833318005571394], ["vlucht", 0.00014833318005571394], ["_NUMdel", 0.00014833318005571394], ["relevant", 0.00014833318005571394], ["vervoerder", 0.00014833318005571394], ["aangetoond", 0.00014833318005571394], ["nadien", 0.00014833318005571394], ["zg", 0.00014833318005571394], ["handhavingstraject", 0.00014833318005571394], ["gestart", 0.00014833318005571394], ["merkt", 0.00014833318005571394], ["awb", 0.00014833318005571394], ["zaterdag", 0.00014833318005571394], ["belangrijk", 0.00014833318005571394], ["betaalbesliss", 0.00014833318005571394], ["snl", 0.00014833318005571394], ["nam", 0.00014833318005571394], ["eiseress", 0.00014833318005571394], ["som", 0.00014833318005571394], [",--,", 0.00014833318005571394], ["aangevall", 0.00014833318005571394], ["ingaand", 0.00014833318005571394], ["drie", 0.00014833318005571394], ["meest", 0.00014833318005571394], ["prangend", 0.00014833318005571394], ["kwestie", 0.00014833318005571394], ["jaarhur", 0.00014833318005571394], ["huurcontract", 0.00014833318005571394], ["juncto", 0.00014833318005571394], ["strekkend", 0.00014833318005571394], ["huurprijs", 0.00014833318005571394], ["markthuurwaard", 0.00014833318005571394], ["hiertoe", 0.00014833318005571394], ["aangewez", 0.00014833318005571394], ["gewend", 0.00014833318005571394], ["hur", 0.00014833318005571394], ["dus", 0.00014833318005571394], ["marktwaardemaatstaf", 0.00014833318005571394], ["dagvaard", 0.00014833318005571394], ["vorderingingedi", 0.00014833318005571394], ["volstan", 0.00014833318005571394], ["voer", 0.00014833318005571394], ["beslist", 0.00014833318005571394], ["maatstaf", 0.00014833318005571394], ["geklaagd", 0.00014833318005571394], ["pleitnota", 0.00014833318005571394], ["kunn", 0.00014833318005571394], ["nop", 0.00014833318005571394], ["toevoeg", 0.00014833318005571394], ["via", 0.00014833318005571394], ["webbr", 0.00014833318005571394], ["raadpleg", 0.00014833318005571394], ["uitgangspunt", 0.00014833318005571394], ["agh", 0.00014833318005571394], ["statusbeoordel", 0.00014833318005571394], ["toegevoegd", 0.00014833318005571394], ["dien", 0.00014833318005571394], ["discrepanties", 0.00014833318005571394], ["echter", 0.00014833318005571394], ["noch", 0.00014833318005571394], ["aannem", 0.00014833318005571394], ["totdat", 0.00014833318005571394], ["vaststat", 0.00014833318005571394], ["dezelfd", 0.00014833318005571394], ["namelijk", 0.00014833318005571394], ["la", 0.00014833318005571394], ["demand", 0.00014833318005571394], ["soci", 0.00014833318005571394], ["tendant", 0.00014833318005571394], ["à", 0.00014833318005571394], ["obtenir", 0.00014833318005571394], ["dommages", 0.00014833318005571394], ["et", 0.00014833318005571394], ["int", 0.00014833318005571394], ["r", 0.00014833318005571394], ["ê", 0.00014833318005571394], ["ts", 0.00014833318005571394], ["sur", 0.00014833318005571394], ["fondement", 0.00014833318005571394], ["articl", 0.00014833318005571394], ["afin", 0.00014833318005571394], ["parer", 0.00014833318005571394], ["pr", 0.00014833318005571394], ["judic", 0.00014833318005571394], ["con", 0.00014833318005571394], ["cutif", 0.00014833318005571394], ["saisie", 0.00014833318005571394], ["conservatoir", 0.00014833318005571394], ["du", 0.00014833318005571394], ["navir", 0.00014833318005571394], ["”,", 0.00014833318005571394], ["cod", 0.00014833318005571394], ["civil", 0.00014833318005571394], ["beslaglegg", 0.00014833318005571394], ["tijd", 0.00014833318005571394], ["aanhang", 0.00014833318005571394], ["ingetrok", 0.00014833318005571394], ["rouen", 0.00014833318005571394], ["intrekt", 0.00014833318005571394], ["hiermee", 0.00014833318005571394], ["onbevoegdverklar", 0.00014833318005571394], ["grondslag", 0.00014833318005571394], ["scheldepoort", 0.00014833318005571394], ["hierdor", 0.00014833318005571394], ["kom", 0.00014833318005571394], ["vervall", 0.00014833318005571394], ["behoev", 0.00014833318005571394], ["vooralsnog", 0.00014833318005571394], ["lat", 0.00014833318005571394], ["rust", 0.00014833318005571394], ["repla", 0.00014833318005571394], [", [", 0.00014833318005571394], ["reager", 0.00014833318005571394], ["jan", 0.00014833318005571394], ["mantgem", 0.00014833318005571394], ["joacim", 0.00014833318005571394], ["status", 0.00014833318005571394], ["anoniem", 0.00014833318005571394], ["getuiget", 0.00014833318005571394], ["verlen", 0.00014833318005571394], ["bopz", 0.00014833318005571394], ["rad", 0.00014833318005571394], ["tucht", 0.00014833318005571394], ["organogram", 0.00014833318005571394], ["bedrijv", 0.00014833318005571394], ["instantie", 0.00014833318005571394], ["be", 0.00014833318005571394], ["indigt", 0.00014833318005571394], ["huurovereenkomst", 0.00014833318005571394], ["person", 0.00014833318005571394], ["uiter", 0.00014833318005571394], ["uba", 0.00014833318005571394], ["dkcl", 0.00014833318005571394], ["aanschrijv", 0.00014833318005571394], ["dagteken", 0.00014833318005571394], ["handelsrent", 0.00014833318005571394], ["helft", 0.00014833318005571394], ["aed", 0.00014833318005571394], ["'", 0.00014833318005571394], ["serienummer", 0.00014833318005571394], ["beginn", 0.00014833318005571394], ["cardio", 0.00014833318005571394], ["cadrio", 0.00014833318005571394], ["sector", 0.00014833318005571394], ["kanton", 0.00014833318005571394], ["tiel", 0.00014833318005571394], ["tsb", 0.00014833318005571394], ["voet", 0.00014833318005571394], ["daarbij", 0.00014833318005571394], ["ontslagvergoed", 0.00014833318005571394], ["toegek", 0.00014833318005571394], ["zaaknummer", 0.00014833318005571394], ["onderbouwd", 0.00014833318005571394], ["fundamentel", 0.00014833318005571394], ["beginsel", 0.00014833318005571394], ["wederhor", 0.00014833318005571394], ["_NUMw", 0.00014833318005571394], ["rechtsvorder", 0.00014833318005571394], ["rv", 0.00014833318005571394], [") –", 0.00014833318005571394], ["samengevat", 0.00014833318005571394], ["vrij", 0.00014833318005571394], ["partner", 0.00014833318005571394], ["arbeidsbelon", 0.00014833318005571394], ["gev", 0.00014833318005571394], ["tabel", 0.00014833318005571394], ["onderan", 0.00014833318005571394], ["pagina", 0.00014833318005571394], ["eijk", 0.00014833318005571394], ["swienink", 0.00014833318005571394], [", €", 0.00014833318005571394], ["bedraagt", 0.00014833318005571394], ["nvesteerd", 0.00014833318005571394], ["voorschot", 0.00014833318005571394], ["titel", 0.00014833318005571394], ["],", 0.00014833318005571394], ["uurtarief", 0.00014833318005571394], ["exclusief", 0.00014833318005571394], ["%", 0.00014833318005571394], ["kantoorkost", 0.00014833318005571394], ["btw", 0.00014833318005571394], ["alsmed", 0.00014833318005571394], ["onderscheid", 0.00014833318005571394], ["voldoet", 0.00014833318005571394], ["eis", 0.00014833318005571394], ["krachten", 0.00014833318005571394], ["stan", 0.00014833318005571394], ["proeftijd", 0.00014833318005571394], ["strafbar", 0.00014833318005571394], ["schuldig", 0.00014833318005571394], ["allianz", 0.00014833318005571394], ["voert", 0.00014833318005571394], ["navolg", 0.00014833318005571394], ["deelgeschilprocedur", 0.00014833318005571394], ["opstell", 0.00014833318005571394], ["schaderegelingsproces", 0.00014833318005571394], ["rechtvaardigt", 0.00014833318005571394], ["gedacht", 0.00014833318005571394], ["vaststellingsovereenkomst", 0.00014833318005571394], ["komt", 0.00014833318005571394], ["wez", 0.00014833318005571394], ["ner", 0.00014833318005571394], ["waarvor", 0.00014833318005571394], ["plausibiliteitscriterium", 0.00014833318005571394], ["basis", 0.00014833318005571394], ["klachtenpatron", 0.00014833318005571394], ["plausibel", 0.00014833318005571394], ["objectief", 0.00014833318005571394], ["vindt", 0.00014833318005571394], ["plat", 0.00014833318005571394], ["geschiedt", 0.00014833318005571394], ["neurolog", 0.00014833318005571394], ["verzeker", 0.00014833318005571394], ["ongevalsgerelateerd", 0.00014833318005571394], ["gerapporteerd", 0.00014833318005571394], ["gezamen", 0.00014833318005571394], ["juist", 0.00014833318005571394], ["criteria", 0.00014833318005571394], ["gekom", 0.00014833318005571394], ["kwaliteit", 0.00014833318005571394], ["bruikbar", 0.00014833318005571394], ["pas", 0.00014833318005571394], ["nadat", 0.00014833318005571394], ["conceptrapport", 0.00014833318005571394], ["uitgat", 0.00014833318005571394], ["gebruik", 0.00014833318005571394], ["nek", 0.00014833318005571394], ["nodig", 0.00014833318005571394], ["werkdag", 0.00014833318005571394], ["hoofd", 0.00014833318005571394], ["bepaald", 0.00014833318005571394], ["dijk", 0.00014833318005571394], ["zelf", 0.00014833318005571394], ["aangegev", 0.00014833318005571394], ["afwez", 0.00014833318005571394], ["arbeidsduurbeperk", 0.00014833318005571394], ["geduid", 0.00014833318005571394], ["terwijl", 0.00014833318005571394], ["medisch", 0.00014833318005571394], ["objectiver", 0.00014833318005571394], ["stoorniss", 0.00014833318005571394], ["blijkt", 0.00014833318005571394], ["geest", 0.00014833318005571394], ["twed", 0.00014833318005571394], ["uitgegan", 0.00014833318005571394], ["toetsingsmaatstaf", 0.00014833318005571394], ["weinig", 0.00014833318005571394], ["onrecht", 0.00014833318005571394], ["terecht", 0.00014833318005571394], ["uitgebracht", 0.00014833318005571394], ["nogmal", 0.00014833318005571394], ["verzekeringsart", 0.00014833318005571394], ["arbeidsdeskund", 0.00014833318005571394], ["kostenbegrot", 0.00014833318005571394], ["volstrekt", 0.00014833318005571394], ["onnod", 0.00014833318005571394], ["onterecht", 0.00014833318005571394], ["bekend", 0.00014833318005571394], ["verondersteld", 0.00014833318005571394], ["deelgeschilrechter", 0.00014833318005571394], ["oploss", 0.00014833318005571394], ["passend", 0.00014833318005571394], ["voorhand", 0.00014833318005571394], ["erflater", 0.00014833318005571394], ["producties", 0.00014833318005571394], ["kuiper", 0.00014833318005571394], ["groefsema", 0.00014833318005571394], ["tenuitvoerlegg", 0.00014833318005571394], ["betekent", 0.00014833318005571394], ["vrouw", 0.00014833318005571394], ["faalt", 0.00014833318005571394], ["vermeend", 0.00014833318005571394], ["getuigenverzoek", 0.00014833318005571394], ["hierop", 0.00014833318005571394], ["volg", 0.00014833318005571394], ["onderzoekswen", 0.00014833318005571394], ["elk", 0.00014833318005571394], ["beslagbesliss", 0.00014833318005571394], ["wrakingskamer", 0.00014833318005571394], ["gerechthof", 0.00014833318005571394], ["doch", 0.00014833318005571394], ["allen", 0.00014833318005571394], ["interbank", 0.00014833318005571394], ["kredietvergoed", 0.00014833318005571394], ["legg", 0.00014833318005571394], ["waard", 0.00014833318005571394], ["toedel", 0.00014833318005571394], ["recreatievilla", 0.00014833318005571394], ["ameland", 0.00014833318005571394], ["pand", 0.00014833318005571394], ["adres", 0.00014833318005571394], ["vervolg", 0.00014833318005571394], ["standpunt", 0.00014833318005571394], ["_NUMg", 0.00014833318005571394], ["sr", 0.00014833318005571394], ["ongelijk", 0.00014833318005571394], ["tenlastelegg", 0.00014833318005571394], ["bewijsoverweg", 0.00014833318005571394], ["gedeputeerd", 0.00014833318005571394], ["schriftelijk", 0.00014833318005571394], ["_NUMbesliss", 0.00014833318005571394], ["vrijwaringszak", 0.00014833318005571394], ["bronnenbad", 0.00014833318005571394], ["belang", 0.00014833318005571394], ["opheff", 0.00014833318005571394], ["international", 0.00014833318005571394], ["karakter", 0.00014833318005571394], ["estro", 0.00014833318005571394], ["datum", 0.00014833318005571394], ["onbetaald", 0.00014833318005571394], ["geblev", 0.00014833318005571394], ["factur", 0.00014833318005571394], ["data", 0.00014833318005571394], ["geleid", 0.00014833318005571394], ["gezinsvoogdijinstell", 0.00014833318005571394], ["verzoekt", 0.00014833318005571394], ["machtig", 0.00014833318005571394], ["verstrek", 0.00014833318005571394], ["plaats", 0.00014833318005571394], ["minderjar", 0.00014833318005571394], ["pleegzorg", 0.00014833318005571394], ["ondertoezichtstell", 0.00014833318005571394], ["olthof", 0.00014833318005571394], ["bokhov", 0.00014833318005571394], ["general", 0.00014833318005571394], ["wetsartikel", 0.00014833318005571394], ["_NUMc", 0.00014833318005571394], ["_NUMb", 0.00014833318005571394], ["_NUMf", 0.00014833318005571394], ["lang", 0.00014833318005571394], ["brugg", 0.00014833318005571394], ["parketnummer_NUM", 0.00014833318005571394], ["bewez", 0.00014833318005571394], ["uitgeleverd", 0.00014833318005571394], ["stuk", 0.00014833318005571394], ["voormeld", 0.00014833318005571394], ["rechtsgeld", 0.00014833318005571394], ["convenant", 0.00014833318005571394], ["voornemen", 0.00014833318005571394], ["dier", 0.00014833318005571394], ["es", 0.00014833318005571394], ["rk", 0.00014833318005571394], ["vierd", 0.00014833318005571394], ["middelniet", 0.00014833318005571394], ["cassatie", 0.00014833318005571394], ["behoeft", 0.00014833318005571394], ["eiser", 0.00014833318005571394], ["enkelvoud", 0.00014833318005571394], ["gezag", 0.00014833318005571394], ["gewijsd", 0.00014833318005571394], ["eindbesliss", 0.00014833318005571394], ["?", 0.00014833318005571394], ["terugbetal", 0.00014833318005571394], ["€ €", 0.00014833318005571394], ["mat", 0.00014833318005571394], ["arbeidsongeschikt", 0.00014833318005571394], ["uitker", 0.00014833318005571394], ["slaagt", 0.00014833318005571394], ["zodat", 0.00014833318005571394], ["verhog", 0.00014833318005571394], ["toewijz", 0.00014833318005571394], ["(à €", 0.00014833318005571394], [", - )", 0.00014833318005571394], ["doorgeleid", 0.00014833318005571394], ["zesd", 0.00014833318005571394], ["brussel", 0.00014833318005571394], ["iibis", 0.00014833318005571394], ["commissariss", 0.00014833318005571394], ["alsrechthebb", 0.00014833318005571394], ["opd", 0.00014833318005571394], ["februari_NUM", 0.00014833318005571394], ["rechthebb", 0.00014833318005571394], ["omgang", 0.00014833318005571394], ["gang", 0.00014833318005571394], ["tussenvonniss", 0.00014833318005571394], ["verbeter", 0.00014833318005571394], ["hiervor", 0.00014833318005571394], ["),", 0.00014833318005571394], ["delta", 0.00014833318005571394], ["lloyd", 0.00014833318005571394], ["fikker", 0.00014833318005571394], ["wit", 0.00014833318005571394], ["aangevang", 0.00014833318005571394], ["rechtsweg", 0.00014833318005571394], ["geschorst", 0.00014833318005571394], ["eerstdien", 0.00014833318005571394], ["sindsdien", 0.00014833318005571394], ["rechtshandel", 0.00014833318005571394], ["nietig", 0.00014833318005571394], ["schorsing", 0.00014833318005571394], ["duurt", 0.00014833318005571394], ["kenbar", 0.00014833318005571394], ["maakt", 0.00014833318005571394], ["verificatie", 0.00014833318005571394], ["laatst", 0.00014833318005571394], ["afdel", 0.00014833318005571394], ["privaatrecht", 0.00014833318005571394], ["onderworp", 0.00014833318005571394], ["kwijting", 0.00014833318005571394], ["reken", 0.00014833318005571394], ["verzoekschrift", 0.00014833318005571394], ["ingekom", 0.00014833318005571394], ["the", 0.00014833318005571394], ["united", 0.00014833318005571394], ["states", 0.00014833318005571394], ["district", 0.00014833318005571394], ["court", 0.00014833318005571394], ["for", 0.00014833318005571394], ["delawar", 0.00014833318005571394], ["verenigd", 0.00014833318005571394], ["amerika", 0.00014833318005571394], ["verdrag", 0.00014833318005571394], ["verkrijg", 0.00014833318005571394], ["buitenland", 0.00014833318005571394], ["handelszak", 0.00014833318005571394], ["gravenhag", 0.00014833318005571394], ["bewijsverdrag", 0.00014833318005571394], ["getuigenverhor", 0.00014833318005571394], ["sub", 0.00014833318005571394], ["inachtnem", 0.00014833318005571394], ["aantal", 0.00014833318005571394], ["special", 0.00014833318005571394], ["vorm", 0.00014833318005571394], ["opgesteld", 0.00014833318005571394], ["opgenom", 0.00014833318005571394], ["bestemd", 0.00014833318005571394], ["waarder", 0.00014833318005571394], ["aandel", 0.00014833318005571394], ["dientengevolg", 0.00014833318005571394], ["geled", 0.00014833318005571394], ["lijd", 0.00014833318005571394], ["welk", 0.00014833318005571394], ["opgemaakt", 0.00014833318005571394], ["vereff", 0.00014833318005571394], ["(€", 0.00014833318005571394], ["dagvaardingskost", 0.00014833318005571394], ["+ €", 0.00014833318005571394], ["bewijslever", 0.00014833318005571394], ["gefourneerd", 0.00014833318005571394], ["procesdossier", 0.00014833318005571394], ["aktewissel", 0.00014833318005571394], ["zull", 0.00014833318005571394], ["fourner", 0.00014833318005571394], ["voormal", 0.00014833318005571394], ["kostenvergoed", 0.00014833318005571394], ["bewezenverklar", 0.00014833318005571394], ["wm", 0.00014833318005571394], ["verz", 0.00014833318005571394], ["ontvankelijkheidsincident", 0.00014833318005571394], ["[…]", 0.00014833318005571394], ["vve", 0.00014833318005571394], ["ondersplits", 0.00014833318005571394], ["verschotten", 0.00014833318005571394], ["duiding", 0.00014833318005571394], ["gom", 0.00014833318005571394], ["ouder", 0.00014833318005571394], ["onderl", 0.00014833318005571394], ["getroff", 0.00014833318005571394], ["regel", 0.00014833318005571394], ["zorg", 0.00014833318005571394], ["opvoedingstak", 0.00014833318005571394], ["onherroep", 0.00014833318005571394], ["parketnummer", 0.00014833318005571394], ["wenst", 0.00014833318005571394], ["verhor", 0.00014833318005571394], ["geschied", 0.00014833318005571394], ["overstan", 0.00014833318005571394], ["hierbij", 0.00014833318005571394], ["raadsher", 0.00014833318005571394], ["benoemd", 0.00014833318005571394], ["daartoe", 0.00014833318005571394], ["paleis", 0.00014833318005571394], ["wilhelminaplein", 0.00014833318005571394], ["wel", 0.00014833318005571394], ["|", 0.00014833318005571394], ["geschill", 0.00014833318005571394], ["salarisverzoek", 0.00014833318005571394], ["koopprijs", 0.00014833318005571394], ["hofstad", 0.00014833318005571394], ["rixtel", 0.00014833318005571394], ["ntimeerde_NUM", 0.00014833318005571394], ["ontbindt", 0.00014833318005571394], ["draagt", 0.00014833318005571394], ["verniet", 0.00014833318005571394], ["besluit", 0.00014833318005571394], ["verhal", 0.00014833318005571394], ["verklar", 0.00014833318005571394], ["kader", 0.00014833318005571394], ["vervang", 0.00014833318005571394], ["gemal", 0.00014833318005571394], ["onttrek", 0.00014833318005571394], ["waardor", 0.00014833318005571394], ["gan", 0.00014833318005571394], ["verzak", 0.00014833318005571394], ["mak", 0.00014833318005571394], ["gemotiveerd", 0.00014833318005571394], ["bewijsaanbod", 0.00014833318005571394], ["gepasseerd", 0.00014833318005571394], ["onderbouw", 0.00014833318005571394], ["gevoegd", 0.00014833318005571394], ["tijden", 0.00014833318005571394], ["werd", 0.00014833318005571394], ["uitgevoerd", 0.00014833318005571394], ["aannemer", 0.00014833318005571394], ["oosterhuis", 0.00014833318005571394], ["onttrok", 0.00014833318005571394], ["toegerek", 0.00014833318005571394], ["handel", 0.00014833318005571394], ["situatie", 0.00014833318005571394], ["opdrachtgever", 0.00014833318005571394], ["zeker", 0.00014833318005571394], ["eenheid", 0.00014833318005571394], ["stelling", 0.00014833318005571394], ["toezicht", 0.00014833318005571394], ["zorgplicht", 0.00014833318005571394], ["voorbij", 0.00014833318005571394], ["gegan", 0.00014833318005571394]];

/***/ },
/* 306 */
/***/ function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	exports.default = [["_NUM", 4.744733430442122], ["_ART", 4.6254735536773275], ["beoordel", 4.578600268779723], ["van", 2.5373873780330425], ["overweg", 2.204231055627909], ["beroep", 0.6894526208989584], ["middel", 0.5934810534029115], ["hoger", 0.4989928177074217], ["geschil", 0.4979544854470317], ["in", 0.44232954292613896], ["en", 0.40480124837204334], ["motiver", 0.32722299520290493], ["verder", 0.28984303382886506], ["grond", 0.2739713835629036], ["bewijs", 0.248903076133488], ["maatregel", 0.17725815016657817], ["ontvank", 0.15693650449894536], ["_PUNCT", 0.15678817131888964], ["straff", 0.12281987308613114], ["cassatie", 0.10561322419966833], ["eerst", 0.10279489377860976], ["verzoek", 0.09300490389493264], ["bewijsoverweg", 0.09300490389493264], ["strafmotiver", 0.0861815776123698], ["straf", 0.0808415831303641], ["sanctie", 0.07728158680902697], [")", 0.07297992458741126], ["klacht", 0.07297992458741126], ["daarvan", 0.07253492504724411], ["(", 0.07253492504724411], ["twed", 0.06318993470373414], ["ten", 0.0563666084211713], ["conventie", 0.05488327662061416], ["reconventie", 0.0542899439003913], ["s", 0.053548278000112734], ["vorder", 0.05295494527988988], ["incident", 0.05117494711922131], ["aanzien", 0.04983994849871989], ["of", 0.04835661669816275], ["principal", 0.04524161991699275], ["incidentel", 0.04227495631587847], ["/", 0.03945662589481991], ["voorgesteld", 0.037824960914207055], ["tot", 0.034413297772925636], ["bewijsmiddel", 0.03203996689203421], ["griev", 0.031298300991755644], ["rechtbank", 0.031001634631644213], ["derd", 0.0308533014515885], ["zak", 0.029666636011142788], ["dor", 0.02892497011086422], ["aanleg", 0.02862830375075279], ["over", 0.028034971030529935], ["uitsprak", 0.027738304670418507], ["bewijsmotiver", 0.027589971490362793], ["bestred", 0.02744163831030708], ["ambtshalv", 0.02744163831030708], ["feit", 0.026996638770139938], [",", 0.02521664060947137], ["met", 0.02269497654852423], ["vor", 0.021359977928022807], ["nader", 0.02076664520779995], ["hoofdzak", 0.0180966479667971], ["op", 0.014536651645459966], ["na", 0.014536651645459966], ["cassatieberoep", 0.014536651645459966], ["aanvrag", 0.013498319385069969], ["betrek", 0.0127566534847914], ["vierd", 0.012608320304735686], ["verwijz", 0.01201498758451283], ["te", 0.010976655324122831], ["is", 0.010828322144067117], ["bij", 0.010828322144067117], ["]", 0.010679988964011403], ["[", 0.01053165578395569], ["wrakingsverzoek", 0.01053165578395569], ["-", 0.00978998988367712], ["dat", 0.00978998988367712], ["namen", 0.00978998988367712], ["voorwaard", 0.009493323523565692], ["verzet", 0.009344990343509978], ["verdacht", 0.009196657163454264], ["partij", 0.008899990803342837], ["sancties", 0.008306658083119981], ["heeft", 0.008306658083119981], ["standpunt", 0.008306658083119981], ["aan", 0.008158324903064267], ["belanghebb", 0.007564992182841411], ["appel", 0.007416659002785697], ["daarvor", 0.007268325822729983], ["artikel", 0.007268325822729983], ["verzocht", 0.007119992642674269], ["staatssecretaris", 0.007119992642674269], ["vrijsprak", 0.007119992642674269], ["onder", 0.006971659462618555], ["beschouw", 0.006971659462618555], ["rechtsoverweg", 0.006823326282562841], ["verwer", 0.006526659922451414], ["herzien", 0.0063783267423957], ["omtrent", 0.006229993562339986], ["gelegd", 0.006229993562339986], ["zijn", 0.006081660382284272], ["vijfd", 0.005933327202228558], ["last", 0.005933327202228558], ["kantonrechter", 0.005933327202228558], ["word", 0.005784994022172843], ["bijzonder", 0.0054883276620614155], ["hof", 0.0054883276620614155], ["zaaknummer", 0.0054883276620614155], ["niet", 0.005339994482005702], ["hoofdelijk", 0.005191661301949988], ["teg", 0.005043328121894274], ["achtergrond", 0.004746661761782846], ["beid", 0.004598328581727132], ["vaststell", 0.004598328581727132], ["beklag", 0.004598328581727132], ["als", 0.004449995401671418], ["vaststaand", 0.0043016622216157045], ["eiser", 0.0043016622216157045], ["dez", 0.004153329041559991], ["legg", 0.004153329041559991], ["benadeeld", 0.004153329041559991], ["die", 0.004004995861504277], ["aangevoerd", 0.004004995861504277], ["“", 0.0038566626814485624], ["algemen", 0.0038566626814485624], ["bezwar", 0.0035599963213371346], ["om", 0.0035599963213371346], ["zij", 0.0035599963213371346], ["wraking", 0.0035599963213371346], ["ter", 0.003263329961225707], ["wet", 0.003263329961225707], ["geschill", 0.003114996781169993], ["procedur", 0.003114996781169993], ["eis", 0.003114996781169993], ["zesd", 0.002966663601114279], ["kan", 0.002966663601114279], ["jar", 0.002966663601114279], ["nieuw", 0.002966663601114279], ["lid", 0.0028183304210585647], ["vrijwaringszak", 0.002669997241002851], ["all", 0.002669997241002851], ["dit", 0.002669997241002851], ["ex", 0.002669997241002851], ["rv", 0.002669997241002851], ["inhoud", 0.002669997241002851], ["bewezenverklar", 0.002521664060947137], ["oordel", 0.002521664060947137], ["gen", 0.002373330880891423], ["person", 0.002373330880891423], ["verzoeker", 0.002373330880891423], ["belang", 0.002373330880891423], ["hebb", 0.002373330880891423], ["justitie", 0.002373330880891423], ["primair", 0.002373330880891423], ["e", 0.002224997700835709], ["ervan", 0.002224997700835709], ["eiswijz", 0.002224997700835709], ["betrok", 0.002224997700835709], ["officier", 0.002224997700835709], ["cassatiemiddel", 0.002224997700835709], ["stat", 0.002224997700835709], ["uitgangspunt", 0.002224997700835709], ["vrijspraakmotiver", 0.002224997700835709], ["tenlastegelegd", 0.0020766645207799953], ["schorsing", 0.0020766645207799953], ["tenlastelegg", 0.0020766645207799953], ["wijzig", 0.0020766645207799953], ["a", 0.0020766645207799953], ["omdat", 0.0020766645207799953], ["voorzien", 0.0020766645207799953], ["zevend", 0.0020766645207799953], ["onderdel", 0.0020766645207799953], ["rad", 0.0019283313407242812], ["gedaagd", 0.0019283313407242812], ["zich", 0.0019283313407242812], ["moet", 0.0019283313407242812], ["voorafgaand", 0.0019283313407242812], ["bevoegd", 0.0019283313407242812], ["besprek", 0.0019283313407242812], ["verker", 0.0017799981606685673], ["beschik", 0.0017799981606685673], ["er", 0.0017799981606685673], ["uit", 0.0017799981606685673], ["voort", 0.0017799981606685673], ["har", 0.0017799981606685673], ["recht", 0.0017799981606685673], ["bedoeld", 0.0017799981606685673], ["ander", 0.0017799981606685673], ["onttrek", 0.0016316649806128534], ["vrijwar", 0.0016316649806128534], ["hog", 0.0016316649806128534], ["inleid", 0.0016316649806128534], ["voorlop", 0.0016316649806128534], ["omstand", 0.0016316649806128534], ["was", 0.0016316649806128534], ["producties", 0.0016316649806128534], ["beslag", 0.0016316649806128534], ["voorzieningenrechter", 0.0016316649806128534], ["tussenkomst", 0.0016316649806128534], ["verweerder", 0.0016316649806128534], ["gegev", 0.0016316649806128534], ["wettelijk", 0.0016316649806128534], ["genoemd", 0.0016316649806128534], ["stelt", 0.0014833318005571395], ["zal", 0.0014833318005571395], ["geval", 0.0014833318005571395], ["veroordeeld", 0.0014833318005571395], ["nar", 0.0014833318005571395], ["subsidiair", 0.0014833318005571395], ["waarop", 0.0014833318005571395], ["voeging", 0.0014833318005571395], ["schriftur", 0.0014833318005571395], ["aanvull", 0.0014833318005571395], ["b", 0.0014833318005571395], ["vast", 0.0014833318005571395], ["c", 0.0014833318005571395], ["dan", 0.0014833318005571395], ["art", 0.0014833318005571395], ["aanduid", 0.0014833318005571395], ["volgend", 0.0014833318005571395], ["toepass", 0.0014833318005571395], ["gemaakt", 0.0014833318005571395], ["arbeidsgehandicapt", 0.0014833318005571395], ["nu", 0.0014833318005571395], ["schad", 0.0014833318005571395], ["onderzoek", 0.0013349986205014254], ["vonnis", 0.0013349986205014254], ["wel", 0.0013349986205014254], ["besluit", 0.0013349986205014254], ["bewez", 0.0013349986205014254], ["gepleegd", 0.0013349986205014254], ["verband", 0.0013349986205014254], ["civiel", 0.0013349986205014254], ["onrechtmat", 0.0013349986205014254], ["tiend", 0.0013349986205014254], ["zaaksdossier", 0.0013349986205014254], ["ingevolg", 0.0011866654404457115], ["plaatsing", 0.0011866654404457115], ["terbeschikkingstell", 0.0011866654404457115], ["voorrad", 0.0011866654404457115], ["slecht", 0.0011866654404457115], ["war", 0.0011866654404457115], ["handel", 0.0011866654404457115], ["?", 0.0011866654404457115], ["o", 0.0011866654404457115], ["had", 0.0011866654404457115], ["kunn", 0.0011866654404457115], ["tuss", 0.0011866654404457115], ["€", 0.0011866654404457115], ["mer", 0.0011866654404457115], ["strafbar", 0.0011866654404457115], ["achtst", 0.0011866654404457115], ["‘", 0.0011866654404457115], ["dag", 0.0011866654404457115], ["bepal", 0.0011866654404457115], ["acht", 0.0011866654404457115], ["behandel", 0.0011866654404457115], ["ontvang", 0.0011866654404457115], ["adres", 0.0011866654404457115], ["jegen", 0.0011866654404457115], ["awb", 0.0010383322603899976], ["vijf", 0.0010383322603899976], ["rechter", 0.0010383322603899976], ["drie", 0.0010383322603899976], ["geding", 0.0010383322603899976], ["betreff", 0.0010383322603899976], ["twee", 0.0010383322603899976], ["verklar", 0.0010383322603899976], ["negend", 0.0010383322603899976], ["betal", 0.0010383322603899976], ["volgen", 0.0010383322603899976], ["dient", 0.0010383322603899976], ["gesteld", 0.0010383322603899976], ["tussenvonnis", 0.0010383322603899976], ["waarvan", 0.0010383322603899976], ["dwangakkoord", 0.0010383322603899976], ["zonder", 0.0010383322603899976], ["per", 0.0010383322603899976], ["januari", 0.0010383322603899976], ["ingesteld", 0.0010383322603899976], ["bevoegdheidsincident", 0.0010383322603899976], ["gedur", 0.0010383322603899976], ["tijdig", 0.0010383322603899976], ["period", 0.0010383322603899976], ["opgenom", 0.0010383322603899976], ["tijd", 0.0010383322603899976], ["datum", 0.0010383322603899976], ["werknemer", 0.0010383322603899976], ["regel", 0.0010383322603899976], ["zoal", 0.0010383322603899976], ["t", 0.0010383322603899976], ["toelat", 0.0010383322603899976], ["dad", 0.0010383322603899976], ["aansprak", 0.0010383322603899976], ["houd", 0.0010383322603899976], ["productie", 0.0010383322603899976], ["misbruik", 0.0010383322603899976], ["bepaald", 0.0008899990803342836], ["wetboek", 0.0008899990803342836], ["verbeurdverklar", 0.0008899990803342836], ["werking", 0.0008899990803342836], ["uitvoerbaarverklar", 0.0008899990803342836], ["(…)", 0.0008899990803342836], ["sprak", 0.0008899990803342836], ["indien", 0.0008899990803342836], ["hij", 0.0008899990803342836], ["geword", 0.0008899990803342836], ["’", 0.0008899990803342836], ["arrest", 0.0008899990803342836], ["ingedi", 0.0008899990803342836], ["terechtzit", 0.0008899990803342836], ["gevorderd", 0.0008899990803342836], ["mededader", 0.0008899990803342836], ["daartoe", 0.0008899990803342836], ["formel", 0.0008899990803342836], ["kost", 0.0008899990803342836], ["status", 0.0008899990803342836], ["gelet", 0.0008899990803342836], ["wijz", 0.0008899990803342836], ["af", 0.0008899990803342836], ["strafoplegg", 0.0008899990803342836], ["betwist", 0.0008899990803342836], ["criminel", 0.0008899990803342836], ["organisatie", 0.0008899990803342836], ["uitlever", 0.0008899990803342836], ["juli", 0.0008899990803342836], ["nederland", 0.0008899990803342836], ["sam", 0.0008899990803342836], ["volgt", 0.0008899990803342836], ["procesbevoegd", 0.0008899990803342836], ["verlop", 0.0007416659002785698], ["ge", 0.0007416659002785698], ["dader", 0.0007416659002785698], ["berust", 0.0007416659002785698], ["sub", 0.0007416659002785698], ["slachtoffer", 0.0007416659002785698], ["december", 0.0007416659002785698], ["treff", 0.0007416659002785698], ["([", 0.0007416659002785698], ["nam", 0.0007416659002785698], ["ook", 0.0007416659002785698], ["werd", 0.0007416659002785698], ["vrag", 0.0007416659002785698], ["gat", 0.0007416659002785698], ["red", 0.0007416659002785698], ["grief", 0.0007416659002785698], ["goed", 0.0007416659002785698], ["mei", 0.0007416659002785698], ["verklaard", 0.0007416659002785698], ["strijd", 0.0007416659002785698], ["wordt", 0.0007416659002785698], ["_NUMbeoordel", 0.0007416659002785698], ["uitsluitingsgrond", 0.0007416659002785698], ["verzoekschrift", 0.0007416659002785698], ["appellant", 0.0007416659002785698], ["re", 0.0007416659002785698], ["integratie", 0.0007416659002785698], ["colleg", 0.0007416659002785698], ["m", 0.0007416659002785698], ["onderhav", 0.0007416659002785698], ["des", 0.0007416659002785698], ["zodat", 0.0007416659002785698], ["gebrek", 0.0007416659002785698], ["verricht", 0.0007416659002785698], ["reken", 0.0007416659002785698], ["gebezigd", 0.0007416659002785698], ["plat", 0.0007416659002785698], ["gedeelt", 0.0007416659002785698], ["_NUMa", 0.0007416659002785698], ["vervolg", 0.0007416659002785698], ["grondslag", 0.0007416659002785698], ["deelnam", 0.0007416659002785698], ["tegenverzoek", 0.0007416659002785698], ["mogelijk", 0.0007416659002785698], ["nodig", 0.0007416659002785698], ["psychiatrisch", 0.0007416659002785698], ["rolnummer", 0.0007416659002785698], ["\"", 0.0007416659002785698], ["total", 0.0007416659002785698], ["veroordel", 0.0007416659002785698], ["binn", 0.0007416659002785698], ["witwass", 0.0007416659002785698], ["reclasser", 0.0007416659002785698], ["”", 0.0005933327202228558], ["beperk", 0.0005933327202228558], ["strafrecht", 0.0005933327202228558], ["inricht", 0.0005933327202228558], ["stelselmat", 0.0005933327202228558], ["geldig", 0.0005933327202228558], ["dagvaard", 0.0005933327202228558], ["_NUMinleid", 0.0005933327202228558], ["regelgev", 0.0005933327202228558], ["betreft", 0.0005933327202228558], ["overwog", 0.0005933327202228558], ["situatie", 0.0005933327202228558], ["relevant", 0.0005933327202228558], ["leid", 0.0005933327202228558], ["daarbij", 0.0005933327202228558], ["dienst", 0.0005933327202228558], ["waarin", 0.0005933327202228558], ["genom", 0.0005933327202228558], ["lat", 0.0005933327202228558], ["militair", 0.0005933327202228558], ["arnhem", 0.0005933327202228558], ["gedrag", 0.0005933327202228558], ["bestaand", 0.0005933327202228558], ["ontslag", 0.0005933327202228558], ["maart", 0.0005933327202228558], ["dien", 0.0005933327202228558], ["termijn", 0.0005933327202228558], ["rabobank", 0.0005933327202228558], ["k", 0.0005933327202228558], ["open", 0.0005933327202228558], ["ministerie", 0.0005933327202228558], ["overweegt", 0.0005933327202228558], ["immer", 0.0005933327202228558], ["uwv", 0.0005933327202228558], ["stell", 0.0005933327202228558], ["], [", 0.0005933327202228558], ["zakelijk", 0.0005933327202228558], ["weergegev", 0.0005933327202228558], ["zin", 0.0005933327202228558], ["aldus", 0.0005933327202228558], ["onrecht", 0.0005933327202228558], ["efficiency", 0.0005933327202228558], ["del", 0.0005933327202228558], ["opgevoerd", 0.0005933327202228558], ["gehel", 0.0005933327202228558], ["nem", 0.0005933327202228558], ["terecht", 0.0005933327202228558], ["zover", 0.0005933327202228558], ["hun", 0.0005933327202228558], ["werkgever", 0.0005933327202228558], ["herroep", 0.0005933327202228558], ["daarmee", 0.0005933327202228558], ["bewijsverwer", 0.0005933327202228558], ["akt", 0.0005933327202228558], ["])", 0.0005933327202228558], ["bestuursrecht", 0.0005933327202228558], ["vereist", 0.0005933327202228558], ["besliss", 0.0005933327202228558], ["gevraagd", 0.0005933327202228558], ["eiseres", 0.0005933327202228558], ["n", 0.0005933327202228558], ["ziekenhuis", 0.0005933327202228558], ["onz", 0.0005933327202228558], ["minister", 0.0005933327202228558], ["vergunn", 0.0005933327202228558], ["http", 0.0005933327202228558], ["//", 0.0005933327202228558], ["nl", 0.0005933327202228558], ["bwbr_NUM", 0.0005933327202228558], ["geldigheidsdatum__NUM", 0.0005933327202228558], ["bestur", 0.0005933327202228558], ["burger", 0.0005933327202228558], ["september", 0.0005933327202228558], ["zo", 0.0005933327202228558], ["instructie", 0.0005933327202228558], ["althan", 0.0005933327202228558], ["november", 0.0005933327202228558], ["belanghebbendes", 0.0005933327202228558], ["curator", 0.0005933327202228558], ["al", 0.0005933327202228558], ["maand", 0.0005933327202228558], ["requisitoir", 0.0005933327202228558], ["hem", 0.0005933327202228558], ["geacht", 0.0005933327202228558], ["gevangenisstraf", 0.0005933327202228558], ["aftrek", 0.0005933327202228558], ["voorarrest", 0.0005933327202228558], ["proeftijd", 0.0005933327202228558], ["uiteenzet", 0.0004449995401671418], ["beslist", 0.0004449995401671418], ["toegepast", 0.0004449995401671418], ["pseudokop", 0.0004449995401671418], ["bovenvermeld", 0.0004449995401671418], ["ha", 0.0004449995401671418], ["ioverweg", 0.0004449995401671418], ["herzieningsverzoek", 0.0004449995401671418], ["aangaand", 0.0004449995401671418], ["–", 0.0004449995401671418], ["waarder", 0.0004449995401671418], ["r", 0.0004449995401671418], ["verorden", 0.0004449995401671418], ["kond", 0.0004449995401671418], ["le", 0.0004449995401671418], ["voorop", 0.0004449995401671418], ["februari", 0.0004449995401671418], ["bereken", 0.0004449995401671418], ["zou", 0.0004449995401671418], ["waarbij", 0.0004449995401671418], ["verhoogd", 0.0004449995401671418], ["werkzam", 0.0004449995401671418], ["geschilpunt", 0.0004449995401671418], ["verschen", 0.0004449995401671418], ["aanhef", 0.0004449995401671418], ["ingang", 0.0004449995401671418], ["vanuit", 0.0004449995401671418], ["bezit", 0.0004449995401671418], ["gedan", 0.0004449995401671418], ["politierechter", 0.0004449995401671418], ["wegen", 0.0004449995401671418], ["dur", 0.0004449995401671418], ["l", 0.0004449995401671418], ["juni", 0.0004449995401671418], ["gerechtshof", 0.0004449995401671418], ["verbod", 0.0004449995401671418], ["gewijzigd", 0.0004449995401671418], ["bedrag", 0.0004449995401671418], ["aangenom", 0.0004449995401671418], ["zulk", 0.0004449995401671418], ["stelling", 0.0004449995401671418], ["samenhang", 0.0004449995401671418], ["central", 0.0004449995401671418], ["verstrek", 0.0004449995401671418], ["geblek", 0.0004449995401671418], ["indigd", 0.0004449995401671418], ["wie", 0.0004449995401671418], ["behoud", 0.0004449995401671418], ["bevorder", 0.0004449995401671418], ["behoev", 0.0004449995401671418], ["houdend", 0.0004449995401671418], ["wsw", 0.0004449995401671418], ["behoort", 0.0004449995401671418], ["vastgesteld", 0.0004449995401671418], ["enkel", 0.0004449995401671418], ["kader", 0.0004449995401671418], ["voorgaand", 0.0004449995401671418], ["sv", 0.0004449995401671418], ["gevoegd", 0.0004449995401671418], ["lijst", 0.0004449995401671418], ["valselijk", 0.0004449995401671418], ["waarheid", 0.0004449995401671418], ["premiejar", 0.0004449995401671418], ["basis", 0.0004449995401671418], ["tijden", 0.0004449995401671418], ["f", 0.0004449995401671418], ["gev", 0.0004449995401671418], ["verzoekt", 0.0004449995401671418], ["toegewez", 0.0004449995401671418], ["waardor", 0.0004449995401671418], ["daarom", 0.0004449995401671418], ["komt", 0.0004449995401671418], ["opmerk", 0.0004449995401671418], ["namelijk", 0.0004449995401671418], ["grot", 0.0004449995401671418], ["notaris", 0.0004449995401671418], ["beoordelingskader", 0.0004449995401671418], ["onverwijld", 0.0004449995401671418], ["spoed", 0.0004449995401671418], ["ee", 0.0004449995401671418], ["schorsingsverzoek", 0.0004449995401671418], ["beoordelingsprocedur", 0.0004449995401671418], ["beroepsgrond", 0.0004449995401671418], ["svb", 0.0004449995401671418], ["voorwerp", 0.0004449995401671418], ["beoordelingsmethodiek", 0.0004449995401671418], ["intrek", 0.0004449995401671418], ["betrekt", 0.0004449995401671418], ["document", 0.0004449995401671418], ["verwet", 0.0004449995401671418], ["reikwijdt", 0.0004449995401671418], ["verklaart", 0.0004449995401671418], ["maatstaf", 0.0004449995401671418], ["bw", 0.0004449995401671418], ["rent", 0.0004449995401671418], ["hen", 0.0004449995401671418], ["g", 0.0004449995401671418], ["kort", 0.0004449995401671418], ["’ [", 0.0004449995401671418], ["veroordeelt", 0.0004449995401671418], ["mak", 0.0004449995401671418], ["ontzegg", 0.0004449995401671418], ["rijbevoegd", 0.0004449995401671418], ["omgang", 0.0004449995401671418], ["strafoverweg", 0.0004449995401671418], ["omvang", 0.0004449995401671418], ["afgewez", 0.0004449995401671418], ["afdel", 0.0004449995401671418], ["overeenkomst", 0.0004449995401671418], ["uren", 0.0004449995401671418], ["poging", 0.0004449995401671418], ["geweld", 0.0004449995401671418], ["geadviseerd", 0.0004449995401671418], ["meldplicht", 0.0004449995401671418], ["hierna", 0.0004449995401671418], ["juist", 0.0004449995401671418], ["verhoud", 0.0004449995401671418], ["bestuursorgan", 0.0002966663601114279], ["geleg", 0.0002966663601114279], ["opgeworp", 0.0002966663601114279], ["iioverweg", 0.0002966663601114279], ["deskundigenbericht", 0.0002966663601114279], ["medisch", 0.0002966663601114279], ["bezwaarschrift", 0.0002966663601114279], ["rk", 0.0002966663601114279], ["ontvankelijkverklar", 0.0002966663601114279], ["getuig", 0.0002966663601114279], ["advies", 0.0002966663601114279], ["zedenzak", 0.0002966663601114279], ["vel", 0.0002966663601114279], ["doorgan", 0.0002966663601114279], ["aanwez", 0.0002966663601114279], ["beweerd", 0.0002966663601114279], ["seksuel", 0.0002966663601114279], ["extra", 0.0002966663601114279], ["zeker", 0.0002966663601114279], ["ontkenn", 0.0002966663601114279], ["international", 0.0002966663601114279], ["buitengewon", 0.0002966663601114279], ["verplicht", 0.0002966663601114279], ["gevolg", 0.0002966663601114279], ["redelijk", 0.0002966663601114279], ["hvjeu", 0.0002966663601114279], ["hoe", 0.0002966663601114279], ["materi", 0.0002966663601114279], ["onaanvaard", 0.0002966663601114279], ["ondernem", 0.0002966663601114279], ["gebracht", 0.0002966663601114279], ["ligt", 0.0002966663601114279], ["zijnd", 0.0002966663601114279], ["schending", 0.0002966663601114279], ["kern", 0.0002966663601114279], ["ongeval", 0.0002966663601114279], ["vennootschap", 0.0002966663601114279], ["functie", 0.0002966663601114279], ["hetzelfd", 0.0002966663601114279], ["boet", 0.0002966663601114279], ["onbevoegd", 0.0002966663601114279], ["amar", 0.0002966663601114279], ["blijv", 0.0002966663601114279], ["computerbestand", 0.0002966663601114279], ["kinder", 0.0002966663601114279], ["ongegrond", 0.0002966663601114279], ["parketnummer", 0.0002966663601114279], ["opzet", 0.0002966663601114279], ["voornoemd", 0.0002966663601114279], ["ieder", 0.0002966663601114279], ["vordert", 0.0002966663601114279], ["geslot", 0.0002966663601114279], ["proceskost", 0.0002966663601114279], ["bmax", 0.0002966663601114279], ["inzak", 0.0002966663601114279], ["zull", 0.0002966663601114279], ["krijg", 0.0002966663601114279], ["verander", 0.0002966663601114279], ["motiveringsklacht", 0.0002966663601114279], ["geschiktheidseis", 0.0002966663601114279], ["inspann", 0.0002966663601114279], ["verded", 0.0002966663601114279], ["noch", 0.0002966663601114279], ["aannem", 0.0002966663601114279], ["verwerpt", 0.0002966663601114279], ["buit", 0.0002966663601114279], ["aangemerkt", 0.0002966663601114279], ["wao", 0.0002966663601114279], ["verminder", 0.0002966663601114279], ["ziekt", 0.0002966663601114279], ["herstel", 0.0002966663601114279], ["verstrekt", 0.0002966663601114279], ["herindicatiebeschik", 0.0002966663601114279], ["doelgroep", 0.0002966663601114279], ["be", 0.0002966663601114279], ["indig", 0.0002966663601114279], ["arbeid", 0.0002966663601114279], ["belemmer", 0.0002966663601114279], ["vanaf", 0.0002966663601114279], ["blijken", 0.0002966663601114279], ["uitker", 0.0002966663601114279], ["dergelijk", 0.0002966663601114279], ["kantor", 0.0002966663601114279], ["eindhov", 0.0002966663601114279], ["afdoen", 0.0002966663601114279], ["doet", 0.0002966663601114279], ["opgegev", 0.0002966663601114279], ["sted", 0.0002966663601114279], ["slot", 0.0002966663601114279], ["wettig", 0.0002966663601114279], ["overtu", 0.0002966663601114279], ["dienstverband", 0.0002966663601114279], ["karakter", 0.0002966663601114279], ["fout", 0.0002966663601114279], ["percel", 0.0002966663601114279], ["mar", 0.0002966663601114279], ["rechtsvervolg", 0.0002966663601114279], ["klager", 0.0002966663601114279], ["procesdossier", 0.0002966663601114279], ["uitvaard", 0.0002966663601114279], ["wetgev", 0.0002966663601114279], ["wnt", 0.0002966663601114279], ["der", 0.0002966663601114279], ["ontvanger", 0.0002966663601114279], ["instantie", 0.0002966663601114279], ["markt", 0.0002966663601114279], ["ker", 0.0002966663601114279], ["verbeter", 0.0002966663601114279], ["amsterdam", 0.0002966663601114279], ["provisionel", 0.0002966663601114279], ["wrakingskamer", 0.0002966663601114279], ["inschrijv", 0.0002966663601114279], ["nt", 0.0002966663601114279], ["administratief", 0.0002966663601114279], ["voegingsincident", 0.0002966663601114279], ["slotsom", 0.0002966663601114279], ["gemeent", 0.0002966663601114279], ["uitgeslot", 0.0002966663601114279], ["voldan", 0.0002966663601114279], ["voormal", 0.0002966663601114279], ["second", 0.0002966663601114279], ["opinion", 0.0002966663601114279], ["schriftelijk", 0.0002966663601114279], ["aanleid", 0.0002966663601114279], ["punt", 0.0002966663601114279], ["bijlag", 0.0002966663601114279], ["voorstel", 0.0002966663601114279], ["inbeslaggenom", 0.0002966663601114279], ["),", 0.0002966663601114279], ["schors", 0.0002966663601114279], ["integriteitsbeoordel", 0.0002966663601114279], ["ʺ", 0.0002966663601114279], ["zoverr", 0.0002966663601114279], ["verblijf", 0.0002966663601114279], ["mevrouw", 0.0002966663601114279], ["],", 0.0002966663601114279], ["ondertek", 0.0002966663601114279], ["samenwon", 0.0002966663601114279], ["rester", 0.0002966663601114279], ["ljn", 0.0002966663601114279], ["bz_NUM", 0.0002966663601114279], ["digital", 0.0002966663601114279], ["bewijsbeslag", 0.0002966663601114279], ["ingrijp", 0.0002966663601114279], ["dwangmiddel", 0.0002966663601114279], ["wederpartij", 0.0002966663601114279], ["hinder", 0.0002966663601114279], ["toegebracht", 0.0002966663601114279], ["mag", 0.0002966663601114279], ["bescheid", 0.0002966663601114279], ["geld", 0.0002966663601114279], ["plausibel", 0.0002966663601114279], ["hetgen", 0.0002966663601114279], ["ingeschakeld", 0.0002966663601114279], ["deskund", 0.0002966663601114279], ["uitvoer", 0.0002966663601114279], ["allereerst", 0.0002966663601114279], ["buitengerecht", 0.0002966663601114279], ["vervolgen", 0.0002966663601114279], ["sluit", 0.0002966663601114279], ["vaststellingsovereenkomst", 0.0002966663601114279], ["gewoont", 0.0002966663601114279], ["mensenhandel", 0.0002966663601114279], ["valsheid", 0.0002966663601114279], ["geschrift", 0.0002966663601114279], ["geldbedrag", 0.0002966663601114279], [",-", 0.0002966663601114279], ["tussenarrest", 0.0002966663601114279], ["beroepsfout", 0.0002966663601114279], ["zorgplicht", 0.0002966663601114279], ["geschond", 0.0002966663601114279], ["eig", 0.0002966663601114279], ["nummer", 0.0002966663601114279], ["personenvervoer", 0.0002966663601114279], ["wp", 0.0002966663601114279], ["taxivervoer", 0.0002966663601114279], ["verleend", 0.0002966663601114279], ["pva", 0.0002966663601114279], ["geconstateerd", 0.0002966663601114279], ["ziet", 0.0002966663601114279], ["vermeerderd", 0.0002966663601114279], ["d", 0.0002966663601114279], ["stichting", 0.0002966663601114279], ["heffingsrent", 0.0002966663601114279], ["tussentijd", 0.0002966663601114279], ["moeder", 0.0002966663601114279], ["minderjar", 0.0002966663601114279], ["wek", 0.0002966663601114279], ["rechtsmacht", 0.0002966663601114279], ["aangezien", 0.0002966663601114279], ["eex", 0.0002966663601114279], ["gunning", 0.0002966663601114279], ["ingeleid", 0.0002966663601114279], ["procesverlop", 0.0002966663601114279], ["ep", 0.0002966663601114279], ["conclusies", 0.0002966663601114279], ["zorginstell", 0.0002966663601114279], ["gebruikt", 0.0002966663601114279], ["vermog", 0.0002966663601114279], ["voorvrag", 0.0002966663601114279], ["kennis", 0.0002966663601114279], ["strekkend", 0.0002966663601114279], ["bewijsuitsluit", 0.0002966663601114279], ["augustus", 0.0002966663601114279], ["ontbind", 0.0002966663601114279], ["vergoed", 0.0002966663601114279], ["kracht", 0.0002966663601114279], ["kenbar", 0.0002966663601114279], ["meest", 0.0002966663601114279], ["beoordelingspunt", 0.0002966663601114279], ["klaagschrift", 0.0002966663601114279], ["april", 0.0002966663601114279], ["hoofdstraff", 0.0002966663601114279], ["verstekverlen", 0.0002966663601114279], ["conclusie", 0.0002966663601114279], ["wat", 0.0002966663601114279], ["keuz", 0.0002966663601114279], ["onherroep", 0.0002966663601114279], ["dus", 0.0002966663601114279], ["vier", 0.0002966663601114279], ["alsmed", 0.0002966663601114279], ["doodslag", 0.0002966663601114279], ["achttien", 0.0002966663601114279], ["zes", 0.0002966663601114279], ["gedragsinterventie", 0.0002966663601114279], ["alcohol", 0.0002966663601114279], ["behaald", 0.0002966663601114279], ["ggz", 0.0002966663601114279], ["instell", 0.0002966663601114279], ["fw", 0.0002966663601114279], ["hier", 0.0002966663601114279], ["brengt", 0.0002966663601114279], ["`", 0.0002966663601114279], ["gvb", 0.0002966663601114279], ["opgebouwd", 0.0002966663601114279], ["vp", 0.0002966663601114279], ["standaardregel", 0.0002966663601114279], ["beginsel", 0.0002966663601114279], ["kredietfaciliteit", 0.0002966663601114279], ["kredietrelatie", 0.0002966663601114279], ["advistal", 0.0002966663601114279], ["opzegg", 0.0002966663601114279], ["voldoend", 0.0002966663601114279], ["novum", 0.0002966663601114279], ["bewijsconstructie", 0.0002966663601114279], ["voordat", 0.00014833318005571394], ["gehoord", 0.00014833318005571394], ["i", 0.00014833318005571394], ["ntimeerd", 0.00014833318005571394], ["aanhoudingsincident", 0.00014833318005571394], ["brouwer", 0.00014833318005571394], ["dam", 0.00014833318005571394], ["onderbouw", 0.00014833318005571394], ["vergelijk", 0.00014833318005571394], ["claim", 0.00014833318005571394], ["motiveringsgebrek", 0.00014833318005571394], ["krachten", 0.00014833318005571394], ["_NUMwc", 0.00014833318005571394], ["beveelt", 0.00014833318005571394], ["vervang", 0.00014833318005571394], ["jeugddetentie", 0.00014833318005571394], ["behor", 0.00014833318005571394], ["tenuitvoerlegg", 0.00014833318005571394], ["meegewerkt", 0.00014833318005571394], ["start", 0.00014833318005571394], ["ondervrijwar", 0.00014833318005571394], ["oeganda", 0.00014833318005571394], ["verdragsstat", 0.00014833318005571394], ["hag", 0.00014833318005571394], ["adoptieverdrag", 0.00014833318005571394], ["verschon", 0.00014833318005571394], ["daisycon", 0.00014833318005571394], ["reacties", 0.00014833318005571394], ["kenmerk", 0.00014833318005571394], ["vermeend", 0.00014833318005571394], ["maakt", 0.00014833318005571394], ["zorgvuld", 0.00014833318005571394], ["afgelegd", 0.00014833318005571394], ["gekek", 0.00014833318005571394], ["elfd", 0.00014833318005571394], ["ad", 0.00014833318005571394], ["_NUMis", 0.00014833318005571394], ["luchtvaartmaatschappij", 0.00014833318005571394], ["compensatie", 0.00014833318005571394], ["aanton", 0.00014833318005571394], ["annuler", 0.00014833318005571394], ["ondank", 0.00014833318005571394], ["voorkom", 0.00014833318005571394], ["jurisprudentie", 0.00014833318005571394], ["vervoerder", 0.00014833318005571394], ["aangepast", 0.00014833318005571394], ["zelf", 0.00014833318005571394], ["inzet", 0.00014833318005571394], ["personeelsmiddel", 0.00014833318005571394], ["behouden", 0.00014833318005571394], ["tijdstip", 0.00014833318005571394], ["offer", 0.00014833318005571394], ["oogpunt", 0.00014833318005571394], ["kennelijk", 0.00014833318005571394], ["vermijd", 0.00014833318005571394], ["waarmee", 0.00014833318005571394], ["geconfronteerd", 0.00014833318005571394], ["vertrag", 0.00014833318005571394], ["vlucht", 0.00014833318005571394], ["derhalv", 0.00014833318005571394], ["stelplicht", 0.00014833318005571394], ["bewijslast", 0.00014833318005571394], ["bevestigt", 0.00014833318005571394], ["discriminatieverbod", 0.00014833318005571394], ["protocol", 0.00014833318005571394], ["evrm", 0.00014833318005571394], ["verlies", 0.00014833318005571394], ["verdienvermog", 0.00014833318005571394], ["carri", 0.00014833318005571394], ["è", 0.00014833318005571394], ["reverlop", 0.00014833318005571394], ["salarisontwikkel", 0.00014833318005571394], ["geweest", 0.00014833318005571394], ["hypothetisch", 0.00014833318005571394], ["uitgegan", 0.00014833318005571394], ["weggedacht", 0.00014833318005571394], ["titulair", 0.00014833318005571394], ["directeur", 0.00014833318005571394], ["brutosalaris", 0.00014833318005571394], [",-,", 0.00014833318005571394], ["uitlop", 0.00014833318005571394], ["enig", 0.00014833318005571394], ["moment", 0.00014833318005571394], ["euro", 0.00014833318005571394], ["insuranc", 0.00014833318005571394], ["dezelfd", 0.00014833318005571394], ["geblev", 0.00014833318005571394], ["salaris", 0.00014833318005571394], ["memorie", 0.00014833318005571394], ["antwoord", 0.00014833318005571394], ["beiderzijd", 0.00014833318005571394], ["teruggav", 0.00014833318005571394], ["relatiev", 0.00014833318005571394], ["risicobeoordel", 0.00014833318005571394], ["geschilomschrijv", 0.00014833318005571394], ["vrijspraakoverweg", 0.00014833318005571394], ["ernstig", 0.00014833318005571394], ["zitting", 0.00014833318005571394], ["behandeld", 0.00014833318005571394], ["bijgestan", 0.00014833318005571394], ["mr", 0.00014833318005571394], ["speksnijder", 0.00014833318005571394], ["raadsman", 0.00014833318005571394], ["vertegenwoord", 0.00014833318005571394], ["gemachtigd", 0.00014833318005571394], ["_NUMde", 0.00014833318005571394], ["_NUMeiser", 0.00014833318005571394], ["destijd", 0.00014833318005571394], ["korporal", 0.00014833318005571394], ["bataljon", 0.00014833318005571394], ["konink", 0.00014833318005571394], ["landmacht", 0.00014833318005571394], ["ambtenarenreglement", 0.00014833318005571394], ["ambt", 0.00014833318005571394], ["geschorst", 0.00014833318005571394], ["eenheid", 0.00014833318005571394], ["aangift", 0.00014833318005571394], ["kinderpornografisch", 0.00014833318005571394], ["materiaal", 0.00014833318005571394], ["gehandhaafd", 0.00014833318005571394], ["multimediafiles", 0.00014833318005571394], ["filmfragment", 0.00014833318005571394], ["foto", 0.00014833318005571394], ["jonger", 0.00014833318005571394], ["werkstraf", 0.00014833318005571394], ["uur", 0.00014833318005571394], ["wangedrag", 0.00014833318005571394], ["pornografisch", 0.00014833318005571394], ["rechtsmiddel", 0.00014833318005571394], ["aangew", 0.00014833318005571394], ["_NUMbij", 0.00014833318005571394], ["vernietigd", 0.00014833318005571394], ["began", 0.00014833318005571394], ["vrijgesprok", 0.00014833318005571394], ["asielrelas", 0.00014833318005571394], ["medepleg", 0.00014833318005571394], ["opiumwet", 0.00014833318005571394], ["meermal", 0.00014833318005571394], ["wederrecht", 0.00014833318005571394], ["verkreg", 0.00014833318005571394], ["voordel", 0.00014833318005571394], ["evenen", 0.00014833318005571394], ["gebaseerd", 0.00014833318005571394], ["rapportag", 0.00014833318005571394], ["verstand", 0.00014833318005571394], [",--", 0.00014833318005571394], ["uitgat", 0.00014833318005571394], ["gehandeld", 0.00014833318005571394], ["hennep", 0.00014833318005571394], ["hiervan", 0.00014833318005571394], ["unigarant", 0.00014833318005571394], ["bedrijfsactiviteit", 0.00014833318005571394], ["megahom", 0.00014833318005571394], ["npb", 0.00014833318005571394], ["bestan", 0.00014833318005571394], ["hieruit", 0.00014833318005571394], ["koopt", 0.00014833318005571394], ["verwacht", 0.00014833318005571394], ["bestemm", 0.00014833318005571394], ["woningbouw", 0.00014833318005571394], ["daardor", 0.00014833318005571394], ["sterk", 0.00014833318005571394], ["waard", 0.00014833318005571394], ["stijg", 0.00014833318005571394], ["aankop", 0.00014833318005571394], ["sedert", 0.00014833318005571394], ["omstrek", 0.00014833318005571394], ["gefinancierd", 0.00014833318005571394], ["kortlop", 0.00014833318005571394], ["lening", 0.00014833318005571394], ["lop", 0.00014833318005571394], ["meningsverschill", 0.00014833318005571394], ["gerez", 0.00014833318005571394], ["financieringswijz", 0.00014833318005571394], ["ongunst", 0.00014833318005571394], ["rentestructur", 0.00014833318005571394], ["geldmarkt", 0.00014833318005571394], ["cassatieklacht", 0.00014833318005571394], ["veertiend", 0.00014833318005571394], ["uw", 0.00014833318005571394], ["boetebeoordel", 0.00014833318005571394], ["bov", 0.00014833318005571394], ["elkar", 0.00014833318005571394], ["bezien", 0.00014833318005571394], ["strafvervolg", 0.00014833318005571394], ["evenmin", 0.00014833318005571394], ["overigen", 0.00014833318005571394], ["ler", 0.00014833318005571394], ["rechtskracht", 0.00014833318005571394], ["ord", 0.00014833318005571394], ["bestuursrechter", 0.00014833318005571394], ["toegekom", 0.00014833318005571394], ["materie", 0.00014833318005571394], ["geoordeeld", 0.00014833318005571394], ["co", 0.00014833318005571394], ["rdinatiewet", 0.00014833318005571394], ["social", 0.00014833318005571394], ["verzeker", 0.00014833318005571394], ["verjaringstermijn", 0.00014833318005571394], ["premievaststell", 0.00014833318005571394], ["gav", 0.00014833318005571394], ["premie", 0.00014833318005571394], ["betoogd", 0.00014833318005571394], ["eendagsmeld", 0.00014833318005571394], ["eran", 0.00014833318005571394], ["degelijk", 0.00014833318005571394], ["arbeidsongeschiktheidsuitker", 0.00014833318005571394], ["waz", 0.00014833318005571394], ["wajong", 0.00014833318005571394], ["arbeidsongeschikt", 0.00014833318005571394], ["voorschrift", 0.00014833318005571394], ["toegek", 0.00014833318005571394], ["strekt", 0.00014833318005571394], ["arbeidsgeschikt", 0.00014833318005571394], ["subsidie", 0.00014833318005571394], ["indicatiebeschik", 0.00014833318005571394], ["doch", 0.00014833318005571394], ["arbeidsovereenkomst", 0.00014833318005571394], ["dienstbetrek", 0.00014833318005571394], ["herindicatie", 0.00014833318005571394], ["arbeidskund", 0.00014833318005571394], ["verkrijg", 0.00014833318005571394], ["stond", 0.00014833318005571394], ["geregistreerd", 0.00014833318005571394], ["aaw", 0.00014833318005571394], ["ingangsdatum", 0.00014833318005571394], ["einddatum", 0.00014833318005571394], ["wa", 0.00014833318005571394], ["ren", 0.00014833318005571394], ["gevall", 0.00014833318005571394], ["diend", 0.00014833318005571394], ["dossieronderzoek", 0.00014833318005571394], ["vind", 0.00014833318005571394], ["getuigenverklar", 0.00014833318005571394], ["aannam", 0.00014833318005571394], ["definitie", 0.00014833318005571394], ["omwill", 0.00014833318005571394], ["meegenom", 0.00014833318005571394], ["hel", 0.00014833318005571394], ["merkt", 0.00014833318005571394], ["aanvraagformulier", 0.00014833318005571394], ["inhield", 0.00014833318005571394], ["gewerkt", 0.00014833318005571394], ["aantal", 0.00014833318005571394], ["brutolon", 0.00014833318005571394], ["apart", 0.00014833318005571394], ["moest", 0.00014833318005571394], ["evenwel", 0.00014833318005571394], ["nagelat", 0.00014833318005571394], ["aanmerk", 0.00014833318005571394], ["vastgelegd", 0.00014833318005571394], ["] (", 0.00014833318005571394], ["), [", 0.00014833318005571394], ["desbetreff", 0.00014833318005571394], ["gesprok", 0.00014833318005571394], ["oplicht", 0.00014833318005571394], ["behoeft", 0.00014833318005571394], ["lastegelegd", 0.00014833318005571394], ["_NUMv", 0.00014833318005571394], ["bindt", 0.00014833318005571394], ["eventueel", 0.00014833318005571394], ["bodemged", 0.00014833318005571394], ["uitbetal", 0.00014833318005571394], ["toeslagrecht", 0.00014833318005571394], ["erkent", 0.00014833318005571394], ["meent", 0.00014833318005571394], ["begrijp", 0.00014833318005571394], ["will", 0.00014833318005571394], ["opgev", 0.00014833318005571394], ["coulanc", 0.00014833318005571394], ["bewar", 0.00014833318005571394], ["opgehev", 0.00014833318005571394], ["connex", 0.00014833318005571394], ["asielberoep", 0.00014833318005571394], ["uitzet", 0.00014833318005571394], ["wijst", 0.00014833318005571394], ["toegezond", 0.00014833318005571394], ["gemoeid", 0.00014833318005571394], ["wrakingsgrond", 0.00014833318005571394], ["tegenargument", 0.00014833318005571394], ["mailincident", 0.00014833318005571394], ["nvbw", 0.00014833318005571394], ["vtw", 0.00014833318005571394], ["dringend", 0.00014833318005571394], ["tijdelijk", 0.00014833318005571394], ["plaatsvind", 0.00014833318005571394], ["locatie", 0.00014833318005571394], ["afgelast", 0.00014833318005571394], ["maximal", 0.00014833318005571394], ["marktdag", 0.00014833318005571394], ["warenmarkt", 0.00014833318005571394], ["grootschal", 0.00014833318005571394], ["evenement", 0.00014833318005571394], ["verplaatst", 0.00014833318005571394], ["gehoud", 0.00014833318005571394], ["oud", 0.00014833318005571394], ["les", 0.00014833318005571394], ["steekpartij", 0.00014833318005571394], ["klaagt", 0.00014833318005571394], ["delictgevar", 0.00014833318005571394], ["stoornis", 0.00014833318005571394], ["sen", 0.00014833318005571394], ["hoofdlijn", 0.00014833318005571394], ["bedenk", 0.00014833318005571394], ["interveni", 0.00014833318005571394], ["famvo", 0.00014833318005571394], ["hernieuwd", 0.00014833318005571394], ["toewijz", 0.00014833318005571394], ["vatbar", 0.00014833318005571394], ["aanbested", 0.00014833318005571394], ["inderdad", 0.00014833318005571394], ["stuk", 0.00014833318005571394], ["knock", 0.00014833318005571394], ["out", 0.00014833318005571394], ["’-", 0.00014833318005571394], ["uitsluit", 0.00014833318005571394], ["geschied", 0.00014833318005571394], ["natura", 0.00014833318005571394], ["ehs", 0.00014833318005571394], ["omschrijv", 0.00014833318005571394], ["uitleg", 0.00014833318005571394], ["pleidooi", 0.00014833318005571394], ["weergav", 0.00014833318005571394], ["preliminair", 0.00014833318005571394], ["scores", 0.00014833318005571394], ["hogeschol", 0.00014833318005571394], ["centres", 0.00014833318005571394], ["expertis", 0.00014833318005571394], ["voorzitter", 0.00014833318005571394], ["wijzigt", 0.00014833318005571394], ["albanes", 0.00014833318005571394], ["nationaliteit", 0.00014833318005571394], ["afgift", 0.00014833318005571394], ["vw", 0.00014833318005571394], ["familielid", 0.00014833318005571394], ["unie", 0.00014833318005571394], ["referent", 0.00014833318005571394], ["roemen", 0.00014833318005571394], ["woont", 0.00014833318005571394], ["werkt", 0.00014833318005571394], ["relatieverklar", 0.00014833318005571394], ["levenspartner", 0.00014833318005571394], ["gemeenschapp", 0.00014833318005571394], ["huishoud", 0.00014833318005571394], ["voer", 0.00014833318005571394], ["feitelijk", 0.00014833318005571394], ["griffie", 0.00014833318005571394], ["bevat", 0.00014833318005571394], ["noem", 0.00014833318005571394], ["precies", 0.00014833318005571394], ["omschrev", 0.00014833318005571394], ["redelijkerwijs", 0.00014833318005571394], ["verlangd", 0.00014833318005571394], ["beslaglegg", 0.00014833318005571394], ["ontaard", 0.00014833318005571394], ["fishing", 0.00014833318005571394], ["expedition", 0.00014833318005571394], ["toe", 0.00014833318005571394], ["rechtsprak", 0.00014833318005571394], ["plausibiliteitscriterium", 0.00014833318005571394], ["voortvloei", 0.00014833318005571394], ["geuit", 0.00014833318005571394], ["wanner", 0.00014833318005571394], ["klachtenpatron", 0.00014833318005571394], ["consistent", 0.00014833318005571394], ["consequent", 0.00014833318005571394], ["patron", 0.00014833318005571394], ["nog", 0.00014833318005571394], ["schakel", 0.00014833318005571394], ["instruer", 0.00014833318005571394], ["criterium", 0.00014833318005571394], ["onderzocht", 0.00014833318005571394], ["ondervond", 0.00014833318005571394], ["navolg", 0.00014833318005571394], ["zozer", 0.00014833318005571394], ["meetbar", 0.00014833318005571394], ["functionel", 0.00014833318005571394], ["mat", 0.00014833318005571394], ["activiteit", 0.00014833318005571394], ["participatie", 0.00014833318005571394], ["allen", 0.00014833318005571394], ["lichaamsfuncties", 0.00014833318005571394], ["anatomisch", 0.00014833318005571394], ["eigenschapp", 0.00014833318005571394], ["omgevingsfactor", 0.00014833318005571394], ["gewog", 0.00014833318005571394], ["”,", 0.00014833318005571394], ["allianz", 0.00014833318005571394], ["medewerk", 0.00014833318005571394], ["verlen", 0.00014833318005571394], ["gedog", 0.00014833318005571394], ["verzekeringsgeneeskund", 0.00014833318005571394], ["kruithof", 0.00014833318005571394], ["arbeidsdeskund", 0.00014833318005571394], ["artos", 0.00014833318005571394], ["rapport", 0.00014833318005571394], ["oog", 0.00014833318005571394], ["_NUMaa", 0.00014833318005571394], ["rechtsvorder", 0.00014833318005571394], ["begrot", 0.00014833318005571394], ["dagteken", 0.00014833318005571394], ["aanzegg", 0.00014833318005571394], ["verschuldigd", 0.00014833318005571394], ["len", 0.00014833318005571394], ["deelgeschilprocedur", 0.00014833318005571394], ["onderhandelingstraject", 0.00014833318005571394], ["stuit", 0.00014833318005571394], ["afwikkel", 0.00014833318005571394], ["onderhandel", 0.00014833318005571394], ["doel", 0.00014833318005571394], ["_NUMw", 0.00014833318005571394], ["argument", 0.00014833318005571394], ["voorhand", 0.00014833318005571394], ["wapen", 0.00014833318005571394], ["munitie", 0.00014833318005571394], ["verblijfsvergunn", 0.00014833318005571394], ["holdi", 0.00014833318005571394], ["zer", 0.00014833318005571394], ["stringent", 0.00014833318005571394], ["beschikt", 0.00014833318005571394], ["transactie", 0.00014833318005571394], ["overzien", 0.00014833318005571394], ["justitieel", 0.00014833318005571394], ["incassobureau", 0.00014833318005571394], ["cjib", 0.00014833318005571394], ["zekerheidsstell", 0.00014833318005571394], ["antwoordakt", 0.00014833318005571394], ["centrada", 0.00014833318005571394], ["economisch", 0.00014833318005571394], ["feitenvaststell", 0.00014833318005571394], ["onbevoegdheidsverklar", 0.00014833318005571394], ["uitstel", 0.00014833318005571394], ["geheimhoud", 0.00014833318005571394], ["beoordelingscriteria", 0.00014833318005571394], ["debeoordelingvanhetbewijs", 0.00014833318005571394], ["ruimtelijk", 0.00014833318005571394], ["kwaliteit", 0.00014833318005571394], ["zorg", 0.00014833318005571394], ["omgev", 0.00014833318005571394], ["rva", 0.00014833318005571394], ["grondstrom", 0.00014833318005571394], ["gemotiveerd", 0.00014833318005571394], ["valt", 0.00014833318005571394], ["repliek", 0.00014833318005571394], ["wetgever", 0.00014833318005571394], ["onbehor", 0.00014833318005571394], ["taakvervull", 0.00014833318005571394], ["bestuurder", 0.00014833318005571394], ["rechtsperson", 0.00014833318005571394], ["uitgewerkt", 0.00014833318005571394], ["aangedrag", 0.00014833318005571394], ["juridisch", 0.00014833318005571394], ["aangevuld", 0.00014833318005571394], ["rolvoeg", 0.00014833318005571394], ["provincie", 0.00014833318005571394], ["executiegeschil", 0.00014833318005571394], ["procesrecht", 0.00014833318005571394], ["gevoerd", 0.00014833318005571394], ["verkort", 0.00014833318005571394], ["buitenland", 0.00014833318005571394], ["afzegg", 0.00014833318005571394], ["vergader", 0.00014833318005571394], ["taliban", 0.00014833318005571394], ["commandant", 0.00014833318005571394], ["lek", 0.00014833318005571394], ["doen", 0.00014833318005571394], ["navrag", 0.00014833318005571394], ["netwerk", 0.00014833318005571394], ["defensie", 0.00014833318005571394], ["plicht", 0.00014833318005571394], ["voldoen", 0.00014833318005571394], ["inlicht", 0.00014833318005571394], ["veiligheidsdienst", 0.00014833318005571394], ["wiv", 0.00014833318005571394], ["schadevergoed", 0.00014833318005571394], ["nakost", 0.00014833318005571394], ["verweerster", 0.00014833318005571394], ["strafzak", 0.00014833318005571394], ["derdenamen", 0.00014833318005571394], ["vierdenamen", 0.00014833318005571394], ["verhog", 0.00014833318005571394], ["toegelicht", 0.00014833318005571394], ["advocat", 0.00014833318005571394], ["contact", 0.00014833318005571394], ["bureau", 0.00014833318005571394], ["jeugdzorg", 0.00014833318005571394], ["psycholog", 0.00014833318005571394], ["vakantie", 0.00014833318005571394], ["gan", 0.00014833318005571394], ["hechting", 0.00014833318005571394], ["daarnaast", 0.00014833318005571394], ["bang", 0.00014833318005571394], ["letsel", 0.00014833318005571394], ["vak", 0.00014833318005571394], ["pleeggezin", 0.00014833318005571394], ["stil", 0.00014833318005571394], ["kom", 0.00014833318005571394], ["ligg", 0.00014833318005571394], ["identiteitskaart", 0.00014833318005571394], ["paspoort", 0.00014833318005571394], ["voortgezet", 0.00014833318005571394], ["duitsland", 0.00014833318005571394], ["woonacht", 0.00014833318005571394], ["mid", 0.00014833318005571394], ["situaties", 0.00014833318005571394], ["toekomt", 0.00014833318005571394], ["vooronderzoek", 0.00014833318005571394], ["hoofdstaf", 0.00014833318005571394], ["executie", 0.00014833318005571394], ["iibeoordel", 0.00014833318005571394], ["inventiviteit", 0.00014833318005571394], ["verblijft", 0.00014833318005571394], ["ziektekost", 0.00014833318005571394], ["awbz", 0.00014833318005571394], ["bijdrag", 0.00014833318005571394], ["kalendermaand", 0.00014833318005571394], ["inkomensgegeven", 0.00014833318005571394], ["teven", 0.00014833318005571394], ["%", 0.00014833318005571394], ["spar", 0.00014833318005571394], ["belegg", 0.00014833318005571394], ["meegeteld", 0.00014833318005571394], ["box", 0.00014833318005571394], ["(€", 0.00014833318005571394], [",-)", 0.00014833318005571394], ["spoedeis", 0.00014833318005571394], ["aard", 0.00014833318005571394], ["in_NUMwater", 0.00014833318005571394], ["mening", 0.00014833318005571394], ["geschikt", 0.00014833318005571394], ["weiger", 0.00014833318005571394], ["jurisdictiegeschil", 0.00014833318005571394], ["alsnog", 0.00014833318005571394], ["verpleg", 0.00014833318005571394], ["overheidsweg", 0.00014833318005571394], ["verleng", 0.00014833318005571394], ["faillissement", 0.00014833318005571394], ["estro", 0.00014833318005571394], ["parti", 0.00014833318005571394], ["beschouwd", 0.00014833318005571394], ["middelenklag", 0.00014833318005571394], ["vijfjar", 0.00014833318005571394], ["leeftijd", 0.00014833318005571394], ["ouder", 0.00014833318005571394], ["zus", 0.00014833318005571394], ["oma", 0.00014833318005571394], ["georgi", 0.00014833318005571394], ["binnengekom", 0.00014833318005571394], ["asielaanvrag", 0.00014833318005571394], ["hierteg", 0.00014833318005571394], ["bestuursrechtsprak", 0.00014833318005571394], ["nr", 0.00014833318005571394], ["v_NUM", 0.00014833318005571394], ["bevestigd", 0.00014833318005571394], ["twaalfd", 0.00014833318005571394], ["bestrijdt", 0.00014833318005571394], ["verscheid", 0.00014833318005571394], ["voert", 0.00014833318005571394], ["uiter", 0.00014833318005571394], ["welk", 0.00014833318005571394], ["firma", 0.00014833318005571394], ["gepasseerd", 0.00014833318005571394], ["hoogt", 0.00014833318005571394], ["aangesprok", 0.00014833318005571394], ["lang", 0.00014833318005571394], ["overschred", 0.00014833318005571394], ["sind", 0.00014833318005571394], ["gehanteerd", 0.00014833318005571394], ["vervall", 0.00014833318005571394], ["opdrachtgever", 0.00014833318005571394], ["reclam", 0.00014833318005571394], ["ontdek", 0.00014833318005571394], ["opdrachtnemer", 0.00014833318005571394], ["helemal", 0.00014833318005571394], ["stand", 0.00014833318005571394], ["gekom", 0.00014833318005571394], ["scenario", 0.00014833318005571394], ["middelonderdel", 0.00014833318005571394], ["vertrouwensbeginsel", 0.00014833318005571394], ["var", 0.00014833318005571394], ["wuo", 0.00014833318005571394], ["oordeelsvorm", 0.00014833318005571394], ["exceptie", 0.00014833318005571394], ["constateert", 0.00014833318005571394], ["daarin", 0.00014833318005571394], ["handelszak", 0.00014833318005571394], ["kantonzak", 0.00014833318005571394], ["vrijwaringsprocedur", 0.00014833318005571394], ["explot", 0.00014833318005571394], ["anticipatie", 0.00014833318005571394], ["kwad", 0.00014833318005571394], ["trouw", 0.00014833318005571394], ["ondernemingsvermog", 0.00014833318005571394], ["vorm", 0.00014833318005571394], ["nietig", 0.00014833318005571394], ["appeldagvaard", 0.00014833318005571394], ["oproep", 0.00014833318005571394], ["beoordelingssystematiek", 0.00014833318005571394], ["vormverzuim", 0.00014833318005571394], ["kempenban", 0.00014833318005571394], ["gruis", 0.00014833318005571394], ["samenvat", 0.00014833318005571394], ["goeder", 0.00014833318005571394], ["gunningscriterium", 0.00014833318005571394], ["beoordelingsaspect", 0.00014833318005571394], ["ontvankelijkheidsverwer", 0.00014833318005571394], ["gts", 0.00014833318005571394], ["dynamisch", 0.00014833318005571394], ["effici", 0.00014833318005571394], ["ntieparameter", 0.00014833318005571394], ["slaagt", 0.00014833318005571394], ["‑", 0.00014833318005571394], ["factorbesluit", 0.00014833318005571394], ["verniet", 0.00014833318005571394], ["lijdt", 0.00014833318005571394], ["methodebesluit", 0.00014833318005571394], ["vreemd", 0.00014833318005571394], ["wacc", 0.00014833318005571394], ["gebrekk", 0.00014833318005571394], ["busines", 0.00014833318005571394], ["cycles", 0.00014833318005571394], ["ntie", 0.00014833318005571394], ["acm", 0.00014833318005571394], ["opdrag", 0.00014833318005571394], ["herstell", 0.00014833318005571394], ["strafkamer", 0.00014833318005571394], ["noord", 0.00014833318005571394], ["holland", 0.00014833318005571394], ["gevang", 0.00014833318005571394], ["nisstraf", 0.00014833318005571394], ["overtred", 0.00014833318005571394], ["diefstal", 0.00014833318005571394], ["voorafgegan", 0.00014833318005571394], ["vergezeld", 0.00014833318005571394], ["oogmerk", 0.00014833318005571394], ["bereid", 0.00014833318005571394], ["gemak", 0.00014833318005571394], ["terwijl", 0.00014833318005571394], ["verenigd", 0.00014833318005571394], ["schuldig", 0.00014833318005571394], ["toegang", 0.00014833318005571394], ["misdrijf", 0.00014833318005571394], ["verschaft", 0.00014833318005571394], ["val", 0.00014833318005571394], ["kostum", 0.00014833318005571394], ["alvoren", 0.00014833318005571394], ["man", 0.00014833318005571394], ["dwangsom", 0.00014833318005571394], ["invorder", 0.00014833318005571394], ["gemeentewet", 0.00014833318005571394], ["hond", 0.00014833318005571394], ["houder", 0.00014833318005571394], ["hondenbelast", 0.00014833318005571394], ["gehev", 0.00014833318005571394], ["minimumeis", 0.00014833318005571394], ["uitnod", 0.00014833318005571394], ["bedrijfsgroep", 0.00014833318005571394], ["zwembad", 0.00014833318005571394], ["hoofdstraf", 0.00014833318005571394], ["oplegg", 0.00014833318005571394], ["verzuim", 0.00014833318005571394], ["aa", 0.00014833318005571394], ["gva", 0.00014833318005571394], ["commissaris", 0.00014833318005571394], ["on", 0.00014833318005571394], ["sandton", 0.00014833318005571394], ["cas", 0.00014833318005571394], ["eerder", 0.00014833318005571394], ["verdenk", 0.00014833318005571394], ["beklaagd", 0.00014833318005571394], ["taakstraf", 0.00014833318005571394], ["behandelverplicht", 0.00014833318005571394], ["xs_NUMall", 0.00014833318005571394], ["een", 0.00014833318005571394], ["cijfer", 0.00014833318005571394], ["beperkt", 0.00014833318005571394], ["kennisnem", 0.00014833318005571394], ["meregaard", 0.00014833318005571394], ["almer", 0.00014833318005571394], ["beoordelingsliss", 0.00014833318005571394], ["weigeringsgrond", 0.00014833318005571394], ["olw", 0.00014833318005571394], ["beginseluitsprak", 0.00014833318005571394], ["grondrecht", 0.00014833318005571394], ["toetsingskader", 0.00014833318005571394], ["betrouw", 0.00014833318005571394], ["partneralimentatie", 0.00014833318005571394], ["fa", 0.00014833318005571394], ["onteigeningsvonnis", 0.00014833318005571394], ["vervroegd", 0.00014833318005571394], ["onteigen", 0.00014833318005571394], ["uitgesprok", 0.00014833318005571394], ["kadastral", 0.00014833318005571394], ["bekend", 0.00014833318005571394], ["kadasternummer", 0.00014833318005571394], ["informatiebeschik", 0.00014833318005571394], ["pand", 0.00014833318005571394], ["bv", 0.00014833318005571394], ["resultat", 0.00014833318005571394], ["ontvangt", 0.00014833318005571394], ["ioaw", 0.00014833318005571394], ["ontbreekt", 0.00014833318005571394], ["causal", 0.00014833318005571394], ["onbevoegdverklar", 0.00014833318005571394], ["rechtsbijstand", 0.00014833318005571394], ["naamswijz", 0.00014833318005571394], ["navorderingsaanslag", 0.00014833318005571394], ["gezag", 0.00014833318005571394], ["beroepschrift", 0.00014833318005571394], ["procespartij", 0.00014833318005571394], ["deskundigenonderzoek", 0.00014833318005571394], ["verschill", 0.00014833318005571394], ["zichzelf", 0.00014833318005571394], ["respecter", 0.00014833318005571394], ["huidig", 0.00014833318005571394], ["partner", 0.00014833318005571394], ["wil", 0.00014833318005571394], ["vader", 0.00014833318005571394], ["verhuiz", 0.00014833318005571394], ["cura", 0.00014833318005571394], ["ç", 0.00014833318005571394], ["ao", 0.00014833318005571394], ["iederen", 0.00014833318005571394], ["onzeker", 0.00014833318005571394], ["ondertoezichtstell", 0.00014833318005571394], ["bewijslever", 0.00014833318005571394], ["opheff", 0.00014833318005571394], ["zekerheidstell", 0.00014833318005571394], ["wijzigingsgrond", 0.00014833318005571394], ["hoofzak", 0.00014833318005571394], ["taxatierapport", 0.00014833318005571394], ["makelar", 0.00014833318005571394], ["taxateur", 0.00014833318005571394], ["procedures", 0.00014833318005571394], ["zijden", 0.00014833318005571394], ["gedaagde_NUM", 0.00014833318005571394], ["vermeerder", 0.00014833318005571394], ["nesia", 0.00014833318005571394], ["wwz", 0.00014833318005571394], ["gegan", 0.00014833318005571394], ["motiveringseis", 0.00014833318005571394], ["verdel", 0.00014833318005571394], ["pensioenaansprak", 0.00014833318005571394], ["bindend", 0.00014833318005571394], ["afsprak", 0.00014833318005571394], ["aegon", 0.00014833318005571394], ["abp", 0.00014833318005571394], ["pensioen", 0.00014833318005571394], ["mercer", 0.00014833318005571394], ["afgewek", 0.00014833318005571394], ["huwelijkssluit", 0.00014833318005571394], ["pensioenrecht", 0.00014833318005571394], ["scheiding", 0.00014833318005571394], ["verev", 0.00014833318005571394], ["echtelied", 0.00014833318005571394], ["helft", 0.00014833318005571394], ["ouderdomspensioen", 0.00014833318005571394], ["huwelijk", 0.00014833318005571394], ["ontstat", 0.00014833318005571394], ["rechtsweg", 0.00014833318005571394], ["definitief", 0.00014833318005571394], ["brandsticht", 0.00014833318005571394], ["interventie", 0.00014833318005571394], ["fairstar", 0.00014833318005571394], ["dockwis", 0.00014833318005571394], ["bewijsmiddelen", 0.00014833318005571394], ["daarteg", 0.00014833318005571394], ["slotoverweg", 0.00014833318005571394], ["dertiend", 0.00014833318005571394], ["uitlat", 0.00014833318005571394], ["zijd", 0.00014833318005571394], ["gezien", 0.00014833318005571394], ["verkapt", 0.00014833318005571394], ["gevolgd", 0.00014833318005571394], ["aanvaard", 0.00014833318005571394], ["oplevert", 0.00014833318005571394], ["hor", 0.00014833318005571394], ["wederhor", 0.00014833318005571394], ["procesord", 0.00014833318005571394], ["gereageerd", 0.00014833318005571394], ["reactie", 0.00014833318005571394], ["blijkt", 0.00014833318005571394], ["afdoend", 0.00014833318005571394], ["geschad", 0.00014833318005571394], ["processuel", 0.00014833318005571394], ["anderzijd", 0.00014833318005571394], ["kreg", 0.00014833318005571394], ["weerwoord", 0.00014833318005571394], ["beslot", 0.00014833318005571394], ["besprok", 0.00014833318005571394], ["rechtsgeld", 0.00014833318005571394], ["opgezegd", 0.00014833318005571394], ["ab", 0.00014833318005571394], ["av", 0.00014833318005571394], ["zie", 0.00014833318005571394], ["kon", 0.00014833318005571394], ["leidrad", 0.00014833318005571394], ["direct", 0.00014833318005571394], ["mocht", 0.00014833318005571394], ["beoordeeld", 0.00014833318005571394], ["hand", 0.00014833318005571394], ["kredietovereenkomst", 0.00014833318005571394], ["laatst", 0.00014833318005571394], ["mee", 0.00014833318005571394], ["kredietverlener", 0.00014833318005571394], ["gebruikmak", 0.00014833318005571394], ["concret", 0.00014833318005571394], ["maatstav", 0.00014833318005571394], ["billijk", 0.00014833318005571394], ["zwaarweg", 0.00014833318005571394], ["bestat", 0.00014833318005571394], ["leidt", 0.00014833318005571394], ["bank", 0.00014833318005571394], ["geldt", 0.00014833318005571394], ["daarenbov", 0.00014833318005571394], ["maatschapp", 0.00014833318005571394], ["zowel", 0.00014833318005571394], ["cli", 0.00014833318005571394], ["nten", 0.00014833318005571394], ["contractuel", 0.00014833318005571394], ["opzicht", 0.00014833318005571394], ["ongeschrev", 0.00014833318005571394], ["betaamt", 0.00014833318005571394], ["hangt", 0.00014833318005571394], ["kredietopzegg", 0.00014833318005571394], ["elk", 0.00014833318005571394], ["overeenstemm", 0.00014833318005571394], ["proportionaliteit", 0.00014833318005571394], ["subsidiariteit", 0.00014833318005571394], ["gezichtspunt", 0.00014833318005571394], ["zojuist", 0.00014833318005571394], ["vermeld", 0.00014833318005571394], ["bankrelatie", 0.00014833318005571394], ["circa", 0.00014833318005571394], ["onderhoud", 0.00014833318005571394], ["vaststat", 0.00014833318005571394], ["nimmer", 0.00014833318005571394], ["afgegev", 0.00014833318005571394], ["getred", 0.00014833318005571394], ["altijd", 0.00014833318005571394], ["volled", 0.00014833318005571394], ["nagekom", 0.00014833318005571394], ["krediet", 0.00014833318005571394], ["vertoond", 0.00014833318005571394], ["courantverhoud", 0.00014833318005571394], ["creditsaldo", 0.00014833318005571394], ["onvoldo", 0.00014833318005571394], ["weersprok", 0.00014833318005571394], ["dekking", 0.00014833318005571394], ["bestond", 0.00014833318005571394], ["kredietaanpass", 0.00014833318005571394], ["oktober", 0.00014833318005571394], ["gekreg", 0.00014833318005571394], ["doordat", 0.00014833318005571394], ["gerelateerd", 0.00014833318005571394], ["openstaand", 0.00014833318005571394], ["ruim", 0.00014833318005571394], ["debiteurenstand", 0.00014833318005571394], ["borgtocht", 0.00014833318005571394], ["voornem", 0.00014833318005571394], ["bijvoorbeeld", 0.00014833318005571394], ["waarschuw", 0.00014833318005571394], ["dar", 0.00014833318005571394], ["nooit", 0.00014833318005571394], ["beher", 0.00014833318005571394], ["doorverwez", 0.00014833318005571394], ["gericht", 0.00014833318005571394], ["zwar", 0.00014833318005571394], ["wer", 0.00014833318005571394], ["begeleid", 0.00014833318005571394], ["aanwijz", 0.00014833318005571394], ["geeft", 0.00014833318005571394], ["uitsprek", 0.00014833318005571394], ["meld", 0.00014833318005571394], ["frequent", 0.00014833318005571394], ["forensisch", 0.00014833318005571394], ["polikliniek", 0.00014833318005571394], ["max", 0.00014833318005571394], ["ernst", 0.00014833318005571394], ["soortgelijk", 0.00014833318005571394], ["plaats", 0.00014833318005571394], ["teneind", 0.00014833318005571394], ["psychisch", 0.00014833318005571394], ["problematiek", 0.00014833318005571394], ["doorbrekingsgrond", 0.00014833318005571394], ["h", 0.00014833318005571394], ["j", 0.00014833318005571394], ["wanbeleid", 0.00014833318005571394], ["verantwoord", 0.00014833318005571394], ["samenstell", 0.00014833318005571394]];

/***/ },
/* 307 */
/***/ function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	exports.default = [["procesverlop", 1.635966642834469], ["_NUM", 0.716597592849154], ["_ART", 0.6421343364611857], ["en", 0.493652823225416], ["van", 0.37513461236090057], ["ontstan", 0.3668279542777806], ["lop", 0.3668279542777806], ["geding", 0.3592629620949392], ["in", 0.12578653668724543], ["feit", 0.09137323891431978], ["beroep", 0.08662657715253694], ["hoger", 0.08647824397248123], ["procesgang", 0.0744632563879684], ["eerst", 0.03456163095298135], ["aanleg", 0.034413297772925636], ["vaststaand", 0.033968298232758494], ["verder", 0.02076664520779995], ["_PUNCT", 0.01957997976735424], ["voorgeschiedenis", 0.01646498298618425], ["processtuk", 0.013794985745181397], ["procedur", 0.007119992642674269], [",", 0.005043328121894274], ["zak", 0.0034116631412814207], ["op", 0.003114996781169993], ["-", 0.002966663601114279], ["is", 0.0028183304210585647], ["heeft", 0.002669997241002851], ["tot", 0.0020766645207799953], [")", 0.0020766645207799953], ["vor", 0.0020766645207799953], ["inleid", 0.0019283313407242812], ["met", 0.0019283313407242812], ["/", 0.0019283313407242812], ["(", 0.0017799981606685673], ["teg", 0.0017799981606685673], ["[", 0.0016316649806128534], ["uitsprak", 0.0016316649806128534], ["hierna", 0.0014833318005571395], ["verzoek", 0.0014833318005571395], ["bij", 0.0014833318005571395], ["dor", 0.0014833318005571395], ["producties", 0.0014833318005571395], ["cassatie", 0.0013349986205014254], ["ingesteld", 0.0013349986205014254], ["j", 0.0013349986205014254], ["nar", 0.0013349986205014254], ["accountantskamer", 0.0013349986205014254], ["te", 0.0011866654404457115], ["onderzoek", 0.0011866654404457115], ["die", 0.0011866654404457115], ["bovenvermeld", 0.0011866654404457115], ["klacht", 0.0011866654404457115], ["ingedi", 0.0011866654404457115], ["volgend", 0.0010383322603899976], ["dat", 0.0010383322603899976], ["verdacht", 0.0010383322603899976], ["incident", 0.0008899990803342836], ["tussenkomst", 0.0008899990803342836], ["brief", 0.0008899990803342836], ["uit", 0.0008899990803342836], ["onder", 0.0008899990803342836], ["aan", 0.0008899990803342836], ["augustus", 0.0008899990803342836], ["aanleid", 0.0008899990803342836], ["]", 0.0008899990803342836], ["nam", 0.0008899990803342836], ["verwijz", 0.0008899990803342836], ["stuk", 0.0008899990803342836], ["conclusie", 0.0008899990803342836], ["weergav", 0.0007416659002785698], ["feitelijk", 0.0007416659002785698], ["instantie", 0.0007416659002785698], ["hof", 0.0007416659002785698], ["rechtbank", 0.0007416659002785698], ["januari", 0.0007416659002785698], ["blijkt", 0.0007416659002785698], ["termijn", 0.0007416659002785698], ["zijn", 0.0007416659002785698], ["ter", 0.0007416659002785698], ["appellant", 0.0007416659002785698], ["redelijk", 0.0005933327202228558], ["verded", 0.0005933327202228558], ["open", 0.0005933327202228558], ["dez", 0.0005933327202228558], ["mer", 0.0005933327202228558], ["als", 0.0005933327202228558], ["artikel", 0.0005933327202228558], ["word", 0.0005933327202228558], ["gerek", 0.0005933327202228558], ["vonnis", 0.0005933327202228558], ["doen", 0.0005933327202228558], ["terechtzit", 0.0005933327202228558], ["vervolgen", 0.0005933327202228558], ["oordel", 0.0005933327202228558], ["juli", 0.0005933327202228558], ["] (", 0.0005933327202228558], ["colleg", 0.0005933327202228558], ["],", 0.0004449995401671418], ["])", 0.0004449995401671418], ["all", 0.0004449995401671418], ["hoofdzak", 0.0004449995401671418], ["eiser", 0.0004449995401671418], ["verweerder", 0.0004449995401671418], ["verlop", 0.0004449995401671418], ["rechter", 0.0004449995401671418], ["mei", 0.0004449995401671418], ["sprak", 0.0004449995401671418], ["twee", 0.0004449995401671418], ["december", 0.0004449995401671418], ["aangehoud", 0.0004449995401671418], ["moet", 0.0004449995401671418], ["stat", 0.0004449995401671418], ["verricht", 0.0004449995401671418], ["opmak", 0.0004449995401671418], ["ministerie", 0.0004449995401671418], ["gewez", 0.0004449995401671418], ["waartoe", 0.0004449995401671418], ["omvang", 0.0004449995401671418], ["opsporingsonderzoek", 0.0004449995401671418], ["juni", 0.0004449995401671418], ["verbal", 0.0004449995401671418], ["niet", 0.0004449995401671418], ["bepaald", 0.0004449995401671418], ["september", 0.0004449995401671418], ["verband", 0.0004449995401671418], ["klager", 0.0004449995401671418], ["november", 0.0004449995401671418], ["binnengekom", 0.0004449995401671418], ["gegev", 0.0004449995401671418], ["dagvaard", 0.0004449995401671418], ["antwoord", 0.0004449995401671418], ["repliek", 0.0004449995401671418], ["dupliek", 0.0004449995401671418], ["bestred", 0.0004449995401671418], ["beschik", 0.0004449995401671418], ["iprocesverlop", 0.0002966663601114279], ["geschil", 0.0002966663601114279], ["genoemd", 0.0002966663601114279], ["herzien", 0.0002966663601114279], ["vrijwaringszak", 0.0002966663601114279], ["stelt", 0.0002966663601114279], ["wat", 0.0002966663601114279], ["februari", 0.0002966663601114279], ["besluit", 0.0002966663601114279], ["hiervor", 0.0002966663601114279], ["gesteld", 0.0002966663601114279], ["geschond", 0.0002966663601114279], ["ten", 0.0002966663601114279], ["gelegd", 0.0002966663601114279], ["vanaf", 0.0002966663601114279], ["commissaris", 0.0002966663601114279], ["jar", 0.0002966663601114279], ["datum", 0.0002966663601114279], ["er", 0.0002966663601114279], ["dan", 0.0002966663601114279], ["strafvervolg", 0.0002966663601114279], ["bedoeld", 0.0002966663601114279], ["evrm", 0.0002966663601114279], ["onderhav", 0.0002966663601114279], ["waarop", 0.0002966663601114279], ["april", 0.0002966663601114279], ["na", 0.0002966663601114279], ["aanvang", 0.0002966663601114279], ["dur", 0.0002966663601114279], ["kan", 0.0002966663601114279], ["alsmed", 0.0002966663601114279], ["of", 0.0002966663601114279], ["behandeld", 0.0002966663601114279], ["siod", 0.0002966663601114279], ["’", 0.0002966663601114279], ["werd", 0.0002966663601114279], ["person", 0.0002966663601114279], ["vier", 0.0002966663601114279], ["aangemerkt", 0.0002966663601114279], ["gedagvaard", 0.0002966663601114279], ["om", 0.0002966663601114279], ["med", 0.0002966663601114279], ["onderzoekswens", 0.0002966663601114279], ["geschorst", 0.0002966663601114279], ["teneind", 0.0002966663601114279], ["aanvull", 0.0002966663601114279], ["process", 0.0002966663601114279], ["lat", 0.0002966663601114279], ["zestien", 0.0002966663601114279], ["getuig", 0.0002966663601114279], ["hor", 0.0002966663601114279], ["gelet", 0.0002966663601114279], ["omstand", 0.0002966663601114279], ["kader", 0.0002966663601114279], ["partij", 0.0002966663601114279], ["eiseres", 0.0002966663601114279], ["hij", 0.0002966663601114279], ["registeraccountant", 0.0002966663601114279], ["woonplat", 0.0002966663601114279], ["betrok", 0.0002966663601114279], ["omtrent", 0.0002966663601114279], ["ra", 0.0002966663601114279], ["zover", 0.0002966663601114279], ["nog", 0.0002966663601114279], ["belang", 0.0002966663601114279], ["fas", 0.0002966663601114279], ["akt", 0.0002966663601114279], ["houdend", 0.0002966663601114279], ["uitlat", 0.0002966663601114279], ["rigas", 0.0002966663601114279], ["marthedes", 0.0002966663601114279], ["tussenarrest", 0.00014833318005571394], ["deskund", 0.00014833318005571394], ["oogart", 0.00014833318005571394], [", (", 0.00014833318005571394], ["benoemd", 0.00014833318005571394], ["verzet", 0.00014833318005571394], ["beid", 0.00014833318005571394], ["procedures", 0.00014833318005571394], ["afzonder", 0.00014833318005571394], ["navolg", 0.00014833318005571394], ["betreft", 0.00014833318005571394], ["vast", 0.00014833318005571394], ["“", 0.00014833318005571394], ["betrek", 0.00014833318005571394], ["f", 0.00014833318005571394], ["verwer", 0.00014833318005571394], ["grondslag", 0.00014833318005571394], ["inval", 0.00014833318005571394], ["leiding", 0.00014833318005571394], ["bijna", 0.00014833318005571394], ["drie", 0.00014833318005571394], ["geduurd", 0.00014833318005571394], ["terwijl", 0.00014833318005571394], ["geweest", 0.00014833318005571394], ["period", 0.00014833318005571394], ["inactiviteit", 0.00014833318005571394], ["tuss", 0.00014833318005571394], ["aanhoud", 0.00014833318005571394], ["eind", 0.00014833318005571394], ["zitting", 0.00014833318005571394], ["subsidiair", 0.00014833318005571394], ["zich", 0.00014833318005571394], ["standpunt", 0.00014833318005571394], ["aanving", 0.00014833318005571394], ["welk", 0.00014833318005571394], ["was", 0.00014833318005571394], ["voorop", 0.00014833318005571394], ["elk", 0.00014833318005571394], ["recht", 0.00014833318005571394], ["behandel", 0.00014833318005571394], ["binn", 0.00014833318005571394], ["waarborg", 0.00014833318005571394], ["strekt", 0.00014833318005571394], ["toe", 0.00014833318005571394], ["voorkom", 0.00014833318005571394], ["langer", 0.00014833318005571394], ["dreiging", 0.00014833318005571394], ["zou", 0.00014833318005571394], ["lev", 0.00014833318005571394], ["vangt", 0.00014833318005571394], ["moment", 0.00014833318005571394], ["vanweg", 0.00014833318005571394], ["jegen", 0.00014833318005571394], ["handel", 0.00014833318005571394], ["waaruit", 0.00014833318005571394], ["opgemaakt", 0.00014833318005571394], ["redelijkerwijs", 0.00014833318005571394], ["kunn", 0.00014833318005571394], ["ernstig", 0.00014833318005571394], ["voornem", 0.00014833318005571394], ["had", 0.00014833318005571394], ["stell", 0.00014833318005571394], ["geval", 0.00014833318005571394], ["dag", 0.00014833318005571394], ["dus", 0.00014833318005571394], ["tijdsverlop", 0.00014833318005571394], ["strafzak", 0.00014833318005571394], ["afhank", 0.00014833318005571394], ["ingewikkeld", 0.00014833318005571394], ["gelijktijd", 0.00014833318005571394], ["berecht", 0.00014833318005571394], ["medeverdacht", 0.00014833318005571394], ["invloed", 0.00014833318005571394], ["raadsman", 0.00014833318005571394], ["kanword", 0.00014833318005571394], ["leid", 0.00014833318005571394], ["vertrag", 0.00014833318005571394], ["afdoen", 0.00014833318005571394], ["wijz", 0.00014833318005571394], ["bevoegd", 0.00014833318005571394], ["autoriteit", 0.00014833318005571394], ["mat", 0.00014833318005571394], ["voortvar", 0.00014833318005571394], ["betracht", 0.00014833318005571394], ["geblek", 0.00014833318005571394], ["social", 0.00014833318005571394], ["inlicht", 0.00014833318005571394], ["opsporingsdienst", 0.00014833318005571394], ["geslot", 0.00014833318005571394], ["officier", 0.00014833318005571394], ["justitie", 0.00014833318005571394], ["bericht", 0.00014833318005571394], ["procesdossier", 0.00014833318005571394], ["gezien", 0.00014833318005571394], ["akkoord", 0.00014833318005571394], ["getek", 0.00014833318005571394], ["beslan", 0.00014833318005571394], ["ordner", 0.00014833318005571394], ["bevat", 0.00014833318005571394], ["pagina", 0.00014833318005571394], ["s", 0.00014833318005571394], ["blijken", 0.00014833318005571394], ["neg", 0.00014833318005571394], ["natur", 0.00014833318005571394], ["rechtsperson", 0.00014833318005571394], ["verschijn", 0.00014833318005571394], ["meervoud", 0.00014833318005571394], ["strafkamer", 0.00014833318005571394], ["hervat", 0.00014833318005571394], ["onbepaald", 0.00014833318005571394], ["tijd", 0.00014833318005571394], ["rechtercommissaris", 0.00014833318005571394], ["gehoord", 0.00014833318005571394], ["oktober", 0.00014833318005571394], ["slot", 0.00014833318005571394], ["inhoud", 0.00014833318005571394], ["[]", 0.00014833318005571394], ["tenlastelegg", 0.00014833318005571394], ["weergegev", 0.00014833318005571394], ["bijzonder", 0.00014833318005571394], ["aanwez", 0.00014833318005571394], ["overschrijd", 0.00014833318005571394], ["rechtvaard", 0.00014833318005571394], ["wet", 0.00014833318005571394], ["complexiteit", 0.00014833318005571394], ["strafbar", 0.00014833318005571394], ["aantal", 0.00014833318005571394], ["- (", 0.00014833318005571394], ["respectiev", 0.00014833318005571394], ["aldus", 0.00014833318005571394], ["vormverzuim", 0.00014833318005571394], ["vorenstaand", 0.00014833318005571394], ["gen", 0.00014833318005571394], ["_NUMa", 0.00014833318005571394], ["sv", 0.00014833318005571394], ["mist", 0.00014833318005571394], ["derhalv", 0.00014833318005571394], ["toepass", 0.00014833318005571394], ["zodat", 0.00014833318005571394], ["ontvankelijkverklar", 0.00014833318005571394], ["vervolg", 0.00014833318005571394], ["red", 0.00014833318005571394], ["daarom", 0.00014833318005571394], ["ord", 0.00014833318005571394], ["verwez", 0.00014833318005571394], ["daarover", 0.00014833318005571394], ["vermeld", 0.00014833318005571394], ["tussenvonnis", 0.00014833318005571394], ["daarin", 0.00014833318005571394], ["comparitie", 0.00014833318005571394], ["plaatsgevond", 0.00014833318005571394], ["gedaagd", 0.00014833318005571394], ["geleg", 0.00014833318005571394], ["verschen", 0.00014833318005571394], ["daarna", 0.00014833318005571394], ["namen", 0.00014833318005571394], ["gevraagd", 0.00014833318005571394], ["waarvan", 0.00014833318005571394], ["heden", 0.00014833318005571394], ["navorder", 0.00014833318005571394], ["aanslag", 0.00014833318005571394], ["vergrijpboetes", 0.00014833318005571394], ["opgelegd", 0.00014833318005571394], ["vrijwar", 0.00014833318005571394], ["destukkenendeprocesgang", 0.00014833318005571394], ["bet", 0.00014833318005571394], ["functionel", 0.00014833318005571394], ["parket", 0.00014833318005571394], ["),", 0.00014833318005571394], ["gedan", 0.00014833318005571394], ["toekom", 0.00014833318005571394], ["waaronder", 0.00014833318005571394], ["fotokopie", 0.00014833318005571394], ["proces", 0.00014833318005571394], ["fiod", 0.00014833318005571394], ["betreff", 0.00014833318005571394], ["verzeker", 0.00014833318005571394], ["vorder", 0.00014833318005571394], ["conservatoir", 0.00014833318005571394], ["beslag", 0.00014833318005571394], ["bre", 0.00014833318005571394], ["she", 0.00014833318005571394], ["afm", 0.00014833318005571394], ["dezelfd", 0.00014833318005571394], ["dit", 0.00014833318005571394], ["geregistreerd", 0.00014833318005571394], ["zaaknummer", 0.00014833318005571394], ["dusverr", 0.00014833318005571394], ["deprocesgang", 0.00014833318005571394], ["wettelijk", 0.00014833318005571394], ["bestur", 0.00014833318005571394], ["²", 0.00014833318005571394], ["primair", 0.00014833318005571394], ["aanvrag", 0.00014833318005571394], ["verzoekster", 0.00014833318005571394], ["noem", 0.00014833318005571394], ["krachten", 0.00014833318005571394], ["vreemdelingenwet", 0.00014833318005571394], ["vw", 0.00014833318005571394], ["bepal", 0.00014833318005571394], ["har", 0.00014833318005571394], ["uitzet", 0.00014833318005571394], ["achterweg", 0.00014833318005571394], ["blijft", 0.00014833318005571394], ["ingewilligd", 0.00014833318005571394], ["uitstel", 0.00014833318005571394], ["verleend", 0.00014833318005571394], ["opnam", 0.00014833318005571394], ["ingang", 0.00014833318005571394], ["uiter", 0.00014833318005571394], ["conventie", 0.00014833318005571394], ["reconventie", 0.00014833318005571394], ["vijftien", 0.00014833318005571394], ["zev", 0.00014833318005571394], ["‘", 0.00014833318005571394], ["zijden", 0.00014833318005571394], ["geconcludeerd", 0.00014833318005571394], ["teven", 0.00014833318005571394], ["wijzig", 0.00014833318005571394], ["eis", 0.00014833318005571394], ["antwoordakt", 0.00014833318005571394], ["hebb", 0.00014833318005571394], ["verzocht", 0.00014833318005571394]];

/***/ },
/* 308 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _data = __webpack_require__(309);

	var _data2 = _interopRequireDefault(_data);

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _underscore = __webpack_require__(210);

	var _underscore2 = _interopRequireDefault(_underscore);

	var _figs = __webpack_require__(224);

	var _figs2 = _interopRequireDefault(_figs);

	var _PercentageBar = __webpack_require__(294);

	var _PercentageBar2 = _interopRequireDefault(_PercentageBar);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	//const LineChart = require('../../../../../charts/bar-chart/BarChart.jsx');

	var FigureWordCount = function (_React$Component) {
	    _inherits(FigureWordCount, _React$Component);

	    function FigureWordCount() {
	        _classCallCheck(this, FigureWordCount);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(FigureWordCount).apply(this, arguments));
	    }

	    _createClass(FigureWordCount, [{
	        key: 'render',
	        value: function render() {
	            //<LineChart labelY='²log frequency' sourceHref={data.url} data={data.data}/>
	            return _react2.default.createElement(
	                'figure',
	                { className: 'chart', id: _figs2.default.figTitleWordCount.id },
	                _react2.default.createElement(
	                    'a',
	                    { href: _data2.default.url },
	                    'Data source'
	                ),
	                _react2.default.createElement(
	                    'table',
	                    { className: ' table' },
	                    _react2.default.createElement(
	                        'thead',
	                        null,
	                        _react2.default.createElement(
	                            'tr',
	                            null,
	                            _react2.default.createElement(
	                                'th',
	                                null,
	                                'Number of words'
	                            ),
	                            _react2.default.createElement(
	                                'th',
	                                null,
	                                'Relative frequency'
	                            ),
	                            _react2.default.createElement(
	                                'th',
	                                null,
	                                'Absolute frequency'
	                            )
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'tbody',
	                        null,
	                        _underscore2.default.map(_data2.default.data, function (obj) {
	                            var percentageTxt = (100 * obj.titleCount / _data2.default.totalCount).toFixed(2) + "%";
	                            return _react2.default.createElement(
	                                'tr',
	                                { key: obj.wordCount },
	                                _react2.default.createElement(
	                                    'th',
	                                    { className: 'nr' },
	                                    obj.wordCount
	                                ),
	                                _react2.default.createElement(
	                                    'td',
	                                    null,
	                                    _react2.default.createElement(_PercentageBar2.default, { percentage: percentageTxt, text: percentageTxt }),
	                                    _react2.default.createElement(
	                                        'span',
	                                        { className: 'perc-text' },
	                                        percentageTxt
	                                    )
	                                ),
	                                _react2.default.createElement(
	                                    'td',
	                                    null,
	                                    _react2.default.createElement(
	                                        'span',
	                                        { className: 'perc-text' },
	                                        obj.titleCount
	                                    )
	                                )
	                            );
	                        })
	                    )
	                ),
	                _react2.default.createElement(
	                    'figcaption',
	                    null,
	                    _react2.default.createElement(
	                        'span',
	                        { className: 'figure-number' },
	                        'Fig ',
	                        _figs2.default.tfidf.num,
	                        '.'
	                    ),
	                    ' Word count in titles, excluding numbering.'
	                )
	            );
	        }
	    }]);

	    return FigureWordCount;
	}(_react2.default.Component);

	exports.default = FigureWordCount;

/***/ },
/* 309 */
/***/ function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	exports.default = {
	    "url": "https://rechtspraak.cloudant.com/docs/_design/stats/_view/word_count_for_title_elements?group_level=1&stale=ok",
	    "data": [{ "wordCount": 1, "titleCount": 74646 }, { "wordCount": 2, "titleCount": 51409 }, {
	        "wordCount": 3,
	        "titleCount": 67914
	    }, { "wordCount": 4, "titleCount": 23850 }, { "wordCount": 5, "titleCount": 28579 }, {
	        "wordCount": 6,
	        "titleCount": 33224
	    }, { "wordCount": 7, "titleCount": 11728 }, { "wordCount": 8, "titleCount": 5950 }, {
	        "wordCount": 9,
	        "titleCount": 4947
	    }, { "wordCount": 10, "titleCount": 4661 }],
	    "totalCount": 311368
	};

/***/ },
/* 310 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var InferringDocumentStructureIntroduction = function (_Component) {
	    _inherits(InferringDocumentStructureIntroduction, _Component);

	    function InferringDocumentStructureIntroduction() {
	        _classCallCheck(this, InferringDocumentStructureIntroduction);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(InferringDocumentStructureIntroduction).apply(this, arguments));
	    }

	    _createClass(InferringDocumentStructureIntroduction, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                'div',
	                null,
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'After we have labeled our sequence of text elements, we wish to infer some sort of document structure. We approach this problem as parsing a sequence of terminals with a Stochastic Context Free Grammar (SCFG) into an abstract parse tree, represent the document section hierarchy.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'In this chapter, we introduce SCFGs and the Cocke–Younger–Kasami algorithm (CYK), a deterministic algorithm for finding the best parse tree in quadratic space and time. We conclude with an evaluation of the results.'
	                )
	            );
	        }
	    }]);

	    return InferringDocumentStructureIntroduction;
	}(_react.Component);

	exports.default = InferringDocumentStructureIntroduction;

/***/ },
/* 311 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _ChapterSectionContent = __webpack_require__(256);

	var _ChapterSectionContent2 = _interopRequireDefault(_ChapterSectionContent);

	var _chapters = __webpack_require__(217);

	var _chapters2 = _interopRequireDefault(_chapters);

	var _sections = __webpack_require__(252);

	var _sections2 = _interopRequireDefault(_sections);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var Methods = function (_Component) {
	    _inherits(Methods, _Component);

	    function Methods() {
	        _classCallCheck(this, Methods);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Methods).apply(this, arguments));
	    }

	    _createClass(Methods, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(_ChapterSectionContent2.default, _extends({}, this.props, { sections: _sections2.default.inOrder }));
	        }
	    }], [{
	        key: 'title',
	        value: function title() {
	            return _chapters2.default.documentStructure.title;
	        }

	        //noinspection JSUnusedGlobalSymbols

	    }, {
	        key: 'getSections',
	        value: function getSections() {
	            return _sections2.default;
	        }
	    }]);

	    return Methods;
	}(_react.Component);

	exports.default = Methods;

/***/ },
/* 312 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var Evaluation = function (_Component) {
	    _inherits(Evaluation, _Component);

	    function Evaluation() {
	        _classCallCheck(this, Evaluation);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Evaluation).apply(this, arguments));
	    }

	    _createClass(Evaluation, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                'div',
	                null,
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    '- minimal edit distance? -'
	                )
	            );
	        }
	    }]);

	    return Evaluation;
	}(_react.Component);

	exports.default = Evaluation;

/***/ },
/* 313 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _Math = __webpack_require__(260);

	var _Math2 = _interopRequireDefault(_Math);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var ContextFreeGrammars = function (_Component) {
	    _inherits(ContextFreeGrammars, _Component);

	    function ContextFreeGrammars() {
	        _classCallCheck(this, ContextFreeGrammars);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(ContextFreeGrammars).apply(this, arguments));
	    }

	    _createClass(ContextFreeGrammars, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                'div',
	                null,
	                _react2.default.createElement(
	                    'section',
	                    null,
	                    _react2.default.createElement(
	                        'h3',
	                        null,
	                        'Stochastic Context Free Grammars'
	                    ),
	                    _react2.default.createElement(
	                        'p',
	                        null,
	                        'Context Free Grammars (CFGs) are grammars'
	                    )
	                )
	            );
	        }
	    }]);

	    return ContextFreeGrammars;
	}(_react.Component);

	exports.default = ContextFreeGrammars;

/***/ },
/* 314 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _Source = __webpack_require__(220);

	var _Source2 = _interopRequireDefault(_Source);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var AdditionalEnrichment = function (_Component) {
	    _inherits(AdditionalEnrichment, _Component);

	    function AdditionalEnrichment() {
	        _classCallCheck(this, AdditionalEnrichment);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(AdditionalEnrichment).apply(this, arguments));
	    }

	    _createClass(AdditionalEnrichment, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                'div',
	                null,
	                'TODO describe name parser; figure of Structured Data Testing Tool https://developers.google.com/structured-data/testing-tool/'
	            );
	        }
	    }]);

	    return AdditionalEnrichment;
	}(_react.Component);

	exports.default = AdditionalEnrichment;

/***/ },
/* 315 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _references = __webpack_require__(221);

	var _references2 = _interopRequireDefault(_references);

	var _bib = __webpack_require__(211);

	var _bib2 = _interopRequireDefault(_bib);

	var _Math = __webpack_require__(260);

	var _Math2 = _interopRequireDefault(_Math);

	var _FigRef = __webpack_require__(245);

	var _FigRef2 = _interopRequireDefault(_FigRef);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var parseFig = {
	    id: 'fig-parsing-triangle',
	    num: 1
	};
	var exampleSentence = 'fish people fish tanks'.split(" ");

	var CYK = function (_Component) {
	    _inherits(CYK, _Component);

	    function CYK() {
	        _classCallCheck(this, CYK);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(CYK).apply(this, arguments));
	    }

	    _createClass(CYK, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                'div',
	                null,
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'The Cocke–Younger–Kasami (CYK) algorithm is an algorithm for parsing Context Free Grammars that was separately discovered by ',
	                    _references2.default.cite(_bib2.default.kasami1965efficient),
	                    ', ',
	                    _references2.default.cite(_bib2.default.younger1967recognition),
	                    ' and ',
	                    _references2.default.cite(_bib2.default.cocke1969programming),
	                    '. The algorithm has worst case complexity of ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: '\\Theta (n^3\\cdot \\left | G \\right |)' }),
	                    ', where ',
	                    _react2.default.createElement(_Math2.default, { l: 'n' }),
	                    ' is the length of the input string and ',
	                    _react2.default.createElement(_Math2.default, { l: '\\left | G \\right |' }),
	                    ' is the size of the grammar.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'The standard version of the CYK algorithm is defined for ordinary context free grammars that are given in Chomsky normal form (CNF), but is is easy to extend the algorithm to work on stochastic grammars with unary rules, as we do in this section. Note that any CFG may be transformed into an equivalent grammar in Chomsky normal form, and this also holds for stochastic CFGs (',
	                    _references2.default.cite(_bib2.default.huang1971stochastic),
	                    '). Also note that converting a grammar to CNF is not without cost: the increase in grammar size is ',
	                    _react2.default.createElement(_Math2.default, {
	                        l: '\\mathrm O (\\left | G \\right |^2)' }),
	                    ' for the best algorithm, but the increase is linear if we use a variation of the algorithm that works on grammars in binary normal form (2NF). (',
	                    _references2.default.cite(_bib2.default.lange2009cnf),
	                    '.)'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'The algorithm is a bottom-up parsing algorithm. The algorith considers every substring from length ',
	                    _react2.default.createElement(_Math2.default, { l: '1' }),
	                    ' to ',
	                    _react2.default.createElement(_Math2.default, { l: 'n' }),
	                    ', and tries to assign a non-terminal label to the substring along with a score. For substrings of length ',
	                    _react2.default.createElement(_Math2.default, { l: '1' }),
	                    ' (individual words), we use the terminal rules in the grammar. For substrings of length ',
	                    _react2.default.createElement(_Math2.default, { l: 'l>1' }),
	                    ' (word sequences), we apply the production rules to every possible combination of two substrings of length ',
	                    _react2.default.createElement(_Math2.default, { l: 'l-1' }),
	                    ' (remember that CNF mandates that all production rules have 2 non-terminals). Every time we apply a rule, we multiply the probability attached to the rule and the probabilities of the constituent substrings.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'This describes the basic version of CYK. In addition to this, we also allow unary rules in our grammar of the form ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'A → B'
	                    ),
	                    ', where ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'A'
	                    ),
	                    ' and ',
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'B'
	                    ),
	                    ' are nonterminals. Extension of the algorith is simple: at the end of every substring assignment, we apply unary rules, and add the result if the rule produces a non-terminal that does not exist with at least that score in the table. We repeat until the cell does not change anymore.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'A visual example of the result table can be found in ',
	                    _react2.default.createElement(_FigRef2.default, { fig: parseFig }),
	                    '.'
	                ),
	                _react2.default.createElement(
	                    'figure',
	                    { id: parseFig.id },
	                    _react2.default.createElement(
	                        'div',
	                        { className: 'table-container' },
	                        _react2.default.createElement(
	                            'table',
	                            null,
	                            _react2.default.createElement(
	                                'tbody',
	                                null,
	                                [0, 1, 2, 3].map(function (rowNum) {
	                                    return _react2.default.createElement(
	                                        'tr',
	                                        { key: rowNum },
	                                        [0, 1, 2, 3].map(function (colNum) {
	                                            return _react2.default.createElement(
	                                                'td',
	                                                { key: rowNum + "," + colNum },
	                                                colNum >= rowNum ? _react2.default.createElement(
	                                                    'div',
	                                                    { className: 'cyk-cell-content' },
	                                                    _react2.default.createElement(
	                                                        'ol',
	                                                        null,
	                                                        CYK.getItems(rowNum, colNum)
	                                                    ),
	                                                    colNum == rowNum ? _react2.default.createElement(
	                                                        'code',
	                                                        { className: 'word' },
	                                                        exampleSentence[colNum]
	                                                    ) : ""
	                                                ) : ""
	                                            );
	                                        })
	                                    );
	                                })
	                            )
	                        )
	                    ),
	                    _react2.default.createElement(
	                        'figcaption',
	                        null,
	                        _react2.default.createElement(
	                            'span',
	                            { className: 'figure-number' },
	                            'Fig ',
	                            parseFig.num,
	                            '.'
	                        ),
	                        ' An example parse chart for the sentence "fish people fish tanks", with the following grammar:',
	                        _react2.default.createElement(
	                            'div',
	                            null,
	                            _react2.default.createElement(
	                                'pre',
	                                { style: { display: 'inline-block' } },
	                                'S  → NP VP  (90%)',
	                                _react2.default.createElement('br', null),
	                                'S  → VP     (10%)',
	                                _react2.default.createElement('br', null),
	                                'VP → V NP   (50%)',
	                                _react2.default.createElement('br', null),
	                                'VP → V      (10%)',
	                                _react2.default.createElement('br', null),
	                                'NP → NP NP  (10%)',
	                                _react2.default.createElement('br', null),
	                                'NP → N      (70%)',
	                                _react2.default.createElement('br', null),
	                                _react2.default.createElement('br', null),
	                                'N  → fish   (20%)',
	                                _react2.default.createElement('br', null),
	                                'N  → people (50%)',
	                                _react2.default.createElement('br', null),
	                                'N  → tanks  (20%)',
	                                _react2.default.createElement('br', null),
	                                'V  → people (10%)',
	                                _react2.default.createElement('br', null),
	                                'V  → fish   (60%)',
	                                _react2.default.createElement('br', null),
	                                'V  → tanks  (30%)'
	                            )
	                        ),
	                        'The top of the triangle represents the substring ',
	                        _react2.default.createElement(_Math2.default, { l: '1' }),
	                        ' to ',
	                        _react2.default.createElement(_Math2.default, { l: '4' }),
	                        ', i.e. the entire sentence. We can derive ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'S'
	                        ),
	                        ' by combining the substring from ',
	                        _react2.default.createElement(_Math2.default, { l: '1' }),
	                        ' to ',
	                        _react2.default.createElement(_Math2.default, { l: '2' }),
	                        ' (',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'fish people'
	                        ),
	                        ') and the substring from ',
	                        _react2.default.createElement(_Math2.default, { l: '3' }),
	                        ' to ',
	                        _react2.default.createElement(_Math2.default, { l: '4' }),
	                        ' (',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'fish tanks'
	                        ),
	                        ') using the rule ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'S → NP VP'
	                        ),
	                        '. The nodes that make up a parse tree to ',
	                        _react2.default.createElement(
	                            'code',
	                            null,
	                            'S'
	                        ),
	                        ' are marked.'
	                    )
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'The implementation of this algorithm can be found on Github.'
	                )
	            );
	        }
	    }], [{
	        key: 'getItems',
	        value: function getItems(rowNum, colNum) {
	            if (rowNum == 0) {
	                if (colNum == 0) return [_react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'N (20%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'V (60%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        _react2.default.createElement(
	                            'strong',
	                            null,
	                            'NP (14%)'
	                        )
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'VP (6%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'S (0.6%)'
	                    )
	                )];
	                if (colNum == 1) return [_react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        _react2.default.createElement(
	                            'strong',
	                            null,
	                            'NP (0.49%)'
	                        )
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'VP (10.5%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'S (1.05%)'
	                    )
	                )];
	                if (colNum == 2) return [_react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'NP (0.0069%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'VP (0.147%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'S (0.09%)'
	                    )
	                )];
	                if (colNum == 3) return [_react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'VP (0.002%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'NP (0.00001%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        _react2.default.createElement(
	                            'strong',
	                            null,
	                            'S (0.019%)'
	                        )
	                    )
	                )];
	            } else if (rowNum == 1) {
	                if (colNum == 1) return [_react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'N (50%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'V (10%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        _react2.default.createElement(
	                            'strong',
	                            null,
	                            'NP (35%)'
	                        )
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'VP (1%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'S (0.1%)'
	                    )
	                )];
	                if (colNum == 2) return [_react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'NP (0.49%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'VP (0.7%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'S (1.89%)'
	                    )
	                )];
	                if (colNum == 3) return [_react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'NP (0.007%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'VP (0.010%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'S (1.323%)'
	                    )
	                )];
	            } else if (rowNum == 2 && colNum == 2) {
	                return [_react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'N (20%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        _react2.default.createElement(
	                            'strong',
	                            null,
	                            'V (60%)'
	                        )
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'NP (14%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'VP (6%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'S (0.6%)'
	                    )
	                )];
	            } else if (rowNum == 2 && colNum == 3) {
	                return [_react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'NP (0.196%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        _react2.default.createElement(
	                            'strong',
	                            null,
	                            'VP (4.2%)'
	                        )
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'S (0.42%)'
	                    )
	                )];
	            } else if (rowNum == 3 && colNum == 3) {
	                return [_react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'N (20%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'V (30%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        _react2.default.createElement(
	                            'strong',
	                            null,
	                            'NP (14%)'
	                        )
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'VP (3%)'
	                    )
	                ), _react2.default.createElement(
	                    'li',
	                    null,
	                    _react2.default.createElement(
	                        'code',
	                        null,
	                        'S (0.3%)'
	                    )
	                )];
	            }
	            return [];
	        }
	    }]);

	    return CYK;
	}(_react.Component);

	exports.default = CYK;

/***/ },
/* 316 */
/***/ function(module, exports) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	var disseminationSections = {
	    additionalEnrichment: {
	        id: 'additional-enrichment',
	        title: 'Additional enrichment'
	    }
	};

	disseminationSections.inOrder = [disseminationSections.additionalEnrichment];

	exports.default = disseminationSections;

/***/ },
/* 317 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _IntroRechtspraakNl = __webpack_require__(219);

	var _IntroRechtspraakNl2 = _interopRequireDefault(_IntroRechtspraakNl);

	var _RechtspraakNlMarkup = __webpack_require__(247);

	var _RechtspraakNlMarkup2 = _interopRequireDefault(_RechtspraakNlMarkup);

	var _Importing = __webpack_require__(248);

	var _Importing2 = _interopRequireDefault(_Importing);

	var _Chapter = __webpack_require__(318);

	var _Chapter2 = _interopRequireDefault(_Chapter);

	var _sections = __webpack_require__(246);

	var _sections2 = _interopRequireDefault(_sections);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var Introduction = function (_Component) {
	    _inherits(Introduction, _Component);

	    function Introduction() {
	        _classCallCheck(this, Introduction);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Introduction).apply(this, arguments));
	    }

	    _createClass(Introduction, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                _Chapter2.default,
	                { path: this.props.path, title: Introduction.title(), sections: _sections2.default.inOrder },
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'In this thesis, we explore the problem of automatically assigning a section hierarchy to sparsely marked up documents in the Dutch case law repository of ',
	                    _react2.default.createElement(
	                        'a',
	                        { href: 'http://www.rechtspraak.nl/' },
	                        'Rechtspraak.nl'
	                    ),
	                    '.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'Having a section hierarchy is obviously useful for rendering the documents to human users: it allows us to display a table of contents and to style section titles. But it is also interesting for more advanced text mining applications such as topic modeling and information extraction. For example, we may want to represent some metadata in a machine-readable manner (e.g., the judgment: \'claimant won\', \'claimant lost\', etc.). It would then be useful to get some context about the section hierarchy.'
	                ),
	                _react2.default.createElement(
	                    'p',
	                    null,
	                    'In this chapter, we provide an introduction to the Rechtspraak.nl data set that we use, with an ',
	                    _react2.default.createElement(
	                        'a',
	                        {
	                            href: '#markup' },
	                        'introduction to the case law XML markup'
	                    ),
	                    '. In the final section of this chapter, we present the Java library that contains all the work that we have undertaken in this thesis, which effectively is a pipeline for  enriching Dutch case law markup. We make some remarks on importing and tokenization of Dutch case law documents, followed by chapters on element tagging and parsing a section tree, which both require more complicated machinery than tokenization.'
	                )
	            );
	        }
	    }], [{
	        key: 'title',
	        value: function title() {
	            return "Introduction";
	        }

	        //noinspection JSUnusedGlobalSymbols

	    }, {
	        key: 'getSections',
	        value: function getSections() {
	            return _sections2.default;
	        }
	    }]);

	    return Introduction;
	}(_react.Component);

	exports.default = Introduction;

/***/ },
/* 318 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _getSectionComponent = __webpack_require__(218);

	var _getSectionComponent2 = _interopRequireDefault(_getSectionComponent);

	var _chapters = __webpack_require__(217);

	var _chapters2 = _interopRequireDefault(_chapters);

	var _ToC = __webpack_require__(216);

	var _ToC2 = _interopRequireDefault(_ToC);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

	/**
	 * A component for a chapter page on our website.
	 */

	var Chapter = function (_React$Component) {
	    _inherits(Chapter, _React$Component);

	    function Chapter() {
	        _classCallCheck(this, Chapter);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Chapter).apply(this, arguments));
	    }

	    _createClass(Chapter, [{
	        key: 'render',
	        value: function render() {
	            var _this2 = this;

	            var subsections = this.props.sections.map(function (section) {
	                var SectionContent = (0, _getSectionComponent2.default)(section.id);
	                return _react2.default.createElement(
	                    'section',
	                    { key: section.id,
	                        id: section.id },
	                    _react2.default.createElement(
	                        'h3',
	                        { className: 'title' },
	                        _react2.default.createElement('a', { className: 'link-up', href: '#' }),
	                        section.title
	                    ),
	                    _react2.default.createElement(SectionContent, _this2.props)
	                );
	            });

	            return _react2.default.createElement(
	                'div',
	                null,
	                _react2.default.createElement(
	                    'h2',
	                    null,
	                    'Table of Contents'
	                ),
	                _react2.default.createElement(_ToC2.default, _extends({ showHome: true }, this.props)),
	                _react2.default.createElement(
	                    'section',
	                    null,
	                    _react2.default.createElement(
	                        'h2',
	                        { className: 'title' },
	                        _react2.default.createElement('a', { className: 'link-up', href: '#' }),
	                        this.props.title
	                    ),
	                    this.props.children,
	                    subsections
	                ),
	                this.linkToNextChapter(this.props.title)
	            );
	        }
	    }, {
	        key: 'linkToNextChapter',
	        value: function linkToNextChapter(title) {
	            var found = false;
	            var _iteratorNormalCompletion = true;
	            var _didIteratorError = false;
	            var _iteratorError = undefined;

	            try {
	                for (var _iterator = _chapters2.default.inOrder[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
	                    var chapter = _step.value;

	                    if (found) {
	                        return _react2.default.createElement(
	                            'p',
	                            { className: 'link-to-next-chapter' },
	                            'Next chapter: ',
	                            _react2.default.createElement(
	                                'a',
	                                { href: _chapters2.default.pathTo(this.props.path, chapter) },
	                                chapter.title
	                            )
	                        );
	                    }
	                    if (chapter.title == title) {
	                        found = true;
	                    }
	                }
	            } catch (err) {
	                _didIteratorError = true;
	                _iteratorError = err;
	            } finally {
	                try {
	                    if (!_iteratorNormalCompletion && _iterator.return) {
	                        _iterator.return();
	                    }
	                } finally {
	                    if (_didIteratorError) {
	                        throw _iteratorError;
	                    }
	                }
	            }

	            return "";
	        }
	    }]);

	    return Chapter;
	}(_react2.default.Component);

	exports.default = Chapter;

	Chapter.propTypes = {
	    title: _react2.default.PropTypes.string.isRequired,
	    sections: _react2.default.PropTypes.arrayOf(_react2.default.PropTypes.shape({
	        component: _react2.default.PropTypes.func.isRequired
	    })).isRequired
	};

	module.exports = Chapter;

/***/ },
/* 319 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _Chapter = __webpack_require__(318);

	var _Chapter2 = _interopRequireDefault(_Chapter);

	var _Methods = __webpack_require__(255);

	var _Methods2 = _interopRequireDefault(_Methods);

	var _Results = __webpack_require__(253);

	var _Results2 = _interopRequireDefault(_Results);

	var _DeterministicTagger = __webpack_require__(284);

	var _DeterministicTagger2 = _interopRequireDefault(_DeterministicTagger);

	var _CRF = __webpack_require__(285);

	var _CRF2 = _interopRequireDefault(_CRF);

	var _FeatureSelection = __webpack_require__(290);

	var _FeatureSelection2 = _interopRequireDefault(_FeatureSelection);

	var _chapters = __webpack_require__(217);

	var _chapters2 = _interopRequireDefault(_chapters);

	var _sections = __webpack_require__(249);

	var _sections2 = _interopRequireDefault(_sections);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable

	// import Introduction from './Introduction/Introduction';


	var Tagging = function (_Component) {
	    _inherits(Tagging, _Component);

	    function Tagging() {
	        _classCallCheck(this, Tagging);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Tagging).apply(this, arguments));
	    }

	    _createClass(Tagging, [{
	        key: 'render',
	        value: function render() {
	            var relativeToRoot = this.props.path.match(/\//g).slice(1).map(function (_) {
	                return "../";
	            }).join("");
	            return _react2.default.createElement(_Chapter2.default, {
	                path: this.props.path,
	                title: Tagging.title(),
	                sections: _sections2.default.inOrder });
	        }
	    }], [{
	        key: 'chapterInfo',
	        value: function chapterInfo() {
	            return _chapters2.default.tagging;
	        }
	    }, {
	        key: 'title',
	        value: function title() {
	            return Tagging.chapterInfo().title;
	        }

	        //noinspection JSUnusedGlobalSymbols

	    }, {
	        key: 'getSections',
	        value: function getSections() {
	            return _sections2.default;
	        }
	    }]);

	    return Tagging;
	}(_react.Component);

	exports.default = Tagging;

/***/ },
/* 320 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _Chapter = __webpack_require__(318);

	var _Chapter2 = _interopRequireDefault(_Chapter);

	var _chapters = __webpack_require__(217);

	var _chapters2 = _interopRequireDefault(_chapters);

	var _sections = __webpack_require__(251);

	var _sections2 = _interopRequireDefault(_sections);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var Inferring = function (_Component) {
	    _inherits(Inferring, _Component);

	    function Inferring() {
	        _classCallCheck(this, Inferring);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Inferring).apply(this, arguments));
	    }

	    _createClass(Inferring, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(_Chapter2.default, {
	                path: this.props.path,
	                title: Inferring.title(),
	                sections: _sections2.default.inOrder });
	        }
	    }], [{
	        key: 'title',
	        value: function title() {
	            return _chapters2.default.documentStructure.title;
	        }

	        //noinspection JSUnusedGlobalSymbols

	    }, {
	        key: 'getSections',
	        value: function getSections() {
	            return _sections2.default;
	        }
	    }]);

	    return Inferring;
	}(_react.Component);

	exports.default = Inferring;

/***/ },
/* 321 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _react = __webpack_require__(4);

	var _react2 = _interopRequireDefault(_react);

	var _Chapter = __webpack_require__(318);

	var _Chapter2 = _interopRequireDefault(_Chapter);

	var _chapters = __webpack_require__(217);

	var _chapters2 = _interopRequireDefault(_chapters);

	var _sections = __webpack_require__(316);

	var _sections2 = _interopRequireDefault(_sections);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

	function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; } //noinspection JSUnresolvedVariable


	var Dissemination = function (_Component) {
	    _inherits(Dissemination, _Component);

	    function Dissemination() {
	        _classCallCheck(this, Dissemination);

	        return _possibleConstructorReturn(this, Object.getPrototypeOf(Dissemination).apply(this, arguments));
	    }

	    _createClass(Dissemination, [{
	        key: 'render',
	        value: function render() {
	            return _react2.default.createElement(
	                _Chapter2.default,
	                { path: this.props.path, title: Dissemination.title(), sections: _sections2.default.inOrder },
	                'In addition to a document transformation pipeline, we present the enriched set of XML documents in a CouchDB database, available at ',
	                _react2.default.createElement(
	                    'a',
	                    {
	                        href: 'https://rechtspraak.cloudant.com/docs/' },
	                    'rechtspraak.cloudant.com/docs/'
	                ),
	                '. We also provide the enriched dataset as a collection of HTML pages, indexed for full text search. The web site is available at ',
	                _react2.default.createElement('a', { href: 'https://rechtspraak.lawreader.nl/' })
	            );
	        }
	    }], [{
	        key: 'title',
	        value: function title() {
	            return _chapters2.default.dissemination.title;
	        }

	        //noinspection JSUnusedGlobalSymbols

	    }, {
	        key: 'getSections',
	        value: function getSections() {
	            return _sections2.default;
	        }
	    }]);

	    return Dissemination;
	}(_react.Component);

	exports.default = Dissemination;

/***/ }
/******/ ])
});
;